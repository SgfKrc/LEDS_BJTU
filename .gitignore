# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
.venv-test/
.venv-edge/
# RPC 版 llama-cpp-python 实验环境（GGML_RPC=ON，见 local_docs/CORE-LLAMA-PC-RPC-01-*.md）
.venv-llama-rpc/
venv/
env/

# Node
node_modules/
dist/
.vite/

plsql.md
SMTP.md

# IDE
.vscode/
.idea/
.claude/
.reasonix/
.codex/

# Local-only documentation
local_docs/

# OS
.DS_Store
Thumbs.db

# Logs
logs/*.log
logs/*.err
logs/*.out
# logs/ 下的一次性调试脚本与临时产物（保留 .gitkeep 与运行日志本身）
logs/*.py
logs/*.json
logs/sd15/
packaging/*.log
src/import_time.log
android/.kotlin/

# Runtime data
chat_history/

# Root-level stray npm lockfile (empty, generated by accident)
/package-lock.json

# Environment
# ⚠️ `.env.bak*` 必须忽略 —— 备份**含真实密钥**（QLH_DB_PASSWORD / QLH_CLUSTER_SECRET /
#    QLH_SMTP_PASSWORD）。2026-09-20 实测：`.env.bak-20260920` 曾以 `??` 出现在
#    `git status` 中，一次 `git add -A` 就会把密钥提交并推送出去。
.env
.env.local
.env.docagent
.env.bak
.env.bak-*
.env.*.bak
node_config.json
node_config.json.tmp
.gitignore_bak
runtime/

# Model weights (downloaded separately)
models/qwen-1_8b-chat/
models/deepseek-*/
models/sd15-*/
models/qwen3-*/
models/gemma4-*/
models/Qwen-*/
.hf-cache/
models/.cache/
!models/README.md

# Large binaries
models/*.gguf
models/**/*.gguf
models/*.sha256
models_pc/*.7z
models_android/*.7z
models_pc.7z
models_android.7z

# Packaging build artifacts
build/
packaging/dist/
packaging/build/
packaging/linux/*.deb
packaging/__pycache__/
# 发布签名私钥（UP-N2 可信发布）：只存在于发布者本机，绝不进入仓库或构建产物
packaging/.signing-keys/

# Packaging virtual environments (CPU + CUDA, must remain separate)
packaging/.venv-packaging/
.venv-packaging/
.venv-packaging-cuda/

# Temp / to-delete
_to_delete/
test-results/
sd15-offline-smoke/

# Local archive for retired files (kept on disk, ignored by git)
_archive/

# Reasonix local tool config
reasonix.toml
tests/simulation/results/
.venv-tui/
control/qlh-control.sqlite3*

# Retired docs (moved out of docs/)
docs_to_delete/
models/qwen3-4b/
.venv-gemma4-native/
.venv-gemma4-pipeline/
.venv-qwen3-sidecar/
models/qwen3-4b-gguf/
models/qwen3-vl-4b-instruct/
models/qwen3-vl-4b-instruct-gguf/
models/qwen3-5-2b/
models/qwenseek-2b/
models/.qlh-*/

# QW3 runtime logs
logs/*.log.*
models/gemma4-native/*.gguf

# Root-level runtime / diagnostic logs (never committed)
/runtime-*.log
/runtime-*.stdout.log
/runtime-*.stderr.log
/surface-ssh*.log
# Harness 实验模型与下载中间产物（模型文件不进 git）
models/qwen2.5-0.5b-instruct/
models/minicpm4-0.5b/
models/distilqwen25-ds3-0324-7b/
models/.*.qlh-import-*/

# frontend_cybergothic: abandoned (kept locally, no longer maintained)
frontend_cybergothic/

# ★ 2026-09-20：两个**桥接器**改为「工作区独立目录」，不再作为主仓子模块。
# 各自是独立 git 仓（本地 clone 各自维护）；主仓只保留本地目录，不入库。
#   tools/reasonix-codex-bridge —— Reasonix/Codex 桥（MCP server + ACP transport）
#   tools/dsh-codex-bridge —— 孪生项目（DSH 侧，vendor 了桥接器运行时与测试）
tools/reasonix-codex-bridge/
tools/dsh-codex-bridge/

# ============================================================================
# ★ 2026-09-20 主仓瘦身（用户裁定）：**开发/实验工具与运行时产物不入库，只留本地**
# ============================================================================
# 整目录裁掉：
#   tools/            —— 开发工具（docagent / toolbox / model_tools 补丁 / modelscope 下载等）
#                        其中 tools/docagent、tools/toolbox 原为子模块，指针已剔除
#   packages/         —— spawnledger（原子模块，指针已剔除）
#   logs/             —— 运行时日志产物（代码里的 /api/logs/* 路由不受影响）
#   docs/agent_tool/  —— 文档审计工具；**注意**其 model-artifacts/remote-model-pins-*.json
#                        是运行时数据，已随迁到 fixtures/model-artifacts/（仍入库）
tools/
packages/
logs/
docs/agent_tool/

# scripts/ —— 只裁「一次性实验/基准」，**保留**基础设施与常用工具。
# 策略：先整体忽略，再白名单放回下列必需项（被 src/ import 的、被测试 import 的、
# 以及上手/流水线要用到的工具）。
scripts/*
!scripts/model_tools/
!scripts/model_tools.py
!scripts/android_validation.py
!scripts/edge_preflight.py
!scripts/experiment_core/
!scripts/experiment_gemma_judge_real.py
!scripts/llama_pc_rpc.py
!scripts/llama_relay_probe.py
!scripts/llama_relay_xframe_probe.py
!scripts/llama_rpc_sim.py
!scripts/setup_envs.py
!scripts/setup_test_env.py
!scripts/setup_qwen3_sidecar_env.py
!scripts/setup_gemma4_pipeline_env.py
!scripts/init-submodules.bat
!scripts/init-submodules.sh
!scripts/run_test_channels.py
!scripts/run_simulation.py
!scripts/startup_matrix.py
!scripts/test_classification_report.py
!scripts/edge_sim_setup.sh
!scripts/cut_layers.py
!scripts/quantize_model.py
!scripts/build_llama_quantize.py
!scripts/inspect_repo_state.py
!scripts/export_model_catalog.py
!scripts/qlh_log.py
!scripts/cluster_node_preflight.py
!scripts/ipv6_dual_stack_gate.py
!scripts/check_doc_links.py
!scripts/find_broken_links.py
!scripts/fix_broken_links.py
!scripts/check_readme_l10n.py
# P1/P2 统一实验驱动族（2026-09-21）：被文档与测试引用，必须入库
!scripts/relay_experiment.py
!scripts/relay_diag_head_norm.py
!scripts/relay_cut_plan.py
!scripts/relay_mid_service.py
!scripts/relay_bitwise_probe.py

# 根目录：过时日志 / 备份（2026-09-20 剔除）
# ⚠️ `bjtu.bat` / `bjtu.sh` **保留** —— 它们仍是活跃入口：
#    `tests/test_qlh_cli.py` 与 `tests/test_test_environment_scripts.py` 直接读它们，
#    `docs/TUI使用指南.md` 等多处引用。原先以为它是「旧项目名遗留」是**误判**。
runtime-master*.log
surface-ssh*.log
.gitignore_bak


