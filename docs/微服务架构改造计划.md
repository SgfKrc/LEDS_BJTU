# QLH Python 后端微服务架构改造计划

> **状态**：执行中（阶段 0 完成；阶段 2：2.1-2.4 完成——端点 96/96 可达、静态托管就绪、TUI T1-T8 完成，待 2.5 退役 / 2.6 验收；**阶段 2 起点已完成**：1.2d task_graph 执行段迁 inference-svc（828be95）+ scheduler 流水线段设计变更不复制（2bc471c）+ scheduler-svc HTTP 壳 :8020（§4.2 透传契约，33 cluster + 3 device 端点）；阶段 1 主体完成（1.1-1.7 全部提交；1.8 本地验收完成——pytest 1171 全绿、冷启动 0.04s/3.85s、/api/health 网关先行实测、MONOLITH 回退通过，双节点实测与本地双引擎推理待真实环境）；阶段 3：3.1 完成 + 3.2 会话/对话域完成（2026-08-04，12 端点 + 25 契约测试，旧 JSON 数据兼容硬验收通过），其余控制面域待迁移；阶段 4 未开始。**2026-08-03 修订：并行共存策略生效**——微服务独立实现、现有 Python 后端源码与运行拓扑冻结不动、2.5/3.3 删除动作统一移至清理阶段，见 §1.4 / §5.0 / §5.1）
>
> **更新日期**：2026-08-03
>
> **适用范围**：Python 后端微服务化（scheduler-svc / inference-svc / gateway / legacy-control）的阶段划分、端点迁移与验收口径
>
> **关联文档**：[Python后端冷启动优化方案](Python后端冷启动优化方案.md) · [模块接口说明](模块接口说明.md) · [整体架构](整体架构.md) · [混合分布式推理体系规划](混合分布式推理体系规划.md) · [总体下一步计划](总体下一步计划.md)
>
> **一句话目标**：把"冷启动优化后依旧庞大"的 Python 单体（`api_server.py` 291KB + `scheduler.py` 496KB，共 20 模块 ≈1.5MB）按控制面/数据面拆分为微服务；**大模型推理核心保留 Python**，**控制面迁移 TypeScript（NestJS + Fastify）**，**调度器保留 Python 独立成服务**。

---

## 一、背景与动机

### 1.1 现状问题

冷启动优化方案（2026-07-11 创建）已落地：保留 sklearn 依赖治理、`.pyc` 预编译、导入面治理、PyInstaller 打包等。但单体架构的**结构性**问题没有消失：

| # | 问题 | 证据 |
|---|------|------|
| 1 | **任何变更都要重启整个进程，重启代价 = 模型加载**（12s 量级，其中 model_module 子树 9.7s） | `import_time.log`：api_server 累计 12.1s；transformers 6.06s、torch 1.97s、sklearn 1.51s |
| 2 | **从节点被迫加载全部代码**。从节点与主节点跑同一套 `api_server.py`（`config.py:350` 的 `QLH_NODE_ROLE` 区分），实际只需要推理数据面（model_module 层段 + tcp_comm 客户端），却 import 了 FastAPI、scheduler、全部 110 个端点 | `api_server.py:7579-7587` 唯一入口 `uvicorn api_server:app` |
| 3 | **跨模块运行时循环依赖**：`api_server.py` 顶层 import scheduler（:48），scheduler 20+ 处函数内 `import api_server as _api` 反访问 `_api.model_manager`（:3461、:8254 等） | scheduler 靠"延迟 import"规避导入环，进程内耦合无法独立演进 |
| 4 | **config.py 是全局可变状态**：scheduler 在运行时写回 `cfg.NODE_ID`（`scheduler.py:127-140`），其他模块函数内重新 `import config as cfg` 感知变化，无显式变更通道 | `config.py:40/403 ↔ node_config.py:221` 函数内双向延迟引用 |
| 5 | **控制面技术栈单一**：会话 CRUD、日志管理、审查投票、邮件通知、设备画像等纯 IO/状态逻辑用 Python 承载，没有获得推理能力，却共同承担冷启动与内存成本 | api_server 端点分布：会话 8 个、日志 12 个、审查 8 个、设置 2 个 |

### 1.2 目标

1. **推理数据面保留 Python**：model_module / llama_engine / paged_kv_cache / island_engine / external_provider / speculative / diffusion 及流水线执行段，独立为 `inference-svc`。
2. **调度器保留 Python 独立成服务**：scheduler 与数据面深度耦合（运行时调用 `model_manager.forward_layers`、张量序列化、KV 缓存生命周期、TCP 主从协议），自身冷启动仅 3.2ms 不是瓶颈，迁移新语言成本极高收益低 → 独立为 `scheduler-svc`。
3. **控制面迁移 TypeScript**：对外 API 网关（110 端点壳 + SSE + 静态前端）→ `api-gateway`（NestJS + Fastify）；会话/日志/审查/通知/设备画像/模型注册/任务图控制面 → `control-svc`（TS）。
4. **冷启动与部署收益**：主节点用户感知可用时间从 12s+ 降到 <1s（网关先行、模型异步加载）；从节点启动到注册从 12s+ 降到 ~2s（不再 import fastapi/scheduler）；服务常驻、崩溃自拉起，模型加载只发生在推理进程内且不随网关重启。

### 1.3 非目标（明确不做）

- 不重写推理核心（量化、KV 缓存、层前向、投机解码算法语义不变）。
- 不改变 TCP 主从协议对外行为（tcp_comm 帧格式、MessageType、HMAC 认证、心跳语义）。
- 不改变前端 / Android / TUI 的 API 契约（`BASE=/api`、SSE 格式、错误 `detail`/`request_id` 结构在阶段 2 保持兼容）。
- 不做容器化（当前交付形态是 Windows 安装包 + Linux .deb + 裸机部署，无 Docker 环境）。

### 1.4 实施策略：并行共存（2026-08-03 修订）

> 本策略修订取代此前“边拆边删”的表述。**新微服务一律独立实现，不修改现有 Python 后端（api_server.py / scheduler.py 及控制面模块）的一行代码，也不改变现有启动方式**；现有后端保持为唯一可运行基线。全部微服务完成并通过验收后，才按 §5.0 标记清单在清理阶段（§5.1）统一清理。

| 规则 | 内容 |
|---|---|
| 1 复制迁移 | 数据面执行段迁 inference-svc 采用**复制**而非移动：源函数保留在 api_server.py / scheduler.py 内不动（单进程拓扑继续可用），服务侧持有等价副本；行为等价纪律不变（副本不改业务逻辑） |
| 2 新增入口 import 复用 | scheduler-svc 通过新建 `scheduler_svc_main.py` import 现有 `scheduler.py` 运行（阶段 0 的 `Scheduler(host=...)` 注入已就绪，构造时传 `InferenceClient`），scheduler.py 源码零改动 |
| 3 并行运行错开端口 | 开发期新旧拓扑同时运行时用 `QLH_*_PORT` / `QLH_SERVER_PORT` 等环境变量错开监听端口；生产切换只发生在清理阶段 |
| 4 删除动作冻结 | 阶段 2.5（FastAPI 退役）、阶段 3.3（Python 控制面清理）、`start_backend.bat` 改造等一切修改/删除现有后端的动作不再于原阶段执行，统一移至清理阶段（§5.1），当前仅保留标记（§5.0） |
| 5 桩与临时服务不动 | `scripts/dev_stubs.py`、`src/legacy_control.py` 是网关当前可运行性依赖，真实 scheduler-svc / inference-svc / control-svc 全部完成前不得移除；它们本身列入 §5.0 待退役标记 |

---

## 二、现状调研摘要（2026-08-02 复核）

### 2.1 模块职责与归属

| 模块 | 大小 | 职责 | 归属 |
|------|------|------|------|
| `model_module.py` | 98KB | 双引擎模型加载/量化/层拆分/前向/chat | **数据面 → inference-svc** |
| `llama_engine.py` | 17KB | llama.cpp GGUF 引擎封装 | **数据面 → inference-svc** |
| `paged_kv_cache.py` | 25KB | 分页 KV 缓存 | **数据面 → inference-svc** |
| `island_engine.py` | 28KB | TP 孤岛 OpenAI 端点适配 | **数据面 → inference-svc** |
| `external_provider.py` | 30KB | 外部推理路由 + 数据作用域门控 | **数据面 → inference-svc** |
| `speculative.py` | 62KB | 投机解码 draft-verify（实验） | **数据面 → inference-svc** |
| `diffusion/` | — | SD1.5 本地引擎 | **数据面 → inference-svc** |
| `task_provider.py` | 41KB | ExecutionProvider 契约 + 本地 provider | 契约跨服务；本地执行 → inference-svc |
| `scheduler.py` | 496KB | 节点/队列/分层/流水线/角色转让/备用主节点 | **控制面+执行编排 → scheduler-svc**（数据面执行段见 2.2） |
| `tcp_comm.py` | 80KB | 主从 TCP 协议 + 张量序列化 | 协议库，scheduler-svc 与 inference-svc 共用 |
| `graph_orchestrator.py` | 26KB | 最大带宽生成树 + DFS 编排（纯算法） | **scheduler-svc** |
| `model_sync.py` | 10KB | Tailnet 模型同步/hash | **scheduler-svc** |
| `config.py` | 27KB | 全局配置 | 共享库（各服务裁剪依赖面） |
| `node_config.py` | 9KB | 本地节点配置 | **scheduler-svc / inference-svc** |
| `api_server.py` | 291KB | FastAPI 110 端点 + 执行段 | 端点壳 → **api-gateway(TS)**；执行段 → **inference-svc** |
| `device_profiler.py` | 51KB | 硬件画像采集（psutil/pynvml） | **共享库，保留 Python**（scheduler-svc/inference-svc 共用；从节点为纯 Python 进程，TS 无法代采；对应 REST 端点迁网关后由 scheduler-svc 承载） |
| `db.py` | 56KB | PostgreSQL 连接池（可选降级） | **control-svc(TS)**（阶段 3 迁移；或改由网关直连） |
| `local_store.py` | 9KB | 本地 JSON 降级存储 | **control-svc(TS)**（阶段 3 迁移，数据格式兼容） |
| `model_config.py` | 14KB | ModelConfig 注册表 | **control-svc(TS)**（阶段 3 迁移） |
| `model_downloader.py` | 21KB | 模型下载引导 | **control-svc(TS)**（阶段 3 迁移） |
| `review.py` | 19KB | 审查票状态机 | **control-svc(TS)**（阶段 3 迁移） |
| `email_notifier.py` | 27KB | SMTP 告警 + IMAP 投票 | **control-svc(TS)**（阶段 3 迁移） |
| `task_graph.py` | 112KB | DAG 工作流（控制面 CRUD + 执行协调） | 控制面 → **control-svc(TS)**；执行段 → **inference-svc** |
| `task_journal.py` | 25KB | SQLite 追加日志 | **control-svc(TS)**（阶段 3 迁移） |
| `task_worker_adapter.py` | 42KB | 远程 worker 控制面 | **scheduler-svc**（与 TCP 协议绑定） |
| `task_worker_protocol.py` | 24KB | 版本化消息协议 | 共享协议库（跨语言） |
| `bootstrap.py` | 7KB | Tailscale 首连引导 | **control-svc(TS)**（阶段 3 迁移） |
| `tui_admin.py` | 70KB | 终端管理菜单（纯 urllib 调 REST） | **不动**（TS 网关契约不变，TUI 零改动） |

### 2.2 api_server.py 与 scheduler.py 内的数据面/控制面切分

- **api_server.py（≈7587 行）**：
  - 数据面（迁 inference-svc）：`run_model`(:2805)、`_execute_chat_full`(:3370)、`_execute_task_graph_chat_with_slot`(:2921)、`_execute_task_worker_stage`(:2781)、`_init_kv_cache`(:1622)、`_generate_followups_llama`(:1403)、`_execute_external_chat`(:2341)、`_run_speculative_experiment`(:2553)。
  - 控制面（迁 TS 网关）：约 110 个 REST 端点（:1668-:7532）、会话/对话管理（:1137-:1220）、日志管理（:6917-:7532）。
- **scheduler.py（≈11200 行）**：
  - 数据面执行段（迁 inference-svc，作为"推理宿主"）：`run_pipeline`/`_run_pipeline`(:9569-:10000)、`_run_master_lm_head`(:8252-:8262)、`_handle_layer_forward_locked`(:8268-:8646)、`_schedule_layer_config._load`(:7720+)、`handle_infer_forward`/`_run_inference`(:7472-:7611)、本地降级 `mgr.chat`(:10567-:10662)、流式包装(:10708-:10758)。
  - 控制面（留 scheduler-svc）：NodeState/NodeRole/NodeInfo(:236-:293)、PipelineQueue MLFQ(:395-:1025)、节点注册/心跳(:1374-:2021)、层分配算法(:2448-:3220)、task-worker 控制面(:3482-:4208)、角色转让/备用主节点(:5167-:6088)、客户端模式(:6395-:7070)、TCP 消息分发(:4230-:4589)。

### 2.3 冷启动分布（import_time.log 复核）

| 模块/子树 | 耗时 | 占比 |
|-----------|------|------|
| model_module 子树 | 9.71s | 80.2%（transformers 6.06s + torch 1.97s + sklearn 1.51s + pandas 0.41s；model_module 自身仅 358ms） |
| config（含 torch 探测） | 69ms | 0.6% |
| scheduler 自身 | 3.2ms | 0.03% |
| db（psycopg2） | 24ms | 0.2% |
| api_server 自身 | 35.6ms | 0.3% |

**结论**：冷启动成本几乎全部来自 model_module 的第三方依赖子树。控制面服务只要不 import model_module/torch/transformers，冷启动可降至亚秒级——这是拆分收益的数学基础。

---

## 三、目标架构

### 3.1 架构图

```
                           主节点 (Master)
┌──────────────────────────────────────────────────────────────────┐
│                                                                  │
│   ┌───────────────┐   HTTP :8020   ┌───────────────────────┐     │
│   │  api-gateway  │◄──────────────►│   scheduler-svc       │     │
│   │  TS :8000     │                │   Python :8020        │     │
│   │  110 端点壳    │                │   节点/队列/分层/角色   │     │
│   │  SSE 代理      │                │   TCP 主节点 :8888     │     │
│   │  静态前端      │                └──────────┬────────────┘     │
│   └───┬───────┬───┘                           │ HTTP :8010        │
│       │       │                               │ (inference client)│
│       │  HTTP :8030                           ▼                   │
│   ┌───▼───────┴───┐                ┌───────────────────────┐     │
│   │  control-svc  │                │   inference-svc       │     │
│   │  TS :8030     │                │   Python :8010        │     │
│   │  会话/日志/审查│                │   model_module/llama  │     │
│   │  画像/模型注册 │                │   KV缓存/流水线执行段   │     │
│   └───────────────┘                │   本地完整推理 chat    │     │
│                                    └───────────────────────┘     │
│                                    ▲  HTTP :8010 (client 角色)    │
└────────────────────────────────────┼─────────────────────────────┘
                                      │ TCP :8888（tcp_comm 主从协议不变）
  从节点 (Client)                      │
┌─────────────────────────────────────┴───────────────────────────┐
│   ┌──────────────────────────────────────────┐                   │
│   │  inference-svc（client 角色）              │                   │
│   │  层段加载 + 层前向 + TCP 客户端 + 设备画像   │                   │
│   │  ★ 不再 import fastapi/scheduler/网关      │                   │
│   └──────────────────────────────────────────┘                   │
└──────────────────────────────────────────────────────────────────┘
```

### 3.2 服务清单

| 服务 | 语言/框架 | 职责 | 端口 | 部署节点 |
|------|-----------|------|------|----------|
| `api-gateway` | TypeScript · NestJS + Fastify adapter | 对外 REST（110 端点契约不变）、SSE 流式代理、CORS/请求 ID、静态前端托管 | 8000 | 主节点 |
| `scheduler-svc` | Python（保留） | 节点生命周期、MLFQ 队列、层分配、角色转让、备用主节点、TCP 主从（tcp_comm）、task-worker 控制面、客户端模式连接、**设备画像采集（共享库）** | 8020（内部 HTTP）+ 8888（TCP） | 主节点 |
| `inference-svc` | Python（数据面） | 模型加载/量化/KV 缓存/前向/chat 流式、流水线执行段、外部辅助引擎、diffusion、投机解码实验 | 8010（内部 HTTP） | 主节点 + 从节点（client 角色） |
| `control-svc` | TypeScript · NestJS + Fastify adapter | 会话/日志/审查/邮件通知/模型注册与下载/任务图控制面/引导 | 8030（内部 HTTP） | 主节点 |

### 3.3 通信矩阵

| 链路 | 协议 | 说明 |
|------|------|------|
| 前端/Android/TUI → api-gateway | HTTP/SSE :8000 | 对外契约，`BASE=/api` 不变 |
| api-gateway → scheduler-svc | HTTP :8020 | 集群/队列/节点/分层只读 + 管理动作 |
| api-gateway → inference-svc | HTTP/SSE :8010 | chat/chat-stream 转发（经 scheduler 状态校验） |
| api-gateway → control-svc | HTTP :8030 | 会话/日志/审查/设置/画像/模型注册 |
| scheduler-svc → inference-svc | HTTP/SSE :8010 | 推理客户端接口：加载/前向/chat/取消 |
| scheduler-svc ↔ 从节点 inference-svc | TCP :8888 | tcp_comm 既有协议，**帧格式与 MessageType 不变** |

### 3.4 端口与环境变量规划

| 变量 | 默认 | 说明 |
|------|------|------|
| `QLH_API_PORT` | 8000 | api-gateway（对外，原 FastAPI 端口不变） |
| `QLH_SERVER_PORT` | 8888 | scheduler-svc TCP 主节点监听（不变） |
| `QLH_SCHEDULER_PORT` | 8020 | scheduler-svc 内部 HTTP |
| `QLH_INFERENCE_PORT` | 8010 | inference-svc 内部 HTTP |
| `QLH_CONTROL_PORT` | 8030 | control-svc 内部 HTTP |
| `QLH_NODE_ROLE` | master | master / client（沿用） |
| `QLH_INFERENCE_HOST` | 127.0.0.1 | scheduler 访问本机/远端 inference-svc 的地址（client 角色同机） |
| `QLH_MONOLITH` | 0 | 阶段 1 回退开关：=1 时保持现单进程启动方式 |

---

## 四、内部接口契约

> 契约即边界。阶段 1 先冻结 `inference-svc` 契约（用契约测试锁定），阶段 2 冻结 `scheduler-svc` 契约，阶段 3 冻结 `control-svc` 契约。各阶段实现方必须以契约为准，调用方不得绕过。

### 4.1 inference-svc 契约（HTTP :8010）

```http
GET  /v1/health                          # 进程存活（不含模型加载），网关探活用
GET  /v1/ready                           # 模型/层段已就绪（区分 /health 与 /ready，冷启动方案 §5.4 要求）
GET  /v1/status                          # 引擎、当前模型、显存、层段、KV 缓存状态

POST /v1/models/load                     # {engine, quant_type, use_compile, model_id, layer_range?}
POST /v1/models/unload                   # {}
POST /v1/models/switch                   # {model_id, engine?}
POST /v1/models/current                  # 当前模型信息

POST /v1/chat                            # 完整对话（JSON，行为对齐 _execute_chat_full）
POST /v1/chat/stream                     # SSE 流式（事件格式与现 /api/chat/stream 一致）
POST /v1/chat/cancel                     # {generation_id}
POST /v1/speculative/run                 # 实验端点（QLH_SPEC_ENABLED 门控沿用）

POST /v1/layers/load                     # 层段加载（client 角色；master 角色本地层段同用）
POST /v1/layers/unload                   # {layer_range}
POST /v1/layers/forward                  # 层段前向：{layer_range, tensor_ref, past_key_values_ref?, ...}
POST /v1/layers/embedding                # Embedding 段
POST /v1/layers/lm_head                  # LM Head 段

POST /v1/kv/init                         # 为任务创建 KV 缓存页
POST /v1/kv/free                         # {task_id} 任务结束/取消时释放
```

**张量跨进程传输策略（阶段 1 决策）**：
- 主节点本机 `scheduler-svc → inference-svc`：`/v1/layers/forward` 的 hidden states 走 **loopback TCP + 既有 `serialize_tensor_fast`**（`tcp_comm.py:670-792`），每 token 传输量 = hidden 向量（Qwen-1.8B：2048 float16 ≈ 4KB），loopback 上开销 ~0.1ms 级。**KV 缓存始终留在 inference-svc 进程内**，跨进程只传 `past_key_values_ref`（任务 ID + 层段），不传 KV 数据。
- 性能验收门槛：分布式流水线 token 级延迟相比单进程基线劣化 <5%。若 loopback pickle 不达标，升级为**共享内存环形缓冲**（`multiprocessing.shared_memory`），契约不变（`tensor_ref` 换为共享内存名+offset），此为本计划的预留方案，不做第一实现。

### 4.2 scheduler-svc 契约（HTTP :8020）

> **对外适配原则（关键）**：§4.2 是**调度器内部契约**，不是网关对外的适配契约。网关对外端点必须按 **api_server 现有路径/方法/字段**原样实现（以 §2.2 域清单 + `tui_admin.py` 全部调用点为准，见 2.4），内部契约与对外契约**不得混用**。典型差异：对外是 `POST/DELETE /api/cluster/spare-master`、`PUT/DELETE /api/cluster/layers`、`GET /api/cluster/config`，对应内部能力是 spare 设置/清除、分层覆盖读写、配置读取——内部端点形态由 scheduler-svc 自行决定，网关负责映射。

> **落地决策（2026-08-02，T3）**：网关对 `/api/cluster/*` 采用 **1:1 透传代理**——内部端点路径 = 对外路径去掉 `/api` 前缀（`GET /api/cluster/nodes` → `GET /cluster/nodes`），实现见 `gateway/src/modules/cluster/cluster.controller.ts`；scheduler-svc 按 §2.2 字段契约提供响应（`gateway/test/fake-scheduler.ts` 是字段对齐的测试基线）。下方 `/v1/*` 清单因此**降级为参考**：scheduler-svc 实现时以透传路径 + §2.2 字段为准，`/v1/*` 形态不再要求。

```http
GET  /v1/status                          # 集群状态（节点、角色、备用主节点、分层覆盖、模式）
GET  /v1/queue                           # MLFQ 队列快照
GET  /v1/nodes                           # 节点列表（含画像、在线状态、层段）
GET  /v1/layers/assignment               # 当前层分配
POST /v1/nodes/{id}/deregister           # 注销节点
POST /v1/queue/pause | /v1/queue/resume | /v1/queue/clear
POST /v1/queue/strategy                  # {strategy: fifo|mlfq}
POST /v1/layers/apply                    # {assignment: [...]} 手工分层覆盖
POST /v1/master/transfer                 # {target_node_id} 角色转让
POST /v1/spare/set | /v1/spare/clear     # 备用主节点
POST /v1/infer/start                     # {request} 提交推理请求（调度器内部入口，网关经此走队列）
POST /v1/infer/cancel                    # {generation_id}
```

### 4.3 control-svc 契约（HTTP :8030，阶段 3 冻结）

按功能域：`/v1/conversations/*`、`/v1/sessions/*`、`/v1/logs/*`、`/v1/review/*`、`/v1/settings`、`/v1/models/registry/*`、`/v1/models/downloads/*`、`/v1/workflows/*`、`/v1/bootstrap/*`。**字段名与错误语义逐一对齐现有端点**（见 `api_server.py` 端点清单，§2.2），前端零改动。设备画像三个端点（profile / auto-configure / select-gpu）在阶段 2 由网关代理到 scheduler-svc（画像采集库留在 Python，从节点需本机采集上报）。

---

## 五、分期实施计划

### 5.0 Python 后端待改动标记清单（并行共存：只标记、不实施）

> 以下为**当前已确认的“将来要改动”清单**，全部处于「标记」状态，不产生任何代码改动。每条列出触发条件：对应微服务完成并通过契约验收后，在清理阶段（§5.1）按此清单执行。标记依据：§2.1 模块归属表 + §2.2 函数级切分（行号基线 2026-08-02）。

**A. 文件级标记**

| 文件 | 大小 | 将来动作 | 触发条件 | 标记 |
|---|---|---|---|---|
| `src/api_server.py` | 291KB | 端点壳删除、归档 `src/legacy/api_server.py`（执行段副本已由 inference-svc 提供） | inference-svc 完成（阶段 1 验收）+ api-gateway 契约对比通过（阶段 2.6） | 🔖 待退役 |
| `src/scheduler.py` | 496KB | 本体不动，继续作为 scheduler-svc 的 import 来源（内部数据面执行段由 scheduler 自身调用，**不复制到 inference-svc**——2026-08-03 设计变更，见 §1.2） | scheduler-svc 进程化完成（1.7） | 🔖 维持 import 复用 |
| `src/local_store.py` | 9KB | 删除（control-svc 会话域接管，旧 JSON 数据格式兼容是硬验收） | control-svc 会话域迁移完成（阶段 3.2） | 🔖 待删除 |
| `src/review.py` | 19KB | 删除（control-svc 审查域接管） | control-svc 审查域完成 | 🔖 待删除 |
| `src/email_notifier.py` | 27KB | 删除（control-svc 通知域接管） | control-svc 通知域完成 | 🔖 待删除 |
| `src/model_config.py` | 14KB | 删除（control-svc 模型注册表接管） | control-svc 模型注册域完成 | 🔖 待删除 |
| `src/model_downloader.py` | 21KB | 删除（control-svc 下载引导接管） | 同上 | 🔖 待删除 |
| `src/task_graph.py` | 112KB | 控制面 CRUD/journal 删除（control-svc 接管）；执行段复制至 inference-svc | control-svc 工作流域完成 | 🔖 待拆分 |
| `src/task_journal.py` | 25KB | 删除（control-svc 接管） | 同上 | 🔖 待删除 |
| `src/bootstrap.py` | 7KB | 删除（control-svc 提供同语义端点） | control-svc 引导域完成 | 🔖 待删除 |
| `src/db.py` | 56KB | 由 gateway / control-svc 直连替代（保留降级语义） | 阶段 3 数据层完成 | 🔖 待替代 |
| `src/device_profiler.py` | 51KB | **保留 Python 共享库**：仅 api_server 内画像端点代码删除，采集逻辑随 scheduler-svc 进程承载 | scheduler-svc 完成 | 🔖 待迁移 |
| `src/legacy_control.py` | — | 删除（阶段 2 期间的 control 域临时桩，被 control-svc 取代） | control-svc 全部域完成 | 🔖 待退役 |
| `scripts/dev_stubs.py` | — | 退役（开发桩，被真实 scheduler-svc / inference-svc 取代） | 阶段 1 完成、真实服务端到端通过 | 🔖 待退役 |

**不动**（共享库/协议，各服务 import 复用）：`config.py`、`node_config.py`、`tcp_comm.py`、`task_worker_protocol.py`、`task_worker_adapter.py`、`graph_orchestrator.py`、`model_sync.py`、`model_host.py`、`node_runtime.py`、`tui_admin.py`；数据面模块（`model_module.py`、`llama_engine.py`、`paged_kv_cache.py`、`island_engine.py`、`external_provider.py`、`speculative.py`、`diffusion/`、`task_provider.py`）复制进 inference-svc 包，原文件保留。

**B. 函数级标记（api_server.py / scheduler.py 内部）**

| 位置 | 内容 | 将来动作 | 标记 |
|---|---|---|---|
| `api_server.py:1668-7532` | 约 110 个 REST 端点壳 | 删除（api-gateway 接管，契约已由 contract_diff 对比） | 🔖 待删除 |
| `api_server.py:1137-1220` | 会话/对话管理 | 删除（control-svc 接管） | 🔖 待删除 |
| `api_server.py:6917-7532` | 日志管理 | 删除（control-svc 接管） | 🔖 待删除 |
| `api_server.py:1403/1622/2341/2553/2781/2805/2921/3370` | 数据面执行段 8 处（`_generate_followups_llama`/`_init_kv_cache`/`_execute_external_chat`/`_run_speculative_experiment`/`_execute_task_worker_stage`/`run_model`/`_execute_task_graph_chat_with_slot`/`_execute_chat_full`） | 复制至 inference-svc（1.2），原函数保留至清理阶段 | 🔖 待复制 |
| `api_server.py:7579-7587` | 唯一入口 `uvicorn api_server:app` | 清理阶段由 gateway + scheduler_svc_main + inference_svc_main 替代 | 🔖 待退役 |
| `scheduler.py:9569-10000 / 8252-8262 / 8268-8646 / 7720+ / 7472-7611 / 10567-10662 / 10708-10758` | 数据面执行段（`run_pipeline`/`_run_pipeline`/`_run_master_lm_head`/`_handle_layer_forward_locked`/`_schedule_layer_config._load`/`handle_infer_forward`/`_run_inference`/本地降级 `mgr.chat`/流式包装） | 复制至 inference-svc 流水线宿主，scheduler.py 本体不动 | 🔖 待复制 |
| `scheduler.py:236-293 / 395-1025 / 1374-2021 / 2448-3220 / 3482-4208 / 4230-4589 / 5167-6088 / 6395-7070` | 控制面（NodeState/MLFQ/注册心跳/层分配/task-worker 控制面/TCP 分发/角色转让/客户端模式） | 保留（scheduler-svc import 复用） | ✅ 不动 |

### 阶段 0：边界治理（纯 Python，目标 1 周）

> 目的：消除跨模块运行时循环依赖与全局可变状态，为进程拆分铺路。**本阶段不改变任何运行拓扑与行为**，是 1089 项回归 + 双节点实测的安全改造。

- [x] **0.1 冻结回归基线** ✅ 2026-08-02
  - 运行 `pytest -q`，确认当前基线（1089 项：1066 passed / 23 skipped），记录 commit 为基线。
  - 2026-08-02 实测：阶段 0 改造前基线 `1086 passed / 3 skipped`；分布式仿真基线（`run_simulation_tests.py simple_qa --type distributed --slaves 2`）**5/5 通过，平均延迟 4.43s**，报告 `tests/simulation/results/distributed_simple_qa_2slaves.json`。真实双节点实测需双机环境，列为阶段 1 前置待办。
  - 排障记录：仿真框架 3 处环境修复（非改造引入）：① Windows 匿名管道缓冲仅 4KB，后端日志（~10KB UTF-8）填满 PIPE 阻塞 uvicorn 启动 → Popen 输出改重定向文件；② `text=True` 以 GBK 解码 UTF-8 输出导致读线程崩溃 → 显式 `encoding="utf-8"`；③ `localhost` 在 Windows 解析为 ::1 而 uvicorn 只绑 IPv4 → 改用 `127.0.0.1`；④ 仿真读 `mode`/`slaves` 字段与 api_server 真实响应 `run_mode`/`nodes` 不一致 → 适配。
  - 运行双节点分布式聊天实测（`run_simulation_tests.py` + 真实双节点各一次），记录延迟/带宽/流式行为快照，作为阶段 1 对比基准。

- [x] **0.2 抽取推理宿主接口 `InferenceHost`** ✅ 2026-08-02
  - 新建 `src/model_host.py`，定义协议（仅类型与签名，行为由现有实现提供）：
    ```python
    class InferenceHost(Protocol):
        def select_engine(self, profile): ...
        def load_model(self, engine, quant_type, use_compile, model_id): ...
        def unload_model(self): ...
        def load_layer_range(self, layer_range, embed, lm_head): ...
        def forward_layers(self, layer_range, hidden, past_key_values, **kw): ...
        def chat(self, messages, **kw): ...
        def chat_stream(self, messages, **kw): ...
        def ensure_full_model(self): ...
    ```
  - `model_module.ModelManager` 实现该协议（现有方法签名已对齐，仅加标注与文档）。
  - **改造 scheduler 对 model_manager 的访问**：删除 scheduler 中 20+ 处函数内 `import api_server as _api`（:3461、:8254 等），改为 `self._host` 注入（`ClusterScheduler(host=...)`），api_server 构造 scheduler 时传入 `model_manager`。这是阶段 1 能拆进程的前提。
  - 2026-08-02 落地：scheduler 中 `import api_server` **12 处全部清零**（另清理 `sys.modules.get("api_server")` 5 处、`from api_server import ...` 1 处、`_api.` 引用 11 处）；`Scheduler(host=None)` 默认取全局单例 `get_model_host()`；api_server 模块底部 `model_host.attach(...)` 挂载 7 个回调（`_execute_task_worker_stage`/`_active_task_graph_model_identity`/`_build_model_chat_prompt`/`_format_model_response`/`_snapshot_recent_logs`/`_filter_recent_logs`/`THINKING_SYSTEM_PROMPT`）。
  - 测试：`tests/` 新增 `test_model_host_protocol.py`（用 `ModelManager` 的鸭子类型实例验证协议可满足；不加载真实模型）。

- [x] **0.3 config 运行时可变状态治理** ✅ 2026-08-02
  - `config.py` 的 `NODE_ID` 写回（`scheduler.py:127-140` `_sync_runtime_node_config`）改为显式 `node_runtime.set_node_id(...)`（新建 `src/node_runtime.py` 单例，持 `NODE_ID`/备用主节点/运行模式等运行时可变项）。
  - 各模块函数内 `import config as cfg` 读 `cfg.NODE_ID` 处改为 `from node_runtime import node_runtime`（grep 定位全部引用）。
  - 模块级只读常量（端口、路径、阈值）保留在 config.py 不动。
  - 测试：`tests/test_node_runtime.py` 覆盖 set/get 与并发访问。
  - 2026-08-02 落地：`node_runtime.py` 单例（NODE_ID/NODE_ROLE）；`scheduler._sync_runtime_node_config` 与 `node_config.apply_runtime_config` 不再写 `cfg.NODE_ID/NODE_ROLE`；`scheduler._configured_node_id`、`api_server._current_node_id_safe` 改读 node_runtime；scheduler 模块级 NODE_ID/NODE_ROLE 仍同步（保持模块内读取行为不变）。`grep cfg.NODE_ID src/` 零残留（仅 node_runtime 文档注释）。

- [x] **0.4 api_server 单例治理** ✅ 2026-08-02
  - `_LazyModelManager`（`api_server.py:113-155`）与模块级 `model_manager`（:372）移到 `src/model_host.py` 内统一持有；api_server 与 scheduler 共享同一实例。
  - 删除 `api_server.py` 中的 `scheduler` 模块级单例创建处的隐式耦合：保持 `scheduler: ClusterScheduler = ClusterScheduler()` 但构造显式传 host。
  - 2026-08-02 落地：`_LazyModelManager` 与 `model_manager` 迁入 `model_host.py`（`ModelHost` 统一持有 manager + `model_loaded`/`generation_config`/`current_quant`/`_db_available`/`full_chat_execution_lock` 运行时状态，属性代理 + `attach()` 回调挂载）；api_server 的 `model_manager = model_host`（兼容名）；`model_loaded`(37)/`generation_config`(11)/`current_quant`(17)/`_db_available`(41) 引用全部迁移到 host。

- [x] **0.5 阶段 0 验收（Go/No-Go）** ✅ 2026-08-02
  - `pytest -q`：1089 项全绿（1066 passed / 23 skipped）。
  - `grep -rn "import api_server" src/scheduler.py`：0 命中。
  - `grep -rn "cfg.NODE_ID" src/`：仅剩 config 定义处与 node_runtime 引用处。
  - 双节点分布式流水线聊天实测通过，性能快照与基线偏差 <3%。
  - commit 并更新 [模块接口说明](模块接口说明.md)（补 `model_host.py`、`node_runtime.py`）。
  - 2026-08-02 实测：`pytest -q` **1108 passed / 3 skipped** 全绿（含新增 test_model_host_protocol 15 项、test_node_runtime 8 项）；`grep "import api_server" src/scheduler.py` 0 命中；`grep cfg.NODE_ID src/` 仅 node_runtime 注释；分布式仿真 5/5 通过（延迟 4.43s，基线偏差 N/A——基线为本次采集）；[模块接口说明](模块接口说明.md) 已补第 8 章。真实双节点流水线实测待双机环境（阶段 1 前置）。

**阶段 0 里程碑**：进程内依赖单向化完成，scheduler 不再反向 import api_server；任何模块可独立 import 而不触发全量加载。

---

### 阶段 1：推理服务进程化（Python，目标 2 周）

> 目的：数据面从单体进程拆出为 `inference-svc` 独立进程。从节点 = 仅 inference-svc（不再 import fastapi/scheduler/网关）；主节点 = api-gateway(阶段 1 仍为 FastAPI) + scheduler + inference-svc 三进程。**本阶段全部仍是 Python，行为等价优先。**

- [x] **1.1 冻结 inference-svc 契约并写契约测试**
  - 新建 `src/inference_service/` 包：
    - `__init__.py`、`protocol.py`（§4.1 契约的 pydantic 模型 + 校验）、`routes.py`（FastAPI 子应用）、`engine_host.py`（模型托管，封装 model_module/llama_engine）、`kv_host.py`（PagedKVCache 生命周期，按任务持有）、`tensor_transport.py`（loopback `serialize_tensor_fast` 复用）。
  - `tests/test_inference_service_protocol.py`：契约字段、错误码、SSE 事件格式测试（不加载模型）。

- [x] **1.2 数据面执行段复制迁移（并行共存：复制，源文件不动）**
  - 把 §2.2 列出的 api_server 执行段（`run_model`、`_execute_chat_full`、`_execute_external_chat`、`_run_speculative_experiment`、`_init_kv_cache` 等）**复制**到 `engine_host.py`，源函数保留在 api_server.py 内（§1.4 规则 1）；函数体不变（禁止顺手重构）；inference-svc 侧模块级状态（`kv_cache`、`session_histories`、`conversation_stats`、`generation_config`、`model_loaded`）在服务内新建等价持有，**不迁移、不触碰 api_server 模块级状态**。
  - ~~把 §2.2 列出的 scheduler 数据面执行段（`_run_pipeline`、`_handle_layer_forward_locked`、`_run_master_lm_head`、`_run_inference`、本地降级 chat、流式包装）复制到 `engine_host.py` 的流水线部分；scheduler.py 源文件保持不动（调用方在 1.7 经 `InferenceClient` 接入）~~。**2026-08-03 设计变更：不复制**——scheduler 流水线段是 `Scheduler` 类内部方法（TCP 消息分发/层分配/流水线执行全部由 scheduler 自身调用），scheduler-svc 已按 1.7 "import 现有 scheduler.py 运行"落地，§4.2 又改为网关→scheduler-svc 透传代理（不经 inference-svc）；复制 ~1400 行会产生无人调用的双份实现（死代码 + 行为漂移风险），违背并行共存维护原则。§5.0 标记相应更新（见下）。
  - 每次迁移一个函数：`pytest` 全量回归 + `git commit`（提交信息 `feat(inference-svc): port <fn> into engine_host`）。
  - 行为等价保障：副本**不改一行业务逻辑**；api_server / scheduler 侧零改动（新旧两侧独立演进，清理阶段再做一致性比对）。

- [x] **1.3 新建推理服务入口 `src/inference_svc_main.py`**
  - uvicorn 启动 `routes.py` 子应用，端口 `QLH_INFERENCE_PORT`(8010)。
  - 角色感知：`QLH_NODE_ROLE=client` 时只启用层段/KV/流水线接口（+ 设备画像上报），不启用 chat 完整接口（从节点无完整模型）；`master` 时全部启用。
  - 模型加载延迟化：**不再在启动时 import model_module 顶层**，首次 `load` 请求时才 import（transformers 6s 从"启动成本"变为"首次加载成本"）。
  - 启动日志打印各阶段耗时，写入 `logs/inference_svc_startup.log`。

- [x] **1.4 新建 `InferenceClient`（供 scheduler-svc 注入，不改 scheduler.py）**
  - 新建 `src/inference_client.py`：HTTP+SSE 客户端，实现与 `InferenceHost` 同构的方法（load/chat/chat_stream/forward_layers/kv_init/kv_free/cancel），内部把 `forward_layers` 的 tensor 走 `tensor_transport` loopback。
  - scheduler-svc（1.7）构造 `Scheduler(host=InferenceClient(host=127.0.0.1:8010))` 注入；**回退 `QLH_MONOLITH=1` 时仍走进程内 model_host**（保证随时一键回单进程）。
  - scheduler.py 源码零改动（阶段 0 的 `host=` 注入参数已就绪）；api_server 进程内的 scheduler 继续使用进程内 host，不受影响。

- [x] **1.5 从节点瘦身（并行共存：新进程独立实现，不动现有 client 角色）**
  - 新建从节点入口 `python src/inference_svc_main.py`（client 角色）：自动连接主节点（连接配置沿用 `QLH_CLIENT_MASTER_HOST/PORT`；`scheduler.py:1271-1280` 的 client 分支逻辑**复制**到 `src/inference_service/peer.py`，复用 `tcp_comm.TCPClient`，scheduler.py 源文件不动）。
  - **验证新入口不再 import fastapi/scheduler/api_server**：`python -X importtime -c "import inference_svc_main"` 断言无 `fastapi`、`scheduler` 条目。
  - 现有从节点（api_server.py client 角色）在清理阶段前保持现状，与新 inference-svc 并行可用；生产切换见 §5.1。

- [x] **1.6 主节点三进程编排（开发期）**
  - `scripts/dev_topology.py`：拉起 scheduler-svc（此时 scheduler 仍嵌在旧 api_server 内）+ inference-svc 两进程，健康检查互等。
  - 旧 api_server 的 scheduler 分支构造改为连 `InferenceClient`。
  - 注意：**阶段 1 的 api_server 仍承载全部 110 端点与调度器**（scheduler 独立成进程是阶段 2 与 3 之间的事——见 1.7 说明）。

- [x] **1.7 scheduler-svc 进程化（本阶段收尾子项，若时间不足整体顺延至阶段 2 起点）**
  > 2026-08-03 完成：`src/scheduler_svc_main.py` 独立进程入口（import 现有 scheduler.py 运行，`Scheduler(host=InferenceClient)` 注入 / `QLH_MONOLITH=1` 回退进程内 model_host）。§4.2 的 HTTP 壳（:8020 契约端点）已随阶段 2 起点完成（见下方阶段 2 起点记录）。
  - 新建 `src/scheduler_svc_main.py`：uvicorn 启动 scheduler 的 HTTP 壳（§4.2 契约），把 scheduler 从 api_server 进程剥离。
  - 主节点最终拓扑：api_server(端点壳) + scheduler_svc + inference_svc 三进程。
  - 该子项是阶段 2 的前置，放在阶段 1 末尾执行；若 Go/No-Go 检查未过则回退到 1.6 的拓扑继续开发，不阻塞阶段 2 的 TS 网关编写。

- [x] **1.8 阶段 1 验收（Go/No-Go）**（2026-08-03 验收：本地项全部完成，见下）
  > **已完成**：pytest 全绿（1171 passed / 2 skipped，含 59 个契约测试）；
  > inference-svc 冷启动 import 0.04s / build_app 3.85s（仅 config→torch 1.8s，
  > model_module 树延迟至首次 load）；client 角色入口无 fastapi/scheduler/
  > api_server import；`QLH_MONOLITH=1` 回退冒烟通过（scheduler-svc role=master）；
  > 主节点 `/api/health` 先行实测通过（仅 api_server 独立启动 1s 内 200，
  > inference/scheduler 未运行不受影响）；冷启动方案附录已更新（附录 C/D）。
  > **待真实环境**：双节点分布式流水线聊天实测（token 级延迟劣化 <5%）、
  > llama_cpp/torch 本地推理实测（models/ 已有 Qwen-1.8B 与 7B GGUF，待有 GPU/双机环境执行）。
  - `pytest -q` 全绿（新增契约测试计入）。
  - 主节点：`/api/health` 在 scheduler 与 inference 均未就绪时仍可访问（网关先行）。
  - 从节点冷启动：`python -X importtime -c "import sys; sys.path.insert(0,'src'); import inference_svc_main"` 全链路 <3s（不含模型加载），且 importtime 日志无 `fastapi`/`scheduler`。
  - 双节点分布式流水线聊天实测：流式行为与阶段 0 基线一致，token 级延迟劣化 <5%。
  - 本地完整推理（llama_cpp 与 torch 两引擎）聊天实测通过。
  - `QLH_MONOLITH=1` 回退后全部功能如常。
  - 更新 [Python后端冷启动优化方案](Python后端冷启动优化方案.md) 附录（新拓扑下的启动指标）。

**阶段 1 里程碑**：推理数据面独立进程化；从节点瘦身完成；主节点"网关先行、模型异步"拓扑成立。

---

### 阶段 2：TS 网关替换 FastAPI 网关（目标 2 周）

> 目的：对外 API 由 TypeScript 网关提供，端口 8000 与全部端点契约不变，前端/Android/TUI 零改动。FastAPI 端点壳退役。

- [x] **2.1 网关工程初始化** ✅ 2026-08-02
  - 新建 `gateway/`：`nest new`（NestJS）+ Fastify adapter（`@nestjs/platform-fastify`），TypeScript strict。
  - 目录：`gateway/src/main.ts`、`gateway/src/app.module.ts`、`gateway/src/common/`（request-id 中间件、错误格式化、日志中间件——语义对齐 `api_server.py:319-366` 的 request_id/detail 行为）、`gateway/src/modules/<域>/`。
  - 配置：`.env` 读取 `QLH_API_PORT`/`QLH_SCHEDULER_URL`/`QLH_INFERENCE_URL`/`QLH_CONTROL_URL`。
  - 测试框架：Jest + Supertest（契约测试：每个端点断言状态码、`detail`/`request_id` 结构）。

- [x] **2.2 端点迁移（按域，每域一个 commit）** ✅ 2026-08-02（~~chat/upload 未完成~~ → 2026-08-03 完成，见下；contract_diff.py 复测随 2.6 执行）
  - 域清单与来源端点（api_server.py 行号见 §2.2 清单）：health/status、chat（代理到 inference-svc + scheduler 校验）、models（registry/load/switch/gguf/download → control-svc 或直连）、device（profile/auto-configure/select-gpu → **代理到 scheduler-svc**，采集库 device_profiler 留 Python）、cluster（status/nodes/connect/register/android/heartbeat/master-health/discover/reset-identity/invite/email-test → scheduler-svc）、queue（→ scheduler-svc）、distributed-inference/layers（→ scheduler-svc）、high-availability（transfer-master/spare-master → scheduler-svc）、review（→ control-svc 或阶段 3 前暂代理 FastAPI 遗留）、workflows、sessions、conversations、settings、logs、bootstrap、experimental/speculative（→ inference-svc）。
  - **每域迁移验收**：该域端点在 TS 网关与旧 FastAPI 返回体逐字段一致（写契约对比脚本 `scripts/contract_diff.py`，双网关同时启动对比响应 JSON）。
  - SSE 流式代理：Fastify 转发 `inference-svc /v1/chat/stream` 的事件，**逐字节保留事件格式**（前端 EventSource 依赖）。
  - **2026-08-02 复查（2.1-2.4 全量复核）**：api_server 96 个端点实测 **95/96 可达**，唯一缺口 `POST /api/chat/upload`（multipart，需 @fastify/multipart，留待 2.6 前处理）。复查发现并修复 3 个问题：① presets / user/settings / db/health 三域缺失（前端 ChatPanel/Settings 依赖）——已补网关透传 + legacy-control 桩；② `GET /api/models` 根路由缺失（前端 fetchModels 依赖，返回模型配置列表 + active_model_id）——已补网关 → inference-svc `/v1/models`；③ `/api/cluster/review/*` 路由归属错误（原随 cluster 透传 scheduler-svc，审查票属控制面）——已改为网关分流到 legacy-control（阶段 3 迁 control-svc）。桩同步补齐：dev_stubs 加 android/register、android/heartbeat、spare-master/logs；legacy_control 加 cluster/review 全端点、logs 扩展 6 端点（download/export/client-error/node/{id}/recent/nodes-summary/{filename}）、models/download GET。
  - **2026-08-03 `POST /api/chat/upload` 完成**（96/96 端点可达）：@fastify/multipart 注册（5MB 限制）+ `chat.controller.ts` upload 端点——语义对齐 api_server.py:1729-1816（约 40 种扩展名校验 / UTF-8→GBK→latin-1 解码探测 / 5000 行截断 / 语言映射），纯本地解析无上游依赖。契约测试 4 用例（txt 解析 / py 语言检测 / 不支持扩展名 400 / 超行截断），网关契约测试 79/79 全绿。

- [x] **2.3 静态前端托管与开发代理** ✅ 2026-08-02
  - 生产：网关托管 `frontend/dist`（端口 8000 直接访问，替代现在 FastAPI 的 StaticFiles）。
  - 开发：`frontend/vite.config.js` 的 proxy target 从 `localhost:8000` 不变（网关仍占 8000）。
  - 验证：`npm run build` 后浏览器全功能走查（前端最近证据 9/9 项重跑）。
  - **实现**：`gateway/src/modules/static/static.service.ts` + Fastify 原生通配路由 `/*`（createApp 中注册，早于 Nest 路由）；`QLH_FRONTEND_DIST` 环境变量可覆盖默认 `<项目根>/frontend/dist`。语义对齐 `api_server.py:7561-7571` StaticFiles(html=True)：GET / 返回 index.html、/assets/* 按文件返回、未命中文件回退 index.html（SPA 路由）；`/api/*` 保持 JSON 404（CatchAllController 不抢）；非 GET 不静态托管；路径穿越防御（解码后按段检查 `..` + resolved 越界双保险，含 %2e%2e/..%2f 变体）。
  - **契约测试**：`gateway/test/static-contract.e2e-spec.ts` 7 用例（根/资源/SPA 回退/API 隔离/API 404/穿越/POST）——实测全绿，网关契约测试 75/75。

- [x] **2.4 TUI 兼容验证（T1-T8 全部完成）** ✅ 2026-08-02
  - **专项文档**：[TUI 适配实施计划](TUI适配实施计划.md)（38 个调用点全表、5 项细节、契约测试 43 用例、7 屏 × 2 角色验收标准，本文档不再重复）。
  - **用例清单 = `tui_admin.py` 的全部 HTTP 调用点**（已审计：38 处，覆盖 25 个唯一端点，全部同步 JSON、零流式/SSE/WebSocket、零 cookie）。审计结论：`ApiClient.request`（tui_admin.py:216-259）硬编码 `base_url + "/api" + path`(:218)、读 JSON `detail` 错误体(:234-244)、`X-QLH-Log-Token` 仅日志 3 端点且**无 token 也允许请求**(:228-229、:1193-1199)、3s 轮询 3 个屏幕(:552、:599、:675、:992)。
  - **5 个必须守住的细节**：① `/api` 前缀原样保留；② 错误体必须是 JSON `detail`（禁止 302/纯文本/204 空体——`/api/cluster/queue/task/{id}` 的 DELETE 返回 JSON `message`/`success`，TUI 读取 :1076-1077）；③ 数值字段保持 number：`avg_rtt_ms`(:723)、`wait_seconds`(:1024)、`gpu_allocated_gb`(:639)、`utilization`(:646) 经 `float()` 转换，输出字符串会致屏幕报错；④ `/api/health` 必须返回 JSON `status=="ok"`（网关先行拓扑下亦然，TUI :602、:1407）；⑤ `/api/status` 的 `gpu`/`kv_cache`/`device` 内嵌结构原样（TUI :642-653）。
  - **轮询友好**：3s 轮询的 Dashboard/Nodes/Queue 三屏不得触发网关 429/限流（urllib 无重试，直接报错）。
  - `tui_admin.py` 零改动跑通全部 7 个屏幕（系统总览/节点管理/分布式与分层/请求队列/设备画像/日志/设置），主节点与从节点角色各跑一遍（从节点视角依赖 `/api/cluster/master-health` 等端点，:686）。
  - 2026-08-02 实测：契约测试 44/44 全绿（tui-contract + rest-contract 共 63 用例）；`scripts/dev_stubs.py` 综合桩（scheduler :8020 + inference :8010）+ `src/legacy_control.py`（:8040）+ 网关 :8100 端到端实测全部通过（health/status 三源聚合、cluster 全方法含 404 透传、device、models、chat 含 SSE 流式、experimental、logs、控制面域 sessions/conversations/settings/review/workflows/bootstrap/models registry/download/gguf、request-id 头、404 兜底）。**待 T7：真实 TUI 7 屏 × 2 角色走查**（需真实 scheduler-svc/inference-svc，桩已就绪）。

- [ ] **2.5 旧 FastAPI 网关退役（冻结：动作移至清理阶段 §5.1，仅保留标记）**
  - 标记（§5.0）：`api_server.py` 端点壳、执行段副本、归档路径 `src/legacy/api_server.py`、`start_backend.bat` 切换方案均已列明。
  - **不执行**：并行共存期间 api_server.py 保持现状作为可运行基线；退役动作（端点壳删除 + 归档 + `start_backend.bat` 切换）统一在清理阶段执行。

- [ ] **2.6 阶段 2 验收（Go/No-Go）**
  - 前端 9/9 功能项回归通过；Android 全无模式连主节点实测通过；TUI 全屏幕实测通过。
  - `scripts/contract_diff.py`：全部端点响应与退役前 FastAPI 一致（已知差异表逐条评审后放行）。
  - SSE 流式首 token 延迟与阶段 1 基线偏差 <10%。
  - 网关冷启动：`node gateway/dist/main.js` 从进程启动到 `/api/health` 响应 <1s。
  - ~~旧 api_server 从启动路径移除，pytest 中依赖 api_server 的测试改为依赖网关契约测试~~（**冻结**：按并行共存策略移至清理阶段 §5.1，其余判据不受影响）。
  - **2026-08-02 工具与前置项进度**：
    - `scripts/contract_diff.py` 已实现（自动提取 api_server 96 端点、双网关结构指纹对比、POST/PUT body 模板、差异分类：一致/差异/待复测/跳过）。双网关实测：**一致 12 / 差异 75 / 待复测(body) 8 / 跳过 1**——差异全部源于桩（dev_stubs/legacy_control）的**模拟粒度**（真实 api_server 校验资源存在性/身份/DB 状态，桩不模拟；桩响应形状为简化版），**无网关自身行为缺陷**；已修复 2 个真实差异：① Fastify 对"带 json content-type 空 body"返回 400（对齐 FastAPI 语义，经 adapter.useBodyParser 自定义 parser）；② `/api/health` 缺 timestamp 字段。差异表将在阶段 1（真实 scheduler-svc/inference-svc 按 api_server 形状实现）与阶段 3（control-svc）落地后复测收敛。
    - **2026-08-03 复测（真实 scheduler-svc 替换 :8020 桩）**：一致 **39** / 差异 **48** / 待复测 8 / 跳过 1（基线 12/75）。本轮修复与结论：① `contract_diff.py` 指纹将 int/float 统一为 "num"——Python json 保留 0.0（float）而 JS `JSON.stringify(0.0)` 输出 0（int），双网关同值异型属编码差异非契约差异（修复 +9 收敛）；② **cluster 域（非 review）已收敛**：nodes/queue/config/layers/my-role/invite/master-health/discover/connect/register/android 双网关指纹全部一致；`/api/cluster/status` 仅"新有旧无"4 字段（max_nodes/node_id/node_role 为网关聚合的壳层增强 + tcp_client，向后兼容**已知差异放行**）；③ 剩余 48 差异全部属 legacy_control 桩模拟粒度（review/logs/conversations/sessions/settings/models 控制面域）——阶段 3 control-svc 落地后复测收敛；④ `POST /api/chat/upload` 已实现（见 2.2），contract_diff `--skip upload` 默认参数改为可指定（upload 现已可测，multipart 模板待补）。
    - **网关冷启动实测：100ms** 到 `/api/health` 响应（`time` 总 0.68s，含 node 启动），达标 <1s。
    - 2.5 的前置（阶段 1 scheduler-svc/inference-svc 进程化）未完成，api_server 端点壳暂不退役；start_backend.bat 保持现状。

**阶段 2 里程碑**：对外服务面全部由 TS 提供；Python 侧只剩 scheduler-svc + inference-svc 两个进程。

---

### 阶段 3：控制面迁移 TS（目标 2-3 周）

> 目的：会话/日志/审查/通知/画像/模型注册/任务图控制面从 Python 迁入 `control-svc`。**状态数据格式必须兼容旧数据文件**（local_store JSON、SQLite journal、PostgreSQL 表）。

- [x] **3.1 control-svc 工程初始化** ✅ 2026-08-04
  - 新建 `control/`（NestJS + Fastify，复用 gateway 的 common 中间件包或抽 `packages/qlh-common`）。
  - 数据层：TypeORM/Prisma 接 PostgreSQL（沿用 db.py 的降级语义：DB 不可用时回落本地 JSON 存储）；本地存储格式与 `local_store.py` 当前 JSON 布局一致。
  - 2026-08-04 落地：工程（8030，env `QLH_CONTROL_PORT`）+ `common/`（request-id / JsonDetailFilter 副本）+ `data/config-dao.ts`（cluster_config 表，降级语义对齐 db.py）+ settings 首迁域；e2e 测试 7 用例全绿（`app.inject` 模式，不用 supertest）。

- [ ] **3.2 逐域迁移（每域：TS 实现 → 契约对比 → 删除 Python 实现 → commit）**（会话/对话域 ✅ 2026-08-04 完成，其余域待迁移）
  - [x] 会话/对话管理（`local_store` 语义 + 现有 8 个端点）：**先做**，旧 JSON 数据可读是硬验收。
    - 2026-08-04 落地：`control/src/data/session-store.ts`（local_store 布局逐字节兼容：`_sessions.json` + `{id}.json`、原子写、`%Y-%m-%dT%H:%M:%S` 本地时间、目录 `QLH_CHAT_HISTORY_DIR` 优先否则 `<cwd>/chat_history`）+ `sessions.controller.ts` 12 端点（sessions×8 + conversations×4，语义对齐 api_server.py:6245-6662 降级路径；DB 分支未迁移，`db_connected` 用 ConfigDao.dbEnabled()，source 恒 local_store）；契约测试 25 用例（含旧 Python 数据预置可读/可改名/可删除、损坏 JSON 重建、default→活跃会话解析、turns 越界 400/无消息 404）全绿；`pytest` 不受影响（Python 侧零改动）。
  - 日志管理（读 `logs/`、`X-QLH-Log-Token` 校验、12 个端点）：注意 Windows 路径与编码差异。
  - 审查 review + email_notifier（SMTP 告警、IMAP 投票、票务状态机；状态存 PostgreSQL，语义对齐 `review.py`）。
  - 设备画像端点（profile / auto-configure / select-gpu）：**采集库 device_profiler 保留 Python**（从节点为纯 Python 单进程，本机画像必须由 Python 采集），网关侧端点代理到 scheduler-svc；字段名与主节点排序打分逐一对齐。
  - 模型注册表 model_config + model_downloader（注册表 CRUD、下载引导、哈希校验、GGUF 扫描）。
  - 任务图控制面 task_graph 的 CRUD/journal（执行段仍在 inference-svc，经 scheduler 提交）。
  - bootstrap 首连引导端点。
  - 用户设置 settings。
  - 迁移顺序原则：**先无状态后状态**、先低风险（日志/设置）后高风险（会话/审查）。

- [ ] **3.3 Python 侧清理（冻结：动作移至清理阶段 §5.1，仅保留标记）**
  - 标记（§5.0）：`local_store.py`、`review.py`、`email_notifier.py`、`model_config.py`、`model_downloader.py`、`task_graph.py`（控制面部分）、`task_journal.py`、`bootstrap.py`、`legacy_control.py`、`dev_stubs.py` 的删除/退役清单已列明；`db.py` 由网关/control-svc 直连替代；`device_profiler.py` 保留为共享库（采集移入 scheduler-svc 进程）。
  - **不执行**：并行共存期间上述文件保持现状；`tui_admin.py` 任何阶段都不动（HTTP 契约）。

- [ ] **3.4 阶段 3 验收（Go/No-Go）**
  - 全部端点由 TS 提供；Python 仅剩 `scheduler_svc_main.py` 与 `inference_svc_main.py` 两个入口。
  - 旧会话 JSON / SQLite journal / PostgreSQL 数据在 TS 侧读取一致（用真实历史数据文件回归）。
  - 设备画像字段与主节点打分排序结果一致（双节点对比实测；采集仍由 Python 共享库完成）。
  - `pytest -q`：Python 测试集收缩后全绿；新增 `gateway/test/`、`control/test/` 覆盖迁移域。

**阶段 3 里程碑**：控制面 100% TS；Python 进程 = 调度 + 推理两个纯推理域服务。

---

### 阶段 4：打包、部署与收尾（目标 1 周）

- [ ] **4.1 Python 侧多进程打包**
  - PyInstaller 增加多入口：`packaging/qlh-scheduler.spec`、`packaging/qlh-inference.spec`（inference 含 torch/transformers 重依赖，独立 spec 独立 venv 策略沿用现有 `.venv-packaging*` 隔离原则）。
  - `packaging/launcher.py` 改为进程编排器：按角色拉起所需进程（主节点 3 进程 / 从节点 1 进程），带健康检查、崩溃拉起、退出级联；`QLH_MONOLITH=1` 保留单进程回退入口（本阶段结束后移除）。

- [ ] **4.2 TS 打包与启动**
  - `gateway/` 与 `control/` 构建为单文件可执行（Node SEA `node --experimental-sea-config` 或 `pkg`），或随安装包附带 Node 运行时目录；验证无 Node 环境机器可运行。
  - Windows Inno Setup 脚本（`setup.iss`/`setup-cuda.iss`）与 Linux `build-deb.sh` 更新：文件清单 + 快捷方式 + systemd 单元（`qlh-gateway.service`、`qlh-scheduler.service`、`qlh-inference.service`，顺序与健康依赖声明）。

- [ ] **4.3 安装包全流程验证**
  - 集显版 / 独显版 / .deb：安装 → 启动 → 健康检查 → 聊天 → 分布式流水线 → 卸载，全链路走查（对齐 README 打包章节）。

- [ ] **4.4 文档与收尾**
  - 更新 README 架构图与目录树、[整体架构](整体架构.md)、[模块接口说明](模块接口说明.md)、[Python后端冷启动优化方案](Python后端冷启动优化方案.md)（终态指标）、[总体下一步计划](总体下一步计划.md)（生命周期登记）。
  - 清理 `src/legacy/`（保留一个版本的归档说明即可，代码由 git 历史承载）。

- [ ] **4.5 终态验收**
  - 冷启动指标达成下表（§六）；前端 9/9；Android Full/Lite；TUI 7 屏；双节点流水线；外部辅助三条路线（island/external/speculative 实验端点）全部回归。

### 5.1 清理阶段（并行共存策略新增：原 2.5 / 3.3 的删除动作汇总）

> 唯一允许“修改/删除现有 Python 后端”的阶段。**触发条件（全部满足）**：① inference-svc 完成阶段 1 验收（含双节点实测、QLH_MONOLITH 回退）；② scheduler-svc 进程化完成；③ api-gateway 阶段 2.6 验收通过（含 contract_diff 收敛、前端 9/9、Android、TUI）；④ control-svc 阶段 3.4 验收通过（含旧数据兼容、画像一致）。

- [ ] 执行 §5.0 标记清单的全部动作（退役/删除/迁移/替代），每项一个 commit，执行后复跑 `pytest -q` 收缩集 + 契约对比。
- [ ] `start_backend.bat` / `start_tui.sh` 等启动脚本切换为新拓扑：TS 网关（`node gateway/dist/main.js`）+ scheduler-svc + inference-svc（+ control-svc）。
- [ ] 从节点启动切换为 `python src/inference_svc_main.py`（client 角色），旧 api_server client 角色退出。
- [ ] 移除 `scripts/dev_stubs.py`、`src/legacy_control.py` 与 `QLH_MONOLITH=1` 回退入口（或保留一个 release 归档）。
- [ ] 更新 README 架构图与目录树、[整体架构](整体架构.md)、[模块接口说明](模块接口说明.md)、[Python后端冷启动优化方案](Python后端冷启动优化方案.md)、[总体下一步计划](总体下一步计划.md)（生命周期登记）。

---

## 六、验证与评判标准

### 6.1 冷启动指标

| 指标 | 现状（2026-07 快照） | 阶段 1 后 | 阶段 4 后 |
|------|---------------------|-----------|-----------|
| 主节点 API 可用（/api/health） | 12.1s（全量 import） | ~2s（网关先起，模型异步） | **<1s**（TS 网关） |
| 主节点首次推理就绪 | 12.1s | 模型加载完成后即可；进程常驻不随重启 | 同左 |
| 从节点启动 → 注册主节点 | 12.1s | **<3s**（无 fastapi/scheduler import） | 同左 |
| 服务崩溃自拉起后恢复 | 12.1s | 推理进程自拉起（模型重载）或热备 | 网关 <1s，推理进程独立拉起 |
| 内存占用（控制面进程） | ~500MB（含模型进程） | 控制面/推理分进程，控制面瘦身 | TS 网关/控制服务 <200MB，模型内存只属推理进程 |

### 6.2 行为回归

- Python `pytest -q`：阶段 0/1 全绿（1066 passed / 23 skipped + 新增契约测试）；阶段 2/3 后剩余 Python 测试集全绿。
- 前端功能项 9/9（README 复核口径）。
- 分布式双节点流水线：流式行为、心跳、断线重连、角色转让、备用主节点提权全链路实测。
- 外部辅助：island（双节点实测）、external（作用域门控 deny/opt_in/allow_all）、speculative 实验端点。
- 契约对比脚本 `scripts/contract_diff.py`：双网关并存期逐字段 diff = 0（已知差异表评审放行）。
- **TUI 契约测试**：以 `tui_admin.py` 38 个调用点为用例清单（2.4），断言路径/方法/响应字段类型（number 字段）/错误 `detail` JSON/DELETE 返回 JSON/`X-QLH-Log-Token` 可选语义 5 项全部通过。

### 6.3 性能门槛（阶段 1/2 Go/No-Go 硬指标）

- 分布式流水线 token 级延迟劣化 <5%（对比阶段 0 双节点基线）。
- SSE 流式首 token 延迟劣化 <10%。
- 层前向 loopback 传输（4KB hidden）单次 <1ms。

---

## 七、风险与回退

| # | 风险 | 影响 | 缓解 / 回退 |
|---|------|------|-------------|
| 1 | 流水线时序敏感，进程间 IPC 引入抖动 | 分布式推理延迟劣化、超时误判 | 本地前向优先 loopback `serialize_tensor_fast`（复用成熟序列化）；预留共享内存方案（契约不变）；每阶段双节点实测把关 |
| 2 | 张量跨进程传输开销失控 | token 级延迟劣化 | KV 缓存永不跨进程（只传 ref）；4KB/token 传输量上限可量化；性能门槛 §6.3 |
| 3 | 行为等价移植中出现隐性差异 | 流式事件、错误结构、状态机偏差 | "纯移动、禁重构"纪律；逐函数 commit + 契约对比脚本；1089 项回归兜底 |
| 4 | TS 网关与 FastAPI 行为差异 | 前端/Android/TUI 兼容破坏 | 前端零改动为硬验收；`contract_diff.py` 逐字段对比；SSE 逐字节保真；**TUI 以 38 个调用点为契约测试用例（2.4），5 项细节（/api 前缀、detail JSON、DELETE 返回 JSON、数值字段类型、Log-Token 可选语义）逐项断言**；**网关适配面必须覆盖 TUI 依赖的全部 cluster/device 端点（§4.2 内部契约之外的约 15 个端点），不得按 §4.2 清单透传实现** |
| 5 | 会话/审查等状态数据迁移不兼容 | 历史数据丢失 | 阶段 3 先迁移数据读取层，旧格式文件直接读取为验收项；PostgreSQL 表结构沿用 |
| 6 | Windows 打包复杂度上升（多进程 + Node 运行时） | 安装包构建失败 | 阶段 4 专项；Node SEA 单文件化；PyInstaller 多 spec 复用现有 venv 隔离策略；`QLH_MONOLITH=1` 保留到阶段 4 末尾 |
| 7 | 团队对 NestJS/TS 后端不熟 | 开发节奏下降 | 控制面为纯 IO/CRUD 无重计算，TS 性能足够；先网关（脚手架自动生成）后控制面；每域独立小步提交 |
| 8 | 从节点多进程部署变复杂 | 从节点配置负担 | 从节点 = 1 进程（inference-svc client 角色），比现状（整包 api_server）更简单；连接配置沿用 `QLH_CLIENT_MASTER_HOST/PORT` |

---

## 八、决策记录

| 日期 | 决策 | 理由 | 影响 |
|------|------|------|------|
| 2026-08-02 | 推理数据面保留 Python，独立 `inference-svc` | model_module 依赖 torch/transformers/llama.cpp，跨语言重写推理核心不可接受；冷启动成本只在模型加载，进程常驻即可规避 | 阶段 1 |
| 2026-08-02 | 调度器保留 Python，独立 `scheduler-svc` | 与数据面深度耦合（运行时调 forward_layers、张量序列化、KV 生命周期、TCP 协议），自身冷启动 3.2ms 非瓶颈；迁移成本极高收益低 | 阶段 1 末 / 阶段 2 起点 |
| 2026-08-02 | 控制面迁移 TypeScript（NestJS + Fastify） | 团队已有 React TS 基础；控制面为 IO/CRUD 场景，TS 启动快、内存低、开发效率高 | 阶段 2/3 |
| 2026-08-02 | 对外端口 8000 与端点契约不变，前端/Android/TUI 零改动 | 客户端面广（Web/Android 两版本/TUI），契约兼容是拆分的安全网 | 全部阶段 |
| 2026-08-02 | 阶段 1 保持"纯移动、禁重构" | 行为等价优先于代码质量；重构收益由阶段 2/3 的 TS 重写承接 | 阶段 1 |
| 2026-08-02 | 从节点瘦身为单进程 inference-svc | 从节点实际只用推理数据面；消除 fastapi/scheduler import 使冷启动 12.1s→<3s | 阶段 1 |
| 2026-08-03 | **并行共存**：微服务独立实现，现有 Python 后端源码与运行拓扑冻结不动；2.5 / 3.3 的删除动作统一移至清理阶段（§5.1），当前仅标记（§5.0） | 现有后端是唯一可运行基线（网关仍依赖 dev_stubs/legacy_control 桩）；边拆边删会破坏可运行性且无法回退 | 阶段 1 迁移改“复制”；2.5 / 3.3 冻结；新增 §1.4 / §5.0 / §5.1 |

---

## 九、附录：任务追踪总表

- [ ] **阶段 0：边界治理（1 周）**
  - [ ] 0.1 冻结回归基线（pytest 1089 + 双节点快照）
  - [ ] 0.2 抽取 `InferenceHost` 接口并改造 scheduler 注入（去 20+ 处 `import api_server`）
  - [ ] 0.3 `node_runtime.py` 承接 config 运行时可变状态
  - [ ] 0.4 api_server 单例治理（model_manager 移至 model_host）
  - [ ] 0.5 验收：回归全绿 + grep 零循环 import + 双节点实测
- [ ] **阶段 1：推理服务进程化（2 周）**
  - [ ] 1.1 inference-svc 契约 + 契约测试
  - [ ] 1.2 数据面执行段复制迁移（并行共存：源文件不动）
  - [ ] 1.3 `inference_svc_main.py` 入口 + 延迟 import
  - [ ] 1.4 `InferenceClient`（loopback 张量传输）
  - [ ] 1.5 从节点瘦身（client 角色单进程）
  - [ ] 1.6 主节点三进程开发拓扑
  - [ ] 1.7 scheduler-svc 进程化（可顺延至阶段 2 起点）
  - [ ] 1.8 验收：冷启动 <3s（从节点）/ 延迟劣化 <5% / QLH_MONOLITH 回退
- [ ] **阶段 2：TS 网关（2 周）**
  - [ ] 2.1 gateway 工程初始化（NestJS + Fastify + 契约测试骨架）
  - [ ] 2.2 端点按域迁移 + contract_diff 逐字段对比
  - [ ] 2.3 静态前端托管与开发代理
  - [ ] 2.4 TUI 兼容验证
  - [ ] 2.5 FastAPI 端点壳退役（冻结 → 清理阶段 §5.1）
  - [ ] 2.6 验收：前端 9/9 + Android + TUI + 网关冷启动 <1s
- [ ] **阶段 3：控制面迁移 TS（2-3 周）**
  - [ ] 3.1 control-svc 工程初始化 + 数据层
  - [ ] 3.2 逐域迁移（会话→日志→审查→模型注册→任务图控制面→bootstrap→设置；画像端点代理 scheduler-svc，采集库留 Python）
  - [ ] 3.3 Python 侧清理（冻结 → 清理阶段 §5.1）
  - [ ] 3.4 验收：旧数据兼容 + 画像一致 + 双网关契约对比
- [ ] **阶段 4：打包与收尾（1 周）**
  - [ ] 4.1 PyInstaller 多入口 spec + launcher 进程编排
  - [ ] 4.2 TS 单文件打包 + systemd/Inno 更新
  - [ ] 4.3 安装包全流程验证（集显/独显/deb）
  - [ ] 4.4 文档更新（README/整体架构/模块接口/冷启动方案/总体计划）
  - [ ] 4.5 终态验收（冷启动指标 + 全功能回归）

---

**文档版本**：1.0
**维护者**：QLH 开发团队
**下次复核触发**：阶段 0 完成、阶段 1 Go/No-Go、阶段 2 Go/No-Go、阶段 4 终态各一次
