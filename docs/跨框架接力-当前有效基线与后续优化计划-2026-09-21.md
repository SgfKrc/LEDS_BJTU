# 跨框架接力（D→L）当前有效基线与后续优化计划

> 更新时间：2026-09-23（新增 §10 待办与 9B/Qwen3.5 两段实测；P0/P1/P2/P4 复核：P2 多轮扫描与 P4 预算工具已增加 fail-closed 数据覆盖闸门；结论仍以本页列出的实测范围为准）
>
> 本文是 D→L 数据的当前索引。旧实验报告保留完整过程，但不能脱离本文的有效性分级单独引用。

## 1. 数据有效性规则

引用 D→L 数据时按以下优先级处理：

1. 当前代码和最新提交对应的实验报告优先；本轮基线以 `610f4b3`（hybrid cache 修复）及其父提交后的代码为准。
2. “上下游都走主仓引擎”必须同时满足：上游使用 `ModelManager.load_layer_range()` / `forward_layers()`，下游使用 `LlamaCppEngine.forward_layers_from_hidden()`。
3. 使用裸 `llama_cpp`、实验区手写前向或旧 runner 的结果可以作为诊断/历史数据，但不能作为最终主仓端到端性能基线。
4. 性能数字必须同时记录模型、层数、切点、dtype/量化、prefill、decode、线程、重复次数、设备和构建指纹；不同口径不得横向比较。
5. 正确性判据与速度判据分离。D→L 的速度不占优不等于容量收益不存在；容量收益必须单独按“单设备必需驻留权重字节”计算，并注明受控预算不等于物理 OOM。
6. 引用“档位 / 速度”前必须核对**实际生效**的 dtype 与开关：主仓层流水线的 `quant_type="int4"` **静默回退 fp16**（按 key 手工物化、不走 `BitsAndBytesConfig`）；上游 compile 在参数量 `< COMPILE_MIN_PARAMS`（1.5B）时被规模门禁用。混合精度档位的 `capacity_gain_x` 只在同精度口径下解释（实测混合精度 < 1×）。
7. **纯 L→L（全部由 llama.cpp 承载，无 torch 参与推理）的「主仓引擎」口径**（★ 2026-09-23 补 §10.1）：
   上游/中段使用 `src/llama_keep_head.py` 的 `KeepHeadUpstream`（配主仓自建的**补丁版 llama.cpp shim**，
   源码在 `scripts/model_tools/keep_head_shim/`，构建见 `scripts/model_tools/build_keep_head_shim.ps1`）；
   末段使用 `ShimTailRunner`（**同一份 shim，首选**）或 `llama_engine.LlamaCppEngine.forward_layers_from_hidden()`
   （pip 绑定，可用但属**另一套构建**）。段工件由 `scripts/cut_layers.py` 生成：
   `--keep-head N`（上游段）/ `--k K1 --end K2`（中段）/ `--k K`（末段）。
   ⚠️ **混用两套构建时必须显式取齐 `flash_attn` 与 batch 线程**，否则近并列的 top-1 会被翻转
   （实测：同一 head 段下 pip 末段 1/32、取齐后 32/32，见 §10.7）；记录里应逐段写出实际 runner 与
   构建标识（§10.2 待办）。

## 2. 当前成立的事实

### 2.1 Windows Triton

README 中“缺失算子只能走 WSL2，Windows 不能用 Triton/fla”已经过时。当前事实是：

- Windows 原生 `torch.compile` 已用 `triton-windows==3.8.0.post28` 实测可用，依赖声明在 `requirements-compile.txt`。
- Windows 启动编译路径还必须设置 `PYTHONUTF8=1`；缺失时可能静默回退 eager。
- WSL2 Ubuntu-22.04 + CUDA/fla 仍是另一条已实测路径，不再是 Windows 的唯一可用路径。
- `flash-linear-attention`/fla 的具体算子收益不能从“compile 可用”直接推出，仍须按模型和算子单独验收。

### 2.2 D→L 正确性与主仓引擎接线

| 证据 | 条件 | 结论 | 有效性 |
|---|---|---|---|
| `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-swap-ld-2026-09-19.json` | qwen2.5-0.5B，12+12 层，gen=32；上游 `model_module`，下游 `llama_engine` | D→L `47.501 ms/step`，与纯 llama.cpp 整模 token 序列一致 | **当前唯一可引用的“两端主仓引擎”端到端速度样本** |
| `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-mainrepo-engine-2026-09-18.json` | qwen2.5-0.5B，主仓 `forward_layers`，CUDA f16 | 1.1109 ms/层；与 llama.cpp CUDA 1.064 ms/层相差 4.4% | 引擎微基准，不能替代端到端 |
| `docs/跨框架层接力-容量收益实测-2026-09-21.md` | qwen2.5-0.5B / qwen3.5-2B；上游主仓分层 + raw `llama_cpp` 下游 | 容量收益 1.568× / 1.547×；qwen3.5 **K=8/12/16/20 全部 32/32**（2026-09-21 重跑） | `capacity_only` / `raw_binding_probe`；不能作为双主仓端到端性能基线 |
| `build/cross-framework-layer-poc/out/p0/*.json`（驱动 `relay_mainrepo_both.py`） | 上游 `load_layer_range` + `forward_layers`，下游 **`llama_engine.forward_layers_from_hidden`**；两模型 × 切点 / 负载 / batch / 混合精度 | **27/27 逐 token 与同精度整模一致**（切点 9、负载 8、batch 4、混合精度 6） | **当前最完整的双主仓端到端正确性覆盖**；具体速度样本仍以 swap-ld 为准 |
| `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-relay-dl-monolithic-2026-09-19.json` | qwen2.5-0.5B；上游主仓，下游裸 `llama_cpp` | eager 约 28.1 ms/step | **部分主仓路径，不能作为两端主仓基线** |

`src/llama_engine.py` 已提供 `forward_layers_from_hidden()`；因此“主仓下游尚未接线”是历史结论，必须停止继续引用。当前仍需要补的是：在该公共接口上用 qwen3.5 hybrid 重新跑完整 D→L 矩阵，并把报告、模型摘要和构建指纹固定下来。⇒ **已于 2026-09-21 结清**：两模型 × 切点 / 负载 / batch / 混合精度共 **27 次全部逐 token 一致**，工件 `build/cross-framework-layer-poc/out/p0/*.json`（每条含模型、切点、层数、dtype、commit 与整模/裁层工件 SHA），明细见《跨框架层接力-容量收益实测-2026-09-21》§9。

### 2.3 D→L 的实际价值

- 速度：单机 CUDA 上，完整 llama.cpp GPU 路径通常更快；D→L 不应宣传为通用提速方案。
- 容量：两段切分把单设备必需驻留权重降到两侧最大段，qwen2.5-0.5B 和 qwen3.5-2B 的实测收益约为 1.568× 和 1.547×。
- 可运行性：同一受控 CUDA 预算 3.0 GB 下，整模拒绝而上游 12 层通过；这是受控分配预算证据，不得写成物理显存 OOM。
- 精度：上游使用 PyTorch 全精度、下游使用 GGUF 量化是有意义的“不完整量化”路径。验收必须与同一份下游量化整模对拍，不能拿量化下游与 f16 整模直接比较输出质量。

## 3. 失效或只能作历史的数字

| 旧表述 | 处理 |
|---|---|
| “WSL2 才能用 Triton/fla” | 删除；改为 Windows `triton-windows` 已实测，WSL2 为并行路径 |
| “D→L 生产准入仍 fail-closed，因为速度不占优” | 删除；当前合同按正确性证据准入，速度只影响默认路由倾向 |
| “21.3 s / 333 ms/步是当前最优主仓基线” | 降级为历史优化链；该组包含旧手动路径和不同对照口径 |
| “下游 `llama_engine` 没有 hidden 续算入口” | 删除；`forward_layers_from_hidden()` 已存在 |
| qwen3.5 D→L `2/32` | 标为 2026-09-21 cache 修复前现象；修复后正确性回归为 32/32，`K=8/12/16/20` 四个切点也已完成正确性重跑；这不等同于多轮性能切点闭环，后者仍待新 gate 复核 |
| qwen3.5 tied 末节点无法加载 | 标为已由 B16 修复；不要继续列为未修工程缺口 |
| P0 CPU 上游切点扫描得出的“不接力最快” | 仅适用于 CPU 上游旧口径；GPU 上游和主仓端到端必须单独引用 |
| “L→L 可以直接用主仓 `forward_layers_to_hidden()` 当上游” | **否证（2026-09-21）**：pip 绑定的 `llama_get_embeddings_ith` 返回的是 `output_norm(H)`（实测 vs `RMSNorm(H)*model.norm.weight`：`rel_err=0.0018` / `cos=0.999998`），比层接力上游所需的 hidden **多一次归一化** ⇒ L→L 端到端 `first_mismatch=2`。**✅ 同日已由补丁版 keep-head 通道解决**（见 §4 P2「多段拓扑」）：`--path l2l_keep_head` / `d2l2l_keep_head` 实测 **32/32**；`l2l_llama`（旧通道）保留为 fail-loud 反例 |

## 4. 实验基线（P0 已于 2026-09-21 完成）

### P0：主仓双引擎 D→L 重跑 —— ✅ 已完成（2026-09-21）

固定 qwen2.5-0.5B 与 qwen3.5-2B 两个模型，使用同一套公共接口：

- 上游：`ModelManager.load_layer_range()` + `forward_layers()`；
- 下游：`LlamaCppEngine.forward_layers_from_hidden()`；
- 工件：裁层 GGUF 由 `scripts/cut_layers.py`（+ `llama-quantize` 造 Q4_K_M）生成，JSON 内记录源模型与工件 SHA-256、层范围、commit 与运行环境；
- 档位：f32 hidden + 下游 f16、上游 f32 + 下游 Q4_K_M、上游 NF4 + 下游 Q4_K_M；
- 负载：prefill 32/128/512，decode 32/64/256，单序列和 batch 2/4；
- **结果：27/27 逐 token 与同精度整模一致**（切点 9、负载 8、batch 4、混合精度 6）；驱动 `build/cross-framework-layer-poc/relay_mainrepo_both.py`（`p0_matrix.ps1` / `p0_quant.ps1`），数据 `out/p0/*.json`，明细见《跨框架层接力-容量收益实测-2026-09-21》§9；
- 通过条件：同精度整模对照逐 token 一致；compile 若造成分叉，只能作为性能实验，不能进入一致性路径。

**P0 执行中新增的引用规则（引用档位/速度前必读）**：

1. `--upstream-quant int4` **不生效**（`load_layer_range()` 按 key 手工物化、不走 `BitsAndBytesConfig`，dtype 实测仍 fp16）⇒ 引用“上游档位”必须核对实际 dtype；真 NF4 需显式把 `nn.Linear` 替换为 `Linear4bit`（本轮 qwen2.5 84 层 / qwen3.5 93 层）。
2. 上游 compile 被规模门禁用（0.5B/2B < `COMPILE_MIN_PARAMS` 1.5B）⇒ 本矩阵是 **eager 上游**口径，不含 compile 收益。
3. **混合精度下容量收益 < 1**（实测 0.2722–1.2359×）：`capacity_gain_x` 只在**同精度**口径下解释。
4. 主仓下游 `forward_layers_from_hidden()` 在 M-RoPE（qwen3.5，`n_pos_per_embd=4`）上逐 token 一致；该契约目前**只有端到端证据、尚无单测固定**。
5. 下游是 CPU-only 绑定 ⇒ **prefill 是主要开销**（K=12 下游 prefill：32 token ≈ 1.5 s、512 token ≈ 5.7 s），且该计时含首次 ubatch 图开销。

**P0 未覆盖**：3 段及以上外推；跨机 SSH/RPC、弱网、长时 decode；下游 GPU offload；上游 compile 档；混合精度只做 K=12 且判据仅为 32 步贪心单 prompt（不构成质量结论）。

### P1：统一实验驱动与报告 schema —— ✅ 已完成（2026-09-21）

把裸 runner、主仓引擎和 L→L 对照统一到一个只读实验驱动，强制写入 relay experiment record：模型身份、权重格式、量化、层布局、引擎接口、commit、环境锁、设备画像和判据。报告必须区分 `mainrepo_end_to_end`、`raw_binding_probe`、`capacity_only` 三类，避免以后再把不同层级数字混成一张表。

**交付物**：

- `scripts/relay_experiment.py` —— 只读统一驱动，四条链路一条命令：`d2l_mainrepo`（主仓下游端到端）/ `d2l_raw_binding`（裸 `llama_cpp` 下游探针）/ `l2l_llama`（L→L 对照）/ `capacity_scan`（容量专项）。写入前必须过 schema 校验（fail-closed）；`--dry-run` 不加载模型即可预检链路与字段。
- `schemas/relay-experiment-record.schema.json` —— 记录 v1。`kind` 三类互斥；schema 负责结构与字段存在性，`src/relay_experiment_record.py` 的接口注册表负责 **kind/path/engines.*_iface** 的语义一致性（错标直接被拒）。
- `src/relay_experiment_record.py` —— 记录构建/校验与接口→(kind, path) 映射；未登记的新链路必须显式登记才能写记录。
- `tests/test_relay_experiment_record.py` —— 27 项守卫（schema 自检、kind/path/接口错标拒绝、四条链路 dry-run、L→L 通道守卫）。

**落地实测（同代码基线）**：四条链路各出一条记录（`build/relay-records/*.json`），kind/path 由接口自动判定：`capacity_only/capacity_scan`（qwen2.5 K=12，容量 1.568×）、`mainrepo_end_to_end/d2l_mainrepo`（qwen2.5 K=12，32/32）、`raw_binding_probe/d2l_raw_binding`（qwen3.5 K=8，32/32）、`raw_binding_probe/l2l_llama`（qwen2.5 K=12，**fail-loud 默认拒绝**，见下）。

**P1 暴露并已固化为守卫的工程约束**：

1. **L→L 上游通道语义不成立**：pip 绑定的 embeddings 通道 = `output_norm(H)`（证据 `scripts/relay_diag_head_norm.py`，复现命令见该脚本 docstring），L→L 端到端 `first_mismatch=2` ⇒ 驱动默认 `fail-loud`，只有 `--allow-normed-upstream` 才放行，且记录里写 `handoff.upstream_applies_output_norm=true`。正确做法是 PyTorch 上游或自建 llama.cpp 的 keep-head / 层输入通道。
2. **下游 hidden 的形状契约**：各分支一律吃 2D `[n_tokens, n_embd]`、返回 `[n_tokens, n_vocab]`。这条曾是真实 bug（raw 分支返回 ctypes 指针 ⇒ 全 0 输出；L→L 分支把特征维当 token 数 ⇒ 索引越界），已由测试与实现共同固定。

### P2：自适应切点与容量/速度双目标 —— ✅ 已完成（2026-09-21）

> 本节是过程记录；**结论与交接见 §7「P2 收口」**。

复用设备画像和评分，按候选节点的单位层耗时、可用内存、网络带宽、首 token 延迟和稳定性求解切点。目标函数同时输出：

- `capacity_feasible`：每段权重与不可分层张量能否装下；
- `latency_estimate`：prefill/decode 的端到端估计；
- `risk_penalty`：弱网、热降频、重试和工件不可得性。

先做 qwen2.5 的 2 段 K 扫描，再做 qwen3.5 hybrid 的合法切点约束（`full_attention_interval` 整倍数），最后才扩到 3 段。手工固定切点只保留为对照。

**交付物**：

- `src/relay_cut_objective.py` —— 求解器：`legal_cuts()`（合法切点，含 `cut_multiple` 硬约束）、`plan_relay_cut_n_segments()`（n 段；容量 + 延迟 + 风险三输出，具名拒绝）、`fit_segment_profile()` / `fit_two_segment()`（从实测回归"固定开销 + 每层耗时"）。与既有 `pipeline_capacity.solve_pipeline_capacity`（纯容量放置）、`relay_planner.plan_relay_cut`（2 段保守切点）分工互补。
- `scripts/relay_cut_plan.py` —— 切点重搜报告：读同一次扫描的端到端记录 → 拟合 → 求解 → 与实测最优对比 → 落可复算报告（`build/relay-records/cut-plan-*.json`）。
- `tests/test_relay_cut_objective.py` —— 20 项守卫（合法切点/对称最优/容量不可行/三段连续覆盖/风险进目标/拒绝路径/拟合还原系数/容量核算/候选空间与画像边界/闭环）。

**闭环结果（同代码基线，本机 RTX 4060；权重取纯延迟，容量来自设备画像）**：

⚠️ **2026-09-21 补测（P2 收口）**：原先 qwen2.5 的实测只覆盖 K=4/8/12/16/20，而全域预测切点
K=1 **落在实测范围之外**（`predicted_cut_in_measured_range=false`）。现已补测 K=1/2/3
（新生成 `cut-k1/k2/k3` 工件）并**统一 warmup 口径**（`--warmup 3`）重跑全部 8 个切点：

| 模型 | 合法切点 | 拟合 r²（上游/下游） | 全域预测切点 | 实测最优 | 闭环判据 |
|---|---|---|---|---|---|
| qwen2.5-0.5b | K=1/2/3/4/8/12/16/20（步长 1） | 0.975 / 0.857 | K=1（**已在实测范围内** ✅） | K=3 | ❌ `fitted_model_disagrees_with_measured_optimum` |
| qwen3.5-2b | K=8/12/16/20（**步长 4 硬约束**） | 0.791 / 0.958 | K=20 | K=20 | ⚠️ 单轮局部结果，待多轮复核 |

实测（decode 每步 ms；`--warmup 3`；8 个切点**全部** per-token argmax 通过）：

| K | 上游 | 下游 | 合计 |
|---|---|---|---|
| 1 | 7.37 | 31.34 | 38.71 |
| 2 | 10.46 | 36.99 | 47.45 |
| 3 | 9.02 | 24.18 | **33.20** |
| 4 | 12.83 | 27.78 | 40.61 |
| 8 | 16.92 | 19.85 | 36.77 |
| 12 | 25.42 | 17.62 | 43.04 |
| 16 | 33.08 | 13.93 | 47.01 |
| 20 | 34.18 | 10.12 | 44.30 |

记录：`build/relay-records/cut-plan-qwen25-05b.json`（闭环报告）、
`build/cross-framework-layer-poc/out/p0/qwen25-05b-k*-b1-p32-g32.json`（同批扫描，身份一致）。

**多轮取中位（P2 遗留①：补足数据，2026-09-21）**：8 个切点 × **5 轮**＝40 次（全部 per-token
argmax 通过），记录 `build/cross-framework-layer-poc/out/p0-repeat/r*-k*.json`；分析工具
`scripts/relay_cut_model_analysis.py`（含 7 项判据测试）：

| cut | 轮次 | 上游中位 | 下游中位 | 合计中位 | ±（max−min 半宽） | 相对离散 |
|---|---|---|---|---|---|---|
| 1 | 5 | 6.20 | 27.10 | **33.29** | 5.97 | 35.85% |
| 2 | 5 | 7.86 | 26.45 | 34.31 | 2.65 | 15.43% |
| 3 | 5 | 10.02 | 25.76 | 35.79 | 2.83 | 15.83% |
| 4 | 5 | 12.52 | 24.98 | 37.51 | 2.71 | 14.47% |
| 8 | 5 | 18.55 | 21.26 | 39.81 | 2.39 | 12.02% |
| 12 | 5 | 23.40 | 17.27 | 40.67 | 7.27 | 35.76% |
| 16 | 5 | 30.59 | 13.46 | 44.06 | 5.30 | 24.07% |
| 20 | 5 | 35.65 | 10.26 | 45.91 | 5.00 | 21.78% |

**结论（P2 最终版）**：

1. **本实验的测量噪声很大（最大相对离散 36%）⇒ 单轮扫描的"最优切点"完全不可信**。单轮曾得出
   实测最优 K=3；多轮取中位后 **total 随 K 单调递增**（分段中位数相加，线性拟合 slope **+0.63**、**r² 0.97**）
   ⇒ 实测最优是 **K=1**，"K=3 最优"只是噪声。
2. **历史补测曾得到中位数拟合结果**：当时用中位记录重跑 `relay_cut_plan.py` 得
   `predicted_cut=1`、`measured_optimum=1`、`passed=true`；但该记录是合成输入，不是端到端可追溯的决策证据。
   当前工具要求直接从带 `record_origin.kind=raw_measurement`、实验身份和 warmup 的原始多轮记录生成分析报告，
   因此这条历史结果不能单独宣称当前闭环完成。
   历史中位合成记录 `build/relay-records/cut-plan-qwen25-05b-median.json` 仅保留为审计材料；
   现在的分析器拒绝 `record_origin.kind=median_of_repeats` 作为原始输入，规划器只消费带来源、身份、warmup
   和多轮闸门的分析报告。
3. **当前数据未检出显著内部最优**：求解器的成本模型
   （`_estimate_latency` 的 `fixed + per_layer × layers`，`SegmentProfile.ms_fixed_decode`）
   对 K **数学上就是线性**，而实测分段中位数相加后的线性度很高（总耗时 r² 0.97；分段拟合 r² 0.996/0.999）。
   在当前切点和噪声水平下**未检出统计显著的内部最优**，现有模型暂可用于容量导向的规划；这不是对所有未测切点
   或更低噪声环境中不存在内部最优的证明，若未来检出显著内部最优再补非线性项。
4. **但切点选择本身仍然重要，收益体现在"端点方向"**：K=1（33.29）vs K=20（45.91）差 **1.38×**，
   远超噪声；且**上游每层 1.54 ms/层（torch/GPU）比下游每层 0.91 ms/层（llama/CPU）贵**
   ⇒ **应尽量把层放在下游一侧**，与"L 档为主"的既有策略一致。

   ⚠️ **场景标注（2026-09-23 补）**：本条结论**只在本节的实验口径内成立**，与 `README.md` 切点扫描节的
   「层推给上游」**不可并列比较** —— 两者模型、量化、单序列口径与测量工具都不同：

   | 来源 | 上游边际 | 下游边际 | 结论 |
   | --- | ---: | ---: | --- |
   | 本节（`relay_cut_model_analysis`，K=1..20，r² 0.996/0.999） | 1.54 ms/层 | 0.91 ms/层 | 放下游 |
   | `README.md` 切点扫描（`CORE-RELAY-XFRAME-02-p0-corrected`，单序列 gen=64，f16 裁层） | 2.48 ms/层（r²=0.944） | 6.43 ms/层（r²=0.991） | 推上游 |

   上游差 1.6×（可能与 `torch.compile` 是否启用有关），而**下游差 7×，用现有信息无法解释**
   ⇒ 两条都**不能**当作普适结论。**待办**：用**同一把尺**（同一模型、同一量化、同一 gen、同一分析脚本）
   重测一组作为唯一口径；在那之前，路由决策应以**容量**为准，不要以"哪侧每层便宜"为准。

**工具口径已收紧**：`relay_cut_model_analysis.py` 默认要求每个切点至少 3 轮、轮数平衡且所有
per-token argmax 记录通过；不满足时返回非零并拒绝生成可用于决策的报告。`total_median_ms` 定义为
上下游分段中位数之和，另列 `median_of_totals_ms`，避免把两个不同统计量混用。

**（保留）合法切点是硬约束**：qwen3.5 的切点必须落在 `full_attention_interval`（=4）的倍数上，
否则裁层 GGUF 层类型错位、无法加载。求解器把它做成 `cut_multiple`，缺省 1（dense 模型）。
hybrid 的每层成本不均匀（r² 0.79）也指向同一处修正方向：按层类型分组拟合，或把实测切点直接当
离散候选（求解器已支持只在这些点上比较）。

**（保留）合法切点是硬约束**：qwen3.5 的切点必须落在 `full_attention_interval`（=4）的倍数上，
否则裁层 GGUF 层类型错位、无法加载。求解器把它做成 `cut_multiple`，缺省 1（dense 模型）。
hybrid 的每层成本不均匀（r² 0.79）也指向同一处修正方向：按层类型分组拟合，或把实测切点直接当
离散候选（求解器已支持只在这些点上比较）。

**3 段的现状（P2 收尾说明）**：求解器已支持任意段数（`plan_relay_cut_n_segments` 的 n 段枚举 + 首段 embedding / 末段 lm_head 归属 + 单测覆盖）。**执行层已于同日打通**，见下节「多段拓扑」。

### P2 续：多段拓扑（1 个 torch 上游 + n 个 llama 下游）—— ✅ 已打通（2026-09-21）

**问题**：链式拼接要求**每个中间段都能"吃 hidden、吐 hidden"**。前半（吃 hidden = `embd` 注入）早已可用；
后半（吐 hidden）在 pip 绑定里**只有 `embeddings` 通道**，而它返回 `output_norm(H)`（见 §3 的否证条目）。

**解（路线 C：补丁版 keep-head 通道）**：

| 环节 | 产物 | 说明 |
|---|---|---|
| 补丁 | `scripts/model_tools/patches/llama-cpp-layer-forward-api.patch`（marker `★ QLH 2026-09-20`，已登记在 `llama_quantize.lock.json`） | 导出 `llama_set/get_embeddings_layer_inp`（第 lid 层**输入** = 前 lid 层输出）与 `set_embeddings_nextn` / `get_embeddings_nextn_ith`（**末层输出，`output_norm` 之前**） |
| 构建 | 用 `android/app/src/main/cpp/llama.cpp`（含补丁，`cb5f7b7a8`）CMake + MinGW 构建 `libllama.dll`（CPU，~70 s） | 命令见 `scripts/model_tools/build_keep_head_shim.ps1` 的提示 |
| shim | `scripts/model_tools/keep_head_shim/qlh_keep_head.c` → `qlh_keep_head.dll` | ⚠️ **必须走 shim**：`llama_context_params` 是按值传参的大结构体，用 pip 的 ctypes 声明调自建 DLL 会字段错位（实测 `Unsupported ctx type`）。shim 与被调 DLL 用**同一份 `include/llama.h`** |
| 绑定 | `src/llama_keep_head.py`（`KeepHeadUpstream`：`forward_tokens_to_hidden` / `forward_hidden_to_hidden`） | 纯 ctypes 调 shim；缺 DLL/符号/工件一律 **fail-loud**（绝不退回 embeddings 通道） |
| 驱动 | `--path l2l_keep_head`（两段）、`--path d2l2l_keep_head`（三段，需 `--mid-model` + `--mid-layers`） | 记录里 `engines.middle_iface` 参与链路签名 ⇒ 三段不会与两段 D→L 撞键 |

**语义验证**（`scripts/relay_diag_head_norm.py` 同族诊断，qwen2.5-0.5B）：
`mode=nextn`（head 裁层工件）与 `mode=layer_inp`（整模 + `cut_layer=K`）给出的 hidden **逐位相同**，
且与 PyTorch 上游 `rel_err=0.00214`（对比旧 embeddings 通道的 `rel_err=11.69`，改善约 **5000×**）。

**端到端实测（同代码基线，本机 RTX 4060，gen=32，prefill=5）**：

| path | 链路 | 结果 | 记录 |
|---|---|---|---|
| `l2l_keep_head` | llama(head12, keep-head) → llama(cut-k12) | **32/32** | `build/relay-records/l2l-keephead-qwen25-k12-b1.json` |
| `d2l2l_keep_head` | **torch(0..7) → llama(blk.8..15, keep-head) → llama(blk.16..23)** | **32/32** | `build/relay-records/d2l2l-keephead-qwen25-k8-16-b1.json` |

**三段多点扫描（4 组切点，全部 32/32）**（驱动脚本 `build/keephead/three_seg_sweep.ps1`）：

| K1 | K2 | 三段划分 | 结果 | 记录 |
|---|---|---|---|---|
| 4 | 16 | torch 0..3 / llama blk.4..15 / llama blk.16..23 | ✅ 32/32 | `build/relay-records/three-seg-k4-16.json` |
| 8 | 12 | torch 0..7 / llama blk.8..11 / llama blk.12..23 | ✅ 32/32 | `build/relay-records/three-seg-k8-12.json` |
| 8 | 16 | torch 0..7 / llama blk.8..15 / llama blk.16..23 | ✅ 32/32 | `build/relay-records/three-seg-k8-16.json` |
| 8 | 20 | torch 0..7 / llama blk.8..19 / llama blk.20..23 | ✅ 32/32 | `build/relay-records/three-seg-k8-20.json` |

**跨机（Relay TCP，loopback + SSH 隧道）**：新增远端角色服务与驱动链路 ——

| 件 | 作用 |
|---|---|
| `src/relay_transport.py::RelayTcpClient.request_hidden()` | **中间段往返**（HIDDEN → HIDDEN）；与既有 `request_token()`（末段，HIDDEN → TOKEN）配对 |
| `src/relay_transport.py::serve_relay_middle_connection()` | 服务端中间段语义（runner 提供 `request_hidden`）；与 `serve_relay_connection`（末段）并立，帧校验同等 fail-closed |
| `scripts/relay_mid_service.py` | 远端段服务：`--role middle`（keep-head，吃 hidden 吐 hidden）/ `--role tail`（llama_engine，吃 hidden 吐 token）；强制 **loopback 绑定**，跨机用 `ssh -L` 隧道 |
| `--path d2l2l_keep_head_net` + `--mid-endpoint host:port` | 三段链路里中段在远端；记录里 `engines.middle_iface = relay_transport.RelayTcpClient.request_hidden`（链路签名唯一，不与本机三段混表） |

实测（loopback，`build/keephead/net_three_seg.ps1`）：torch 上游 → **远端** keep-head 中段（blk.8..15）
→ 末段 llama（blk.16..23）= **32/32**（`build/relay-records/net-three-seg-k8-16.json`）。

**Android 段（同一 shim 语义的 JNI 版本）**：

* 现状核查：Android 既有的 `nativeLayerForwardHidden` 用的是 `llama_get_embeddings_ith`
  —— 与 pip 绑定同一缺陷（返回 `output_norm(H)`）⇒ **它当中间段会多一次归一化**。
* 新增：`nativeLayerForwardHiddenKeepHead`（C++ `qlh_layer_forward_keep_head_impl`，与
  `keep_head_shim/qlh_keep_head.c` **同语义**：nextn 通道 + 每个 token 都标 `logits=1`，
  否则 `t_h_nextn` 越界）+ Kotlin `layerForward(..., keepHead = true)`（默认 false 向后兼容，
  `-3` 映射为 `keep_head_nextn_unavailable` 并 fail-closed）+ 能力上报
  `keep_head_middle` / `middle_channel=keep_head_nextn`。
* **交叉编译已通过**（NDK `27.2.12479018`，`-DANDROID_ABI=arm64-v8a`）：产出
  `libqlh_llama_jni.so`（64.5 MB）。**真机数值验证待设备**（y700 不在线）——
  不因交叉编译通过就宣称 Android 段已验收。

**当前限制（引用前必读）**：① 上游/中间段的 `param_bytes` 记的是**工件文件字节**
（非 `llama_model_size`），容量判据请以实测为准；
② shim 由 MinGW 构建 ⇒ 运行时需同目录的 `libgcc_s_seh-1 / libstdc++-6 / libwinpthread-1 /
libgomp-1`（`libgomp-1` 来自 ggml-base 的 OpenMP，缺它会报 "Could not find module ... or one of its
dependencies"；已在构建脚本里固化）；③ 三段是**串行 hop**，每 hop 传激活
（896 维 f32 ≈ 3.5 KB/token）⇒ 延迟随段数线性增加，`latency_estimate` 已单列 `transfer_ms` / `hops`；
④ 跨机只验证到 **loopback + 隧道**这一层（真跨机还需远端到位与 SSH 隧道实测）；
⑤ Android 只到**交叉编译**；⑥ 三段多点扫描覆盖 4 组切点，未做 5 段以上与切点-容量的联合扫描。

### P3：激活与缓存数据流 —— 部分完成（多序列已实证；量化往返已实证；**线上压缩已上 wire（2026-09-23）**；传输-计算重叠未完成）

> 本节是过程记录；**结论与交接见 §5「P3 收口」**。

**① prefill 传整段 / decode 只传末位（已完成，且已显式化）**

链上每一段都是这个策略：prefill 一次给整段 hidden（让下游一次建立本地 KV/recurrent state），
decode 每步只给末位激活。现在它**写进记录**（`load.activation_policy =
prefill:full_sequence+decode:last_position`），引用任何带宽/延迟数字前先核对该字段。

**② 多序列 `seq_ids` / `positions` 显式绑定（已完成）**

| 件 | 内容 |
|---|---|
| shim | `qlh_kh_load(..., n_seq_max)` + 新入口 `qlh_kh_forward_embd_seq(embd, n_tokens, n_past, n_seq_id, seq_ids, positions, out)`（三个数组可 NULL ⇒ 单序列向后兼容） |
| 绑定 | `KeepHeadUpstream.forward_hidden_to_hidden(hidden, n_past=…, seq_ids=…, positions=…)` —— 与 `llama_engine.forward_layers_from_hidden()` **同一契约**；`seq_id ≥ n_seq_max` 直接 fail-loud |
| 驱动 | 三段/两段 keep-head 路径按 `--batch` 传显式 seq/pos（prefill：`[b for b in range(B) for _ in range(L)]` / `[i for _ in range(B) for i in range(L)]`） |

实测（三段 K=8/16，gen=32）：**batch 2 ✅ 32/32、batch 4 ✅ 32/32**（判据 = 每条序列的贪心与整模逐 token 一致）。

⚠️ **回归教训（已固化为用例）**：改造多序列时曾把单序列的 `n_past` 丢掉（NULL positions 时从 0 开始），
表现为**第二步 decode 直接失败**（`tokens ... have inconsistent sequence positions`）。
现在 `qlh_fill_explicit` 在 `positions == NULL` 时回落到 `n_past + i`，并有
`test_single_sequence_positions_stay_consecutive_across_steps` 守卫。

**③ hidden 线上压缩（已上 wire，2026-09-23；原 A5）**

协议侧不再固定 f32：帧头 `flags` 的**低 3 位**表达档位（`none` / `f16` / `int8_block128` /
`int4_block128`），**保留位仍拒**、旧对端见到非零 flags 直接 `unsupported_flags` ⇒
**向后兼容、无需升 `RELAY_WIRE_VERSION`**（`flags=0` 与旧帧逐字节一致）。压缩只发生在上行 wire：
服务端按档位解回 f32 再喂 runner ⇒ **对段实现（shim / pip 引擎 / Android JNI）完全透明**。
实现：`src/relay_hidden_quant.py`（编解码）+ `src/relay_transport.py` 的
`RELAY_QUANT_CODES` / `quantize_upload` / `frame_hidden_bytes`；探针开关 `--hidden-quant`。

判据（**per-token argmax**，0.5B 两段 / prefill 32 / gen 32，与同精度整模对照）：

| 档位 | 判据 | 记录 |
| --- | --- | --- |
| `f16` | ✅ **32/32** | `build/relay-records/l2l-net-local-05b-2seg-quant-f16.json` |
| `int8_block128` | ✅ **32/32** | `build/relay-records/l2l-net-local-05b-2seg-quant-int8_block128.json` |
| `int4_block128` | ❌ **23/32**（first_mismatch=9） | `build/relay-records/l2l-net-local-05b-2seg-quant-int4_block128.json` |

⇒ **`f16` 与 `int8_block128` 可作 wire 压缩档；`int4_block128` 不满足 argmax 判据**（如实标 FAIL，
不作为可选档）。

**下行（服务端 → 客户端）也已启用**（同轮）：档位由服务端**单方面决定**
（`serve_relay_middle_connection(hidden_quant=)` / 服务进程 `--hidden-quant`，只有 `--role head|middle`
会回 HIDDEN），客户端按帧里的档位解压 ⇒ **两端可各自选档**。实测（0.5B，远端 head 段 + 远端 tail 段，
判据 per-token argmax）：

| 上行 / 下行 | 判据 | 记录（`build/relay-records/`） |
| --- | --- | --- |
| `none` / `none`（对照） | ✅ 32/32 | `l2l-net-local-05b-2seg-remotehead-none.json` |
| `f16` / `f16` | ✅ 32/32 | `l2l-net-local-05b-2seg-remotehead-f16.json` |
| `f16` / `int4_block128` | ❌ 23/32（first_mismatch=9） | `l2l-net-local-05b-2seg-downlink-int4.json` |

最后一行的**反向判据**价值：它的失败模式与上行 `int4` **完全一致**（23/32、first_mismatch=9）⇒
**反证"下行压缩确实作用在数值上"**（若下行档位被忽略，这一条会 PASS）。误差反馈仍未做。



`--hidden-quant none|f16|bf16|int8_block128`（在每一跳注入前量化、使用前反量化，语义同本地与跨机）：

| 档位 | 量化后模拟字节（896 维，非 wire） | 三段 gen=32 结果 | 记录 |
|---|---|---|---|
| `none`（f32） | 3584 | ✅ 32/32 | `three-seg-k8-16.json` |
| **`f16`** | **1792（0.5×）** | ✅ 32/32 | `three-seg-k8-16-hq-f16.json` |
| **`bf16`** | **1792（0.5×）** | ✅ 32/32 | `three-seg-k8-16-hq-bf16.json` |
| **`int8_block128`** | **924（0.26×，含每 128 维 1 个 f32 scale）** | ✅ 32/32 | `three-seg-k8-16-hq-int8_block128.json` |

判据是 **per-token argmax**（不是 cosine）；模拟字节写入
`device_profile.hidden_bytes_per_token_effective`。当前 `RELAY_WIRE_VERSION=1` 的
`HIDDEN`/`HIDDEN_SEQ` 数据区仍固定为 `float32_le`，实际线上字节写入
`metrics.hidden_wire_bytes_per_token`，且 `metrics.hidden_wire_compression=false`。
因此本节量化结果只能说明数值往返，不构成线上带宽收益证据。
⚠️ **不得外推**：这只是 gen=32、单 prompt、f16 下游、3 段的结果；长序列/多 prompt/弱网下的
误差累积与"误差反馈"回路都还没做（P4 会接量化边界）。

**④ 预取 / 传输与计算重叠（未实现；当前只有估算）**

用已有实测即可给出可重叠上限：三段每 hop 传 896×4 = 3.5 KB/token，同机 loopback
串行化 ≈ 0.03 ms、RTT ≈ 0.1 ms 量级，而每步计算 ≈ 70 ms（上游 ~32 + 中段 ~18 + 末段 ~21）
⇒ 同机可重叠窗口 **< 1%**，与既有"通信/同步仅 0.34%"的结论一致 ⇒ **不同机重投同步微优化**。
跨机时该比例按 `传输时间 / 步时` 重算（`latency_estimate.transfer_*` 已单列），
**弱网下才值得做预取/压缩-计算重叠**，届时先测网络与解码计算的实际重叠窗口。

**P3 遗留项（本机部分已完成，2026-09-21 续）**：

| 项 | 状态 | 证据 |
|---|---|---|
| 长序列 × 多序列 | ✅ 完成 | 三段 K=8/16：prefill 128/512 × batch 2/4 **全 32/32**（`three-seg-k8-16-p*-b*.json`）；中段与末段的 `n_batch` 按 `batch × prefill` 构造（不够会撞 `GGML_ASSERT(n_tokens_all <= cparams.n_batch)`） |
| 跨机多序列 | ✅ 完成（loopback） | 新增 `HIDDEN_SEQ` 帧（payload 带 `seq_ids`/`positions`，meta 白名单校验）；远端 runner 不支持显式绑定则 `hidden_seq_unsupported` fail-loud。跨机三段 batch 1/2/4 **全 32/32**（`net-three-seg-k8-16-b*.json`） |
| 跨连接串味（真实 bug） | ✅ 修 | 同进程服务多条连接时新连接从位置 0 开始会与上一条的 KV 位置冲突（远端 `runner_failed`）⇒ shim 新增 `qlh_kh_reset()`、绑定 `reset()`、worker 加 `reset` op、`relay_mid_service` 每连接 `reset()` |
| Android 多序列 | ✅ 交叉编译通过 | `nativeLayerForwardHiddenKeepHeadSeq`（显式 seq/pos）+ Kotlin `layerForward(..., seqIds, positions)`；真机数值验证待设备 |
| 误差反馈 / 残差补偿 | ⛔ 未做 | 只做了 f16/bf16/int8 的**单向**量化往返（见下表） |
| 真实弱网下的预取/重叠 | ⛔ 未做 | 同机估值 < 1%（见上文④）；弱网需真实链路或多机注入延迟 |

### P3 续二：弱网模拟 + Surface 中段（2026-09-21）

**① 弱网模拟**（驱动侧注入，远端不改）：`--mid-bandwidth-mbps` / `--mid-extra-latency-ms`。
两个口径都先踩了坑，记录在此以免重犯：

1. `--hidden-quant` 起初只压「中段 → 末段」那一跳（本机内存拷贝）⇒ 弱网下 f16/int8 **毫无收益**
   （实测 132.3 → 131.1 ms）。跨机的瓶颈在线上 ⇒ 改为压**进出远端的两个方向**。
2. 旧实验曾把“按目标精度估算的字节”当成线上字节；但当前 `RELAY_WIRE_VERSION=1`
   的帧数据区仍固定为 `float32_le`，`_quantize_hidden` 也会回转为 f32 后再发送。
   因此旧记录中的 f16/int8 线路字节和弱网收益是**模拟估算，不是实际 wire 证据**，已由本轮
   驱动修正为按真实 payload 长度统计；真正的 wire quantization 需要后续协议版本单独实现。

旧模拟结果（三段 K=8/16，prefill 5，gen 32，判据 per-token argmax，**仅作数值往返记录**）：

| 链路档位 | 中段耗时 | 模拟线路字节（上/下） | 相对 f32 |
|---|---|---|---|
| bw 1 Mbps + 20 ms，**f32** | 133.0 ms | 129024 | 1.00× |
| bw 1 Mbps + 20 ms，**f16** | 104.7 ms | 64512 | 1.27× |
| bw 1 Mbps + 20 ms，**int8_block128** | **89.7 ms** | 33264 | **1.48×** |
| bw 0.2 Mbps + 100 ms，**f32** | 514.8 ms | 129024 | 1.00× |
| bw 0.2 Mbps + 100 ms，**f16** | 371.5 ms | 64512 | 1.39× |
| bw 0.2 Mbps + 100 ms，**int8_block128** | **300.5 ms** | 33264 | **1.71×** |

**口径修正**：上表的“模拟线路字节/相对 f32”是旧驱动按目标精度模拟的结果，不能用于宣称
真实弱网收益。当前驱动会记录 `metrics.hidden_wire_dtype=float32_le`、
`metrics.hidden_wire_compression=false`，弱网实测需要在真正的 wire quantization 协议落地后重跑。

**② Surface（Windows x86_64）作为中段**：

- 之前判定"本机 SSH 到 Surface 会 hang"是**我用了错的用户名**（`SgfKrc@` → `Permission denied
  (publickey,password,keyboard-interactive)` → 回落键盘交互 ⇒ 命令挂起）。正确为
  `surface@100.100.52.106`（免密）。
- 环境：Python 3.12.10 + numpy 2.5.2，默认 shell 为 cmd。
- 传件：x86_64 产物 9 个 DLL（`libllama.dll` / `ggml*.dll` / `qlh_keep_head.dll` / MinGW 运行时）
  + 脚本 + `qwen25-05b-f16-mid8-16.gguf`（517 MB）；启动脚本 `build/keephead/surface_mid_service.bat`。
- 结果：**b=1 → 32/32**（up 12.0 / **mid 51.1** / down 13.2 ms）、**b=2 → 32/32**（19.7 / 71.2 / 17.6）。

**三处中段（同 8 层、batch 1）对照**：

| 中段设备 | 中段耗时 | 通道 |
|---|---|---|
| Surface（x86_64 Windows） | 51.1 ms | SSH 隧道 + Relay TCP |
| y700（ARM64 Android） | 64.4 ms | SSH 隧道 + Relay TCP |
| 本机 loopback | ~35 ms（未见网） | 本机 TCP |

### P3 续三：量化误差测量 + 多跳（四段）拓扑（2026-09-21）

**① 量化误差曲线**（`--track-quant-error`，按用户裁定"**先只测量，不改链路**"）。
链路为三段（K=8/16），gen 32，判据 per-token argmax —— **9 档全 32/32**（记录 `quantcurve-*.json`）：

| prefill | f16 rel_mean | bf16 rel_mean | int8_block128 rel_mean |
|---|---|---|---|
| 128 | 8.88e-05 | 1.40e-03 | 2.15e-02 |
| 512 | 8.81e-05 | 1.40e-03 | 2.21e-02 |
| 1024 | 8.79e-05 | 1.40e-03 | 2.24e-02 |

三点结论：
1. **不随序列长度增长**：prefill 128 → 1024，f16 8.88e-5 → 8.79e-5、int8 2.15e-2 → 2.24e-2；
2. **不随步数累积**：每档"前半段 vs 后半段"同量级（int8 p128：2.15e-2 vs 2.16e-2）；
3. 精度阶梯：bf16 比 f16 差约 16×（8 位尾数），int8_block128 比 f16 差约 250×（≈1/127 量级）。

⇒ **当前不需要误差反馈**：误差既不随长度/步数累积，也不改变任何 token。残差补偿/自适应位宽
只有在更激进的压缩（int4/2bit）或更严判据（逐 logits 距离）下才值得引入 —— 那属于 P4 的精度预算。

**② 多跳拓扑（四段）**：`torch(0..7) → Surface(8..15) → y700(16..19) → 本机(20..23 + head)`

- 驱动侧：`--mid-endpoint` 改为可重复 + 新增 `--mid-stop`（每段结束层号）+ `_MiddleChain`
  （对外接口与单个中段完全相同 ⇒ 主循环零改动，hidden 依次穿链）；
- y700 那一跳用 **`mode=layer_inp` + `cut_layer=4`**（工件 `cut-k16` = blk.16..23，取工件内第 4 层
  的输入 = 原模型前 20 层输出）⇒ **不必生成新的区间工件**，用已有工件即可拼出四段；
- 结果：**b=1 → 32/32**（up 11.7 / mid 116.3 / down 9.2 ms）、**b=2 → 32/32**
  （`net-four-seg-k8-16-20-b{1,2}.json`）；
- `layer_layout` 自恰：upstream 8 + middle 12 + downstream 4 = 24 ✓。

**③ 方法论修正（我自己的验证缺陷，记录以免重犯）**：用 shell 变量残留（`$out`/`$v`）判断
"每档是否成功"，曾在循环里造成 **9 档里 8 档"看起来成功"实际没跑**（命令行未执行、变量保留了
上一轮的值）。改为：**每档先删旧记录 → 跑完只看记录文件是否存在 + 其中 `verdict.passed`**
（见 `build/keephead/run_quantcurve.ps1`）。跨机/多机实验尤其要守这条 —— 远端失败与本地误判
都会伪装成成功。

**仍未覆盖**：更激进量化（int4/2bit）下的误差反馈；多跳中"异构 kernel 逐比特一致性"的系统性对照。

### P3 续：真跨机 + Android 段（2026-09-21，y700 真机）—— ✅ 已达成

**设备**：`y700-1`（Lenovo Legion Y700，Snapdragon 8 Gen 3 / Android，Termux + SSHD 8022，
Python 3.14.6 + numpy 2.4.4）。主机侧用 `ssh -N -L 50180:127.0.0.1:50180 y700` 隧道
（`RelayTcpClient` 只接受 loopback，与本仓既有纪律一致）。

**arm64 侧（本机交叉编译 → 传到设备）**：

| 件 | 编译/位置 | 说明 |
|---|---|---|
| `libllama.so` + `libggml*.so` | NDK `27.2.12479018`，`-DANDROID_ABI=arm64-v8a`，用**含补丁**的 `android/app/src/main/cpp/llama.cpp`（`cb5f7b7a8`） | 39.5 MB；与本机 x86_64 shim 同源 |
| `libqlh_keep_head.so` | 同一 NDK clang 编译 `scripts/model_tools/keep_head_shim/qlh_keep_head.c` | 11 KB；**同一份 C 源**（语义天然一致） |
| 脚本 | `relay_mid_service.py` + `src/relay_transport.py` + `llama_keep_head.py` | 设备侧 `LD_LIBRARY_PATH=<bin> PYTHONPATH=.` |
| 工件 | `qwen25-05b-f16-mid8-16.gguf`（517 MB，blk.8..15） | 传到 `/sdcard/Download/QLH/models/` |
| 启动 | `build/keephead/y700_mid_service.sh` | `--role middle --mode nextn --n-batch 2048`（★ 2026-09-24：脚本里的 `--n-seq-max` 已由 **8 改为 1** —— llama.cpp 把 `n_ctx` 按 `n_seq_max` **均分**，本服务用于**单序列**层接力时 8 会把每序列预算压到 `n_ctx/8`，长 decode 会在中途报 `llama_decode rc=1`（"failed to find a memory slot for batch"）；多序列实验请显式传 `--n-seq-max N` 并把 `--n-ctx` 放大 N 倍） |

**设备侧冒烟**（`build/keephead/termux_smoke.py`）：加载 0.22 s、前向 61.9 ms、
**清 KV 后同位置重跑逐位一致**、多序列（4 token / 2 序列）通过。

**真跨机三段（中段在 y700）**：`torch(0..7) → [SSH 隧道] → y700(blk.8..15) → [隧道] → llama(blk.16..23)`

| 运行 | 结果 | 记录 | 上游 / **中段** / 末段（ms/步） |
|---|---|---|---|
| batch 1 | ✅ **32/32** | `net-y700-three-seg-k8-16-b1.json` | 12.3 / **64.4** / 13.0 |
| batch 2（多序列 `HIDDEN_SEQ` 帧跨真机） | ✅ **32/32** | `net-y700-three-seg-k8-16-b2.json` | 17.9 / **61.7** / 16.3 |

**读法**：Android 段的每步 64 ms 里同时含**两跳跨机往返**与**设备上 8 层 CPU 计算**；
batch 2 时中段几乎不涨（61.7 ms）⇒ 该段是**吞吐型**而非延迟型瓶颈。
这组数字是"跨机值不值得"的第一份真机依据（尚未做带宽受限与多节点对比）。

### P4：量化边界与精度预算 —— 首轮边界扫描（2026-09-21）

> 本节是过程记录；**结论与交接见 §6「P4 收口」**。

票面四个问题（上游全精度是否降低下游量化误差传播 / 下游量化的误差是否随切点累积 /
f32 hidden 的通信成本是否被 batch 或弱网放大 / 何时优先容量何时优先质量）本轮先回答**可测的部分**。

**本轮新增能力**（主仓实现，含回归测试 `tests/test_relay_p4_quant.py`）：

1. hidden 压缩新增 **`int4_block128`**：有符号 4bit + 每 128 元素一个 f32 scale，
   刻意只用 −7..7（不用 −8）以避免非对称偏置；线路字节 = `(w+1)//2 + blocks*4`
   ⇒ 896 维 **476 B/token**（对照 f16 = 1792、int8 = 924）。
2. 判据升级为 **argmax + 边距（`margin` = top1 − top2）**：argmax 仍是一致性判据；
   margin 回答"这个档位是否已经脆弱"。**整模 baseline 也记 margin**，可与接力路径对比。
3. **上游 `int8` / `int4` 变成真实档**（本轮补齐）：`int4` → bnb `Linear4bit(quant_type="fp4")`
   （对称 4bit），`int8` → bnb `Linear8bitLt`（LLM.int8）。两者都必须**显式替换**，
   因为 `load_layer_range()` 手工物化权重、不走 `BitsAndBytesConfig`（见"口径修正"）。
   替换数写入记录（`nf4_replaced_linears`），并新增 `quant_probe` = 参数张量包装类型清单，
   作为"量化档确实生效"的证据。回归测试钉住"不得再走 HF `quant_type`"。

**首轮矩阵**（三段 K=8/16，prefill 128，gen 32，单 prompt；记录 `p4m-*.json`。
`int8`/`int4` 上游档本轮已由 bnb 显式替换补齐 —— 见下"口径修正"）：

| 上游 | hidden | passed | relay margin | baseline margin | Δ | hidden 相对误差 |
| --- | --- | --- | --- | --- | --- | --- |
| fp16 | f16 | ✅ | 8.8125 | 8.807 | +0.06% | 8.9e-05 |
| fp16 | int8_block128 | ✅ | 8.4278 | 8.807 | −4.31% | 2.2e-02 |
| fp16 | **int4_block128** | ✅ | 8.2086 | 8.807 | −6.79% | 0.355 |
| **int8** | f16 | ✅ | **8.7469** | 8.807 | **−0.68%** | 8.8e-05 |
| int8 | int8_block128 | ✅ | 8.4212 | 8.807 | −4.38% | 2.2e-02 |
| int8 | int4_block128 | ✅ | 8.1019 | 8.807 | −8.01% | 0.354 |
| **int4 (fp4)** | f16 | ✅ | **7.6749** | 8.807 | **−12.85%** | 8.9e-05 |
| int4 (fp4) | int8_block128 | ✅ | 7.6525 | 8.807 | −13.11% | 2.1e-02 |
| int4 (fp4) | **int4_block128** | ✅ | **7.2171** | 8.807 | **−18.05%** | 0.348 |
| **nf4** | f16 | ✅ | **9.5629** | 8.807 | **+8.58%** | 8.9e-05 |
| nf4 | int8_block128 | ✅ | 9.6874 | 8.807 | +10.00% | 2.1e-02 |
| nf4 | int4_block128 | ✅ | 8.4386 | 8.807 | −4.18% | 0.357 |

**加压**（长 prefill × 最激进档；记录 `p4p-*.json`）：

| prefill | 上游 | hidden | passed | relay margin | baseline margin | Δ |
| --- | --- | --- | --- | --- | --- | --- |
| 512 | fp16 | int4_block128 | ✅ | 8.5858 | 9.2816 | −7.50% |
| 512 | nf4 | int4_block128 | ✅ | 8.9028 | 9.2816 | −4.08% |
| 1024 | fp16 | int4_block128 | ✅ | 8.5446 | 9.0867 | −5.97% |
| 1024 | nf4 | int8_block128 | ✅ | 9.7137 | 9.0867 | +6.90% |

**边界定位（多 prompt，本机三段 K=8/16，prefill 128，gen 32；记录 `p4x-*.json`）**：
上表是**单个** prompt 的结论，换 prompt 后边界立刻显形 —— 对照组（fp16×f16）6/6 通过，
最激进档（上游 fp4 + hidden int4）**6 个里翻了 3 个**：

| prompt | 对照（fp16×f16） | 基线 margin | 最激进（int4×int4_block128） | relay margin | Δ |
| --- | --- | --- | --- | --- | --- |
| natural-short | ✅ 1/1 | 6.1491 | ❌ **0/1** | 4.0134 | −34.73% |
| natural-long | ✅ 1/1 | 5.8149 | ❌ **0/1** | 3.8284 | −34.16% |
| code | ✅ 1/1 | 3.7555 | ❌ **0/1** | 3.2340 | −13.89% |
| symbols | ✅ 1/1 | 7.2553 | ✅ 1/1 | 6.9092 | −4.77% |
| repeat | ✅ 1/1 | 7.7688 | ✅ 1/1 | 6.2258 | −19.86% |
| mixed-zh | ✅ 1/1 | 6.8580 | ✅ 1/1 | 4.7972 | −30.05% |

**读法（本轮真正的结论）**：**相对衰减（Δ）不能预测翻转，绝对边距可以** ——
- `code` 只衰减 13.89% 就翻了（因为它的**基线边距本来就低**：3.7555），
- `mixed-zh` 衰减 30.05% 却没事（基线 6.8580）。
- 按**绝对边距**看分得很干净：**翻转档 relay margin ≤ 4.01（3.23 / 3.83 / 4.01）**，
  **未翻转档 ≥ 4.80（4.80 / 6.23 / 6.91）** ⇒ 本批数据下**阈值落在 4.0~4.8 之间**。
⇒ **margin 具有预测能力**：它能在 token 真的翻转之前指出"这个档位在这条 prompt 上已经危险"，
  这正是 argmax 做不到的（argmax 只在翻转后才变化）。

**四段跨机加压（torch → Surface → y700 → 本机，prefill 128，gen 32）**：

| 档位 | passed | relay margin | baseline | Δ |
| --- | --- | --- | --- | --- |
| f16 | ✅ | 8.8148 | 8.807 | +0.09% |
| **int4_block128**（线路两侧） | ✅ | 7.8579 | 8.807 | −10.78% |

⇒ **跨机 + 段数不是边界的来源**：四段链路（含真机 Surface/y700）+ 4bit hidden 压缩仍在安全区
（margin 7.86 ≫ 阈值 ~4.5）。**越界的是"上游 4bit（fp4）叠加 hidden 4bit"这条组合**。

**结论（诚实版）**：

1. **边界已定位，而单 prompt 看不见它**：单 prompt 下即使 hidden 4bit（元素误差 0.355）+ 上游 fp4、
   prefill 128→1024 也**不翻**；但**换 prompt 立刻显形** —— 最激进档 6 个 prompt 里翻 3 个
   （对照组 6/6 通过）。⇒ **单 prompt 的"全绿"从来不是安全证据**；
   能画出边界的是**绝对 margin**（翻转档 ≤ 4.01 / 未翻转档 ≥ 4.80 ⇒ 阈值落在 4.0~4.8）。
2. ⇒ **要分辨压缩档的优劣，只能靠 margin**（或更多样的 prompt）。这正好验证了
   "argmax 作判据 + margin 作观测量"的双轨设计：argmax 给**否决权**，margin 给**分辨力**。
3. **【本轮最有价值的发现】上游量化**表**比位宽更伤边距**：同是 4bit、上游驻留字节都是 302 MB，
   **fp4（`int4` 档）把 margin 压低 12.85%**，而 **nf4 反而抬高 8.58%** —— 相差约 **21 个百分点**；
   相比之下 hidden 侧从 8bit 换到 4bit 只变动约 **4.5 个百分点**。
   ⇒ 工程含义：**先挑量化表（nf4 优于对称 fp4），再谈位宽**；"4bit 一定比 8bit 差"这个直觉
   在本链路里并不成立。
4. **上游 int8 几乎无损**：`int8` 档在 f16 hidden 下 margin 仅 −0.68% ⇒ **LLM.int8 是"便宜的容量手段"**
   （容量降到 4bit 档同级的收益需要另行核验，但边距代价明显小于 4bit 档）。
5. **margin 随 hidden 位宽单调变薄**：f16 ≈ baseline（+0.06%）、int8 −4.3%、int4 −6.8%（fp16 上游）；
   长上下文下同样稳定在 −7.5% ~ −4% 区间。幅度温和，说明 4bit 压缩的余量还很大。
6. **待查（假设已被收窄）**：nf4 抬高 margin 曾疑似"4bit 噪声改变 logits 尺度"，但本轮
   发现 **fp4 反向压低** ⇒ 起作用的更可能是**量化表本身**（nf4 的 outlier-aware 非对称表），
   而不是"4bit"这个位宽 ⇒ 下一步可直接对比 nf4/fp4 的权重重构误差与 logits 尺度变化。
   `load_layer_range()` 手工物化权重 ⇒ **静默回退 fp16**（症状：int8/int4/fp16 三档 margin
   连小数位都完全相同）。现已改为 bnb 显式替换，四档 margin 立刻各不相同
   （fp16 8.8125 / int8 8.7469 / int4 7.6749 / nf4 9.5629），且 int4/nf4 的上游驻留字节
   降到 302 MB（fp16 511 MB）⇒ **档位真实生效**。
   ⚠️ 遗留小问题：`int8` 档的 `resident_weight_bytes.upstream` 仍报 510 MB（`_param_bytes`
   对 `Int8Params` 仍按 fp16 估算）⇒ 字节口径待修（不影响 margin 结论）。

**精度预算表（P4 第 4 问的答案；工具 `scripts/relay_quant_budget.py`，含 6 项判据测试）**：

12 个不同分布的 prompt × 5 个档位（每格 prefill 128、gen 32；判据 per-token argmax）：

| upstream | hidden | n | 翻转 | 无余量 prompt | margin_min（有余量子集） | 中位 | Δmin | 线路 B/token | 上游驻留 | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| fp16 | f16 | 12 | 0 | 3 | 5.0892 | 5.6833 | +0.05% | 3584 | 510.9 MB | **safe** |
| fp16 | int4_block128 | 12 | **8** | 3 | 3.9064 | 4.8836 | −35.85% | 3584 | 510.9 MB | tight |
| int4 (fp4) | f16 | 12 | 3 | 3 | 4.8971 | 5.8203 | −12.62% | 3584 | 302.1 MB | tight |
| int4 (fp4) | int4_block128 | 12 | 7 | 3 | 3.8284 | 4.7363 | −39.36% | 3584 | 302.1 MB | tight |
| nf4 | f16 | 12 | 3 | 3 | **5.2932** | 6.1803 | −24.56% | 3584 | 302.1 MB | **safe-headroom-only** |

**判据与用法**（已工具化，`--flip-threshold` / `--safety-margin` 可调）：

- 闸门：`margin_min(有余量子集) ≥ flip_threshold(4.5) + safety_margin(0.5) = 5.0`；
- **分层**：基线 margin 本身就低于闸门的 prompt（本例 3 条：`code` / `numbers` / `rare`）**没有任何
  预算空间**，单独记 `no_hd` 而不参与档位判定 —— 否则对照档 `fp16×f16` 也会被判 tight（实测踩到）；
- 建议在 `safe` / `safe-headroom-only` 档里按"线路字节 + 上游驻留"最省者给出，但
  `safe-headroom-only` **必须先确认基线 margin 达到闸门**，否则只能回退全精度 → **`nf4 × f16`（条件安全）**。

**结论（直接回答 P4 票面第 4 问"何时优先容量、何时优先质量"）**：

1. **可用的激进档只有"上游 4bit、hidden 不压"**：`nf4 × f16` 判 **safe-headroom-only**（上游驻留 511 → 302 MB，
   **−41%**）；`int4(fp4) × f16` 虽然**翻转全部发生在无余量 prompt 上**，但最坏余量 4.8971
   仍差 0.1 未过闸门 ⇒ 判 tight（**这两档的差别再次印证"量化表 > 位宽"**：同为 4bit 上游，
   nf4 5.2932 vs fp4 4.8971）；
2. **hidden 侧不能压到 int4**：`fp16 × int4_block128` 在 12 条 prompt 上翻 **8** 条 ——
   这是本轮最重要的修正（上一轮单 prompt 曾得出"hidden int4 也安全"的假象）；
3. **"上游 4bit + hidden 4bit"双压必然越界**（翻 7/12）；
4. **存在一类输入本身没有量化预算**：3 条 prompt 的**全精度接力基线** margin 已低于闸门 ⇒
   对这类输入只能走 fp16 全精度链路（或换更保守的路由），量化不是"少省一点"而是"必然出错"；
5. **容量优先**：只要输入不落在"无余量"类别，`nf4 × f16` 可同时满足质量与容量；
   **质量优先**：输入基线偏低时宁可不压缩。

**仍未覆盖（P4 后续）**：多 prompt 分布下的翻转率（当前是单 prompt，无法给出"什么时候会翻"）；
四段 × int4 的跨机加压；int8/int4 上游真实档；把 margin 衰减换算成"可接受的压缩档预算"。

最终输出应是按模型、切点和设备画像选择的策略表，不能全局武断把上游默认改成 f32 或 f16。

### P4.5：主节点动态选举与配套分布式管理 —— 立项（2026-09-21）

**动因（P4 收口后暴露的结构性问题）**：主节点身份由**部署时的环境变量**决定
（`src/config.py:450` 的 `NODE_ROLE` 默认值 + `CLIENT_MASTER_HOST` 静态配置；`bootstrap` 只管
"广播哪个 host"，不是选举），而主节点的职责 —— 设备画像、容量计划、层段合同、准入、
epoch fencing、任务图调度 —— **已经与 GPU 算力解绑**。⇒ 集群的管理角色被钉在一台
**与它实际能力无关**的机器上；而边缘集群的算力排序本来就会变（设备上/下线、负载波动、
热降频）。

**裁定（2026-09-21）**：主节点身份不该是永久的，应由集群**自选 CPU 算力最强的节点**，
并实现配套的分布式管理机制。

**票面与设计**：见 `主节点动态选举与分布式管理-P4.5立项-2026-09-21.md`。
2026-09-22 设计审计已否决“广播 + 本地 epoch + 先到先得”的自动选举草案：`presence_lease_*` 不作为主租约，
自动可写主必须持有严格多数 `QuorumCertificate`；无多数派只读。当前下一票为 `HA-CONTRACT-01`，
随后依次完成状态盘点、最小多数派租约、全写入口 fencing、分区测试、auto、交接、恢复和跨机证据。

**判据要点（G4，数值透明）**：主节点迁移**不得**改变数值面 —— 迁移前后同一 prompt 的
per-token argmax 必须一致（沿用 P3/P4 的判据与记录 schema）。P4.5 是**控制面**重构，
这正是 README「这是什么软件」一节里"控制面可以演进"的那部分。

### P5：长时、跨机和故障证据

在 P0/P1 完成后补做跨机 SSH/RPC、弱网、断线恢复、长时 decode、远端资产自动分发和 3 段容量外推。没有这些证据时，D→L 可称为“正确性和容量已验证的架构能力”，不称为完成生产性能验收。

**长时 decode（2026-09-24，同机 D→L 已完成）**：现有最长基线是 141 步，本轮把同机 D→L 拉到
**gen=1024**（约 7×）。口径与判据：`relay_experiment.py --path d2l_mainrepo --layers 1 --gen 1024`，
判据仍是 **per-token argmax 全程一致**（不得用 cosine 替代）。

- **PASS**：`verdict={passed:true, criterion:per_token_argmax, matched_runs:1/1, first_mismatch_index:null}`
  —— 1023 个 decode 步全程一致（耗时 121.9 s）。
- **无退化证据**：进程 RSS 在 32 步与 1024 步两次运行中几乎相同（**2.2269 GB vs 2.2267 GB**，差 0.2 MB）
  ⇒ 未见内存增长/泄漏。
- 逐段耗时（1023 步）：上游 `mean 7.05 ms / std 10.81`、下游 `mean 56.50 ms / std 35.79`、整模基线
  `mean 37.88 ms / std 11.69`（std 主要来自首步长尾；记录未保存逐 token 序列，故不做前后半段对比）。
- 证据：`build/relay-records/d2l-longrun-k1-g1024-20260924.json`。
- **跨机长时未做**：2026-09-24 各远端节点均离线（Surface `tailscale` 为 `offline, last seen 4h ago`、
  y700 离线 11 h）⇒ 属**卡外部条件**（需节点在线），不是本机未推进。

**n 段容量外推（2026-09-24，同机，本机部分已完成）**：P5 列的另一项。用**同机三段**
（head8 → mid8-12 → cut-k12；本机 middle/tail 服务、`--middle-endpoint` 走 loopback）验证正确性，
再用**真实工件字节**做容量外推：

- **正确性**：`l2l_net_3seg` **PASS 32/32**（`middle_hops=32`、`first_mismatch=None`；三段 shim/DLL 的
  sha256 全部一致 ⇒ 无构建混用）。证据
  `build/relay-records/l2l-net-local-05b-3seg-capacity-20260924.json`。
- **容量（0.5B f16；字节取实际工件文件）**：

  | 配置 | 各段 MiB | 最大段 MiB | 收益 = 整模 / 最大段 |
  | --- | --- | --- | --- |
  | 整模 | 948.1 | 948.1 | 1.000× |
  | head8 + tail8 | 492.9 + 720.5 | 720.5 | 1.316× |
  | head8 + cut-k12 | 492.9 + 606.7 | 606.7 | **1.563×** |
  | **head8 + mid8-12 + cut-k12（三段）** | 492.9 + 379.1 + 606.7 | 606.7 | **1.563×** |

- **结论（容量外推的一般式）**：`收益 = 整模字节 / max(各段字节)` ⇒ **增加段数本身不带来容量红利**，
  只有把**最大段**压下去才提升容量。0.5B 的瓶颈是**末段**（`cut-k12` = 606.7 MiB：层数更多且含非分层
  权重），所以**三段与两段（head8 + cut-k12）收益完全相同（1.563×）**，并与既有「两段 1.547–1.568×」
  口径一致。⇒ 要进一步提升收益必须**继续切末段**（需要 4 段拓扑，而当前探针只支持一个 middle 段）。

**压缩档的误差累积（2026-09-24，同机 512 步）**：A5 的压缩档验证只到 32 步，这里拉到 **gen=512**
（同一跳、payload 走 wire 编解码 + 服务端解码）：

| 档位 | gen=512（本轮） | gen=32（A5） |
| --- | --- | --- |
| `none` | ✅ **512/512** | ✅ 32/32 |
| `f16` | ✅ **512/512** | ✅ 32/32 |
| `int8_block128` | ✅ **512/512** | ✅ 32/32 |

⇒ **`f16` 与 `int8_block128` 的误差不随 decode 步数累积**（至少到 512 步）；`int4_block128` 在 32 步
即 23/32 失败，无需再测长时。证据 `build/relay-records/l2l-net-local-05b-quant-*-g512-ctx8192.json`。
（口径：三档都必须在 **ctx=8192** 的服务上跑 —— 见下条。）

**⚠️ 本轮发现的真问题（已定位并给出修复规则）**

1. **长 decode 的"ctx 硬边界"—— 根因是 `n_ctx` 被 `n_seq_max` 均分**（2026-09-24 定位）。
   症状：长 decode 在中途报 `runner_failed`；**此前所有 relay 实验都 ≤256 步，所以从未暴露**。
   定位手段：让 shim 透出 `llama_decode` 的**原始** rc（新增导出 `qlh_kh_last_error`；`qlh_kh_forward*`
   的返回码仍是 `-2`，**不破坏既有契约**）并打印**实际生效**的 ctx，于是看到：
   * `llama_decode rc=1` = llama.cpp 的 **"failed to find a memory slot for batch"**；
   * tail 段 worker 日志：`ctx ready: n_ctx=2048 n_batch=1024 (requested n_ctx=1024 … n_seq_max=8)`
     ⇒ **llama.cpp 把 `n_ctx` 向上取整后按 `n_seq_max` 均分** ⇒ 单序列可用槽位 = `n_ctx / n_seq_max`
     = 2048/8 = **256** ⇒ decode 到第 **256** 步连 `batch of size 1` 都找不到槽位而失败；
   * 与两个历史观测**完全吻合**：`4096/8 = 512` ⇒ 失败在 ~**481**（481 + 32 prefill ≈ 513）；
     `8192/8 = 1024` ⇒ 失败在 ~**1000**。
   ⇒ **规则**：单序列长 decode 必须 **`--n-seq-max 1`**（此时每序列拿到全部 `n_ctx`），或者把
   `--n-ctx` 放大到 `n_seq_max` 倍。`relay_mid_service.py` 现在启动即打印 `ctx_per_seq=`，并在
   `n_seq_max > 1` 时给出 WARN。**这不是 llama.cpp 的缺陷，是调用方把并发序列数设得比实际大。**

   **规则已落到代码（把这类坑一次性清掉，2026-09-24）**：
   * `KeepHeadUpstream.ctx_per_seq` = `ctx_per_seq_for(n_ctx, n_seq_max)`（纯函数、可单测），
     `n_seq_max > 1` 时构造即告警 ⇒ 不变式在**唯一入口**上可见，不再靠各脚本各自记着。
   * `relay_mid_service.py` 的 `--n-seq-max` **默认从 8 改为 1**（对齐 relay 的单序列用法；
     多序列实验显式传）。
   * `relay_experiment.py` 的 `_load_keep_head_segment` 增加**启动前**预算校验（fail-closed）：
     `batch × prefill + gen + 256 > ctx_per_seq` 直接拒绝启动，并提示所需的 `--n-ctx`；
     实测 `--n-ctx 4096` 通过、`--n-ctx 256`（需求 320）被明确拒绝。
   * 紧贴预算的余量统一为 **256**：`relay_l2l_net_probe.py`（两处 `+8`）、
     `relay_bitwise_probe.py`（`+8`）、`verify_xframe_longseq.py`（`+64`）。
   * **顺带修复一个既有 bug**（与本坑无关，是在验证时撞上的）：`relay_experiment.py:middle`
     只在三段分支赋值却在函数尾部无条件引用 ⇒ 2 段链路（`l2l_keep_head` 等）必然
     `UnboundLocalError`。修后 `l2l_keep_head` 实测 `passed=True tokens_match=True`。
2. **decode 失败会让服务进程直接退出**（已修）：`relay_mid_service.py` 以前在 `runner_failed` 后
   仍调 `runner.reset()`，而 worker 已不可用 ⇒ 异常冒到顶层 ⇒ 进程退出（表现为后续档位 2.2 s
   秒失败，与"模型算错"难以区分）。现在改为 **fail-closed 但不退出**：把 runner 标记为 degraded、
   后续会话立即 `[reject] runner unavailable` 并关闭连接，**进程保持存活**（心跳/日志继续可见）。

（本轮为**纯本机**工作：远端节点 2026-09-24 全部离线，故长时/累积/三段均在同机或 loopback 上完成。）

## P0-P2 复核修正（2026-09-21）

- P1 的记录校验已收紧：`write_record()` 现在也会按 `engines.*_iface` 反推并核对 `kind/path`；schema 同时要求上下游接口字段存在。
- P0 本地矩阵驱动在 runner 非零退出时改为立即失败，不再继续打印“all done”。该驱动与原始工件仍在被忽略的 `build/` 实验目录中，27/27 是本机可审计证据，不是清洁检出的可复现交付物。
- P2 的候选空间超出 `max_candidates` 时改为 fail-closed，不再静默截断；非分层权重在首尾段各驻留一次，容量总量按两次计入。
- 旧的两段保守规划器 `src/relay_planner.py` 也已收紧：设备画像只有总 RAM、没有 available headroom 时，不再用总量冒充可用容量。
- `scripts/relay_cut_plan.py` 现在兼容 P1 `layer_layout` 记录，并拒绝混合模型、commit、负载或链路身份的记录。未显式提供 `--layer-bytes-mib` 时，整模字节均匀摊层仅是容量近似，非分层权重不再重复计入。
- P2 结论需要降级：qwen3.5-2B 的 K=20 仍是 4 个合法切点的单轮局部结果，qwen2.5-0.5B 的早期全域预测为未实测 K=1；二者均不能引用为当前多轮闭环，需使用新的原始多轮分析报告重新确认。
- Windows 原生 ABI 污染已隔离：keep-head 自带的 `ggml.dll` 与 pip `llama_cpp` 不能在同一测试进程混载；真实 keep-head 用例由 `tests/test_llama_keep_head_subprocess.py` 在子进程执行，失败仍会使外层回归失败。
- 生产 keep-head 路径也已隔离：若 pip `llama_cpp` 已在当前进程加载，`KeepHeadUpstream` 自动经 `src/llama_keep_head_worker.py` 转发，避免依赖 DLL 装载顺序；未加载 pip ABI 时仍可直接调用 shim。

## 5. P3 收口：激活与缓存数据流 —— 结论页

P3 已完成多序列显式绑定、长序列/多序列和真实跨机链路验证；量化目前只完成数值往返实验。
线上 hidden 压缩、误差反馈和传输-计算重叠仍未闭环。本节是**结论索引**；过程与逐次实测见 §4。

### 5.1 票面四项

| # | 票面项 | 状态 | 关键结论 | 记录（`build/relay-records/`） |
| --- | --- | --- | --- | --- |
| ① | prefill 传整段、decode 传末位 | ✅ | `activation_policy = prefill:full_sequence+decode:last_position` 落进记录；三段 K=8/16 端到端 32/32 | `three-seg-*.json` |
| ② | 多序列 `seq_ids` / `positions` 显式绑定 | ✅ | 本机与跨机（`HIDDEN_SEQ` 帧）都走显式绑定；远端不支持显式绑定一律 `hidden_seq_unsupported` fail-loud，绝不按隐式位置猜 | `three-seg-k8-16-p*.json`、`net-*-b2.json` |
| ③ | hidden 块量化 / 误差反馈 | 压缩**已上 wire**；误差反馈未实现 | 档位在帧头 `flags` 低 3 位（`src/relay_transport.py:43-45` 的 `RELAY_FLAG_QUANT_MASK` / `RELAY_QUANT_MODES`），**不升** `RELAY_WIRE_VERSION`，旧对端见非零 flags 直接 `unsupported_flags` ⇒ **天然 fail-closed**。判据（**per-token argmax**）：`f16` **32/32** ✅、`int8_block128` **32/32** ✅、`int4_block128` **23/32** ❌（**不作可选档**）；上下行可各自选档（下行档位由服务端单方面决定、客户端跟随解压）。⚠️ 不得把模拟字节或弱网收益当成真实压缩证据；**误差反馈未实现** | `quantcurve-*.json`、`l2l-net-local-05b-quant-*-g512-ctx8192.json` |
| ④ | 预取与传输-计算重叠 | ⛔ 未实现 | 只有同机 < 1% 的估算。⚠️ 原文"真实 wire 压缩**待协议实现后重测**"已过时 —— wire 压缩**已实现**（见 ③）⇒ **压缩相关**的重叠测量现在**可以**做；**真实弱网**重叠仍需外部条件 | — |

### 5.2 遗留项

| 项 | 状态 | 结论要点 |
| --- | --- | --- |
| 长序列 × 多序列 | ✅ | 中段与末段的 `n_batch` 必须 ≥ `batch × prefill`；prefill 512 × batch 4 → 32/32 |
| 跨机多序列 | ✅ | `HIDDEN_SEQ` 帧 + 服务端每连接 `qlh_kh_reset()`；顺带修掉一个真实的跨连接串味 bug（同进程第 2 条连接与上一条的 KV 位置冲突） |
| 真跨机（Android 段） | ✅ | y700（ARM64/Termux）当中间段 32/32；mid 64.4 ms/步（含两跳往返），batch 2 几乎不涨 ⇒ 该段是**吞吐型**而非延迟型瓶颈 |
| Surface 中段 | ✅ | x86_64（Windows）当中间段 32/32，mid 51.1 ms/步 |
| 弱网压缩收益 | ⛔ 口径作废，待重测 | 旧表是按目标精度计算的模拟值；当前驱动明确记录 wire 仍为 `float32_le`，不能据此宣称带宽收益 |
| 误差测量（不改链路） | ✅ | 9 档全 32/32；`rel_mean`：f16 8.8e-5 / bf16 1.4e-3 / int8 2.2e-2，且不随 prefill(128→1024) 与步数变化 |
| 多跳（四段）拓扑 | ✅ | torch → Surface → y700 → 本机 32/32（batch 1/2）；`--mid-endpoint` 可重复 + `--mid-stop` + `_MiddleChain`（主循环零改动） |

### 5.3 由 P3 得出的三条可复用结论
1. **数值量化与 wire 压缩必须分开记账**：当前 `--hidden-quant` 是量化后回转 FP32 的数值实验，
   Relay 帧仍发送 FP32；任何模拟字节都不能写成真实带宽收益。后续必须先扩展协议并由实际
   payload 长度出报告，再重跑弱网矩阵。
2. **验证不得依赖 shell 变量残留**：曾因此让 9 档里 8 档"看起来成功"实际根本没跑。改为
   "每档先删旧记录 → 跑完只看记录文件存在 + `verdict.passed`"（跨机实验尤其要守）。
3. **跨机代价与批处理强相关**：batch 2 时 Android 段耗时几乎不涨（吞吐型），单序列 decode 才对
   RTT 敏感 ⇒ 引擎层选档应按画像与批大小决定，而不是一律"本机优先"。

### 5.4 异构 kernel 逐比特一致性（2026-09-21 实测）

探针 `scripts/relay_bitwise_probe.py`：三平台共用**同一份输入字节**（`bitwise/input.npy`，sha256
`088d83e3…`；不是各自按 seed 重算）、同一份工件 `qwen25-05b-f16-mid8-16.gguf`、同一份 C 源编出的
shim、`threads=4`、8 tokens × 896 维：

| 平台 | 输出 sha256 | mean_abs |
| --- | --- | --- |
| 本机 x86_64 | `2eb55979…` | 0.72972679 |
| Surface x86_64 | `2eb55979…`（与本机**逐比特相同**） | 0.72972679 |
| y700 ARM64 | `a4bf986c…` | 0.72984803 |

逐元素比较（记录 `bitwise/compare.json`）：

| 对比 | 逐比特一致 | 差异元素 | `max_abs` | `rel` |
| --- | --- | --- | --- | --- |
| 本机 ↔ Surface（同架构） | ✅ 是 | 0 / 7168 | 0.0 | 0.0 |
| 本机 ↔ y700（跨架构） | ❌ 否 | 7168 / 7168 | 0.0086 | **1.0e-3** |

三点结论：

1. **跨机本身不引入数值差异**：同架构 + 同二进制 + `GGML_NATIVE=OFF` ⇒ 输出逐比特相同。
   因此"跨机"与"跨架构"必须分开表述——前者可以要求逐比特，后者不能。
2. **跨架构漂移是 ulp 级归约顺序差异**：`rel ≈ 1.0e-3`，比 P3 测得的 int8 量化误差（2.2e-2）
   **小 20×**；不是逻辑错误，与本仓既有的上游记录一致（ARM KleidiAI numerical drift）。
3. **验收方式因此分层**：引擎层内部（同架构多节点）可继续要求逐比特；异构混部时按
   "容差 + per-token argmax"验收（P3 的四段实测 32/32 已是该形式的佐证）；记录必须写清参与
   节点的**架构与 kernel 版本**，否则这类差异无法归因。

**数值量级的读法（`1e-3` 到底意味着什么）**：

- **它是"同一层段、不同指令集"的天然下限，不是可忽略的噪声**。跨架构不可能逐比特：浮点
  乘加的**结合顺序**在 SIMD 宽度/Lane 数/是否 FMA 不同时必然不同，误差沿层累积——本段
  **仅 8 层**就累计到 1e-3 相对量级。所以"跨架构逐比特"不是"暂时没做到"，而是**目标本身不成立**。
- **它给"跨层接力的误差预算"定了参照系**。链路里真正可控、可选的误差来源是
  **hidden 压缩**（int8 2.2e-2）与权重/激活量化；跨架构漂移（1e-3）比它们**小约 20×**。推论：
  ① 压缩档应按**最坏节点**（异构）的容差来选，而不是按本机；
  ② 只要能容忍 int8 压缩，就一定能容忍异构节点间接力——四段实测 32/32 正是这个推论的验证。
- **但不能拿单段数字外推全链**：1e-3 是"8 层、一次跳"的量级，层数与跳数都会放大它
  （上游对 ARM KleidiAI drift 的记录同向）。P4 的精度预算必须**按段数 × 层数**外推。
- **最直接的工程价值**：它把"验收判据可以从逐比特退到『容差 + per-token argmax』"从一个工程
  直觉变成**有数值、有归因、有边界**的决定——即"退到哪、为什么可以退、什么时候不能退"。

### 5.5 仍未覆盖（转 P4 / P5）

- ✅ **已由 P4 覆盖**：int4 档的量化误差与边界（`int4_block128` + 上游 fp4/nf4）见 §6；
- ✅ **已完成**：多跳中异构 kernel 逐比特一致性 —— 见 §5.4（x86_64 逐比特相同、ARM64 漂移 1e-3）；
- ⏳ 仍待做：2bit 等更激进档的误差反馈；真实弱网（非模拟注入）与长时稳定性（→ P5）。

## 6. P4 收口：量化边界与精度预算 —— 结论页

P4 票面四问均已作答。本节是**结论索引**；过程与逐次实测见 §4 的 P4 节（边界扫描 → 12 档矩阵 →
多 prompt 翻转率 → 四段跨机加压 → 精度预算表）。

### 6.1 票面四问

| # | 票面问题 | 状态 | 关键结论 | 记录 / 工具 |
| --- | --- | --- | --- | --- |
| ① | 上游全精度是否降低下游量化误差传播 | ✅ | 上游 int8 几乎无损（margin −0.68%）；**fp4 伤边距 −12.85%、nf4 反而抬高 +8.58%** ⇒ **量化表比位宽更关键** | `p4m-*.json`（12 档） |
| ② | 下游量化误差是否随切点累积 | ✅ | **不累积**：误差不随长度/步数增长（§5.2）；四段跨机 + hidden int4 仍在安全区（margin 7.86） | `quantcurve-*.json`、`p4x-four-seg-*.json` |
| ③ | f32 hidden 通信成本是否被 batch / 弱网放大 | ✅ | 弱网下压缩收益 1.48×~1.71×，与 RTT **正交**（§5.2 弱网表） | `weaknet-*.json` |
| ④ | 何时优先容量、何时优先质量 | ✅ | 输入先通过基线 margin 闸门，再用 **`nf4 × f16`**（容量 −41%）；否则走全精度链路 | `p4b-*.json` + `relay_quant_budget.py` |

### 6.2 精度预算表（12 prompt × 5 档，60 次实测）

| upstream | hidden | 翻转 | 无余量 prompt | margin_min（有余量子集） | 中位 | Δmin | 上游驻留 | 判定 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| fp16 | f16 | 0 | 3 | 5.0892 | 5.6833 | +0.05% | 510.9 MB | **safe** |
| fp16 | int4_block128 | **8** | 3 | 3.9064 | 4.8836 | −35.85% | 510.9 MB | tight |
| int4 (fp4) | f16 | 3 | 3 | 4.8971 | 5.8203 | −12.62% | 302.1 MB | tight |
| int4 (fp4) | int4_block128 | 7 | 3 | 3.8284 | 4.7363 | −39.36% | 302.1 MB | tight |
| **nf4** | **f16** | 3 | 3 | **5.2932** | 6.1803 | −24.56% | 302.1 MB | **safe-headroom-only** |

闸门 = `flip_threshold(4.5) + safety_margin(0.5) = 5.0`（判据用**有余量子集**的最坏 margin）。

### 6.3 由 P4 得出的四条可复用结论

1. **单 prompt 的"全绿"不是安全证据**：同一条链路单 prompt 下 12 档全绿、连 35% 的元素级误差都不翻；
   换成 12 条 prompt，`fp16 × int4_block128` 立刻翻 **8** 条。⇒ 量化边界**必须用 prompt 集测**，
   单点结论只能当"没有反例"，不能当"安全"。
2. **相对衰减不能预测翻转，绝对 margin 可以**：基线低的 prompt 衰减 13.9% 就翻（`code`，基线 3.7555），
   基线高的衰减 30.1% 仍不翻（`mixed-zh`，基线 6.8580）。⇒ 判据要用**绝对 margin**，
   而不是"掉了百分之几"。
3. **判据必须分层**：基线 margin 本身就低于闸门的输入（本例 3/12，`code`/`numbers`/`rare`）
   **没有任何量化预算** —— 不是"少省一点"而是"必然出错"；把它们算进档位判定会让对照档也被判 unsafe。
   因此工具把含这类翻转、但有余量子集通过闸门的档位标为 **`safe-headroom-only`**，不得当作全局 safe。
4. **口径先定死**：`int8`/`int4` 上游档曾因把 `quant_type` 交给 HF 而**静默回退 fp16**
   （症状：三档 margin 连小数位都相同）。⇒ 量化对照实验的**第一步**应是验证档位真的生效
   （记录里现在带 `quant_probe` 与替换计数）。

### 6.4 可复用入口（均已入库）

| 入口 | 用途 |
| --- | --- |
| `scripts/relay_quant_budget.py` | 由记录汇总预算表 + 判定 + 选档建议；默认要求每档至少 6 个且一致的 prompt，并区分 `safe` / `safe-headroom-only`（`--flip-threshold` / `--safety-margin` / `--min-prompts` 可调） |
| `scripts/relay_bitwise_probe.py` | 异构 kernel 逐比特一致性探针（`gen-input` / `run` / `compare`） |
| `--track-quant-error`（驱动参数） | 量化往返误差测量（**只读统计，不改链路**） |
| `--hidden-quant int4_block128` | 4bit hidden 压缩档（896 维 → 476 B/token） |

### 6.5 仍未覆盖（转 P5）

- **只基于一个模型**：全部结论来自 qwen2.5-0.5B + f16 工件，未跨模型验证；
- **生成长度只有 32 步**：更长生成下的翻转是否被放大未测；
- **真实弱网**（非模拟注入）下的量化边界；
- ~~把预算闸门**接进 CI**（`--strict` 退出码化），让"档位不越界"成为回归门。~~ ✅ **已完成
  （2026-09-23，A10）**：实际落地为 `relay_quant_budget.py --fail-on {off,unsafe,tight}`（默认 `off`
  ⇒ 保持旧行为）+ `--self-check`（合成记录验证"合格集必须 `safe`、越线集必须 `unsafe` 且闸门必须红"），
  由 `.github/workflows/relay-gates.yml` 在 push / PR 上跑；故障注入用例见 `tests/test_ci_relay_gates.py`。

## 7. P2 收口：自适应切点与容量/速度双目标 —— 结论页

P2 的票面（双目标求解 + 切点闭环）与两条遗留项均已收口。本节是**结论索引**；过程见 §4 的
「P2」与「P2 续：多段拓扑」两节。

### 7.1 交付物与状态

| 项 | 状态 | 说明 |
| --- | --- | --- |
| `src/relay_cut_objective.py` | ✅ | `legal_cuts()`（含 `cut_multiple` 硬约束）/ `plan_relay_cut_n_segments()`（容量 + 延迟 + 风险三输出、具名拒绝）/ `fit_segment_profile()` |
| `scripts/relay_cut_plan.py` | ✅（2026-09-22 加固） | 只消费分析报告；原始/中位记录、缺来源/缺 warmup 或不满足多轮闸门均拒绝进入规划 |
| `scripts/relay_cut_model_analysis.py` | ✅（2026-09-22 加固） | 每切点至少 3 轮且轮数/round 集合平衡、身份和 warmup 一致、拒绝重复/中位输入；输出 **"当前数据是否检出显著内部最优"** |
| 多段拓扑执行层 | ✅ | 见 §4「P2 续」（1 个 torch 上游 + n 个 llama 下游） |
| 切点闭环（qwen2.5-0.5b） | ✅ **已用新 gate 重跑（2026-09-23）** | 3 轮 × 8 切点（K=1/2/3/4/8/12/16/20，`--warmup 3`）的**原始多轮记录**进 ≥3 轮闸门：拟合 r²=**0.9935**（8 点）、最大相对离散 34.91%、未检出显著内部最优 ⇒ **预测切点 K=1 = 实测最优 K=1，闭环 PASS**。旧单轮的 `fitted_model_disagrees_with_measured_optimum`（预测 1 / 实测 3）确认为**单轮噪声** |
| 切点闭环（qwen3.5-2b hybrid） | ✅ **已用同一 ≥3 轮闸门复核（2026-09-23）** | 3 轮 × 5 切点（K=4/8/12/16/20，`cut_multiple=4`）：拟合 r²=**0.9938**（5 点）、未检出显著内部最优 ⇒ **实测最优 K=4（最小合法切点）= 预测切点，闭环 PASS**。⚠️ 旧的"K=20 最优"作废：它来自**另一套上游接口**（`ModelManager.load_layer_range`）的单轮扫描，且其上游测量 `std/mean≈1.78`（噪声极大） |

**复算（判据：报告由下面的命令逐字重跑可得；原始记录已随 `build/` 落盘）**

```bash
# ① 原始多轮扫描（R=1..3 轮 × 每个合法切点 K；qwen2.5 的 K∈{1,2,3,4,8,12,16,20}）
python scripts/relay_experiment.py --path d2l_mainrepo --model-dir models/qwen2.5-0.5b-instruct \
  --layers K --cut-model build/cross-framework-layer-poc/out/qwen25-05b-f16-cut-kK.gguf \
  --whole-model build/cross-framework-layer-poc/out/qwen25-05b-f16.gguf \
  --prefill 32 --gen 32 --batch 1 --threads 8 --warmup 3 \
  --json-out build/cross-framework-layer-poc/out/p0-gate/rR-kK.json
# ② 多轮分析（闸门：每切点 ≥3 轮、身份/warmup 一致、原始测量来源）
python scripts/relay_cut_model_analysis.py \
  --records "build/cross-framework-layer-poc/out/p0-gate/r*-k*.json" \
  --total-layers 24 --min-rounds 3 --out build/relay-records/cut-model-analysis-qwen25-gate.json
# ③ 闭环规划（消费 ②，不接受非多轮/非原始输入）
python scripts/relay_cut_plan.py \
  --analysis-report build/relay-records/cut-model-analysis-qwen25-gate.json \
  --records "build/cross-framework-layer-poc/out/p0-gate/r*-k*.json" \
  --total-layers 24 --cut-multiple 1 --json-out build/relay-records/cut-plan-gate-qwen25.json
```

qwen3.5-2b 同形，差别只有三处：`--model-dir models/qwen3-5-2b`、记录目录 `out/p0-gate-35/`、
`--cut-multiple 4 --n-embd 2048`（K∈{4,8,12,16,20}；K=4 的裁层工件由 `scripts/cut_layers.py --k 4` 现切）。


### 7.2 四条可复用结论

1. **测量噪声必须先量化，再下"最优"结论**：8 个切点 × 5 轮的**最大相对离散 36%**，单轮曾得出
   错误的"最优切点 K=3"；多轮取中位后最优是 K=1，闭环才真正闭合。**2026-09-23 用新 gate 的
   原始多轮记录正式确认**：3 轮 × 8 切点 ⇒ 中位线性度 r²=0.9935、最大相对离散 34.91%，
   预测切点与实测最优一致（K=1）⇒ 闭环 PASS。同一条纪律在 qwen3.5-2b 上也复现了一次：
   旧的"K=20 最优"是单轮 + 上游测量 `std/mean≈1.78` 的产物，3 轮复核后实测最优是 **K=4**。
2. **口径先行**：`--warmup` 不统一会得出无效结论（同一参数下 K=1 的上游耗时 18.5 → 6.4 ms）。
   这与 P4 的"hidden 压缩作用在哪一跳 / 线路字节怎么算"是同一类错误。
3. **当前数据未检出显著内部最优**：求解器的 `fixed + per_layer × layers` 对切点数学上线性，
   实测中位数线性度 r² **0.996 / 0.999**；这只支持当前观测范围内暂不引入非线性项，不能证明内部最优永远不存在。
4. **切点收益体现在"端点方向"**：K=1（33.29 ms）vs K=20（45.91 ms）差 **1.38×**；上游每层
   **1.54 ms/层**（torch/GPU）比下游 **0.91 ms/层**（llama/CPU）贵 ⇒ **应尽量把层放在下游一侧**，
   与「L 档为主」的既有策略一致。

### 7.3 仍未覆盖

- **纯边缘集群（无 PC）的 L→L 多段证据**：本轮按"不假设集群里有 PC"显式登记，
  设计见 P4.5 §3.5（退化路径 + 判据不降级）；
- ~~qwen3.5 的 K=20 结论目前仍来自 4 个合法切点的单轮扫描，状态是"单轮局部结果，整体待多轮复核"，后续应按同一 ≥3 轮闸门复核；~~ ✅ **已用同一 ≥3 轮闸门复核（2026-09-23）**：3 轮 × 5 切点 ⇒ 实测最优 **K=4**、闭环 PASS；旧 K=20 作废（口径差异见 §7.1）。**跨口径不可比**的教训再次成立：新旧两次结论连方向都相反（旧"K 越大越好"、新"K 越小越好"），根因是 `upstream_iface` 不同（`ModelManager.load_layer_range + forward_layers` vs `model_module.forward_layers`）—— 这正是"跨报告数字不构成对照"的一例。
- qwen3.5 的按层类型分组拟合（仅当将来真要追求"内部最优"时才需要）—— **仍未做**；当前 5 个切点的中位线性度 r²=0.9938，未检出显著内部最优（`interior_optimum_significant=false`），故不引入非线性项。

## 8. 部署选型：何时用 D→L，何时纯 L→L

本节把 P2/P3/P4/P4.5 的实测结论收敛成**部署决策**。三条链路都已真机验证过（判据一律
**per-token argmax 与同精度整模一致**，不得用 cosine 代替）；选型只决定"用哪条"，不改变判据。

### 8.1 一句话规则

| 集群情况 | 选型 | 理由 |
| --- | --- | --- |
| **有 CUDA 节点，且该节点能装下整模** | **不要接力**：单机全 llama + CUDA | 实测 **26.6 ms/token vs 接力 129.1 ms/token（4.85×）**；接力的价值是**容量**，不是速度 |
| **有 CUDA 节点，但整模装不下** | **D→L**（PyTorch 上游 + llama.cpp 下游） | CUDA 上游划算，下游吃 llama 的容量与量化；**上游拆层自由**（见 §8.2） |
| **没有任何 CUDA / 没有任何能跑 torch 的节点**（纯边缘：Linux 板 + Android） | **纯 L→L**（全 llama.cpp） | torch 上游在纯 CPU 边缘上不划算；L→L 已真机验证（含"完全无本机"形态） |

**D 档（PyTorch 上游）是给"集群里有 CUDA"准备的**：它的价值是吃 GPU 算力、并承担"整模装不下"
时的上游容量。若集群里没有 CUDA，D 档不该被启用 —— 这就是退化路径的分界线。

### 8.2 上游拆层的自由度差异（选型里最容易被忽略的一条）

| | D 档上游（PyTorch） | L 档段（llama.cpp） |
| --- | --- | --- |
| 切点怎么定 | **任意层**：`model_module.load_layer_range(0, K)` 直接按 `K` 切 | 需要**裁层 GGUF 工件**（`qwen25-05b-f16-cut-kN.gguf`） |
| 代价 | 无需预制工件；改切点＝改一个参数 | **工件要预先离线生成并传输**（数百 MB/份）；改切点＝生成并搬一份新工件 |
| 额外约束 | 无（dense / hybrid 均可任意切） | hybrid 的切点还必须是 `full_attention_interval` 的整数倍（qwen3.5 为 4），否则裁层工件层类型错位、**无法加载** |
| 精度 | 上游 dtype / 量化可自由组合（fp16 / int8 / nf4） | 受工件量化档与 `--hidden-quant` 限制 |

⇒ **计划内的多切点搜索、动态改切点应优先在 D 档做**；L 档切点适合**固定几个候选**（把实测切点
当离散候选，见 §7）。P2 的实测也支持这一点：L 档切点扫描的噪声（最大相对离散 **36%**）大于
切点之间的真实差异，**精调切点没有可测收益**（§7.2）。

### 8.3 三条链路的实测状态

| 链路 | 拓扑 | 实测 | 记录 |
| --- | --- | --- | --- |
| **D→L** | torch 上游 + llama 下游（本机） | ✅ 32/32 | `three-seg-*`、`p4m-*` |
| **D→L 跨机** | torch(本机) → Surface → y700 → 本机 | ✅ 32/32（三段 / 四段） | `net-*-three-seg-*`、`net-four-seg-*` |
| **纯 L→L** | llama head 段 → llama 中间段 → llama 末段 | ✅ 32/32（两段 / 三段，含**中段在 y700**） | `l2l-net-3seg-device-mid.json` |
| **纯 L→L 完全无本机** | y700 head12 → y700 cut-k12 tail（本机只发命令） | ✅ **32/32** | `l2l-net-device-only-2seg.json` |
| **纯 L→L 多跳（三段全设备）** | y700 head8(0..7) → y700 mid8-16(8..15) → y700 cut-k16 tail(16..23)（本机只发命令） | ✅ **32/32** | `l2l-net-device-only-3seg.json` |
| **纯 L→L 跨设备（三段）** | y700 head8 → **Surface** mid8-16 → y700 tail | ✅ **32/32** | `l2l-net-cross-device-3seg.json` |

**多跳（三段全设备）已通过**：`head → middle → tail` 三个角色同机（y700），本机全程只发命令，
判据仍是 per-token argmax **32/32** ⇒ 三段拓扑的**远端角色齐备性**得到验证（§8.3 的三帧表）。

**跨设备形态已通过（2026-09-22）**：`y700 head8 → Surface mid8-16 → y700 tail`，判据 per-token
argmax **32/32** —— **两台真机（ARM64 Android + x86_64 Windows）协作完成整条接力，本机不参与计算**。

⚠️ **首次尝试失败过，根因不在代码，值得记下来**：那次失败正好赶上**局域网崩盘**；而本轮用
`ssh ... python3 relay_mid_service.py`（服务**跑在 ssh 会话里**）⇒ 网络中断把 ssh 会话带走、
**远端服务随之退出**。此后隧道端口**仍在本机监听**，于是新连接被"接受"后立即被 reset
（`ConnectionResetError` 10054）—— 现象看起来像模型层错误，实际是**远端根本没有服务**。
**重起四个服务后一次通过。**

⇒ **部署结论（已并入 §8.6）**：服务必须能**脱离 ssh 会话存活**。"前台跑在 ssh 里"只适合实验；
生产形态需要常驻 + 健康检查（否则一次网络抖动就会让远端段静默消失，而客户端只会看到一个与
"模型出错"难以区分的连接错误）。**已具备（R-R9，2026-09-24）**：
`relay_mid_service.py --detach --log-file <p> [--pid-file <p>]`（零依赖脱离会话）
+ `relay_health.py --probe`（**协议级**探活；`--check` 只测端口会漏报"端口在监听但对端已死"）
+ `--heartbeat-interval` 心跳。详见 §8.6。

（诊断过程中另修掉一个**探针真 bug**：`finally` 里的 `client.close()` 抛异常会**掩盖原始错误**，
表现为只看到"关闭失败"—— 现在关闭失败被吞掉，不再掩盖循环里的真实异常。）

远端角色由**帧类型**区分（三者齐备才谈得上"全设备"）：

| 帧 | 角色 | 语义 | 服务端 |
| --- | --- | --- | --- |
| `TOKENS` | 上游段（head） | 吃 **token** 吐 hidden | `--role head`（keep-head shim） |
| `HIDDEN` / `HIDDEN_SEQ` | 中间段（middle） | 吃 hidden 吐 hidden | `--role middle`（keep-head shim） |
| （送 `HIDDEN`）→ 回 `TOKEN` | 末段（tail） | 吃 hidden 吐 token | `--role tail`（shim 或 pip 绑定） |

### 8.5 时序与吞吐（2026-09-22 实测）+ 健康检查

**① 分段计时**（探针 `--json-out` 现带 `timing.segments_ms` / `end_to_end_ms` / `per_step_e2e_ms`）：

四条链路（prefill 32 / gen 32，**全部 PASS 32/32**，判据 per-token argmax）：

| 链路 | 整模 | 接力 | e2e 均值 | e2e **中位** | e2e 最大 | 分段均值（head / mid / tail） |
| --- | --- | --- | --- | --- | --- | --- |
| **A** 全 y700（head8 / mid8-16 / tail16） | 1.12 s | 5.86 s | 176 ms | 148 ms | 970 ms | 60 / 58 / 58 ms |
| **B** 本机 head8 + y700 mid8-16 + y700 tail16 | 1.38 s | 4.87 s | 146 ms | 129 ms | 535 ms | **30** / 75 / 41 ms |
| **C** y700 head8 + **Surface** mid8-16 + y700 tail16 | 1.43 s | 59.1 s | 1831 ms | 799 ms | **27608 ms** | 48 / **1711** / 72 ms |
| **D** 本机 head8 + **Surface** mid8-16 + y700 tail16 | 1.44 s | 40.3 s | 1237 ms | 880 ms | 12419 ms | **32** / **1084** / 121 ms |

对照整模：32 步约 **1.1~1.4 s**（≈35~45 ms/步）⇒ 接力 A/B 组慢 **约 4~5×**，与既有裁决一致
（**接力是容量手段，不是提速手段**）。

**从数字里读出的四条结论**：

1. ⚠️ **真正的瓶颈是"跨网络/经中继"，不是算力**（这条结论经**首轮误判后更正**，见下）：
   C/D 组慢的根因是 **Surface 不在局域网** ⇒ 其流量走 **Tailscale DERP 中继（东京）**。
   证据：`tailscale ping` 显示 Surface `via DERP(tok)`、而 y700 是 `via 192.168.0.100`（局域网直连）；
   本机→Surface 的 RTT 实测 **777 ms**，与 C 组 e2e **中位 799 ms 几乎完全吻合** ⇒ **每步一次往返**，
   时间全花在网络上。**更正说明**：首轮我据"Surface 是低功耗 Windows 笔记本"下了"中段别放这类机器"
   的结论 —— 那是错的。查证后排除两件事：① 我们编的 Windows DLL 并非"通用无 SIMD"
   （`GGML_AVX2/F16C/FMA=ON`、`Release -O3`）；② 同段（8 层）在 y700（ARM）上 58 ms，
   在 i5-1035G4 上也不该差 30×。
   ⇒ **部署结论（正确版）**：中段必须与调用方**在同一局域网**（y700 直连时整段链路 ~150 ms 中位）；
   跨网络尤其经中继的路径，每步一次往返 ⇒ **不可用**。
2. **x86 主机跑 head 段比 ARM 设备快 2×**（28~32 ms vs 48~60 ms）⇒ 有 PC 时让 PC 承担上游段是划算的。
3. ⚠️ **冷启动尖峰必须单独看**：C 组首步 **27.6 s**、D 组首步 12.4 s，之后落到 0.7~2.2 s
   ⇒ 只看**均值会被首步严重污染**（C 组均值 1831 ms 而中位仅 799 ms）。
   这与"std 几乎等于均值"是**同一条教训的第二面**：**边缘/异构环境里的时序结论必须先量化离散度、
   并区分冷热**（P2 的"单轮扫描噪声 36%"也是同一根源）。
4. **首轮误判本身也是一条教训**：我据"设备类型"（Windows 笔记本 vs Android 平板）解释了 30× 的差异，
   而真正的原因是**网络路径**。⇒ 性能归因必须**先量出瓶颈在哪一环**（RTT / 分段耗时），
   不要在没测网络时用"设备档次"讲故事 —— 这与"接力不是提速手段"那类结论同源：
   **先测，再解释**。

**② 一个真 bug（本轮定位并修复）**：服务端把 `CLOSE` 帧处理成 `runner.close()` ——
而**探针每次实验结束都会发 `CLOSE`** ⇒ 引擎被销毁 ⇒ **该服务此后每个连接都失败**
（实测：帧完全正确——32 tokens / 114688 字节——却报 `rc=-5 参数非法`，现象与"模型算错"**难以区分**，
只能靠重启服务恢复；这正是"B 组时通时不通"的真因）。
`CLOSE` 的语义应是"结束**本次会话**"而不是"销毁服务端引擎"：现在只 `reset()`，**不 `close()`**
（引擎属于服务进程、要跨连接复用；真正释放发生在进程退出）。
⚠️ 更值得记的是：**旧行为被一条测试固化了**（`..._closes_runner` 断言 `runner.closed is True`）
⇒ 测试全绿而服务只能服务一次。现在改为断言"只 reset 不 close"，并**新增双会话回归测试**。

**③ 健康检查（已实现并验证）**：

- **服务端心跳**：`--heartbeat-interval`（默认 5 s）定期刷新 ready 文件的 `alive_at` / `pid`
  ⇒ **外部据此判断服务是否还活着**（实测 `alive_at` 从 15:15:14 → 15:15:20 ✓）；
- **客户端探活**：探针在收发**之前**先探远端段，远端段已退出时给出明确诊断
  （"远端服务已退出 / 隧道已断" + "检查该端 ready 的 `alive_at`"），**而不是让它表现成"模型算错"**
  —— 这正是 §8.6 那条教训的落地（实测对 `127.0.0.1:50999` 无人监听给出该诊断 ✓）。
  ⚠️ **已升级为协议级（R-R9，2026-09-24）**：原先只做 **TCP connect**，而 §8.6 那种故障
  **恰恰是"端口仍在监听、connect 会成功"** ⇒ TCP 探活会**漏报**。现在改走
  `scripts/relay_health.py` 的 `probe_relay`（真发 `CLOSE` 帧并等 `TOKEN(-1)` 应答），
  并在 `_RemoteMiddleSegment` 建连**之前** fail-loud
  （实测：假段「accept 后立刻 close」被拒，`handshake_failed` / `ConnectionAbortedError`）。
- **独立健康检查命令** `scripts/relay_health.py`：把两者合起来并给出**可进 CI 的退出码**
  （0 = 全健康 / 1 = 有异常），支持 `--check name=tcp:host:port`、`--ssh-ready name=alias:path`
  （经 ssh 读远端 ready 并检查**心跳新鲜度**，默认 30 s 过期）、`--json`（供驱动解析）。
  实测：四段 TCP 全 OK、y700 两段心跳 `age=4.8 s / 2.2 s`、故障项给出 hint 且 `exit=1` ✓。
  ⚠️ 顺带修掉它自己的一个真 bug：输出用了 `⇒`(U+21D2)，在 **GBK 控制台**下抛 `UnicodeEncodeError`
  把健康检查打成 traceback（**检查工具自伤，比不检查更吵**）⇒ 现在强制 UTF-8+replace、
  输出只用 ASCII 箭头，并有回归测试钉住；
- **脱离 ssh 会话**：初始做法是 `nohup ... &`（当时记录「Termux 无 `setsid`」—— 那指的是
  **`setsid` 命令**缺失，与 `setsid(2)` **系统调用**是否可用无关）。**现已内置（R-R9）**：
  `relay_mid_service.py --detach --log-file <p>` 用 `Popen` + `start_new_session=True`
  （POSIX，等效 `setsid(2)`）/ `DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP`（Windows）**重起自己**，
  父进程随即退出 ⇒ 服务不依赖发起会话。
  ⚠️ **Termux 上该分支未实测**（本机是 Windows）：远端设备首次使用请先跑一次并看 `--log-file`。
- ✅ **已接线（R-R9 收尾，2026-09-24）**：`--probe` 不再是"只是个可调用命令" ——
  `scripts/relay_experiment.py` 的 `_require_relay_alive()` 在 `_RemoteMiddleSegment` 建连前调用它，
  不通即 `SystemExit`（**fail-loud**），探活成功的端点进程内缓存（失败不缓存 ⇒ 下次仍真探）。
  ✅ **CI 侧已补完（同日）**：`relay-gates.yml` 跑的 `relay_health.py --self-check` 从四条扩到
  **五条**方向断言，新增「**协议活段必须被接受**」—— 现场起一个最小**真协议**服务
  （`serve_relay_middle_connection` + 假 runner）⇒ `probe_relay` 必须 `protocol_handshake_ok`。
  前四条只验"该拒的要拒"（防漏报），**没验"不该拒的不能拒"**（**防误杀** —— 误杀比漏报更难查：
  表现是"接力直接起不来、链路上却看不到任何探活字样"）。连带修：该自检现在复用
  `src/relay_transport.py` ⇒ **需要 numpy**，workflow 已显式 `pip install numpy`。
  **CI 覆盖什么 / 不覆盖什么**：**进** CI 的是**合成输入 + loopback**（两条闸门的 `--self-check`、
  五条方向断言，秒级）；**不进** CI 的是**真段跑真模型**（需 shim + 段工件）、
  `--ssh-ready` 心跳新鲜度（需真机）、跨机可用性（Surface / y700 / 隧道）。

**已解决（本轮之前的"待办"，如实收口）**：A 组（全 y700）通、B 组（上游在本机）报 `rc=-5` 的差异
**已查清** —— 根因是 `CLOSE` 帧误销毁引擎（见 ②），**不是**"上游在本机"的差异；
修复后 A/B 交替连跑三次全部 PASS 32/32。跨设备性能数据也已补齐（见 ①）。
⇒ 真正剩下的待办只剩**一条**：**中段跨网络时的可用方案**（当前结论：经中继不可用）。
~~把健康检查接进 CI~~ ✅ **已完成（2026-09-23，A10）**：真机健康检查本身不适合进 CI（需要真实
服务/设备），落地为**方向性兜底** —— `relay_health.py --self-check` 用本机 loopback（活监听 +
已释放端口）验证"该判死必须判死"，与预算闸门一起由 `.github/workflows/relay-gates.yml` 在每次
push / PR 上跑 ⇒ **改动判定方向会让 CI 变红**（已用故障注入实测）。

**工具**：`scripts/relay_sync_device.ps1`（`-Device y700|surface`）—— 同步 relay 相关文件到设备后
**逐文件 sha256 回读比对 + 关键能力抽查**（`forward_hidden_to_token` / `--heartbeat-interval` /
`--role head` / `TOKENS`），不一致即 `exit 1`。它的存在理由很具体：**"改了本机、忘同步设备"已经
犯过两次**（漏传绑定层 ⇒ 设备端 `AttributeError`；漏传服务脚本 ⇒ 设备端 `unrecognized arguments`）。

### 8.6 设备侧依赖（决定"能不能上"）

- **head / middle 段必须走我们编译的 shim**：它们要取**中间层**输出，而 pip 绑定的
  `llama_cpp.llama_cpp` **不认识**我们补丁新增的符号（实测 `llama_get_embeddings_nextn_ith` /
  `llama_get_embeddings_layer_inp` / `llama_set_embeddings_layer_inp` / `llama_set_embeddings_nextn`
  四个**全部 MISSING**）⇒ 走 pip 路线就得连绑定层一起打补丁并长期跟进上游；
- **tail 段**：`--role tail` 既支持 shim（`ShimTailRunner`，推荐 —— 与 head/middle 同一份 C 源）
  也支持 pip `llama_cpp`（`TailRunner`）；**只做 tail 且装包便宜**的设备可用后者作过渡，
  但记录必须写明该段用的是外部 pip 版本；
- **编译发生在开发机**（NDK 交叉编译，已在真机验证），**设备只收产物** —— 边缘设备不该承担编译；
- 数值一致性：同链路尽量共用**同一份构建配置**的 llama.cpp；跨架构漂移已知约 `1e-3`（§5.4），
  而"两套构建配置混用"会引入不可控差异。
- **服务必须能脱离 ssh 会话存活**（2026-09-22 实测教训）：本轮用 `ssh ... python3
  relay_mid_service.py` 起远端段（实验方便），但**网络抖动会把 ssh 会话带走、服务随之退出**；
  此后隧道端口**仍在本机监听**，新连接被"接受"后立即 reset（`ConnectionResetError` 10054）——
  现象与"模型层错误"**难以区分**。
  ⇒ **已具备（R-R9，2026-09-24）**：
  - **常驻**：`relay_mid_service.py --detach --log-file <path> [--pid-file <path>]` —— 用
    `subprocess.Popen` **重起自己**（Windows `DETACHED_PROCESS|CREATE_NEW_PROCESS_GROUP`；
    POSIX `start_new_session=True`，即 `setsid` 等效）后**父进程退出** ⇒ **服务不依赖发起会话**。
    零依赖：**不**引入 `systemd` / `pywin32`。
  - **心跳**：`--heartbeat-interval`（默认 5 s）刷新 `--ready-file` 的 `alive_at`（发现
    "进程还在但引擎已废"）。
  - **探活**：`scripts/relay_health.py --probe NAME=tcp:HOST:PORT` —— 做一次**协议级**握手
    （`CLOSE` → 服务端回 `TOKEN(-1)`，**不经 runner、不加载模型**）。这一条**专治**上面那个坑：
    `--check`（只 `connect`、不发帧）对"端口在监听但对端已死"**会判健康**（**已知漏报**，
    `--self-check` 里**如实复现**），而 `--probe` **判死**。
    停服务：先 `--probe` 确认活性，再按 `--pid-file`（Windows `taskkill /PID <pid> /T /F` / POSIX `kill`）。
  - **CI 兜底**：`relay_health.py --self-check` 用本机 loopback 做**四条方向断言**
    （活监听 / 已释放端口 / **"接受后立刻 close"的假服务** / **只监听不应答**）；
    `tests/test_ci_relay_gates.py` 另有**变异测试**锁住（把握手失败改判健康 ⇒ 自检必须红）。

## 9. 当前不再投入

- 通过减少同步次数、复用 `llama_batch` 或把 relay 控制逻辑下沉 C++ 来追求单序列速度；现有剖析表明它们不是主耗时。
- 把 D→L 宣传为 CUDA 节点上的全局提速替代方案；有 CUDA 且能整模加载时优先 llama.cpp CUDA 或合适的 RPC 分片。
- 用任务图整请求并行冒充模型层分片；任务图只保留故障绕行和暂时无法分片时的回退。
- **投机解码进生产解码循环 —— 永久关闭**（用户裁定 **2026-09-23**；原备忘 A4，裁量理由：收益/复杂度不划算）。
  `src/speculative.py` 保留为**默认关闭的独立实验端点**（`experimental_speculative_chat`），
  不再接线到 `model_module` 的生产解码循环，也不再作为待办跟踪。

## 10. 待办（未完成，2026-09-23 登记）

### 10.1 纯 L→L 的「主仓引擎」口径（已补，2026-09-23）

§1 规则 2 只定义了 D→L 的「上下游都走主仓引擎」；纯 L→L 链路（§8.3）的上游 / 中段走
`src/llama_keep_head.py` + 主仓 shim（补丁版 llama.cpp），末段走 `ShimTailRunner`（同一 shim）或
`llama_engine.LlamaCppEngine`（pip `llama_cpp`）。**已补为 §1 规则 7**：写明每段允许哪套构建、
段工件由 `scripts/cut_layers.py` 生成（三种模式见 §10.3），并要求混用两套构建时**显式取齐**
`flash_attn` 与 batch 线程（依据即 §10.7 的单变量实测）。

### 10.2 记录里的逐段引擎 / 构建标识（已完成，2026-09-23）

`scripts/relay_l2l_net_probe.py` 的记录新增 **`segment_engines`**（逐段）：

| 字段 | 含义 |
| --- | --- |
| `runner` | 该段实际实现（本机 head = `llama_keep_head.KeepHeadUpstream`；远端 = `relay_mid_service --role …`） |
| `channel` / `mode` | 本机 head 段的通道与 keep-head 模式 |
| `build` | **构建标识**：shim 与同目录 `libllama`/`ggml*` 的 `sha256`、pip `llama_cpp` 版本与其 `lib/llama.dll` 摘要、段工件的名字/大小（**GB 级工件默认不算 sha256**，`--digest-artifacts` 可开） |

远端段的 `build` 来自**服务端 ready 文件**（`relay_mid_service` 已把同一结构写进 `ready.build`；
探针用 `--tail-ready-file` / `--middle-ready-file` / `--head-ready-file` 读取）。**没给 ready 文件时
显式记 `{"source": "remote_unknown"}`** —— 记录要能区分「未知」与「没写这个字段」（旧 ready 文件记
`ready_file_without_build`）。实现集中在 `src/relay_segment_info.py`（三入口：`collect_local_build` /
`load_ready_build` / `unknown_remote_build`）。证据：
`build/relay-records/l2l-net-local-05b-2seg-seginfo.json`（0.5B 两段，PASS 32/32，逐段带摘要）。

### 10.3 段工件生成器（已完成，2026-09-23）

`scripts/cut_layers.py` 现在覆盖**三种段**（manifest 带 `mode` / `source_layer_range`）：

| 段 | 命令 | 语义 |
| --- | --- | --- |
| 上游段（head） | `--keep-head N` | 保留 `blk.0..N-1`（**不重命名**），供「llama 当上游」 |
| 中段（middle） | `--k K1 --end K2` | 保留源模型 `blk.K1..K2-1` 并重编号为 `blk.0..` |
| 末段（tail） | `--k K` | 丢弃前 K 层（原行为，仍产 `RelayTrimPlan` 合同段） |

hybrid 约束依旧：`K`/`N`、`end` 与**段内层数**都必须是 `full_attention_interval` 的整数倍。
上游段 / 中段**不给** `RelayTrimPlan` 合同（那种合同只描述 tail 语义，硬套会误导下游校验）。
测试：`tests/test_cut_layers.py::TestHeadAndMiddleModes`（含互斥、区间与 interval 校验）。

### 10.4 9B（Qwen3.5，hybrid + M-RoPE）接力实测（2026-09-23，本机）

段全在本机；切点 K = 8 / 16（32 层，`full_attention_interval=4` ⇒ 合法）；模型
`models/qwen3-5-9b-gguf/Qwen3.5-9B-Q4_K_M.gguf`，对照**同精度整模**；判据 per-token argmax：

| 拓扑 | 段工件（每段只装自己那段） | 判据 | 记录 |
| --- | --- | --- | --- |
| 两段（tail 走 shim） | head16（`--keep-head 16`）+ tail16（`--k 16`） | ✅ **32/32** | `l2l-net-local-9b-2seg-head16-layerout.json` |
| 两段（tail 走 pip `llama_engine`，参数与 shim 取齐后） | 同上 | ✅ **32/32** | `l2l-net-local-9b-2seg-piptail-default.json` |
| 三段（架构槽位扩展后复测） | head8 + mid8-16 + tail16 | ✅ **32/32** | `l2l-net-local-9b-3seg-archs.json` |

三段分段耗时（gen=32，均值）：head 105.8 / middle 99.7 / tail 139.2 ms，e2e 344.7 ms。
0.5B 回归仍 PASS（`l2l-net-local-05b-2seg-layerout.json`）。

**9B 与 0.5B 不可互推**：0.5B 是 dense / `LLAMA_ROPE_TYPE_NORM` / 无 recurrent 层；9B 是 hybrid
（linear attention 的 conv + recurrent 状态）+ M-RoPE（`rope type = 40 = IMROPE`）。这两条拓扑
第一次跑时**都分叉**（`l2l-net-local-9b-2seg-fixed.json` 1/32、`l2l-net-local-9b-2seg-tailpip.json`
1/32）—— 根因是下面两个 bug，修好后才得到上表结果。

### 10.5 M-RoPE 模型上 embd 注入通道的位置布局（已修，2026-09-23）

llama.cpp 只在 **token 通道**把同一位置广播到全部 RoPE 分量；**embd 注入通道**按 planar 读
`batch.pos[j*n_tokens + i]`（`llama-batch.cpp::llama_batch_allocr::ubatch_add`，
`src_off = batch.token ? 0 : j*batch.n_tokens`），要求调用方给出 `n_tokens * n_pos_per_embd`
个位置，而 `llama_batch_init()` 只分配 `n_tokens` 个 ⇒ M-RoPE 模型（`n_pos_per_embd = 4`）会读到
缓冲区之外（0.5B 的 `NORM` 只跑一次循环，所以历史记录从未暴露它）。

- 修法（三处同一判定，均由 `llama_model_rope_type()` 决定 1 还是 4）：`qlh_keep_head.c` 的
  `qlh_fill_pos_planar()`；`src/llama_engine.py:forward_layers_from_hidden()`；Android
  `qlh_llama_jni.cpp` 的 `qlh_rebuild_pos_planar()`（顺带修掉旧实现**丢弃显式 `positions`**
  的实错 —— 多序列路径原先用 `pos_base + i` 覆盖）。
- 证据：同一 head 段下 tail 段由 FAIL 变 PASS（§10.4 两段行）。

### 10.6 Qwen3.5 的层输出通道（已修：`layer_inp` 的 `lid == n_layer` 槽位）

`src/models/qwen35.cpp`（主 graph）把 `t_h_nextn` 设在 `build_norm(..., model.output_norm, ...)`
**之后**（`h_nextn` 的消费方是 MTP head，期望 norm 后），而层段接力的下游期望**未经
`output_norm`** 的层输出 ⇒ `mode="nextn"` 取到的值多了一次 RMSNorm；qwen2 与 qwen35 的
`t_h_nextn` 因此**不是同一个东西**（这与 P1 否证 pip `embeddings` 通道的老问题同源）。

- 修法（用户裁定「扩补丁」，2026-09-23）：`layer_inp` 的槽位**多分配一个**
  （`cparams.embeddings_layer_inp` / `embd_layer_inp` 大小 = `n_layer + 1`），索引 `lid == n_layer`
  即「第 n_layer 层的输入」= **末层输出（`output_norm` 之前）**；各架构 graph 在 `output_norm`
  **之前**登记该槽位。shim 与 Android JNI 的 `nextn` 模式随之统一改用该通道 ⇒ **与架构无关**；
  `t_h_nextn` 的既有语义（MTP）不动。
- 已登记槽位的架构（9 个）：`qwen2`、`qwen35`、`gemma4`、`llama`、`openai-moe`、`qwen3`、
  `qwen3moe`、`qwen35moe`、`qwen3next`；未登记的架构会在 decode 时 GGML_ASSERT（fail-closed）。
- 证据：§10.4 的三行（含架构扩展后的三段复测）；0.5B（qwen2）回归仍 PASS。
- **已接线（2026-09-23）**：协议层新增**可选**字段 `middle_channel`
  （`extract_hidden` | `keep_head_layer_out`；两侧同集合，**缺省 = 旧行为**），并沿
  `AndroidFullWorkerStageExecutor` → `TaskWorkerService` → `LayerForwardRequest` →
  `LocalInferenceEngine.layerForward(keepHead=…)` **全程透传**；worker capabilities 另外上报
  `middle_channel` / `n_pos_per_embd`，调度侧据此可知该节点中间段实际可走哪条通道。
  ⚠️ 落坑记录：两侧的层段字段集合是**精确**校验（`_require_exact_fields` / `requireExact`）⇒
  把可选字段直接并进「必填集合」会让缺该字段的旧对端被判 `field_mismatch`（实测踩到）；
  实现改为按「payload 里是否真的出现」动态放宽。
- **当前状态（2026-09-24 按代码核实）**：① `seq_ids` / `positions`（多序列显式位置）**已上协议**
  —— `src/task_worker_protocol.py:79` 的 `_LAYER_FORWARD_OPTIONAL_FIELDS` 含这两个字段，Android 侧为
  **同集合**且 parse 后透传到 `layerForward(seqIds=, positions=)`，形状非法即 `invalid_<field>` fail-closed；
  但**主仓下发侧仍无生产者**（真实多序列下发属 A1 Relay 生产执行路径，见
  `docs/未完成工作备忘-2026-09-23.md` §1 A12）；② 真机（APK/JNI）上的 `layer_forward` 数值**尚未实测**，
  现有 D/E 证据来自 Termux 里的原生可执行文件（绕过 JNI 封装）；③ Android 侧 `hidden_spec.dtype`
  **已同时接受 `float32` / `float16`**（`AndroidFullWorkerStageExecutor.kt:201` 校验、`:354` 取字节宽度；
  f16 在本段转 f32）—— 事先存在的对称性缺口**已补齐**。

### 10.7 pip `llama_engine.forward_layers_from_hidden()` 的分叉（已定位并修，2026-09-23）

**根因是 flash attention 开关，不是语义 bug**：pip 绑定的 `Llama.__init__` 会**无条件**把
`flash_attn_type` 写成 `DISABLED`、把 `n_threads_batch` 写成 `multiprocessing.cpu_count()`，而自建
shim 那份 llama.cpp 是 `flash_attn = auto → enabled`、`n_threads_batch = n_threads`
（取证：pip 日志 `flash_attn = disabled` / `n_threads_batch = 20`；shim 日志
`Flash Attention was auto, set to enabled`）。两者在 32-token prefill（`ubatch.n_tokens > 1`）上
都会改变浮点结果。pip 自带的是 llama.cpp `4df29be`（比子模块的 `47e1de77` 更新），planar 位置
语义与 IMROPE 支持均具备 ⇒ 版本差异**不是**原因。

**单变量对照**（同一 head 段 = 整模 + `layer_inp`(16)；tail = pip `llama_engine`）：

| tail 参数 | 判据 | 记录 |
| --- | --- | --- |
| pip 默认（FA disabled + `n_threads_batch=20`） | ❌ 1/32 | `l2l-net-local-9b-2seg-layerinphead-piptail-fixed.json` |
| 只对齐 `n_threads_batch=8`（FA 仍 disabled） | ❌ 1/32 | `l2l-net-local-9b-2seg-piptail-tbonly.json` |
| **只打开 FA**（线程仍 `cpu_count`） | ✅ **32/32** | `l2l-net-local-9b-2seg-piptail-faonly.json` |
| 两者都对齐（= 新默认） | ✅ **32/32** | `l2l-net-local-9b-2seg-piptail-default.json` |

⇒ **判据关键是 FA**；线程数不是判据关键，但一并取齐。

- 修法：`scripts/relay_mid_service.py` 的 `TailRunner` 默认 `flash_attn=True`、
  `n_threads_batch=n_threads`（与 shim 取齐），并新增 `--tail-no-flash-attn` /
  `--tail-threads-batch` 以便复现差异；服务启动时上报 `flash_attn` / `n_threads_batch` /
  `llama_cpp_version`（§10.2 记录字段的起步实现）。
- 结论（并入 §8.6 口径）：**两套构建混用时，FA 与 batch 线程必须显式取齐**，否则近并列的 top-1
  会被翻转；「tail 段只走 shim」不再是必需，但共用同一份构建仍是首选。
