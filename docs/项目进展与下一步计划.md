# 项目进展与下一步计划

> 状态：现行（阶段性进展快照）
>
> 更新日期：2026-08-11
>
> 适用范围：本文是阶段性进展快照与下一步决策入口，能力边界以各专项文档与源码为准。
>
> 前置阅读：[文档状态与维护规则](文档状态与清理清单.md)、[张量并行外部辅助与混合拆分调研方案](张量并行外部辅助与混合拆分调研方案.md)（下称《调研方案》）、[三种分布式拆分细化实施方案](三种分布式拆分细化实施方案.md)（下称《细化方案》）、[混合分布式推理体系规划](混合分布式推理体系规划.md)（下称《混合规划》）
>
> 总排期入口：自 2026-07-28 起，工作优先级、生命周期与阶段门统一由[总体下一步计划](总体下一步计划.md)维护；本文 §5 保留任务编号来源，当前状态以本次复核与总排期为准。
>
> 说明：下方未带本次日期的 MODEL-FLEET 增量段保留为历史证据；当前状态、测试数字和下一票以本段 `2026-08-10 当前复核` 及总计划入口为准。

> **2026-08-10 MF-AUTH-N1W Web 首登增量**：前端已接 owner 首次创建、Auth App 本地二维码/字符串、首次 TOTP 确认、恢复码一次展示、TOTP/恢复码登录、会话恢复与 logout；Bearer 只存 `sessionStorage` 且当前只随认证/用户请求发送。gateway 增加认证能力协商，`auth/users` 固定走 control-svc；旧 Python 单体明确 404 时兼容进入，其他探测错误 fail-closed。前端单元 `29/29`、浏览器 `4/4`、gateway `107/107`，两端构建通过、前端生产依赖审计 0。N1W 为 `Completed（开发门）`，N1 整体仍为 `Validating`。

> **2026-08-10 MF-AUTH-N1A 业务授权与账户管理增量**：gateway 已落全局四级授权矩阵（公开认证、节点机器信任、已登录业务、owner/admin 管理），通过 control `/auth/session` 实时校验 Bearer；节点注册/心跳/发现/模型文件分发保留机器信任路径，`QLH_AUTH_REQUIRED` 可显式控制 rollout。control 新增本地会话列表/撤销，用户列表补充 TOTP 状态与活跃会话数且不暴露 token hash，admin 不能替换 owner Auth App。Web 账户工作区已接会话撤销、恢复码轮换、用户创建/编辑/暂停/启用、Auth App 二维码/字符串配置和恢复码一次展示；桌面/移动响应式场景均通过。control `313/313`、gateway `113/113`、前端 `29/29`、浏览器 `5/5`，构建通过、前端生产依赖审计 0。N1A 为 `Completed（开发门）`，N1 整体仍为 `Validating`。

> **2026-08-10 MF-AUTH-N2 API 增量**：Tailscale 绑定 API 已接入 control：本人/owner/admin 权限边界、pending→active、旧 active→revoked 原子换网、跨用户身份冲突、撤销幂等和 `credential_ref` 脱敏均通过；OAuth Apps 保持禁用。新增契约 `3/3`，control 全量 `309/309`，构建通过。客户端接线由同日 N2 客户端增量覆盖，真实 Tailscale CLI/浏览器授权、旧节点实际撤销和双机实网仍待验收。

> **2026-08-10 MF-AUTH-N2 客户端接线增量**：账户工作区已接当前 Tailscale 绑定状态、官方 CLI/本机状态方式选择、pending 确认、稳定 ID 录入、原子换网历史和撤销；客户端请求不携带密码、token 或 `credential_ref`。前端单元 `30/30`、浏览器 `5/5`、构建通过；N2 客户端开发门完成，真实 Tailscale CLI/浏览器、旧节点网络撤销和双机实网仍保持 `Validating`。

> **2026-08-10 MF-AUTH-N2L 本机状态适配增量**：control-svc 已提供受认证保护的本机 Tailscale 只读探测，使用无 shell 的 `tailscale status --json`、可覆盖 CLI 路径、有界超时/输出、短缓存和并发合并；只返回绑定所需候选 ID、主机/DNS 展示和受信 Tailscale IPv4/IPv6，不返回 loginName、公钥、Peer/Health、stderr 或本机路径。账户工作区可读取并填充候选，但保持显式 pending/confirm。N2L 开发门和 Windows 只读探测完成，未执行真实绑定或换网；N2 整体仍为 `Validating`。

> **2026-08-10 MF-AUTH-N1 Linux adapter 续票**：Linux Secret Service credential adapter 已接入默认凭据存储；`secret-tool` 只通过标准输入/输出传输密钥，JSON/SQLite 只保存随机 opaque handle，覆盖和删除会清理 keyring 项，超时/服务不可用保持 fail-closed。新增适配器契约 `2/2`，control 全量 `311/311`，构建通过。Web 接线已由后续 N1W 完成，Linux 实机 DBus/Secret Service 与完整安装首登仍待最终联合发布门，N1 继续 `Validating`。

> **2026-08-10 MODEL-TOOLS P0 增量**：P0 三件套已实施：`python scripts/model_tools.py inspect|verify|sweep`，底层位于 `scripts/model_tools/`。工具保持只读，GGUF 采用无外部包的头部/张量描述解析，`gguf_verify` 支持相邻 `.sha256` 侧车与可选全文件哈希，`models_sweep` 复用 SD manifest 校验并对 junction 不跟随遍历。定向测试 `5/5`；真实 `H:\qlh_models` junction 根目录发现 GGUF×2、SD manifest×5、PyTorch 目录×2，全部有效且无孤儿候选。该阶段 Python 全量回归 `1714 passed / 27 skipped`。当前状态为 `Validating`，待安装包/跨平台入口和发布路径复核；P1 后续状态由下方磁盘治理增量覆盖。

> **2026-08-10 MODEL-TOOLS P1 磁盘治理增量**：新增 `model_disk_usage` 与 `models_clean`。前者对 junction 实际目标按顶层资产统计 logical/allocated/unique allocated bytes；后者默认 dry-run，`--apply --confirm CLEAN` 才允许删除，并在删除前再次拒绝越界路径和 junction/reparse point。缓存/旧备份需要额外类别开关，重复内容永不自动删除。定向测试 `11 passed / 1 skipped`、Python 全量 `1720 passed / 28 skipped`；真实 `models` junction 与 `H:\qlh_models` 统计均为 204 文件、41,238,213,220 bytes，8 个 cache 候选默认安全删除数 0，未执行删除。磁盘治理开发门完成，P1 下一票转 SD prompt/sampler 批测。

> **2026-08-10 MODEL-TOOLS P1 SD 批测增量**：新增 `sd15_prompt_batch` / `sd15_sampler_matrix`，固定矩阵上限 64，输出 PNG/contact sheet/JSON，复用资产完整性校验和现有自动图像门；引擎请求级新增 Euler/DDIM scheduler。CUDA 侧车真实 4-step Euler+DDIM（2 输出）与 2 prompts×1 seed（2 输出）均通过自动门、hash 唯一、CLI 退出码 0。相关定向 `91 passed / 6 skipped`、CUDA 侧车恢复 `2/2`，Python 全量 `1723 passed / 30 skipped`。P1 SD 批测开发门完成，完整质量门/人工目视/安装包跨平台仍待验收，下一票转 P2。

> **2026-08-10 MODEL-TOOLS P2 同步检查增量**：新增 `models_sync_status inventory|compare`，以不含根路径的 schema v1 清单比较顶层模型资产。默认只读路径/大小形成结构摘要，显式 `--full-hash` 才读取内容；模式不同拒绝混比，外来清单校验失败返回 2，差异返回 1，一致返回 0。嵌套 junction/reparse point 不跟随，结果只列相对资产 ID。定向 `24 passed / 2 skipped`、Python 全量 `1728 passed / 31 skipped`；真实 `models` 与 `H:\qlh_models` 各识别 10 个资产并结构一致，输出无绝对路径。本票开发门完成，真实双机/跨平台仍待验收，下一票 `llm_smoke_matrix`。

> **2026-08-10 MODEL-TOOLS P2 LLM 冒烟矩阵增量**：新增 `llm_smoke_matrix`，从注册表发现 GGUF/Safetensors 单元，固定中文、英文、精确 JSON 和中等上下文 marker 四项。每个单元独立离线子进程，HF 缓存用完即清，token/单元数/超时有硬上限；缺资产或 RAM/VRAM 余量不足显式跳过，报告不留生成原文和模型路径。Qwen 1.8B 两格式 `2/2 units、8/8 jobs` 通过；完整注册表 10 单元中 2 通过、6 缺资产、2 资源拒绝、0 失败，执行门通过但覆盖不完整。定向 `29 passed / 2 skipped`、Python 全量 `1738 passed / 31 skipped`。结果见下条 GGUF 预检增量。

> **2026-08-11 L4-SD1.5 P1 / SD-N3.4 恢复 fencing 开发门完成**：TaskGraph 对已返回但未提交的结果增加幂等 disposer；远端 Diffusion 在 ingest 后取消/lease 过期立即删除主节点 CAS；精确 `distributed:<workflow_id>` owner scope 清理支持撤销重启遗留 lease；SQLite journal 恢复 `result_ready` 图像工作流时，启动同步回收未提交输出。新增取消/ingest 竞态、跨 owner 清理、SQLite journal 恢复用例，扩大定向回归 `133/133` 全部通过，Python 全量 `1770 passed / 31 skipped`。`SD-N3.4` 开发门关闭，双物理 PC 与固定多 seed fan-out/fan-in 转入下一票 `SD-N4`。

> **2026-08-11 L4-SD1.5 P1 / SD-N4 非硬件开发门完成**：固定四 seed `image_generate` fan-out、精确 manifest Worker 池、稳定轮转/fallback、单节点串行降级和主节点 `image_grid` fan-in 已落地。2×2 网格与成员图共用 owner scope 和父引用；API 按实际 winner 返回每图 `stage/provider/node/attempt`，仅至少两个实际 Worker 节点参与时报告分布式。单 seed 失败、取消、网格写入后回收和 journal 恢复均有自动化证据，扩大相关回归 `220/220`，Python 全量 `1777 passed / 31 skipped`。第二台 CUDA PC 缺位，跨机吞吐、网络/OOM/重连和 UI 发布门保持 `Blocked`；下一票转 `UP-N6.0` 启动器安装清单。

> **2026-08-11 UP-N6.0 安装完整性基线开发门完成**：新增严格 schema v1 `manifest/install-manifest.json`，对最终程序树的规范路径、大小、SHA-256 和 kind 做 UP-N2 Ed25519 签名；路径逃逸、链接/reparse、大小写碰撞、未知字段和用户数据根都 fail-closed。CPU/CUDA/Launcher/Linux 构建强制 `QLH_SIGNING_KEY`，Windows Inno `ssPostInstall`、Linux `postinst` 和 Launcher 远端 A/B staging 均验签，后者还绑定版本/变体/包身份。相关回归 `111/111`、Python 全量 `1795 passed / 31 skipped`，冻结验签 EXE 用临时授权链实测通过。干净机安装仍待跨平台验收，下一票 `UP-N6.1 verify`。

> **2026-08-11 UP-N6.1 runtime verify 开发门完成**：在已签名安装清单上新增 `quick/full/deep` 只读检查、稳定 JSON/退出码、`qlh-launcher verify`/`bjtu verify` 路由和启动调用异常后的自动 quick。验证只按签名清单枚举程序文件，用户数据根零遍历；full 对大文件只校验大小和固定区段可读性，schema v1 没有签名抽样摘要，完整防篡改由 deep 全量 SHA-256 提供。删除入口、签名包缺关键入口、同尺寸篡改、`health.ok` 丢失和签名失效均有故障注入，打包链路 `119/119`、Python 全量 `1803 passed / 31 skipped`。Windows/Linux 干净机验收仍未完成，下一票 `UP-N6.2 diagnose`。

> **2026-08-11 UP-N6.2 diagnose 开发门完成**：新增只读 `diagnose` 知识库和 `qlh-launcher diagnose`/`bjtu diagnose` 路由，覆盖安装签名、程序文件、版本、DLL/pyd、CUDA 驱动、磁盘、网络症状、权限、疑似杀软和模型错误共 10 类。诊断以 quick 结构化结果为证据，CUDA 驱动探测无 shell 且 3 秒有界；原始错误文本不写 JSON，模型和用户数据零扫描。GUI/TUI 可展示建议，UP-N4 ZIP 仅附状态目录内去路径、脱敏的诊断摘要。打包链路 `126/126`、Python 全量 `1810 passed / 31 skipped`；Windows/Linux 干净机验收仍未完成，下一票 `UP-N6.3 repair`。

> **2026-08-10 MODEL-TOOLS P2 GGUF 转换预检增量**：新增 `gguf_convert`/`gguf-convert` 只读 dry-run，从注册表或显式 source 读取 config、tokenizer、权重分片 index，校验 vendored `convert_hf_to_gguf.py` 架构注册、Python 依赖、`llama-quantize`、目标冲突、源边界、目标卷空间和可写性；不创建 staging、不调用转换器、不覆盖工件。F16/BF16/F32/Q8_0 为单步计划，Q4/Q5/K 系列为 F16→`llama-quantize` 两步，命令路径使用占位符。Qwen 1.8B F16 真实预检通过，Q4_K_M 因量化器缺失阻塞；目标未生成且 JSON 无绝对路径。定向 `40 passed / 2 skipped`、Python 全量 `1744 passed / 31 skipped`。执行事务已由下条完成。

> **2026-08-10 MODEL-TOOLS P2 GGUF 转换执行事务增量**：默认 dry-run 不变，写入必须精确提供 `--target --apply --confirm CONVERT`。执行前重跑预检，在目标同卷 staging 中以离线、无 shell、有界超时的子进程转换；Q4/Q5 先检查 F16 中间件，最终工件做 GGUF 结构和全 SHA256 校验。摘要旁车先于 GGUF 以无覆盖硬链接发布，失败、超时、取消、目标/旁车竞态均不覆盖且回收临时文件。合成真实子进程覆盖 F16/Q4 成功及全部故障边界，定向 `48 passed / 2 skipped`，Python 全量 `1752 passed / 31 skipped`。真实 Qwen 仅完成 F16/Q4 dry-run 与未确认执行拒绝，未转换、未生成新模型。量化器固定/打包开发票已由下条完成；真实转换仍需用户批准。

> **2026-08-10 MODEL-TOOLS P2 llama-quantize 固定与分发增量**：新增锁文件与无网络构建脚本，固定 llama.cpp `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`。Windows CPU-only 静态 EXE 构建、`--help`/Q4_K_M smoke、PE 系统 DLL 门、逐文件 manifest、确定性 ZIP 和 SHA256 均通过；受管发现标记 `managed_package/verified`。Qwen 1.8B Q4_K_M dry-run 已从 `quantizer_missing` 变为 `valid=true`，没有转换或新工件。工具链定向 `73 passed / 2 skipped`，Python 全量 `1760 passed / 31 skipped`。Linux 构建脚本和 `.deb` 固定安装路径已接线，Linux 真机编译/运行库/安装/执行仍待验收；下一票 Linux 真机工具链门。

> **2026-08-10 L4-SD1.5 P1 / SD-N3.4 真实 Worker 增量**：新增离线双进程 smoke，主进程运行 v3 TCP 协调器、TaskGraph 与持久 CAS，子进程实际加载固定原版 SD 1.5 Diffusers 并通过认证 HTTP 分块回传结果。RTX 4060 Laptop GPU 上 seed `19950101`、512×512、4-step 实测去噪 6.000 秒、端到端 15.765 秒，PNG 360,858 bytes、SHA256 `86665855…05bfa`，重复运行完全一致；报告拒绝模型绝对路径、集群密钥、grant 和 lease。SD 分布式定向回归 `94/94`，Python 全量 `1765 passed / 31 skipped`。本票关闭同机真实 Diffusers Worker 开发门，但 `SD-N3.4` 仍为 `In Progress`；下一票为迟到结果/journal 恢复 fencing，之后才进入两台物理 PC 与 SD-N4 多 seed 验收。

> **2026-08-10 当前复核**：`M1.3`、`MF-AUTH-N1W`、`MF-AUTH-N1A`、`MF-AUTH-N2` 客户端接线和 `MF-AUTH-N2L` 开发门已完成，`MF-AUTH-N1/N2` 整体保持 `Validating`。SQLite schema v7、owner bootstrap、TOTP 双下发/确认、恢复码、登录/撤销、用户管理、业务 API 会话/角色矩阵、账户管理 UI、Tailscale 绑定/换网、本机状态安全投影、审计和停用会话撤销均已落地。当前相关基线为 Python `1714 passed / 27 skipped`、control `317/317`、gateway `113/113`、前端 `30/30`、浏览器 `5/5`。当前执行维持 N2 外部验证；开发团队 Linux 真机仍在申请，Secret Service/DBus、完整安装首登、纯 IPv6/混合双栈、真实 Tailscale 和双机撤销集中到最终跨平台联合验收，当前不得标为通过。

> **本次 MODEL-FLEET 增量**：MF-N1-M5.0、M2.4-M2.5 Web 控制面和 M3.1-M3.3 网络/运行时/恢复门已完成；Python 全量 `1729 passed / 7 skipped`、Python schema `25/25`、control-svc `301/301`、gateway `104/104`、前端单元 `27/27`、模型工件浏览器 mock `1/1`，生产依赖审计 0 vulnerability。除 DeepSeek 7B GGUF 外，固定 Qwen2.5 0.5B revision `7ae5576…` 已经 `127.0.0.1:7897` 完整拉取 10 文件/999,604,126 bytes，工件 `f8dcc256…bbe9c` 在 CUDA BF16 两次离线加载 `ready` 并通过本地 deployment prepare。M3.3 已证明真实 control 跨进程从 393,216-byte staging partial 恢复，以及代理在 196,608 bytes 掉线、短时 503 后同一 job 有限重试成功；永久不可用明确失败。显存支线 `MF-MEM-N0/N1/N2` 合成侧车、N2R 真实 7B NF4 基线、N2F 固定 64 MiB bridge/取消/长输出门和 N2P Windows 真实延迟决策已完成；N2P 已冻结双槽 host prefetch 默认优化，不进入默认吞吐路径。认证支线 `MF-AUTH-N0` 已完成文档调研：QLH 采用本地主节点 Auth App/TOTP 登录，认证/用户/绑定数据不交给开发组管理；Tailscale 只做稳定 ID 绑定和网络授权，不是 QLH 的通用登录提供方。存储主线 `M1.2` 七片及双平台隔离环境门已完成：Python/Node 共用用户可写 SQLite，Windows 11 的干净目录、损坏活动库、NTFS 写入 deny ACL，以及 WSL2 Ubuntu 22.04 ext4 的干净目录、损坏库、`0555/0444` 权限门均通过并 fail-closed；所有探针使用临时目录且已清理。`NET-N1` 已完成 TCP、Python API、control-svc、gateway、分发服务和 PC/TUI/Android 的本地双栈实现，Windows 真实三服务及 Windows/WSL socket 门通过；双机纯 IPv6/混合网、真实 Tailscale IPv6 与安装包仍待验收。总体下一步仍执行 `M1.3` 远端 PostgreSQL 退场准备，之后进入 `MF-AUTH-N1/N2`；网络实机门在条件具备时并行补齐。独立 `.qlhbackup` 只包含 SQLite，`.qlhmigrate` 才携带实际引用 blob，两者都不迁移 OS 凭据密文；当前没有登录 API、TOTP 生成或真实 Tailscale 授权。远端数据库仍默认关闭。以上不等于 Windows/Linux 完整安装、首次认证、gated/认证代理、非 Windows credential adapter、真实 Tailscale、Auth App 实现、PC Worker 或 PG 长时验收。

本文只做三件事：把当前能力按统一状态口径列一遍、把本轮产出说清楚、把下一步排成可判定完成与否的清单。**它不新增任何技术结论**；所有下一步都是《调研方案》§4 既有执行方案（RQ1–RQ8、E0–E4、阶段 0–3）与各专项文档既有里程碑的延续，不另立平行计划。

---

## 1. 状态口径

全文只用四档状态，互不混用：

| 状态 | 含义 |
|------|------|
| 已实施并验证 | 代码在树内，且有通过的自动化测试或实测记录支撑；实测条件在"关键限制"列注明 |
| 已实施未接生产 | 代码在树内、自测通过，但入口是实验开关/实验端点，未进入默认执行路径 |
| 已实施并编译验证 | 代码在树内，目标源码已通过编译，但尚缺安装、运行或目视验收证据 |
| 已实施未编译验证 | 代码在树内，但本轮环境无法编译或运行，正确性尚无任何执行证据 |
| 规划中 | 只有设计与判据，无代码 |

---

## 2. 一页速览

读法：**"当前状态"列是本文唯一的能力口径**，与"关键限制"列必须一起读——一条能力标为"已实施并验证"，只说明它在"关键限制"注明的条件下被验证过，不代表在真机集群、真实模型或生产路径上成立。凡在两列之间产生歧义的，以"入口文档"列指向的专项文档与源码为准。

### 2.1 能力总表

| 工作流 / 模块 | 能力 | 当前状态 | 入口文档 | 关键限制 |
|---|---|---|---|---|
| 分布式控制面 | 注册、心跳、断线重连、健康监控、分层配置推送 | 已实施并验证（单机双进程） | 《细化方案》§3.1、本文 §3.1 | 真机双设备联调未做（从节点硬件维修中）；本轮 6 项缺陷修复只在模拟双节点复现过 |
| PyTorch 层间流水线 | 连续层段分配、`LAYER_FORWARD`/`CHAIN_FORWARD`、增量 KV | 已实施并验证（单机） | [分布式推理流水线实施计划](分布式推理流水线实施计划.md)、《细化方案》§3 | 《细化方案》§3.1 的 13 行待测试矩阵全部待真机执行；未满足 §3.3 六条证据前不得标"已验收" |
| 任务链 / Worker 体系 | `dual_candidate` DAG、journal、Provider registry、PC Full Worker v2 | 已实施并验证（本机门槛） | [任务链下一阶段实施计划](任务链下一阶段实施计划.md) | 停在 **TC-N2.4 物理设备准入**；`adapter_connected=false`、`task_dispatch_enabled=false` 保持不变 |
| 跨平台 TUI 管理菜单 | 7 个管理屏幕、`--plain` 降级、`--host` 直管远程主节点、一键启动（自动带后端）、T9 Textual 简化聊天页（`bjtu chat`） | 已实施并验证（单机双进程 + 网关契约 44/44 + 7 屏 × 2 角色走查；T9.0-T9.5、54/54 布局走查及 Launcher/BJTU 接线均已完成） | [TUI 使用指南](TUI使用指南.md)、[TUI 适配与聊天页实施计划](TUI适配实施计划.md)、README「TUI 管理菜单」节 | Windows/Linux 真机远程管理未做；inference-svc interactive 历史事务提交、分布式真机指标一致性、主应用包聊天依赖和默认入口仍待验收 |
| Web 前端（聊天/管理/任务链/本地图像） | 会话、管理面板、工作流可观测摘要、SD 1.5 本地文生图、图生图、参考图、局部重绘与指令编辑工作区 | 已实施并验证（本地 SD-N1、SD-N5.1/5.1A/5.2/5.3 的自动门与 Edge 链路；N5.1/5.1A/5.3 双人目视已完成） | README、《任务链计划》§N1.6/TC-N2.3、[SD 1.5 计划](SD%201.5引擎与分布式图像生成实施计划.md) | 图像页只在主节点开放且固定显示“本地”；正式离线资产包、真实 Diffusers Worker、跨 PC fan-out/fan-in 和分布式统计尚未完成；IP-Adapter 不承诺精确身份锁定 |
| Web 前端 · MODEL-FLEET 本地控制面 | 工件汇总、运行时可运行性、导入并检查、pull 任务进度/取消、来源/DPAPI 凭据/许可管理、拉取预检确认、运行时复检/失效和 Git 风格用户代理 | 已实施并验证（前端单元 27/27；本地 mock 浏览器 1/1；Qwen2.5 真实工件经同一 control API 验收） | `frontend/src/components/ModelFleetPanel.jsx`、`control/src/modules/models/artifact-inventory.controller.ts`、`control/src/modules/models/model-security.controller.ts` | Web 交互仍为本地 mock 浏览器门；真实完整 pull/Safetensors 单机准入已完成，跨节点分发和 Tailscale 未完成 |
| Web 前端 · 本地主节点认证 | owner 首登、Auth App QR/字符串、首次确认、恢复码、登录/会话恢复/logout、账户与用户管理、会话撤销、Tailscale 绑定/换网 | 已实施并验证（N1W/N1A/N2 客户端开发门；前端 30/30、浏览器 5/5、gateway 113/113） | `frontend/src/components/AuthGate.jsx`、`frontend/src/components/UserManagementPanel.jsx`、`frontend/src/components/TailscaleBindingPanel.jsx`、`gateway/src/modules/auth/auth-policy.guard.ts` | gateway 已执行 public/machine/authenticated/manager 四级矩阵；真实 Tailscale、默认发布拓扑切换、Linux/完整安装与真实跨节点证据留最终联合验收 |
| Web 前端 · 外部推理开关 | 设置面板中的"允许外部/优先外部"开关 | 规划中 | 本文 P1-4 | `frontend/src/api/client.js` 已能传 `allow_external`/`prefer_external`，但无 UI 入口 |
| Android 客户端（功能层） | 全有/全无模式、SAF 模型目录、llama.cpp JNI；llama.cpp 已迁移为锁定 submodule | 已实施并验证（Full Release 构建） | [Android版本远期计划](Android版本远期计划.md)、`android/app/src/main/cpp/LLAMA_CPP_VERSION.md` | 不参与 PyTorch 层拆分（`pipeline_worker=false`）；完整 Worker 未实现；迁移后真机回归未做 |
| Android 客户端（UI 层重构） | Material 3 主题体系、`ui/components/Common.kt`、三屏重排 | **已实施并编译验证** | 本文 §3.3 | 2026-08-03 `assembleFullDebug` + `assembleLiteDebug` 均构建成功（`app-full-debug.apk` ~29.9 MB、`app-lite-debug.apk` ~18.4 MB）；尚未安装 APK 或完成真机目视验收 |
| 路线 A：TP 孤岛 | `engine=island`，孤岛=一个逻辑高算力节点，承担整请求 | 已实施并验证（阶段 1 PoC） | [TP孤岛接入指南](TP孤岛接入指南.md) | 不参与层拆分（A-1 层段形态未实现）；未在真实 vLLM/SGLang 孤岛上实测 |
| 路线 B：外部推理服务 Provider | `engine/execution_mode=external_api`，按请求路由 | 已实施并验证（阶段 1 PoC） | [外部推理服务Provider接入指南](外部推理服务Provider接入指南.md) | 数据作用域默认 `opt_in` 且需显式 `allow_external`；无会话粘性、无成本硬限、无前端开关 |
| 路线 C：draft-verify 投机解码 | 投机采样核心 + `SpeculativeSession` + 外部 verify 客户端 | **已实施未接生产**（阶段 0-1 探索性 PoC） | [投机解码外部辅助实施说明](投机解码外部辅助实施说明.md) | 入口仅 `POST /api/experimental/speculative`，默认关闭时 404；draft 侧是占位实现；`PagedKVCache.truncate(n)` 已完成，但尚未接入真实 draft 或生产循环 |
| 独立 Launcher/Updater | GUI/TUI Bootstrap、版本清单、Ed25519 验签、版本目录、A/B 自更新和回滚 | 已实施并验证（源码、Launcher ZIP 与本机双槽闭环） | [独立安装包启动器与自动更新方案](安装包自动更新引导器方案.md) | Windows 主应用发现/切换、Linux `.deb` 干净机、断网/权限场景和 Android 更新仍待验收 |
| 打包分发 | PC 集显/独显安装包、Linux .deb、Android APK、分发服务器 | 已实施并验证（历史主应用包 + 2026-08-08 Launcher ZIP） | README「打包版」节、`packaging/README.md` | 主应用本轮尚未完成统一版本清单与干净机验收；TUI 聊天依赖尚未进入主应用包 |
| 弱网通信 | UDP 阻断诊断、自建 DERP 基线、WSS/Transport v2/netshim 路线 | 已完成基线与方案调研 | [抗弱网通信协议专项计划](抗弱网通信协议专项计划.md) | NW1/NW2 观测与 DERP 运维加固待执行；WSS、ICE-TCP、Transport v2、UDP-over-WSS 均未实现 |

### 2.2 分布式执行形态对照（《文档状态与维护规则》第 10 条）

"分布式"不用是/否表示。当前可选执行形态如下：

| 执行模式 | Provider | 参与节点（统计口径） | 故障边界 | 降级路径 |
|---|---|---|---|---|
| `pytorch_layer` | 本地 PyTorch 层段（`scheduler` + `model_module`） | 主节点 + 同 artifact 的 PC 从节点 | 节点下线 → 重编排 → 当前 attempt 失败 | 回退主节点完整模型 |
| `task_graph` | `local_full_model` / `remote_full_worker` | 主节点 + 已认证 PC Full Worker（仅完成 attempt 的计入 `nodes_participated`） | attempt / lease / epoch / winner fencing | 跨 Provider 重派 → 本地 `local_full_model` |
| `island`（路线 A） | 网关节点的 island 引擎 | 孤岛计 **1 个**逻辑节点；岛内 N 张卡不得计入 | 网关探活失败 = 该逻辑节点下线 | 复用现有节点下线 → 重编排路径 |
| `external_api`（路线 B） | `ExternalOpenAIProvider` / `ExternalChatClient` | 外部端点**不入图**、不计为集群节点 | HTTP 4xx/5xx/超时按 `retryable` 分类 | 回退本地路径并记 `fallback_reason` |
| `speculative_assisted`（路线 C） | 本地 draft + `ExternalVerifyClient` | 本机 + 外部 verify 端点 | 取消粒度为轮边界；已发出的 verify 不可服务端取消 | **无生产降级路径**——未接入 `/api/chat`，仅实验端点可达 |

---

## 3. 本轮完成了什么（2026-07-26）

### 3.1 分布式控制面：6 项已验证缺陷 + 3 项附带问题

**做了什么。** 这 6 项都不是"代码不好看"，而是在真实集群里会表现为具体运行故障——对答辩与结题材料而言，这一列比修复本身更有说明力：

| 缺陷 | 此前会怎样表现为运行故障 |
|---|---|
| 客户端接收循环异常未终结连接 | **僵尸连接**：心跳仍在发、主节点仍视其在线、该节点永不重连；派下去的任务无人执行 |
| 帧中途超时未复位读取状态 | 字节流失步，其后所有帧解析错位 |
| 注册失败使重连线程被孤儿化 | 恢复延迟最长 **60 秒** |
| 服务端 5s 收包超时架空 12s 心跳容忍 | 从节点卡顿 6 秒即被**误踢**，并触发**全量重推分层**——一次网络抖动等于整集群重编排 |
| 三处 ACK 绕过发送锁 | 帧交叉损坏 |
| 9 个 async 端点同步阻塞事件循环 | 连接不可达主节点时**前端整站假死约 50 秒** |

附带修复三项：`Scheduler._tcp_client` 未初始化、健康监控首轮假"恢复"误报、一个在原始基线即失败的测试夹具缺陷。

**怎么验证的。** 双节点实测（单机双进程模拟）：注册 20 ms、`SIGKILL` 后约 1 秒标记离线、重连秒级恢复、30 秒无抖动。

**还剩什么。** 真机双设备联调。**从节点硬件仍在维修**；此前的上线失败是硬件故障而非软件缺陷，但这也不能反过来当作"真机已验证"。见 P0-2。

### 3.2 跨平台 TUI 管理菜单

**做了什么。** `src/tui_admin.py`（1749 行，纯标准库零依赖，Windows/Linux/macOS），7 个管理屏幕：系统总览、节点管理、分布式与分层、请求队列、设备画像、日志、设置。非 tty 环境自动降级 `--plain` 编号菜单；`--host` 可直接管理远程 Tailscale 主节点。2026-08-02 TUI 网关适配（T1-T8）完成：`gateway/test/tui-contract.e2e-spec.ts` 44/44（38 端点调用点 + 5 项契约细节 + 错误契约），`src/legacy_control.py` 承载 `/logs/*`，`tui_admin.py` 零改动。2026-08-03 复核：契约测试与双角色走查再次全绿；`start_tui.bat` / `start_tui.sh` 升级为一键启动（自动探测并启动后端 → 等待 `/api/health` 就绪 → 进入 TUI，退出后后端常驻）。使用说明见 [TUI 使用指南](TUI使用指南.md)。

**怎么验证的。** 双节点（单机双进程）实测，含"连接主节点"全流程；`scripts/tui_walkthrough.py --mode master|client` 7 屏 × 23 动作自动化走查（配套 `scripts/dev_stubs.py` 桩与 `src/legacy_control.py`）；网关契约测试 `npm run test:tui`。

**还剩什么。** 真机 Windows / Linux 各跑一轮，见 P0-3。

### 3.2.1 T9 简化聊天页（Textual，`bjtu chat`）

**做了什么。** `src/tui_chat.py`（Textual 8.2.8 + httpx 可选依赖，独立进程入口，不影响管理 TUI），参考 Claude Code 交互形态的终端对话页。后端契约同步落地：`streaming_mode=interactive`（start→token*→done|error|cancelled，完成时 user+assistant 一次事务提交）、请求级 `routing_preference`（local_only 强制本地 / distributed_preferred 可回退 / distributed_required 明确失败，均接入 full 与流式执行路径）、`src/tui_sse.py` 增量 SSE 解码器与 `src/tui_shared.py` 共享层（端点常量/命令注册表/metrics 格式化，纯标准库可复用）。聊天页支持：fixture 重放与真实后端双模式、Enter 发送 / Alt+Enter 换行、Ctrl+C 取消（epoch fencing 防迟到 token 污染）、错误恢复状态机（断线/空响应/取消均有确定状态）、会话 new/resume/rename/delete 与启动恢复最近会话、metrics 状态行（引擎/路由/tokens/tok/s/fallback 原因）。环境：`packaging/requirements-tui.txt`、`scripts/setup_tui_env.py`（.venv-tui 隔离、离线 wheelhouse）、`bjtu chat` 启动路由（缺依赖只提示，不污染全局解释器）。

**怎么验证的。** `tests/test_chat_interactive.py`（17 用例：事件序列/取消/错误/事务提交/路由偏好四语义）、`tests/test_tui_sse.py`（13 用例：任意 chunk 切分/UTF-8/多事件/keepalive/残帧）、`tests/test_tui_chat.py`（17 用例：Textual headless fixture 重放/命令/会话/竞态/错误恢复）、`tests/test_tui_shared.py`（15 用例）；T9 相关回归保持通过，完整 Python 回归的最新总数见 §3.5（当前存在两项独立协议测试失败）。

**还剩什么。** 终端布局快照验收已完成（`scripts/tui_chat_walkthrough.py` 54/54 三尺寸通过，截图见 `build/tui-chat/`）；inference-svc 执行段路由偏好同步已完成（interactive 历史事务提交仍待）；物理从节点可用后验证 distributed 真实参与与跨端指标一致；安装包携带聊天依赖与默认入口决策（T9.6）。

### 3.3 Android UI 层重构

**做了什么。** **仅 UI 层**：ViewModel、网络与 Service 层零改动。新增 Material 3 主题体系（`ui/theme/`）与 `ui/components/Common.kt`，重排 ChatScreen / SettingsScreen / SessionListScreen；其中 SettingsScreen 由 61 KB 混排改为 8 个分组卡片，36 个参数 / 回调 / testTag 全部保留。

**怎么验证的。** 2026-07-28 在 Windows/JDK 21 环境执行 `android\\gradlew.bat :app:compileFullDebugKotlin`，构建成功（16 个 task，11 executed / 5 up-to-date）；2026-08-03 在 Windows/JDK 21 执行 `android\\gradlew.bat :app:assembleFullDebug :app:assembleLiteDebug`，**BUILD SUCCESSFUL**（1m31s，80 tasks），产出 `app-full-debug.apk`（~29.9 MB）与 `app-lite-debug.apk`（~18.4 MB）。这证明 Full/Lite 的资源处理、KSP 与 Kotlin 编译、native 库与打包链路可通过，但不等同于 APK 安装或 UI 目视验收。

**还剩什么。** 执行 `assembleFullDebug`、安装 APK，并目视核对亮/暗主题、长消息气泡与三屏交互；这是明确的待办项，见 P0-1。

### 3.4 外部辅助三条路线（《调研方案》§2 的 A / B / C）

**路线 A「TP 孤岛」——阶段 1 PoC 已实施并验证。** `src/island_engine.py`，`engine="island"`，后端为 OpenAI 兼容端点。孤岛对集群呈现为**单个逻辑高算力节点**，承担整请求推理，**不参与 PyTorch 层拆分**（主节点画像过滤 + 网关侧拒收 `LAYER_CONFIG` 双保险）。`QLH_ISLAND_*` 含聚合能力声明，用于画像与排序加权。验证：专项单测 + 双节点 e2e。指南见《TP孤岛接入指南》。剩余：真实 vLLM/SGLang 孤岛实测、`internal_benchmark_passed` 门控、A-1 层段形态。

**路线 B「外部推理服务 Provider」——阶段 1 PoC 已实施并验证。** `src/external_provider.py`，`engine/execution_mode="external_api"`。**数据作用域门控是硬约束**：`deny` / `opt_in`（默认）/ `allow_all`，非法取值 fail-closed 回落 `deny`；请求需显式 `allow_external`，并由 `prefer_external` 或长度阈值触发；外部失败回退本地并记 `fallback_reason`；流式为真流式逐 token。验证：专项单测 + e2e（`deny` 档位下用户正文零出网、凭据零泄漏）。审查中另修复 6 个真实缺陷，其中两个值得记录：**外部失败回退绕过流水线预约/转发约束，导致已预约的 worker 误加载整模型**；**`/api/status` 同步探活冻结事件循环 3 秒**。剩余：会话粘性、成本上限、前端开关。

**路线 C「draft-verify 投机解码」——阶段 0-1 探索性 PoC 已实施，未接生产。** `src/speculative.py`；入口是独立实验端点 `POST /api/experimental/speculative`，`QLH_SPEC_ENABLED` 默认关闭时返回 404；**未接入 `model_module` 生产解码循环**。验证：专项单测含 20 万次抽样的逐 token 边缘分布、6 万次会话联合分布等价性验证，以及 8 个注入缺陷全部被杀死的变异测试。已知边界（详见《投机解码外部辅助实施说明》§7）：

- 接受判定**精确**——不对 top-k 截断行做归一化。曾因归一化把接受比值放大 `1/M`、草稿被橡皮图章（`M` 越小接受率越趋近 1.0，输出退化回 draft 分布），已修复。
- 修正 token 的残差采样仍受 top-k 截断：`QLH_SPEC_TOP_LOGPROBS` 20 → TV 0.155、64 → 0.058、200 → 0.035、全量 → 0.035。
- 标准 `completions` 的 `echo=true` 使下行约 12 KB/轮（《调研方案》估 1 KB/轮）；要达该量级须自建只回后缀行的 verify 端点。
- `top_logprobs` 的 key 是 token 文本，vLLM 需 `--return-tokens-as-token-ids`。
- **`PagedKVCache.truncate(n)` 已完成**：按“移除尾部 n 个 token”语义同步截断页表、回退尾页 `used` 并回收完整空页；独立 pytest、固定种子随机状态机和 CUDA 快速冒烟均通过。它尚未接入真实 draft/生产循环，不能据此宣称投机解码已生产化。
- draft 侧目前是占位实现；真实收益必须用同族小模型（Qwen-1.8B draft + Qwen-7B/14B verify，共享 tokenizer）实测，**不承诺加速比**。

### 3.5 测试与运行环境

2026-08-09 L0-5 收口：两个 v2 测试调用的 `_validate_capabilities(..., version=2)` 已落地（`tests/test_external_provider.py`、`tests/test_speculative.py`）；干净工作区全量 `pytest -q` 实测 **1657 passed、7 skipped、0 failed**，L0-5 转 `Completed`。MODEL-FLEET 增量 control-svc 全量 **274/274 passed**，gateway **104/104 passed**，前端单元 **27/27 passed**，模型工件浏览器 mock **1/1 passed**。2026-08-06 的 `1416 passed / 4 skipped` 仅保留为历史复核数字。

SD 1.5 侧车已完成原版、图生图、IP-Adapter reference、inpaint 和 InstructPix2Pix 的本地自动门；N5.1/5.1A/5.3 的双人目视也已完成。分布式图像已越过 N3.4 fake executor 边界，在同一 Windows CUDA 主机上完成独立真实 Diffusers/GPU Worker 的 v3 TCP+认证 HTTP 单 Stage 闭环；迟到结果/journal 恢复、两台物理 PC、多 seed fan-out/fan-in 和 UI 统计仍未完成。90s DreamBooth 4.87 GB 组合资产固定下载、校验和十种子自动门均有记录。Android Full/Lite assemble 及迁移后的 Full Release 构建通过，仍未安装 APK 或完成真机目视验收。另：`api_server.py` 两处仅 3.12 可解析的 PEP-701 f-string 已改写，全项目现可在 3.10 / 3.11 / 3.12 解析。

---

### 3.6 本轮明确没有做的事

为避免下一阶段在错误前提上排期，以下事项本轮**一件都没做**，不要在任何材料里当作已完成：

- 没有做任何真机双设备联调——控制面、TUI、层间流水线、任务链 Worker 的真机证据一律为空。
- Android Full Debug 已通过 Kotlin 编译，但没有执行 APK 安装与真机目视验收；Lite 本轮未编译。
- 没有完成主应用的统一版本发布包和干净机验收；但 2026-08-08 已构建 Launcher ZIP，并通过独立 Setup 隔离安装/卸载及 Launcher A/B 双槽 download → stage → health → activate → rollback 闭环。TUI 聊天依赖仍未进入主应用包，路线 A/B/C 仍未进入默认生产路径。
- 没有改动 `model_module` 的生产解码循环，也没有把 `speculative_assisted` 加进 `/api/chat` 的 `execution_mode` 取值集合。
- 没有把路线 A/B 的 Provider 自动注册进聊天任务链协调器（避免"首个兼容者"字典序选择劫持默认本地工作流）。
- 没有改动 `GraphOrchestrator` 的图算法，也没有引入超节点画像字段。
- 没有在任何真实 vLLM / SGLang / llama.cpp RPC 后端上跑过端到端实测；校园网 UDP 阻断与自建 DERP 可达性已完成脱敏基线采集，WSS/Transport v2/netshim 尚未实现。

---

## 4. 当前能力边界（必须诚实）

| 容易被误读的说法 | 实际情况 |
|---|---|
| "QLH 支持投机解码" | 路线 C **不是生产能力**。入口只有实验端点 `POST /api/experimental/speculative`，`QLH_SPEC_ENABLED` 默认关闭时 404；`/api/chat` 的 `execution_mode` 取值集合仍只有 `auto` / `task_graph`，未接入生产解码循环 |
| "Android 客户端已重构完成" | 只改 UI 层；Full Debug Kotlin 已编译通过，但尚未安装和目视验收。`gradlew.bat assembleFullDebug`、安装与逐页核对完成前，不能算 UI 验收完成 |
| "分布式控制面已修复并验证" | 验证在**单机双进程**下完成。从节点硬件在维修，真机双设备联调尚未做。此前的上线失败是硬件故障而非软件缺陷，但这不等于"真机已验证" |
| "全量回归已全绿" | 当前成立：M1.2 Python 主运行路径第六片后全量实测 `1720 passed / 7 skipped / 0 failed`；L0-5 关闭时基线为 `1657 / 7 / 0`。此前（2026-08-08）临时验证为 `1643 / 7 / 0`，当时正式基线为 `1641 / 7 / 2` |
| "PoC 已经跑出性能数字了" | **PoC 的性能数字都未在真实模型上实测**。路线 C 的 `acceptance_rate` / `tokens_per_round` 来自 mock verify 后端 + 占位 draft；路线 A/B 尚无真实 vLLM/SGLang 端到端实测。**不承诺任何加速比** |
| "接上孤岛就是多节点分布式推理" | 对 QLH，孤岛是**一个**逻辑节点。岛内 N 张卡不得计入 `nodes_participated`（《细化方案》§6.2） |
| "外部推理服务已经可用了" | 默认 `opt_in` 且请求必须显式 `allow_external`，未授权数据不出集群。是否放开 `allow_all` 是**人的决定**，不能因为要跑通实验就改默认值 |
| "1650 项全部通过" | 不准确；临时验证结果为 `1643 passed / 7 skipped / 0 failed`，但当前仓库正式基线仍为 `1641 passed / 7 skipped / 2 failed`。跳过项仍需对应环境或依赖，失败项需先落地修复 |
| "任务链分布式已经能用了" | 停在 **TC-N2.4 物理设备准入**：`adapter_connected=false`、`task_dispatch_enabled=false` 依旧保持，只有显式实验开关下的路径可用 |
| "投机解码的输出分布与大模型严格一致" | 该性质是**投机采样纯核心**的性质。经 OpenAI 兼容接口只能拿到 top-k logprobs：接受判定精确，但**修正 token 的残差采样被 top-k 截断**，偏差随 `QLH_SPEC_TOP_LOGPROBS` 增大而收敛，自建端点返回完整 logits 才能彻底消除 |
| "孤岛可以承担层间流水线的重层段" | 那是 A-1 形态，**尚未实现**。已实施的是整请求形态：主节点在 `compute_layer_assignment` 中按 `device_info.island.enabled` 过滤孤岛节点，网关侧收到分层配置也会拒绝 |
| "Android 可以参与分布式推理" | 取决于指哪一种。Android 可作请求端与 presence 节点；**不参与 PyTorch 层间拆分**（上报 `pipeline_worker=false`，主节点排除 `node_type=android`）；完整 Worker 尚未实现 |

---

## 5. 下一步计划

排序原则：**先把 L0-5 的临时验证修复正式落地，再完成 Android 安装/目视证据**（P0-5、P0-1）；**硬件一到位立刻做解锁面最大的联调**（P0-2），随后按《调研方案》§4.3 的阶段 1 → 2 → 3 推进。Launcher/Updater 的干净机验收与弱网观测作为可并行的发布与网络支线，不改变 L0/L1 关键路径。所有编号中的 RQ/E/阶段均指《调研方案》§4。

### 5.1 关键路径

```text
不依赖硬件的并行支线
  P0-5 全量回归两项失败 ───────────────► （L0 基线恢复可解释）
  P0-1 Android 安装/目视 ─────────────────► （UI 重构获得运行证据）
  P0-4 truncate(n) ──────────────► P1-5 真实 draft 实测 ──► P2-3 自建 verify 端点
  P1-6 文档收口 ─────────────────────────► （结题材料可用）
  MODEL-FLEET M0.1 ──► M1.1/M2.1 ──► M3.1/M4.1 ──► M5.0 模拟器

硬件关键路径（唯一串行瓶颈）
  [从节点硬件维修完成]
        │
        ▼
  P0-2 真机控制面联调 + E0 ──┬──► P0-3 TUI 真机验证
                             ├──► P1-1 TC-N2.4 物理准入 ──┬──► P2-4 TC-N3.1 模板
                             │                            └──► P2-6 Android 完整 Worker
                             └──► P1-2 层间流水线验收 ─────► P2-5 RQ6 压缩基准

依赖外部算力（与硬件路径可并行）
  [可用 OpenAI 兼容端点]
        ├──► P1-3 孤岛实测 E1/E1b ──► P2-2 超节点画像 / A-1 层段
        └──► P1-4 路线 B 生产化 E2 ──┐
                                     ├──► P2-1 E3 混合任务链（还需 P1-1）
  P1-1 ──────────────────────────────┘
```

**唯一的串行瓶颈是从节点硬件。** 它一天不到位，P0-2 以下整条链都无法开始；因此外部算力支线（P1-3、P1-4、P1-5）应当**提前锁定端点资源并先跑**，不要排在真机联调之后。

### 5.2 与既有阶段编号的对应关系

本文不新立阶段，只把既有编号排进优先级：

| 本文编号 | 对应既有阶段 / 里程碑 | 出处 |
|---|---|---|
| P0-2 | 阶段 0 的 E0 链路基准；RQ1 | 《调研方案》§4.2 / §4.3 |
| P0-4 | C-1.3 本地 KV 回滚 | 《投机解码…说明》§8 |
| P1-1 | TC-N2.4 物理设备准入 | 《任务链计划》§TC-N2.4 |
| P1-2 | 层间拆分待测试矩阵与验收证据 | 《细化方案》§3.1 / §3.3 |
| P1-3 | 阶段 1（TP 孤岛 PoC 的实测部分）；E1 / E1b；RQ2 | 《调研方案》§4.3 |
| P1-4 | 阶段 2（Provider 路由）；E2；RQ8 | 《调研方案》§4.3 |
| P1-5 | E4；C-1.2 / C-1.4；§4.3 止损判据 | 《调研方案》§4.2、《投机解码…说明》§8 |
| P2-1 | 阶段 3（混合编排）；E3 | 《调研方案》§4.3 |
| P2-2 | RQ3 / RQ7 超节点模型 | 《调研方案》§3.2 / §4.1 |
| P2-4 | TC-N3.1 | 《任务链计划》§TC-N3.0 末尾 |
| P2-6 | Android P1 / P2 | 《Android版本远期计划》§8 |

《混合规划》H1（Provider 网关）与 H6（Auto planner，shadow 优先）仍是这些工作的上位框架；路线 B 的 Provider 落地即 H3 ExoProvider 的通用化形态，本文不重复其验收口径。

### 5.3 P0：立即（不等硬件的先做完）

| 编号 | 目标 | 前置条件 | 完成判据 | 关联编号 / 文档 |
|---|---|---|---|---|
| **P0-1** | Android UI 安装与目视核对 | 一台装有 JDK 17 + Android SDK API 34+ 的本地机器；Full/Lite assemble 已有证据 | 安装 Full APK；亮/暗/跟随系统及三屏清单通过；36 个参数/回调/testTag 全部可见可操作；确认 ViewModel/网络/Service 层无改动；迁移后的 Full/Lite 启动不回归 | README「Android 客户端」节；《Android版本远期计划》§8 P0 |
| **P0-2** | 真机双设备控制面联调 + E0 链路基准 | **从节点硬件维修完成**；两端同版本；Tailscale 同账号 | 注册 / 心跳 / 断线 / 重连 / 主节点重启五类故障注入在真机复现，且**不出现**僵尸连接、误踢重推分层、前端假死；直连与 DERP 各测一轮并留存 `tailscale status` 路径证据；产出《调研方案》§1.2 表格的实测版 | **E0**、**RQ1**；《细化方案》§3.1「网络异常」「主节点状态」行 |
| **P0-3** | TUI 真机远程管理验证 | P0-2 双机在线 | `python src/tui_admin.py --host <主节点 Tailscale IP>` 在 Windows 与 Linux 各跑一轮；7 个屏幕全部拉到真实数据；"连接主节点"全流程成功；管道/非 tty 下 `--plain` 正确降级 | README「TUI 管理菜单」节 |
| **P0-4（Completed 2026-07-30）** | 实现 `PagedKVCache.truncate(n)` | 无外部依赖 | 页表尾部截断 + 尾页 `used` 回退 + 空页回收三者齐备；往返一致性、20 轮页面复用、200 步固定种子状态机和 CUDA 冒烟通过；生产投机解码接线仍需独立评审 | **C-1.3**（《投机解码…说明》§8） |
| **P0-5（Completed 2026-08-09）** | 收口全量回归的两项协议校验失败 | 无外部依赖；两个 v2 测试调用显式 `version=2` 已落地 | 两个 v2 测试调用明确传入 `version=2`，并在干净工作区以 `pytest -q` 达到 `1650` 项、`0 failed` | `tests/test_external_provider.py`、`tests/test_speculative.py`；全量实测 `1657 passed / 7 skipped / 0 failed` |

> P0-4 当时排在 P0，是因为它不依赖硬件与外部端点，并且属于生产投机解码必需的本地正确性基础；现已完成。即使后续 C-1 触发止损被冻结，`truncate(n)` 对本地多模型投机解码仍然有用。

### 5.4 P1：本阶段

| 编号 | 目标 | 前置条件 | 完成判据 | 关联编号 / 文档 |
|---|---|---|---|---|
| **P1-1** | 完成 **TC-N2.4 物理设备准入** | P0-2；物理 PC 从节点加载与主节点完全一致的 Qwen-1.8B PyTorch artifact | 《任务链计划》§TC-N2.4「尚未完成」五条全过：真实 Qwen 推理、DeepSeek 兼容模型同等实测、错模型/缺模型/运行中切换/断网/重连/主节点重启故障注入、Tailscale RTT/吞吐/长文本/连续任务/资源压测、集显与独显安装包双端运行验证。全部通过前 `adapter_connected` 继续保持 `false` | 《任务链计划》§TC-N2.4、§14；《细化方案》§9「任务链拆分」 |
| **P1-2** | PyTorch 层间流水线真机验收 | P0-2 | 《细化方案》§3.1 十三行待测试矩阵全过，且 §3.3 六条证据齐备：实际 `execution_mode=pytorch_layer`、至少一个从节点完成层前向、层段/耗时/任务计数可追溯、输出通过基线比较、故障与取消测试通过、性能或容量至少一项明确收益 | 《细化方案》§3.1 / §3.3 |
| **P1-3** | 路线 A 真实孤岛实测 | 一台多卡机（或同 LAN 同构 GPU 组）；`vllm serve --tensor-parallel-size N` 起 OpenAI 端点 | **E1**：孤岛节点排序上浮并稳定承担整请求，达成容量收益（本地单卡装不下的 7B 可用）**或**端到端 P50 ≥20% 改善（《细化方案》§5.12 门槛）；网关 `SIGKILL` = 逻辑节点下线并触发重编排。**E1b**（llama.cpp `--rpc` GGUF 变体）30 分钟长稳且断线可清理，否则记 No-Go | **E1 / E1b**、**RQ2**、阶段 1；《TP孤岛接入指南》§4 |
| **P1-4** | 路线 B 生产化三件套：会话粘性 / 成本上限 / 前端开关 | 可用外部 OpenAI 兼容端点（即 **E2** 环境） | ① 同一会话连续请求固定同一 provider，切换需显式，并产出跨轮 prefill 重建成本曲线；② 可配置 token/费用上限，超限 fail-closed 且返回中文错误；③ 前端设置面板出现"允许外部推理""优先外部"两个开关，默认关闭，且 `deny` 档位下不可选 | **E2**、**RQ8**、阶段 2；《外部推理服务Provider接入指南》§9 |
| **P1-5** | 路线 C 真实 draft 模型接线与止损判据复核 | P0-4 完成；Qwen-1.8B draft + Qwen-7B/14B verify（共享 tokenizer）；verify 端启用 `--return-tokens-as-token-ids`（必需）与 `--enable-prefix-caching`（建议） | 给出真实模型下的 `acceptance_rate` / `tokens_per_round` / `verify_share` 实测值，并按止损判据给出**明确结论**：若 `tokens_per_round ≤ 1.5`，或 `RTT > tokens_per_round / 本地 tok/s`，则冻结 C-1、归档结论、只保留纯核心 | **E4**、**RQ5**、《调研方案》§4.3 止损；**C-1.2 / C-1.4** |
| **P1-6** | 文档与结题材料收口 | P0-5 完成后复核 | ① docs 内不存在指向不存在文件的链接，且 README 不再推荐已废弃迁移方案；② README 的版本、测试数字与 Python 版本声明与最新实测一致；③ 每篇现行文档头部状态/更新日期/适用范围齐全；④ 本文速览表、总体计划、专项文档状态互相一致；⑤ 全量回归失败项已解释或关闭 | 《文档状态与维护规则》§2 规则 1 / 4 / 6 / 8 |

> 排序理由：P0-5 的修复方案已临时验证但尚未落地，P0-1 当前又受 Android 设备缺失阻塞；代码修复落地后先关闭 L0-5，再在设备/模拟器可用时完成 Android 安装与目视。P1-1 与 P1-2 共用 P0-2 的真机窗口，且分别是任务链与层间两条主线唯一的"未验收"门槛；P1-3 / P1-4 / P1-5 依赖的是**外部算力**而非项目硬件，可并行推进；P1-6 可随本轮文档复核穿插。

### 5.4A L4-MODEL-FLEET 无硬件并行支线

L4-MODEL-FLEET 已从远期候选提升为当前 `P1` 并行主线。它不等待 Android、从节点或多 PC 设备，先推进控制面和本地工件正确性：

| 阶段 | 当前状态 | 本阶段目标 | 完成判据 | 下一动作 |
|---|---|---|---|---|
| M0.1 契约收口 | `Completed（本地门）` | schema 版本、迁移映射、fetcher 事件和未知字段策略统一 | Python/TS 各 `25/25`；兼容清单、合法/非法时序、终止态和单调进度均有证据 | 独立 fetcher venv/打包实测仍留在环境门 |
| M1.1 本地事实源收口 | `Completed（兼容基线）` | 记录现有 SQLite 重启、outbox 回放和 PG 降级能力，作为迁移前基线 | 事务回滚、重启保留 registry/job/profile、离线积压/恢复补投、退避和 stale version 拒绝均通过；不再把远端 PG 视为长期事实源 | `M1.2` 已完成；当前进入 `M1.3` |
| M1.2 主节点 SQLite 资产自持 | `Completed（存储实现与双平台隔离环境门，2026-08-09）` | 无 PostgreSQL 时，SQLite 独立承载认证、用户、Tailscale 绑定、模型/工件、集群、任务、会话、配置、审计和备份索引 | 七片已完成；Windows 11 与 WSL2 Ubuntu 22.04 分别通过干净临时目录、损坏活动库和真实 NTFS ACL/Linux 权限门，Python/Node 共库、schema v5 与 fail-closed 均有证据。Python `1720 passed / 7 skipped`、control-svc `297/297`、gateway `104/104`；迁移包仍不携带 OS 凭据密文 | 下一票执行 `M1.3`；完整安装与首次认证属于后续联合发布门 |
| M1.3 远端 PostgreSQL 退场准备 | `Completed（2026-08-10）` | 远端仅保留用户主动发起的一次性迁移审计边界，默认无 projector/outbox 运行时依赖 | SQLite v6 退场状态、备份/恢复演练、manifest 哈希锁定、配置/env/outbox 清理和 verify 均通过；远端不可用不影响本地核心功能 | 当前进入 `MF-AUTH-N1` 本地用户管理 |
| M2.1 工件导入收口 | `Completed（实际运行已登记）` | 既有模型目录迁移到内容寻址仓库 | 8 条目/10 来源实跑；2 个模型 ID 的 4 个工件、46 个 blob 成功，6 个源路径缺失；详见专项计划 §16.0 与源码测试 | Qwen 1.8B legacy adapter/信任策略 |
| M2.2 运行时准入侧车 | `Completed（基础与本机门）` | 权重加载隔离、超时/崩溃回收、节点/profile 状态登记 | DeepSeek 7B GGUF `ready`，超出本机资源门的 Safetensors `resource_rejected`；Qwen2.5 0.5B Safetensors 已由 M3.2 在 CUDA 主机真实加载 `ready`；详见专项计划 §16.0 与源码测试 | 更大 Safetensors/量化 profile 与真实 Worker 仍逐模型准入 |
| M2.3 运行时准入接线 | `Completed（本地控制面门）` | import/pull/API/部署过滤共用 runtime record | query/retry/invalidate/import、pull adapting、指纹/`stale`、prepare/activate 双检；DeepSeek API retry→prepare `ready`；详见专项计划 §16.0 与源码测试 | 本地 UI 已完成；真实 scheduler/Worker 心跳 |
| M2.4 本地模型管理控制面 | `Completed（本地 mock 门）` | artifact/runtime/pull/import/proxy 状态进入 Web 设置工作区 | `GET /models/artifacts` 汇总 alias/存储/runtime、`GET /models/pull` 任务列表、网关透传、导入/复检/失效/取消/代理交互；前端 27/27 + 浏览器 mock 1/1 | 真实权重与 Safetensors 主机由 M3.2 验收；跨节点分发仍待验收 |
| M2.5 来源/凭据与拉取预检 UI | `Completed（本地 mock + loopback 门）` | source、credential status、license acceptance 和 resolve 确认进入 Web | 来源编辑/禁用/恢复默认；DPAPI 凭据只写、状态列表与删除；许可接受/撤销；拉取携带 `source_id`，切源或改清单后旧预检失效；gateway 104/104 | 真实 gated 账号与非 Windows credential adapter |
| M3.1 一键 pull 收口 | `Completed（Windows 安全门 + 小工件真实网络门）` | resolve、Range、恢复、凭据、代理、gated 许可 | DPAPI、代理优先级、gated 双重门、blob size/LFS SHA、严格 206/Content-Range 与 7897 中断恢复；详见专项计划 §16.0 与源码测试 | 真实权重由 M3.2 验收；进程重启 partial、物理掉线、gated 和非 Windows adapter 仍待验收 |
| M3.2 真实权重与 Safetensors 准入 | `Completed（单机真实网络 + CUDA 门，2026-08-09）` | 完整公开仓库 pull、内容寻址去重、Safetensors 静态检查和离线试加载 | 固定 `Qwen/Qwen2.5-0.5B-Instruct@7ae5576…` 10 文件/999,604,126 bytes 经 7897 完整下载与 SHA 校验；工件 `f8dcc256…bbe9c`，494,032,768 参数在 CUDA BF16 两次加载 `ready`（2,534/1,674ms），本地 deployment prepare=`ready`；当时 control 267/267 | M3.3 已完成；gated 账号仍为外部凭据门 |
| M3.3 进程重启与代理掉线恢复 | `Completed（真进程 + 可控代理 socket 门，2026-08-09）` | 跨 control 生命周期复用同一 SQLite/staging；瞬时传输故障有限重试 | 393,216-byte partial 经第二个真实 control 重新 resolve 后同偏移 206 续传；代理在 196,608 bytes 断开并短时 503，同一 job 第 3 次尝试恢复；响应头超时不截断慢速大文件流；最终 size/SHA 一致，永久不可用为 `transfer_unavailable`；control 274/274 | 真实 gated/认证代理与非 Windows credential adapter 仍为环境门；下一可执行票 `MF-MEM-N2` |
| MF-MEM-N0 显存分页能力门 | `Completed（文档与本机诊断门，2026-08-09）` | 判断 8 GB NVIDIA GPU 的 UVM/主动预取可行性并规划支线 | 本机 WDDM 且 `concurrentManagedAccess=0`；Windows/WSL UVM `No-Go`、显式 pinned-memory pager `Go`、原生 Linux UVM `Conditional Go`；API 语义、传输下界和 Promote/Freeze 门已写入专项 §7.6 | `MF-MEM-N1` 已完成；先做隔离 `explicit_layer_pager`，不修改生产 runtime |
| MF-MEM-N1 固定 CUDA transport | `Completed（Windows 真实 CUDA transport 门，2026-08-09）` | 64/256/1024 MiB 对照 pageable/pinned、单/双/三槽和计算重叠 | EX-N1 两轮均 `12/12 passed`；pinned raw H2D `11.8-12.2 GB/s`、D2H `12.6-13.1 GB/s`；双槽 pipeline 增益不足 2% 且 P95 stall 放大，三槽冻结；checksum 与峰值资源均通过 | `MF-MEM-N2` 合成 sidecar 已完成；真实层工件对照尚未接入 |
| MF-MEM-N2 合成 explicit layer pager | `Completed（合成 sidecar gate，2026-08-09）` | 固定 64 MiB 双槽、有界 host 预取、显式 copy/compute event、取消和回收 | EX-N2 `4/4 passed`；CUDA batch=1/4 输出 digest 一致，取消第 2 层后 allocated/reserved=0，CPU equality control 通过，专项测试 `6/6`；WDDM overlap=0%，不宣称吞吐收益，未接生产 runtime | N2R 真实 NF4 placement/层迁移已通过；固定 64 MiB bridge 进入 N2F |
| MF-MEM-N2R 真实 7B NF4 对照 | `Completed（真实基线与模块级适配器，2026-08-09）` | 固定 Safetensors 上 resident NF4、`device_map=auto`、显式 module move hook、CPU-only 安全门 | artifact gate `3/3`、真实矩阵 `6/6`；context=128 batch=1 resident/auto tok/s `4.62/4.54` 且 digest 一致；context=2048 两个 batch 点通过；28 层 page-in/out 各 28 次；CPU-only 因 RAM 预算明确拒绝 | 固定 tile/延迟决策已由 N2F/N2P 隔离验证 |
| MF-MEM-N2F 真实固定 tile/取消/长输出门 | `Completed（真实固定 tile/取消/长输出门，2026-08-09）` | 固定 64 MiB × 2 bridge、显式分块搬运、layer-boundary cancellation、ctx8192 边界和 output 32/128 | 计划 `3/3 passed`；ctx128 完整、ctx128/8192 取消、output 32/128 完成；bridge 与模块驻留分开记录，专项回归 9/9 | 仅关闭受控搬运/回收门，不宣称吞吐优化；N2P 已完成并冻结默认 host prefetch |
| MF-MEM-N2P 真实预取/重叠决策 | `Completed（Windows 真实延迟/预取决策，2026-08-09）` | 预取距离 0/1、host staging overlap、CUDA stream overlap、TTFT/inter-token P50/P95/max 和 resident NF4 对照 | 计划 `3/3 passed`；digest 一致；host overlap 100% 但 CUDA H2D overlap 0%；E2E/TTFT 未改善，inter-token P95 仅小幅改善；取消回收通过 | `Freeze` 双槽 host prefetch 默认优化；N3 仅在原生 Linux + 32 GB RAM 条件满足时进入 |
| M4.1 多集群档案收口 | `Completed（控制面门）` | 档案 CRUD、验证、端点和 cluster_id 隔离 | schema-shaped DTO、activate/current、删除清理、active/env/Tailscale 候选顺序、双集群隔离均通过 | PC/Android/TUI UI 与真实 Tailscale |
| MF-AUTH-N0 认证边界调研 | `Completed（文档调研，2026-08-09）` | Auth App/TOTP、主节点数据所有权、Tailscale 稳定 ID 绑定和第三方协议门 | RFC 6238、`otpauth://` 双下发、Aegis GPL-3.0、Tailscale 非 IdP、OAuth clients 与 alpha OAuth Apps 差异已写入专项 §4/§10/§14/§20；不把 email/loginName 当主键 | 下一票 `MF-AUTH-N1`；无代码实现 |
| MF-AUTH-N1 本地用户管理 | `Validating（N1W/N1A 开发门完成，2026-08-10）` | 主节点 owner/admin/member、TOTP 字符串/二维码注册、恢复码、登录/撤销/审计、业务 API 角色授权和用户管理 UI | SQLite schema v7、Windows DPAPI/Linux Secret Service adapter、control 会话/用户契约、gateway 四级授权矩阵；control `313/313`、gateway `113/113`、前端 `30/30`、浏览器 `5/5`；用户资产仍只由主节点 SQLite/OS credential store 持有 | N1 外部验证与最终跨平台联合验收；N2 客户端开发门已关闭，不接 Tailscale OAuth |
| MF-AUTH-N2 Tailscale 一一绑定/换网 | `Validating（N2L/客户端开发门完成，2026-08-10）` | `tailnet_id + tailscale_user_id + node_id` 绑定，已有账号授权、首次注册引导和换 tailnet | control N2 定向契约 `7/7`、全量 `317/317`；本机状态安全投影、账户工作区、CLI/本机状态方式选择、pending 确认、原子换网历史和撤销已通过；前端 `30/30`、浏览器 `5/5`；Windows 只读探测可识别候选 ID 与 IPv4/IPv6；OAuth Apps 保持禁用 | 真实绑定/换网、旧节点网络侧撤销、Linux 和双机实网仍待验收；不以 loginName/email 作主键 |
| M5.0 无硬件部署模拟器 | `Completed（模拟门）` | prepare/activate/partial/rollback 状态机 | 三节点能力矩阵、错 digest/离线拒绝、epoch 激活、重启读取、rollback 均通过 | 真实 PC Worker/分发/预热/性能 |

真实 Tailscale、多 PC 分发、Worker 预热和跨节点回滚仍属于原 M5 的 L1 硬件门，不能用本支线的 mock 或单机 sidecar/API 证据替代；并且 Tailscale 绑定不等于 QLH 登录。M3.2 已完成公开真实权重与 Safetensors CUDA 准入，M3.3 已完成真实 control 重启 partial 与代理 socket 掉线恢复，MF-MEM-N1、N2 合成 sidecar、N2R 真实 NF4 placement/层迁移、N2F 固定 tile/取消/长输出与 N2P 延迟决策已形成基准；N2P 已冻结双槽 host prefetch 默认优化。`M1.2` 已完成七片：默认 local-only；SQLite 设置/会话/消息/workflow journal/review；SQLite v4 artifact/引用完整性；`.qlhmigrate` 流式迁移 SQLite 快照和实际引用 blob；SQLite v5 本地用户、TOTP 引用、恢复码哈希、审计与 Tailscale 绑定；Python 单体与 inference-svc 共库并本地优先；Windows/Linux 隔离运行时的干净目录、损坏库和真实只读权限门。认证资产可随 SQLite 重启和迁移，但 OS 凭据密文不会随包迁移，也尚未开放认证功能。下一步执行 `M1.3`，之后才进入 `MF-AUTH-N1`。完整安装、首次认证、模型导入和任务恢复组合冒烟仍是后续联合发布门。远端数据库默认关闭，仅作为用户主动兼容导出窗口，不能成为认证、模型、任务、恢复或备份依赖。显存票仍仅在原生 Linux + 32 GB RAM 条件满足时进入 `MF-MEM-N3` UVM 能力门，真实 gated/认证代理继续等待可用环境。详细方案见[一键模型部署与自治集群远期计划](一键模型部署与自治集群远期计划.md) §10、§11、§16.0。

### 5.5 P2：探索

| 编号 | 目标 | 前置条件 | 完成判据 | 关联编号 / 文档 |
|---|---|---|---|---|
| **P2-1** | E3 混合任务链（阶段 3） | P1-1 通过，且 P1-3 / P1-4 至少一条达门槛 | `candidate_a`=本地或流水线、`candidate_b`=外部端点、`aggregate`=本地的混合图跑通；嵌套统计准确（外层 `task_graph` + 内层 `stage_provider`），孤岛不得按卡数计入参与节点；外部失败时 fallback 链完整 | **E3**、**RQ7**、阶段 3；《细化方案》§4.15 / §4.16 / §6.2 |
| **P2-2** | 超节点画像进 `GraphOrchestrator` + A-1 层段孤岛 | P1-3 达门槛 | `island_aggregate_vram_mb` 仅在 `internal_benchmark_passed=true` 时被 `_extract_vram_mb` 采用；`_assign_layers` 的连续覆盖与首末段不变量单测不破；改动量评审结论落文档 | **RQ3 / RQ7**、《调研方案》§3.2；《TP孤岛接入指南》§4「后续」 |
| **P2-3** | C-1.1 自建 verify 端点（只回后缀 γ+1 行） | P1-5 未触发止损 | 下行回到 KB 级/轮，与《调研方案》§2.3 估算对齐；`bytes_down` 与 `bytes_down_ideal_suffix` 之比接近 1 | **C-1.1**（《投机解码…说明》§8） |
| **P2-4** | TC-N3.1 第二个固定模板与 payload 预算 | P1-1 | 分块汇总模板的受控 fan-out/fan-in、输入大小限制与摘要合并落地；**开始实现前**先冻结分块数量、单块字节数、总 token 预算与失败阈值；不开放模型生成任意 DAG | 《任务链计划》§TC-N3.0 末尾建议 |
| **P2-5** | RQ6：激活 / KV 的 INT8 传输量化基准 | P1-2（需真机流水线基线作对照） | 给出压缩开/关的 bytes/token、首 token 延迟与输出一致性对照；不达标则关闭开关并记录结论 | **RQ6**；[分布式推理流水线实施计划](分布式推理流水线实施计划.md)「张量压缩传输」 |
| **P2-6** | Android 完整 Worker 原型 | P1-1 通过（《细化方案》§8 优先级 3：先 PC 再 Android） | 《Android版本远期计划》§8 P1 验收：主节点派发一项完整推理、Android 返回结果、断网后任务可重派且旧结果被丢弃 | 《Android版本远期计划》§8 P1 / P2；《任务链计划》§14 |

---

## 6. 风险与依赖

| 风险 / 依赖 | 影响 | 处置 |
|---|---|---|
| 从节点硬件维修工期不确定 | P0-2 及其下游 P1-1、P1-2 全部阻塞 | 先做无硬件依赖项（P0-1、P0-4、P1-6）；硬件到位后按 P0-2 → P1-1 / P1-2 串行占用设备窗口 |
| 外部 OpenAI 兼容端点的可得性与成本 | 路线 B/C 的 E2、E4 无法实测 | vLLM 最小配置要求：`--tensor-parallel-size N`（A/B）、**`--return-tokens-as-token-ids`（C 必需**，否则 `top_logprobs` 的 key 是 token 文本，无法按 id 构造残差分布）、`--enable-prefix-caching`（C 建议，省算力不省带宽）。按小时租用只跑关键点（《调研方案》§4.5） |
| 拿不到同构多卡 / 快速 LAN 环境 | 路线 A 的多机孤岛无法实测 | 只做单机多卡形态，多机岛结论标"待硬件"（《调研方案》§4.5） |
| **数据合规（路线 B/C 出网）** | 用户对话正文离开集群 | 作用域档位**必须由人显式决定**，不得为跑通实验把默认改成 `allow_all`；默认 `opt_in` 且请求需 `allow_external`，取值写错 fail-closed 回落 `deny`。结题演示若涉及外部端点，须在材料中标注哪些数据出网 |
| llama.cpp RPC 上游破坏性变更 | E1b 返工 | 锁定 commit（沿《混合规划》§14 惯例）；失败仅影响 GGUF 变体，不影响 A 主形态 |
| `truncate(n)` 回滚实现出错 | KV 被静默污染，表现为输出重复或位置错位，且难以归因 | 必须有 `append_kv` → `truncate` → `get_all_kv` 往返一致性单测与页计数不泄漏断言，才允许接 C-1.5 |
| 时间与人力（三小组并行） | 真机窗口与外部算力窗口互相挤占 | 按《调研方案》§4.4 与 README 团队分工：分布式架构组承担 P0-2 / P0-3 / P1-1 / P1-2 / P1-3；模型优化组承担 P0-4 / P1-5 / P2-5；前端与文档组承担 P0-1 / P1-4 的前端开关 / P1-6 |

---

## 7. 变更记录

- 2026-07-26｜分布式控制面：修复 6 项已验证缺陷 + 3 项附带问题（僵尸连接、帧中途超时失步、重连线程孤儿化、5s 收包超时架空 12s 心跳容忍、三处 ACK 绕过发送锁、9 个 async 端点阻塞事件循环；附带 `Scheduler._tcp_client` 未初始化、健康监控首轮假"恢复"、测试夹具缺陷）。
- 2026-07-26｜新增 `src/tui_admin.py`（1749 行，7 个管理屏幕，纯标准库），配套 `start_tui.sh` / `start_tui.bat`。
- 2026-07-26｜Android UI 层重构：新增 `android/app/src/main/java/com/qlh/inference/ui/components/Common.kt` 与 `ui/theme/` Material 3 主题体系，重排三个屏幕；当日未编译，后于 2026-07-28 通过 Full Debug Kotlin 编译。
- 2026-07-26｜新增 `src/island_engine.py`（路线 A，`engine="island"`）与 `tests/test_island_engine.py`。
- 2026-07-26｜新增 `src/external_provider.py`（路线 B，`external_api`，数据作用域硬门控）与 `tests/test_external_provider.py`。
- 2026-07-26｜新增 `src/speculative.py`（路线 C，实验端点 `POST /api/experimental/speculative`，默认关闭）与 `tests/test_speculative.py`。
- 2026-07-26｜`src/api_server.py` 两处 PEP-701 f-string 改写，全项目现可在 Python 3.10 / 3.11 / 3.12 解析。
- 2026-07-26｜新增文档：《张量并行外部辅助与混合拆分调研方案》《TP孤岛接入指南》《外部推理服务Provider接入指南》《投机解码外部辅助实施说明》。
- 2026-07-26｜新增本文《项目进展与下一步计划》。
- 2026-07-28｜完成文档复核：Python 全量回归 1030 passed / 23 skipped / 3 failed，前端 9/9 且构建成功，Android `:app:compileFullDebugKotlin` 成功；据此修正测试基数与 Android UI 验证状态。
- 2026-07-30｜完成 L0-1：关闭端口的 `ConnectError`/`ConnectTimeout` 跨平台契约收口，新增确定性分类测试；2026-07-31 的后续全量回归基线为 `1066 passed / 23 skipped`。
- 2026-07-30｜完成 L0-4：实现 `PagedKVCache.truncate(n)`、页面回收复用与容量原子拒绝；CUDA 快速冒烟 `62/62`，2026-07-31 的后续 Python 全量回归为 `1066 passed / 23 skipped`。同轮修复孤岛短请求吞吐用墙钟计算导致的偶发 `0 tok/s`。
- 2026-08-02｜TUI 网关适配完成（T1-T8）：网关契约测试 44/44（`gateway/test/tui-contract.e2e-spec.ts`）、`src/legacy_control.py` 承载 `/logs/*`、`scripts/tui_walkthrough.py` 7 屏 × 2 角色自动化走查、`tui_admin.py` 零改动；Python 全量回归 1086 passed / 3 skipped、前端 9/9。
- 2026-08-03｜TUI 专项复核：契约测试 44/44、master/client 走查双 PASS；`start_tui.bat` / `start_tui.sh` 升级一键启动（自动启动后端 + 等待就绪 + 进入 TUI，退出后后端常驻）；新增 [TUI 使用指南](TUI使用指南.md)，登记 [TUI 适配实施计划](TUI适配实施计划.md) 为 Active（在用）。
- 2026-08-03｜Android UI 编译验收推进：`assembleFullDebug` + `assembleLiteDebug` 双变体 BUILD SUCCESSFUL（Full ~29.9 MB / Lite ~18.4 MB）；剩安装与真机/模拟器目视核对（L0-2）。同日 Python 全量回归 `1112 passed / 3 skipped`，交付全局 `bjtu` 命令（自动启动后端+TUI）。
- 2026-08-08｜Launcher/Updater 发布纵切：UP-N2 验签、UP-N3 版本目录与 UP-N4 A/B 自更新基础均已实现；Launcher ZIP（`packaging/dist/QLH-Launcher-v0.1.8.1.zip`）完成构建、签名源站发布、真实 HTTP 验签和双槽回滚闭环；Windows 主应用切换、Linux `.deb` 干净机、断网/权限场景仍待验收。
- 2026-08-08｜Android 原生依赖迁移：llama.cpp 从 vendored 源码迁移为 submodule（gitlink `47e1de77`），Full Release 构建通过；迁移方案已标记废弃，版本事实源为 `android/app/src/main/cpp/LLAMA_CPP_VERSION.md`，真机回归待做。
- 2026-08-08｜弱网基线与质量门复核：确认校园网 `UDP=false`、自建 DERP 可达，建立 NW1-NW5 专项路线；全量 Python 回归收集 1650 项，`1641 passed / 7 skipped / 2 failed`，两项失败均为 `_validate_capabilities` 测试调用缺少 `version` 参数，新增 P0-5 跟踪。
- 2026-08-08｜L0-5 临时验证：在未保留代码改动的临时工作区为两个 v2 直接调用补齐 `_validate_capabilities(..., version=2)`，全量回归达到 `1643 passed / 7 skipped / 0 failed`；因本轮仅做文档工作，修复尚未落地，L0-5 仍为 Validating。
- 2026-08-08｜P0-1 环境核对：仓库已有 Full/Lite Debug、Full/Lite Release APK，SDK 路径可见，但 `adb devices -l` 无设备；Android 安装与三屏目视验收保持未完成，下一动作是连接真机或启动兼容模拟器后留存安装日志与截图。
- 2026-08-08｜L4-MODEL-FLEET 启动无硬件并行批次：专项由 P2 Candidate 提升为 P1 Active/Candidate，按 M0.1→M1.1/M2.1→M3.1/M4.1→M5.0 推进；启动验证为 Python schema `17/17`、control-svc 七套专项 `64/64`，首个执行票为 MF-N1 契约收口。真实 Tailscale 与跨 PC 分发仍保留硬件门。
- 2026-08-08｜L4-MODEL-FLEET MF-N1-M5.0 本地门完成：兼容矩阵与 fetcher 时序 fixture、SQLite 事务/重启/PG 离线补投、批量模型导入 CLI/报告、pull dry-run/磁盘拒绝/来源切换/恢复重 resolve、cluster profile activate/current/发现优先级、三节点部署模拟器全部落地；Python schema `25/25`、control-svc 全量 `225/225`。
- 2026-08-08｜L4-MODEL-FLEET M2 实际迁移完成：8 个内置条目/10 个预期来源实跑，2 个模型 ID 的 Safetensors+GGUF 共 4 个工件成功，6 个条目按本地源缺失登记；流式大文件复制/摘要与 64 MiB 有界 GGUF 元数据解析落地，control-svc 全量 `226/226`。下一票为 M3 OS credential store/显式代理/gated 许可门，并准备 DeepSeek 7B runtime sidecar。
- 2026-08-08｜L4-MODEL-FLEET M3 Windows 安全门与小工件真实网络门完成：DPAPI、Bearer、gated 双重检查、SSE 拒绝终态、零明文和 `QLH_HTTP_PROXY > user > direct` 落地；resolver 改为 blob size/LFS SHA fail-closed，downloader 严格验证 206/Content-Range。`127.0.0.1:7897` 的环境/SQLite 用户代理均完成 456318-byte 完整下载、16384-byte 中断、Range 恢复和 SHA 一致；control-svc `250/250`，生产依赖审计 0 vulnerability。下一票为 DeepSeek 7B runtime sidecar。
- 2026-08-08｜L4-MODEL-FLEET M2 runtime sidecar 完成：独立 Python loader、Node 超时/崩溃回收、硬链接运行时视图、SQLite v2 状态登记和 CLI 落地；DeepSeek 7B GGUF 逐文件 SHA 后由 `llama-cpp-python/0.3.28` 实际加载为 `ready`，Safetensors 按资源余量为 `resource_rejected`；control-svc `257/257`、schema `25/25`、生产依赖 0 vulnerability，全程未使用代理。下一票为准入链路/API/部署过滤。
- 2026-08-09｜L4-MODEL-FLEET M2.4 UI 完成：在既有 runtime admission 基础上完成 Web 工件控制面，新增 alias/存储/runtime 汇总、pull 列表、网关精确透传，以及导入/复检/失效/取消/用户代理交互；control-svc `265/265`、gateway `103/103`、前端单元 `27/27`、浏览器 mock `1/1`、schema `25/25`，本阶段未使用代理。下一票为真实模型权重完整 pull、Safetensors 适配主机和来源/凭据 UI。
- 2026-08-09｜L4-MODEL-FLEET M2.5 来源/凭据与预检 UI 完成：新增无密文凭据状态列表、gateway `resolve/credentials/licenses` 精确透传，以及来源编辑/禁用/恢复、DPAPI 凭据只写/删除、许可接受/撤销和基于 `source_id` 的重新 resolve→确认拉取；control-svc `265/265`、gateway `104/104`、前端单元 `27/27`、浏览器 mock `1/1`，本阶段未使用代理。下一票收窄为真实模型权重完整 pull 与 Safetensors 适配主机。
- 2026-08-09｜L4-MODEL-FLEET M3.2 真实权重与 Safetensors 准入完成：固定 `Qwen/Qwen2.5-0.5B-Instruct@7ae557604adf67be50417f59c2c2f167def9a775`，经 `127.0.0.1:7897` 完整拉取 10 文件/999,604,126 bytes 并校验为工件 `sha256:f8dcc2568764cbd9551ae9b6250526170bb20b29ed8c562c9ce02bff143bbe9c`。实跑发现并修复 pull 仅检查 GGUF、Safetensors 能力错误 fail-closed，以及大文件整块哈希和已存在 LFS blob 未复用三处缺口；恢复 job 复用 988,097,824-byte 权重，仅需 11,506,302 bytes。494,032,768 参数由 Transformers 4.47.1/Torch 2.13.0+cu126 在 CUDA BF16 两次离线加载 `ready`（2,534/1,674ms），本地 deployment prepare=`ready`；control-svc `267/267`、schema `25/25`、生产依赖 0 vulnerability。下一票为 M3.3 真实进程重启 partial 恢复与代理掉线门。
- 2026-08-09｜L4-MODEL-FLEET MF-MEM-N0 显存分页调研完成：本机 RTX 4060 Laptop 8 GB/WDDM 为 `managedMemory=1`、`concurrentManagedAccess=0`，GPU 目标 `cudaMemPrefetchAsync`/`cudaMemAdvise` 不满足能力门；256 MiB pinned buffer 临时基准为 H2D 12.142 GB/s、D2H 13.033 GB/s。结论为 Windows/WSL UVM `No-Go`、显式 pinned-memory layer pager `Go`、原生 Linux UVM 主动预取 `Conditional Go`；`MF-MEM-N1` 可与 M3.3 并行，完整阶段和止损门见专项 §7.6。
- 2026-08-09｜L4-MODEL-FLEET M3.3 真实进程重启与代理掉线完成：下载器只对瞬时 fetch/stream/408/425/429/5xx 做最多 4 次有限重试，每次重新读取 staging 长度并严格验证 206/Content-Range；协议、摘要和磁盘错误不重试。连接/响应头超时在收到响应后解除，不会误杀慢速大文件流。真进程 smoke 在 393,216 bytes 强杀首个 control，第二进程从同偏移恢复；代理在 196,608 bytes 断开并短时 503，同一 job 第 3 次尝试恢复，两个 4 MiB 工件 size/SHA 一致。新增持久恢复字段、`transfer_unavailable`、loopback-only HTTP 源门；control-svc `274/274`、生产依赖 0 vulnerability。下一票 `MF-MEM-N1`。
- 2026-08-09｜L4-MODEL-FLEET MF-MEM-N1 固定 CUDA transport 完成：新增独立 PyTorch harness 与 EX-N1 12 单元矩阵，固定 64/256/1024 MiB × pageable、pinned 单槽、双槽、三槽，两轮均 `12/12 passed`，每个工件 checksum 正确。pinned raw H2D 约 `11.8-12.2 GB/s`、D2H 约 `12.6-13.1 GB/s`；双槽 pipeline 增益不足 2% 且 P95 stall 约为单槽 1.85-1.98 倍，三槽 P95 约 2.72-2.97 倍并冻结。下一票 `MF-MEM-N2` 64 MiB 双槽隔离 `explicit_layer_pager`，暂不接生产 runtime。
- 2026-08-09｜L4-MODEL-FLEET MF-MEM-N2 合成 explicit layer pager 完成：新增隔离 `scripts/model_fleet_explicit_layer_pager.py` 与 EX-N2 4 单元计划，固定 64 MiB 双槽有界 host 预取、显式 copy/compute event、CUDA batch=1/4 digest equality、取消第 2 层和关闭后 VRAM allocated/reserved=0、CPU equality control 全部通过，计划 `4/4 passed`、专项测试 `6/6`。WDDM overlap=0%，不宣称吞吐优化；下一票 `MF-MEM-N2R` 真实 INT8/NF4 工件对照，暂不接生产 runtime。
- 2026-08-09｜L4-MODEL-FLEET MF-MEM-N2R 真实 7B NF4 placement 与模块级层迁移：固定 `deepseek-r1-distill-qwen-7b` Safetensors 15,231,233,024 bytes，artifact gate `3/3`、真实对照 `6/6`；resident/auto context=128 batch=1 tok/s `4.62/4.54` 且 digest 一致，context=2048 两个 batch 点通过，28 层 page-in/out 各 28 次、H2D/D2H 各 3.039 GiB；CPU-only 因可用 RAM 约 10.2 GB 小于安全需求约 20.4 GB 明确拒绝。固定 64 MiB tile bridge、context=8192、output 32/128 和取消未完成；下一票 `MF-MEM-N2F`，暂不接生产 runtime。
- 2026-08-09｜L4-MODEL-FLEET MF-MEM-N2F 真实固定 64 MiB bridge/取消/长输出门：新增 `explicit-layer-pager-64` 与固定 7B NF4 实验计划，64 MiB × 2 bridge 计划 `3/3 passed`；ctx128 完整、ctx128/8192 layer-boundary cancellation 通过，补充 output 32/128 完成，专项回归 `9/9`。桥接只限制单次传输块，模块驻留单独记录，VRAM allocated/reserved 约 2.75/5.63 GiB；128 token 约 431.7 s、0.297 tok/s，未接生产 runtime。下一票 `MF-MEM-N2P` 预取/重叠与延迟决策。
- 2026-08-09｜L4-MODEL-FLEET MF-MEM-N2P 真实预取/延迟决策：新增预取距离 0/1、host staging overlap、CUDA stream overlap、TTFT/inter-token 统计和 `plan-model-fleet-n2p-latency-v1.json`；resident/serial pager/distance=1 计划 `3/3 passed`，同 digest `e958d843…`。distance=1 host staging overlap 100% 但 CUDA H2D overlap 0%，E2E 约慢 1.0%、TTFT 约慢 6.7%、inter-token P95 仅改善约 3.6%，取消回收补充门通过；结论 `Freeze` 双槽 host prefetch 默认优化，保留显式 pager 容量 profile。下一票仅在原生 Linux + 32 GB RAM 条件满足时进入 `MF-MEM-N3`。
- 2026-08-09｜L4-MODEL-FLEET `MF-AUTH-N0` 认证与 Tailscale 绑定调研完成：冻结 QLH 本地主节点 Auth App/TOTP 登录、字符串与二维码双下发、恢复码与本地审计；用户/密钥/绑定数据不投影开发组或远端 PostgreSQL。基于 RFC 6238、`otpauth://` Key URI、Aegis GPL-3.0 和 Tailscale 官方身份/OAuth 文档，明确 Tailscale 不是 QLH 通用 IdP，OAuth clients 仅 API client credentials，OAuth Apps 设备授权当前 alpha/单 tailnet；下一票 `MF-AUTH-N1` 本地用户管理，随后 `MF-AUTH-N2` 一一绑定/换 tailnet。
- 2026-08-09｜L4-MODEL-FLEET 存储主权重排：将 M1.1 标记为 SQLite/远端 PG 兼容基线，新增 M1.2 主节点 SQLite 资产自持与加密备份/恢复、M1.3 远端 PostgreSQL 退场准备；默认 `local_only`，远端仅用户主动单向兼容导出，最终可删除且不影响核心运行。认证、模型、工件、集群、任务、会话和备份属于用户主节点资产，开发组不保留远端副本或重置权。下一票 `M1.2`。
- 2026-08-09｜L4-MODEL-FLEET M1.2 实施首片：control-svc 默认 local-only，`/user/settings` 始终写入主节点 SQLite，旧 PostgreSQL projector 在未显式启用时不启动；`SqliteStore` 新增加密 backup/verify/restore（scrypt + AES-256-GCM、quick-check、失败回滚）与 `storage:backup` CLI；旧 v2 SQLite endpoint 无唯一索引时改为兼容 upsert。控制面全量 `277/277`，剩余核心域统一迁移、artifact manifest 和跨平台干净机验收继续推进。下一票仍为 M1.2。
- 2026-08-09｜L4-MODEL-FLEET M1.2 核心域第二片：SQLite v3 新增 `sessions`、`session_messages`、`workflow_journal`、`review_tickets`；control-svc 默认写入主节点 SQLite，旧 JSON 首次启动只读导入且原文件保留；新增跨实例持久化/导入测试。control-svc 当前全量 `280/280`，剩余认证域、Python 主运行路径、artifact manifest 和跨平台故障门继续推进。
- 2026-08-09｜L4-MODEL-FLEET M1.2 artifact/引用第三片：SQLite v4 新增 `artifact_manifests`；manifest 写入 fail-closed 校验 alias/path、逐文件摘要、聚合 artifact digest 与 blob 存在性，启动时执行 SQLite↔文件系统双向 reconcile。加密 backup/verify/restore dry-run 新增 manifest payload、deployment/runtime 引用及 `--model-store` 外部 blob 校验；SQLite 备份不包含 blob 字节。control-svc `283/283`、gateway `104/104`；下一票仍为认证域、Python 主运行路径、blob 迁移包和跨平台故障门。
- 2026-08-09｜L4-MODEL-FLEET M1.2 本地资产迁移第四片：新增 `.qlhmigrate` 流式 scrypt + AES-256-GCM 格式和 `package/verify-package/restore-package`；从 SQLite 快照枚举实际引用 blob，包头 AAD、逐条大小/SHA、写入期二次摘要、manifest 重建、空间预检及 SQLite/模型目录双回滚均落地。自动门覆盖本机全新目录恢复、篡改、缺 blob、空间不足、不可创建目标、SQLite 阶段失败及切换后 reindex 失败；control-svc `290/290`、gateway `104/104`。后续认证存储第五片已完成。
- 2026-08-09｜L4-MODEL-FLEET M1.2 认证资产存储第五片：SQLite v5 新增 `local_users`、TOTP 引用、恢复码 scrypt/Argon2id 哈希、`auth_audit_events` 与 `tailscale_bindings`；本地 owner/用户名、active/pending、外部身份唯一约束，TOTP 轮换和换网事务回滚通过。审计拒绝 secret/token/恢复码 URI，认证操作不写 outbox；重启和 `.qlhmigrate` 新目录恢复通过。迁移包不携带 DPAPI/系统凭据密文，当前不开放登录/TOTP/Tailscale API；control-svc `295/295`。下一票收口 Python 主运行路径。
- 2026-08-09｜L4-MODEL-FLEET M1.2 Python 主运行路径第六片：`src/local_store.py` 改为共享 WAL SQLite，Node/Python 默认路径统一并兼容 cwd 旧库；Python 不修改 `PRAGMA user_version`，旧 JSON 只读导入一次且原文件保留。单体 API 与 inference-svc 启动执行可写门，会话/设置/健康/推理历史本地优先，旧 PostgreSQL 仅兼容导出；测试隔离不再写用户数据库。Python `1720 passed / 7 skipped`、control-svc `297/297`、gateway `104/104`，control 构建通过。下一票为 Windows/Linux 干净机、活动库损坏和真实只读 ACL 门。
- 2026-08-09｜L4-MODEL-FLEET M1.2 双平台隔离环境第七片：新增 Python/Node 存储探针与 `scripts/model_fleet_storage_environment_gate.py`。Windows 11 通过干净目录、损坏库和 NTFS 写入 deny ACL；WSL2 Ubuntu 22.04 ext4 通过干净目录、损坏库和 `0555/0444` 权限门；两端共库保持 schema v5，损坏/只读均 fail-closed。修正 `SqliteStore.health()` 写探针失败状态为 `unavailable`；临时 ACL、Node 和运行目录全部清理，未使用 7897。control-svc `297/297`、构建通过，Python 定向 `116/116`，全量基线仍为 `1720 passed / 7 skipped`。`M1.2` 存储实现与隔离环境门关闭，下一票 `M1.3`。
- 2026-08-09｜L4-MODEL-FLEET `NET-N1` IPv4/IPv6 本地双栈修复：TCP 数据面、Python API、control-svc、gateway、独立分发服务和 PC/TUI/Android URL authority 已统一；Tailscale ULA、IPv4-mapped IPv6、MagicDNS/广告地址契约已覆盖。Windows 真实 Python/control/gateway 均接受 `127.0.0.1` 与 `::1`，Windows/WSL `socket-helper` 同端口双监听通过；Python `1729 passed / 7 skipped`、control-svc `301/301`、gateway `104/104`、Android Full Debug Kotlin 编译通过。状态为 `Validating`，剩余双机纯 IPv6/混合网、真实 Tailscale IPv6 和安装包门；总体下一票不变，仍为 `M1.3`。
- 2026-08-10｜L4-MODEL-FLEET `MF-AUTH-N1W` Web 首登开发门完成：前端接通 owner bootstrap、Auth App 本地 QR/字符串、首次确认、恢复码、TOTP/恢复码登录、会话恢复与 logout；gateway 提供能力协商并强制 auth/users 走 control-svc，旧单体明确兼容、5xx fail-closed。前端 `29/29`、浏览器 `4/4`、gateway `107/107`，构建通过。下一票 `MF-AUTH-N1A`；Linux/安装包/双栈双机/Tailscale 证据集中到最终跨平台联合验收。
- 2026-08-10｜L4-MODEL-FLEET `MF-AUTH-N1A` 业务授权与账户管理开发门完成：gateway 全局公开/机器/已登录/manager 授权矩阵与 control Bearer 会话校验完成；control 会话列表/撤销、用户元数据和 admin-owner TOTP 边界完成；Web 账户工作区覆盖用户管理、Auth App 二维码/字符串配置、恢复码与会话操作。control `313/313`、gateway `113/113`、前端 `29/29`、浏览器 `5/5`，构建通过。下一票转 `MF-AUTH-N2` 客户端绑定/换网；Linux/安装包/双栈双机/Tailscale 证据集中到最终跨平台联合验收。
- 2026-08-10｜L4-MODEL-FLEET `MF-AUTH-N2` 客户端开发门完成：账户工作区接入绑定状态、CLI/本机状态方式选择、pending 确认、稳定 ID 换网历史和撤销；前端 `30/30`、浏览器 `5/5`，构建通过。N2 整体仍待真实 Tailscale CLI/浏览器、旧节点网络撤销和双机实网联合验收。
- 2026-08-10｜L4-MODEL-FLEET `MF-AUTH-N2L` 本机状态适配开发门完成：control 以无 shell 子进程只读解析 `tailscale status --json`，只返回安全候选字段；Web 自动填充但不绕过 pending/confirm。N2 定向契约 `7/7`、control `317/317`、gateway `113/113`、前端 `30/30`、浏览器 `5/5`；Windows 本机只读探测识别 ID 与 IPv4/IPv6 且无禁返字段，未执行绑定或换网。
- 2026-08-10｜L4-MODEL-TOOLS P1 磁盘治理开发门完成：统一 CLI 新增 junction 感知的 `model_disk_usage` / `models_clean`，清理默认 dry-run，真实删除须 `--apply --confirm CLEAN` 且拒绝越界/reparse point；cache/旧备份额外 opt-in、重复内容只 review。定向 `11 passed / 1 skipped`；真实模型库 204 文件、41,238,213,220 bytes，8 个 cache 默认安全删除数 0，未删除资产。下一票 SD prompt/sampler 批测。
- 2026-08-10｜L4-MODEL-TOOLS P1 SD 批测开发门完成：新增 `sd15_prompt_batch` / `sd15_sampler_matrix`，统一写出图像指标、contact sheet 和 JSON；真实 CUDA 侧车完成 4-step Euler+DDIM 与 prompt×seed smoke，均自动门通过，定向回归 `91 passed / 6 skipped`、全量 `1723 passed / 30 skipped`。下一票转 P2 工具评估，完整矩阵和人工目视留外部验证。
- 2026-08-10｜L4-MODEL-TOOLS P2 `models_sync_status` 开发门完成：schema v1 相对路径清单、结构/内容摘要、缺/多/不一致分类、外来清单 fail-closed 和 CLI `0/1/2` 退出码落地；真实本机 junction/目标根 10/10 资产结构一致，输出无绝对路径。定向 `24 passed / 2 skipped`、全量 `1728 passed / 31 skipped`。下一票 `llm_smoke_matrix`，真实双机/跨平台留联合验收。
- 2026-08-10｜L4-MODEL-TOOLS P2 `llm_smoke_matrix` 开发门完成：注册表双格式发现、固定四项提示词、隔离 worker、临时离线缓存、资源预检和执行/覆盖双门落地；Qwen 1.8B 两格式 `2/2 units、8/8 jobs` 通过，注册表 10 单元为 2 通过/8 跳过/0 失败，覆盖不完整。定向 `29 passed / 2 skipped`、全量 `1738 passed / 31 skipped`。下一票 `gguf_convert` 第一阶段。
- 2026-08-10｜L4-MODEL-TOOLS P2 `gguf_convert` 第一阶段开发门完成：只读源资产/index、架构/工具链/空间/路径预检与 dry-run 计划落地；F16 单步和 Q4_K_M 两步量化计划分开，目标冲突 fail-closed，未写入模型工件。Qwen 1.8B F16 预检通过，Q4_K_M 因缺 `llama-quantize` 阻塞。定向 `40 passed / 2 skipped`、全量 `1744 passed / 31 skipped`。执行事务已由同日下一条完成。
- 2026-08-10｜L4-MODEL-TOOLS P2 `gguf_convert` 执行事务开发门完成：`--apply --confirm CONVERT`、同卷 staging、离线无 shell 子进程、超时/取消清理、Q4 中间件检查、最终 GGUF+SHA256 校验与旁车先行的无覆盖发布已落地；合成 F16/Q4 成功和故障/竞态门通过。定向 `48 passed / 2 skipped`、全量 `1752 passed / 31 skipped`；真实 Qwen 未转换。量化器固定/打包已由同日下一条完成，真实转换仍需用户批准。
- 2026-08-10｜L4-MODEL-TOOLS P2 `llama-quantize` 固定与分发开发门完成：锁定 llama.cpp revision，Windows CPU-only 静态量化器经 CMake/Ninja 构建，PE 依赖仅系统 DLL；manifest/逐文件 SHA256/确定性 ZIP、受管发现和 PyInstaller/Linux `.deb` 接线完成。EXE SHA256 `d91893de…30794`，ZIP SHA256 `e3361e96…0b32`；Qwen Q4 dry-run `managed_package/verified`，真实 Qwen 未转换。Linux 真机门仍待设备。
- 2026-08-10｜L4-SD1.5 P1 / SD-N3.4 真实 Worker 开发门通过：新增强制离线的真实双进程 CUDA smoke，完成 v3 TCP、认证 HTTP、真实 Diffusers 推理、协调器 CAS 和 TaskGraph commit；4-step PNG SHA256 `86665855…05bfa` 且重复一致，定向 `94/94`。下一票先补迟到结果/journal 恢复，再进入双物理 PC 和 SD-N4。
- 2026-08-11｜L4-SD1.5 P1 / SD-N3.4 恢复 fencing 开发门完成：未提交 Provider 结果自动 disposer、ingest 后取消/lease 清理、精确 owner scope 批量回收和 SQLite journal 重启恢复清理落地；取消/跨 owner/重启竞态用例通过，扩大定向回归 `133/133`，Python 全量 `1770 passed / 31 skipped`。下一票进入两台物理 CUDA PC 的 SD-N4 固定四 seed fan-out/fan-in、网格聚合与真实节点统计。
- 2026-08-11｜L4-SD1.5 P1 / SD-N4 非硬件开发门完成：固定四 seed DAG、精确 manifest Worker 池、单节点串行降级、主节点 2×2 CAS 网格、实际 winner 统计和失败/取消/重启清理落地；扩大相关回归 `220/220`，Python 全量 `1777 passed / 31 skipped`。物理双机吞吐、故障和 UI 发布验收保持 `Blocked`；当前执行入口转 `UP-N6.0`。
- 2026-08-11｜UP-N6.0 安装完整性基线开发门完成：签名 schema v1 安装清单、构建期 fail-closed、Windows Inno/Linux `postinst` 验签、Launcher 自更新身份绑定和用户数据根双重排除落地；相关回归 `111/111`、Python 全量 `1795 passed / 31 skipped`，冻结验签 EXE 已实测。下一票 `UP-N6.1 verify`；Windows/Linux 干净机安装留最终跨平台验收。
- 2026-08-11｜UP-N6.1 runtime verify 开发门完成：签名先行 quick/full/deep、JSON/退出码、`qlh-launcher verify`/`bjtu verify` 和启动调用异常后的 quick 落地；只枚举签名程序路径，用户数据零遍历。full 大文件采样只证明可读性，deep 完整 SHA-256 才给出防篡改结论。打包链路 `119/119`、Python 全量 `1803 passed / 31 skipped`；下一票 `UP-N6.2 diagnose`，Windows/Linux 干净机验收仍待执行。
- 2026-08-11｜UP-N6.2 diagnose 开发门完成：内置 10 类本地症状知识库、quick 证据、CUDA 无 shell 驱动探测、磁盘/权限只读检查、CLI/JSON/GUI/TUI 和诊断 ZIP 脱敏无路径摘要落地；原始错误文本不持久化，模型/用户数据零扫描。打包链路 `126/126`、Python 全量 `1810 passed / 31 skipped`；下一票 `UP-N6.3 repair`，Windows/Linux 干净机验收仍待执行。
