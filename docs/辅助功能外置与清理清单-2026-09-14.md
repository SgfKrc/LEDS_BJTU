# 辅助功能外置与清理清单（2026-09-14）

> 状态：**清单**（分类处置建议；逐项落地时转开发票）；处置分四类——**外置（先包化）/ 待确认 / 清理 / 保留（防误判）**。
>
> 创建日期：2026-09-14
> 来源：2026-09-14 主仓盘点（顶层目录 + `scripts/` 67 个 py + `tools/` 非子模块件 + `src/tui_*` + `packaging/`）与《通用工具与子项目候选计划-2026-09-12》对照。
> 关联：[通用工具与子项目候选计划](通用工具与子项目候选计划-2026-09-12.md)（A/B/C 档四条件判定与抽离形态）、[Koakumix 计划](Koakumix子项目定位与增强路线计划-2026-09-14.md) §7、[总体下一步计划](总体下一步计划.md)。

## 1. 甲类：A 档外置（候选计划已评定，5 项）——只差执行

| 编号 | 对象 | 位置 | 规模（实测） | 状态 / 下一步 |
| --- | --- | --- | --- | --- |
| `UNI-A1` | 进程归属票据 + fail-closed 重置 | `scripts/demo/{demo_ownership,defense_reset}.py` | 601 行 + 9 测试 | ✅ **内部独立包已完成**（`packages/spawnledger/`，2026-09-14）；下一步 = 独立远端 + submodule 化（按候选计划 §7） |
| `UNI-A2` | 可复现演示证据链工具 | `scripts/demo/*` + `scripts/perf_report.py` + 三份机器清单 | ~2700 行 / 53 测试 | 待排期（建议第 2 顺位） |
| `UNI-A3` | Ed25519 签名更新链路 | `packaging/`（signing/update/updater/install_manifest/repair/version_store/launcher_slots） | **4035 行 / 13 个测试文件** | 待排期（第 4 顺位；建议毕设主线）；抽离前 `packaging/` 不动 |
| `UNI-A4` | 零依赖 TUI 管理面板 | `src/tui_admin.py` + `tui_chat/tui_sse/tui_shared` | 3971 行 / 5 测试文件 | 待排期（第 3 顺位）；HTTP 客户端集中（`tui_admin.py:253-296`），契约层成本低 |
| `UNI-A5` | 多机同步 + 签名补丁分发 | `tools/ssh_sync_{contract,helper,deployment,client}.py` + `tools/patch_{listener,dispatch}.py` | ~2200 行 / 6 测试文件 | 待排期（第 5 顺位，收益最低） |

> 统一抽离方式（候选计划已定）：内部整理（配置数据化 + README/验收门）→ 独立仓库（可先 private）→ 主仓换 submodule，参照 `docagent`/`reasonix-codex-bridge` 先例。

## 2. 乙类：本轮新发现（09-12 盘查后的增量）

| 对象 | 位置 | 判定 | 处置建议 |
| --- | --- | --- | --- |
| ✅ **旧前端已删除** | `frontend/` | 用户决策（2026-09-14）：**废弃删除**（非归档）——`frontend/` 54 tracked 文件随 `148a0a6` 移除；打包链路本就只用 `frontend_cybergothic/dist`（spec 已核） | **已完成**（2026-09-14） |
| ✅ **微服务改造叫停** | `gateway/`（qlh-api-gateway）+ `control/`（qlh-control-svc） | 用户决策（2026-09-14）：**阶段 2/3 叫停删除**——160 tracked 文件随 `148a0a6` 移除；`api_server`/`inference_service` 的探测降级逻辑天然兼容；cybergothic 代理 8000 直指 api_server | **已完成**（2026-09-14）；DB 实验模型注册功能随叫停**放弃**；`legacy_control.py` 桩与 TUI 走查引用留后续跟进 |
| 单文件下载器 | `tools/modelscope_download.py` | 模型资产工具散件 | 并入 `UNI-B5`（模型资产工具组）一并处理 |
| 实验执行器簇 | `scripts/run_experiments.py`、`experiment_*`（8 个）、`benchmark_*`、`quality_gate_*` | 与 EX-N3 证据链/生产门**强绑定** | **保留**；其中通用化部分在 `UNI-A2` 抽离时顺带参数化 |
| 环境治理 | `scripts/setup_envs.py`（663 行）+ `setup_*_env.py` ×4 | `UNI-C1`（已评定） | 待排期；抽离时环境定义数据化为 `envs.yaml` + 快照漂移检测 |
| 缓存/夹具工具 | `scripts/check_prompt_cache.py`、`harness_workbench/tools/fixture_manifest.py` | CACHE-01 / Koakumix 夹具契约的一部分 | **保留**（契约内工具，不外置） |

## 3. 丙类：本地残留清理（gitignored——不占 git，占磁盘与视线）

| 目录 | 内容 | 建议 |
| --- | --- | --- |
| `_to_delete/` | 空目录 | 删除 |
| `docs_to_delete/` | 空目录 | 删除 |
| `runtime/` | `api_server-restart.{err,out}.log` | 清理（可再生成） |
| `dist/` | 构建产物 | 清理（可再生成） |
| `chat_history/` | 会话 JSON（7 项） | 保留（运行数据） |
| `local_docs/` | 个人笔记 + **`Koakumix.png`（图标已在！）** | 保留个人区；**图标转正式资产位**（登记进 Koakumix 文档） |
| `node_modules/`（顶层） | JS 依赖 | 保留（构建需要） |

> 删除类操作按纪律：先 dry-run 列清单 → 确认 → 执行 → 后核验。

## 4. 丁类：明确保留（防误判——本轮逐一核对）

- **`gateway/`（qlh-api-gateway）与 `control/`（qlh-control-svc）不是辅助功能**——是"QLH 微服务改造"**阶段 2/3 主线**（对外 REST/SSE 代理 + 控制面服务，TypeScript/NestJS），近期仍有提交（如 `891d551` auth-policy）；连同 `frontend_cybergothic/`（唯一产品前端）、`android/`（端侧）一并保留。
- `src/`（含推理服务/调度核心）、`schemas/`、`fixtures/`（版本化契约）、`harness_workbench/`（Koakumix，B 档条件外置——过渡三件已完成）。
- `scripts/` 文档审计族（`doc_maintenance_audit.py`、`check_doc_links.py`、`docagent_*`）——与 `tools/docagent` 子项目配套，保留。
- `packaging/`——UNI-A3 抽离前留在主仓。

## 5. 建议执行序

1. **丙类清理**（零风险，先做）+ 图标转正；
2. **`frontend/` 定位确认**（消除总计划漂移）；
3. **`UNI-A1` 收尾**（独立远端 + submodule 化，建立"抽包 + 搬门"模板）；
4. 按候选计划：`UNI-A2` → `UNI-A4` → `UNI-A3`（毕设主线）→ `UNI-A5`；
5. `UNI-C1`（venv 治理）与 B 档 harness 模块按 Koakumix 触发条件评估。

## 6. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-14 | 首版：甲（A 档 5 项状态）/乙（frontend 定位存疑、modelscope_download、实验簇、UNI-C1、缓存夹具工具）/丙（本地残留清单）/丁（gateway+control 等防误判保留）四类清单与执行序 |
