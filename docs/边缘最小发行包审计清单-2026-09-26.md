# 边缘最小发行包审计清单（2026-09-26）

本清单针对“无 CUDA/边缘设备默认去 torch 化，Windows/Linux/Android 可分别交付最小发行包”的发布门。它区分了包体不携带 torch、运行时不安装 torch、以及真机功能验收三个不同结论；任何一个未完成都不能宣称去 torch 化发行完成。

## 当前基线

| 平台 | 现状 | 结论 |
| --- | --- | --- |
| Windows | `packaging/packaging/qlh-slim.spec` 排除 torch 系并把 `src` 放入 `_internal`；`qlh_launcher` 识别该形态。`runtime_guard` 已增加 `llama_cpp_only` / `torch_cpu` / `torch_cuda` profile，旧 CPU/CUDA 清单名保持兼容。 | **profile 代码门已通过**；Windows 真构建、干净机首启与默认 profile 选择仍待验收，不能把包体门当完整发行门。 |
| Linux | `packaging/packaging/linux/build-deb.sh:174-215` 在包内创建 venv，并无条件安装 CPU/CUDA torch；`requirements-cpu.txt` 也面向 torch + llama.cpp 双引擎。 | 不是去 torch 化最小包；需新增 llama.cpp-only 发行变体或明确将 torch 变体排除在 Edge 发行之外。 |
| Android | `android/app/build.gradle.kts:43-108` 仅构建 `arm64-v8a` Full/Lite APK；`scripts/android_validation.py:146-242` 已分离 JVM、APK、设备证据。 | JNI/APK 开发门存在；真机 worker、模型分片、断线/热/电量、安全及签名发布门仍未闭合。 |

## 当前已落地

- `packaging/packaging/runtime_guard.py`：`llama_cpp_only` 的 required-module probe 只包含 FastAPI/Uvicorn/httpx/psutil/dotenv/llama_cpp；缺依赖时 fallback/pip 命令不包含 torch/Transformers。`torch_cpu`/`torch_cuda` 保留既有行为。
- `packaging/packaging/qlh_launcher.py`：支持 `QLH_RUNTIME_PROFILE` 与 `--runtime-profile`，`--runtime-check` 报告实际 profile；旧 `requirements-runtime-cpu.txt` / `requirements-runtime-cuda.txt` 路径不变。
- `packaging/packaging/requirements-runtime-llama_cpp_only.txt`：新增 Edge profile 清单。
- 验证：`packaging/tests/test_runtime_guard.py` + `packaging/tests/test_qlh_launcher_slim.py` **23 passed**；Edge/manifest 回归 **47 passed**。

## P0 阻断项

- **运行时去 torch 化的剩余门**：profile contract 已拆开，但默认 CPU SLIM 仍保持 `torch_cpu` 兼容行为；发行构建必须显式选择 `llama_cpp_only`，并让启动器/安装 manifest/能力广告使用同一 profile。Edge 入口的动态 import 与干净 venv 门仍未完成。
- **Linux 最小变体**：`packaging/packaging/linux/build-deb.sh:206-215` 当前无条件装 torch，且 `control-cpu` 宣传 PyTorch 备选引擎（`packaging/packaging/linux/control-cpu:14-16`）。应补 `llama-cpp-only`/edge 变体：只带受管 `llama-quantize`、`llama-cpp-python`（或纯 native RPC worker）、FastAPI/TUI 所需轻依赖；安装后执行 `python -c 'import importlib.util; assert ... torch is None'` 作为硬门。
- **动态导入审计**：对 Edge 入口及 `src/` 做静态扫描和干净 venv 运行扫描，禁止直接/间接 import `torch`, `transformers`, `accelerate`, `bitsandbytes`。不能只依赖 PyInstaller `excludes`，因为 SLIM 运行的是源码 `uvicorn src.api_server:app`（`packaging/packaging/qlh_launcher.py:199-218`）。
- **包体边界复验**：Windows/Linux 构建后逐文件检查不得出现 torch、CUDA DLL、开发脚本、测试、agent 配置；现有 `install_manifest.py` 签名清单只证明文件完整性，不证明依赖闭包，需增加依赖/ELF/PE 扫描报告并纳入发布附件。

## P1 平台审计与修复

### Windows

- 为 SLIM 增加 CPU-GGUF 首启路径：无网络或无 Python 时应能给出可操作失败，不得偷偷创建 torch runtime；外部 runtime 目录需记录 engine/profile 与版本锁。
- `qlh-slim.spec:59-64` 的排除列表应配套静态 import 检查；对 `_internal/src` 做 `compileall`、只读 CLI、API health 和 GGUF 单轮生成 smoke。
- 真实干净 Windows 机仍需完成安装、升级、断网、权限受限、repair/回滚和卸载后用户数据保留；当前文档仅记录 Launcher 基础验收，不能替代主应用验收。

### Linux

- `build-deb.sh:25` 版本号与 `packaging/packaging/version.txt`、Launcher 版本存在漂移风险；构建前应由单一版本源生成 `.deb`/manifest/服务文件。
- `build-deb.sh:190-202` 在 ensurepip 不可用时在线下载 `get-pip.py`；最小离线发行必须提供受管 bootstrap 方案或明确把联网安装列为安装前置，并将 SHA-256/代理/证书失败写入验收门。
- `.deb` 需在 Ubuntu 22.04、Debian 12 原生主机分别做 `dpkg-deb --contents`、`ldd`、安装/升级/卸载、systemd 无头启动、桌面/TUI/API health 和 GGUF 生成；WSL 只能作为构建辅助，不能替代运行时证据。

### Android

- `android/app/build.gradle.kts:43-108` 当前只有 arm64 ABI；需明确设备矩阵（16 KB page、Android API、arm64 真机/模拟器）和 Full/Lite 包的模型/worker 能力差异。
- `scripts/android_validation.py:226-242` 的 `arm64_native_worker` 目前只由“安装+启动+ABI”推导，不能证明 JNI RPC worker 实际运行。应增加 logcat/JNI `nativeRpcStart`、模型分片请求、连续生成、取消、重连、热/电量采样证据。
- `android/app/src/main/AndroidManifest.xml` 开启 `usesCleartextTraffic` 和多个前台服务；发布前必须做 TLS/网络安全配置、最小权限审计、签名/anti-debug/备份策略审计，并验证 release APK 的 native 库和 proguard 规则。

## 统一验收门

1. **依赖门**：Edge 三平台干净环境中 `torch`、`transformers`、`accelerate`、`bitsandbytes` 不存在；启动、健康检查、GGUF 单轮生成通过。
2. **功能门**：Windows/Linux 本地 GGUF 生成；Android Full Worker 真实 JNI RPC、模型分片和取消/重连通过；失败时保持 llama.cpp 本地回退。
3. **安全门**：安装清单签名、更新/repair、TLS/证书、文件权限、用户数据保留和卸载回滚通过；包内无测试/agent/密钥/开发工具。
4. **资源门**：记录每个平台安装包大小、峰值 RSS、模型文件大小、首 token/生成 tok/s、热/电量和网络流量；不得用探针或模拟进程替代真机证据。
5. **发布证据门**：每个产物附构建环境锁定、依赖清单、SBOM/许可证、SHA-256、签名 manifest、测试命令和原始 JSON/日志；三平台均有可复现命令。

## 建议下一票

`EDGE-MIN-RELEASE-01`：**profile 化 runtime contract 已完成第一切片**（`llama_cpp_only` / `torch_cpu` / `torch_cuda`，定向 23 passed）；下一步补 Linux llama.cpp-only `.deb` 和 Windows SLIM CPU-GGUF 真包路径，再以 Android 真机 JNI/分片证据作为独立设备门。未完成前，发行说明只能写“Windows 包体/profile 开发门通过，跨平台去 torch 化与真机发布门待验收”。
