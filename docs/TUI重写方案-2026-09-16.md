# Koakuma TUI 重写方案（2026-09-16）

> 状态：**方案待评审**
> 关联：[TUI 使用指南](TUI使用指南.md) · [TUI 指令集](TUI指令集.md) · [TUI 适配实施计划](TUI适配实施计划.md) · [整体架构](整体架构.md)

## 0. 一句话

把"**两个窗口 + 两个 TUI**（聊天用 Textual / 管理用标准库）"收敛为 **单进程、单窗口、单 TUI**：
`qlh` 一条命令 → **进程内**起后端 → 播**启动动画** → 进入**统一 TUI**（聊天与全部管理页同在一个界面）。

## 1. 现状与痛点（均有实证）

| 事实 | 证据 |
| --- | --- |
| 一键启动要**两个窗口** | `start_tui.bat`：`start "QLH 后端 API" cmd /k "... python src\api_server.py"`（新窗口）+ 当前窗口跑 TUI |
| 聊天与**管理是两个 TUI** | `src/tui_chat.py`（715 行，**Textual**）vs `src/tui_admin.py`（2889 行，**零依赖标准库**） |
| 入口语义不统一 | `bjtu` = 一键；`qlh`（无参）**只打印 usage**；`qlh admin` 进 TUI 但**不拉后端**；`qlh chat` 走 Textual 聊天页 |
| 依赖分裂 | 管理 TUI 零依赖；聊天页缺 Textual 时**静默退出**（`tui_chat.py:15`） |
| 动画只覆盖一条路径 | 本轮新增 `src/tui_splash.py`（灰蓝 alt screen），仅接在 `tui_admin.py` |

**用户痛点原文**：开两个窗口、聊天和其他页面分开，"太蛋疼"。

## 2. 目标（验收口径）

1. **单命令**：`qlh`（无参数）= 一键：起后端 → 等 `/api/health` → 播动画 → 进 TUI；
2. **单进程单窗口**：后端在 **TUI 进程内的线程**里跑（`uvicorn.Server`），**不再 `start` 新窗口**；
3. **单 TUI**：聊天页成为统一 TUI 的**一个页面**，与其余管理屏平级、可互相跳转；
4. **零第三方依赖**（保持主仓/L 档边界，**不引入 Textual**）；
5. **保持既有能力**：7 个管理屏、26 条命令、`--plain` 降级、**单命令模式仍不拉后端**（`qlh status` 不误启）；
6. **动画只在进入交互 TUI 时播一次**（`tui_splash.py` 已就位；alt screen 无残影）。

## 3. 架构

```text
qlh（qlh.py，唯一入口）
  └─ KoakumaApp（单一 TUI 进程）
       ├─ BackendSupervisor   ← 探活 8000；未起则进程内 uvicorn（daemon 线程）
       ├─ TuiSplash           ← 启动动画（与 health 探测并行，不延长启动）
       ├─ AnsiTerm            ← 备用屏 + 原始按键（复用 tui_admin 现有实现）
       └─ PageStack           ← 统一页面栈（复用 tui_admin 的 Screen 基类）
            ├─ ChatPage       ← 由 tui_chat 的 SSE 逻辑重写为零依赖（保留 tui_sse 解析）
            ├─ Status / Model / Device / Cluster / Queue / Logs / Settings   ← 现有 7 屏
            └─ Rag / Pipeline / Runtime / Registry                            ← 本次补齐的域
```

**关键设计点**

1. **后端进程内化**：`api_server.py` 已暴露 `app = FastAPI(...)` 与 `register_uvicorn_server()`
   （`src/api_server.py:327`、`:819`），可在 `daemon` 线程内 `uvicorn.Server(app).run()`；
   后端日志沿用既有约定写入 `logs/`（**不写 TUI 的 tty**），因此不污染界面；
2. **退出语义不变**：仍由 `/api/system/shutdown` 触发优雅退出（`api_server.py:807-811` 已有该机制），
   避免 Windows 上线程内信号处理问题；
3. **页面抽象复用**：`tui_admin.py` 的 `Screen`（`:626`）与 `AnsiTerm`（`:347`，已用备用屏 `\x1b[?1049h/l`）
   即为统一的页面/终端基类；聊天页实现同一接口（`render()` + `on_key()` + actions）；
4. **HTTP 复用**：`ApiClient`（含 `X-QLH-Log-Token`）；聊天流式复用 `tui_sse.py` 的 SSE 解析。

## 4. 页面清单（统一后）

| 页面 | 来源 | 说明 |
| --- | --- | --- |
| **聊天** | `tui_chat.py` 重写为零依赖 | SSE 流式、会话列表（`/api/chat/stream`、`/api/chat/clear`、`/api/sessions*`） |
| 状态 / 模型 / 设备 / 集群 / 队列 / 日志 / 设置 | `tui_admin.py` 现有 7 屏 | 原样保留（不动协议） |
| **RAG** | 新增 | `/api/rag/*`（11 个端点） |
| **Pipeline** | 新增 | `/api/cluster/qwen3/local-chain/*`（7 个） |
| **Runtime** | 新增 | `/api/cluster/model-runtime/*`（6 个） |
| **Registry** | 新增 | `/api/models/registry*`、`/api/models/local-assets*`（5 个） |

> 端点覆盖目标：现状 **41/120** → P3 后约 **70/120**（其余为内部/运维端点，不要求 TUI 入口）。

## 5. 迁移路径（分步、每步独立可跑可回退）

| 阶段 | 动作 | 判据 |
| --- | --- | --- |
| **P0（已完成）** | `src/tui_splash.py`（灰蓝、alt screen）+ `APP_TITLE="Koakuma"` / `APP_SUBTITLE="leds-bjtu"` + `--no-splash` | TUI 测试 85 passed；dump 验证无 `2J`、有 `1049h/l` |
| **P1** | `qlh`（无参）走一键路径；后端改为**进程内线程**（不再新窗口） | 单窗口完成"起→等→动画→TUI"；`/api/system/shutdown` 语义不变 |
| **P2** | 聊天页并入统一 TUI（零依赖重写，复用 `tui_sse` 解析） | 聊天与控制同界面；Textual 变为纯可选 |
| **P3** | 补齐 4 个新域页面（RAG / Pipeline / Runtime / Registry） | 端点覆盖 41 → ~70 |
| **P4** | 退役 `tui_chat.py`（或降级为兼容 shim）；同步 README 与 `TUI使用指南` | `qlh` 单入口；**无 Textual 也能全功能** |

## 6. 风险与处置

| 风险 | 处置 |
| --- | --- |
| 进程内 uvicorn 与 TUI 争用 stdin/stdout | 后端日志写 `logs/` 文件；TUI 独占 tty |
| Windows 线程内 uvicorn 信号处理 | 复用 `register_uvicorn_server()`；退出统一走 `/api/system/shutdown` |
| 聊天页零依赖重写导致体验回退 | 保留 `tui_sse` 流式解析 + 分段刷新；必要时保留 `qlh chat` 作为 Textual 兼容入口 |
| 大改风险 | P0–P4 分阶段，每阶段独立可跑、可回退；不动 `/api/*` 契约 |

## 7. 明确不做

1. 不把 Textual 变成**必需**依赖（保持零依赖发行边界）；
2. 不改 `/api/*` 契约（只增加消费方）；
3. 不在本方案内改 Relay / RPC 语义；
4. 不恢复已裁撤的生图/邮件/旧前端。

## 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-16 | 新建：记录用户裁决（重写 TUI、统一为单进程单窗口单 TUI）与 P0–P4 迁移路径；P0 已完成（灰蓝 Koakuma 动画 + 标题/副标题）。 |
