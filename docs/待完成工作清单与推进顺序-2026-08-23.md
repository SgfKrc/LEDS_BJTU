# QLH 待完成工作清单与推进顺序

> 文档状态：现行（本轮筛查结论）
>
> 更新日期：2026-08-23
>
> 适用范围：对 `docs/` 与 `local_docs/` 的现行计划、完成度、已知问题和验收登记进行筛查后，给出下一步开发、文档治理和环境验收的唯一待办分层。本文件不替代《总体下一步计划》的长期优先级。

## 1. 筛查结论

本轮以《总体下一步计划》《验收清单与资源限制登记》《已知问题记录》为事实优先级；专项计划用于补充验收条件。历史“下一票”、旧阶段表和 `local_docs/` 的接续记录不单独产生新开发票。

结论如下：

1. 前端 `frontend_cybergothic` 的页面/API 接线、Android 原有接线、DOCAGENT、离线整合包、RAG S5D、集群入群、NW3.1/NW4 本机门、QW3 无硬件开发链和 `CY-PKG-01` 均已收口。不得因旧计划仍写“下一票”而重复开发；标准桌面打包入口已统一到新前端，真实首启/升级继续归 E8，Android 又新增分期移动控制面。
2. `NET-DUALSTACK-PREF-01`、`DOC-HYG-02`、Android `AND-CTRL-01/02/03/04/05` 与 `CY-PKG-01` 已完成本机门；Android UI 统一风格 `AND-UI-01..05` 已完成本机实现，后续转真实桌面包/E8 与 Android 管理面真机联调。
3. 其余开放项主要是设备、外部网络/证书、真实模型、真实工件或长时间窗口的验收；它们应排入验收队列，不应以模拟代码替代。
4. 微服务迁移保持冻结；任意 DAG、跨模型 hidden-state 和自建 verify 后缀均是条件性架构工作，须先重新立项或满足触发条件。

## 2. 可立即开始的工作

| 优先级 | 票号 | 工作 | 退出条件 | 限制 |
|---|---|---|---|---|
| 已完成（本机） | `CY-PKG-01` | `frontend_cybergothic/dist` 已成为 `api_server.py`、主 pywebview Launcher 与 CPU/CUDA/Slim/Linux 标准包的默认前端资源；旧 `frontend/dist` 只能显式覆盖。 | 源码/冻结包路径、显式兼容覆盖、标准构建输入和反代契约定向回归通过。 | 本票不改 Android 视觉、不改 API；干净 Windows/WebView2、Linux `.deb`、签名更新和首启仍由 E8 验收。 |
| 已完成（本机） | `AND-CTRL-01` | Android Settings 只读集群概览，修正 `/api/cluster/status` 的节点映射 DTO，显示运行/就绪/当前任务和有界节点列表。 | Full JVM `128 passed`，`compileFullDebugAndroidTestKotlin` 通过。 | 真机/真实主节点后置；不开放任何集群写操作。 |
| 已完成（本机） | `AND-CTRL-02` | Android 模型舰队只读汇总，统一本地已选、主节点当前、注册表、资产状态和已验签 GGUF 清单；既有 SAF 下载进度保持原面板。 | 有界 8 项投影、路径/URL 不入 UI 状态、Full JVM `132 passed`、`compileFullDebugAndroidTestKotlin` 通过。 | 不改注册表、下载源、代理或远程模型；真实主节点/大工件/SAF 后置。 |
| 已完成（本机） | `AND-CTRL-03` | Android 审计与活动只读投影，提供有界工作流阶段/attempt/review 摘要；主节点提供 `summary=1` 安全投影和 `limit`。 | Full JVM `135 passed`、后端任务图定向 `1 passed`、`compileFullDebugAndroidTestKotlin` 通过。 | 真实主节点、真机授权和长期活动列表验收后置；禁止投票、重派、删除与结果提交。 |
| 已完成（本机） | `AND-CTRL-04` | Android 账户/Auth App 会话页，复用既有 Keystore 登录、会话、TOTP/恢复码与登出。 | Full JVM `138 passed`；capability 不可用/401/退出均 fail-closed，秘密不进 UI 持久化。 | 不展示 TOTP seed/恢复码/bearer；真实 Auth App/真机后置。 |
| 已完成（本机） | `AND-CTRL-05` | Owner/admin 主节点管理折叠组：管理摘要、成员/绑定安全投影、管理审计与撤销二次确认。`review_admin` 未迁移时只读提示。 | Full JVM、Full/Lite AndroidTest 编译通过；confirm token 仅在 ViewModel 当前请求内流转，成员/绑定/审计有界。 | 真实 control/gateway、权限失效、审计落盘和真机联调后置；不伪造入群审批。 |
| 已完成（本机） | `AND-UI-01` | Android 主题令牌、顶部栏、底部导航、设置分组和静态角标向 `frontend_cybergothic` 的低成本视觉语言对齐。 | 黑白主题基线不变；低圆角/细边框/表面层级/苍紫强调已接入；Full/Lite 编译通过。 | 暗浅色、字体放大、窄屏和低端设备真机验收后置；不迁移桌面侧栏/Canvas。 |
| 已完成（本机） | `AND-UI-02` | Chat / Session 气泡、会话卡、输入栏接入苍紫、暗金和受控荧光绿语义色。 | Assistant/加载暗金细节、输入焦点/发送荧光绿、当前会话状态明确；不改变业务逻辑和触控目标。 | 真机暗浅色、长文本、键盘和低端滚动验收后置；荧光绿不得扩展为大面积主题色。 |
| 已完成（本机） | `AND-UI-03` | Diffusion / Model 输入、结果、模型行和下载状态接入统一表面与语义色。 | 参考图/结果细边框、生成焦点荧光绿、下载暗金、完成状态荧光绿；不改变 SD/SAF/SHA 逻辑。 | 真机图片预览、浅色模式、下载进度和低端滚动验收后置。 |
| 已完成（本机） | `AND-UI-04` | Settings / Account / Audit / Cluster 状态语义和列表密度统一。 | 可达/可执行荧光绿、等待/未知暗金、失败/撤销错误色；成员与审计默认 8 条并可展开；不改变权限或后端契约。 | 真机 owner/admin、长列表、TalkBack 与浅深色对比验收后置。 |
| 已完成（本机） | `AND-UI-05` | Full/Lite 性能与无障碍收口。 | SettingRow 48dp 触控高度、Button/heading 语义、大字体重排、Chat 无动画滚动、LazyColumn 列表；新增 1.6 倍字体回归。 | 真机 TalkBack、窄屏、截图、低端硬件和完整安装包验收后置。 |
| 已完成（本机） | `NET-DUALSTACK-PREF-01` | 显式 `/api/cluster/connect` 成功后持久化用户选择的 endpoint；启动时该偏好覆盖陈旧 `.env`，自动重连按“首选 → bootstrap 原地址 → Tailnet”回退。 | 偏好/跨 cluster 清理/回退顺序/API 显式持久化断言完成；`test_scheduler.py` `284 passed`，API/bootstrap/专项 `36 passed`。 | 不开放 `task_dispatch_enabled`，不把本地测试当作 IPv6-only 安装包或生产路由验收。 |
| 已完成（文档） | `DOC-HYG-02` | 已同步弱网/集群 RAG 本机门、前端/Android 接口票、Android 旧路线文档的“当前能力”表述和生命周期索引。 | 文档头部、索引和正文不再把已完成的 NW3.1/NW4、RAG S5D、`CY-API-01..07`、`AND-API-01..05` 写成待开发；旧 Android 路线文档已明确为历史架构基线。 | 只修正文档事实，不改变已冻结的微服务或产品范围；删除的历史文件不恢复。 |

### 本轮已完成的文档治理

`DOC-HYG-01` 已在本轮完成：`frontend_cybergothic` 清单与 README 对已迁移的 StudyPact 计划改为指向 `local_docs/`。链接检查在修复后应为零失效链接。

## 3. 条件性工作，不自动开工

| 候选 | 触发条件 | 当前结论 |
|---|---|---|
| `TC-N3.1` 任意异构 DAG | 产品明确要求超出已实现的固定 LLM→SD 模板，且接受新的权限、失败收敛和质量矩阵。 | 任务图已支持按 Stage 使用不同模型；不支持把不同模型的 hidden states 直接拼接。先写范围/契约，不直接改执行器。 |
| 投机解码 `verify` 后缀端点 | 外部辅助路线 L3-3 未止损，且有受信外部 verify Provider。 | 保持条件票；当前实验实现不进入生产解码循环。 |
| 微服务 3.4 Go/No-Go | Python 功能面稳定窗口结束，完成契约重新对齐。 | `Frozen`。不得继续迁移或删除单体路径。 |
| UVM / 显存分页 N3 | 原生 Linux、32 GB RAM 等能力门满足。 | Windows 8 GB WDDM 的 UVM 已是 No-Go；保留显式 pager 的已验证容量 profile。 |

## 4. 环境验收队列

下表按依赖和可操作性排序。每一项已经具有开发门或局部证据，缺的是对应环境的真实证据。

| 队列 | 验收项 | 所需条件 | 完成判据 |
|---|---|---|---|
| E1 | `T-RACE-6-C` 与 L1 联合稳定性 | 主从双机可用、可安排长窗口 | 长时多轮、升级恢复、断电恢复；中文输出质量、重连/重派和 Full Worker 状态一致，无错误生产回退。 |
| E2 | IPv4-only / IPv6-only / 双栈安装包矩阵 | 可切换网络、Windows/Linux 包 | 在 `NET-DUALSTACK-PREF-01` 后验证发现、注册、心跳、重连和升级均遵从新排序。 |
| E3 | NW1.5/NW3 真实 WSS/443 | 域名、生产/测试证书、可达 443 端点与双机 | TLS/WSS 数据面、DERP 对照、Transport v2 流量、故障与回退证据；默认 Legacy 路由不被误切换。 |
| E4 | 认证、入群与补丁分发联合验收 | Auth App、Tailnet、双机；Linux 时增加 Secret Service/DBus | TOTP、入群一次性授权、换网/撤销、SMTP 可选链路、SSH `status/fetch/apply/verify` 和跨进程密钥保护均按 fail-closed 工作。 |
| E5 | Android Full/Lite 真机 | Android 设备、PC 主节点、必要模型/网络 | 主题、触控、IME、系统栏、更新/日志/SAF；Full Worker offer→result、Gemma4 图片、远程 SD、下载续传及热/电量/长时边界。 |
| E6 | Gemma/Qwen/SD 真实模型与 CUDA | 足够 RAM/VRAM，必要时两台 CUDA PC | Gemma 外部干净机、8K/16K/音频；SD img2img/IP-Adapter 和多 seed 双机；Qwen sidecar/CUDA parity、QW3.8 9B→27B 分期与 Qwen3-VL-8B 资源门。 |
| E7 | RAG 规模与 ANN | 真实文档规模、长时 embedding Provider，安装 `sqlite-vec` 时另开基准 | provider soak、模型摘要切换、SQLite lease 恢复、容量曲线；`sqlite-vec` 仅在独立 benchmark 通过后才可从 `NO_GO` 进入候选。 |
| E8 | 发布与安装 | 外部干净 Windows/Linux、签名 Launcher 包、PyInstaller 打包环境 | 赛博哥特桌面包在 Windows/WebView2 与 Linux `.deb` 首启、下一签名包下载进度、TUI 真实后端、瘦身包首次依赖引导、离线整合包真实全量出包及 Android SAF 导入。 |

## 5. 不应误排为待开发的事项

- `NW3.1`、`NW4`、`CLUSTER-JOIN`、`RAG-S5D`、SSH 补丁传输 S0-S2、`BUNDLE-M1`、`EX-N3-PROD` 和 DOCAGENT M1-M3 已完成本机开发/质量门。
- Android 的 `AND-A1`、`AND-A2`、`AND-B`、`AND-C`、`AND-E`、`AND-F`、`AND-API-01..05` 及 `AND-CTRL-01/02/03/04/05` 均已完成客户端/协议开发；剩余的是物理设备、真实主节点和外部更新链路验收。`AND-CTRL-05` 不扩大 review_admin 未迁移的权限边界。
- `frontend/` 已冻结，只作为历史对照；新问题只在 `frontend_cybergothic/` 重新拆票。`CY-PKG-01` 是把该产品前端纳入默认桌面交付的例外迁移票，不恢复旧前端开发。Android UI 以《安卓UI统一风格改进计划-2026-08-23》为准，不把桌面 Canvas 和高密度工作台移植到移动端。
- QW3 的硬件无关 sidecar、传输、ledger 和恢复链已收口；不能用新的模拟票代替真实模型/CUDA/双机验收。

## 6. 文档治理与优先级规则

1. 《总体下一步计划》继续是唯一总排序入口；本文件每次状态变化后只更新分层与链接，不复制阶段日志。
2. 《项目进展与下一步计划》是阶段快照；其中与总计划不一致的旧段落必须加日期或降为历史参考，不得被当成下一票。
3. `local_docs/后续排期-开发票双机验收票与硬件阻塞.md` 仅作接续参考。其已完成的 NW/集群/RAG 条目不得重新排期；下一轮在 `DOC-HYG-02` 中补充“截至日期”和现行入口链接。
4. 《Android版本远期计划》保留为跨运行时边界说明，不能再作为 Android 当前功能状态的唯一来源；实际排期以《安卓与PC功能差距清单》为准。
5. 生产开放仍要求真实质量、稳定性、网络和安装包证据全部达到各自门槛。任何一项本机模拟通过都不能替代这些证据。

## 7. 执行顺序

1. Android UI `AND-UI-01..05` 已完成本机开发门，转真实桌面包/E8、Android 管理面真机联调、TalkBack/字体放大与低端设备验收。
2. `review_admin` 审批后端仍未迁移，保持只读，不因 Android 入口而扩大权限范围。
3. 条件具备时安排 E2 的 IPv4-only/IPv6-only/双栈安装包与升级矩阵。
4. 有双机窗口时优先 E1；有证书/443 条件时并行 E3；其余按设备与模型资源安排 E4-E8。只有在触发条件明确后，才重新评审任意 DAG、verify 后缀或微服务恢复，不把它们插队到验收主线前。

## 8. 筛查依据

- [总体下一步计划](总体下一步计划.md)
- [验收清单与资源限制登记](验收清单与资源限制登记.md)
- [已知问题记录](已知问题记录.md)
- [集群接入稳定性与本地RAG实施计划](集群接入稳定性与本地RAG实施计划.md)
- [桌面赛博哥特与安卓原生界面复核](桌面赛博哥特与安卓原生界面复核-2026-08-23.md)
- [后续排期-开发票双机验收票与硬件阻塞](../local_docs/后续排期-开发票双机验收票与硬件阻塞.md)
