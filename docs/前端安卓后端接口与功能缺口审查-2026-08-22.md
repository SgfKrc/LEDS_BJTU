# 前端、安卓与后端接口及功能缺口审查

> 审查日期：2026-08-22  
> 状态：审查基线完成；`FE-AUD-01/02` 已实现，分期票持续执行
> 适用范围：`frontend_cybergothic`、Android Full/Lite、Python 单体后端、control/gateway 面；旧 `frontend/` 仅作历史对照

## 1. 结论摘要

QLH 当前没有确认的“新前端调用不存在后端接口”缺口。后端接口已经存在但新前端尚未形成完整用户工作流的能力，集中在：Auth App 首次配置与恢复码、集群档案激活/端点/层配置、模型资产导入与部署模拟、RAG embedding 任务、分布式生图高级流程、任务图/Sidecar 诊断。

Android 的 HTTP 控制面已覆盖登录、会话、聊天、远程 SD、主节点 GGUF 下载、bootstrap 和节点注册；本地 Room/Keystore、下载 Range/SHA、更新包校验和 Worker v2 协议也已有开发门。尚未形成产品闭环的是 Android presence 心跳、PC scheduler 对 Android Full Worker 的正式准入、任务图 Stage 执行/重派，以及 Auth App 配置和日志/更新 UI 的接线。以上均可先做本机开发，真实设备/长时/温控验收后置。

旧 `frontend/` 从本次审查起冻结：不再新增功能、接口或视觉票，只保留作行为和接口迁移对照。新前端唯一开发目标为 `frontend_cybergothic/`。

## 2. 审查方法与扫描证据

### 2.1 机械扫描

使用既有工具：

```powershell
.venv-test\Scripts\python.exe scripts\api_gap_scan.py --include-gateway --include-control --json
.venv-test\Scripts\python.exe scripts\api_gap_scan.py --include-gateway --include-control --ownership
```

审查前的混合基线为后端 `417` 条、前端 `255` 条；缺口 A `0`、缺口 B `138`、方法软缺口 `2`。`FE-AUD-01` 完成后，默认 canonical 扫描只包含 `frontend_cybergothic`：证据条目 `214` 条，缺口 A `0`、缺口 B `182`（内部协议 `115`、未消费 `67`）、软缺口 `0`；显式加入旧前端后为 `canonical=214 + legacy=161`，缺口 B `116`（内部协议 `107`、未消费 `9`）、软缺口 `0`。`FE-AUD-02` 在相同 JSON 中增加 `ownership_schema=qlh.api_ownership.v1`、`ownership_counts` 和按 endpoint 聚合的 `ownership` 数组；三面真实 ownership 域分类为 experimental `5`、internal protocol `120`、product `167`。证据条目包含共享 client 路由和页面/组件/Hook 的受限 symbol-call，不等同于唯一接口数量。

### 2.2 误报边界

- 默认工具只扫描 `frontend_cybergothic`；旧 `frontend/` 只有传入 `--include-legacy-frontend` 才进入对照。不能把混合数量视为新前端能力。
- TypeScript 泛型调用窗口已修复；跨行 `request<Result<...>>()` 不再截断 method，当前软缺口为 `0`。
- 缺口 B 包含节点内部、bootstrap、Android register/heartbeat、Qwen3 data plane 和 gateway/control 重复路由；这些不应复制成普通用户按钮。
- 报告会区分 `shared_client` 声明与页面/组件/Hook 的静态 symbol-call 证据；无法追踪的动态调用仍只算共享 client 证据，不宣称完整运行时覆盖。
- ownership 的 `domain` 与 `frontend_status` 分离：`experimental`/`internal_protocol` 描述后端契约域，`page_consumed`/`component_consumed`/`hook_consumed`/`client_declared`/`unconsumed` 描述前端证据；不得把实验或内部接口因有页面/Hook 证据而降级为普通产品接口。

## 3. `frontend_cybergothic` 真实缺口

| 票号 | 优先级 | 缺口与证据 | 开发边界 |
|---|---:|---|---|
| `CY-API-01` | P0 | Auth App bootstrap、`users/:id/totp`、TOTP verify、recovery-codes rotate 未进入 `data/api.ts`/Account 页面；当前只能登录、退出、会话撤销、用户启停和 Tailscale 绑定 | 首次主节点初始化、字符串/QR 下发、一次性恢复码展示与轮换；秘密不进 SQLite 明文/日志；不改变现有登录契约 |
| `CY-API-02` | P1 | 集群 profiles 已有 list/create/delete/verify client，但 current/activate/endpoints verify、pipeline capacity、layers、distributed-inference、sidecar contract 没有完整页面流程 | Cluster Admin 增加档案切换、端点检查、容量/层计划只读视图和 Sidecar 生命周期；写操作继续 role-gated、二次确认 |
| `CY-API-03` | P1 | Models 页只有本地资产只读预检、load/unload、prepare pipeline、GGUF 浏览器下载；artifact inspect/register/import、模型文件/assignment、runtime contract/deployment simulation 未形成管理工作流 | 导入/拉取/许可证/来源/失败恢复/部署模拟；不把旧单机 loader 或整模网络传输重新接回产品路由 |
| `CY-API-04` | P1 | Settings 只覆盖 RAG health、来源/FTS 基础操作；embedding jobs、capacity、cancel/run、ANN decision 无页面反馈 | 有界任务状态、容量摘要、provider/model tag 失效提示；默认 FTS/有界 cosine，`sqlite-vec` 仍按 NO_GO 处理 |
| `CY-API-05` | P2 | Image Studio 已有基础 generate/job/cancel/blob；edit/img2img 完整表单、asset inspect/import/download、distributed workflow blob/history 未形成完整用户路径 | 生成/编辑/局部重绘、任务历史、失败重试和 blob 生命周期；真实 CUDA/双机质量门后置 |
| `CY-API-06` | P2 | Tasks 只有队列/工作流摘要和取消；Stage 输入输出、attempt/worker、重试原因、journal cleanup/优化 shadow 结果没有完整详情 | 只读阶段详情、稳定错误码、重派/取消边界和审计链接；不得直接选择未准入的优化图 |
| `CY-API-07` | P2 | `storage/health`、model pipeline assignment、logs client diagnostics、speculative experiment 等已存在 API 主要停留在共享 client 或未提供合适入口 | 按管理员/实验门显示，明确 local-only、实验态和不可用原因；不把内部 data plane 公开为普通表单 |
| `CY-API-08` | P0 | 已由 `FE-AUD-01` 完成 | 扫描器默认只扫 `frontend_cybergothic`，旧 `frontend/` 需显式 `--include-legacy-frontend`；报告已区分接口声明、页面/组件/Hook 消费证据和内部协议 |

### 3.1 已确认无需另立缺口票

新前端已实际接线或已有可用页面：聊天 SSE/取消/附件、会话 CRUD、基础 SD 生成与任务轮询、模型加载/卸载/预检、设备画像/GPU 选择、RAG FTS/来源删除、日志预览/下载/导出、审查工单、集群节点注册/删除/邀请、用户管理与 Tailscale 绑定、模型搜索/下载任务（P0c/P1a/P1b）。接口缺口报告中的重复条目不能再次开票。

## 4. Android 与后端缺口

| 票号 | 优先级 | 当前状态 | 后端/客户端缺口 | 可先做的开发 |
|---|---:|---|---|---|
| `AND-API-01` | P0 | 部分实现 | Android 以重复 register 代替 `/api/cluster/android/heartbeat`，没有独立 presence lease/断线退避和服务器时间围栏 | ApiClient DTO、前台 Service 心跳状态机、generation/attempt fencing、失败码和离线 UI；真机网络门后置 |
| `AND-API-02` | P0 | 协议已完成，准入未接 | `android_full_worker` 已被 v2 schema 接受，但 scheduler 仍只注册/准入 PC Full Worker；没有 Android hello→offer→lease→result 的生产 adapter | PC scheduler Android adapter、模型身份/单并发/电量温控能力门、重派与迟到结果审计；不需要第二台设备即可用 fake worker 完成 |
| `AND-API-03` | P1 | 远期未实施 | Android Worker 尚未进入任务图 Stage executor，P2 DAG 仍停留计划 | 把 Android Full、PC Full、PyTorch pipeline 三类 executor 接到统一 Stage contract；先用模拟 Android worker 和 journal 验证 |
| `AND-API-04` | P1 | 能力闸门已完成 | Gemma4 mmproj 资源登记/JNI capability 已有，但 `qlh_llama_jni.cpp` 尚未接入图像 token/MTMD forward；Full 当前对本地图像 fail-closed | 接入 mmproj load/encode/cleanup 与 path-free capability 结果；真实 Gemma 12B RAM/视觉质量验收后置 |
| `AND-API-05` | P2 | 开发资产存在，UI 未接 | `AndroidAppUpdateManager` 有清单、进度、SHA、签名校验，但没有 MainViewModel/Settings 入口；本地日志也没有上传 `POST /api/logs/client-error` 或节点诊断汇总 | 更新检查/下载/安装权限状态 UI，日志脱敏导出/可选上报、连接健康报告；真实 APK/商店安装门后置 |
| `AND-API-06` | P2 | 已覆盖，待验收 | 登录、Keystore session、远程 SD、GGUF Range/SHA、Room 会话均已接后端 | 不再重复开发；只补错误重试、分页和验收证据 |

### 4.1 Android 明确不做

Android Lite 不承担 Worker；Android 本地 SD、本地判题、任意 PyTorch 层间拆分和未验证 GPU backend 不进入当前开发票。Android 图像生成继续走 PC 主节点远程 API，避免把 PC SD 资产和 CUDA 质量门复制到移动端。

## 5. 全量 `docs/` 审查结论（排除硬件阻塞）

### 5.1 已收口或仅待环境验收

以下文档的本机开发票已闭合，不再重复开票：模型一键下载 P0a/P0b/P0c/P1a/P1b、RAG S0-S5D、T-RACE-0 至 T-RACE-6B、CLUSTER-JOIN、SYNC-SSH-S0/S1/S2、NW3.1/NW4 本机门、任务图 TG-OPT-G0-G5.3-P0、QW3 现有开发门、Android AND-A1-01/02/03、AND-B-01/02/03、AND-A2-01/02、AND-C-01、AND-E-01、AND-F-01。其余真实 CUDA、双机长时、Linux 真机、WSS/443、安装包和大模型质量均属于后置环境门。

### 5.2 仍有软件开发工作的现行计划

| 票组 | 文档依据 | 是否硬件阻塞 | 建议处理 |
|---|---|---|---|
| `CY-API-01..08` | 本文、`frontend_cybergothic功能复核与页面重构清单.md`、`接口契约对齐审计计划.md` | 否 | 按第 6 节执行，cyber-gothic 为唯一前端目标 |
| `AND-API-01..05` | `安卓与PC功能差距清单.md`、`Android版本远期计划.md` | 否（真机仅验收） | 先做协议/adapter/UI 接线，完成后再申请真机门 |
| `BUNDLE-M1` | `离线资产一键整合包设计.md` | 否 | 实现 PC/Android bundle builder、manifest/容量预检、ZIP_STORED 分发与恢复检查 |
| `EX-N3-PROD` | `自动化优化实验与报告方案.md` | 否 | 将已完成的实验质量证据升级为生产规则，保持 prompt/输出脱敏；不重跑被资源阻塞的模型 |
| `DOCAGENT-M2-GATE` | `文档维护Agent工具设计.md` | 否 | 冻结 4 个人工标签样本，完成语义一致率门；不改变 git 为事实源的约束 |
| `DOCAGENT-M3-QA` | `文档维护Agent工具设计.md` | 否 | 用现有本地 embedding/FTS 做 30 条变更描述→文档 top-5 命中率基准；质量不达标时保留 FTS，不把 RAG 当事实源 |
| `MODEL-TOOLS-LINUX` | `模型通用小工具调研与方案.md` | **是** | 排除本次开发队列，等待 Linux 真机 |
| `MF-MEM-N3`、QW3.8 S1-S4、SD CUDA/N4、Gemma 外部门、NW WSS/443 | 各专项计划 | **是** | 只保留验收登记，不提前修改生产路径 |

### 5.3 文档治理项

- `frontend/` 相关旧清单改为历史对照，不再被总体计划当作待开发前端。
- `接口契约对齐审计计划.md` 的 150/138 条 B 缺口不能直接当产品缺陷；以后报告必须带 canonical 前端、内部协议和“仅 API client 未接页面”三个维度。
- `docs/文档状态与清理清单.md` 的文档维护 Agent 条目已在本轮同步为 M1、M2.1-M2.4、M3.1-M3.4 开发完成；后续只保留语义质量/真实 provider 验收状态，避免把治理票误报为业务缺口。

### 5.4 文档维护工具复核

`scripts/doc_maintenance_audit.py --fail-on none` 本轮扫描 66 份文档、命中 42 项，主要是历史日期早于近期代码提交、部分旧文档前 12 行没有状态行，以及新/未提交审查文档被标为工作树变更。它们属于元数据治理提示，不应自动转换为产品开发票。

- `QW3.8-27B` 的“规划（S0 完成）”状态与正文完成词命中 R1，属于措辞/规则误报，专项仍按硬件后置处理。
- `离线资产一键整合包设计` 的 M1 未开始与正文设计完成标记并存，实际开发票仍为 `BUNDLE-M1`，本审查将其保留为未完成软件票。
- 旧 TUI Q&A 链接已从 README 移除并指向现有 TUI 计划/指令集；相对链接检查最终为 70 份文档、482 条链接全部通过。

## 6. 分期开发票

### Phase 0：审计基线与前端边界

1. `[x]` `FE-AUD-01`：canonical API gap scanner，默认只扫 `frontend_cybergothic`，旧前端显式对照，修复跨行 method/泛型调用识别并加入受限 symbol-call 证据；专项 `12 passed`，canonical/legacy 两种真实仓库扫描均通过。
2. `[x]` `FE-AUD-02`：生成可持续的 endpoint ownership 表，区分页面消费、共享 client 声明、内部节点协议和实验端点；`--ownership` 输出表格，`--json` 提供 `qlh.api_ownership.v1` CI 字段，专项 `15 passed`。

### Phase 0.5：cyber-gothic 可用性修复

3. `[x]` `CY-UX-FIX-01`：修复 Audit 聚合日志缺失时间字段导致的 render 崩溃；Downloads 接入页面背景、动效和统一面板层；Activity 日志改为每页 24 条的固定分页。TypeScript/build、对比度和定向 Playwright `8 passed`；真实后端长时日志与低端设备滚动性能仍后置验收。

### Phase 1：身份与集群控制面

4. `CY-API-01`：Auth App bootstrap/QR/字符串/TOTP verify/recovery rotate 页面与 API client。
5. `CY-API-02`：Cluster profiles activate/current/endpoints、pipeline capacity/layers、Sidecar 合同只读与 role-gated 操作。
6. `AND-API-01`：Android heartbeat/presence lease 与离线状态机。
7. `AND-API-02`：PC scheduler Android Full Worker adapter，先接 fake worker，不等待真机。

### Phase 2：模型、RAG 与工作流产品面

8. `CY-API-03`：模型资产导入/拉取/来源/许可证/部署模拟和失败恢复。
9. `CY-API-04`：RAG embedding job/capacity/ANN decision 工作区。
10. `CY-API-06`：任务图 Stage/Attempt/Provider 详情、重派和审计。
11. `AND-API-03`：Android Full Worker 接入任务图 Stage executor，补模拟跨候选回归。
12. `BUNDLE-M1`：离线资产一键整合包开发门。

### Phase 3：多模态、运维与质量收口

13. `CY-API-05`：SD 编辑/分布式任务/结果历史工作区。
14. `CY-API-07`：storage/log diagnostics、实验端点和实验门状态。
15. `AND-API-04`：Android mmproj/JNI 图像理解接线，保持资源门 fail-closed。
16. `AND-API-05`：Android 更新/日志/连接诊断入口。
17. `EX-N3-PROD`、`DOCAGENT-M2-GATE`、`DOCAGENT-M3-QA`：分别完成生产质量证据、文档语义基准和文档 RAG 质量基准。

每票的“开发完成”只表示源码、契约和本机测试通过；真实 Android 设备、CUDA、双机网络、安装包、WSS/443 和质量采样仍按原专项文档单独验收，不得用 fixture 或静态扫描代替。`FE-AUD-02` 的 ownership 是静态证据聚合，不替代运行时路由审计。
