# 通用工具与子项目候选计划（2026-09-12）

> 状态：**候选池，未排期**；本文只做"哪些模块值得抽成通用子项目/工具"的筛选与优先级排序，逐项落地时再转开发票，不在此重复立项。
>
> 创建日期：2026-09-12
> 适用范围：从主仓与 `harness_workbench/` 中筛选可跨项目复用、可独立维护的模块。**不覆盖**已独立子项目（`tools/docagent`、`tools/reasonix-codex-bridge`）、已实施的 MODEL-TOOLS 小工具（[模型通用小工具调研与方案](模型通用小工具调研与方案.md)）与已在排期的 harness 开发票（[harness 下一阶段开发票计划](harness下一阶段开发票计划.md)）。
> 来源：2026-09-12 模块盘查（规模/依赖/耦合度实测）+ 用户对 C 类候选的取舍意见（保留 venv 环境治理；抗弱网不予考虑；异构探测低优先）。

---

## 1. 判定标准

进入 A/B 档的候选须**同时**满足四条：

1. **跨项目复用面** —— 脱离 QLH 业务仍有真实使用方；
2. **自包含度高** —— 不 import 主项目业务模块，边界可以一刀切；
3. **有可机器验证的验收门** —— 抽出时可把门一起带走（本仓库最有价值的部分其实是门）；
4. **依赖轻** —— 纯标准库或单一同质依赖。

反面理由（为什么标准要严）：多一个仓库 = 契约漂移 + 双 CI + 版本对齐成本。**"代码量大"本身不构成抽离理由**——这与[既有独立化评估](项目后续可做方向发散与子项目独立化评估-2026-09-09.md) §4 对 harness 整体独立化给出的保留意见一致。

---

## 2. A 档：已经事实独立，只差"挂牌"

| 编号 | 候选 | 位置与规模 | 依赖 | 复用场景 | 毕设适配 |
| --- | --- | --- | --- | --- | --- |
| `UNI-A1` | **进程归属票据 + fail-closed 重置** | `scripts/demo/demo_ownership.py`(204) + `defense_reset.py` + `reset.bat/sh` + `tests/test_defense_reset.py` | 仅 `psutil` | 任何"启动子进程做演示/测试/压测"的项目都需要"不留残留、不误杀" | 中：体量小，适合作为工程可靠性主题的一块 |
| `UNI-A2` | **可复现演示证据链工具** | `scripts/demo/defense_{demo,benchmark,story,preflight}.py` + `scenarios.json`/`storyline.json`/`checklist.json` + `scripts/perf_report.py`(503) | 纯标准库 | 需要"给人看演示"的项目：可复现 + 防夸大 + 声明边界（`claim_guard`） | 中高：证据工程方法论，易写成选题 |
| `UNI-A3` | **Ed25519 签名更新链路** | `packaging/signing.py`(577) + `update_core.py`(563) + `updater.py`(646) + `install_manifest.py`(886) + `repair.py`(633) + `version_store.py`/`launcher_slots.py` | `cryptography`（`signing.py` 明确标注为唯一非标准库依赖），其余纯标准库 | 桌面应用发布框架：清单签名/key rotation/原子版本/A-B 自更新回滚 | **高**：安全 + 工程双料，体量足够做毕设主线 |
| `UNI-A4` | **零依赖 TUI 管理面板** | `src/tui_admin.py`(2867) + `tui_chat.py`(715) + `tui_sse.py`(155) + `tui_shared.py`(234) | 管理面板纯标准库（Windows ctypes / POSIX termios 双实现 + 非 ANSI 降级）；聊天页可选 Textual + httpx | "任意 HTTP API 后端的终端管理台"——只需抽一层 API 契约 | 高：终端 UI 经典题，已有真实使用场景 |
| `UNI-A5` | **多机同步 + 签名补丁分发** | `tools/ssh_sync_*.py`(约 1300) + `patch_dispatch.py`(295) + `patch_listener.py`(506) | 标准库 + SSH 通道 | 小团队多机部署/热修 | 中高 |

抽离方式统一为：**内部整理（把内嵌配置数据化 + 补 README/验收门）→ 独立仓库（可先 private）→ 主仓换 submodule**，参照 `docagent` 与 `reasonix-codex-bridge` 的先例，一次提交完成目录替换与 gitlink 更新。

---

## 3. B 档：harness 内部模块，先做"内部独立包"

harness **整体**独立化已判定为"大创结题后再议"（契约漂移 + 真实运行时验收未完成）。本档是其**前置步骤**：先在 `harness_workbench/` 内建立独立包（自己的 `pyproject.toml` + 测试 + 不改主项目 import 路径），跑通后再择机挂 submodule——风险远低于整包独立。

> 现状说明：`harness_workbench/` 目前**没有** `pyproject.toml` 或独立 `requirements.txt`，依赖管理挂在主仓，这是"先建包"这一步要补的第一件事。

| 编号 | 候选 | 规模 | 复用场景 | 毕设适配 |
| --- | --- | --- | --- | --- |
| `UNI-B1` | **RAG 工具包**（`rag/` 1465 + `tools/rag_baseline.py` 598 + 主仓 `src/rag_store.py`） | ~2700 | 分块/索引/改写/重排 + hit@5/MRR 质量门；六张 RAG 票已完成、主仓与 harness 双端实现可对照 | **极高**（RAG 富矿，工程量与验收门都现成） |
| `UNI-B2` | **context_engine**（上下文预算 + STATE 压缩） | 1227 | 任何 LLM 应用的上下文工程 | 高 |
| `UNI-B3` | **判题口径 + 评测账本**（`tools/judge_policy.py` 435 + `eval/` 910 + `tools/benchmark_ledger.py` 551） | ~1900 | LLM 输出判题的可复现口径与可审计账本 | 高（评测基础设施） |
| `UNI-B4` | **model_profiles + adapters** | 990 + 937 | 小模型能力探测/画像 + OpenAI 兼容适配 | 高 |
| `UNI-B5` | **模型资产工具组**（`tools/download_runner.py` 697 + `model_card.py` 620 + `manifest_health.py` 462） | ~1800 | 模型下载治理/模型卡/清单体检 | 中高 |
| `UNI-B6` | **mcp_server** | 1337 | "把任意本地工具暴露成 MCP" | 中高 |
| `UNI-B7` | **red_team_lab** | 326 | LLM 安全样本集（可与安全类选题结合） | 中 |

依赖实测：`rag/` 等模块接近纯标准库（检索路径仅见 `unicodedata` 等），抽包时以"零或极少第三方依赖"为目标。

---

## 4. C 档：只保留一项入选 + 一项低优先观察

### 4.1 入选：`UNI-C1` venv 环境治理工具

- 位置与规模：`scripts/setup_envs.py`（663 行），**纯标准库**（`argparse`/`venv`/`shutil`/`subprocess`/`tempfile` 等，无第三方）。
- 现状能力（实测 CLI）：`--list` 环境清单、`--all` 全量、`--only`/`--skip` 选择、`--no-node` 跳过 Node 子项目、`--check` 只校验不安装、`--snapshot` 导出 `requirements-lock/*.lock.txt`、`--dry-run`、`--torch-index-url` 指定 PyTorch 源（torch 刻意不自动装）。
- 抽离形态：把内嵌的环境定义**数据化**（一份 `envs.yaml`/`envs.json` 描述"主环境 / 测试 / 打包 / TUI"各层），保留 `--check`/`--snapshot` 语义并增加**快照漂移检测**（当前 `--check` 只做健康检查，不与 lock 比对），产出通用的 *multi-venv manager*。
- 依赖：零第三方。
- 验收门：`--check` 只读校验；`--snapshot` 可复现导出；漂移检测对人为改动的环境报差异；全部离线可跑。
- 毕设适配：中高（工程效能方向，可扩展到 CI 缓存与锁文件治理）。

### 4.2 低优先观察项：`UNI-C2` 异构设备能力探测

- 位置与规模：`src/device_profiler.py`（1308 行）。
- 保留原因：抽离后可用于"某台设备能否跑某个模型/引擎"的通用准入判断。
- 现状阻力：采集项与 QLH 节点语义耦合（角色、集群档案等），抽离前需先做"采集层 / 判定层"分离。
- 优先级：**低**（用户 2026-09-12 取舍）。具备实施条件时再转票。

---

## 5. 明确不纳入（含理由）

| 候选 | 位置 | 不纳入理由 |
| --- | --- | --- |
| 抗弱网与 Transport v2 | `src/cluster_transport.py`(815) | 技术价值最高，但团队成员均为**软件工程专业**，不选网络协议方向做毕设（2026-09-12 决定） |
| 图算法编排 | `src/graph_orchestrator.py`(707) | 未入选（用户取舍） |
| 冻结契约与迁移映射 | `schemas/`（8 份 JSON Schema + migration-map） | 未入选 |
| 赛博哥特设计系统 | `frontend_cybergothic/`（主题组件 + 对比度门 + Playwright 回归） | 未入选 |
| 控制面微服务 | `control/`、`gateway/`（NestJS + Fastify） | 与 QLH 控制面语义绑死，抽离收益低于维护成本 |
| 主业务面 | `src/local_store.py`、`src/api_server.py` 等 | 它们就是"主项目"本身，抽走即掏空主仓 |
| TUI 聊天页单独成库 | `src/tui_chat.py`/`tui_sse.py` | 并入 `UNI-A4` 一起交付，不单独设项 |

---

## 6. 优先级与建议执行序

近 4 周建议顺序（由易到难、由小到大）：

1. `UNI-A1` 进程归属与重置（最小、最通用，1–2 天可完成内部整理）
2. `UNI-A2` 演示证据链工具（把"演示方法论"固化为工具）
3. `UNI-B1` RAG 工具包（地基最厚：六张票完成 + 双端实现，先建内部独立包）
4. `UNI-A4` 零依赖 TUI 面板（需先抽 API 契约层）
5. `UNI-A3` 签名更新链路（体量最大，适合作为毕设主线，建议放到有一位同学专责时启动）
6. 其余按需：`UNI-A5`、`UNI-B2`~`UNI-B7`、`UNI-C1`（venv 治理可与任何阶段并行）

毕设一人一题映射建议：

| 题目方向 | 主候选 | 体量 | 交付物形态 |
| --- | --- | --- | --- |
| 签名与安全更新 | `UNI-A3` | 大 | 独立仓库 + 签名/回滚验收门 + 演示更新链路 |
| RAG 工程化 | `UNI-B1` | 大 | 独立包 + hit@5/MRR 门 + 双端对照报告 |
| 上下文工程 | `UNI-B2` | 中 | 独立包 + 压缩策略对照（预算/降级曲线） |
| 评测口径与账本 | `UNI-B3` | 中 | 独立包 + 可复现判题口径 + 账本审计 |
| 终端工具链 | `UNI-A4` | 中 | 独立包 + API 契约 + 跨平台 TUI 录屏 |
| 工程效能（环境治理） | `UNI-C1` | 小-中 | 独立包 + 漂移检测门 |
| 进程与资源可靠性 | `UNI-A1` + `UNI-A2` | 小-中 | 两个小工具合并成一个"可复现演示与清理"题目 |

---

## 7. 团队分工与落地约定

1. **先内部独立包，后独立仓库**：`packages/<name>/`（自带 `pyproject.toml` + `tests/` + README）→ 验收门跑通 → 再建远端仓库并换 submodule。这样主仓不被搅动，也避免整包独立的契约漂移。
2. **搬代码必须搬门**：抽离时把验收门、脱敏契约（不落正文/密钥/绝对路径）、fail-closed 语义一并带走；只搬代码不搬门，子项目讲不出"可证明能用"。
3. **每个子项目自带四件套**：README（含用法与边界）、验收门命令、离线可跑的测试、脱敏示例输出。
4. **不复制源码回主仓**：主仓只保留 gitlink 与接线文档（沿用 `docagent`、`reasonix-codex-bridge` 的做法）。
5. **一个候选一张票**：本文只提供候选池与排序；正式开工时在开发票文档登记，附依赖与验收门。

---

## 8. 与既有结论的关系

| 既有结论 | 出处 | 本文的关系 |
| --- | --- | --- |
| `docagent` 已独立为子项目 | [子项目独立化评估](项目后续可做方向发散与子项目独立化评估-2026-09-09.md) §4 | 已完成，本文不重复 |
| `reasonix-codex-bridge` 已注册为子模块 | 同上（2026-09-12 后续） | 已完成，本文不重复 |
| harness 整体"大创结题后再独立" | 同上 §4.1 | 本文 §3 是其**前置步骤**（先内部独立包） |
| MODEL-TOOLS 小工具（`scripts/model_tools.py` 系列）已实施 | [模型通用小工具调研与方案](模型通用小工具调研与方案.md) | 已落地，本文不重复；其"统一入口 + 子命令"形态可作为抽包模板 |
| 答辩工具链（DEF-P1~P4、A1~A4）已完成 | [开发票计划](开发票计划-审计收口与答辩演示-2026-09-09.md) | 本文 `UNI-A2` 是把它提炼为通用工具 |

---

## 9. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-12 | 首版：A 档 5 项（A1 进程归属/重置、A2 演示证据链、A3 签名更新、A4 零依赖 TUI、A5 多机同步与补丁分发）、B 档 7 项（harness 内部包，RAG 优先）、C 档 1 项入选（venv 环境治理）+ 1 项低优先观察（异构设备探测）；抗弱网（软工专业不选网络方向）、图算法、`schemas/`、前端设计系统、`control/`+`gateway/` 与主业务面明确不纳入 |
