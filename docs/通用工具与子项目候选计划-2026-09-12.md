# 通用工具与子项目候选计划（2026-09-12）

> 状态：**候选池，未排期**；本文只做"哪些模块值得抽成通用子项目/工具"的筛选与优先级排序，逐项落地时再转开发票，不在此重复立项。
>
> 创建日期：2026-09-12
> 适用范围：从主仓与 `harness_workbench/` 中筛选可跨项目复用、可独立维护的模块。**不覆盖**已独立子项目（`tools/docagent`、`tools/reasonix-codex-bridge`）、已实施的 MODEL-TOOLS 小工具（[模型通用小工具调研与方案](模型通用小工具调研与方案.md)）与已在排期的 harness 开发票（[harness 下一阶段开发票计划](harness下一阶段开发票计划.md)）。
> 来源：2026-09-12 模块盘查（规模/依赖/耦合度实测）+ 用户对 C 类候选的取舍意见（保留 venv 环境治理；抗弱网不予考虑；异构探测低优先）。

---

## 1. 判定标准

进入 A/B 档的候选须**同时**满足四条：

1. **跨项目复用面** —— 脱离 QLH 业务仍有真实使用方；
2. **自包含度高** —— 不 import 主项目业务模块，边界可以一刀切；
3. **有可机器验证的验收门** —— 抽出时可把门一起带走（本仓库最有价值的部分其实是门）；
4. **依赖轻** —— 纯标准库或单一同质依赖。

反面理由（为什么标准要严）：多一个仓库 = 契约漂移 + 双 CI + 版本对齐成本。**"代码量大"本身不构成抽离理由**——这与[既有独立化评估](项目后续可做方向发散与子项目独立化评估-2026-09-09.md) §4 对 harness 整体独立化给出的保留意见一致。

---

## 2. A 档：已经事实独立，只差“挂牌”

| 编号 | 候选 | 规模（实测） | 依赖 | 工作量估计 | 毕设适配 |
| --- | --- | --- | --- | --- | --- |
| `UNI-A1` | 进程归属票据 + fail-closed 重置 | 302 + 299 行、9 个测试 | `psutil` | 1–2 人日 | 中 |
| `UNI-A2` | 可复现演示证据链工具 | 约 2700 行、53 个测试 | 纯标准库 | 3–5 人日 | 中高 |
| `UNI-A3` | Ed25519 签名更新链路 | 4035 行、13 个测试文件 | `cryptography` | 5–10 人日 | **高** |
| `UNI-A4` | 零依赖 TUI 管理面板 | 3971 行、5 个测试文件 | 纯标准库（聊天页可选 Textual + httpx） | 3–5 人日 | 高 |
| `UNI-A5` | 多机同步 + 签名补丁分发 | 约 2200 行、6 个测试文件 | 纯标准库（实测无 paramiko） | 3–5 人日 | 中高 |

抽离方式统一为：**内部整理（把内嵌配置数据化 + 补 README/验收门）→ 独立仓库（可先 private）→ 主仓换 submodule**，参照 `docagent` 与 `reasonix-codex-bridge` 的先例，一次提交完成目录替换与 gitlink 更新。

### 2.1 `UNI-A1` 进程归属票据 + fail-closed 重置（最小可行）

**现状（实测）**：`scripts/demo/demo_ownership.py` 302 行 + `defense_reset.py` 299 行 + `reset.bat`/`reset.sh` + `tests/test_defense_reset.py` 267 行 9 个测试（8 个常规 + 1 个需 `QLH_RUN_DEFENSE_RESET_INTEGRATION=1` 的实机门）。能力：崩溃可恢复的所有权票据（`pid`/`create_time`/`kind`/`root_pid`，原子写入 `*.tmp` → `os.replace`）；默认 dry-run，`--apply` 前按 PID 创建时间、工作目录、内置命令白名单**三重复核**；身份错配/无法停止/端口占用统一 `REVIEW_REQUIRED` 并保留现场；幂等（第二次 apply 为零票据）。

**四条件对照**：复用面——任何"启动子进程做演示/测试/压测"的项目都需要它；自包含——票据层只依赖 `psutil`，耦合点是 `OWNERSHIP_ROOT`/`FRONTEND_ROOT` 与 `_command_matches` 里 QLH 形态的命令白名单（npm/vite/esbuild、`uvicorn src.api_server:app`、两个 demo worker）；验收门——既有 9 个测试可迁移（含"篡改票据必须 `REVIEW_REQUIRED`""幂等""只删已死进程票据"）；依赖——`psutil`（BSD）。

**抽离形态**：包 `spawnledger`（暂名）——`OwnershipLedger(source, ports=[])` → `register(kind, pid)` → `close()`，CLI `reset [--apply] [--report PATH]`。命令白名单改为**注入策略**（调用方给出 `{kind: matcher(command, cwd)}`，默认策略覆盖常见 launcher 形态）；`OWNERSHIP_ROOT`、报告路径参数化；`reset.bat/sh` 退化为薄包装。

**前置与工作量**：抽策略层 + 参数化路径 + 补 POSIX 分支测试 ≈ 1–2 人日。本机只有 Windows，POSIX 终止路径必须在 WSL 或 CI 上验一次。

**风险**：跨平台终止分支未实测；误杀风险靠"三重复核 + 默认 dry-run"缓解——抽离时**不得**降级为"信任票据内容"。

### 2.2 `UNI-A2` 可复现演示证据链工具

**现状（实测）**：`scripts/demo/defense_demo.py` 819 + `defense_preflight.py` 517 + `defense_benchmark.py` 442 + `defense_story.py` 245 + `benchmark_worker.py` 101 + `failure_worker.py` 95（约 2200 行），加 `scripts/perf_report.py` 503 行与三份机器清单（`scenarios.json`/`storyline.json`/`checklist.json`）；测试 6 个文件共 53 个用例。能力：固定路由/成功标记契约、故障注入 worker（固定退出码 23）、`claim_guard`（`redacted`/`live_cluster`/`real_model_loaded`/`physical_dual_host` 必须全 false 才 PASS）、300 秒故事线的时间窗与证据清单校验、12 项自动 + 7 项人工 + 7 条回退的现场清单、性能报告拒绝把 fixture 延迟写成 TTFT/tok-s。

**四条件对照**：复用面——任何要"给人演示且不能夸大"的项目；自包含——耦合点是 `SCENARIO_SOURCE_PATHS` 指向 QLH 前端源码、`ROOT/FRONTEND_ROOT` 常量与"模型范围 deferred"这类业务检查；验收门——53 个用例可迁移，其中已含负向门（伪造成功标记失败、fixture 延迟不得当性能指标）；依赖——纯标准库。

**抽离形态**：`demo-evidence` 工具集——三份契约文件 + 三个校验器（storyline 时间线 / checklist 预检 / 性能报告）+ 一个启动器骨架（fixture 与 live 双模式、启动预算、脱敏报告）。QLH 特有部分（前端路由、模型目录、端口策略）以**适配器 + 配置**注入，不写进工具内核。

**前置与工作量**：路径与业务检查参数化、报告 schema 独立成文件、补一份"如何接到别的前端"的示例 ≈ 3–5 人日。

**风险**：契约文件里带文档路径（`docs/答辩…md`），抽离后需要允许外部文档根；演示脚本与真实进程交互多，跨平台需复验。

### 2.3 `UNI-A3` Ed25519 签名更新链路（体量最大、通用度最高）

**现状（实测）**：`packaging/` 下 `signing.py` 577 + `update_core.py` 563 + `updater.py` 646 + `install_manifest.py` 886 + `repair.py` 633 + `version_store.py` 319 + `launcher_slots.py` 411 = **4035 行**；`tests/` 下 13 个相关测试文件（`test_signing`、`test_updater`、`test_version_store(_cli)`、`test_install_manifest`、`test_repair`、`test_launcher*` 6 个）。能力：清单签名/验签（Ed25519 + `key_id`/`signature`/`signed_at` + 信任密钥目录）、key rotation、原子版本与 A/B 自更新回滚（槽位）、修复流程、静态分发服务器（`serve.py` 680 行）。

**四条件对照**：复用面——桌面应用"签名更新 + 回滚 + 修复"是高频需求，通用度为本档最高；自包含——框架层已与 QLH 载荷分离（`launcher.py` 1665 行是 QLH 专用，不在抽离范围）；验收门——13 个测试文件可直接迁移；依赖——`cryptography` 是 `signing.py` 明确标注的唯一非标准库依赖（Apache-2.0/BSD 双许可）。

**抽离形态**：`signed-update`（暂名）——`manifest`（生成/校验/签发）、`verify`（验签 + 信任钥加载 + rotation）、`install`（原子安装/槽位/回滚）、`repair`、`serve`（只读分发）。QLH 专属的模型门（`test_launcher_model_gate`）与赛博哥特桌面壳留给使用方。

**前置与工作量**：把"更新框架"与"QLH 应用载荷"之间剩余耦合点（版本号来源、安装包命名、平台 spec）显式化；验签后端保持可替换（`cryptography` 为首选，可留 openssl CLI 兜底）≈ 5–10 人日，建议作为一位同学的毕设主线。

**风险**：与 Inno Setup / Windows 代码签名的耦合部分不可跨平台移植；真实发布链路（干净机首启、Linux `.deb` 安装）目前仍是主仓的后置验收门，抽离后需重新登记验收边界。

### 2.4 `UNI-A4` 零依赖 TUI 管理面板

**现状（实测）**：`src/tui_admin.py` 2867 + `tui_chat.py` 715 + `tui_sse.py` 155 + `tui_shared.py` 234 = **3971 行**；`tests/` 下 5 个文件（`test_tui_chat`、`test_tui_commands`、`test_tui_packaging`、`test_tui_shared`、`test_tui_sse`）。终端能力层：Windows 用 `ctypes` 启用 VT + `msvcrt` 读键，POSIX 用 `termios`/`tty` + `select`，非 ANSI/管道自动降级为纯文本编号菜单。

**关键实测**：HTTP 调用**集中在一个客户端类**（`src/tui_admin.py:253-296`：`base_url` + `"/api" + path` 拼装、`urllib.request` 收发、统一 `ApiError` 与后端不可达提示；文件内不含散落的 `/api/` 字面量）。这意味着抽"API 契约层"的成本低于预期——端点集中在变量拼接处。

**四条件对照**：复用面——"任意 HTTP API 后端的终端管理台"；自包含——需把"菜单结构 + 端点契约"数据化，其余（渲染/按键/降级）已是通用件；验收门——既有 5 个测试文件，另可加"菜单契约快照比对"；依赖——管理面板纯标准库；`tui_chat` 依赖 Textual + httpx（源码模式可选安装、安装包内置）。

**抽离形态**：`tui-console`（暂名）——终端能力层（ANSI 检测 / 按键 / 降级）+ 数据驱动菜单引擎（菜单项 → HTTP 调用 → 表格渲染 → 详情页）+ 可选 SSE 聊天页。使用方只提供"菜单定义文件 + 端点契约"。

**前置与工作量**：把 `tui_admin` 内硬编码的菜单/文案结构抽成数据；为端点契约补一份快照测试 ≈ 3–5 人日。

**风险**：跨平台终端差异代码里已处理，但 POSIX 分支未在真机复验；中文宽字符对齐在不同终端下需抽样验证。

### 2.5 `UNI-A5` 多机同步 + 签名补丁分发

**现状（实测）**：`tools/ssh_sync_contract.py` 379 + `ssh_sync_helper.py` 447 + `ssh_sync_deployment.py` 354 + `ssh_sync_client.py` 216 + `patch_listener.py` 506 + `patch_dispatch.py` 295 ≈ **2200 行**；测试 6 个文件（`test_ssh_sync_{client,contract,deployment,helper}`、`test_patch_dispatch`、`test_patch_listener`）。依赖实测：**无 paramiko**，走标准库 `socket`/`subprocess`/`hashlib`/`base64`；补丁签名复用 `packaging/signing.py`。

**四条件对照**：复用面——小团队多机部署与热修（比 A3/A4 窄）；自包含——契约里带"节点身份/集群角色"概念，需抽象为通用配置；验收门——6 个测试文件；依赖——纯标准库。

**抽离形态**：`cluster-patch`（暂名）——补丁打包 / 分发 / 监听 / 应用 + 契约校验（帧、分支、force-clean、代理）。节点身份与集群语义改由使用方配置提供。

**前置与工作量**：契约层解耦（把集群概念换成通用"主机清单"）≈ 3–5 人日。

**风险**：与主仓集群概念绑定较深；收益低于 A3/A4，建议排在它们之后。

### 2.6 A 档横向对比与建议顺序

| 编号 | 复用面 | 自包含度 | 验收门完整度 | 工作量 | 建议顺序 |
| --- | --- | --- | --- | --- | --- |
| `UNI-A1` | 高（任何 spawn 子进程的项目） | 高（只需抽策略层） | 高（9 测试含负向门） | 小 | **第 1** |
| `UNI-A2` | 中高（要给人演示的项目） | 中（QLH 路径/业务检查需参数化） | 高（53 测试） | 中 | 第 2 |
| `UNI-A3` | 高（桌面应用发布框架） | 高（框架/载荷已分离） | 高（13 个测试文件） | 大 | 第 4（毕设主线） |
| `UNI-A4` | 中高（终端管理台） | 中高（菜单与端点需数据化） | 中（5 个测试文件 + 待补契约快照） | 中 | 第 3 |
| `UNI-A5` | 中 | 中（需解集群概念） | 中（6 个测试文件） | 中 | 第 5 |

> 顺序理由：A1 最小且直接复用（顺便建立"抽包 + 搬门"的模板）→ A2 把已完成的方法论固化 → A4 需一次数据化改造 → A3 体量大、适合作为毕设主线，需要有专人负责时启动 → A5 收益最低，排最后。

> 术语说明：`UNI-A*` 是本计划自用的候选编号；开工时按 §7 第 5 条约定在开发票文档登记正式票号。

---

## 3. B 档：harness 内部模块，先做"内部独立包"

harness **整体**独立化已判定为"大创结题后再议"（契约漂移 + 真实运行时验收未完成）。本档是其**前置步骤**：先在 `harness_workbench/` 内建立独立包（自己的 `pyproject.toml` + 测试 + 不改主项目 import 路径），跑通后再择机挂 submodule——风险远低于整包独立。

> 现状说明：`harness_workbench/` 目前**没有** `pyproject.toml` 或独立 `requirements.txt`，依赖管理挂在主仓，这是"先建包"这一步要补的第一件事。

| 编号 | 候选 | 规模 | 复用场景 | 毕设适配 |
| --- | --- | --- | --- | --- |
| `UNI-B1` | **RAG 工具包**（`rag/` 1465 + `tools/rag_baseline.py` 598 + 主仓 `src/rag_store.py`） | ~2700 | 分块/索引/改写/重排 + hit@5/MRR 质量门；六张 RAG 票已完成、主仓与 harness 双端实现可对照 | **极高**（RAG 富矿，工程量与验收门都现成） |
| `UNI-B2` | **context_engine**（上下文预算 + STATE 压缩） | 1227 | 任何 LLM 应用的上下文工程 | 高 |
| `UNI-B3` | **判题口径 + 评测账本**（`tools/judge_policy.py` 435 + `eval/` 910 + `tools/benchmark_ledger.py` 551） | ~1900 | LLM 输出判题的可复现口径与可审计账本 | 高（评测基础设施） |
| `UNI-B4` | **model_profiles + adapters** | 990 + 937 | 小模型能力探测/画像 + OpenAI 兼容适配 | 高 |
| `UNI-B5` | **模型资产工具组**（`tools/download_runner.py` 697 + `model_card.py` 620 + `manifest_health.py` 462） | ~1800 | 模型下载治理/模型卡/清单体检 | 中高 |
| `UNI-B6` | **mcp_server** | 1337 | "把任意本地工具暴露成 MCP" | 中高 |
| `UNI-B7` | **red_team_lab** | 326 | LLM 安全样本集（可与安全类选题结合） | 中 |

依赖实测：`rag/` 等模块接近纯标准库（检索路径仅见 `unicodedata` 等），抽包时以"零或极少第三方依赖"为目标。

---

## 4. C 档：只保留一项入选 + 一项低优先观察

### 4.1 入选：`UNI-C1` venv 环境治理工具

- 位置与规模：`scripts/setup_envs.py`（663 行），**纯标准库**（`argparse`/`venv`/`shutil`/`subprocess`/`tempfile` 等，无第三方）。
- 现状能力（实测 CLI）：`--list` 环境清单、`--all` 全量、`--only`/`--skip` 选择、`--no-node` 跳过 Node 子项目、`--check` 只校验不安装、`--snapshot` 导出 `requirements-lock/*.lock.txt`、`--dry-run`、`--torch-index-url` 指定 PyTorch 源（torch 刻意不自动装）。
- 抽离形态：把内嵌的环境定义**数据化**（一份 `envs.yaml`/`envs.json` 描述"主环境 / 测试 / 打包 / TUI"各层），保留 `--check`/`--snapshot` 语义并增加**快照漂移检测**（当前 `--check` 只做健康检查，不与 lock 比对），产出通用的 *multi-venv manager*。
- 依赖：零第三方。
- 验收门：`--check` 只读校验；`--snapshot` 可复现导出；漂移检测对人为改动的环境报差异；全部离线可跑。
- 毕设适配：中高（工程效能方向，可扩展到 CI 缓存与锁文件治理）。

### 4.2 低优先观察项：`UNI-C2` 异构设备能力探测

- 位置与规模：`src/device_profiler.py`（1308 行）。
- 保留原因：抽离后可用于"某台设备能否跑某个模型/引擎"的通用准入判断。
- 现状阻力：采集项与 QLH 节点语义耦合（角色、集群档案等），抽离前需先做"采集层 / 判定层"分离。
- 优先级：**低**（用户 2026-09-12 取舍）。具备实施条件时再转票。

---

## 5. 明确不纳入（含理由）

| 候选 | 位置 | 不纳入理由 |
| --- | --- | --- |
| 抗弱网与 Transport v2 | `src/cluster_transport.py`(815) | 技术价值最高，但团队成员均为**软件工程专业**，不选网络协议方向做毕设（2026-09-12 决定） |
| 图算法编排 | `src/graph_orchestrator.py`(707) | 未入选（用户取舍） |
| 冻结契约与迁移映射 | `schemas/`（8 份 JSON Schema + migration-map） | 未入选 |
| 赛博哥特设计系统 | `frontend_cybergothic/`（主题组件 + 对比度门 + Playwright 回归） | 未入选 |
| 控制面微服务 | `control/`、`gateway/`（NestJS + Fastify） | 与 QLH 控制面语义绑死，抽离收益低于维护成本 |
| 主业务面 | `src/local_store.py`、`src/api_server.py` 等 | 它们就是"主项目"本身，抽走即掏空主仓 |
| TUI 聊天页单独成库 | `src/tui_chat.py`/`tui_sse.py` | 并入 `UNI-A4` 一起交付，不单独设项 |

---

## 6. 优先级与建议执行序

近 4 周建议顺序（由易到难、由小到大）：

1. `UNI-A1` 进程归属与重置（最小、最通用，1–2 天可完成内部整理）
2. `UNI-A2` 演示证据链工具（把"演示方法论"固化为工具）
3. `UNI-B1` RAG 工具包（地基最厚：六张票完成 + 双端实现，先建内部独立包）
4. `UNI-A4` 零依赖 TUI 面板（需先抽 API 契约层）
5. `UNI-A3` 签名更新链路（体量最大，适合作为毕设主线，建议放到有一位同学专责时启动）
6. 其余按需：`UNI-A5`、`UNI-B2`~`UNI-B7`、`UNI-C1`（venv 治理可与任何阶段并行）

毕设一人一题映射建议：

| 题目方向 | 主候选 | 体量 | 交付物形态 |
| --- | --- | --- | --- |
| 签名与安全更新 | `UNI-A3` | 大 | 独立仓库 + 签名/回滚验收门 + 演示更新链路 |
| RAG 工程化 | `UNI-B1` | 大 | 独立包 + hit@5/MRR 门 + 双端对照报告 |
| 上下文工程 | `UNI-B2` | 中 | 独立包 + 压缩策略对照（预算/降级曲线） |
| 评测口径与账本 | `UNI-B3` | 中 | 独立包 + 可复现判题口径 + 账本审计 |
| 终端工具链 | `UNI-A4` | 中 | 独立包 + API 契约 + 跨平台 TUI 录屏 |
| 工程效能（环境治理） | `UNI-C1` | 小-中 | 独立包 + 漂移检测门 |
| 进程与资源可靠性 | `UNI-A1` + `UNI-A2` | 小-中 | 两个小工具合并成一个"可复现演示与清理"题目 |

---

## 7. 团队分工与落地约定

1. **先内部独立包，后独立仓库**：`packages/<name>/`（自带 `pyproject.toml` + `tests/` + README）→ 验收门跑通 → 再建远端仓库并换 submodule。这样主仓不被搅动，也避免整包独立的契约漂移。
2. **搬代码必须搬门**：抽离时把验收门、脱敏契约（不落正文/密钥/绝对路径）、fail-closed 语义一并带走；只搬代码不搬门，子项目讲不出"可证明能用"。
3. **每个子项目自带四件套**：README（含用法与边界）、验收门命令、离线可跑的测试、脱敏示例输出。
4. **不复制源码回主仓**：主仓只保留 gitlink 与接线文档（沿用 `docagent`、`reasonix-codex-bridge` 的做法）。
5. **一个候选一张票**：本文只提供候选池与排序；正式开工时在开发票文档登记，附依赖与验收门。

---

## 8. 与既有结论的关系

| 既有结论 | 出处 | 本文的关系 |
| --- | --- | --- |
| `docagent` 已独立为子项目 | [子项目独立化评估](项目后续可做方向发散与子项目独立化评估-2026-09-09.md) §4 | 已完成，本文不重复 |
| `reasonix-codex-bridge` 已注册为子模块 | 同上（2026-09-12 后续） | 已完成，本文不重复 |
| harness 整体"大创结题后再独立" | 同上 §4.1 | 本文 §3 是其**前置步骤**（先内部独立包） |
| MODEL-TOOLS 小工具（`scripts/model_tools.py` 系列）已实施 | [模型通用小工具调研与方案](模型通用小工具调研与方案.md) | 已落地，本文不重复；其"统一入口 + 子命令"形态可作为抽包模板 |
| 答辩工具链（DEF-P1~P4、A1~A4）已完成 | [开发票计划](开发票计划-审计收口与答辩演示-2026-09-09.md) | 本文 `UNI-A2` 是把它提炼为通用工具 |

---

## 9. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-12 | 首版：A 档 5 项（A1 进程归属/重置、A2 演示证据链、A3 签名更新、A4 零依赖 TUI、A5 多机同步与补丁分发）、B 档 7 项（harness 内部包，RAG 优先）、C 档 1 项入选（venv 环境治理）+ 1 项低优先观察（异构设备探测）；抗弱网（软工专业不选网络方向）、图算法、`schemas/`、前端设计系统、`control/`+`gateway/` 与主业务面明确不纳入 |
| 2026-09-12 | **A 档细化**：新增 §2 概览表（规模/依赖/工作量/毕设适配）与 §2.6 横向对比；`UNI-A1`~`UNI-A5` 各补五段——现状（实测）、四条件对照、抽离形态、前置与工作量、风险。新增实测事实：`tui_admin` HTTP 调用集中于单一客户端类（`src/tui_admin.py:253-296`）、packaging 相关测试 13 个文件、patch/ssh_sync 无 paramiko、签名更新链路合计 4035 行 |
