# QLH 主仓拆分迁移干跑报告（2026-09-15）

> 状态：**迁移基线已完成：四个外置仓库已提交并推送，主仓结构收口在本次提交完成**
>
> 目的：固化 QLH 主仓边界，把产品壳、发布、Android 和非核心工具迁移到独立 Git 仓库；不直接删除任何文件。

> 当前执行记录：四个目标目录已创建，Android、Release、Shell、Toolbox 的代码/测试/资源/文档/专用虚拟环境已完成物理 MOVE；主仓跟踪项出现 `D` 是源目录移出后的待提交索引差异，不代表使用了破坏性删除。
>
> 安全规则：所有高危移动先完成本报告中的源/目标/数量核对；所有删除语义改为移动到主仓 `_to_delete/`。主仓已有用户改动不覆盖、不回滚。

## 1. 主仓保留边界

| 范围 | 保留内容 | 原因 |
| --- | --- | --- |
| 核心运行时 | `src/`、`schemas/`、核心 `requirements*.txt` | Engine L/D、Control、合同和 TUI 后端 |
| 核心测试证据 | `tests/` 中引擎/控制/协议/模型资产/边缘测试、`fixtures/` | 主线质量门和可复现实验 |
| 主线交互 | `src/tui_admin.py`、`start_tui.*` | 跨平台终端主入口；不依赖 Web/Android |
| 主线环境 | `.venv-edge`、`.venv-test`、D 档模型实验环境 | Edge L 和 D 档推理/回归开发 |
| 模型资产 | `models` 外部 junction、manifest/profile、模型下载合同 | 权重由用户持有；主仓只保留资产合同和入口 |

## 2. 独立仓库迁移计划

| 源 | 目标 | 迁移内容 | 动作 |
| --- | --- | --- | --- |
| `android/` | `G:\C\PYT\qlh-android` | Android 代码、JVM/instrumentation 测试、JNI、Gradle wrapper、资源、Android 文档；llama.cpp 重新固定为独立子模块 | MOVE + 外部 Git 仓库；主仓后续只保留 gitlink/合同 |
| `packaging/` + 发布测试 | `G:\C\PYT\qlh-release` | spec/launcher/installer、发布测试、requirements-lock 发布项、签名/离线包文档 | MOVE + 外部 Git 仓库；主仓后续只保留发布合同说明 |
| `frontend_cybergothic/` | `G:\C\PYT\qlh-shell` | Web/桌面壳代码、测试、public 资源、README、构建产物和 Node 依赖 | MOVE + 外部 Git 仓库；不进入主仓默认环境 |
| `tools/ssh_sync_*`、`tools/patch_*`、根目录 patch 脚本 | `G:\C\PYT\qlh-toolbox` | SSH、补丁分发、答辩/网络/环境工具及其测试/文档 | MOVE + 外部 Git 仓库；主仓不依赖 |
| `.venv-packaging*` | `G:\C\PYT\qlh-release\.venv-*` | 发布专用 CPU/CUDA 虚拟环境 | MOVE；不纳入 Git |
| `.venv-tui` | `G:\C\PYT\qlh-shell\.venv-tui` | Textual 可选聊天壳环境 | MOVE；标准库 `tui_admin` 不受影响 |

## 3. 迁移前数量与风险核对

| 源 | 干跑文件数 | Git 状态 | 风险 |
| --- | ---: | --- | --- |
| `android/` | 13,245（含子模块工作树） | 主仓普通目录；迁移后由 qlh-android 固定 llama.cpp 子模块，QLH epsilon 补丁由主仓工具补丁管理 | 高：先完成外置仓库提交和 clean clone 复核 |
| `packaging/` | 8,815 | 主仓跟踪 63 项 | 高：spec 依赖主仓 `src/` 和外部壳，迁移后只作发布仓工具 |
| `frontend_cybergothic/` | 7,269 | 被 `.gitignore` 忽略 | 中：含 `node_modules/build/dist`，全部移动，不删除 |
| `.venv-packaging*` | 74,605 | 被忽略 | 高：约 5.1 GiB，先确认目标不存在和剩余空间 |
| `.venv-tui` | 2,900 | 被忽略 | 中：可选环境，主线标准库 TUI 不依赖它 |
| `node_modules/`、`build/`、`dist/`、日志缓存 | 生成物 | 被忽略或运行时目录 | 低：不迁入 Git；需要清理时只移动到 `_to_delete/` |

## 4. 执行顺序

1. 创建四个目标目录并初始化独立 Git 仓库；目标已存在或非空时立即停止。
2. 迁移 Android 源码，重新添加 `llama.cpp` 子模块，复现 QLH 的 `conversion/base.py` 补丁，运行 Android 静态/JVM 门后提交外部仓库。
3. 迁移发布、桌面壳和工具链代码/测试/资源/文档；各自提交外部仓库。
4. 主仓移除原始跟踪目录；物理 MOVE 完成后由主仓提交记录删除/重命名，不能用 `git rm --cached` 制造“文件还在本地但不受管理”的双重状态；任何未纳入外部仓库的内容先移动到 `_to_delete/migration/`。
5. 更新 `.gitmodules`、README、环境脚本、文档链接和测试选择器；默认主线只准备 Python/Edge/Test，Node 和发布环境需显式进入支线仓库。
6. 运行主线测试、外部仓库测试、文档链接检查和各仓库 clean clone 检查；任何失败不删除 `_to_delete/` 中的回滚材料。

## 5. 暂不移动

- `harness_workbench`、`tools/docagent`、`tools/reasonix-codex-bridge`、`packages/spawnledger` 已是独立子模块，本票只复核 gitlink，不重复迁移。
- `.venv-edge`、`.venv-test` 和主线 D 档模型实验环境留主仓；多模态模型按模型舰队按需选择，实际权重仍在外部模型目录。
- `src/tui_admin.py`、`start_tui.*` 留主仓；`tui_chat.py` 的 Textual 依赖随产品壳迁移，不成为 Edge 硬依赖。

## 6. 回滚条件

- 目标仓库初始化、外部提交或 clean clone 失败；
- Android 子模块 revision/工作区补丁无法复现；
- 主线导入了 Web/Android/发布路径，或 Edge/TUI 无法从干净主仓运行；
- 文档链接、测试选择器或模型合同出现无法解释的断裂。

本报告以迁移前安全基线为起点，并已追加源/目标 Git commit、测试结果和 `_to_delete/` 清单；本次主仓提交完成后，四仓物理迁移基线闭合。

## 7. 已执行记录（2026-09-15）

| 项目 | 结果 |
| --- | --- |
| Android MOVE | `G:\C\PYT\qlh-android`；代码、测试、Gradle、资源和专用构建输出已迁出；内部 llama.cpp 重新登记为 submodule，固定 `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`；QLH epsilon 补丁由主仓 `tools/model_tools/patches/` 管理 |
| Release MOVE | `G:\C\PYT\qlh-release`；`packaging/`、发布测试、发布锁文件和 `.venv-packaging*` 已迁出；spec、serve、launcher 已支持 `QLH_CORE_ROOT` / `QLH_SHELL_ROOT` / `QLH_ANDROID_ROOT` |
| Shell MOVE | `G:\C\PYT\qlh-shell`；`frontend_cybergothic/`、Shell 测试、API gap scanner、启动脚本和 `.venv-tui` 已迁出 |
| Toolbox MOVE | `G:\C\PYT\qlh-toolbox`；演示、性能、SSH/patch 工具、fixture、测试和文档已迁出 |
| 主仓新增合同 | `docs/主仓拆分后仓库边界与环境合同-2026-09-15.md`；主仓 `.gitmodules` 已移除旧 Android 嵌套声明 |
| 回滚保留区 | `_to_delete/migration/` 保留旧 Android llama.cpp 工作树、三次中断 clone/登记目录、根 `node_modules`、历史 `.deb` 及四仓 clean-clone 校验目录；未执行直接删除 |
| 远端 | 四个外置仓库已分别推送到 `https://github.com/SgfKrc/qlh-{android,shell,release,toolbox}.git`；远端 `main` 均从本轮迁移基线提交开始 |

## 8. 已完成的迁移闸门

1. 四个外置仓库已添加正确 `origin`，并形成迁移基线 commit：Android `9006a7e`、Shell `aafc94d`、Release `13a9625`（含后续测试归属修复）、Toolbox `a47350b`。
2. 主仓完整 Python 回归 `2686 passed, 19 skipped`；Shell `18 passed`；Release `215 passed, 2 skipped`；Toolbox `111 passed, 1 skipped`；Android Full/Lite JVM 测试、APK assemble 和 JNI/CMake 构建均通过；Shell `typecheck` 与生产构建通过。
3. 主仓活动文档链接检查通过：65 个文件、506 条相对链接全部存在；外置演示文档中的跨仓证据链接已改为 `external/qlh-core/...`。
4. 四个 commit 已完成敏感文件和构建产物检查后推送到对应 GitHub `main`；四个远端 clean clone 工作树均 clean，Android 递归子模块准确检出 `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`。

## 9. 待完成工作统计（2026-09-15）

迁移阻塞项：**0 项**。

主仓结构收口：本次提交包含 `.gitmodules`、README、路径适配、迁移文档和源目录移出产生的索引差异；用户已有的非迁移脏改动未覆盖、未回滚。

后置验收项：**3 项**，不阻塞本次物理迁移基线。

1. Windows WebView2、Linux `.deb` 首启/升级和签名发布的真实环境验收。
2. Android 真机 Full/Lite、模型下载和分布式 Worker 联调。
3. 主线 Edge L/D 分布式推理性能、设备矩阵和模型舰队的真实硬件验收。
