# QLH 跨框架层接力：实验数据汇总

> **状态：持续更新**（最后更新：2026-09-18）
> 配套文件：[`跨框架层接力-项目报告.md`](跨框架层接力-项目报告.md)（叙事与结论）
>
> **纪律（本项目明文规定）**
> 1. 只收**报告原文写过**的数字，**不做推算**；每条都给来源与测试条件。
> 2. 只有「**同进程、同条件复测**」的对比数字才构成对照；**跨报告 / 跨脚本的数字不构成对照**。
> 3. 不同条件的两个数字并列时必须标 **⚠️ 不可直接比较** 并说明差在哪。
> 4. `-r < 3`（重复次数不足）的效应量不得采信（跨运行漂移可达 12–17%）。
> 5. 生产准入：`admit_relay_xframe` **仍为 fail-closed 关闭**（所有 `CORE-RELAY-XFRAME-02` 报告均带此字段）。

---

## A. 起点基线（2026-09-16 前后）

| 指标 | 数字 | 条件 | 来源 |
|---|---:|---|---|
| llama.cpp GPU Q4_K_M | **74.40 t/s** | prompt 5 / gen 24 | `docs/引擎单序列与并发性能对比-2026-09-16.md §2` |
| llama.cpp GPU f16 | **51.77 t/s** | 同上 | 同上 |
| torch GPU f32+eager+**有** KV | 17.29 t/s | 同上 | 同上 |
| torch GPU f32+eager+**无** KV | 10.0 t/s | 同上 | 同上 |
| torch GPU f16+sdpa | **7.76 t/s**（当时反而更慢） | 同上 | 同上 |
| 并发扩展率（batch 1→32） | llama f16 **11.4×** / torch f16+sdpa **13.2×** | batch 1→32 | 同上 §4.3/§4.4 |
| `PIPELINE_MAX_CONCURRENT` | **1**（串行） | — | `src/config.py:396` |
| llama.cpp KV 复用 | **未启用**（`reset_kv_cache()` 为 no-op；`src/` 内 `cache_prompt|prompt_cache|n_cache_reuse` 零命中） | — | `src/llama_engine.py`、`docs/推理性能现状调研-2026-09-16.md §` |

---

## B. 层接力的核心实测

### B1. P0 切点扫描 v1（**CPU 上游**，后被 v2 修正）

来源：`local_docs/CORE-RELAY-XFRAME-02-sweep-2026-09-18.json`
条件：Qwen3.5-2B（24 层，`full_attention_interval=4`）；上游 torch **f32+eager, CPU**；下游 llama.cpp **f16 裁层 GGUF, CPU**；prompt=5、gen=64；**同进程双后端 + 双侧 KV 复用**。

| 上游层 N | 下游层 | 上游 ms/步 | 下游 ms/步 | 合计 ms/步 | 64-token 一致性 |
|---:|---:|---:|---:|---:|:--:|
| 0（不接力） | 24 | 1.937 | 184.833 | **186.77** | 一致 |
| 4（当时默认） | 20 | 42.685 | 171.594 | 214.279 | 一致 |
| 8 | 16 | 77.905 | 145.928 | 223.833 | 一致 |
| 12 | 12 | 120.821 | 103.794 | 224.615 | 一致 |
| 16 | 8 | 137.248 | 84.624 | 221.872 | 一致 |
| 20 | 4 | 182.895 | 66.808 | 249.703 | 一致 |

- 全部切点 `tokens_digest` 相同（`a0afef59b5588881`）⇒ **逐 token 一致**。
- ⚠️ v1 结论「不接力最快（186.77，比 N=4 快 12.9%）」**已被 v2 修正**（v1 的上游用了 CPU）。

### B2. P0 切点扫描 v2（**GPU 上游**，取代 v1 的结论）

来源：`local_docs/CORE-RELAY-XFRAME-02-p0-corrected-2026-09-18.json`
条件：同 B1，但上游改 **torch f16 + CUDA**，配 `--upstream-partial`（只加载 embed + layers.0..N-1）。

| 上游层 N | 下游层 | 上游 ms/步 | 下游 ms/步 | 合计 ms/步 | vs「不接力」(186.77) |
|---:|---:|---:|---:|---:|---:|
| 8 | 16 | 42.841 | 126.131 | 168.972 | 1.105× |
| 12 | 12 | 57.493 | 97.498 | 154.991 | 1.205× |
| 16 | 8 | 68.392 | 78.008 | 146.400 | 1.276× |
| **20（最优）** | 4 | 72.331 | 56.753 | **129.084** | **1.45×** |

- 拟合单位层成本：上游 **2.4842 ms/层**（截距 25.4851）、下游 **5.6906 ms/层** ⇒ **层尽量交给 GPU 上游**。
- 5.6× 之谜的归因：同脚本同口径 **cpu 34.9 ms / cuda 8.3 ms** ⇒ 由**设备**解释，不是实现差异。
- 约束：N 必须是 `full_attention_interval`（=4）的整数倍；N=2 报 `missing tensor 'blk.1.ssm_conv1d.weight'`。
- GPU 上游必须 `--upstream-partial + f16`（f32 整模 2B ≈ 8.7 GB 超 8 GB 显存）。

### B3. 权重占比（现固定 4 层切点为何不合理）

来源：`docs/同进程双后端接力实现与性能-2026-09-16.md §14.1`
条件：`models/qwen3-5-2b` **632 个张量**（单分片 4.55 GB, f16），`2274.1 M` 元素。

| 部分 | 元素 | 占比 |
|---|---:|---:|
| visual（多模态，文本推理不需要） | 331.4 M | 14.6% |
| embed_tokens | 508.6 M | 22.4% |
| **language L0-3** | 228.9 M | **10.1%** |
| **language L4-23** | 1144.4 M | **50.3%** |
| 其他（norm/rotary/mtp） | 60.8 M | 2.7% |

⇒ 上游（embed+L0-3）= **32.4%（1.47 GB）**；下游（L4-23）= **50.3%（2.29 GB）**。

### B4. 上游「不加载整模」的可行性

来源：`docs/同进程双后端接力实现与性能-2026-09-16.md §14.2/§14.5`
条件：`--upstream-partial`，f16。

| 项 | 数字 |
|---|---|
| 上游（embed + L0-3）体积 | **1.47 GB**（f16）/ 2.95 GB（f32）；张量数 54；`missing=1(norm.weight)` |
| 数值 | **bit-exact**（`max|diff| = 0`） |
| 内存（整模 → 层流水线） | 参数 1881.8 M → **737.4 M**（2.55×）；加载 **5.68 s → 0.14 s（41×）**；RSS **7.87 → 2.56 GB（3.1×）**；CUDA **7.53 → 1.475 GB（5.1×）** |

### B5. 同机双后端交叠（P2）

来源：`local_docs/CORE-RELAY-XFRAME-02-p2-2026-09-18.json`
条件：同机、同模型 Qwen3.5-2B f16；上游 torch GPU N=20；下游 llama.cpp CPU（`cut-k20`）；gen=64、**2 条序列**。

| 模式 | wall_s | ms/token | 吞吐 |
|---|---:|---:|---:|
| serial | 12.942 | 101.113 | 154.531 |
| **threaded（交叠）** | 10.023 | **78.308** | **199.532** |

- 加速 **1.291×**；理论天花板约 **1.79×**（取上游 72.3 / 下游 56.8 之较大者），实测达 **约 72%**。
- 两条序列 token 与单序列基线**完全一致**。

### B6. 上游/下游争用（真接力 2.8× 偏差）

来源：`local_docs/CORE-RELAY-XFRAME-02-real-relay-2026-09-18.json`、`...-real-relay-v2-...json`、`...-upstream-profile-...json`
条件：Qwen2.5-0.5B（12 层）；上游 `load_layer_range`+`forward_layers`（CUDA, f16）；下游 llama.cpp 裁层 GGUF（CPU）；gen=32；**同一进程、同一模型、同一 prompt、同一设备**下依次测量。

| 变体 | forward ms/步 | forward ms/层 | 下游 ms/步 |
|---|---:|---:|---:|
| A forward-only | 12.089 | 1.0075 | 0.0 |
| B +convert | 12.277 | 1.0231 | 0.0 |
| C +downstream（真接力） | 30.003 | 2.5003 | 28.656 |
| D +whole-model | 30.798 | 2.5665 | 24.856 |

| 差分（ms/步） | 值 |
|---|---:|
| B−A（张量转换） | +0.188 |
| **C−B（下游 decode 期间对上游的干扰）** | **+17.726** |
| D−C（多驻留整模的内存压力） | +0.795 |
| **C/A** | **2.482×** |

- 归因：**下游 CPU decode 期间的上游干扰**（推测内存/总线带宽争用，**未用计数器验证**）。
- 撤回项：v1 的「`forward_layers()` 手动逐层比整段前向慢约 3×」为**跨报告比较之误**；同进程实测只慢 **1.318×**（A 1.1109 vs B 0.8427 ms/层），且两路径 hidden `max|diff| ≈ 44.365` ⇒ **性能可比、数值不可比**。
- 真接力整体（P11，gen=32）：上游 37.899 ms/步、下游 17.846 ms/步、合计 **55.745 ms/步**；token 与纯 llama.cpp 整模**逐 token 一致**。

### B7. 与「不接力」的对照（口径必须写明）

来源：`local_docs/CORE-RELAY-XFRAME-02-p2-2026-09-18.json`
条件：同机、同模型 Qwen3.5-2B f16；llama.cpp 侧 `build-cuda`、`-ngl 24`、t=8。

| 配置 | ms/token | 相对 |
|---|---:|---:|
| **llama.cpp + CUDA**（`-ngl 24`） | **26.6**（37.54 tok/s） | 1.0× |
| 跨框架接力最优（GPU 上游 N=20 + CPU 下游） | 129.084 | **4.85× 慢** |
| 跨框架接力 threaded 交叠 | 78.308 | 约 **2.94× 慢** |

`-ngl` 曲线（ms/token, build-cuda）：`0→91.1`、`4→71.0`、`8→60.5`、`12→52.9`、`16→44.2`、`20→34.7`、`24→26.6`。
⚠️ 报告警示：**build-cuda 即使 `-ngl 0` 也比 build-cpu 快约 2×**（91.1 vs 194.2）⇒ **跨构建比较不可靠**。

### B8. B13：hybrid（Qwen3.5）的层流水线（**已完成**）

来源：`local_docs/CORE-RELAY-XFRAME-02-b13-hybrid-pipeline-2026-09-18.json`
条件：**同进程**；Qwen3.5-2B（hybrid，24 层）；**split=12**（上游 0-11 含 embed、下游 12-23 含末节点口径）；
prefill=16、gen=32、**repeats=3**；CUDA f16；**compile 关闭**（比语义不比性能）。

| 项 | 数字 |
|---|---:|
| 上游 0-11 ms/步 | **25.251 ± 0.232** |
| 下游 12-23 ms/步 | **23.960 ± 0.602** |
| 合计 ms/步 | **49.211** |
| **逐 token argmax vs 单段 0-24** | **完全一致**（33 个 token，首个差异 = None） |

- **结论：段间不需要传任何 cache。** 契约本就如此 —— `past_key_values` 的索引对应「**本节点本地层**」
  （`src/model_module.py:3039`）；`_kv_cache` 按 `task_id` 存「本节点层范围的 KV」（`src/scheduler.py:1090`）。
- hybrid 的 `linear_attention` 层不写 KV、用 conv/recurrent state，**只依赖本层输入序列** ⇒ 只要
  **prefill 阶段上游传完整序列的 hidden**，下游就能从 token 0 正确累积 ⇒ **不引入额外跨节点状态传输**。
- ⚠️ **对照口径**：单段 vs 两段是「同进程、同脚本、同输入条件」；单段口径 A3 已证与 transformers 整模 argmax 一致。
  ⚠️ **耗时只在两段侧测量**（8GB 卡放不下单段 4GB + 两段 4GB 同时驻留 ⇒ 单段跑完先释放），参照侧仅用于 token 对照。
- ⚠️ **副产品（主仓缺陷）**：tied 模型（Qwen3.5）**无法做末节点**（`has_lm_head=True` 抛
  `qwen2 分层权重不完整: lm_head.weight`，`src/model_module.py:1698`）⇒ 见 `TODO-B16`。本实验因此把下游
  也按 `has_lm_head=False` 加载，再补 `norm` + **上游那份 tied embed 权重**的 matmul（同 A3 口径）。

---

## C. 引擎本体与编译

### C1. 主仓引擎 vs llama.cpp

| 指标 | 数字 | 条件 | 来源 |
|---|---:|---|---|
| `forward_layers` | **1.1109 ms/层** | Qwen2.5-0.5B, 12 层, gen=64 | `local_docs/CORE-RELAY-XFRAME-02-mainrepo-engine-2026-09-18.json` |
| llama.cpp CUDA | **1.064 ms/层** | 同上 | 同上 |
| 比值 | **1.044×**（差 4.4%） | 同上 | 同上 |
| 对 CPU llama.cpp | 0.32× | 同上 | 同上 |

### C2. torch 上游开关矩阵

来源：`local_docs/CORE-RELAY-XFRAME-02-torch-modes-2026-09-18.json`（Qwen3.5-2B, gen=64, cuda）

| 档 | ms/层 | 备注 |
|---|---:|---|
| f16+sdpa | **1.665** | = 主仓 CUDA 默认 |
| llama.cpp CUDA（对照） | 1.108 | 仅差 **1.5×** |
| f32→f16 | 只快 **8.2%** | |
| eager→sdpa | 快 **6.9%** | |

### C3. 编译「层循环」（A4 / B14）

| 实验 | 档 | ms/步 | std | ms/层 | 加速 | argmax |
|---|---|---:|---:|---:|---:|:--:|
| **A4**（非 hybrid） | A 逐层 | 10.269 | 1.155 | 0.8558 | 1.0× | — |
| | **C 层循环+compile** | **6.133** | 0.167 | 0.5111 | **1.674×** | 逐 token 一致 |
| **B14**（hybrid） | A 逐层 | 41.058 | 2.542 | 1.7107 | 1.0× | — |
| | **C 编译层循环** | **32.349** | 1.245 | 1.3479 | **1.269×** | 逐 token 一致 |

- A4 条件：**qwen2.5-0.5b, 12 层, prefill 64, gen 32, repeats=5**（`local_docs/...-a4-layer-loop-2026-09-18.json`）
- B14 条件：**qwen3-5-2b, 24 层, prefill 32, gen 32, repeats=3**（`local_docs/...-b14-hybrid-layer-loop-2026-09-18.json`）
- ⚠️ **不可直接比较**：模型、层数、prefill 都不同；hybrid 的 18 层是 `linear_attention`（GatedDeltaNet），可融合点比纯 attention+MLP 少，收益偏低属合理。
- ⚠️ **已被否决的 A4 v1**：直接编译**整段 `Qwen2Model`** 得 2.325×，但**语义错误**（多算一次 `self.norm`，argmax 从 decode 第 1 步分叉）⇒ **不得引用 2.325×**。

### C4. 整模 compile / cache 机制（含被推翻的结论）

来源：`local_docs/CORE-RELAY-XFRAME-02-b6-static-cache-...json`、`...-b8-...json`、`...-cache-mechanism-...json`、`...-compile-corrected-...json`、`...-compile-numerics-...json`

| 结论 | 数字 | 条件 |
|---|---:|---|
| 整模 compile（P9） | eager 1.0443 → **0.3258 ms/层**（3.2×） | 0.5B/12 层，CUDA f16 |
| cache-mechanism：compile(Dynamic) 四档 | **2.503 / 2.342 / 2.067 / 1.689×** | prefill 16/128/512/1024，repeats=5 |
| **Static vs Dynamic（compile 下）** | **0.75–1.0×（反而更慢且不稳）** | ⇒ **不上 static cache** |
| `recompile_limit` 8→64 | 额外 **+44%**（7.24 → 5.042 ms） | 0.5B/12 层, gen=141 |
| compile-corrected 非 hybrid | eager 12.958 → **1.79×**（limit=8）/ **2.57×**（limit=64） | gen=141 |
| compile-corrected hybrid | eager 40.808 → **1.57×**（limit=8） | Qwen3.5-2B, gen=64 |
| B10 长序列（0.5B/12 层） | 2.082 / 1.613 / 1.928 / 1.994× | prefill 64/256/512/1024 |
| B10 更大模型（7b, 28 层取 8 层） | **1.099 / 1.105 / 1.129×** | prefill 64/256/512 |
| **B10 复现（B11 本次，0.5B/12 层）** | **1.739 / 1.731 / 1.834 / 1.864×** | prefill 64/256/512/1024 ⇒ **与上行不复现**（256 不再是低谷） |

- ⚠️ **B10 的「256 档异常」经 B11 复核后两项作废**（报告 `local_docs/CORE-RELAY-XFRAME-02-b11-2026-09-18.json`）：① 「**只有** 256 分叉」不成立 —— B10 原脚本在同机复现得 256(位置 100) + **512(位置 105) 都分叉**，B11 的 inorder/isolate 亦同；② 「256 档 std 偏大 ±1.134」**未复现**（复现得 0.015，小约 75×），且本次复现里**分叉档 std 最小、未分叉的 1024 档反而更大** ⇒ std 与分叉**无因果**。**保留并加强的部分**：`USE_COMPILE`/层循环编译与 eager **数值不等价**，分叉位置**输入相关且确定性可复现**（100 / 105 在多处独立测量一致），同一档位的 compile 结果**还依赖编译上下文**（512 档 isolate 与 A 一致、inorder 则分叉）⇒ **不能靠"形状白名单"规避** ⇒ **凡要求「逐 token 一致」的场景一律不得开启该开关**。另注：B10 测于 09-18 16:39，当时 `_LayerLoop` 为 A4 版（16:35）；现行已被 B14 改写（19:51）⇒ 分叉档位集可变（**时间线旁证，未做因果对照**）。

- ⚠️ **B6 的两处结论已被 `cache-mechanism` 推翻**：① D（static）「最优 2.069×」是**异常值假象**；② B「比 A 慢 21%」**口径不干净**。
- **B5 跨机 vs 同机（Qwen3.5-2B / 上游主仓 4 层 + 下游裁层 20 层 / gen=32 / ssh -L）**：threads=4 跨机 **173.038** vs 同机 **209.818** ⇒ 跨机快 1.213×；threads=2 跨机 **221.664** vs 同机 **196.420** ⇒ 同机快 1.129×；各自最优跨机快 **1.135×**。**交叠**：同机 196.420 → **146.501（1.341×）**；跨机 221.664 → 219.402（1.01×）。⚠️ 主变量是「下游 `--threads` × 空闲核数」；且 N=4 时上游只占 ~1% ⇒ 交叠上限本就 ≈1.01（**不与 P2 的 101.11 ms/token 相比**：上游口径 f16+sdpa ≠ P2 的 f32+eager）。
- ⚠️ **B1「长序列 compile 劣化」已撤销**（脚本取错模块）。
- ⚠️ 「compile 下 hybrid 恒定慢 1.5×」→ 更正为 **快 1.57×**。

---

## D. 数值一致性

### D1. 跨框架接力的逐 token 一致性

| 场景 | 结果 | 来源 | 条件 |
|---|---|---|---|
| 跨框架接力 f32（长序列） | **141/141**；cosine min **0.999999537** / mean 0.999999830 / max 0.999999927 | `local_docs/CORE-RELAY-XFRAME-01-dl-fixed-prefill-141-2026-09-16.json` | 上游 pytorch, 下游 llama.cpp, seq=141, T=0, top_p=1 |
| 同进程 + KV + 手动 4L | **64/64**（`first_divergence_step=null`） | `...-manual4L-2026-09-16.json` | prompt 5 / gen 64 / threads 8 |
| 同进程 + 双侧 KV | **64/64** | `...-sameproc-kv-2026-09-16.json` | 同上 |
| compile 修复后 24 步 | argmax 完全一致 | `...-compile-fix-2026-09-18.json` | gen=24, 12 层 |
| 多种精度（汇总） | f16 **141/141**、f32 **141/141**、Q4_K_M 下游 **141/141**、E4(NF4+Q4_K_M) **141/141**、**E3(NF4+f16) 48/141**（首分歧 49） | `docs/跨框架接力量化与加速解耦分析-2026-09-16.md §2.2` | gen=141 |
| ⚠️ E7 多 prompt 复验（5 条异质 prompt × gen=64） | E3 全一致 **1/5**、E4 全一致 **3/5**；中文两边都崩（2/64） | 同上 §2.5 | ⇒ **单 prompt 结论不可泛化** |

### D2. hybrid（Qwen3.5）分段前向 vs transformers 整模

来源：`local_docs/CORE-RELAY-XFRAME-02-a3-qwen35-2026-09-18.json → verification.verdict`
条件：`load_layer_range(0,24)` 全 24 层 + `forward_layers`，f16；对照 transformers 整模 `Qwen3_5ForCausalLM`；prefill=16（脚本按末节点口径补 `transformer.norm` 后算 logits）。

| 指标 | mainrepo | transformers |
|---|---:|---:|
| **argmax** | 109705 | 109705 ⇒ **一致** |
| logits max\|diff\| | | **0.0043125152587890625** |
| logits mean\|diff\| | | **0.0003583759244065732** |
| logits_absmax | 16.55118751525879 | 16.546875 |

### D3. compile 非 bit-exact = f16 的 1 ULP

来源：`local_docs/CORE-RELAY-XFRAME-02-compile-numerics-2026-09-18.json`
条件：qwen2.5-0.5b, **12 层, gen=141, prefill=64, f16, sdpa**（A eager 11.37 ms/step）。

| 档 | ms/step | 相对 A | hidden max\|diff\| |
|---|---:|---:|---:|
| A eager | 11.37 | 1.0× | — |
| B compile(default) | 6.584 | 1.727× | **0.015625** |
| C +emulate_precision_casts | 6.121 | 1.858× | 0.015625 |
| D +deterministic | 6.68 | 1.702× | 0.015625 |
| E +no_epilogue_fusion | 5.736 | 1.982× | 0.015625 |
| F +no_split_reductions | 5.774 | 1.969× | 0.015625 |

- 结论：差异 = **f16 的 1 ULP**（`0.015625 = 2^-6`，恰为 f16 在 `|x| ∈ [16,32)` 的 ULP）；逐项排除后**唯一剩余来源 = attention 实现通路**（`fuse_attention` 把 bmm+softmax 融回 aten SDPA）。
- **表述纪律**：上游 issue 原话指出 compile 版本对照 **float64** 基线时 **rtol 更好** ⇒ **不得表述为「compile 更差」**；正确说法是「**与 eager 非逐位一致**」。
- ⚠️ 另一值 `0.078125` 来自 P9 **整模** compile（gen=64）⇒ **不同轮次/序列，不可混用**。

---

## E. 下游 llama.cpp 侧

| 项 | 数字 | 条件 | 来源 |
|---|---:|---|---|
| 下游 GPU offload | **2.18×** | 跨进程 + KV + 4L 接力，gen=64（⚠️ CUDA 版 `llama-cpp-python` **未构建**，同进程用不了） | `docs/跨框架接力性能优化-汇总报告-2026-09-16.md §2.1` |
| offload=0 → 99 | 93 s → 81 s（**仅 13%**） | 跨进程 + 4L + `--keep-kv`，64 步 | `docs/同进程双后端接力实现与性能-2026-09-16.md §13.3` |
| GPU offload（整模 llama-bench） | **3.15×**（v1）/ **3.267×**（v2） | 整模 `qwen35-2b-f16.gguf`，`-p 5 -n 64`，`-ngl 0 → 24` | `local_docs/...-downstream-opts*.json` |
| flash_attn / KV q8_0 / 分块 | 1.00× / **0.92×** / **0.74×**；v2 带 std：fa −1.7~−5.3%、KV q8_0 −1.1~−5.4% | 整模 llama-bench | 同上 |
| `--override-tensor` | `token_embd.weight=CPU` **−29%**、`output.weight=CPU` **−9%**；只前 4 层 GPU **19.59 < 全 CPU 22.50（慢 13%）** | `qwen35-2b-q4km.gguf`，`-p 5 -n 24` | `docs/同进程双后端接力实现与性能-2026-09-16.md §16.2` |

⚠️ **不可直接比较**：**2.18×（接力跨进程口径）** 与 **3.15×/3.267×（整模 llama-bench 口径）** 是不同场景。
⚠️ `downstream-opts-v2` 明确：**必须 `-r ≥ 3` 带 std**（`-r 1` 跨运行漂移 12–17%）。

---

## F. 优化总账（同进程、gen=64）

来源：`docs/同进程双后端接力实现与性能-2026-09-16.md`、`docs/跨框架接力性能优化-汇总报告-2026-09-16.md`

| 措施 | 收益 | 备注 |
|---|---:|---|
| 上游手动 4 层（减少无谓计算） | 上游 **10.7×**（407.6 → 38.1 ms/步） | bit-exact |
| 双侧 KV 复用 | **3.15×**（167 → 53 s） | |
| 消除进程边界（同进程） | **8%**（182 → 167 s） | |
| **累计最优**（同进程+KV+手动 4L） | **8.5×**（21.3 s / 64 步 ≈ **333 ms/步**） | ⚠️ 该口径为 **CPU 上游** |
| 对照 llama.cpp 原生整模 GPU | ≈ 0.9 s / ≈ **13.5 ms/步** | 当时差距「约 20×」 |
| 耗时剖析 | **99.3% 为两侧计算**（上游 66.5% + 下游 32.9%）；通信/同步/batch 合计 **0.34%** | 五段剖析 |
| 复用 `llama_batch` | **≈ 0**（甚至更慢 68 s > 53 s） | |
| 上游低精度（反直觉） | f16 **慢 8.4×**、bf16 **慢 8.7×**、NF4 **慢 1.09×** | f32 39.8 → f16 333.9 ms/步 |
| 上游固定开销 | 4 层占 ~97%，固定开销仅 **4.7%**；CUDA Graph 最多省 ~5% | ⇒ 「CUDA Graph 不值得做」 |

---

## G. 环境 / 容量 / 模型

| 项 | 数字 | 条件 | 来源 |
|---|---:|---|---|
| **L 档 venv（无 torch）** | **120 MB**（实测 0.57 s 冷启动） | `.venv-edge`；门槛 ≤300 MB | `local_docs/边缘基线框定-2026-09-14.md`、`local_docs/主仓架构核查报告-2026-09-16.md` |
| 全量 venv（D 档） | **5.1 GB**（其中 torch 一项 **4.3 GB = 84%**） | | `local_docs/边缘基线框定-2026-09-14.md` |
| Qwen3.5-2B | 1.94 B 参数；GGUF f16 **3.62 GiB** / Q4_K_M **1.21 GiB** | | `docs/引擎单序列与并发性能对比-2026-09-16.md §` |
| Qwen3.5-2B safetensors | 单分片 **4.55 GB**（f16）/ 632 张量 / 2274.1 M 元素 | | `docs/同进程双后端接力实现与性能-2026-09-16.md §14.1` |
| qwen2.5-0.5b f16 GGUF | 整模 **948 MB** / 裁层（K=12）**607 MB（64%）**；sha256 前缀 `B754A8FD7D483693` | | `local_docs/...-a1-cut-layers-2026-09-18.json` |
| qwen35-2b f16 | 整模 **3.63 GB** / `cut-k20` **1.50 GB（41%）** | | `local_docs/...-compile-default-relay-audit-2026-09-18.json` |
| torch 各档显存峰值（Qwen3.5-2B, 24 层, gen=64） | f16-sdpa **3.54 GB**；f32-sdpa **7.05 GB**；nf4-sdpa **1.68 GB**；int8-sdpa **2.27 GB** | cuda | `local_docs/...-torch-modes-2026-09-18.json` |
| PyTorch 量化（Qwen-1.8B, RTX, 50 token） | fp16 **3.47 GB / 53.2 tok/s**；int8 **2.30 GB / 9.8**；int4 **1.75 GB / 28.7** | ⚠️ int8 反而比 int4 慢 | `docs/推理性能现状调研-2026-09-16.md §2.6` |
| 量化 2bit/4bit | **实装为 Candidate（调研完成未实施）** | | `local_docs/主线四维审计报告-2026-09-14.md` |

---

## H. 口径冲突 / 存疑（引用前必读）

1. **「慢约 20×」vs「4.85×」** —— 参照物与接力最优口径**都不同**：`~20×` 是 **CPU 上游**（333 ms/步）对照 llama.cpp 整模 GPU（≈13.5 ms/步）；`4.85×` 是 **GPU 上游 N=20**（129.084）对照 `llama.cpp+CUDA 26.6`。**不可当同一量互相印证。**
2. **llama.cpp GPU 参照数本身有多个不一致值**：13.5 / 26.6 / 25.53 / 91.1 / 194.2 ms ⇒ **参照物必须同构建**（报告已自述）。
3. **上游 GPU 每层成本**：README 写「2.5–4.3 ms/层」，JSON 原始值为 3.617–5.355（含固定开销），拟合值 2.4842 ⇒ **引用以 JSON 原始字段为准**。
4. **compile 的 max\|diff\| 有两个值**：0.015625（compile-numerics, gen=141）vs 0.078125（P9 整模, gen=64）⇒ **不可混用**。
5. **A4（1.674×）与 B14（1.269×）不可直接比较**（模型/层数/prefill 均不同）。
6. **A4 的 2.325× 已废弃**（语义错误）。
7. **B6 的 static cache 结论已被推翻**（compile 下 Static 反而更慢 0.75–1.0× 且不稳）。
8. **「IPC 只占 8%」有分母依赖** —— 两侧都优化后 IPC 反成主导 ⇒ 引用必须注明当时两侧都在慢的状态。
9. **下游 offload 的 2.18× / 13% / 3.15%(3.267×) 跨场景**，不可互相替代。
10. **跨框架低比特一致性与 prompt 强相关** ⇒ 准入须按 prompt 分布统计。
11. **同一 llama-bench 命令跨运行漂移 ~1.13×**（10.98 vs 12.37 t/s）⇒ **凡 `-r < 3` 的效应量不得采信**。
12. **methodology caveat（报告原文）**：① 脱离端到端链路的孤立测量不可信（曾把上游单步测低约 5.6×）；② 任何对比数字必须**同进程、同条件**复测后才可写进结论；③ 测量脚本本身须先证明「它在测被测对象」。

---

## 待办（本文件将随实验推进更新）

- ~~B11：prefill=256 的异常档复核~~ ⇒ **已完成（2026-09-18）**：分叉位置**确定性可复现**（256→100、512→105），但 B10 的「**仅** 256 分叉」与「256 档 std ±1.134」**均不复现**（512 也分叉；复现 std 0.015）⇒ **不按「特定形状风险」登记**，应登记为「编译与 eager 数值不等价是普遍的、输入与上下文相关」。报告 `local_docs/CORE-RELAY-XFRAME-02-b11-2026-09-18.json`。
- ~~B11 续：分叉成因~~ ⇒ **已完成（2026-09-19）**：**机制 = 数值差累积 × argmax 平局** —— compile/eager 的末位 hidden 差从第 1 步就有（**0.011719**），随序列放大到分叉附近 **2.639648（225.245×）**；翻转步附近 top1−top2 间隔 **C = 0.0（完全平局）** ⇒ 差一旦超过该步 gap 就必然翻转 ⇒ **分叉位置由输入的 gap 分布决定**（故可复现、不可预测）。`torch._dynamo.reset()` 后结果**不变** ⇒ 否定「编译上下文」假说（但**512 档的跨进程差异仍未定位**）。报告 `local_docs/CORE-RELAY-XFRAME-02-b11-fork-cause-2026-09-19.json`。
- ~~B5：跨机对照~~ ⇒ **已完成（2026-09-19）**：Surface 续杯在线后跑通 ssh -L 跨机接力。**主变量是「下游 `--threads` × 空闲核数」，不是「同机 vs 跨机」**：threads=4 跨机快 **1.213×**、threads=2 同机快 **1.129×**、各自最优跨机快 **1.135×**。**交叠**：同机 **1.341×（有效）**、跨机 **1.01×（无效）** —— 收益来自「下游能否占住独立核」，且切点 N=4 时上游仅占 ~1% ⇒ 上限本就 ≈1.01（P2 的 1.291× 源于其上下游耗时相当）。**数值**：各档 32-token 贪心序列**全部相同**。报告 `local_docs/CORE-RELAY-XFRAME-02-b5-2026-09-19.json`。
- B15：Qwen-1.8B 在 transformers 5.x 下的**运行期**实测（目前只过了导入关）
- A7：主运行时 transformers 升级评估
- ~~**tied 模型的末节点**（`lm_head.weight` 缺失导致分层加载校验失败）需修或明确声明不支持~~ ⇒ **已修（B16，2026-09-18）**：新增 `_is_tied_word_embeddings()` 探测 + 用 `embed_tokens` 充作 `lm_head` 并重绑共享；新增 `tests/test_tied_word_embeddings.py`（7 passed）；全量 2775 passed / 1 failed（已知 flaky）。

---

## 图集

本文件的数字已绘成图 —— 由 `build/cross-framework-layer-poc/make_figures.py` **直接从** `local_docs/CORE-RELAY-XFRAME-02-*.json` 生成（不手绘、不推算；缺字段则跳过并报告），输出 `docs/figures/cross-frame-relay/`，**每张图的图注都带来源与测试条件**：

| 图 | 内容 |
| --- | --- |
| `fig1-p0-sweep.png` | P0 切点扫描（CPU 上游 v1 vs GPU 上游 v2；最优 N=20） |
| `fig2-ngl-curve.png` | `-ngl` 曲线（唯一大杠杆）与接力最优的对照 |
| `fig3-compile-gains.png` | A4 / B14 编译「层循环」收益（1.674× / 1.269×） |
| `fig4-weight-split.png` | 权重占比（现固定 4 层切点为何不合理） |
| `fig5-optimization-ladder.png` | 优化总账累进（182 s → 21.3 s） |
| `fig6-baseline-engines.png` | 起点：两套割裂引擎的性能档位差 |
| `fig7-contention.png` | 同机上下游争用（P12：C/A = 2.482×） |

全部图已纳入 [`跨框架层接力-项目报告.md`](跨框架层接力-项目报告.md) §6.4。
