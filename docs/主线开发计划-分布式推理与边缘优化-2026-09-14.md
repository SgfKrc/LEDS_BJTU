# QLH 主线开发计划：分布式推理与边缘优化（2026-09-16）

> 文档状态：**现行**
>
> 生命周期：**主线基线（Active）**
>
> 适用范围：QLH 主仓的引擎、控制面、TUI、模型资产契约、推理优化和验收基建。
>
> 本文是当前主线计划；历史票据仍保留编号和证据，但不再决定主仓边界。支线见支线开发计划：外置迁移与 Koakumix-2026-09-14。

> **现行进度覆盖（2026-09-16 晚）**：`CORE-RELAY-XFRAME-01` 已完成上游 #28963 调用方 `pos` 修复后的 D→L 141-token prefill、逐步生成和 loopback TCP 双进程证据，均严格 `141/141`。下一阶段是 SSH 隧道/跨机、f32、采样矩阵和故障清理；Relay 默认关闭，生产门保持 fail-closed。较早的失败数字保留在变更记录中作审计证据。

## 1. 主线裁决

QLH 主仓只解决一个问题：**在异构设备上把推理任务可靠地跑起来，并用可复现证据证明内存、时延、吞吐和故障边界**。

### 1.1 复核收口票（2026-09-20）

最近一轮大提交复核发现，主线功能基线已通过全量回归，但认证/来源边界、TOTP 审批、TUI 流式认证、RAG 外置残留和 llama-cpp-python 锁文件仍未完全收口。修复票统一登记在[已知问题记录](已知问题记录.md)的“2026-09-20 安全与迁移复核修复票”中，当前优先顺序为：

1. `SEC-AUTH-BOUNDARY-01`：统一 `/api` 来源边界，并在强制认证模式保护推理/控制面；保留健康、登录、bootstrap 和节点内部协议例外。**已完成（2026-09-20）**。
2. `SEC-AUTH-SESSION-01`：密码/禁用/角色变更吊销旧会话。
3. `SEC-AUTH-JOIN-01`：真实 Auth App/TOTP 接线，移除布尔审批接缝。
4. `SEC-TOTP-RATE-01`：按账户隔离 TOTP 失败与 replay 状态。
5. `SEC-TUI-SSE-AUTH-01`：补齐 TUI SSE Bearer/source 透传。
6. `MIG-RAG-HARNESS-01`：完成 RAG 外置后的 harness/docagent 迁移收口。
7. `DEP-LLAMA-LOCK-01`：统一 llama-cpp-python 主环境、Edge、测试和 packaging 锁定。
8. `SEC-RELAY-ERROR-01`：Relay 错误对端脱敏，详细异常仅写本地日志。

这些票是安全与工程收口，不改变 llama.cpp 分片/RPC 主线、Edge <=1B 本地推理和 TUI 主入口基线；第 1 票已完成，后续票继续按上述顺序推进。

主仓由五个相互约束的部分组成。**引擎层统一命名为 Koakuma（混合推理引擎），内部承载两套 backend**：llama.cpp/GGUF 与 PyTorch/Safetensors；**有 CUDA 的节点保留 torch 能力并默认开启（显式开关，可关闭并退化为 llama.cpp）**，**无 CUDA 节点（含边缘）去 torch 化为硬约束**（2026-09-16 v7 用户裁决）：

| 部分 | 责任 | 是否交付能力 |
| --- | --- | --- |
| **Koakuma · llama.cpp backend（Engine L）** | GGUF/llama.cpp 单机推理、模型分片/RPC、CPU/弱 GPU 路径、KV/缓存和边缘入口 | 是；PC 与边缘版均为生产目标；**无 CUDA 节点的唯一路径** |
| Relay R | hidden-state 层接力兼容合同与验证：先覆盖 llama.cpp→llama.cpp，再验证 PyTorch→llama.cpp | 是架构攻关能力；当前 PC-only、默认关闭，不替代 L/RPC |
| **Koakuma · torch backend（Reference P → D 档）** | PyTorch/Safetensors 层流水线与张量传输 | **有 CUDA 节点：默认开启的加速 backend（可关闭并退化）**；同时继续作正确性/性能/容量对照与 D→L Relay 来源侧；**不进入无 CUDA 节点与边缘安装** |
| Control | 节点、能力画像、调度、租约、任务路由、集群状态和降级 | 是；控制面与数据面同仓但分层 |
| Contracts & evidence | 模型画像、manifest、协议、质量门、性能实验和回归测试 | 是；没有证据的能力不能进入“支持” |

本次基线重排只改变实验轨道和下一票顺序，不撤销 `CORE-LLAMA-PC-RPC-01` 已完成的自动分层、租约和故障治理工作；其性能准入仍按原票继续。Relay 先行是为了把已取得的 L→L 架构证据固化为主仓合同，不能替代 RPC 的部分驻留目标。

**主项目交互入口是 TUI。** 当前统一入口是 `qlh`（无参）/ `qlh chat`（Textual 外壳，9 个功能屏 + 1 个调试兜底屏）；Edge/排障基线与 CI 用**只读薄层** `python src/tui_commands.py <command>`（纯标准库、无 UI 依赖、不启动后端）。`qlh_edge` 提供极简 HTTP 供远程任务和外部壳调用；Web、Android 和图形启动器不是主仓的交付入口。

主仓不再新增下列能力：图像生成/编辑、通用知识库和长期记忆、Web 产品界面、Android 产品界面、安装器/更新器、答辩演示、联网 Fetch、邮件通知和面向用户的运营工作台。这些能力的归属与迁移顺序见支线计划。

## 2. Koakuma 引擎、模型舰队与参考引擎

### 2.1 运行基线

| 角色 | 引擎 | 模型格式 | 目标设备 | 依赖边界 |
| --- | --- | --- | --- | --- |
| **主线 L（llama.cpp backend）** | llama.cpp：本地使用 `llama-cpp-python`，分布式使用 `llama-server`/原生 `ggml-rpc-server` | GGUF | Edge、CPU、集显、普通 PC、Android、**任意无 CUDA 节点** | Edge 默认 <=1B 本地推理；Edge 同时可作 RPC worker 参与更大模型；**默认不装 torch（硬约束）** |
| **D 档（torch backend）** | PyTorch/Transformers | Safetensors 等原生权重 | **有 CUDA 的节点（PC/工作站/CUDA 服务器）** | **默认开启的加速 backend，可显式关闭并退化为 L 档**；同时用于正确性、性能和算法对照；**不进入无 CUDA 节点与边缘安装** |

主线的技术目标是让一个 GGUF 模型在单节点装不下时由 llama.cpp host 与 RPC backend 共同承载，每个节点只保有实际分配的模型数据/计算资源。Edge 节点不是 HTTP-only 客户端：它应在本地用 <=1B 模型独立完成推理，并在资源允许时承担更大模型的分片计算。Silica Cluster 提供 Android 侧实现参考；QLH 必须用实测证明“部分驻留、容量合并、输出正确、可恢复”，不能用整模型复制或任务图 Stage 并行替代。

模型舰队只登记**模型身份、格式、引擎、能力、资源预算、模板和工件摘要**。权重仍由用户选择、下载和持有。用户通过 TUI 的模型列表/画像/选择操作决定使用文本模型或可选多模态模型；QLH 不把某一个视觉模型写成所有设备的必选依赖。

没有本地模型不等于主程序不能启动：正常启动仍进入控制面、TUI 和模型下载工作区，用户可以先从模型舰队选择或下载模型；只有 `--check-only` 这类严格诊断模式才要求本地模型资产存在。无模型时推理请求应明确报告缺失并保持可下载/可选择状态，而不是阻塞进入主程序。

多模态是**可选模型能力**而不是主线产品形态：模型画像必须声明 `vision`、输入格式、上下文预算、内存预算和平台支持；设备没有对应资源时要明确降级到文本模型。主仓保留能证明分布式推理边界所需的多模态协议/运行时，但边缘最小安装不预装多模态大件。

### 2.2 分布式形态排序

| 形态 | 主线归属 | 适用范围 | 当前结论 |
| --- | --- | --- | --- |
| **① llama.cpp 模型分片/RPC** | L 档主线 | PC、Android、CPU/弱 GPU 节点 | **主攻形态**；目标是单机装不下时每节点只承载部分模型，Silica 式 Android worker 纳入攻关 |
| **② 层接力兼容（Relay R）** | L/R 的架构兼容轨道 | 当前仅 PC；同引擎已验证，跨引擎待验证 | 裁层 GGUF + hidden/`embd` 接力已经完成同机单进程 16/16 行为等价；不进入 Edge 默认路由，不替代 RPC |
| **③ Koakuma torch backend（原 Reference P）** | **有 CUDA 节点的默认加速 backend**，兼容对照与 Relay D→L 来源 | **有 CUDA 的节点**（PC/工作站/CUDA 服务器）；**不适用无 CUDA 节点与 Edge** | **默认开启，可显式关闭并退化为 L 档**；对照用途保留 |
| **④ 任务图/整请求分发** | 兼容回退 | 任务级异构协作、临时无法分片的节点 | **迁就型临时方案**；可用于回退和对照，不再作为模型分布式主路线 |

llama.cpp 当前还提供两类与 GGUF 相关的能力，但要分开表述：

- 同一主机的 `--split-mode layer/row/tensor` 和 `--tensor-split` 是本地多设备拆分能力；`tensor` 仍按上游标注保守处理。
- `ggml-rpc-server` + `--rpc` 可以把 GGUF 权重/KV 按设备内存比例放到远端设备，**但上游仍标为 proof-of-concept、fragile、insecure**。QLH 将其作为主线攻关对象，但在准入前必须保持显式开关、隔离网络、模型校验和可回退。
- 外部 [Silica Cluster](https://github.com/ShintoChakkiath/Silica-Cluster-Decentralized-Mobile-AI) 证明 Android 可以承载 `libllama.so`/`librpc.so` 并接入 `--rpc`；其源码中的 `-ngl`/`--tensor-split` 是 offload/比例 split，不是 QLH 的连续 `layer_range` 协议。QLH 的创新任务是把它升级为可校验的模型分片合同，并先在 PC 单机双进程完成证据闭环。

旧的 PyTorch `LAYER_FORWARD` 协议不直接接收 GGUF，也不因 Relay 试验而扩大为通用张量协议。Relay 使用独立的 hidden handoff 合同：当前已证明 L→L 的裁层模型接力行为等价；D→L（PyTorch→llama.cpp）同机文件交接仅 5-token prefill 通过，141-token f16/f32 均出现错误 argmax，尚未通过。**XFRAME 首轮（2026-09-16）已把根因收敛到 llama.cpp 长批 `embd` 输入图/续算路径**（控制组：跨框架 hidden cosine 1.0、llama.cpp 自身 hidden 注入仍 130/141、完整模型改收 token embedding 仍 132/141 且首分歧提前到 5；源码依据 `build_inp_embd()` 在 embedding 模式下仍构造未写入的 token 分支）。该轨道 PC-only、显式开启，不进入 Edge 默认路径或替代 llama.cpp RPC。**无 CUDA 节点不得依赖 torch**。

Qwen 在 Android 的主路线是 llama.cpp/GGUF；ExecuTorch 保留为独立研究对照，不进入主线发行或默认运行时。多模态仍由模型舰队按设备能力按需选择，不把视觉模型做成边缘必选依赖。

## 3. 边缘版硬基线

边缘版是主线的硬交付约束，不是删减后的宣传名：

| 项目 | 基线 |
| --- | --- |
| 运行档位 | llama.cpp/GGUF；Edge 默认 <=1B 本地推理，CPU 路径永远可用；RPC 分片是 Edge/PC 的正式协作能力 |
| 交互 | Textual 外壳 `qlh`（统一入口，9 个功能屏 + 1 个调试兜底屏）；Edge/CI 用只读薄层 `python src/tui_commands.py <command>`（纯标准库）；最小 HTTP 仅用于状态、健康、推理和任务协议 |
| 依赖 | `llama-cpp-python`/llama.cpp、FastAPI、Uvicorn、psutil、httpx 等最小集合；**边缘/无 CUDA 发行禁止 torch**（有 CUDA 节点由 Koakuma torch backend 提供加速，不适用本条） |
| 环境体积 | venv 目标不超过 300 MB；当前开发测量约 120 MB |
| 冷启动 | 健康检查目标不超过 15 秒；当前开发测量约 0.57 秒 |
| 资源 | 默认选择 <=1B 和 Q4；更大模型通过集群分片按节点能力装载；能力画像必须在加载前检查 RAM、上下文和工件摘要 |
| 可选能力 | 多模态随模型舰队按需选择；没有对应模型或预算时保持 <=1B 文本本地路径可用；节点故障时允许调度绕行到其他分片、完整模型节点或本地小模型；任务图仅作临时回退 |
| 明确排除 | Web/Android UI、图像生成、RAG、邮件/审批、安装器、演示、联网工具和重型实验依赖 |

边缘版可以作为单机产品运行，也可以作为任务池节点加入集群；“边缘”不等于只能被远程调用。主仓必须持续保留一条不依赖 CUDA、torch 或产品壳的可运行路径。

## 4. 角色与依赖纪律

```text
TUI / qlh_edge
        |
        v
Control  ------ task / model / capability contracts ------>  Koakuma · llama.cpp backend
        |                                                    (无 CUDA 节点与 Edge)
        |                                                    Koakuma · torch backend
        |                                                    (有 CUDA 节点，默认开启)
        +------------- shard / lease / fallback -------------+

Reference/对照用途(有 CUDA 节点内) <------ comparison/evidence ------ Control

External shell / Koakumix / Patchouli
        \\------------ HTTP + schemas only ------------> QLH
```

- Control 可以调用 Koakuma；**引擎层不得 import Control**；**调度层不关心节点用哪个 backend**（只依据能力表与合同）。
- TUI 只依赖稳定的 HTTP/协议和模型能力摘要，不读取内部路径、凭据或实验临时文件。
- 产品壳、Koakumix 和 Patchouli 只能通过协议、manifest 或模型工件边界接入，不复制主仓推理实现。
- 实验和质量门中与推理优化直接相关的代码留主仓；演示、报告排版、网络探针和环境编排外置。

## 5. 主线开发票

| 顺序 | 票号 | 交付 | 依赖 | 状态 |
| ---: | --- | --- | --- | --- |
| 1 | `CORE-BASELINE-01` | 固定 TUI 主入口、模型舰队和边缘硬基线；同步 README/总计划 | 本文 | **已完成（2026-09-15）** |
| 2 | `CORE-TUI-01` | `qlh chat` 默认入口、模型舰队列表/选择、能力与当前档位展示 | `CORE-BASELINE-01` | **已完成（2026-09-15）** |
| 3 | `CORE-EDGE-01` | 边缘安装清单、无 torch 导入门、`qlh_edge` 最小路由和 CPU 回归 | `CORE-TUI-01` | **已完成（2026-09-15）** |
| 4 | `CORE-MF-01` | 统一模型画像：格式/引擎/模板/thinking/vision/资源预算/sha256；新模型按画像准入 | `CORE-BASELINE-01` | **已完成（2026-09-15）** |
| 5 | `CORE-CUTOVER-01` | 功能裁切/重写断点与子模块低耦合门：清理旧前端挂载、邮件、生图和旧微服务启动依赖；接通 TUI/API/模型合同；确认外置仓只通过稳定协议接入 | `CORE-MF-01` | **已完成（2026-09-15）** |
| 6 | `CORE-LLAMA-SINGLE-01` | Edge/PC 共用 llama.cpp 单机基线：默认 <=1B GGUF、模板/thinking、流式、多模态按模型舰队选配、TUI/API 与无 torch 导入门；记录模型/内存/质量/吞吐基线 | `CORE-CUTOVER-01` | **已完成（2026-09-15）** |
| 7 | `CORE-LLAMA-SIM-01` | **Edge/PC 同机双进程模型分片模拟**：1 个 llama host + 1 个 `ggml-rpc-server` worker；证明部分驻留、受控容量合并、输出对拍、断开回退和无 torch；不把受控 budget 冒充物理 OOM | `CORE-LLAMA-SINGLE-01` | **已完成（2026-09-15）** |
| 8 | `CORE-RELAY-01` | Relay 兼容合同与 L→L 主仓接入：裁层 GGUF、层号/模型身份、hidden shape/dtype、`embd` 注入、逐 token argmax 对拍、失败回退；不改旧 `LAYER_FORWARD` | `CORE-LLAMA-SINGLE-01`；实验 §7.8–§7.11 | **已完成（2026-09-16；合同/规划器/探针/回退门通过，生产接线后置）** |
| 9 | `CORE-RELAY-XFRAME-01` | PC-only D→L（PyTorch→llama.cpp）同机变体，再做跨进程/网络 handoff、长序列、f32/采样矩阵；分开记录行为等价与 bitwise 一致。**根因已定案**：上游 #28963 的调用方 `pos` 越界读（M-RoPE + `embd` 批次）。**修复顺序**：① 调用方 `pos` 按 `n_pos_per_embd*n_tokens` 提供 → ② 重跑完整模型 token/embedding 141-token 控制组 → ③ 裁层 D→L → ④ 实时双进程/网络/采样 | `CORE-RELAY-01`；`CORE-PYTORCH-COMPARE-01` | **进行中（2026-09-16；根因定案并独立验证；修复后 D→L **五条证据全通过**：prefill `141/141`（严格 argmax、cosine_min 0.9999995）、逐步生成 `141 步一致`、采样矩阵（`T=0` 确定；`T>0` 须"单侧 RNG"，共享 seed 非充分）、f32 权重对照 `141/141`、**跨机 SSH 隧道逐步生成 `141 步一致`**（A=本机 PyTorch，B=Surface 从节点裁层 llama.cpp）。报告见 `local_docs/CORE-RELAY-XFRAME-01-dl-*.json`。**★ 2026-09-19 收口**：准入所需的**四项证据已补齐**（prompt 分布 8/8、长序列至 547 token、弱网断线 fail-closed、协议/版本一致性指纹）；准入判定**仍为拒绝**，但理由已从「缺证据」精确化为「**正确性已验证、性能不具优势**」（合同新增 `RelayXFrameEvidence`）。剩余：长时运行、更大模型容量收益与远端资产分发自动化；**生产准入保持 fail-closed**） |
| 10 | `CORE-LLAMA-PC-RPC-01` | PC 真机 RPC：多节点模型分片、模型身份/摘要、画像驱动层分配、租约、epoch fencing、断线重分配、吞吐与内存收益；不接受整模复制冒充分片 | `CORE-LLAMA-SIM-01`；auth 侧依赖 `AUTH-TOTP-01/02` | **已完成（2026-09-16）** —— 真机链路全部通过：双机模型身份/摘要一致、画像驱动自动分层（`1/25`）、lease TTL/renew、epoch fencing 拒绝旧 lease、断线本地回退、部分驻留均验证；本轮新增**远端资产同步计划** `RemoteAssetSyncPlan`（非破坏性传输，`scripts/llama_pc_rpc.py` +248 行 / `tests/test_llama_pc_rpc.py` +56 行）。性能口径**如实记录、不宣称收益**（分布式请求 657ms vs CPU 回退 313ms）；长时运行与更大模型容量收益后置 |
| 10A | `CORE-TRANSPORT-PORT-01` | 控制面传输端口收口：`scheduler` 通过 `transport_port` 使用服务器、客户端、协议枚举和张量编解码；保留现有 TCP 替身与行为 | `CORE-LLAMA-PC-RPC-01` | **已完成（2026-09-16，本票首张）** —— `scheduler.py` 不再直接导入 `tcp_comm`、构造 `TCPServer` 或调用旧张量编解码器；动态适配器和边界回归通过。票 2-5 仍排队 |
| 10B | `CORE-KOAKUMA-ENGINE-01`（草案名 `CORE-KOAKUMA-ENGINE-ABC-01`） | Koakuma 引擎合同、backend 注册表、节点画像选择器和能力探测；上层不得读取 `_engine_type` | `CORE-TRANSPORT-PORT-01` | **已完成（2026-09-17）** —— CUDA 画像默认 PyTorch、无 CUDA 默认 llama.cpp、显式 llama.cpp 可退化；API/调度器/inference-service 统一消费公共 backend/能力接口，定向回归 `592 passed, 1 skipped`，动态配置修复后复核 `309 passed, 1 skipped` |
| 10C | `CORE-NO-TORCH-GUARD-01` | L-tier 无 torch 硬门：惰性分页 KV、默认 GGUF 配置不触发 torch、API/GGUF/TUI 隔离启动守卫 | `CORE-KOAKUMA-ENGINE-01` | **已完成（2026-09-17）** —— `paged_kv_cache` 移除顶层 torch 依赖；默认 `llama_cpp` 不做 torch 设备探测，显式 PyTorch/auto 保留探测；无 torch 阻断子进程完成 API 冷启动、假 GGUF 推理和 TUI 入口，真实安装 torch 的默认 GGUF 导入检查通过；定向 `7 + 22 + 5 passed` |
| 10D | `CORE-HIDDEN-CALLBACK-01` | 隐藏回调显式化：以类型化、不可变 bundle 注入调度依赖；禁止 host 私有属性回调 | `CORE-KOAKUMA-ENGINE-01` | **已完成（2026-09-17）** —— API composition root 以 `SchedulerCallbackSet` 原子装配 7 项依赖，`ModelHost`、`Scheduler` 和远程 `InferenceClient` 都走显式公开 bundle；静态边界无 `_ATTACHED_ATTRS` / `get_attachment` / `.attach(` 和 `self._host._`，乱序字段装配回归与本地/远程调度契约通过（`53 + 7 passed`） |
| 10E | `CORE-DATAPLANE-NO-WRITEBACK-01` | 禁止 TCP 数据面反射式改写控制面；以显式注册/事件替换 Windows re-exec 修复写回，并覆盖双 server 无串台 | `CORE-TRANSPORT-PORT-01`；`CORE-HIDDEN-CALLBACK-01` | **已完成（2026-09-17）** —— 删除 `callback_owner._tcp_server = self` 反射写回；消息、断连、注册确认三类事件在 `TCPServer.start()` 启动 I/O 线程前显式注册，调度器以线程局部 source context 区分并发 server。双 server 无串台、bound-method owner 不变、ACK 顺序及传输/调度/API/协议回归通过 |
| **10F** | **`CORE-LAYER-CONTRACT-01`** | **层段协议：统一节点抽象与顶层透明性**。把 `local` / `remote_rpc` / `cross_framework` 三类节点统一为 `PipelineNode(layer_range, engine, location, capacity)`；补「并集恰好覆盖」校验（不重叠/不空洞）与**聚合资源视图**（顶层只读）；取消「兜底」概念（**Silica 式 = 借算力节点**，与 Silica 调研一致：它只能借算力、不能容量合并）。设计见 [层段协议立项](层段协议立项-2026-09-17.md) + [可行性确认](层流水线节点类型与顶层透明性-可行性确认-2026-09-17.md)；既有基础已具备（`build_assignment_manifest` / `RelayTrimPlan` / `solve_pipeline_capacity` / `require_distributed` / `RpcShardLease`）| `CORE-LLAMA-PC-RPC-01`（✅ 已满足）| **已完成（2026-09-17）** —— 新增纯合同层 `pipeline_node_contract`，以不可变节点/容量/工件对象统一容量计划、assignment manifest、Relay handoff 与 RPC lease；严格校验半开层段的完整覆盖、边界组件归属、模型身份、架构与 GGUF block/MTP 几何。容量准入附带公开布局，TUI/API 提供不含地址的只读聚合资源视图；不改变现有执行器字典接口 |
| **10G** | **`CORE-RESHARD-01`** | **自动重分片**：节点失效后把其 `layer_range` **重新分配给幸存节点**（区别于现有「断线本地回退」——后者是把全部层收回本地）。**前置三项**：① 容量预检（幸存节点临时多装层，否则直接 OOM）；② 权重可得性（`RemoteAssetSyncPlan` 扩展）；③ epoch 原子切换（复用已有 fencing）| `CORE-LAYER-CONTRACT-01`；`CORE-LLAMA-PC-RPC-01` | **已完成开发门（2026-09-17）** —— 新增独立 `pipeline_reshard`：断线后只以在线幸存节点重解完整且连续的布局，容量不足或两节点以下时 fail-closed；层段、模型摘要、工件类型、Embedding/LM Head 归属均须由已验证资产覆盖。现有两阶段加载仅在同一容量 `plan_id` 与布局摘要一致、全节点 ready 后提交新 topology epoch，`RpcShardLeaseBook.check()` 拒绝旧 epoch。`RemoteAssetSyncPlan` 仅在实际同步成功后才可导出整 GGUF 工件证明；API/TUI 提供只读状态。开发回归 `287 + 164 passed`；真实 PC/Android 故障注入、同步和性能验收后置。 |
| 11 | `CORE-LLAMA-ANDROID-01` | Silica 式 Android `librpc.so`/`ggml-rpc-server` worker：交叉编译、真机接入、模型分片、能耗/热状态、断线与安全；Android UI 不回流主仓 | `CORE-LLAMA-PC-RPC-01`（✅ 已满足） | **P0 已完成（2026-09-16）；P1 开发门已补齐，设备门待 ARM64 环境** —— P0 交付：① 交叉编译 `arm64-v8a-16k`（16 KB page 对齐已固化进 `app/build.gradle.kts`）；② **JNI 打通** `libqlh_llama_jni.so`（+4 个 `nativeRpc*`，App 进程内独立线程跑 `ggml_backend_rpc_start_server`，**不 fork**；上游无优雅停止 API，故 `stop_supported=false` 如实上报）；③ 引擎/Stage 硬编码改能力探测（对齐 `CORE-KOAKUMA-ENGINE-ABC-01` 判据）；④ 嵌套 submodule 规范化为 `47e1de77`。本票新增 `scripts/android_validation.py`：JVM/build、x86_64 AVD 控制面、ARM64 AVD/QEMU/远程 adb 三层证据分离。**遗留（设备门 + 工程债）**：ARM64 环境运行 RPC worker、模型分片、能耗/热/断线与安全实测、`System.loadLibrary` 运行时路径与 APK 打包验证、嵌套 submodule 跨机 fetch 未根治（建议 `scripts/init-submodules.*` 显式传代理）；无在线 Android 设备的开发机只能记为 JVM/build 通过，不能记 P1 完成 |
| — | `CORE-LLAMA-ANDROID-01` **实测快照（2026-09-19）** | 本机实跑 `python scripts/android_validation.py`：**`jvm_unit_tests` PASS / `apk_arm64_jni` PASS / `adb_available` PASS，`online_device` FAIL（0 台）**，`arm64_abi=not_observed`，证据 `{"jvm_and_build": true, "apk_device_control_plane": false, "arm64_native_worker": false}`；前置 `java`(JDK 21)/`adb` 就绪，`gradle` 用 `gradlew`。**结论：卡点是「无 ARM64 设备」，不是「未推进」**——A 层（JVM 单测）已通过且 **B 层（`--assemble` 出 APK）无需真机即可推进**，尚未请求（`apk_build=not_requested`）；C 层需 x86_64 AVD、D 层需 ARM64 AVD/QEMU 镜像、E 层为真机硬门。分层判据见 [`android/Android验证替代路径-2026-09-18.md`](../android/Android验证替代路径-2026-09-18.md) |
| 12 | `CORE-PYTORCH-COMPARE-01`（原 `CORE-DIST-01`） | **Koakuma torch backend 的对照与收敛**：与 llama.cpp 在同模型/同提示下比较正确性、内存、吞吐和故障边界，并为 D→L Relay 提供来源侧证据；**作为有 CUDA 节点的默认加速 backend 进入生产路径（v7 裁决）**，同时保留对照用途 | `CORE-CUTOVER-01` | **首轮对照完成（2026-09-19）** —— 见 §7.1；`out/pytorch-vs-llama.json` |
| 13 | `CORE-TASK-FALLBACK-01`（原 `CORE-POOL-01`） | 保留任务图/整请求分发作为临时迁就回退：租约、取消、重连、完整模型身份；禁止扩展为模型分片替代物 | `CORE-CUTOVER-01` | 规划（低优先级） |
| 14 | `CORE-RELEASE-01` | 主仓最小发行包：TUI + **Koakuma**（按节点选 backend）+ control/contracts + 必要质量门；**无 CUDA/Edge 发行不得含 torch 依赖**，Relay 默认关闭 | `CORE-LLAMA-PC-RPC-01`；Relay 轨道不阻塞最小发行 | 规划 |
| 15 | `CORE-ET-EXP-01` | ExecuTorch 端侧实验：独立环境、真机对照、只产出研究证据；不进入主线发行 | 无（主线外实验） | 保留但不阻塞 |

每张主线票必须给出：模型工件、进程/节点拓扑、实际分片或回退形态、每节点驻留量、正确性、性能指标、失败边界、降级路径和可复现命令。没有这些字段，只能记为调研或 PoC。

## 6. 当前不做与止损

- 真机分布式之前必须依次通过：`CORE-CUTOVER-01` 的裁切/低耦合门、`CORE-LLAMA-SINGLE-01` 的 Edge/PC 单机基线、`CORE-LLAMA-SIM-01` 的同机 host + worker 双进程分片门；任何一门失败都停在当前阶段并保留 Edge <=1B 本地 llama.cpp 回退。
- **不把 llama.cpp RPC / Android RPC Worker 在双进程与 PC RPC 门未通过前接成默认生产路由**；未完成模型校验、身份、传输保护、回滚和 soak 证据时保持显式关闭。通过后允许进入 llama.cpp 生产路径，仍不允许整模复制冒充分片。
- **Relay 不得因已有 L→L 16/16 或 D→L 短样本就直接进入默认生产路由**；`CORE-RELAY-XFRAME-01` 的正确性**已打通**（上游 #28963 调用方 `pos` 修复后，f16 / Q4_K_M 下游 / 两侧低比特 E4 均 **141/141** 严格 argmax，跨机 SSH 隧道逐步生成亦一致），但**性能已收口为「不具备优势」**（最优 333 ms/步 vs llama.cpp 原生整模 GPU 13.5 ms/步，差约 20×；见 [性能汇总报告](跨框架接力性能优化-汇总报告-2026-09-16.md)）→ **Relay 的定位是「能力组合」而非「提速」，生产门保持 fail-closed**。Relay 与 RPC 是两条不同能力：前者验证架构兼容与跨引擎组合，后者解决模型部分驻留。
- 不把任务图/整请求分发写成“模型已分布式”；它只是单机装得下完整模型时的临时迁就回退，主线不再追加其 Stage 类型或整模 Worker 能力。
- 不在 llama.cpp 主线改造未完成前删除 PyTorch 代码；改造完成后 **torch 能力归入 Koakuma torch backend 并由有 CUDA 的节点默认启用**，**无 CUDA 节点与边缘发行仍不得依赖 torch**。
- 不把 ExecuTorch 作为第三引擎合入主仓；实验可以独立推进，但不改变 llama.cpp 主线和无 CUDA 节点的无 torch 边界。
- 不因产品壳尚未外置而扩充 Web/Android 功能；外壳只消费已冻结合同。
- 不为了让旧票“看起来完成”而恢复已裁撤的生图、微服务或旧前端路径。
- **主仓运行时不依赖被修改的 llama.cpp 上游**：`embd` 非确定性、长批 `embd` 输入图等只作上游 issue 素材收集；实验 fork 内的假设补丁仅用于定位（若验证有效再单独评估人审后提交上游）。

主线票据文档不再作为持续维护入口；邮件通知、SMTP/IMAP 配置和邮件投票已在本轮从主仓移除，审查工单仍通过 TUI/API 查询、投票和过期处理。

> **2026-09-18 / `CORE-LLAMA-ANDROID-01` 开发机替代验证**：由于当前开发机无 Android 真机，新增 `scripts/android_validation.py` 与 [Android 验证替代路径](../android/Android验证替代路径-2026-09-18.md)。脚本默认执行 `:app:testFullDebugUnitTest`，可选构建 `fullDebug`，并在存在 adb 设备时区分 x86_64 控制面、ARM64 ABI 和 native worker 证据。x86_64 模拟器不满足 `arm64-v8a` RPC worker 门；ARM64 AVD/QEMU 只补 ABI/JNI 兼容性，热/电、后台回收、弱网、长时和真实模型分片仍必须由真机或远程 ARM64 设备完成。无设备运行脚本时应如实记录为 `jvm/build-pass-device-pending`，不自动算票完成。

> **2026-09-16 / `CORE-LLAMA-PC-RPC-01` 增量**：`scripts/llama_pc_rpc.py` 新增远端 GGUF 资产同步合同：默认只读查询并输出 SHA256/大小校验计划；显式 `--apply-remote-model-sync` 才允许 `scp` 上传，远端以 `.part` 暂存、校验后替换，旧文件仅移动到 `_to_delete`。专项定向回归 `9 passed`。真实 1.1GB 传输、长时稳定性和更大模型容量收益仍需受控实跑，性能未达标前生产 RPC 继续 fail-closed。

## 7. 基线验收

`CORE-BASELINE-01` 的本机验收内容：

1. 本文、[定位与模块归属终局决策](QLH定位与模块归属终局决策-2026-09-14.md)、[异构分布式形态决策](QLH异构分布式形态决策-2026-09-14.md) 和 README 的边界一致。
2. 当前入口文档（README、架构/接口说明、计划和文档索引）不再把图像生成、旧前端或 Web 产品壳写成主项目当前能力；归档历史保留原始证据并明确标注。
3. 裁撤后的历史全量回归快照为 `3301 passed, 20 skipped`；本轮文档/环境脚本定向回归为 `38 passed`，均不替代后续真实硬件验收。
   ★ **2026-09-19 实测更新**：当前串行全量为 **`2934 passed / 13 skipped / 0 failed`**。与旧快照的差值来自此后**主动裁撤**（RAG 存储与 `/api/rag/*` 移除、`qwen-1_8b` 退役与下载预设移除）以及新增用例（认证、TUI 补齐、画像分档、跨框架接力），不是回归。
4. 迁移前文档链接检查曾通过；迁移后活动入口的断链已归入支线 `SIDE-DOC-01` 收口，不阻塞主线运行时代码。
   ★ **2026-09-19 实测更新**：`docs/` 活动入口（不含 `archive/`）**实际只剩 1 条断链**（`文档状态与清理清单.md` 里 `SD 1.5…` 用了 `%20` 编码，文件名实为空格），**已在本轮修正**；原先「77 条」的记录已过时（后续清理已收口，且 `../../qlh-android`、`../../qlh-release`、`../../qlh-shell`、`../../qlh-toolbox`、`../tools/docagent` 等外部链接的兄弟仓均真实存在，非断链）。

## 变更记录

- **2026-09-19**：**`CORE-RELAY-XFRAME-01` 收口 + `CORE-PYTORCH-COMPARE-01` 首轮对照完成。**
  **① #9 四项准入证据补齐（本机范围）**：prompt 分布 8 条 × 16 步 **8/8 逐 token 全一致**；长序列 prefill **64/128/256/547** 四档全一致；弱网断线新增 `tests/test_relay_failclosed_network.py`（超时/断连/乱序/非 loopback/runner 异常）并**加固桥**（原先非 `OSError|RelayProtocolError` 的异常不回错误帧 ⇒ 对端永久挂死）；协议/版本一致性指纹落 `out/xframe-consistency.json`（下游 fork 带 **8 个本地补丁**，两侧必须同构建）。**准入仍为拒绝**，但理由精确化为「正确性已验证、性能不具优势」（合同新增 `RelayXFrameEvidence`）。
  **② 顺带修掉两个工具缺陷**：`relay-gen.cpp` 的 `n_ctx`/`n_batch` **硬编码 512**，547-token prompt 令其以 `0xC0000409` **崩溃且无提示** ⇒ 改为 `--n-ctx` 可配 + **越界前置检查**；`relay-gen-dl.cpp` 的 `n_batch` 同为 512（常驻进程每步收整段序列）⇒ 与 `n_ctx` 联动。
  **③ #12 首轮对照（`out/pytorch-vs-llama.json`）**：同模型 `qwen3-5-2b` + 同 int4 + 同提示、3 轮重复：
  | backend | 加载 | 稳态吞吐 | 峰值显存 |
  |---|---|---|---|
  | PyTorch（CUDA） | 9.92 s | 13.78 ± 3.08 tok/s | 1.78 GB |
  | llama.cpp（**主仓为 CPU-only 构建**） | 1.61 s | 20.14 ± 2.37 tok/s | ≈0 |

  另用官方 `llama-bench`（同构建同模型）测出 llama.cpp 自身的设备差：**pp64 243.8→3676.1 t/s（15.1×）**、**tg32 23.4→136.3 t/s（5.8×）**。
  ⇒ **关键结论**：**主仓 `llama-cpp-python` 是 CPU-only**（`supports_gpu_offload()=False`、只有 `ggml-cpu.dll`），所以默认配置下 **GPU 的 PyTorch 反而优于 CPU 的 llama.cpp**；而**同为 GPU 时 llama.cpp 快 6.8×**（136.3 vs 20.1）—— 这**正面印证了 v7「有 CUDA 节点默认开启 torch backend」的裁决**，也说明该默认值依赖「llama.cpp 未编 CUDA」这一前提，一旦主仓切到 CUDA 版 llama.cpp 就需重新裁决。
  行为等价性：两侧工件本就不同（safetensors vs 转换+量化的 GGUF）⇒ **不作逐字一致要求**，改为记录最长公共前缀（`en-factual`/`zh-factual` 开头完全一致，答案同为 Paris / 北京）。

- **2026-09-17**：完成 `CORE-KOAKUMA-ENGINE-01`：新增纯标准库 `koakuma_engine.py`，统一 backend 注册、别名、公共能力和 `select_backend(node_profile)`；`ModelHost`/`ModelManager` 接入统一选择器，API/调度器/inference-service 清除 `_engine_type` 私有读取并以能力探测控制层流水线。定向回归 `592 passed, 1 skipped`，运行时配置修复后复核 `309 passed, 1 skipped`；下一张低耦合风险票为 `CORE-NO-TORCH-GUARD-01`。

- **2026-09-17**：完成 `CORE-NO-TORCH-GUARD-01`：`paged_kv_cache` 改为实际使用时才解析 torch；默认 `llama_cpp` 配置跳过 torch 设备探测，显式 `pytorch`/`auto` 保留原行为；隔离子进程阻断 torch 后完成 API 冷启动、假 GGUF 单轮推理与 `tui_admin.main(--plain)` 入口启动，真实安装 torch 的默认 GGUF 导入也保持 torch 未加载。定向回归 `7 + 22 + 5 passed`，下一张低耦合风险票为 `CORE-HIDDEN-CALLBACK-01`。

- **2026-09-17**：完成 `CORE-HIDDEN-CALLBACK-01`：移除 `ModelHost.attach/get_attachment` 和模块级 `_ATTACHED_ATTRS`；以不可变 `SchedulerCallbackSet` 将 API 辅助函数一次性显式注入 `ModelHost`、`Scheduler` 及远程 `InferenceClient` 适配器。调度器不再读取宿主私有回调属性，未配置 bundle 时在推理锁前 fail fast；乱序 bundle 装配、本地 task-worker 与 scheduler-svc 专项回归 `53 + 7 passed`。下一张低耦合风险票为 `CORE-DATAPLANE-NO-WRITEBACK-01`。

- **2026-09-17**：完成 `CORE-DATAPLANE-NO-WRITEBACK-01`：TCP 数据面移除对 callback owner 的反射和 `_tcp_server` 写回；`TCPServer.start()` 在接收/心跳线程启动前显式安装消息、断连、注册确认事件。调度器用线程局部 server source 包装事件，两个 server 并发时各自读取正确连接归属，普通控制面仍读取默认 server；wrapper 不持调度锁，业务回调继续按各自锁策略执行。回归 `TCP/Transport 103 + Scheduler 285 + API/分布式 107 + inference-service 108 passed`；低耦合五票收口完成，下一票为 10F / `CORE-LAYER-CONTRACT-01`。

- **2026-09-17**：完成 `CORE-LAYER-CONTRACT-01`：新增独立 `pipeline_node_contract`，统一 `PipelineNode`、`PipelineNodeCapacity`、`PipelineArtifactRef` 和完整 `PipelineLayout`；将容量计划、assignment manifest、Relay handoff、RPC lease 显式映射进同一半开区间合同。布局对完整覆盖、embedding/LM Head 边界、模型摘要、架构与 GGUF `block_count`/`nextn_predict_layers` 失配 fail-closed；调度器仅在已准入容量计划附加该公开布局。新增 `GET /api/cluster/resources` 与 `/nodes` TUI 聚合资源行，只显示 CPU/RAM/GPU 容量和可用性，不暴露地址或路径。回归 `合同 39 + API/TUI 115 + Scheduler 286 passed`；下一票为 10G / `CORE-RESHARD-01`。

- **2026-09-17**：完成 `CORE-RESHARD-01` 开发门：新增引擎中立的 `pipeline_reshard` 协调器，失效节点不再参与容量重解；候选布局必须保持分布式完整覆盖，未验证目标权重或边界组件、容量不足、过期 epoch、计划/布局不一致均拒绝发布。已有 PyTorch 两阶段加载以恢复计划同一 `plan_id`/合同摘要绑定 ready ACK 后，才将 topology lease 原子重派到新 epoch；`RemoteAssetSyncPlan` 只能由 `already_current`/`applied` 同步结果导出全 GGUF 可用性。新增只读 `/api/cluster/pipeline-reshard` 与 TUI 观测。回归 `Scheduler 287 + core/API/TUI/RPC 164 passed`；真实 PC/Android 故障注入和性能验收后置，下一票为 `CORE-LLAMA-ANDROID-01`。

- **2026-09-16**：登记并完成 `CORE-TRANSPORT-PORT-01` 首张低耦合风险票：新增 `src/transport_port.py` 控制面端口，`scheduler.py` 清除对 `tcp_comm` 的直接传输实现依赖；新增适配器动态替身和静态边界回归。Koakuma 引擎抽象、无 torch 守卫、显式回调和数据面写回仍分别由票 2-5 处理。
- **2026-09-17**：**新增票 10F `CORE-LAYER-CONTRACT-01`（层段协议统一抽象）与 10G `CORE-RESHARD-01`（自动重分片）**；更正 §6 的 Relay 表述（141-token 由「失败」更正为「正确性已打通、性能已收口为不具优势」）。依据：`Silica式分片方案调研-2026-09-17.md`、`层段协议立项-2026-09-17.md`、`层流水线节点类型与顶层透明性-可行性确认-2026-09-17.md`。**核实并更正**：票 10 已实现 lease/epoch/fencing/断线本地回退/画像驱动分层/`RemoteAssetSyncPlan`，故 10G 范围收窄为「真正的自动重分片」（含容量预检）。

| 2026-09-15 | `CORE-BASELINE-01` 完成：主线/支线计划、角色边界、TUI/Edge 基线、无模型启动边界、GGUF 分布式调研结论和当前入口文档统一；下一票为 `CORE-TUI-01` |
| 2026-09-15 | `CORE-TUI-01` 完成：新增 `qlh.py`、`qlh.bat`、`qlh.sh` 统一跨平台入口；TUI 增加模型舰队状态/格式/引擎/设备档位展示与 `/model fleet`、`/model select`；邮件通知全链路移除；下一票为 `CORE-EDGE-01` |
| 2026-09-15 | `CORE-EDGE-01` 完成：新增 `requirements-edge.txt`、根入口 `qlh_edge.py`、`scripts/edge_preflight.py` 和边缘回归；当前下一票为 `CORE-MF-01` |
| 2026-09-15 | `CORE-MF-01` 完成：主项目 `/api/models`、Harness `/v1/models` 与 `/v1/model-profiles` 统一格式/引擎/模板/thinking/vision/资源预算/摘要字段；四个新模型可见、可选，未验证能力仍保持 candidate；下一票为 `CORE-CUTOVER-01` |
| 2026-09-15 | `CORE-CUTOVER-01` 完成：主 API 移除外置 Web 静态挂载；Edge 保留本地 llama.cpp <=1B 默认路径，并新增能力合同与可选原生 `llama.cpp rpc-server` worker 生命周期面；定向回归 `28 passed`，API/模型相邻回归 `67 passed`，Edge 预检通过；下一票为 `CORE-LLAMA-SINGLE-01` |
| 2026-09-15 | `CORE-LLAMA-SINGLE-01` 完成：`LlamaCppEngine` 接入画像模板/thinking 控制，ModelHost 画像透传，TUI interactive 与 fast SSE 接入真实 llama.cpp 流式；新增 `scripts/llama_single_baseline.py`。Qwen2.5-0.5B、Qwen3-0.6B、MiniCPM4-0.5B 均真实加载并产生完整/流式非空结果，Qwen3 能力快照为 `thinking_controlled=true`；定向回归 `81 passed`，Edge 预检通过；DS3-7B 因本机可用内存边界不执行性能实测，沿用既有 DSW-D1/AUD-RT-01 证据边界；下一票为 `CORE-LLAMA-SIM-01` |
| 2026-09-15 | `CORE-LLAMA-SIM-01` 完成：新增 `scripts/llama_rpc_sim.py` 与专项判定测试；DS3-7B 同机 `llama-server --device RPC0` + `ggml-rpc-server --device CUDA0` 实测 12/29 层 offload，CPU/RPC backend buffer 分离，RPC 远端驻留 1990.18MiB、分布式 backend 驻留 3853.60MiB，受控 worker budget 2048MiB；分布式与 CPU 回退均返回 `Okay` 且 `output_match=true`，断 worker 可检测，worker 未加载整份 GGUF；专项 `3 passed`。本票不宣称物理单进程 OOM 或跨机/生产 RPC 已通过；下一票为 `CORE-LLAMA-PC-RPC-01` |
| 2026-09-16 | **基线重排：新增 Relay R 架构兼容轨道**：`docs/跨框架层接力重启评估-2026-09-15.md` §7.8–§7.11 已证明裁层 GGUF + `llama_batch.embd` 注入的 L→L 逐 token 16/16 行为等价；同一批实验同时确认 `embd` 路径进程内 bitwise 非确定，因此主线只接受 argmax/行为判据，暂不进入 Edge 默认路由。主线顺序改为 `CORE-RELAY-01` → `CORE-RELAY-XFRAME-01` → `CORE-LLAMA-PC-RPC-01` → Android；D→L、跨进程/网络、长序列仍未验证。下一票为 `CORE-RELAY-01` |
| 2026-09-16 | `CORE-RELAY-01` 完成：主仓接入 `src/relay_contract.py` / `src/relay_planner.py` 与 `scripts/llama_relay_probe.py`；合同允许上下游具体 GGUF 摘要不同，但强制逻辑模型摘要、裁层 block_count/MTP 布局、embedding shape 和层边界一致；探针拒绝短/伪造 token 序列，并可实测单机回退。定向回归 `43 passed`；生产路径仍默认关闭。下一票为 `CORE-RELAY-XFRAME-01` |
| 2026-09-16 | `CORE-LLAMA-PC-RPC-01` 增量：新增 `src/llama_rpc_planner.py`，消费 `DeviceProfiler.to_dict()` 同形画像，按 CPU 核数/频率/利用率、RAM、worker budget 与网络代价自动决定 RPC 层数；Surface 画像为 4 核/1.498GHz/8.36GiB 可用内存，自动选 `1/25`，不再默认手工写死层数。新增 `src/llama_rpc_contract.py` 的 lease TTL、renew、epoch fencing 与重分配；最终报告 `local_docs/CORE-LLAMA-PC-RPC-01-auto-lease-final-2026-09-16.json` 验证远端 `Okay`、RPC0 驻留 243.44MiB、远端 RSS 250.71MiB、断线后本地回退和旧 lease 拒绝。分布式请求 657ms、同机 CPU 回退 313ms，当前 CPU worker 对小模型没有吞吐收益，因此性能准入不宣称完成；直连长时与更大模型容量收益另行后置 |
| 2026-09-16 | `CORE-RELAY-XFRAME-01` 增量：新增 XFRAME 实验准入合同、fail-closed 门与 `scripts/llama_relay_xframe_probe.py` raw logits 证据判定器。Qwen3.5-2B/K=4 的 D→L 同机文件交接中，5-token f16 为 5/5；141-token f16 为 132/141（首分歧 18），全 f32 GGUF 为 128/141（首分歧 14）。完整 f32 llama.cpp 与 PyTorch 末位 cosine 0.9999999 且 argmax 相同，故失败不能归因于 f16 权重量化。**控制组进一步收敛根因**：llama.cpp 自身 layer 3 hidden 与 PyTorch 平均 cosine 1.0（最大绝对误差 ≈2.44e-4）；llama.cpp 自己导出的 hidden 注入裁层模型仍只有 130/141；**完整模型直接改收 token embedding 也只有 132/141、首分歧提前到 5** → 问题在 llama.cpp 长批 `embd` 输入图/续算路径（`build_inp_embd()` 在 embedding 模式下仍构造未写入的 token 分支）。专项测试继续通过；生产门保持关闭 |
| 2026-09-16 | **v7 用户裁决**：① 引擎层统一命名 **Koakuma（混合推理引擎）**（llama.cpp / torch 两 backend）；② **有 CUDA 的节点保留 torch 能力并默认开启（显式开关，可关闭并退化为 llama.cpp）**，取代 v5「PyTorch 降为 PC 对照」；③ 「禁止 torch」收窄为**无 CUDA 节点与边缘发行**的硬约束；④ 票 12 `CORE-PYTORCH-COMPARE-01` 由"不再作为生产路线"改为"进入生产路径（CUDA 节点默认加速）+ 保留对照"；⑤ 票 9 补充 XFRAME 四步修复顺序；⑥ 止损条目补充"主仓不依赖被修改的上游" |
| 2026-09-15 | 历史记录（已被 v5 取代）：外部复核 Silica Cluster，确认 Android + llama.cpp RPC 可运行但未证明连续 `layer_range`；`CORE-GGUF-RPC-01` 纳入 Android 候选、模型身份和安全失败矩阵，旧 D 档 `CORE-DIST-01` 曾被列为下一票 |
| 2026-09-15 | 历史记录（已被 v5 取代）：用户将 Silica 式 Android RPC 由"隔离候选"升级为攻关，原计划采用 S1 本机 2 worker PoC → S2 跨机 → S3 Android 真机 worker；现已由 `CORE-LLAMA-SINGLE-01` / `CORE-LLAMA-SIM-01` / `CORE-LLAMA-PC-RPC-01` / `CORE-LLAMA-ANDROID-01` 重排为统一闸门，并补层段合同、身份、传输保护与租约 |
| 2026-09-15 | **主线 v5 重排**：llama.cpp 模型分片/RPC 升为生产主攻；新增“裁切/低耦合 → 单机 → 单机双进程 → PC 真机 → Android”硬门；PyTorch 改为 PC 对照，任务图改为临时整模回退 |

| 日期 | 变更 |
| --- | --- |
| 2026-09-14 | 历史记录（已被 v5 取代）：主仓曾收敛为双引擎、三种分布式形态、TUI 和优化/验收基建；边缘 L 档设为硬交付；GGUF RPC 标记为隔离对照实验 |
## Startup optimization tickets

| Order | Ticket | Scope | Status |
| ---: | --- | --- | --- |
| 1 | `STARTUP-OPT-01` | Shorten the missing-local-backend probe without changing the readiness poll contract | **Done (2026-09-18)** |
| 2 | `STARTUP-OPT-02` | Remove the eager Torch import from the default llama.cpp startup path | **Done (2026-09-18)** |
| 3 | `STARTUP-OPT-03` | Separate process/API readiness from full runtime readiness | **Done (2026-09-18)** |
| 4 | `STARTUP-OPT-04` | Move distributed network discovery and identity work off the critical startup path | **Done (2026-09-18)** |
| 5 | `STARTUP-OPT-05` | Add the edge/full/TUI cold-start and no-Torch regression matrix | **Done (2026-09-18)** |
