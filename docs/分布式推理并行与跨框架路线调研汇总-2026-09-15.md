# QLH 分布式推理并行与跨框架路线调研汇总（2026-09-15）

> 文档状态：**调研汇总（现行）**
>
> 适用范围：llama.cpp/GGUF 单机与模型分片/RPC 主线、PC PyTorch 对照、任务图回退，以及远期方向（跨框架逐层接力、ExecuTorch）。
>
> 关联：[主线开发计划：分布式推理与边缘优化](主线开发计划-分布式推理与边缘优化-2026-09-14.md) · [QLH 异构分布式形态决策](QLH异构分布式形态决策-2026-09-14.md) · [三种分布式拆分细化实施方案](archive/distributed/三种分布式拆分细化实施方案.md) · [投机解码外部辅助实施说明](archive/relay/投机解码外部辅助实施说明.md)
>
> 采信纪律：本报告的每条数字/结论都标注来源等级——**本地代码核实**（可在仓内复跑）＞**上游原文抓取**（URL + 抓取日期）＞**估算**（写明算法）＞**未查到**。估算不写成实测，"未查到"不写成"不存在"。

## 0. 执行摘要

四份调研（A 并行手段 / B 开源引擎 / C 跨框架逐层接力 / D ExecuTorch）以及本次 Silica Cluster 外部复核指向同一结论：

**当前没有可直接接入的生产级跨异构引擎，但 llama.cpp RPC 是唯一已有 Android 实现参考、且值得本项目主动攻克的模型分片入口。** 本地新增实验又证明了 L→L 裁层 GGUF + `embd` hidden handoff 的 16/16 行为等价，因此增加一个 PC-only Relay 架构兼容轨道；它不替代 RPC，也不把 D→L、跨进程/网络或长序列写成已支持能力。任务图/整请求分发仍有价值，但只能解决“某个节点已经装得下完整模型”的迁就场景，不能替代“单机装不下、节点各持一部分”的分布式目标；PyTorch 层流水线保留为 PC 对照和 Relay 来源。

本次新增的 [Silica Cluster](https://github.com/ShintoChakkiath/Silica-Cluster-Decentralized-Mobile-AI) 证明了"Android 原生应用承载 llama.cpp RPC worker"这条工程路径可以实现。**按 2026-09-15 用户裁决，本方向由"隔离候选"升级为攻关**：它是目前唯一有真实实现参考（且含 Android）的"模型切分"路径，上游通道是开着的（对比"跨框架逐层接力"的 issue #27112 仍 open）。需要说清的是：Silica **没有**证明跨节点连续 `layer_range` 协议已存在——QLH 要做的正是把"比例/上限"升级为**可校验的部分驻留/分片合同**；它验证的是**可行性**，不是生产就绪（无身份校验、无传输保护、无租约）。技术原理、分片改造与 G0-G4 闸门见 §2.2.1。

| 调研 | 一句话结论 | 证据强度 |
| --- | --- | --- |
| **A 异构并行手段** | 只传 token 类并发仍是低风险收益；模型分片/RPC 是高风险但必须攻克的容量路线 | 中（含仓内代码证据；分片收益待实测） |
| **B 开源分布式推理引擎** | 无可直接集成的生产方案；Silica + llama.cpp RPC 是主攻候选，`distributed-llama` 等继续作对照 | 中（部分项目近况未核实；Silica 代码已核对） |
| **C 层接力兼容 Relay** | **L→L 已进入主线架构轨道**；**D→L 正确性已打通**（修复 `pos` 越界根因后 **f16 141/141**，Q4_K_M 下游亦 **141/141**，两侧均低比特 E4 亦 **141/141**）；**性能已收口（§5.2）**：端到端改进 **8.5×** 后为 **333 ms/步**，但仍慢于 llama.cpp 原生整模 GPU **~20×** → **接力定位为「能力组合」而非「提速」**；生产门保持关闭 | 强（本地可复跑实验 + §5.2） |
| **D ExecuTorch**（**实验先行**，用户裁决） | **不设前置条件，实验立即立项**（`CORE-ET-EXP-01`，独立环境）——Qwen3-4B `.pte` 导出 → 真机对照 → 产出决策证据；**是否接入主项目另行裁决**。事实基础：Qwen3 支持=Llama 示例参数化复用（1.4 起新增 Qwen3-VL export/eval 覆盖）；无官方 Qwen3 端侧数据；Android API 已在 1.4 **完成** Java→Kotlin 迁移 | 中（官方 release notes；无 Qwen3 真机数据） |

## 1. 调研 A：异构设备上的非线性并行手段

### 1.1 事实基线（本地代码核实）

| 证据 | 位置 | 含义 |
| --- | --- | --- |
| `PIPELINE_MAX_CONCURRENT = 1` | `src/config.py:366` | PyTorch 参考层流水线当前**串行**：同一时刻只有一个节点在算 |
| `PipelineQueue` / `compute_layer_assignment` | `src/scheduler.py:387/2448` | 已有层分配与队列基础，但未做 micro-batch 交叠 |
| 单序列 `llama_decode`（`n_batch = 64`） | `src/llama_engine.py:663-697` | L 档侧当前是**单请求**解码，未启用 continuous batching |

### 1.2 手段评估

| 手段 | 原理 | 本机可行性 | 关键限制 |
| --- | --- | --- | --- |
| Continuous batching（llama-server） | 多请求在服务端动态合批 | ✅ 可行 | 依赖 llama-server 常驻；QLH 侧需接线 |
| 多请求流式交叠 | 串行流水线内交错 micro-batch | ✅ 可行（改造量大） | 需要调度层重写 + 队列/取消/超时语义 |
| 非均匀层分配 | 按显存/算力比例切层 | ✅ 可行 | 需真实标定每层耗时（当前缺失） |
| draft-verify / 投机解码 | 小模型草稿 + 大模型校验 | ⚠️ 受限 | 见 `EX-SPEC-ROLE-01`：本机实测混合词表族不可用、最好仅 1.342× 且分布一致性 fail |
| 级联 / 路由 | 小模型先答、大模型兜底 | ✅ 可行 | 需要质量判据与回退策略（纯调度层，无新依赖） |
| 本地 prefix cache / 会话级 KV 复用 | 复用前缀命中 | ✅ 可行 | 单机内收益明确；跨节点迁移不划算 |
| 跨节点张量并行（TP） | 层内切分 | ❌ 不可行 | 要求同构 GPU + 高带宽互联 |
| 序列/上下文并行 | 序列切分 | ❌ 不可行 | 每步同步 + 带宽门槛 |
| 专家并行（MoE-EP） | 专家分布 | ❌ 不可行 | 本机模型非 MoE；异构下通信开销压倒收益 |
| prefill/decode 分离 | 两阶段分离部署 | ❌ 不可行 | KV 需跨节点搬运（~200KB/token 估算） |
| KV 迁移 / 跨节点缓存 | 缓存移动 | ❌ 不可行 | 同上网带宽门槛 |

### 1.3 伪并行清单（必须避免）

跨节点 TP、跨框架逐层接力、prefill/decode 分离与 KV 迁移、训练式 1F1B/zero-bubble、hidden 压缩、RAM 受限的多副本——这些在本机组合下"看起来并行、实际受带宽或串行依赖限制"。

### 1.4 PyTorch 参考路径的三个对照项（不改变主线）

1. **真实标定 + 非均匀层分配**：先测每层真实耗时与内存，再按比例切层（当前分配缺依据）；
2. **多请求流式交叠**：在 `PipelineQueue` 上引入 micro-batch 交错（把 `PIPELINE_MAX_CONCURRENT=1` 变成可配置且可解释）；
3. **会话级 KV 复用 + 队列优先级**：先做单机收益最大的缓存复用，再叠加优先级调度。

> 估算说明（非实测）：decode 每跳 hidden 约 4KB（1.8B/24 层/hidden 2048/bf16 估算）、KV 约 200KB/token——两者差 50×，这是"带宽不是瓶颈、同步才是"的判断依据。

## 2. 调研 B：开源分布式推理引擎

### 2.1 核心结论

**2026-09 不存在可直接拿来用的“生产级 + 跨异构 + 模型分片”方案。** 这不是放弃模型分片的理由，而是 QLH 的核心科研工程问题：以 llama.cpp RPC 为底座，先验证部分驻留和容量聚合，再补身份、租约、故障和安全合同。

### 2.2 分档推荐

| 档位 | 项目 | 理由 / 风险 |
| --- | --- | --- |
| **值得借鉴设计**（不直接引入） | `accelerate` `device_map` | 零新依赖的同机异构**上界基线**（做对照极合适） |
| | `petals` | 层流水线 + 私有 swarm 语义最贴近 QLH 形态；**2024-09 后停更** |
| | `ktransformers` | CPU 侧 AMX/AVX2 kernel 与 hot/cold 放置思路可借鉴 |
| | `exo` | 拓扑感知放置协议；但 Linux 侧目前仅 CPU 后端 |
| **主攻底座** | llama.cpp `--rpc` + Silica Cluster | 原生 GGML RPC、已有 Android 参考；上游自述 PoC/fragile/insecure，QLH 需补部分驻留、身份、租约、故障和传输保护 |
| **仅作对照实验** | `b4rtaz/distributed-llama`（**新发现**） | 纯 C++ 跨机张量并行、无 torch、CPU worker 可参与；限制：节点数必须 2ⁿ、节点上限 = KV heads 数、量化组合受限（q40/f32） |
| | PowerInfer / MLC-LLM / Xinference / OpenLLM | 定位或异构能力不匹配 |
| **不建议** | TensorRT-LLM / LMDeploy / TGI | 强绑定 NVIDIA 栈或已转维护模式（TGI 为未证实传闻） |

#### 2.2.1 Silica Cluster（Android + llama.cpp RPC）→ 本方向由"隔离候选"**升级为攻关**

> **2026-09-15 用户裁决**："这本来就是这个项目要研究和攻克的问题"——Android 参与分布式推理不是外围候选，而是 QLH 的核心命题之一；既然上游通道是开着的，就应当借鉴、改造、迁移、整合、实现，而不是只做隔离验证。

**一、它为什么能用（技术原理；源码/上游核对，非估算）**

1. **底层是 llama.cpp 自带的分布式后端，不是 Silica 自研**：`ggml-rpc-server` 把一台设备的 GGML 后端暴露为 RPC 端点，`llama-cli --rpc host:port[,host:port…]` 让**主 host 把远端设备当作可用 backend**参与同一模型的计算。构建开关 `GGML_RPC=ON` 即启用。本机已有可执行：`runtime/llama-cpp/b10964/ggml-rpc-server.exe` 与 `llama-cli.exe`（`--version` = `0.4.1-dev (build 10964)`），具备**立刻起 PoC** 的条件；PC 的 Python server 也已暴露 `rpc_servers`/`tensor_split` 参数，但不能代替原生 RPC 的驻留实测。
2. **Silica 的贡献是"把这条能力搬到 Android"**：应用内携带 `libllama.so`（主节点角色）与 `librpc.so`（worker 角色），经标准 `--rpc` 接入；`BinaryRunner.kt` 在没有层数元数据时退化为 `--tensor-split` 比例，有层数时把剩余层数交给 `-ngl`。**它证明了 Android 能编译/加载 GGML 后端并以 RPC 方式参与计算**——这正是"Android 不该只当任务池客户端"的第一份外部实证。
 3. **分配语义必须辨析清楚（不因激进而改事实）**：`-ngl` 是"设备最多装载多少层"，`--tensor-split` 是"各设备张量比例"，**两者都不是** QLH 的连续 `layer_range` 合同。QLH 的第一验收目标应定义为**可校验的部分驻留/分片合同**，只有实测能证明连续层所有权时才称为 layer-range。
 4. **"上传整模型"是 Silica 的工程选择，不是 QLH 的目标**：`SilicaService.kt` 的 full-offload 路径先把完整 GGUF 上传到主 worker；QLH 主线必须优先验证标准 `--rpc` 路径是否能让 worker 只持有被分配的部分，并用 RSS/PSS、backend buffer、文件映射和容量上限证据排除整模复制。
5. **安全缺口是可补的**：上游 README 明确本地 RPC **未加密**，Silica 也没有节点身份/模型摘要/租约/故障恢复协议。QLH 侧**已有可用件**：Ed25519 入群合同 + 一次性 nonce（`src/cluster_join.py`）、TOTP 一步确认（`AUTH-TOTP-01` 设计）。

**二、借鉴 → 改造 → 迁移 → 整合 → 实现**

| 阶段 | 具体做什么 | 本机可行性 |
| --- | --- | --- |
| **借鉴** | Android 三件套分工（`libllama.so` 主节点 / `librpc.so` worker / 标准 `--rpc` 接线）；参数化入口 `-ngl` + `--tensor-split` | ✅ 二进制已就位 |
| **改造** | ① **分片合同**：先定义 `shard_spec`/模型摘要/部分驻留证据，再决定能否映射为 layer-range；② **免整模型上传**：验证 worker 只持有分配部分；③ **身份（auth）**：worker 必须持有可验证身份 + 一次性入群授权（复用 `src/cluster_join.py` 的 Ed25519 + 一次性 nonce），高危动作走 **TOTP 一步确认**；④ **可用性**：分片**租约**（有时限、可回收）+ 故障恢复（掉线重分配、恢复不双写）+ **epoch fencing** + 备用主节点快转让 + RPC 限定内网/隧道 | ✅ 先做单机与双进程；③④的完整设计见[形态决策 §2.1](QLH异构分布式形态决策-2026-09-14.md) |
| **迁移** | Android Full 构建启用 **`GGML_RPC=ON`**，产出 `librpc.so` 与 `ggml-rpc-server`（**当前构建未启用，是明确缺口**） | ⚠️ 需 Android 工具链 |
| **整合** | llama.cpp 分片成为主形态；任务图只保留完整模型回退；PyTorch 只保留 PC 对照 | ✅ 设计层 |
| **实现** | 四步升档：**G0** 裁切/低耦合门 → **G1** PC 单机 → **G2** 同机 host + 1 worker 双进程 → **G3** PC 跨机 → **G4** Android 真机 worker | ✅ **G0-G2 可本机先做** |

**三、为什么这条线值得激进**

QLH 的命题是"异构 + 弱节点也能参与算力"。llama.cpp RPC 是目前**唯一**有真实实现参考（且含 Android）的"模型切分"路径——相比"跨框架逐层接力"（上游无公开中间层 API，已止损），它的**上游通道是开着的**。做成它，等于给"Android 从客户端变成算力"提供第一条可验收路径。

**四、保留的边界（激进不等于丢失纪律）**

- Silica 是 **AGPLv3**：**不复制其代码**，只借鉴协议边界与验证方法；直接集成前须单独做许可证与发布形态评估。
- 仍**不宣称**"跨 Android 精确按层拆分"已被验证；当前主张是“llama.cpp 模型分片/RPC 为攻关主线，PyTorch 仅 PC 对照”，实测不通过就保留单机/任务图回退。
- 证据等级不变：Silica 的结论为**上游源码核对**（非本机复现）；本轮"升级为攻关"是**方向裁决**，不是把既有证据升级。

### 2.3 三个关键问答

- **a. 混合设备（含无 CUDA 节点）跑 Transformer 有成熟方案吗？** 没有。现有分布式引擎都以"同构加速器 + 高带宽互联"为隐含前提。
- **b. 是否能用 PC llama.cpp 替代 torch 生产路径？** 可以作为攻关假设，但必须依次通过单机基线、同机双进程部分驻留、PC 真机 RPC 和故障/安全门；在全部门通过前不能删除 PyTorch 回退。
- **c. 有没有方案能替代“完整模型任务图”？** llama.cpp RPC 是唯一值得主动验证的模型分片底座；任务图只能作为迁就回退，不能回答单机装不下的问题。

## 3. 调研 C：跨框架逐层接力与 Relay（2026-09-16 基线更新）

### 3.1 核心障碍（上游原文支撑，强度：强）

| 障碍 | 事实 | 来源 |
| --- | --- | --- |
| llama.cpp **无公开中间层 API** | 公开接口只提供 token embedding（`llama_batch.embd`，`n_embd` 维）与 logits；post-block residual 无公开读写通道 | llama.cpp 上游 issue **#27112**（open，2026-08-15）："没有公开方式读取中间层，需要的人都在维护私有 fork" |
| 层间观测需自研补丁 | 只能走 `ggml_backend_sched_set_eval_callback` 一类内部钩子 | 同上（ggml-backend.h 全文抓取） |
| 先例不对口 | Petals = **同框架**（PyTorch）层接力；KTransformers = **算子级**混合（非 hidden 交接）；distributed-llama = **张量并行**（非层接力） | 各自 README（抓取） |
| 层语义差异（待 PoC 核对） | QKV 融合布局、RMSNorm eps、GQA 布局、RoPE 实现、attention mask 约定——GGUF 与 HF 导出逐层差异需 `convert_hf_to_gguf.py` / `gguf-py` 逐项核实 | **标注为待核对**（本次未逐项验证） |
| 量化误差累积 | **未查到**公开的跨框架逐层误差测量 | 未查到 ≠ 不存在 |

### 3.2 最小 PoC（若将来重启，5 步 + 判据）

1. 单机内用带钩子的 llama.cpp 补丁取第 k 层 hidden；
2. 同一权重在 PyTorch 侧复现前 k 层，逐层对拍；
3. 未量化路径判据：**cosine ≥ 0.999** 视为对齐（否则终止）；
4. 换 Q4_K_M 路径重测，记录误差是否随层数放大；
5. 只有前两步成立，才评估跨节点传输与协议设计。

### 3.3 重启条件（满足任一）

- llama.cpp 上游提供**公开**中间层读写 API（issue #27112 关闭）；
- QLH 需要一个**无法用"只传 token"方案解决**的异构分工场景（例如某设备只能承担特定层且必须跨框架）；
- PyTorch 参考路径的串行流水线被证明是首要瓶颈，且 micro-batch 交叠已到上限；
- 出现成熟的第三方跨框架层接力项目可直接引用。

### 3.4 重启评估（2026-09-15：用户裁决放宽约束 + 上游实查后）

**结论：L→L 的本地架构闭环已成立，但 D→L 长序列行为等价未成立。** D→L 的 5-token prefill 证明接口存在性，141-token f16/f32 的错误 argmax 则证明只传 hidden 尚不足以准入。因此 Relay 继续作为独立 PC 研究票，不进入默认生产。详述见独立文档[跨框架层接力重启评估 §7.12](archive/relay/跨框架层接力重启评估-2026-09-15.md)。

| §3.3 条件 | 判定 | 依据（2026-09-15 GitHub API 实查） |
| --- | --- | --- |
| 1 公开中间层 API | ❌ **未命中** | issue #27112 仍 open（仅 1 条评论，updated 2026-09-09） |
| 2 无法用"只传 token"解决 | ❌ **当前未命中** | llama.cpp 自身覆盖 CPU/GPU，弱节点用 GGUF 即可覆盖现有硬件组合 |
| 3 PyTorch 串行是首要瓶颈 | ⏳ **未验证** | §1.4 三项前置改进尚未执行 |
| 4 成熟第三方实现可引用 | ⚠️ **部分命中** | **PR #27073**（hidden-state extraction API + tools + **server endpoint**，4904 行 / 37 文件）**`merged: false`**，关闭原因为自动 PR checker 而非技术否决；已有人 cherry-pick 并在 Windows 上跑通 |

**变化要点**：①**用户接受自研/fork**（2026-09-15 裁决）→ §3.1 两个"强"障碍的根源都是"不能改 llama.cpp"，放宽后降级，代价转为**维护成本**；②**上游已有现成实现** → PoC 步骤 1 不必从零自研。**未变的部分**：量化误差累积仍无公开测量（本方向生死题）；prompt cache 与 RPC backend 曾不兼容（**跨机前缀缓存须单独验证**）。

**上游旁证**：#28863 —— Qwen3.8-27B Q4_K_XL **同机 2 卡 tensor-split 仅 1.31×**、每卡 DRAM 利用率 **66%→43%** —— 为 §1「带宽不是瓶颈、同步才是」提供上游数据佐证。

**落点**：模型转换、裁切与自定义 llama.cpp runner 留在已被 `.gitignore` 覆盖的 `build/cross-framework-layer-poc/`；稳定的 Relay 合同、L→L 探针以及 D→L raw logits 判定器已进入主仓，D→L 长序列修复和实时跨边界仍按 `CORE-RELAY-XFRAME-01` 继续。

## 4. 调研 D：ExecuTorch（2026-09-15 用户裁决：实验先行、接入另议）

### 4.1 关键事实（官方页面与 release notes，抓取于本次会话）

| 维度 | llama.cpp/GGUF（QLH 现状） | ExecuTorch | 结论 |
| --- | --- | --- | --- |
| Qwen3 支持 | 完整（GGUF 转换 + K-quant 全谱） | **本质是 Llama 示例的参数化复用**（官方原文："Qwen 3 uses the same example code as our optimized Llama model"，仅 `model_class` + config 差异） | 无独占优势 |
| 端侧性能数据 | QLH 侧**无真机基线**（未测） | 官方**无 Qwen3 数据**；只有 Llama 3.2 1B/3B（OnePlus 12：3B 4bit SpinQuant 19.7 t/s）与 Llama 3 8B（S22 7.85 / S24 10.91 t/s） | 不可直接比较 |
| Android API 稳定性 | 自有 JNI（`libqlh_llama_jni.so`，`android/app/src/main/cpp/CMakeLists.txt:50,52`） | **Java→Kotlin 迁移已完成**：v1.4.1（2026-08-14）release notes 原文 "Migrated Android Module, TrainingModule, SGD, and LLM extension APIs from Java to Kotlin"，Kotlin 成为主 API（stable 文档尚停在 1.3 的 Java library 表述） | "会返工"的风险已解除，绑定时机成熟度上升 |
| 官方覆盖进展（1.4 新增） | — | **SmolLM2 与 Qwen3-VL 的 export/evaluation 覆盖**（1.4 Highlights）；`GenerationConfig` 新增 `grammar`/`grammar_type`（结构化输出） | 对 QLH 的 Qwen3-VL-4B 工件是利好，列入观察指标 |
| 量化/NPU | K-quant 全谱 | low-bit kernels "still experimental"、NPU LLM 量化 "work underway"、Vulkan LLM 在 "What is coming next" | ExecuTorch 当前更弱（实验要核实的关键差异点） |
| 多模态 | **已接 MTMD（mmproj GGUF）** | 未查证 | 现状领先 |
| 分发体积 | .gguf 单文件 | AAR 按后端拆分（xnnpack/qnn/vulkan），仅 arm64-v8a / x86_64；**具体体积未查到** | 实验采集项 |

### 4.2 决策（用户 2026-09-15）：实验先行，接入另议

**策略变更**：不再采用"等 GGUF 硬缺口或真机 >1.5× 才立项"的条件式观察；改为**主动实验**——ExecuTorch 实验本身**立即立项**（票 `CORE-ET-EXP-01`，独立环境，不阻塞主线），**是否接入主项目（第三引擎 / Android 端侧替代）另行裁决**；实验价值不依赖接入结论。

**实验目标（不预设结论）**：
1. 独立环境用官方链路导出 Qwen3-4B `.pte`（int4），记录转换链路与算子覆盖；
2. Android 真机跑通，采集 TTFT / decode t/s / RSS / 体积；
3. 与现有 GGUF + llama.cpp（`libqlh_llama_jni.so`）在同机同量化级别对照；
4. 产出可复现证据（数据 + 命令 + 版本号），作为后续裁决依据。

**接入裁决条件（实验后再评估，需至少满足其一）**：
- GGUF 出现**硬能力缺口**（某目标模型无法转换/量化）且 Android 有明确需求；
- 真机同量化级别 **decode t/s 领先 > 1.5×**（实测量，非官方页面数字）；
- Kotlin 生态集成在工程成本/维护性上**显著优于**现有 JNI 路径（需量化对比）。

### 4.3 实验步骤（已立项：`CORE-ET-EXP-01`）

① 独立环境导出 Qwen3-4B `.pte`（int4）→ ② 真机验证与现有 JNI **同进程共存** → ③ TTFT / decode t/s / RSS / 体积对照 → ④ 记录算子覆盖与输出质量差异 → ⑤ 出报告交裁决（接入与否另议）。

环境隔离要求：实验依赖**不进入** L 档边缘安装与主仓运行时（"无 torch 可运行"硬约束保持）；实验产物与脚本独立存放，不并入主线发行。

## 5. 统一结论与行动建议

| 时间尺度 | 行动 | 依据 |
| --- | --- | --- |
| **G0-G1（本机先做）** | `CORE-CUTOVER-01` 断点/低耦合检查 → `CORE-LLAMA-SINGLE-01` PC llama.cpp 单机基线；先证明裁切/重写没有断入口，再证明无 torch 单机可用 | 用户本轮主线顺序 |
| **G2（本机双进程）** | `CORE-LLAMA-SIM-01`：1 个 llama host + 1 个 `ggml-rpc-server` worker；验证部分驻留、容量合并、正确性、断开回退和进程内存，禁止整模复制冒充分片 | 本节 §2.2.1（Silica/llama.cpp RPC） |
| **G3-G4（真机攻关）** | `CORE-LLAMA-PC-RPC-01` PC 跨机模型分片 → `CORE-LLAMA-ANDROID-01` Android `librpc.so` worker；同步补身份、传输保护、租约、epoch fencing 与热/能耗证据 | 用户本轮主线顺序 |
| **实验线（现在就做，独立环境）** | `CORE-ET-EXP-01` ExecuTorch 端侧实验（导出 → 真机 → 与 GGUF/JNI 同量化对照）；只产出决策证据，**接入另议** | 调研 D §4.2（用户裁决） |
| **中期（需要硬件/工程）** | `accelerate device_map` 上界基线对照；`distributed-llama` 2 节点对照实验（只产出决策证据） | 调研 B §2.3 |
| **架构兼容（正确性已打通，性能已收口）** | `CORE-RELAY-01` 完成；`CORE-RELAY-XFRAME-01` **D→L 正确性已打通**（修复 `pos` 越界根因后 f16 / Q4_K_M / 两侧低比特 E4 均 **141/141**）；**性能实测收口见 §5.2**（端到端 8.5× 改进后仍慢于原生 ~20×，定位为**能力组合**而非提速）| §5.2；不改变 L/RPC 生产默认路径 |

### 5.1 对主线计划止损清单的影响

- **ExecuTorch 条**：按用户 2026-09-15 裁决维持"**不立即合入 + 实验先行**"（由 `CORE-ET-EXP-01` 在独立环境执行）——止损只约束"立即合入"，不约束实验；接入裁决条件见 §4.2。
- **GGUF RPC 条（本轮改写）**：原表述"不把 llama.cpp RPC / Silica 式 Android RPC 接成默认生产路由"是**保守止损**。现按用户裁决改为**分阶段准入**：S1/S2 PoC 通过，**且**身份校验、传输保护、回滚与 soak 证据齐备后，**允许作为 Android/GGUF 侧的正式路径接入**；在此之前仍不默认开启。即**止损对象由"方向"收窄为"未验收前的默认开启"**。

### 5.2 性能实测收口（2026-09-16，D→L 跨框架接力）

**背景**：§3 的跨框架接力已从"当前不推荐开工"转为"可研究、前置成本下降"。本节补齐此前缺失的**性能维度**收口。

**实测**（本机 RTX 4060 Laptop 8GB，prompt=5，gen=64，下游 f16 裁层，**全程 64/64 argmax 一致**）：

| # | 配置 | 端到端 | 说明 |
| ---: | --- | ---: | --- |
| 1 | 跨进程 + 每步全段重算 | 182 s | 起点 |
| 2 | 同进程双后端 + 全段重算 | 167 s | 消除 IPC → **仅 8%** |
| 3 | + 双侧 **KV 复用** | 53 s | **3.4×（真正杠杆①）** |
| **4** | **+ 上游只跑前 4 层（手动前向）** | **21.3 s** | **总计 8.5×（真正杠杆②，上游侧 10.7×）** |
| 5 | 跨进程 + 4L + 下游 GPU offload | 81 s | 受跨进程 IPC 拖累 |

**各手段单独收益**：KV 复用 **3.15×** / 上游只跑 4 层 **10.7×**（上游侧）/ 下游 GPU offload **2.18×**。
**证伪项**：消除进程边界 8%、复用 `llama_batch` ≈0、朴素层截断数值破坏（0/8）。

**★ 结构性结论（对 §3 的重启判断有直接影响）**：

1. **接力在单序列上不具备性能优势** —— 最优 333 ms/步 vs llama.cpp 原生整模 GPU **13.5 ms/步**（差 **≈25×**）。⚠️ 口径注（2026-09-19 核）：参照不唯一 —— 按 `13.5 ms/步` 算 **≈25×**（`333 ÷ 13.5 = 24.7`），按 `-ngl 24` 的 `26.6 ms/token` 算 **12.5×**；参照谱与算式见 [跨框架层接力-实验数据汇总](archive/relay/跨框架层接力-实验数据汇总.md) §F/§H。
2. **根因是 batch=1 的 memory-bound 特性** —— 上游 torch-GPU **9.2 ms/层** ≈ 下游 llama.cpp-CPU **8.3 ms/层**；
   **决定速度的是"搬运多少字节"，不是"CPU 还是 GPU"**；
3. **低精度不是解** —— 实测上游 f16 **反而慢 8.4×**、bf16 慢 8.7×；NF4 只慢 1.09× 但**也无速度收益**
   （4 层场景是**延迟受限**而非带宽受限）→ **NF4 的价值在容量（7 GB → 1.8 GB），不在速度**；
4. **上游侧已无优化空间** —— 4 层计算占 **97%**，固定开销仅 **4.7%**
   → **CUDA Graph 等 launch 优化最多省 ~5%，不值得做**；
5. **→ 接力的正确定位是「能力组合」**（能跑 llama.cpp 跑不了的东西），**不是「提速」**。

**与 §3.3 重启条件的关系**：本轮**不改变**重启条件的技术判断，但**显著降低了"以性能为目的"的重启动机**；
若重启，应以**能力/兼容性**为目标，而非吞吐。

**完整证据**：[跨框架接力性能优化-汇总报告](archive/relay/跨框架接力性能优化-汇总报告-2026-09-16.md)（含五次自我推翻与全部数据）、
[同进程双后端接力实现与性能](archive/relay/同进程双后端接力实现与性能-2026-09-16.md)（实现细节与逐轮剖析）。

## 6. 证据强度与未决项（诚实清单）

**强证据（上游原文/仓内可复跑）**：
- PyTorch 参考路径的串行与层分配入口（`src/config.py:366`、`src/scheduler.py:387/2448`）、llama.cpp 单序列解码（`src/llama_engine.py:663-697`）；
- llama.cpp 无公开中间层 API + issue #27112 open；
- ExecuTorch 的 Qwen3 复用声明、Android API **Java→Kotlin 迁移完成**（v1.4.1 release notes）、low-bit/NPU 状态（官方 release notes 原文）。
- Silica Cluster 的 Android + `libllama.so`/`librpc.so` 接线与 `--rpc` 调用（上游仓库 README 与源码）；证据只支持"**Android 承载 GGML/RPC 的可行性**"，**不支持**"生产就绪"或"精确层段协议"。本方向已于 2026-09-15 升级为**攻关**（用户裁决），但**证据等级不变**——升级的是方向，不是证据。

**中等证据（资料/单面检索）**：
- 开源引擎对比表中的版本/star/近况（部分未逐条核实，TGI "维护模式"为未证实传闻）；
- 并行手段的可行性分档（基于代码事实 + 算术估算，非本机吞吐实测）。

**未查到（不等于不存在）**：
- 跨框架逐层接力的量化误差公开测量；
- Qwen3 在骁龙/联发科 GPU/NPU 的公开 token/s（**`CORE-ET-EXP-01` 将以真机实测补上这一格**）；
- ExecuTorch AAR 体积、`.pte` 与 `.gguf` 同模型体积对比（同为实验采集项）；
- `distributed-llama` 在 1GbE 下的公开加速比。

**已知调研缺口**：
- HexGen-2、mlx↔PyTorch（dlpack）互操作、vLLM layerwise/pipeline-parallel 文档未检索；
- `llama_get_embeddings_ith` / `llama_set_embeddings` 的精确语义未在抓取窗口内确认（间接支撑）；
- grep.app 代码级搜索被反爬拦截，"无跨框架层接力开源项目"的否定结论强度有限。

## 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-15 | 新建：汇总四份调研（A 并行手段 / B 开源引擎 / C 跨框架逐层接力 / D ExecuTorch），给出近期-中期-远期行动分档与止损清单影响评估 |
| 2026-09-15 | **更正（用户指出 + 复核）**：ExecuTorch 的 Android API 迁移状态此前误写为"迁移中"；经 v1.4.1 release notes 原文核对为**已完成**（Module/TrainingModule/SGD/LLM extension APIs 全部 Java→Kotlin），相应行与主线计划 §6 注记已同步；同时补录 1.4 新增的 Qwen3-VL/SmolLM2 覆盖与 `GenerationConfig` grammar 字段 |
| 2026-09-15 | **策略调整（用户裁决）**：ExecuTorch 由"保持观察（条件式触发）"改为"**实验先行、接入另议**"——新增主线票 `CORE-ET-EXP-01`（独立环境实验），§0/§4.2/§4.3/§5/§5.1 与主线计划 §5 票表、§6 止损注记同步 |
| 2026-09-15 | **外部候选复核**：Silica Cluster 确认 Android + llama.cpp RPC 可运行；因其仍是标准 RPC/offload 与比例 split，未证明连续 `layer_range`；纳入 llama.cpp 模型分片攻关，不把整模任务图当作等价分布式 |
| 2026-09-15 | **v5 主线调整**：分布式实测前增加裁切/低耦合门、PC 单机门、同机双进程门；PC llama.cpp 成功后作为生产主引擎，PyTorch 降为 PC 对照，任务图降为临时回退 |
| 2026-09-15 | **重启评估（用户裁决 + 上游实查）**：跨框架逐层接力（§3）由"当前不推荐开工"上调为"**可研究、前置成本下降**"——① 用户接受自研/fork 推理引擎；② 上游存在**可用未合并实现** **PR #27073**（hidden-state extraction API + tools + **server endpoint**，4904 行 / 37 文件，`merged: false`，关闭原因为自动 PR checker 而非技术否决）。该记录保留为 D→L 背景，不直接代表主线准入。 |
| 2026-09-16 | **L→L Relay 实验纳入现行基线**：裁层 GGUF + `llama_batch.embd` 注入逐 token 16/16 行为等价，`embd` 进程内 bitwise 非确定性和跨进程差异同步登记。新增 `CORE-RELAY-01`（主仓合同接入）与 `CORE-RELAY-XFRAME-01`（D→L/跨边界验证）；Relay 默认关闭，不替代 llama.cpp RPC。 |
| 2026-09-16 | **D→L XFRAME 首轮实测**：5-token f16 prefill 为 5/5，但 141-token f16 仅 132/141、全 f32 GGUF 仅 128/141；完整 f32 llama.cpp 与 PyTorch 末位仍近乎一致，排除“只需提高 GGUF 权重精度”的解释。生产门保持关闭，长序列状态/位置语义成为下一修复点。 |
| 2026-09-16 | **性能实测收口（新增 §5.2）**：D→L 接力端到端 **182 s → 21.3 s（8.5×）**，全程 64/64 argmax 一致。真正杠杆为 **KV 复用 3.15×** 与 **上游只跑前 4 层 10.7×**（手动前向，与完整模型 `hidden_states[4]` **bit-exact**）；证伪项：消除进程边界 8%、复用 `llama_batch` ≈0、朴素层截断数值破坏。**结构性结论**：batch=1 是 memory-bound，上游 torch-GPU 9.2 ms/层 ≈ 下游 llama.cpp-CPU 8.3 ms/层 → **接力在单序列无性能优势**（最优 333 ms/步 vs 原生 13.5 ms/步，差 **≈25×**）。⚠️ 口径注（2026-09-19 核）：参照不唯一 —— `13.5 ms/步` 下 **≈25×**（`333 ÷ 13.5 = 24.7`）、`-ngl 24` 的 `26.6 ms/token` 下 **12.5×**；见[跨框架层接力-实验数据汇总](archive/relay/跨框架层接力-实验数据汇总.md) §F/§H。，低精度（f16 慢 8.4×）亦非解 → **接力的定位是「能力组合」而非「提速」**。证据：[跨框架接力性能优化-汇总报告](archive/relay/跨框架接力性能优化-汇总报告-2026-09-16.md)。 |
