# 未完成工作备忘（非并行组）

> 状态：**现行**
>
> 更新日期：2026-09-23
>
> 适用范围：登记**申报面除以并行组（`TORCH-*` 系列）之外**的未完成、未接线与待外部条件的条目，
> 便于收尾时一眼看全。**并行组的票不在此登记** —— 见
> [`KTransformers优化迁移调研与算法数据层优化方向-2026-09-23.md`](KTransformers优化迁移调研与算法数据层优化方向-2026-09-23.md) §7 与
> [`主线开发计划-分布式推理与边缘优化-2026-09-14.md`](主线开发计划-分布式推理与边缘优化-2026-09-14.md) §5A。
>
> 口径：每条尽量给**代码级证据**（文件:行号）与**完成判据**；文档标注与代码现状不一致时，以代码为准，
> 并在同一次修改中把文档改准（见 §4 变更记录）。

---

## 1. 待接线 / 待实现（工程可做，不依赖外部条件）

| # | 条目 | 现状证据（代码为准） | 完成判据 | 出处 |
| --- | --- | --- | --- | --- |
| A1 | **Relay 的生产执行路径（调度层）** | `src/relay_transport.py` 的 `RelayTcpClient` / `serve_relay_middle_connection` 消费者**全在 `scripts/`**（`relay_l2l_net_probe.py` / `relay_mid_service.py` / `relay_experiment.py` / `verify_xframe_consistency.py`）；`src/scheduler.py:235` 的 `_node_supports_forward_layers()` 只做**准入判据**（用在 `:1691/1783/1844-1869/2286/2322/2897`）⇒ 引擎层已接线（`llama_engine.forward_layers_from_hidden` / `forward_layers_to_hidden` + `model_module.forward_layers`），**调度/流水线没有端到端跨机 hidden 接力执行** | 调度侧存在一条可跑的 cross-framework 执行路径（合同层已备：`pipeline_node_contract` 的 `cross_framework` 节点 + `pipeline_layout_from_relay_handoff`），且有端到端回归 | 主线计划 `:137`（`CORE-RELAY-01`「生产接线后置」）、`docs/模块接口说明.md:140` |
| A2 ✅ | **Android `middle_channel`（层输出通道）透传 —— 已接线（2026-09-23）** | 协议新增**可选**字段 `middle_channel`（`extract_hidden` / `keep_head_layer_out`；两侧同集合、**缺省 = 旧行为**）；`AndroidFullWorkerStageExecutor` → `TaskWorkerService` → `LayerForwardRequest` → `LocalInferenceEngine.layerForward(keepHead=…)` 全程透传；capabilities 另上报 `middle_channel` / `n_pos_per_embd`。⚠️ 精确字段集**不能**直接并入可选字段（实测踩到，会让旧对端 `field_mismatch`），实现按「payload 里是否出现」动态放宽 | 剩余：真机一次 `coordinator → task-worker → JNI` 的 `layer_forward` 数值实测（属 §2 B1） | 跨框架接力 §10.6①（已更新） |
| A3 | **`PIPELINE_MAX_CONCURRENT` 无消费者** | `src/config.py:410` 是**唯一定义**（全仓 grep 无消费者）；`local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-p2-status-2026-09-19.json:34` 亦记「交叠未进主仓」 | 要么真接线（属并行组 P1「多请求交叠」），要么保持**显式「未接线」标注** | `archive/relay/并行与吞吐路线复核.md:126`、`archive/relay/推理性能现状调研-2026-09-16.md:185` |
| A4 ❌ | **投机解码进生产解码循环 —— 永久关闭（2026-09-23 用户裁定）** | 现状：`src/speculative.py` 在 `src/` 只被 `src/api/routes_chat.py` 的实验端点引用，生产解码循环无任何引用 | **不做**（裁量：收益/复杂度不划算）—— 保留为默认关闭的独立实验端点；已在 `docs/跨框架接力…§9 当前不再投入` 登记。本行不再作为待办跟踪 | 用户裁定 2026-09-23 |
| A5 ✅ | **hidden 压缩上 wire —— 已完成（2026-09-23）** | 用帧头 `flags` **低 3 位**表达档位（`none`/`f16`/`int8_block128`/`int4_block128`）：**不需要升 `RELAY_WIRE_VERSION`** —— 旧对端见到非零 flags 直接 `unsupported_flags` ⇒ 天然 fail-closed、向后兼容；压缩只在上行 wire，服务端按档位解回 f32 再喂 runner（**对段实现完全透明**）；探针新增 `--hidden-quant` | 已满足（判据 **per-token argmax**）：`f16` **32/32**、`int8_block128` **32/32**、`int4_block128` **23/32 FAIL** ⇒ 4bit **不作为**可选档 | 跨框架接力 §5.1③（已更新）。**下行（服务端→客户端）同轮启用**：档位由服务端 `--hidden-quant` 单方面决定、客户端跟随解压（两端可各自选档）；实测 上行`none`/下行`none` **32/32 ✅**、`f16`/`f16` **32/32 ✅**、`f16`/`int4_block128` **23/32 ❌**（失败模式与上行 int4 **完全一致** ⇒ **反证下行压缩确实生效**） |
| A6 ✅ | **接力记录逐段 runner/构建标识 —— 已完成（2026-09-23）** | 记录新增 `segment_engines`（逐段 `runner`/`channel`/`build`）；`build` 含 shim 与同目录 `libllama`/`ggml*` 的 sha256、pip `llama_cpp` 版本、段工件名字/大小（GB 级工件默认不算 sha256，`--digest-artifacts` 可开）；远端段读服务端 `ready.build`，没给 ready 文件时**显式** `remote_unknown` | 已满足（实现集中在 `src/relay_segment_info.py`，证据 `l2l-net-local-05b-2seg-seginfo.json`） | 跨框架接力 §10.2（已更新） |
| A7 ✅ | **三种段工件生成器 —— 已完成（2026-09-23）** | `scripts/cut_layers.py` 新增 `--keep-head N`（上游段，不重命名）与 `--k K1 --end K2`（中段，重编号）；manifest 带 `mode`/`source_layer_range`；上游段/中段不给 `RelayTrimPlan` 合同（那合同只描述 tail） | 已满足（`tests/test_cut_layers.py::TestHeadAndMiddleModes`） | 跨框架接力 §10.3（已更新） |
| A8 ✅ | **纯 L→L 的「主仓引擎」口径 —— 已成文（2026-09-23）** | 作为 §1 **规则 7**：写明各段允许的构建、段工件生成方式，并要求混用两套构建时显式取齐 `flash_attn` 与 batch 线程 | 已满足 | 跨框架接力 §10.1（已更新） |
| A9 | **Gemma4 PyTorch 层适配的主运行时准入** | `pipeline_model_descriptor.py:336` 的 `runtime_block_reason`：`gemma4_unified` 需隔离 Transformers sidecar、「主运行时禁止复用 Qwen2 执行器」；`scheduler_sidecars.py:815` 只在 **sidecar profile** 路径把 `pipeline_runtime_supported` 置 True | 按 `已知问题记录.md` #11 的关闭标准（真实工件 + CUDA + 双机准入） | `已知问题记录.md` #11（`:499-508`） |
| A10 ✅ | **量化预算闸门 / 健康检查的 CI 兜底 —— 已完成（2026-09-23）** | ① `relay_quant_budget.py` 新增 `--fail-on {off,unsafe,tight}`（默认 `off` ⇒ 保持旧行为；判定达级即以 1 退出）；② **不新增脚本**（`scripts/*` 在白名单里，避免往 `.gitignore` 加例外）：把自检做进两个已在白名单的脚本 —— `relay_quant_budget.py --self-check`（合成记录验证"合格集必须 `safe`、越线集必须 `unsafe` 且 `--fail-on unsafe` 必须红"）与 `relay_health.py --self-check`（本机 loopback：活监听必须判健康、已释放端口必须判死）；③ 新增 workflow `.github/workflows/relay-gates.yml`（push/PR 跑两条 `--self-check`）；④ 更正 `checks.yml` 里"健康检查不在 CI 里"的旧注释 | 已满足 —— 判据是"驱动/CI 里有一条**会因闸门失败而红**的检查"：**故障注入实测**（`_verdict` 的 `unsafe` 改 `safe`、等级表压平、`_check_tcp` 的失败分支改判健康）⇒ 自检均 **exit 1** 并逐条报出失败项，已固化为 `tests/test_ci_relay_gates.py` 的 3 条负向回归 | 跨框架接力 §8.5 / §6.6（均已更新） |
| A11 ✅ | **切点闭环与多轮复核 —— 已完成（2026-09-23）** | 先侦察才发现真实缺口：新 gate 要求输入记录带 `record_origin.kind=raw_measurement` + `load.warmup`，而现存 `p0-repeat/r*-k*.json` 是**加固前**生成的（缺这两字段）⇒ 必须**重新生成记录**，不是"复算一下"。做法：① qwen2.5-0.5b：`relay_experiment.py --path d2l_mainrepo … --warmup 3` 重跑 **3 轮 × 8 切点**（K=1..20，**24/24 per-token argmax PASS**）⇒ r²=**0.9935**、**预测=实测=K=1 ⇒ PASS**（旧单轮的"预测 1 / 实测 3 ❌"确认为单轮噪声）；② qwen3.5-2b hybrid：**3 轮 × 5 切点**（K=4/8/12/16/20，`--cut-multiple 4`，**15/15 PASS**；K=4 工件由 `cut_layers.py --k 4` 现切后补测）⇒ r²=**0.9938**、**实测最优=K=4 ⇒ PASS**，**旧的"K=20 最优"作废**（来自另一套 `upstream_iface` 的单轮扫描，上游 `std/mean≈1.78`） | 已满足：文档 §7.1 附**逐字复算命令**；输入是 `build/…/p0-gate{,-35}/r*-k*.json` 原始多轮记录；产物 `cut-model-analysis-qwen{25,35}-gate.json` 与 `cut-plan-gate-qwen{25,35}.json`（均 `passed=true`、`admitted=true`） | 跨框架接力 §7.1 / §7.2 / §7.3（均已更新） |
| A12 ✅ | **`seq_ids` / `positions`（多序列显式位置）—— 已上协议（2026-09-23）** | 协议两侧（主仓 `_LAYER_FORWARD_OPTIONAL_FIELDS` / Android `layerForwardOptionalFields`）新增这两个**可选**字段，值校验同为：长度 == `hidden_spec.n_tokens`、取值非负整数，错误码统一 `invalid_<field>`；Android 执行器解析后透传到 `layerForward(seqIds=, positions=)`（**形状非法即 fail-closed**，不静默退化成单序列） | 已满足（**主仓下发侧仍无生产者** —— 目前只打通通路；真实多序列下发属 Relay 生产执行路径 A1） | 本轮（2026-09-23） |
| A13 ✅ | **Android 的 `hidden_spec.dtype` 与主仓对齐 —— 已修（2026-09-23）** | 协议层与执行器都改为接受 `float32` / `float16`；执行器新增 `hidden_f16`（LE 16 位 base64）解析并在本段转 f32（`halfToFloat`，覆盖规格化/非规格化/Inf/NaN）⇒ 消掉「主仓发得出、Android 收不了」的隐性不对称 | 已满足（当前主仓侧也无 f16 生产者，属对称性补齐） | `AndroidFullWorkerStageExecutor.kt`、`TaskWorkerProtocol.kt` |
| A14 ✅ | **worker 隔离路径下的「末段能力」回归测试 —— 已补（2026-09-23）** | `tests/test_llama_keep_head_subprocess.py::test_worker_path_supports_last_segment_hidden_to_token`：在**已 import llama_cpp** 的进程里构造 worker 隔离，用真实 shim + `qwen25-05b-f16-tail8.gguf` 跑一次 `forward_hidden_to_token`（旧实现必抛「缺 `qlh_kh_forward_embd_token`」）；缺工件时按本仓惯例 skip | 已满足 | 本轮修复记录 |

## 2. 卡外部条件（记录了，但本机无法收口）

| # | 条目 | 卡点 | 出处 |
| --- | --- | --- | --- |
| B1 | **APK/JNI 的 `layer_forward` 数值** | 设备门已过（Y700 真机 `native-worker-candidate`），但**APK 内部从未跑过一次 `layer_forward`**；且应用反复闪退（#26.4 待 logcat）；y700 现经动态 adb 端口接入（历史 serial 失效，Termux sshd 8022 为备选路径） | 主线计划 `:149`、`已知问题记录.md` #26.4（`:910-942`）、P4.5 立项 §21 |
| B2 | **双机/多机窗口 —— 部分推进（2026-09-24）** | ✅ **Surface 已打通**（`surface@100.100.52.106`，Tailscale 直连 7–19 ms；链路实测 3.65/4.39 MB/s）；✅ **`HA-CROSSHOST-01` 长时已做**（`--long-steps 1000` ⇒ `accepted_frames=1000`、`rto_ms=612`、passed）与**弱网注入已做**（新增 `--inject-delay-ms`，0/50/200 ms ⇒ RTO 637/895/1789，三档 passed；**属应用层注入、非真实弱网**）；✅ 顺带复核了跨机层接力（`l2l-net-surface-05b-2seg.json` **PASS 32/32**，两端 shim/DLL sha256 一致）。✅ **durable journal replication 核心已实现**（`src/journal_replication.py` + `src/journal_replication_transport.py`：checkpoint、事件哈希链、SQLite WAL/FULL 副本、增量幂等、分叉/缺口/篡改拒绝、HMAC TCP + durable ACK、接收进程终止后重开恢复；复制专项 **12 passed**，扩展 HA/journal 回归 **67 passed**），但**物理跨节点故障注入、重试/背压、quorum commit watermark 和密钥部署仍缺**，所以不算生产 HA 收口。**仍缺**：运营商/真实链路的丢包与抖动（网络层注入已做，见下）、`CORE-RESHARD-01` 真实 PC/Android 故障注入（Android 侧 y700 目前间歇可达）、P4.5 §22 多候选选举（需 ≥3 节点）。✅ **真实弱网（网络层）已做（2026-09-23，P4.5 §26）**：`--weaknet-bridge` 作 SSH `ProxyCommand` 注入延迟+丢包 ⇒ 纯延迟 30 ms 档 passed（RTO 679→**1459 ms**），**丢包 5 % 起 failed**（8 s 连接超时，SSH 成功率 1/3）⇒ 结论"丢包下需重试/退避"。✅ **quorum 证书交换跨机已做（2026-09-23，P4.5 §24）**：`--quorum-exchange` ⇒ Surface 侧起真 voter（自带 SQLite 账本），本机 + 远端两票颁证、对端账本持久化摘要与证书逐字一致、只联系单机时 `quorum_unavailable` fail-closed。~~D8 双 Worker 竞速~~ **已摘出本表**（2026-09-23 核实：降级/待路线决策，见 `验收清单与资源限制登记.md:64` —— 推测执行支线无产品接线、整模 worker 已定为低优先级临时回退，**双机窗口不解锁**） | 主线计划 `:146`、P4.5 §11/§18/§20/§24/§25、验收清单 `:64` |
| B3 | **大内存/大显存与 CUDA 工件** | A2 DeepSeek-7B 冒烟矩阵、A5 Qwen3-VL-8B（内存条待购）、A6 gemma4 8K/16K；D29 P1 下游 GPU offload 阻塞于**未构建 CUDA 版 `llama-cpp-python`** | 验收清单 `:26/29/30/86` |
| B4 | **Linux 真机 / 干净机 / 签名包** | C1 Linux 真机全家桶、C2 外部干净机、C4 签名 Launcher 包复验、B4 双栈、B5 身份重置复验、D10 真 PyInstaller 构建待打包环境 | 验收清单 `:39-50/66` |
| B5 | **真实网络（非 loopback、非模拟）** | 弱网量化边界与长时稳定性（§6.5/§5.5）；中段跨网络方案（经 DERP 中继不可用，需同局域网部署） | 跨框架接力 `:718/774/905-906/947` |

## 3. 设计内的 fail-closed（**不算债**，避免误判为未完成）

- Relay / keep-head / RPC 的**生产准入默认关闭**（`admit_relay_xframe` 等按证据回 `admitted`，无证据即 fail-closed）；
- `SEC-AUTH-JOIN-01` 默认 **501**（入群授权绑定本地 Auth App/TOTP 一次性验证）——`已知问题记录.md:29` 已标「已完成」；
- `QLH_CONTROL_FENCE_REQUIRED` 与 voter 集需**显式启用**才生效（P4.5 §3.2）；
- `NODE_ROLE=auto` **未默认启用**（等 `HA-CROSSHOST-01` 物理验收与 handoff/recovery 门）。

## 4. 本机可完成工作（2026-09-24 整理）

把 7 份台账（主线计划 / KTransformers 迁移调研 / 本备忘 / 验收清单 / 已知问题记录 / P4.5 立项 /
跨框架接力）合起来核对后的「**不需要外部条件就能推进**」清单。§1/§2 是**本仓视角的未完成项**，
本节是**可执行性视角**的分类（含并行组 `TORCH-*` 线，因为本机是唯一有 CUDA 的工作节点）。

### 4.1 并行组那边（`TORCH-*` / PyTorch 上游线）本机可做

| # | 项 | 本机可做的动作 | 依据 |
| --- | --- | --- | --- |
| P-H1 | `TORCH-HW-ADMIT-01` **CPU 抖动收敛** | 按 2026-09-24 的抖动诊断改**测量流程**（固定亲和性 + 线程数 == 域内物理核数，或按 P/E 域分别标定），再用**同一预登记门**重测完整矩阵 | 迁移调研「CPU 时延抖动诊断」段：P/E 混合、单核差 2.76×，同构域内 CV 0.027–0.041（门 0.10） |
| P-H2 | `TORCH-HW-ADMIT-01` **CUDA 抖动诊断** | `nvidia-smi --query-gpu=clocks.sm,clocks.max.sm,temperature.gpu,power.draw` **只读采样（无需管理员）**，量化「GPU 频率摆动 × 时延抖动」 | 该票 CUDA 侧 6/12 超限；文档原记「未采频率/温度」；实测本机 `clocks.sm=210 / max=3105 MHz` |
| P-H3 | `TORCH-HW-ADMIT-01` **跨机链路诊断** | 用 `scripts/torch_lan_echo_probe.py` + `--weaknet-bridge` 复测 Surface，定位「大帧 CV 0.97」是链路还是序列化开销 | 迁移调研记 Tailnet 大帧 CV 0.40–0.97、WLAN 直连 median 159.7/782.5 ms |
| P-H4 | `TORCH-HETERO-PLAN-01` **去合成化** | 输入从"合成成本"换成 `torch_operator_profile` 的**真实 97/250 签名画像** | 该票自述"证据全部为合成输入" |
| P-H5 | `TORCH-ACT-COMPRESS-01` **矩阵扩展** | 0.5B + 2B 双模型、长 prompt 的压缩门禁矩阵（真实弱网部分仍需外部） | 该票只做了 0.5B / 单 prompt |
| P-H6 | 迁移调研 §6 **P1 多请求/多序列交叠** | 把 `PIPELINE_MAX_CONCURRENT` 真正接线（= 本备忘 A3 的"真接线"分支） | 迁移调研 :123 |
| P-H7 | 迁移调研 §6 **P2 mmap 冷启动/缺页专项** | 模型放磁盘时的首 token 抖动测量 | 迁移调研 :126 |
| P-H8 | `TORCH-RUNTIME-ADMIT-01` | ❌ **锁定，不得启动**（硬依赖 HW-ADMIT 完整准入） | 主线 :176 |
| P-H9 | `TORCH-MOE-PLACEMENT-01` | ⚠️ 本机**无 MoE 样本**（已核实 `models/` 与工件目录）⇒ 需先下载 | 该票"排队" + 缺样本 |

### 4.2 这边（relay / HA / 分布式 / 验收）本机可做

| # | 项 | 本机可做的动作 | 依据 |
| --- | --- | --- | --- |
| R-R1 | **A1 Relay 生产执行路径** | 调度侧接线（合同层 `cross_framework` + `pipeline_layout_from_relay_handoff` 与引擎入口都在主仓，且无生产消费者） | §1 A1；**需用户裁范围** |
| R-R2 | **HA：丢包下的重试/退避** | P4.5 §26 诊断出的硬边界（丢包 5% ⇒ 8 s 连接超时；SSH 成功率 1/3）⇒ 加重试/退避后重测 | P4.5 §26；`HA-CROSSHOST-01` 正等这条 |
| R-R3 | **#22 双机复验** | Surface 已可 SSH ⇒ 同步源码后复验「自动两节点层计划 + Full Worker」 | 已知问题记录 :732 |
| R-R4 | **B5 / #8 身份重置 + LAN 回退复验** | 只需"已登录 Tailnet 的实机"⇒ Surface 即可 | 验收清单 :40 |
| R-R5 | **`CORE-RELAY-XFRAME-01` 剩余** | ✅ **长时运行：同机已做（2026-09-24）** —— 同机 D→L `gen=1024`（约 7× 既有基线）**per-token argmax 全程一致（1023 步，`first_mismatch=None`）**，且 RSS 32 步 vs 1024 步几乎相同（2.2269 vs 2.2267 GB ⇒ 无泄漏）；**跨机长时卡外部**（节点离线）。✅ **n 段容量外推：同机已做** —— 同机三段 `l2l_net_3seg` **PASS 32/32**，容量对照给出**一般式 `收益 = 整模 / max(各段)`**：三段与两段（head8+cut-k12）收益**同为 1.563×**（瓶颈是末段 606.7 MiB）⇒ **加段数不增收益**，只有压低最大段才提升（需 4 段拓扑）。**仍可做**：不可分层张量缩减（需设计裁定） | 主线 :138；跨框架接力 P5 段（已更新） |
| R-R6 | **`CORE-LLAMA-PC-RPC-01` 长时运行** | 双机窗口已开 | 主线 :139 |
| R-R7 | **D29 优化项** | P0 切点重搜 / P2 批量交叠 / P3 混合精度累积测量 / **优化候选 rank1–4（KV 预分配、`Tensor.to`、RMSNorm 融合、buffer 复用）** | 验收清单 :86 |
| R-R8 | **跨框架接力 §5.1③/④** | 误差反馈 + 预取与传输-计算重叠（纯协议/驱动内实现） | 跨框架接力 :690–691 |
| R-R9 | **§8.6 服务常驻** | Windows 服务 / `nohup` + 健康检查（`scripts/relay_health.py` 已具备） | 跨框架接力 :1043 |
| R-R10 | **D1/D3/D4/D5/D6/D9/D12/D15** | 文档直接标「随时可跑 / 本机可立即验收」 | 验收清单 :57–71 |
| R-R11 | ⚠️ `journal_replication` 的 transport 接线 | 在制品已在工作区（666 + 214 行，未提交）⇒ **由实现方推进，本线不碰** | P4.5 :649–681 |

### 4.3 需要外部条件（摘要）

Linux 真机 / Android 真机 + logcat（含 #26 闪退）/ **≥3 节点组网**（§22 多候选选举）/ 大内存硬件
（A2·A5·A6）/ 23.9 GB Gemma4 工件（#11·A9）/ CUDA 版 `llama-cpp-python`（D29-P1）/ **GPU 锁频的
管理员权限** / 真实运营商线路（弱网硬边界）/ 纯边缘集群（跨框架接力 §7.3）/ 签名包与打包环境
（B4·C4·D10）/ MoE 样本。

### 4.4 建议动手顺序（判据明确 + 本机可闭环 + 直接解卡）

1. **R-R2** HA 丢包重试/退避（解 P4.5 §26 的硬边界，`HA-CROSSHOST-01` 就等它）；
2. **P-H2** CUDA 抖动诊断（GPU `210/3105 MHz` 已现端倪，补齐 HW-ADMIT 另一半证据）；
3. **P-H1** CPU 抖动收敛后的整矩阵重测（同一预登记门，CPU 4/12 有机会归零）；
4. **R-R7** D29 优化候选 rank1–4（收益方向明确，属接力性能主线）；
5. **P-H4** HETERO-PLAN 去合成化（把已完成的票从"合成"推到"实测"）。

### 4.5 文档-代码陈述待同步（3 处，**未擅改**）

- `docs/跨框架接力…md` §10.6 仍写「① `seq_ids`/`positions` **未上协议** ③ Android `hidden_spec.dtype`
  仍只收 `float32`」—— 已由本备忘 **A12/A13**（2026-09-23）完成；
- 同文档 §5 结论页仍写「当前 wire 仍是 FP32」—— 已由 **A5**（2026-09-23）完成；
- 本备忘 A3 引用的 `src/config.py:410` 已漂移到 **`:414`**。

### 4.6 `bench_pytorch_vs_llama.py` 的处理结论（2026-09-24）

`scripts/bench_pytorch_vs_llama.py`（票 12 `CORE-PYTORCH-COMPARE-01` 的对照脚本）**存在于工作区但
不在仓库** —— 它是 **2026-09-20「主仓瘦身」（用户裁定：开发/实验工具与运行时产物不入库、只留本地）
时有意移出**的（同一批还有 `batch_xframe_acceptance.py`、`build_offline_bundle.py` 等），
`.gitignore:155` 有该裁定说明。**核实结论：文档此前就不存在对它的路径式引用**（全仓 grep 只有它
自身的 docstring）；已在 `docs/主线开发计划…md:150` 的票 12 状态里补一处**只提脚本名**的说明
（"随主仓瘦身移出仓库、仅本地保留，本文不再给出其调用示例"），**不把它加回仓库**。

## 5. 变更记录

| 日期 | 变更 |
| --- | --- |
| 2026-09-23 | 新建本备忘（刨除并行组 `TORCH-*` 票）；收口两处陈旧表述：`docs/主线开发计划-分布式推理与边缘优化-2026-09-14.md:138`（`CORE-RELAY-XFRAME-01` 状态列改为「2026-09-21 最新口径在前、历史在后」）、`docs/验收清单与资源限制登记.md:78`（D22 删除「生产准入保持 fail-closed 关闭」与已完成项「更大模型容量收益」）；`src/config.py:410` 的 `PIPELINE_MAX_CONCURRENT` 加显式「未接线」标注（A3） |
| 2026-09-23 | **A6/A7/A8 完成**：记录逐段构建标识（`segment_engines` + 服务端 `ready.build`）、`cut_layers.py` 三种段（上游/中段/末段）、L→L 口径成文（§1 规则 7）。过程中修掉**两个真 bug**：① `llama_keep_head.forward_hidden_to_token` 的「可选符号检查」漏了 worker 前置条件 ⇒ worker 隔离路径下把「符号在子进程」误判成「缺 `qlh_kh_forward_embd_token`」（表现：tail 段 `runner_failed`）；② `relay_segment_info.collect_local_build` 原先**隐式 import `llama_cpp`**，污染 `sys.modules` 并改变 keep-head 的隔离判据（正是触发 ① 的条件）⇒ 改为不做隐式导入、由调用方传入。新增待办 **A14**（worker 隔离路径的末段能力缺回归测试） |
| 2026-09-23 | **A4 永久关闭**（用户裁定：投机解码不进生产解码循环，收益/复杂度不划算）⇒ `跨框架接力 §9` 登记为「当前不再投入」，本备忘不再跟踪。**A12/A13/A14 完成**：① 协议两侧新增可选 `seq_ids` / `positions`（长度 == `hidden_spec.n_tokens`、取值非负，错误码 `invalid_<field>` 两侧一致）+ Android 全链路透传到 `layerForward(seqIds=, positions=)`，形状非法 fail-closed；② Android 的 `hidden_spec.dtype` 与主仓对齐（`float32`/`float16` 都接受，`hidden_f16` 在本段转 f32），消掉「协商通过、执行必失败」的隐性不对称；③ 补上 worker 隔离路径的末段能力回归测试（真实 shim + tail 工件，旧实现必抛「缺符号」）。验证：主仓 **120 passed**；`test_llama_keep_head_subprocess` **3 passed**；Android `:app:testFullDebugUnitTest` **BUILD SUCCESSFUL** |
| 2026-09-23 | **A5 完成**（hidden 压缩上 wire，与并行组的 `torch/scheduler` 线零重叠）：新增 `src/relay_hidden_quant.py`（f16/int8/int4 block128 的线上编解码）+ `relay_transport` 的 `flags` 低 3 位档位表达（**不升 version**、旧对端天然 fail-closed）、`quantize_upload` / `frame_hidden_bytes`；探针加 `--hidden-quant` 并写入记录。**判据（per-token argmax，0.5B 两段）**：`f16` 32/32 ✅、`int8_block128` 32/32 ✅、`int4_block128` **23/32 ❌**（first_mismatch=9）⇒ 4bit 不作可选档（如实记录）。修掉一个实现 bug：解码漏除 `levels`（结果放大 127×/7×，由最小样例定位）。验证：relay 相关 **69 passed** |
| 2026-09-23 | **A5 下行（服务端→客户端）补完**：`serve_relay_middle_connection(hidden_quant=)` + 服务进程 `--hidden-quant`（仅 `--role head|middle` 会回 HIDDEN，且写进 ready 上报）、客户端三处响应按帧里档位解回 f32（调用方永远看到 f32）。**实测（0.5B，远端 head 段 + 远端 tail 段，per-token argmax）**：`none`/`none` 32/32 ✅、`f16`/`f16` 32/32 ✅、`f16`/`int4` **23/32 ❌** —— 最后一条是**反向判据**（失败模式与上行 int4 完全一致 ⇒ 证明下行档位确实作用在数值上，而非被忽略）。期间修掉一次误操作（`tests/test_relay_middle_transport.py` 的函数定义被拼接成一行，已复原并加类/用例）。验证：relay 相关 **75 → 49 passed**（含新增下行 2 例） |
| 2026-09-23 | **A10 完成**（与并行组的 `torch/scheduler` 线零重叠）：① `relay_quant_budget.py` 新增 `--fail-on {off,unsafe,tight}`（**默认 `off` ⇒ 旧行为不变**；判定达级即 `return 1`）—— 此前该脚本无论档位多不安全都 `return 0`，无法当闸门；② **自检不新增脚本**（`scripts/*` 走白名单，不往 `.gitignore` 加例外 ⇒ 也避开并行组正在改的 `.gitignore`）：做进两个已在白名单的脚本 —— `relay_quant_budget.py --self-check` + `relay_health.py --self-check`，顺带把 `main` 里内联的判定抽成 `_verdict()`，让自检与生产**走同一套**判定；③ 新增 `.github/workflows/relay-gates.yml`（push/PR 跑两条 `--self-check`）；④ 更正 `checks.yml` 的旧注释。**关键证据 = 故障注入**：`_verdict` 的 `unsafe`↦`safe`、等级表压平、`_check_tcp` 失败分支改判健康 ⇒ 自检均 **exit 1** 并逐条报失败项（该红会红）✓；三条负向已固化为 `tests/test_ci_relay_gates.py` 回归。验证：闸门相关 **25 passed** |
| 2026-09-23 | **A11 完成**（与并行组的 `torch/scheduler` 线零重叠）：侦察先行 —— 新 gate 的输入契约是"记录带 `record_origin.kind=raw_measurement` + `load.warmup`"，而现存 `p0-repeat/r*-k*.json` 是**加固前**产物（两字段都缺）⇒ 结论是**必须重新生成记录**（不是复算）。据此重跑：qwen2.5-0.5b **3 轮 × 8 切点**（24/24 PASS）、qwen3.5-2b **3 轮 × 5 切点**（15/15 PASS，K=4 工件现切）⇒ 两模型闭环**均 PASS**（qwen2.5 预测=实测=K=1；qwen3.5 实测最优=**K=4**，旧"K=20"作废）。**顺带查清一处口径陷阱**：旧 qwen3.5 上游接口是 `ModelManager.load_layer_range + forward_layers`、新的是 `model_module.forward_layers` ⇒ 新旧结论**方向相反**，属跨口径不可比（`std/mean≈1.78` 的噪声也是佐证）。**零代码改动**（`build_record` 早已写 `record_origin`，:283）。未做项：按层类型分组拟合（仍无显著内部最优，暂无必要）。验证：两模型报告 `passed=true`；文档 §7.1 附逐字复算命令 |
| 2026-09-23 | **B2 部分推进**（双机窗口打开后）：① 打通 Surface（`surface@100.100.52.106` + 默认密钥；当时超时是用户名/网络问题）⇒ 跨机层接力 `l2l-net-surface-05b-2seg.json` **PASS 32/32**（两端 shim/DLL sha256 一致），同机/跨机端到端 101.8 → **126.7 ms/token（+24.9, +24%）**；② `HA-CROSSHOST-01` 补两项 §18 的 open 检查 —— **长时**（`--long-steps 1000`、`rto_ms=612`、passed）与**弱网注入**（给 `ha_crosshost_physical_smoke.py` 新增 `--inject-delay-ms`，0/50/200 ms ⇒ RTO 637/895/1789，三档 passed；证据带 `scope=app_layer_control_frame_only` 与"非真实弱网"标注）；回归 7 passed，默认 0 ⇒ 旧行为不变。**仍 open**：真实弱网、quorum 证书交换跨机、持久日志复制、`CORE-RESHARD-01` 真实故障注入、D8 双 Worker、§22 多候选选举 |
| 2026-09-23 | **HA：quorum 证书交换跨机已做**（P4.5 §24）：`ha_crosshost_physical_smoke.py --quorum-exchange` —— Surface 侧起真 voter（自带 SQLite 账本 + 私钥，配置随源码用 repr 字面量下发、**不走命令行**）；本机 collector 只联系 voter-a + 远端 voter-b（`quorum_size=2`）⇒ **两票来自两台主机**、`validate_certificate` 通过、**对端账本摘要与证书逐字一致**、只联系单机时 `quorum_unavailable` fail-closed；**不改 quorum 代码**（用 `_RemoteVoter` 代理 —— 正是 `QuorumVoter` 注释里的 "future transport"）。回归 **22 passed** |
| 2026-09-23 | **HA：真实弱网（网络层）已做**（P4.5 §26）：新增 `--weaknet-bridge`（可作 SSH `ProxyCommand`，注入每块延迟 + 整块丢弃、由 TCP 重传补）⇒ **纯延迟 30 ms 档 passed**（RTO 679→**1459 ms**），**丢包 5% 起 failed**（8 s 连接超时；SSH 成功率 1/3）⇒ 结论「**丢包下需重试/退避**」（已登记）。过程中修 3 个真 bug：voter_set JSON 走命令行被 SSH→cmd 引号吃掉、`--help` 在 GBK 控制台崩、桥接把 `BufferedReader` 当 socket（无 `recv`）。`production_availability_claim` 仍 `false` |
| 2026-09-24 | **并行组 `TORCH-*` 在制品分批提交**（8 批代码/配置 + 1 批文档，9 笔已推）：`OP-PROFILE-01`、`OP-REGISTRY-01`、`HETERO-PLAN-01`、`PHASE-PLAN-01`、`ACT-COMPRESS-01`、`HW-ADMIT-01` 及 `test_torch_lan_echo_probe`；**顺带修**「测试 import 了被 `.gitignore` 挡住的脚本」⇒ 补 `torch_lan_echo_probe.py` 白名单（否则别人 checkout 后必 ImportError）。torch 全量 **128 passed** |
| 2026-09-24 | **CPU 时延抖动诊断完成**（解并行组 `TORCH-HW-ADMIT-01` 的稳定性门缺口）：新增 `scripts/torch_cpu_jitter_diagnosis.py`（**只诊断**）+ 3 例回归；定位根因 = **P/E 混合架构**（`i9-13900H` 逐核双峰 14.5–16.0 vs 38.3–40.0 ms，**P 快 2.76×**）+ **线程/物理核不匹配**（默认跨 P/E CV 0.075–0.322；同构域内 **0.027–0.041**）⇒ **门没问题，测量流程才是问题**。顺带修 ctypes `GetCurrentProcess` 的 HANDLE 被截断（`SetProcessAffinityMask` 静默失败）。文档见迁移调研「CPU 时延抖动诊断」段 |
| 2026-09-24 | **本备忘新增 §4「本机可完成工作」**（7 份台账合核：并行组 `TORCH-*` / 本线 relay·HA·验收 / 卡外部摘要 / 建议动手顺序 / 文档-代码待同步 3 处）；并处理 `bench_pytorch_vs_llama.py`：核实它是 **2026-09-20「主仓瘦身」有意移出**（`.gitignore:155`，非漏提交）、文档**本就无路径式引用** ⇒ 仅在主线计划票 12 补一处**只提脚本名**的说明，**不加回仓库** |
| 2026-09-24 | **本机可完成工作（§4）落地第一项：`CORE-RELAY-XFRAME-01` 的「长时运行」同机部分**。口径与判据：`relay_experiment.py --path d2l_mainrepo --layers 1 --gen 1024`（约 7× 既有 141 步基线），判据仍是 **per-token argmax 全程一致**。结果 **PASS**：`matched_runs=1/1`、`first_mismatch_index=null`，1023 个 decode 步全程一致（121.9 s）；**无退化证据**：RSS 32 步 2.2269 GB vs 1024 步 **2.2267 GB**（差 0.2 MB）⇒ 无泄漏。逐段耗时：上游 7.05/±10.81 ms、下游 56.50/±35.79 ms、基线 37.88/±11.69 ms（std 来自首步长尾）。证据 `build/relay-records/d2l-longrun-k1-g1024-20260924.json`。**跨机长时未跑**：当日 Surface `offline (last seen 4h)`、y700 离线 11 h ⇒ 标为**卡外部条件**（需节点在线），不是未推进。文档：跨框架接力 P5 段已补该实测段 |
| 2026-09-24 | **本机可完成工作（§4）落地第二项：`CORE-RELAY-XFRAME-01` 的「n 段容量外推」同机部分**。① 正确性：同机三段（head8 → mid8-12 → cut-k12，本机 middle/tail 服务走 loopback）`l2l_net_3seg` **PASS 32/32**（`middle_hops=32`、`first_mismatch=None`，三段 shim/DLL sha256 一致）；② 容量（真实工件字节）：整模 948.1 MiB、head8+tail8 ⇒ 最大段 720.5 ⇒ **1.316×**、head8+cut-k12 ⇒ 606.7 ⇒ **1.563×**、**三段 ⇒ 仍是 606.7 ⇒ 1.563×**。**结论（一般式）**：`收益 = 整模字节 / max(各段字节)` ⇒ **加段数不增容量收益**，只有压低**最大段**才提升；0.5B 的瓶颈是末段（`cut-k12` 606.7 MiB）⇒ 三段与两段收益相同，与既有「两段 1.547–1.568×」口径一致；要再提升需 4 段拓扑（当前探针只支持一个 middle 段）。证据 `build/relay-records/l2l-net-local-05b-3seg-capacity-20260924.json`。文档：跨框架接力 P5 段已补该实测段 |
