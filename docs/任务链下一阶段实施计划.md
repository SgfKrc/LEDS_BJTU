# 任务链下一阶段实施计划

> 状态：已完成（N1.0-N1.6，TC-N1 已达到准入门槛）
>
> 更新日期：2026-07-14
>
> 适用范围：当前 `dual_candidate` 单机 POC 到可恢复、多 Provider、可接 PC Full Worker 的任务链底座

## 1. 当前基线

当前任务链已经具备：

- 固定 `dual_candidate` DAG；
- Workflow、Stage、Attempt 的基础内存状态；
- 环、自依赖、未知依赖和重复 Stage ID 校验；
- 查询、列表、协作取消和有限记录清理；
- `execution_mode=task_graph`、实验开关和准确的本地 POC 统计；
- 最终聚合后再写会话历史，中间候选不直接暴露给用户；
- `result_ready -> completed/cancelled` 结果提交屏障和终态不可逆约束；
- Workflow、Stage、Attempt 的显式状态转移表和统一转移入口；
- 用户态 `STATE_DIR`、SQLite WAL append-only journal 和 snapshot 查询投影；
- journal 单实例进程锁、启动时保守恢复和幂等恢复事件；
- 只删除终态 Workflow 的按时间/数量保留策略和管理 API；
- 结构化 Provider 数据契约、线程安全 registry 和原子 reservation；
- `LocalFullModelProvider(max_concurrency=1)`、取消传播和必达释放；
- attempt 记录实际 Provider、节点、reservation 和白名单化执行指标；
- 按 Provider 槽位预留的有界 ready-Stage 并行调度；
- deterministic fake/in-process Worker、并行取消和关闭栅栏；
- lease epoch、唯一 winner、结果 fencing 和纯 Stage 受控 fallback；
- 工作流 API/UI 可观测摘要及 Worker v1 golden protocol；
- journal 写入失败时任务图 fail-closed，普通聊天继续可用；
- API、取消、历史隔离、thinking 过滤和模型切换保护测试。

2026-07-14 基线命令：

```powershell
.\.venv-packaging\Scripts\python.exe -m pytest `
  tests\test_task_graph.py tests\test_task_graph_api.py `
  tests\test_task_graph_parallel.py tests\test_task_graph_fencing.py `
  tests\test_task_journal.py `
  tests\test_task_provider.py tests\test_task_worker_protocol.py `
  -q -p no:cacheprovider
```

当前任务图、journal、Provider、协议和 API 专项结果：`104 passed`。完整测试结果：`805 passed, 23 skipped`。

当前生产路径仍是单进程、单本地 Provider、串行执行；多 Provider 只在本机模拟环境完成验证：

- Workflow、Stage、Attempt 状态变化已写入本地 journal，终态可跨 Coordinator 查询；
- `StageSpec.provider` 已成为 registry 的硬选择约束，生产任务通过真实 Provider 执行；旧回调入口只保留为测试/兼容适配器；
- 当前只注册主节点 `local_full_model`，其 reservation 并发上限为 1；
- 默认两个 ready 候选继续按本地单槽串行执行；指定不同 fake/in-process Provider 时可有界并行；
- accepted attempt 已携带 lease ID、单调 epoch 和可选到期时间；Stage 只接受当前 epoch 的唯一 winner；
- 模拟/远端形态 Provider 可在 lease 到期或显式可重试错误后受控 fallback；默认生产 Stage 不配置 fallback，本地真实模型不做超时强杀；
- 启动时会把持久化的非终态 attempt 转为 `expired`，把未完成 Stage 和 Workflow 转为可解释终态并追加恢复事件；
- `result_ready` 在重启后保守转为 `failed`，不会恢复模型内部状态或重复提交会话结果；
- journal 清理只作用于终态 Workflow；运行中或等待恢复的记录不会因时间或数量上限被删除。

## 2. 下一阶段目标

下一阶段定义为 **TC-N1：可恢复任务状态与 Provider 边界**。

它不要求真实从节点在线，目标是先在单机和模拟 Worker 上证明：

1. 每次状态变化都能持久化、重放和审计；
2. 主节点重启不会把未完成任务误报为运行中，也不会重复提交结果；
3. 逻辑 Stage、执行 attempt 和 Provider 槽位相互独立；
4. 两个独立 ready Stage 可以由不同模拟 Provider 并行执行；
5. 迟到、重复和旧 epoch 结果不能污染聚合；
6. 上述能力通过后，真实 PC Full Worker 只需接入协议和传输，不再改变核心状态机。

该阶段细化现有 T0、T1 和 T2 的交界部分：先完成 T0，再用本机模拟完成 T1 的协议内核和 T2 的并行汇合，最后等待真实设备验收 T1。

## 3. 本阶段不做

- 不接 Android Full Worker；Lite 版继续不承担 Worker 任务。
- 不把当前 PyTorch 层流水线连接改造成任务 Worker。
- 不开放模型生成任意任务图。
- 不实现 DAG 图转树、公共子任务合并或推测执行。
- 不在真实 PC Worker adapter 和版本化协议准入前实现远端网络重试。
- 不恢复崩溃前模型内部生成状态；进程重启后只能从已提交 Stage 检查点或完整 Stage 输入重做。
- 不把外部 PostgreSQL 作为任务执行前提。
- 不为了并行强行让同一个本地模型实例同时处理两个请求。

任务图优化按[任务图图树变换调研与实施计划](任务图图树变换调研与实施计划.md)后置，在 Provider、持久化和唯一提交稳定后从 shadow 模式开始。

## 4. 先修正状态机

### 4.1 增加结果提交屏障

当前协调器在最终 Stage 完成时立即把 Workflow 标为 `completed`，但 API 尚未把回答写入会话历史。取消发生在两者之间时，代码再把 `completed` 改为 `cancelled`。这会让事件重放无法判断哪个终态才有效。

建议 Workflow 状态调整为：

```text
created
  -> running
  -> result_ready
       -> completed
       -> cancelled
  -> failed
  -> cancelled
```

语义：

- `result_ready`：最终 Stage 输出已经在任务侧原子提交，但尚未确认写入用户会话。
- `completed`：会话历史/最终响应提交屏障已经通过。
- `cancelled`：提交屏障前取消，结果不得进入用户会话。
- `failed`：图、Stage、Provider 或提交校验失败。

`completed`、`failed`、`cancelled` 都是不可逆终态。删除 `completed -> cancelled` 转移，用 `commit_result()` 和 `discard_result()` 从 `result_ready` 分流。

### 4.2 Stage 与 Attempt 状态

Stage：

```text
blocked -> ready -> leased -> running -> validating -> completed
                                      \-> failed|cancelled
blocked|ready                          -> skipped|cancelled
```

Attempt：

```text
offered -> accepted -> running -> committing -> completed
   |          |          |            |
   +----------+----------+------------+-> rejected|failed|expired|cancelled
```

首个本地 Provider 可以省略网络 offer/accept，但事件仍使用同一语义，避免之后为远端 Worker 再改状态模型。

### 4.3 统一状态变更入口

禁止业务代码继续直接写 `workflow.state = ...`。每次转移通过单一方法完成：

```python
transition(
    entity_type,
    entity_id,
    expected_state,
    next_state,
    event_type,
    event_payload,
)
```

转移方法必须：

- 在实体锁内比较 expected state；
- 生成唯一 `event_id` 和工作流内递增 `sequence`；
- 先持久化事件和最新投影，再对外发布新状态；
- 重复 `event_id` 幂等成功；
- 非法转移返回结构化错误；
- 不在事件中写入完整 prompt、候选正文、密钥或数据库凭据。

## 5. 本地持久化方案

### 5.1 存储选择

采用 Python 标准库 `sqlite3` 建立独立任务 journal，不复用当前聊天历史 JSON 文件。

理由：

- 事件 ID、工作流 sequence 和唯一提交需要数据库约束；
- 一个事务可以同时追加事件和更新查询投影；
- SQLite 能处理进程异常后的事务回滚；
- 不需要联网安装新依赖；
- 比整文件 JSON 重写更适合高频状态事件。

事件表保持 append-only；可更新的 snapshot 只是查询加速投影，不是事实来源。

### 5.2 可写目录

安装包默认位于 Program Files，任务 journal 不能写在 exe 目录。新增独立 `STATE_DIR`：

```text
Windows: %LOCALAPPDATA%\QLH-Edge-Inference\state
Linux:   $XDG_STATE_HOME/qlh-edge-inference
         或 ~/.local/state/qlh-edge-inference
Override: QLH_STATE_DIR
```

任务数据库建议为：

```text
STATE_DIR/task_graph/task_graph.sqlite3
```

CPU/集显版和 CUDA 版若允许同时运行，应使用实例/节点命名空间，避免两个进程共用同一个协调器数据库。命名空间至少包含稳定 `node_id`；不得用会变化的显示名称。

### 5.3 最小 schema

```sql
CREATE TABLE workflow_events (
    event_id TEXT PRIMARY KEY,
    workflow_id TEXT NOT NULL,
    sequence INTEGER NOT NULL,
    entity_type TEXT NOT NULL,
    entity_id TEXT NOT NULL,
    event_type TEXT NOT NULL,
    occurred_at REAL NOT NULL,
    payload_json TEXT NOT NULL,
    UNIQUE (workflow_id, sequence)
);

CREATE TABLE workflow_snapshots (
    workflow_id TEXT PRIMARY KEY,
    last_sequence INTEGER NOT NULL,
    state_json TEXT NOT NULL,
    updated_at REAL NOT NULL
);
```

后续 payload blob、数据库同步 outbox 和保留策略可以增加独立表，不能把候选正文直接塞进事件表。

### 5.4 SQLite 运行约束

- 数据库必须位于本地文件系统，不放在 Tailscale/SMB 网络共享目录。
- 启用 WAL、foreign keys、busy timeout，并显式选择满足崩溃恢复要求的 synchronous 级别。
- 每个写事务短小，不在事务内做模型推理或网络等待。
- 初始化/迁移失败时，任务图功能应返回“journal 不可用”，不能静默退回不可恢复内存模式后仍宣称可恢复。
- 普通聊天模式不应被任务 journal 故障拖垮。
- 打包测试必须确认 `_sqlite3` 被包含且安装后的用户目录可写。

## 6. 重启恢复语义

TC-N1 不尝试续跑崩溃时的模型调用，采用保守恢复：

| 恢复前状态 | 恢复动作 |
|------------|----------|
| `completed` | 恢复为只读终态，可查询，不重复写会话 |
| `failed`/`cancelled` | 恢复原终态 |
| `result_ready` | 检查最终提交标记；未确认时标记 `failed`，错误码 `coordinator_restarted_before_result_commit` |
| `running/validating` Stage | 当前 attempt 标记 `expired`，Workflow 标记 `failed` |
| `created/ready/leased` | 释放 reservation/lease，Workflow 标记 `failed` |

恢复本身也必须追加事件，例如 `workflow_recovered`、`attempt_expired`、`workflow_interrupted`，不能只改内存快照。

后续实现 Stage checkpoint 和 payload store 后，才允许创建新 attempt 从已提交 Stage 输出继续。恢复绝不接续 PyTorch hidden state、KV cache 或 llama.cpp 内部上下文。

## 7. Provider 边界

### 7.1 最小接口

建议先采用结构化 Protocol，而不是把模型回调继续扩展：

```python
class ExecutionProvider(Protocol):
    provider_id: str

    def inspect(self) -> ProviderCapabilities: ...
    def reserve(self, request: StageRequest) -> Reservation: ...
    def execute(
        self,
        attempt: StageAttempt,
        reservation: Reservation,
        cancel_event: threading.Event,
    ) -> StageResult: ...
    def cancel(self, attempt_id: str) -> None: ...
    def release(self, reservation_id: str) -> None: ...
```

`reserve()` 必须原子占用并发槽。单纯读取能力快照后直接 execute 会在并发时超卖 Worker。

### 7.2 首批 Provider

只实现：

1. `LocalFullModelProvider`：包装当前 `model_manager.chat`，`max_concurrency=1`。
2. `DeterministicFakeProvider`：测试用，可控制延迟、失败、阻塞、重复结果和迟到结果。
3. `InProcessWorkerProvider`：使用与未来 PC Worker 相同的 offer/accept/lease/result 数据对象，但传输保持进程内。

真实 PC Worker 不在此批次接线。先通过 fake/in-process Provider 证明调度和状态机，再把传输替换为 TCP/WebSocket adapter。

### 7.3 Provider registry

Registry 负责：

- Provider ID 唯一性和生命周期；
- 能力快照及更新时间；
- Stage 硬约束过滤；
- reservation 和 release；
- Provider 健康状态；
- 选择原因和排除原因；
- 取消广播和关闭清理。

Registry 不负责修改 DAG，也不直接持久化会话正文。

## 8. ready Stage 并行调度

N1.4 已完成按 Provider 槽位的有界并行；生产模板的两个候选因共用单槽本地 Provider 仍串行，指定不同模拟 Provider 时执行以下流程：

1. 在锁内计算 ready Stage 集合。
2. 为每个 Stage 选择并 reserve Provider。
3. 释放工作流锁后提交执行，模型调用期间不持有状态锁。
4. 每个完成回调只提交对应 attempt 结果。
5. 在锁内经 fencing 闸门提交当前 epoch 的唯一 winner，再释放下游。
6. aggregate 只触发一次。

重要约束：

- 同一个 `LocalFullModelProvider` 仍保持并发 1，因此当前单模型 POC 行为不会因为线程池而变成不安全并发。
- 两个不同 fake/in-process Provider 可以并行，用于无从节点条件下验证 DAG 调度。
- 任务图全局并发、每 Workflow 并发和每 Provider 并发分别限制。
- 取消后不再创建新 attempt；已运行 Provider 收到取消并最终释放 reservation。
- 任一 future 抛异常都转为 Stage/Attempt 失败事件，不允许调度线程退出后留下永久 `running`。

## 9. Lease、epoch 与唯一提交

每个 Stage 维护：

- `lease_epoch`：每次重派单调增加；
- `winner_attempt_id`：成功提交后不可更改；
- `output_digest`：已提交输出摘要；
- `reservation_id`：用于释放 Provider 槽位。

提交条件：

```text
workflow 非终态
AND stage 尚无 winner
AND attempt_id 属于该 stage
AND attempt.lease_epoch == stage.lease_epoch
AND lease 未过期
AND output schema/digest 校验通过
```

重复提交同一 `attempt_id + digest` 幂等返回已提交；同一 attempt 不同 digest、旧 epoch 或不同 attempt 的迟到结果必须拒绝并记录原因。

TC-N1 只在模拟 Worker 上验证 lease 到期和重派。真实网络心跳、时钟偏差和断线检测在 PC Worker 接线时再做，但核心提交规则不再改变。

## 10. 实施批次

### N1.0：冻结状态机与提交语义

状态：**已完成（2026-07-14）**。

- 增加 `result_ready`。
- 删除 `completed -> cancelled` 逆转。
- 将所有状态修改收口为显式转移函数。
- 补终态不可逆和提交/丢弃竞态测试。

验收：取消发生在模型完成、最终 Stage 提交、会话写入三个边界时，状态和历史始终一致。

实现结果：`TaskGraphCoordinator.run()` 在最终 Stage 完成后返回 `result_ready`；API 在同一取消临界区追加会话并调用 `commit_result()`，取消抢先时通过 `discard_result()` 回滚历史。完成、失败、取消终态不可逆，重复 commit/discard 和迟到取消已有专项测试。验收时任务图专项测试 `32 passed`，完整测试 `733 passed, 23 skipped`。

### N1.1：用户态 STATE_DIR 与 SQLite journal

状态：**已完成（2026-07-14）**。

- 新增路径配置和目录初始化。
- 实现 schema、事务追加、snapshot 投影和查询。
- Coordinator 注入 journal 接口；测试可使用内存或临时 SQLite。
- journal 故障结构化上报，不影响普通聊天。

验收：强制关闭并重新创建 Coordinator 后，所有终态工作流仍可查询，事件 sequence 连续且不重复。

实现结果：

- 新增 `task_journal.py`，事件 append 与 snapshot 投影在同一 SQLite 事务中提交，使用 WAL、foreign keys、busy timeout 和 `synchronous=FULL`。
- 默认写入 `%LOCALAPPDATA%/QLH-Edge-Inference/state` 或 XDG state 目录；可用 `QLH_STATE_DIR` 覆盖。实例文件名包含应用变体、稳定节点 ID 的可读前缀和哈希，避免 CPU/CUDA 或不同节点共用数据库。
- 任务图关闭时不创建 SQLite；启用后初始化、健康检查或写入失败均返回结构化 `503`，不会静默退回内存执行，普通聊天路径不受影响。
- 持久化 snapshot 仅保存输出摘要、大小和 `error_present`，不写 prompt、候选正文或原始异常文本。
- 新 Coordinator 可以查询历史终态；当启动恢复尚未执行时，非终态会明确标记 `recovery_pending/persisted_unrecovered`，N1.2 已在应用启动路径完成其保守终态化。
- CPU/集显和 CUDA PyInstaller spec 已显式包含 `task_graph`、`task_journal`、`sqlite3` 和 `_sqlite3`。
- 验收时专项测试 `58 passed`，完整测试 `748 passed, 23 skipped`。

### N1.2：保守恢复与保留策略

状态：**已完成（2026-07-14）**。

- 启动时重放/加载 snapshot。
- 非终态 attempt 统一过期，工作流进入可解释失败。
- 增加按终态时间和数量清理 snapshot/event 的管理操作。
- 运行中的记录不得因 `max_records` 满而被静默删除。

验收：在 planning/running/result_ready 等位置模拟崩溃，重启后无幽灵运行态、无重复结果提交。

实现结果：

- 应用创建 Coordinator 时先扫描非终态 snapshot；运行中的 attempt 进入 `expired`，执行中的 Stage 进入 `failed`，尚未开始的 Stage 进入 `skipped`，Workflow 进入 `failed`。
- `running` 与 `result_ready` 使用不同固定错误码；恢复事件与新 snapshot 在同一 SQLite 事务中提交，sequence 连续，重复恢复不会追加第二个事件。
- 不恢复 KV、采样器、隐藏状态或未提交的最终正文；`result_ready` 重启后也不会自动提交会话历史。
- journal 文件使用跨进程单实例锁，旧 Coordinator 关闭后不可继续写入，避免两个进程同时恢复同一实例。
- 支持 `QLH_TASK_GRAPH_RETENTION_DAYS` 与 `QLH_TASK_GRAPH_RETENTION_MAX_RECORDS`；启动自动清理，也可由主节点调用 `POST /api/workflows/journal/cleanup`。
- 清理在一个事务中分批删除终态事件和 snapshot，非终态记录始终不进入候选集。
- 验收时任务图、journal 和 API 专项测试 `54 passed`，完整测试 `755 passed, 23 skipped`。

### N1.3：Provider registry 与本地 Provider

状态：**已完成（2026-07-14）**。

- 定义能力、请求、结果、reservation 和错误类型。
- 将 `local_full_model` 从标签变成真实 Provider。
- API 只负责构建模板输入和最终会话提交，不直接承担 Stage 调度状态。
- 保持当前默认关闭和单本地模型行为。

验收：现有 N1.0-N1.2 专项测试语义不回归，指标中的 Provider 来自实际执行记录而非硬编码。

实现结果：

- 新增 `task_provider.py`，定义 `ProviderCapabilities`、`StageRequest`、`StageAttempt`、`Reservation`、`StageResult`、稳定错误码和 `ExecutionProvider` Protocol。
- `ProviderRegistry` 实现 Provider ID 唯一性、能力/健康查询、Stage 类型硬过滤、原子 reservation、注销、取消、释放和关闭清理；身份不匹配或重复 reservation 会被拒绝。
- `LocalFullModelProvider` 包装现有本地模型调用并固定 `max_concurrency=1`；长期 Provider 使用通用分派器，每个请求携带不落盘的内部执行上下文，不会复用上一次请求参数。
- Coordinator 在 Stage 执行前 reserve，在所有成功、失败和取消路径释放；取消会传播到当前 Provider。旧 `execute_stage` 参数被收口为临时 Provider 适配器，既有测试不形成旁路。
- attempt snapshot/event 记录实际 Provider、Provider 类型、节点和 reservation；只允许 token 用量、速度、模型 ID 等白名单 metadata 进入 journal，不保存候选正文。
- API 顶层 `provider/subproviders/participating_nodes` 从成功 attempt 推导，另用 `orchestrator=task_graph` 表示编排方式；工作流列表返回实际 registry 能力状态。
- CPU/集显和 CUDA PyInstaller spec 已包含 `task_provider`。
- 验收时任务图、journal、Provider 和 API 专项测试 `70 passed`；含打包声明测试 `81 passed`，完整测试 `771 passed, 23 skipped`。

### N1.4：有界并行与模拟 Worker

状态：**已完成（2026-07-14）**。

- ready Stage 使用有界 executor。
- 接入两个可控 fake/in-process Provider。
- 验证 fan-out 并行、fan-in 单次聚合、Provider 槽位和协作取消。
- 本地真实模型 Provider 仍串行。

验收：两个 200 ms 模拟候选的工作流明显低于 400 ms 串行基线；时间断言保留 Windows CI 裕量，并同时用 barrier 证明真实重叠。

实现结果：

- 新增 `QLH_TASK_GRAPH_MAX_PARALLEL_STAGES`，Coordinator 使用惰性创建的有界 `ThreadPoolExecutor`；该上限同时约束单 Workflow batch 和 Coordinator 全局 Stage 并发。
- 调度线程先按 registry 能力和剩余槽位选择 ready batch，再原子 reserve，最后 submit；未提交的 reservation 会立即释放，不使用“future 启动后再抢槽”的竞态路径。
- 同一 Provider 每轮不超过其剩余并发槽；生产 `local_full_model(max_concurrency=1)` 因而保持严格串行，不会同时调用两次本地模型。
- 新增 `DeterministicFakeProvider` 和 `InProcessWorkerProvider`；支持延迟、起跑 barrier、阻塞、指定 Stage 失败、调用记录和协作取消，但不会自动注册到生产 API。
- 所有并行 future 均收敛后再决定 Workflow 结果；异常优先级为 journal 不可用、取消、Stage 顺序错误。单支失败不会遗留其他永久 `running` attempt，aggregate 只触发一次。
- Coordinator close 会先拒绝新 Workflow、广播取消、关闭 executor 并等待活动状态机退出，最后关闭 Provider registry 和 journal；API shutdown 还受完整聊天互斥锁保护，不会越过 `result_ready -> completed` 提交边界。
- 专项测试证明两个 250 ms 候选存在真实时间重叠且总耗时低于 450 ms、三个候选受全局并发 2 限制、本地单槽严格串行、失败/取消槽位归零、并行 journal sequence 连续、关闭取消终态先落盘。
- 验收时任务图、journal、Provider、并行调度和 API 专项测试 `77 passed`；含打包声明测试 `88 passed`，完整测试 `778 passed, 23 skipped`。

### N1.5：lease/fencing 故障注入

状态：**已完成（2026-07-14）**。

- 模拟 accept 超时、执行超时、断线、重复结果和迟到结果。
- 实现 epoch 增加、旧 attempt 过期和唯一 winner。
- 首版 fallback 只在明确可重试错误和纯 Stage 上启用。

验收：100% 故障注入循环中 aggregate 最多执行一次，旧 attempt 从不覆盖新 winner，Provider 槽位最终全部释放。

实现结果：

- `StageSpec` 增加有界且唯一的 `fallback_providers`、显式 `pure` 和 `lease_timeout_seconds`；默认 `pure=False` 且无 fallback，生产模板行为不变。
- 每个 accepted attempt 获得唯一 lease ID 和递增 epoch；Stage snapshot 记录当前 epoch、选中 Provider、唯一 `winner_attempt_id` 与输出 SHA-256。
- Provider 结果携带 `attempt_id + lease_epoch`。registry 只为未填写身份的旧本地适配器绑定当前 attempt，不覆盖 Provider 明确返回的错误身份。
- 所有正常返回和外部模拟返回统一经过 fencing 闸门；错误 Provider、错误/旧 epoch、过期 lease、未知 attempt、冲突 digest 和 winner 之后的迟到结果均拒绝并记录不含正文的原因事件。
- 相同 `attempt_id + digest` 重复提交幂等返回；同一 attempt 的不同 digest 和旧 attempt 迟到提交不能改变 winner 或输出。
- reserve/accept 和 execute 失败只有同时满足“错误明确 `retryable=True`、Stage 显式 `pure=True`、仍有下一个 Provider”才重派；旧 accepted attempt 在重派前进入 `expired`。
- `DeterministicFakeProvider` 支持有限次数 accept 超时和执行断线注入；真实 `local_full_model` 仍受 epoch/winner fencing，但不启用 wall-clock lease 到期，不会因 N1.5 被强杀。
- 输出必须是可严格 JSON 编码且不含 NaN/Infinity 的 dict，随后才计算稳定 digest；不可序列化输出不能成为 winner，也不能触发 fallback。
- 20 轮全故障循环逐轮验证旧 epoch 拒绝、aggregate 恰好一次和全部 reservation 归零；错误 epoch 在 winner 前也不能提交，Provider 错误 attempt 身份不会被静默修正。
- 验收时 N1.5 专项测试 `11 passed`，任务图、journal、Provider 和 API 专项测试 `88 passed`，完整测试 `789 passed, 23 skipped`；Pylance 同源检查零诊断。

### N1.6：API、UI 与真实 PC Worker 准入

状态：**已完成（2026-07-14）**。

- 查询接口增加 journal 状态、恢复原因、lease epoch、winner 和实际 Provider。
- UI 区分 `result_ready`、恢复失败、重试和迟到结果拒绝。
- 增加 Worker 协议 golden fixtures 和版本协商字段。
- 输出真实 PC 接线前的准入报告。

验收：不看后端日志也能判断工作流实际路径和失败边界；协议 fixture 稳定后才能开始真实 PC Worker adapter。

实现结果：

- Stage/Workflow snapshot 持久化重派次数、结果拒绝次数、最后拒绝原因和时间；这些字段与对应 journal 事件一起提交，写入失败时同步回滚。
- 工作流列表和详情统一返回 `observability`：明确 `result_ready`、恢复原因、重派、结果拒绝、winner 数量、实际 Provider 和实际节点；详情同时返回去除本地路径的 journal 状态。
- 前端按 workflow ID 轮询真实详情并在请求结束时读取终态；状态带区分结果待提交、重启恢复失败、重派、迟到/冲突/旧 epoch 结果拒绝和 journal 不可用。
- 新增 `task_worker_protocol.py`，冻结 `qlh.task_worker` v1 的九类消息、严格字段、身份、能力、输入/输出摘要、租约截止时间、错误码和 8 MiB 大小上限。
- 版本协商选择最高公共版本，无交集、非法范围或错误 hello_ack 均使用稳定错误码拒绝；协议状态明确返回 `adapter_connected=false` 和 `transport=not_implemented`。
- golden fixture 覆盖 hello/ack、offer/accept、renew、result/error、cancel/ack；CPU/集显与 CUDA spec 均显式包含协议模块。
- 输出[PC 完整任务 Worker 接线准入报告](PC完整任务Worker接线准入报告.md)：允许开始 TC-N2 adapter 开发，但真实传输、双进程和实机测试通过前禁止宣称 Worker 已可用。
- 验收时 Worker 协议专项 `15 passed`、任务图/Provider/journal/协议/API 专项 `104 passed`、前端状态测试 `6 passed`；生产构建成功，完整 Python 回归 `805 passed, 23 skipped`，Pylance 同源检查零诊断。

## 11. 文件边界建议

考虑当前仓库仍以 `src/*.py` 为主，下一阶段先采用小模块，避免为了目录重构扩大变更面：

```text
src/task_graph.py          # DAG、实体与协调器；逐步移除直接状态赋值
src/task_journal.py        # SQLite 事件、snapshot、迁移和恢复
src/task_provider.py       # Provider Protocol、registry、reservation
src/task_worker_protocol.py# offer/lease/result 结构和校验，不含传输
src/api_server.py          # API 接线、模板输入、最终结果提交
src/config.py              # STATE_DIR、journal 与并发配置

tests/test_task_graph.py
tests/test_task_graph_parallel.py
tests/test_task_journal.py
tests/test_task_provider.py
tests/test_task_worker_protocol.py
tests/test_task_graph_api.py
```

当 Provider 类型和模板数量明显增加后，再迁移到 `src/task_graph/`、`src/execution/` 包；本阶段不做纯目录重构。

## 12. 必测故障矩阵

| 场景 | 通过条件 |
|------|----------|
| 事件重复写入 | 同 `event_id` 幂等，sequence 不跳变 |
| sequence 冲突 | 事务失败且内存状态不前进 |
| journal 文件不存在 | 自动建库和迁移 |
| journal 路径不可写 | 任务图不可用原因明确，普通聊天可用 |
| SQLite 正在 busy | 有界等待，超时后不留下半状态 |
| 写事务中进程终止 | 重启后只见完整事务 |
| `result_ready` 前取消 | 不写会话，Workflow cancelled |
| 会话提交成功后重复请求 | 不重复追加消息或统计 |
| running 时重启 | attempt expired，Workflow failed，不显示运行中 |
| 两个 Provider 同时完成 | 各自 Stage 正确提交，aggregate 一次 |
| 同一 Stage 新旧 attempt 同时返回 | 只有当前 epoch winner 可提交 |
| Provider execute 抛异常 | Stage 失败、槽位释放、工作流终态明确 |
| Provider cancel 抛异常 | Coordinator 继续完成取消，记录清理错误 |
| executor worker 意外退出 | 不留下永久 ready/running Stage |
| 数据库离线 | journal 和任务执行不受影响 |
| 安装目录只读 | journal 写入用户 STATE_DIR |
| PyInstaller 环境 | `_sqlite3` 可导入、迁移和恢复 |

## 13. 总体验收门槛

TC-N1 完成必须同时满足：

1. 所有 Workflow、Stage、Attempt 转移都有事件且可重放。
2. 终态不可逆，最终结果提交不会在取消竞态中重复或丢失。
3. 主节点重启后不存在幽灵 `running`，终态记录可查询。
4. 外部数据库不可用不影响 journal。
5. 两个模拟 Provider 能并行完成独立候选并正确 fan-in。
6. lease/epoch 能拒绝重复、迟到和旧 attempt 结果。
7. Provider reservation 在成功、失败、取消和异常路径都释放。
8. 现有任务链 API、thinking、历史隔离和模型切换测试不回归。
9. 集显和 CUDA PyInstaller 环境都能创建并恢复 journal。
10. 统计继续如实标记模拟/本地执行，不能把模拟 Worker 宣称为真实分布式参与。

## 14. 后续衔接

TC-N1 通过后，下一阶段才是 **TC-N2：真实 PC Full Worker adapter**：

- 在现有集群连接上增加版本化 Stage 控制消息，或使用隔离通道；
- 接入 capability、offer/accept、lease renew、result 和 cancel；
- 用真实从节点验证断线、超时、模型不一致和任务计数；
- 不改变 TC-N1 已验证的 DAG、journal、Provider 和 fencing 语义。

PC Worker 验收后再进入 Android Full Worker。Android Lite 版继续只作为请求端，不接收完整 Worker 任务。

TC-N1 已完成。当前下一项是准入报告中的 **TC-N2.0 传输适配**：只接 hello、版本协商、能力和健康状态，不立即开放任务分发；真实传输、双进程和实机门槛通过前，`adapter_connected` 必须保持 `false`。
