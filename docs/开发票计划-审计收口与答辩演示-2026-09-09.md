# 审计收口与答辩演示开发票计划

> 更新日期：2026-09-12
> 当前票：已完成票为 `1～20、22、25～35、37～43、46～50`（共 44 张）；`HW-DSV4-9B-EVAL-01` 本机跳过并交主节点；`TOOL-RXB-T3` 已完成真实读链路，本机桥接专项下一张为 `TOOL-RXB-C3`
> 排序原则：先做不依赖外部硬件的可复现演示入口，再关闭安全/发布门，最后执行真实运行与完整 UI 验收。

## 票队列

| 顺序 | 票号 | 主题 | 交付 | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 1 | `DEF-P1` | 一键演示模式 | fixture/live 双模式启动器、健康检查、脱敏报告、跨平台入口 | 无 | **已完成** |
| 2 | `AUD-AUTH-01` | 模型接口安全门 | 下载、注册、本地资产接口的认证/来源门设计与测试 | control/gateway 认证边界确认 | **已完成** |
| 3 | `AUD-PIN-01` | 远程工件身份固定 | 四个远程预设的 revision、文件清单和 SHA-256 审阅登记 | 发布源工件可访问 | **已完成** |
| 4 | `AUD-SBX-01` | MiniCPM4 探测沙箱 | `trust_remote_code` 探测迁移到低权限进程/容器 | 部署环境 | **已完成** |
| 5 | `AUD-RT-01` | 真实 runtime smoke | llama-cpp/MiniCPM4 模板、thinking、架构兼容真实冒烟 | 固定工件、运行时依赖 | **已完成** |
| 6 | `AUD-SW-01` | 真实模型切换验收 | load/switch 成功、失败、回滚和量化不匹配断言 | 固定工件、可用运行时 | **已完成** |
| 7 | `AUD-MCP-UI-01` | MCP 负向 UI 门 | 权限、未知工具、坏参数、后端业务错误和 SSE 失败态 | Harness UI | **已完成** |
| 8 | `AUD-CY-E2E-01` | CyberGothic 全量回归 | 完整 Playwright 矩阵和失败产物收口 | Edge/前端环境 | **已完成** |
| 9 | `DEF-P2` | 失败注入演示 | worker 退出/断连→重派→完成的可重复演示 | `DEF-P1`、本机双进程 | **已完成** |
| 10 | `DEF-P3` | 分布式 benchmark | 固定负载、JSON/图表、单机/双机标识 | `DEF-P1`；双机数据后置 | **已完成** |
| 11 | `DEF-P4` | 集群拓扑演示 | readiness 快照、节点状态、层段分配脱敏展示 | `DEF-P2`、现有画布 | **已完成** |
| 12 | `DEF-A1` | 答辩故事线 | 5 分钟脚本、证据链接、翻车兜底 | `DEF-P1`～`P4` | **已完成** |
| 13 | `DEF-A2` | 性能报告整理 | 将 benchmark/TTFT 结果生成结题表格 | `DEF-P3` | **已完成** |
| 14 | `DEF-A3` | 现场检查清单 | 设备、电量、网络、模型、端口和恢复动作检查 | `DEF-P1` | **已完成** |
| 15 | `DEF-A4` | 演示环境重置 | 仅清理本票产生的进程/临时状态，支持重复演示 | `DEF-P1` | **已完成** |

## 已完成票 `AUD-AUTH-01`

验收结果：
- `src/model_api_access.py` 统一提供模型 API 来源门：默认只允许 loopback；跨机访问必须显式设置 `QLH_MODEL_API_TRUSTED_CIDRS`，无效/未配置时 fail-closed。
- 单体 `api_server` 与 `inference-svc` 的模型加载/切换、注册、下载、GGUF、本地资产接口均接入来源门；网关继续负责 Web Bearer 会话和 owner/admin 管理员角色。
- `gateway` 的 registry/GGUF/download 路由现在向 control-svc 透传 Bearer；`control-svc` 默认绑定 `127.0.0.1`，避免绕过网关直接暴露控制面。
- 测试证据：`tests/test_model_api_access.py` 5 passed；gateway 全量 122 passed；gateway/control TypeScript build passed；模型下载和 inference-svc 相关 Python 回归通过。
- 远程部署配置约束已明确：必须由 gateway 提供登录入口，或显式配置受控 Tailnet/CIDR，并在防火墙层限制 control-svc；不能仅依赖默认监听地址。

## 后续票 `DEF-P1`

验收条件：

- 默认 `fixtures` 模式只启动现行前端 `frontend_cybergothic`，不访问模型、网络或真实写接口；已废弃的 `frontend` 目录不参与演示入口。
- `live` 模式必须显式指定，后端和前端仅绑定 loopback，后端 `/api/health` 成功后才报告就绪。
- 每次运行生成 `build/defense-demo/latest.json`，不写绝对路径、凭据或模型输出。
- `--duration` 支持自动退出，便于 CI 和答辩彩排；进程退出时回收本票启动的子进程。
- Windows 和 Unix 均有薄入口，核心逻辑只维护一份 Python 实现。
- `scripts/demo/scenarios.json` 固定登记对话、图像与脱敏拓扑三条预置剧本，启动时只校验 `frontend_cybergothic` 的 fixture 与页面标记，不调用模型、不写真实接口。
- 启动健康等待受 120 秒硬上限约束；缺少现行前端 npm 依赖时立即中文 fail-closed，不把等待超时伪装成成功。

本轮进展：

- `tests/test_defense_demo.py`：`9 passed`。
- fixture 彩排：`--duration 3` 自动退出，现行 `frontend_cybergothic` 的 Vite 就绪且无残留监听，报告写入 `build/defense-demo/fixture-run-final.json`；`DEF-P4` 已将报告扩展为包含 `fixture-dialog`、`fixture-image`、`fixture-topology` 与 `startup-budget` 固定成功步骤。
- `frontend_cybergothic`：`npm run typecheck` 通过。
- live 彩排：后端 `/api/health` 与 Vite 均就绪；使用 loopback HTTP/TCP 端口，4 秒自动退出后无残留监听进程；报告写入 `build/defense-demo/live-run-final.json`。
- `DEF-P1` 收口当时未执行真实模型加载；后续 `AUD-RT-01`/`AUD-SW-01` 已在本机固定 GGUF 与 `llama-cpp-python 0.3.28` 环境完成，见本文件对应完成记录。

## 已完成票 `AUD-MCP-UI-01`

验收结果：

- 新增 `harness_workbench/ui_react/scripts/mcp_negative_ui_gate.mjs`，通过 Vite + Playwright fixture 逐项注入权限拒绝、未知工具、坏参数、后端业务错误和 SSE 业务失败；不访问真实网络、模型或写接口。
- Harness UI 保留 HTTP status、错误码和 `retryable` 信息；HTTP 403/400 与 MCP `isError` 均不会误显示为成功结果，SSE 业务错误保持在线状态并给出失败提示。
- 负向门脚本输出：`{"status":"passed","cases":["permission","unknown_tool","invalid_arguments","backend_error","sse_error"]}`；同时覆盖 malformed JSON 参数和 MCP manifest 中未配置/过期工具。
- 验证证据：Harness `npm run typecheck` 通过；`npm run test:mcp-negative` 通过；`tests/test_harness_mcp_api.py tests/test_harness_mcp_server.py tests/test_harness_ui.py` 为 `14 passed, 1 skipped`。

## 已完成票 `AUD-CY-E2E-01`

验收结果：

- 现行前端 `frontend_cybergothic` 使用系统 Edge 执行完整 Playwright 矩阵，覆盖 API 错误语义、角色/权限、模型下载、会话、生图、审计、集群、任务、响应式、键盘可达、工作台分屏和 SSE fixture。
- 首轮发现并修复两项真实 UI 稳定性问题：移动端上下堆叠时 pane 内容收缩/入场位移造成边界抖动；日志字体基线差异造成容器查询同行断言不稳定。修复集中在 `frontend_cybergothic/src/styles/workbench.css`，不涉及模型或后端协议。
- 验证证据：`frontend_cybergothic/npm run test:e2e` 最终 `64 passed`，使用 Edge + Vite fixture，未启动真实模型、未访问外部服务；失败截图/trace 仅保留在被忽略的 `build/cybergothic-e2e` 产物目录。

## 已完成票 `DEF-P2`

验收结果：

- `scripts/demo/defense_demo.py --mode failure` 以演示进程作为 coordinator，并启动唯一一个 `scripts/demo/failure_worker.py` 子进程；双方只通过随机端口上的 loopback TCP 通信，构成固定的本机双进程演示。
- 故障 worker 完成真实 task-worker v2 握手并接受 Stage 后，以固定退出码 `23` 立即退出；生产 `RemoteFullWorkerProvider` 观察断连并记录 `remote_worker_disconnected`，首 attempt 被标为 `expired`。
- Stage 固定为可安全重试的 pure stage，TaskGraph 仅重派一次到 `demo-local-recovery`，lease epoch 从 `1` 递增到 `2`，结果显式提交后工作流为 `completed`。
- `build/defense-demo/latest.json` 与指定报告仅保留进程类型、状态、provider、重试原因、lease epoch 和退出码；不保留请求、模型输出、集群密钥、绝对路径或动态端口。
- 自动回归：`tests/test_defense_demo.py` 为 `12 passed`，其中包含真实双进程退出/重派/完成验收；相邻 task-worker 断连与独立进程回归为 `2 passed`。独立 `failure` 命令再次彩排成功，约 `7.4s` 完成且无故障 worker 或监听端口残留。
- 本票强制 Hugging Face/Transformers offline，未加载模型、未访问外网，也未依赖已废弃的 `frontend` 目录。

## 已完成票 `DEF-P3`

验收结果：

- 新增 `scripts/demo/defense_benchmark.py` 与跨平台薄入口 `benchmark.bat`/`benchmark.sh`；默认固定负载为 `12` 次计量、`2` 次预热、每轮一个 TaskGraph `full_inference` fixture Stage 和 `288` 字节固定载荷，并记录 workload SHA-256。
- 同一次运行先测 `single_host_single_process` 进程内 fixture，再通过 `scripts/demo/benchmark_worker.py` 的真实 task-worker v2 握手测 `single_host_dual_process` loopback worker；两组均走生产 TaskGraph 提交路径，但不加载模型、不访问外网。
- 每次生成 `build/defense-benchmark/latest.json` 和 `latest.svg`，并支持 `--report`/`--chart` 指定副本；JSON 记录 min/median/mean/p95/max/串行吞吐、拓扑、host/process 数和样本数，SVG 无第三方绘图库依赖。
- 声明防误读门固定为 `real_model_performance=false`、`physical_dual_host_performance=false`；图表醒目标记 `FIXTURE · SINGLE HOST · NOT MODEL PERFORMANCE`，真实双机栏固定 `not_run / physical_dual_host_data_pending`，不会用 loopback 数据冒充双机性能。
- 本机正式彩排（`12+2` 固定负载）：进程内 median/p95 为 `1.553/1.963 ms`，loopback 双进程为 `2.348/3.593 ms`；这些数字仅是当前开发机本次 TaskGraph 控制面 fixture 采样，不是模型 TTFT、tokens/s 或双机收益。
- 验证证据：`tests/test_defense_benchmark.py` + `tests/test_defense_demo.py` 为 `16 passed`；Windows `benchmark.bat` 入口实跑通过；Edge 实际渲染 SVG 正常；worker 退出码 `0`，无残留 benchmark worker。

## 已完成票 `DEF-P4`

验收结果：

- 新增唯一快照源 `frontend_cybergothic/src/data/defense-topology.json`，固定登记 `def-p4-fixture-v1` 的 3 个节点别名、3 项 readiness 检查和 24 层 end-exclusive 分配；只允许 alias/role/state/heartbeat/summary 白名单，不含主机名、地址、硬件、凭据或动态时间。
- 现行 `frontend_cybergothic` 新增独立答辩路由 `#/cluster?fixtures=1&defense=topology`；页面以 `FIXTURE / REDACTED / NOT LIVE` 醒目标注数据边界，展示 readiness、节点状态、拓扑连线和 `L0–L7`/`L8–L23` 层段，并用同一脱敏别名驱动已有 constellation canvas。普通集群管理页保持原行为，不会把快照冒充实时状态。
- `scripts/demo/defense_demo.py` 将拓扑纳入 fixture 前置门：校验声明边界、拒绝 IPv4/越权节点字段、要求节点别名唯一、所有 readiness 通过、层段连续无重叠且完整覆盖。`demo.bat --scenario topology --open` 与 Unix 对应入口可直接打开答辩页；非 fixture 模式使用预置场景会 fail-closed。
- 正式彩排报告 `build/defense-demo/topology-run-final.json` 记录 `nodes=3; layers=24; claim=fixture/redacted/not-live`，直接 URL 正确，启动耗时 `1.672s`，自动退出后端口无残留；未加载模型、未访问真实集群或外部服务。
- 验证证据：`tests/test_defense_demo.py` 为 `16 passed`；现行前端 typecheck、27/27 对比度门与生产构建通过；DEF-P4 Edge 宽屏/390px 专项为 `2 passed`。全量 Edge 同轮中本票和普通集群页均通过，总计 `64 passed`，另有 2 个既有工作台几何时序断言首次抖动，随即定向复跑为 `2 passed`，不涉及本票文件。

## 已完成票 `DEF-A1`

验收结果：

- 新增 `docs/答辩演示-5分钟故事线-2026-09-10.md`：6 段时间轴从 `0:00` 连续覆盖到 `5:00`，串联 P1 单入口、P4 脱敏拓扑、P2 断连重派、P3 benchmark 声明门和真实模型票后置；附开场前命令、90 秒紧急版、高频追问与逐段讲词/屏幕/动作/成功标记。
- 每一段均登记明确的翻车触发条件、替代动作和证据来源；现场复演失败时要求准确声明“本轮不可用”，区分历史彩排、自动测试与当前运行，禁止把测试绿灯口述成现场执行成功。
- 新增机器清单 `scripts/demo/storyline.json` 与跨平台校验入口 `story.bat`/`story.sh`。`scripts/demo/defense_story.py` 强制总时长恰好 300 秒、分段无空洞/重叠、P1～P4 覆盖完整、每段均有兜底、讲稿时间窗一致、所有本地链接已登记，并拒绝 live、旧 `frontend`、模型加载/下载命令及任何越界声明。
- 证据索引在 A1 初始收口时将 10 个仓库内源码/测试/文档列为必需；A2 性能结题表接入后为 11 个。另将 4 个被 `.gitignore` 排除的 `build/` 彩排产物列为可选可再生；换机缺少彩排产物不会伪装成必需证据丢失，也不会让临时产物成为唯一证据。
- 声明门固定为 `real_model_demonstrated=false`、`physical_dual_host_demonstrated=false`、`model_performance_claimed=false`、`external_network_required=false`。性能讲词要求读取开场前新生成的图表，不背诵旧开发机数值。
- A1 初始验证：`scripts/demo/story.bat --report build/defense-story/a1-final.json` 输出 `duration=300s segments=6 evidence=10+4`，`tests/test_defense_story.py` 与 P1～P4 相邻测试合计 `29 passed`；A2 接入后为 `evidence=11+4`，A3 接入后为 `evidence=13+5`，A4 重置证据接入后的最新校验为 `evidence=17+5`。

## 已完成票 `DEF-A2`

验收结果：

- 新增 `scripts/perf_report.py` 与跨平台入口 `scripts/demo/performance_report.bat`/`.sh`，从 P3 JSON 和独立真实模型性能 JSON 生成 `docs/答辩演示-性能结题表-2026-09-10.md` 及脱敏校验报告；工具只读结构化输入，不启动后端、不访问网络、不加载模型。
- P3 输入必须保持 `qlh.defense_benchmark.v1`、`task_graph_control_plane_fixture`、固定 workload identity/SHA、12 个样本、单机 1/2 进程拓扑、worker 正常退出以及 `real_model_performance=false`、`physical_dual_host_performance=false`；样本漂移、分位次序错误或越界声明均 fail-closed。
- 新增 `qlh.real_model_performance.v1` 输入契约：未来真实数据必须绑定模型工件 SHA-256、格式/量化/tokenizer/context、环境、节点别名拓扑、prompt set SHA、token 数/并发/轮数、TTFT/inter-token/E2E/decode/资源指标和重试/回落事件；拒绝额外输出字段、IPv4、身份缺失或 host 数与双机声明不一致。
- 当前使用 `scripts/demo/real-model-performance-not-run.json`，原因固定为 `real_model_environment_unavailable`；结题表的 TTFT、inter-token、E2E、tokens/s、峰值内存和模型加载时间全部显示 `NOT RUN`，不解析缺少完整身份/环境/拓扑 JSON 的旧 `sse_ttft_compare.py` 控制台输出。
- 结题表将 P3 两组数据单列为 TaskGraph 控制面 fixture：进程内 median/p95 `1.553/1.963 ms`，loopback 双进程 `2.348/3.593 ms`；吞吐单位明确为串行 `tasks/s`，不是 tokens/s。结论资格矩阵明确真实模型、物理双机、模型质量和通用加速比当前均不可用。
- A1 第 5 段现直接引用性能结题表，开场准备在 benchmark 后自动重建汇总；A2 收口时故事线校验为 `duration=300s segments=6 evidence=11+4`。`tests/test_defense_performance_report.py` 与 P1～P4/A1 相邻测试合计 `36 passed`，生成报告敏感扫描为空。

## 已完成票 `DEF-A3`

验收结果：

- 新增 `docs/答辩现场检查清单-2026-09-10.md`，按 T-30/T-20/T-15/T-10/T-2 五个时间点覆盖设备、供电、投屏、loopback、网络边界、模型口径、5174/8000/8001 端口、证据页、计时与最后人工门；明确 20 秒动态演示止损线和 90 秒紧急故事线。
- `scripts/demo/checklist.json` 固化 7 类、12 项自动检查、7 项人工确认和 7 条恢复动作；每个 ID 必须出现在清单正文，恢复动作必须绑定仓库内证据或明确标注为可再生的 `build/` 证据。
- 新增 `scripts/demo/defense_preflight.py` 与 `checklist.bat`/`.sh`：只探测 Python、Node/npm、现行 `frontend_cybergothic` Vite、Edge、磁盘、电源状态、TCP loopback、端口和证据入口；不探测外网、不枚举模型文件、不加载模型。JSON 报告只含版本、容量、状态和仓库相对引用，不含进程命令、绝对路径或凭据。
- 端口策略明确：5174 是 fixture 阻断门；8000/8001 是 live/API/cluster 的非阻断提示；P2/P3 使用动态 loopback。端口占用只记录 `owner=not-inspected`，要求人工确认归属或改端口，禁止按端口号盲杀非本票进程。`--strict-warnings` 可让任何提示以退出码 1 阻止正式上台。
- 模型检查固定为 `DEFERRED / non-blocking` 并保留 `real_model_claim=false`；缺模型不会阻断 fixture，现场预检也不会用历史 `AUD-RT-01`/`AUD-SW-01` 验收结果冒充本次现场模型已加载。
- 本机正式预检：Python 3.12、Node/npm、Edge、119.84 GB 可用空间、电源接通且电量 100%、loopback、5174、8001、11 个证据文件和 10 个跨平台入口通过；8000 被既有进程占用，仅产生 1 个非阻断 warning，未检查或终止占用者。
- A1 开场准备已先执行 reset 与 checklist；A4 接入后预检要求 15 个证据文件和 12 个跨平台入口，最新故事线校验为 `duration=300s segments=6 evidence=17+5`。A3 收口时专项与 P1～P4/A1/A2 相邻测试为 `45 passed`；全程未加载模型或访问外部服务。

## 已完成票 `DEF-A4`

验收结果：

- 新增 `scripts/demo/demo_ownership.py`：P1/P2 启动器与 P3 benchmark 为自己创建的 frontend、backend、failure-worker、benchmark-worker 写入崩溃可恢复的所有权票据，并持续捕获其进程树；正常退出在进程全部死亡后自动撤销票据。
- 票据只记录 PID、创建时间、固定进程类型、根 PID 和登记的 loopback 端口，不记录命令正文或秘密；重置时不信任票据内容，必须按 PID 创建时间、精确工作目录和参数/路径级内置白名单重新验证，防止 PID 复用或篡改票据扩大终止权限。
- 新增 `scripts/demo/defense_reset.py` 与 `reset.bat`/`.sh`。默认模式只 dry-run；显式 `--apply` 才会停止身份复核通过的本票进程，并仅删除已死亡、端口已释放的对应票据及严格命名的原子写入残片。损坏票据、身份错配、无法停止或端口仍占用统一返回 `REVIEW_REQUIRED`，保留现场供人工确认。
- 新增 `docs/答辩演示-环境重置说明-2026-09-10.md`，并将 reset 作为故事线与现场清单的第一步。模型目录/缓存、数据库、会话、用户配置、IDE/浏览器、未登记进程与端口、以及全部脱敏演示证据均明确排除在清理范围外。
- 实机故障恢复门使用现行 `frontend_cybergothic` 的 Vite 进程树模拟启动器异常退出：dry-run 发现 5 个登记进程且停止数为 0；apply 精确停止 5 个、释放本轮动态端口并撤销票据；第二次 apply 为零票据，证明可重复且幂等。
- 验证证据：A4 隔离/篡改/命令白名单/幂等/实机门共 `9 passed`（实机门串行运行）；P1～P3/A1/A3 相邻回归并行结果为 `46 passed, 1 skipped`，被跳过项即需串行的同一实机门，随后单独执行为 `1 passed`。未加载或枚举模型，未访问外网，未检查或终止既有 8000 端口占用者。

## 待完成统计

审计报告中的 8 项待完成工作已拆为 `AUD-AUTH-01`、`AUD-PIN-01`、`AUD-SBX-01`、`AUD-RT-01`、`AUD-SW-01`、`AUD-MCP-UI-01`、`AUD-CY-E2E-01` 七张票；认证/来源门单独作为第一张审计票，其余真实运行/自动化项目各一票。答辩工具另排 8 张票（`DEF-P1`～`P4`、`DEF-A1`～`A4`），合计 **15 张开发票**。

真实运行票使用本机固定 GGUF 和系统 Python 中的 `llama-cpp-python 0.3.28` 完成，没有以 fixture 或前端构建替代真实加载。当前 15 张票中 **15 张已完成、0 张待完成**。DS3 的模板、GGUF 结构/哈希和转换计划已通过；其 7B 权重加载因本次可用内存低于保守门槛未强行执行，不属于第 5 票规定的 MiniCPM4 runtime 验收范围，也不据此声明 DS3 推理性能。

## 已完成票 `AUD-RT-01`

- 三个亚 1B 模型的本地 tokenizer 模板均在 `windows-restricted-token-low-integrity` 沙箱中真实执行；Qwen3 的 `enable_thinking` 开关被识别并完成 enabled/disabled 渲染，Qwen2.5 与 MiniCPM4 明确记录为 `accepted_but_undeclared`，不伪称原生 thinking 开关。
- MiniCPM4-0.5B Q4_K_M 由真实 `llama_cpp` 加载并完成 4/4 次非空生成，GGUF 架构元数据与转换器登记一致；三个亚 1B GGUF 合计完成 12/12 次非空生成。
- 严格输出质量与 runtime 兼容性分门：Qwen3/MiniCPM4 在 exact JSON/长上下文 marker 上未全过，但加载、模板和生成链路通过，因此不再被误报为架构不兼容。
- DS3 的模板执行、GGUF 结构与 sidecar SHA、Qwen2 架构映射、Q4_K_M 转换工具链/磁盘计划均通过；运行证据保存在可再生的 `build/audits/AUD-RT-01-*.json`，持久结论见 `docs/agent_tool/audits/AUD-RT-01-model-startup-and-harness-2026-09-10.md`。

## 已完成票 `AUD-SW-01`

- 真实路径完成 Qwen2.5-0.5B 加载/生成、切换到 Qwen3-0.6B、未注册目标预检拒绝、缺失 GGUF 触发加载失败并回滚，以及 PyTorch + GGUF 量化值不匹配的 400 拒绝；`tests/test_real_model_smoke.py` 实跑 `2 passed`。
- 实测发现 `switch_model()` 会在确认目标未注册前先卸载健康模型，导致失败后活跃模型清空。现将未注册目标拒绝提前到卸载前，返回 `active_model_preserved=true` 与 `requested_model_id`，且不触碰现有引擎。
- 已有“加载后失败再回滚”路径仍保留并在真实缺失工件场景验证：返回 `MODEL_LOAD_FAILED_ROLLED_BACK` 后 Qwen3 仍处于已加载状态。
- 自动回归新增“未知目标不得卸载健康模型”和 runtime/quality 分门断言；专项代码回归为 `78 passed, 4 skipped`，其中 2 个真实模型用例随后在真实运行时单独通过。

## `AUD-PIN-01` 完成记录

- 新增 `docs/agent_tool/model-artifacts/remote-model-pins-2026-09-09.json`，登记四个远程模型的官方仓库、完整 commit revision、允许文件集、必需文件和逐权重 SHA-256。
- `src/model_download_jobs.py` 对四个新 preset 强制读取并校验 pin；Hugging Face 下载固定 `revision` 与 `allow_patterns`，落盘后校验必需文件、精确权重集合和逐文件 SHA-256，失败时不发布目标目录。
- 回归证据：`tests/test_model_download_jobs.py` 为 `15 passed, 1 skipped`；未执行真实模型下载，避免在当前环境消耗大模型带宽和磁盘。

## 已完成票 `AUD-SBX-01`

- 新增 `scripts/model_tools/sandbox_runner.py`：Linux 使用 bubblewrap user/pid/network/mount namespace，macOS 使用只读禁网 profile，Windows 使用 restricted token、low-integrity token 和 Job Object；没有可用 OS 后端时明确返回 `os_sandbox_unavailable`，不降级为普通子进程。
- `small_model_probe` 先把非权重 metadata 和经允许的远程代码复制到一次性 snapshot；Windows snapshot/cache/temp 目录施加低完整性标签，worker 自身校验 sandbox 标识后才执行 tokenizer。
- 实机证据：当前 Windows 主机以 `windows-restricted-token-low-integrity` 启动 worker；MiniCPM4 本地模板探测在 sandbox 内完成，模板渲染通过、`thinking` 结果为 `accepted_but_undeclared`、未加载权重。
- 回归证据：`tests/test_small_model_probe.py` 与 `tests/test_model_probe_sandbox.py` 共 `9 passed`；Windows 低完整性本身不提供内核级禁网，因此仍强制 `HF_HUB_OFFLINE`/`TRANSFORMERS_OFFLINE`，真实 runtime 和网络隔离增强留在后续环境验收中。

## 发散规划转票（2026-09-09，按票实施）

> 来源：2026-09-09 发散讨论（docagent 易用性/定点审查 + harness 上下文/RAG 压榨 + 榨干模式调研）。本批最初均为规划票；自 2026-09-10 起按队列逐票开工，每票单独附实现与验收门。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 16 | `DOC-ENV-01` | docagent 环境变量配置便利化 | `.env.docagent` 读取/校验/提示增强：字段级校验与默认值提示、脱敏检查（sk 不落日志/输出）、`--env` 显式指路与 profile 化配置、缺失时 fail-closed 中文提示 | docagent 独立仓库（已独立） | **已完成（2026-09-10）** |
| 17 | `DOC-AUDIT-ENTRY-01` | docagent 指定条目定点审查 | 针对**指定文档条目**核对实际状态（如"某验收条目表述与实际证据不一致/文档落后"）：按条目/锚点定位 → 提取声明 → 对照 git 提交/测试证据/工作区状态 → 输出"表述 vs 实际"差异清单（复用 R1/R3 信号，但定点+可指定路径而非全库扫描） | `DOC-ENV-01`（配置入口）或独立 | **已完成（2026-09-11）** |
| 18 | `HW-CTX-SQZ-01` | harness 上下文进一步压榨 | 分层预算扩展、更多压缩策略（verbatim/掩码/结构化 STATE 变体）、与长期记忆联动的折叠策略、超预算降级曲线 | harness S1/S8 | **已完成（2026-09-11）** |
| 19 | `HW-RAG-SQZ-01` | harness RAG 进一步压榨 | 检索质量（chunking/混排）、embedding provider 可替换性落地、容量预算与引用边界强化、跨会话检索复用 | harness S4 RAG | **已完成（2026-09-11）** |
| 20 | `HW-R1` | 榨干小模型潜力模式调研 | 上限研究问题清单与首批实验设计（模型上限构成/主导因素消融/多模型协同帕累托/v2 口径真实上限/公开证据对照） | 指向[发散文档 §2.4](项目后续可做方向发散与子项目独立化评估-2026-09-09.md)（H-R1） | **已完成（2026-09-11）** |

> 说明：本批 6 张不计入 §待完成统计的 15 张开发票；`DOC-ENV-01`、`DOC-AUDIT-ENTRY-01`、`HW-CTX-SQZ-01`、`HW-RAG-SQZ-01`、`HW-R1`、`EX-CTX-MEAS-01` 均已完成，按当前资源约束继续转入批次 2 的纯软件票。

### 已完成票 `DOC-ENV-01`

- 初始化远端非空子项目 `tools/docagent`，在其 `main` 分支完成独立实现；主仓只通过 submodule gitlink 引用，不把子项目源码复制回主项目。
- 新增严格、专用的 `.env.docagent` 合同与安全示例：只读取专用文件，不合并主 `.env` 或进程环境；拒绝未知/重复字段、不安全 URL、非 loopback Ollama 地址、越界 profile 路径及不完整远程配置。
- 新增 `docagent config` 和 `--env`：显式配置文件不存在或内容无效时以退出码 2 和中文提示 fail-closed；扫描未配置 env 时保留既有机械扫描兼容性；profile 优先级为 `--profile` > `DOCAGENT_PROFILE` > 既有默认。
- 输出只报告配置来源、默认值、连接范围及密钥是否已配置；API key 与完整远程 endpoint 不进入对象表示、stdout、stderr、JSON 或扫描报告。独立扫描器本票不发起 LLM/网络请求，provider 调用仍属于后续 M2 迁移边界。
- 本机验收未加载任何模型：语法检查通过；docagent/M1-M3 相关轻量回归 `135 passed`，其中独立环境与兼容入口聚焦回归 `29 passed`。
- 后续票：`DOC-AUDIT-ENTRY-01` 已完成，后续转入 `HW-CTX-SQZ-01`。

### 已完成票 `DOC-AUDIT-ENTRY-01`

- `tools/docagent` 新增只读命令 `audit-entry`：必须给出仓库相对 `--doc`，并在 `--entry`（文字条目）与 `--anchor`（标题锚点）中二选一；文字命中歧义时 fail-closed，可用一基 `--occurrence` 显式消歧。
- 审计只读取目标行或目标标题章节，不递归扫描全库。输出 `qlh.docagent.entry-audit.v1` JSON/Markdown/text，结构化记录声明、生命周期口径、R1/R3 定点信号、Git 最新提交、工作区状态、证据文件存在性及“表述 vs 实际”差异；报告固定声明 `read_only=true`、`tests_executed=false`。
- 支持重复 `--evidence` 绑定仓库内证据；绝对路径、路径穿越、docs 目录内输出及歧义/缺失选择器均以退出码 2 拒绝。测试计数若没有机器可读结果工件，只报告 `TEST_RESULT_UNBOUND`，不把文档中的 `passed` 字样冒充本轮实测。
- `--env` 与 profile 优先级复用 `DOC-ENV-01`；`--fail-on none|info|warn|error|R1|R3` 可用于信息采集或 CI 门。声明文本对 API-key 形态、Bearer 和绝对路径脱敏，报告仅保留仓库相对证据路径。
- 同票修复 R2 工作区漏报：Git porcelain 的前导空格不再被 `strip()` 删除，仅 unstaged 的文档修改可被稳定识别。独立子项目回归为 `39 passed`，主项目 docagent 邻接回归为 `53 passed`；定点自审见 [DOC-AUDIT-ENTRY-01 审计记录](agent_tool/audits/DOC-AUDIT-ENTRY-01-2026-09-11.md)。
- 后续票：`HW-CTX-SQZ-01` 已完成，下一票为 `HW-RAG-SQZ-01`。

### 已完成票 `HW-CTX-SQZ-01`

- 扩展 `harness_workbench/context_engine`：新增 `adaptive`、`state`、`verbatim`、`mask` 四种显式压缩策略，结构化 STATE 支持 `compact`、`lines`、`nonempty` 三种稳定变体；默认 `adaptive` 保持既有行为并在 STATE 不适配时降级到完整消息 verbatim，再退到窗口裁剪。
- 扩展 `LayeredBudget`：新增可选 `state_budget`，预算账本现在可独立核算 memory/RAG/STATE/recent context 四层，旧三层调用保持兼容；适配变体 digest 同时绑定这些策略字段，避免策略漂移后复用旧实验结果。
- 长期记忆联动：上下文构建支持按 owner scope 和 query 有界召回完整记忆条目，保留 entry id 引用；召回与折叠均不跨用户 scope，不切片单条记忆，预算不足时明确 `context.memory_recall_omitted`。
- 新增可审计降级曲线：每个 snapshot 记录 mask、state/verbatim、window、memory_recall 步骤的前后 token、动作和详情；不静默截断轮次，工具输出仍遵循最近一次保留策略。
- 离线验收：新增 `tests/test_harness_context_squeeze.py`，覆盖 30 轮 fixture 的策略矩阵、轮次边界、STATE→verbatim 降级、记忆 scope/引用和四层预算；专项及邻接 harness 回归 `27 passed`，未加载模型、未联网。
- 后续票：`HW-RAG-SQZ-01` 已于 2026-09-11 收口，当前执行票为 `HW-R1`。

### 已完成票 `HW-RAG-SQZ-01`

- `harness_workbench/rag/query.py` 提供无模型、可复现的 NFKC 查询归一化与有界别名改写；每次检索保留原查询和实际变体，最多 8 路，避免无界扩写。
- `rag/retriever.py` 提供 `HybridRagRetriever`：FTS 多路召回可叠加可替换 `EmbeddingProvider`，以加权 RRF 融合并按 chunk ID 去重；provider 异常自动回退 FTS，并输出路由计数和候选数。
- `RagStore` 增加 sentence/paragraph/fixed 三种分块粒度、source/title 元数据过滤、owner scope 硬隔离、SQLite 快照校验的跨会话查询缓存；缓存只按查询/配置摘要复用，文档快照变化即失效。
- `build_context` 同时执行字符与 token 预算，整块保留 citation，超限逐块记录 `chars`/`tokens`/`invalid` 原因，不做静默半截；API 与 MCP 均支持增强检索、过滤和双预算参数。
- 离线验收：新增 `tests/test_harness_rag_squeeze.py`，覆盖查询改写、三种分块、混合融合与 owner 隔离、provider 降级、跨会话缓存失效、元数据过滤、token 引用边界；专项 `7 passed`，S4 回归 `9 passed`，完整 harness/docagent/doc-maintenance 回归 `232 passed, 1 skipped`，未加载模型、未联网。
- 后续票：`HW-R1` 已于 2026-09-11 收口，当前执行票为 `EX-CTX-MEAS-01`。

### 已完成票 `HW-R1`

- 新增 `harness_workbench/research/ceiling.py` 研究合同：固定 5 个模型研究对象（Qwen2.5-0.5B、Qwen3-0.6B、MiniCPM4-0.5B、QW1.8B、DS3-0324-7B）、6 个问题和 6 个因素，生成稳定的 baseline/ablation/policy/roles 实验单元及 study digest。
- 首轮问题覆盖：上下文窗口与早期事实召回、模板/stop 与格式率、STATE/检索记忆成本、单模型与 draft/verify 协同、Q4_K_M/Q8_0 成对量化、v1 `192 + normalized_contains` 与 v2 `512 + loose_contains` 判题口径；每个单元要求 holdout，单因素变化并保留 seed。
- 提供 `compare_factor` 和 `pareto_points`，只输出方向性差异、回归和支配关系，不自动选择生产赢家；`EvidenceRecord`/`evaluate_evidence_gate` 对 profile/artifact/fixture digest、runtime、runner、权重和网络状态做准入边界，fixture/injected 结果永远保持 candidate。
- 登记公开证据边界：Qwen2.5/Qwen3 技术报告、MiniCPM4 官方项目资料、llama.cpp 量化文档只用于定义可核对的公开主张和实验对照，不替代本机质量、成本或资源实测。
- 离线验收：新增 `tests/test_harness_ceiling_research.py`，覆盖 study digest 稳定性、输入边界、因素差异、Pareto 支配、fixture 结果降级和真实证据门；专项 `6 passed`，未加载模型、未联网。
- 后续票：`EX-CTX-MEAS-01`；真实模型执行、三轮标定、RSS/VRAM 与公开分数复核不在本票内。

### 已完成票 `EX-CTX-MEAS-01`

- 新增 `harness_workbench/research/context_measure.py`：固定 30 轮对话和 3 条早期事实，按 64/96/128/192/256/384 输入 token 预算分别运行滑窗（`window`）、STATE 摘要（`state`）和长期记忆（`memory`）三条路径。
- 每个 cell 输出 early-fact recall、输入/STATE/记忆 token、遗漏消息、记忆写入/召回数、压缩策略和 fixture digest；`ContextMeasureReport.as_dict()` 提供可序列化 JSON，`to_markdown()` 提供论文/答辩表格和 chart-ready `series`。
- 全程使用确定性 tokenizer、规则摘要器和隔离 SQLite memory store；报告固定 `runner_kind=fixture`、`weights_loaded=false`、`network_used=false`，不把早期事实召回曲线解释为 QW1.8B 真实回答质量。
- 离线验收：新增 `tests/test_harness_context_measure.py`，覆盖 30 轮 fixture、digest 稳定性、三策略/预算矩阵、滑窗/STATE/记忆差异、JSON/Markdown 产物和输入边界；专项 `5 passed`。
- 观测摘要：滑窗在本 fixture 的 6 个预算点均为 `0/3`；STATE 在 128 token 达到 `3/3`；长期记忆在 192 token 达到 `3/3`。这些是策略装配门证据，不是模型能力结论。

后续票：`TOOL-JUDGE-POLICY-01`。

### 已完成票 `TOOL-JUDGE-POLICY-01`

- 新增 `harness_workbench/tools/judge_policy.py`：锁定 v1 `192 + normalized_contains` 与 v2 `512 + loose_contains` 两套政策，对同一 completion 只在内存中判定并输出状态、差异原因和计数，不持久化 completion 原文。
- 支持从 rubric mapping/JSON 文件加载 correctness 条目、SHA-256 校验、公共 prompt ID 对齐；报告记录 v1/v2 rubric 身份、prompt-set 是否一致、输出摘要 hash、`rescued/regressed/unchanged/invalid` 分类和 fixture-only 边界。
- `JudgePolicyReport.as_dict()` 提供可审计 JSON，`to_markdown()` 提供答辩表格；内置 calibration fixture 演示同一输出由 v1 `2/4` 到 v2 `4/4`，其中 2 条被 v2 救回。该结果只证明判题工具口径差异，不证明任何模型质量。
- 离线验收：新增 `tests/test_harness_judge_policy.py`，覆盖内置标定、digest/Markdown 稳定性、缺失/截断/跳过、项目 rubric 加载、SHA 门和政策 kind 漂移；专项 `5 passed`。

后续票：`TOOL-MANIFEST-HLTH-01`。

### 已完成票 `TOOL-MANIFEST-HLTH-01`

- 新增 `harness_workbench/tools/manifest_health.py`：只读扫描模型资产、sidecar SHA 声明、JSON manifest/lock/index 条目和仓库级 `.gitignore` 命中情况；默认只读元数据，`verify_hash=True` 才显式读取完整文件计算 SHA-256。
- 报告固定 `read_only=true`、`network_used=false`、`weights_loaded=false`，输出稳定 digest、JSON/Markdown 两种审计产物，并跳过 symlink/junction，避免扫描越界。
- 当前 `models/` 静态结果：扫描 22 个文件、3 个模型资产，模型资产全部命中 `.gitignore`；Qwen GGUF sidecar 为 `declared`、两片 safetensors 为 `aggregate_declared`，index 清单检查 195 个条目通过；Gemma lock 引用的 `main_gguf`/`mmproj` 当前缺失，报告明确为不通过，不将缺失资产伪装为可用模型。
- 离线验收：新增 `tests/test_harness_manifest_health.py`，覆盖仓库级 ignore、sidecar 校验、manifest 条目、路径穿越拒绝、默认声明模式、显式 hash mismatch 和缺失根目录；专项 `6 passed`，与相邻 harness 工具回归合计 `36 passed`。

本票只完成资产清单与发布边界体检，不加载 QW1.8B 或其他模型，也不声明模型质量。下一票进入 `TOOL-CTX-RESS-01`。

### 已完成票 `TOOL-CTX-RESS-01`

- 新增 `harness_workbench/tools/ctx_ressure.py`：复用 30 轮固定 fixture 和既有 `ContextPolicy`，按预算矩阵运行 `window`/`state`/`memory` 三策略，输出折叠、早期事实召回、记忆写入/召回和 chart-ready 曲线。
- 新增六项不变量检查：矩阵完整、输入预算不越界、fixture/seed provenance 一致、召回曲线不随预算回退、记忆召回不超过写入且非 memory 策略无记忆副作用、确实观测到 30 轮折叠。
- 提供 `python -m harness_workbench.tools.ctx_ressure` CLI，支持 `--budgets`、`--seed`、`--json` 和 `--markdown`，默认输出答辩 Markdown；报告 schema 为 `qlh.harness.ctx_ressure.v1`，固定 `runner_kind=fixture`、`network_used=false`、`weights_loaded=false`。
- 当前默认矩阵 18 个 cell 全部通过：`window` 在本 fixture 未召回早期事实，`state` 首次达到 3/3 的预算为 128，`memory` 首次达到 3/3 的预算为 192；这些是上下文装配曲线，不是模型回答质量。
- 离线验收：新增 `tests/test_harness_ctx_ressure.py`，专项 `6 passed`；与上一票 manifest 及相邻工具回归合计 `42 passed`。

本票完成 ContextPolicy 压力测试和早召回报告入口，不加载 QW1.8B、不联网、不把 fixture 曲线解释为真实模型能力。下一票进入 `TOOL-REDTEAM-LAB-01`。

### 已完成票 `TOOL-REDTEAM-LAB-01`

- 新增 `harness_workbench/tools/red_team_lab.py`：复用既有 `RedTeamGate`，逐条演练 prompt injection、tool authorization、image path、context injection 四类 12 条攻击样本，并附 1 条完全满足本地 allowlist/verified/production gate 的安全工具调用作为放行对照。
- 报告记录期望/实际 block 或 allow、结构化 reason、schema 状态、未授权放行和 fixture digest；故意不回显攻击 payload/原始注入文本，避免答辩报告携带危险样本。
- CLI 支持 `--list`、`--family`、`--fixture`、`--without-safe`、`--json`、`--markdown`；期望结果漂移或未授权放行时返回非零退出码，默认 fixture-only、无模型、无网络。
- 当前默认演练：12 条攻击全部拦截，安全本地工具调用放行，`mismatch=0`、`unauthorized_pass=0`。
- 离线验收：新增 `tests/test_harness_red_team_lab.py`，覆盖全量演练、过滤、期望漂移、digest/脱敏、CLI 报告和失败码；专项及原红队回归 `11 passed`。

本票只证明既有安全门可逐条复演和审计，不代表模型生成安全性或真实工具执行已验收。下一票进入 `TOOL-TRACE-RPL-01`。

### 已完成票 `TOOL-TRACE-RPL-01`

- 新增 `harness_workbench/tools/trace_replay.py`：将项目进展记录和验收清单中已确认的双机事实归一化为四条脱敏时间线，覆盖 8 月 20 日 QW1.8B 分层验收、Full Worker 重启恢复、远端断连→过期→一次重派→主节点完成、Tailnet IPv6 双机完成；不把缺失的原始日志伪装成实时采集结果。
- `TraceEvent`/`TraceScenario` 保留事件顺序、状态、来源锚点和别名化 workflow ref；输入 JSON 可用 `--input` 回放，原始地址、绝对路径、凭据字段在进入报告前拒绝，工作流标识只保留稳定短 hash。
- CLI：`python -m harness_workbench.tools.trace_replay --list` 列出场景；`--scenario`、`--kind`、`--date` 支持答辩时缩小时间线；`--json`/`--markdown` 生成报告。报告固定校验顺序、终态、来源相对路径、载荷脱敏和离线/无权重边界。
- 当前默认回放：4 个场景、13 个事件，覆盖 8 月 20 日 QW1.8B 分层验收和 8 月 21 日任务链/IPv6 事实；`events_ordered`、`terminal_completion`、`source_refs_relative`、`payloads_redacted`、`offline_boundary` 五项检查全部通过；不启动服务、不访问网络、不加载 QW1.8B。
- 离线验收：新增 `tests/test_harness_trace_replay.py`，专项 `10 passed`；覆盖默认场景、恢复转移、过滤、digest/Markdown 稳定性、规范化输入、schema/原始地址拒绝、时序不变量和 CLI 产物。

本票完成答辩展示用的双机事实时间线回放入口；它不替代原始日志留存、长时双机复验或真实模型质量验收。下一票进入 `TOOL-PROMPT-LAB-01`。

### 已完成票 `TOOL-PROMPT-LAB-01`

- 新增 `harness_workbench/tools/prompt_lab.py`：复用 `PromptProfile` 和 `render_prompt_messages()`，对同一组规范化对话执行多模板 A/B 渲染；内置 QW1.8B/Gemma-small 两套 profile 家族，默认 2 个 profile × 3 个 case = 6 个渲染单元。
- 报告比较 system prompt、stop、thinking、tool mode、structured-output policy 的字段差异，并记录每个 profile/case 的渲染 digest、消息数、字符数和确定性 token 估算及 B-A delta；不回显 system prompt 或消息正文。
- CLI：`python -m harness_workbench.tools.prompt_lab --list` 列出内置 profile/case；`--profile`、`--case` 支持缩小矩阵；`--profile-file`/`--case-file` 接收 `qlh.prompt_lab.v1` 规范化 JSON；`--json`/`--markdown` 生成答辩报告。
- 报告固定检查渲染矩阵完整性、身份引用、system 注入次数、profile digest 差异、字段 diff、原文省略和 fixture/offline 边界；输入绝对路径、未知 schema、重复 ID 均 fail-closed。
- 当前默认 A/B：`qwen_chat_v1-minimal` 与 `qwen_chat_v1-structured` 的可解释差异为 `system_prompt` 与 `structured_output`，structured 版本在三个 case 均增加 27 字符、3 个估算 token。
- 离线验收：新增 `tests/test_harness_prompt_lab.py`，专项 `9 passed`；覆盖默认矩阵、digest/脱敏、profile/case 过滤、Gemma 家族、自定义 JSON、schema/路径/重复 ID 拒绝和 CLI 产物。

本票完成 PromptProfile 差异与渲染成本的可复现对照，不执行 QW1.8B 或其他模型，不把 token 估算写成真实 tokenizer/质量结论。下一票进入 `TOOL-BENCH-LDG-01`。

### 已完成票 `TOOL-BENCH-LDG-01`

- 新增 `harness_workbench/tools/benchmark_ledger.py`：读取结构化 benchmark JSON，归一化为可引用记录行、单机/双机/未运行分组和有限指标摘要；不解析自由文本日志，不补猜缺失指标。
- 默认收口既有 `qlh.defense_benchmark.v1` 与 `qlh.real_model_performance.v1`：4 行记录分别为进程内 fixture、loopback worker fixture、物理双机 `not_run`、真实模型 `not_run`；控制面 `throughput_tasks_per_second` 保留为 `tasks/s`，不改标为模型 `tokens/s`。
- `--input` 可重复接收散落 JSON；`--root` 递归扫描并忽略不支持 schema 的文件；支持 `qlh.benchmark_ledger.v1` 的 `records[]` 输入，可聚合多模型记录。报告保留来源相对路径和稳定 source/report digest。
- 记录拒绝绝对路径、IP/IPv6、凭据字段、未知 schema、非有限数字和重复 ID；每行显式标记 `claim_class`、`claim_scope`、`eligible_for_claim`，物理双机和模型性能缺失保持 `NOT RUN`。
- 当前默认 ledger：4 条记录、单机控制面 2 条、未运行 2 条；来源/数值/声明资格/离线边界检查全部通过。
- 离线验收：新增 `tests/test_harness_benchmark_ledger.py`，专项 `10 passed`；覆盖默认边界、digest/Markdown、多模型聚合、输入拒绝、root 忽略、相对来源、重复 ID、构造器和 CLI 产物。

本票完成散落实验 JSON 到答辩引用汇总表的只读聚合入口；不启动服务、不访问网络、不加载 QW1.8B，也不把控制面 fixture 或功能验收改写成模型性能。下一票进入 `TOOL-MODEL-CARD-01`。

### 已完成票 `TOOL-MODEL-CARD-01`

- 新增 `harness_workbench/tools/model_card.py`：复用 `manifest_health` 的工件、sidecar、lock/index 结果，并用有界 GGUF 头解析器只读取白名单元数据；不物化 tokenizer 大数组，不读取 tensor 数据，不启动推理后端。
- 模型卡 schema 为 `qlh.harness.model_card.v1`，合并内置 `ModelProfile` 的 revision/backend/候选能力状态和 GGUF 声明；输出模型身份、工件字节数、SHA 声明状态、量化、上下文、tokenizer、manifest 缺口、证据相对路径、稳定 digest 与限制项。
- CLI：`python -m harness_workbench.tools.model_card --root models --model-id QW1.8B --json build/model-card/latest.json --markdown build/model-card/latest.md`；也可用 `--health-json` 复用已有体检 JSON，或用 `--profile` 加载规范化画像。
- 当前 QW1.8B 卡识别 GGUF v3、195 个 tensor、20 项元数据、`qwen` 架构、`Q4_K_M`、tokenizer `gpt2` 和 GGUF 声明上下文 8192；同时保留内置 profile 的 `candidate`、`production_eligible=false`、运行上下文 4096 差异说明，不把任一声明解释为质量结论。
- 当前扫描 22 个文件、3 个权重工件、2 个 manifest；模型卡结构校验通过，但因 Gemma lock 的 `main_gguf`/`mmproj` 缺失而显式标记 `incomplete`，没有把整个资产目录伪装成完整发布包。
- 离线验收：新增 `tests/test_harness_model_card.py`，专项 `11 passed`；覆盖合成/损坏 GGUF 头、画像合并、manifest 缺口、缺失根目录、digest/脱敏、schema/路径拒绝和 CLI 双产物；完整 harness 回归 `194 passed, 1 skipped`。

本票完成 manifest/GGUF 元数据到答辩模型卡的只读生成入口；固定 `weights_loaded=false`、`network_used=false`，不做模型冒烟，也不声明延迟、内存、正确率或生成质量。下一票进入 `TOOL-DL-RUNNER-01`。

### 已完成票 `TOOL-DL-RUNNER-01`

- 新增 `harness_workbench/tools/download_runner.py`：接收 `qlh.download_manifest.v1`，要求非浮动 revision、逐文件 SHA-256/size pin、HTTPS 无凭据来源和仓库相对目标路径；拒绝绝对路径、路径穿越、重复文件和 reparse point。
- 下载状态写入目标旁的 `.part` 与 `.part.json`，按有界 `Range` 分片续传；成功分片重置连续失败计数，临时 5xx/超时按上限重试；服务端忽略 Range 时安全回到零点重传。
- 完成后先全量 SHA-256，再原子改名发布目标并原子写 `.sha256` sidecar；已有匹配目标直接 `already_ready`，已有不匹配目标拒绝覆盖。发布后复用 `manifest_health`，health error 会阻断报告 valid。
- `MemoryDownloadTransport` 提供无网络 Range/失败注入夹具；`UrllibDownloadTransport` 明确默认 disabled。CLI `python -m harness_workbench.tools.download_runner --manifest PATH` 默认只生成 plan，只有调用方显式注入 transport 并允许执行时才会传输；本轮不触发真实模型下载。
- 离线验收：新增 `tests/test_harness_download_runner.py`，专项 `13 passed`；覆盖 manifest pin、plan 边界、首传/sidecar、分片续传、重试、幂等、SHA 失败不发布、Range 忽略回退、目标保护、网络禁用和 CLI 双产物。

本票完成模型下载的可复现编排与发布前校验，但没有下载新模型、没有加载权重、没有做模型冒烟；下一票进入 `TOOL-API-WB-01`。

### 已完成票 `TOOL-API-WB-01`

- 新增 `harness_workbench/tools/api_workbench.py`：以显式 `ProbeCase` 对照 harness `/healthz`、`/v1/models`、`/v1/chat/completions`、SSE 与主项目 `/api/health`、`/api/models`、`/api/chat`、`/api/chat/stream`，记录请求摘要、状态码、响应形状、错误码和有界耗时样本。
- 默认使用确定性 `MemoryAPITransport` fixture；`UrllibAPITransport` 必须显式 `enabled=True` 才允许 HTTP，路径、header、payload 和报告均做 fail-closed/脱敏处理，不保存 prompt、message、响应正文、凭据或绝对地址。
- 默认 5 个 case 离线验收为 `4 matched / 1 drifted / 0 failed`：`chat-invalid` 明确记录 harness `400 invalid_messages` 与主项目 `422 validation_error` 的契约漂移；流式 case 按 SSE 完成事件归一化后匹配。报告固定 `network_used=false`、`weights_loaded=false`。
- 验证证据：`tests/test_harness_api_workbench.py` `9 passed`；harness 工具邻接回归 `216 passed, 1 skipped`。本票不启动服务、不联网、不加载 QW1.8B，也不声明模型质量。

本票完成 OpenAI 兼容层与主项目 API 的最小契约漂移嗅探入口；下一票进入 `FUN-CLI-01`。

### 已完成票 `FUN-CLI-01`

- 新增 `harness_workbench/tools/fun_cli.py`，合并 `qlh_say` 与 `model_quotes` 两个零依赖入口：ASCII 横幅、`bar/blocks/dots/steps/none` 进度样式、固定 fixture 语录卡片、JSON/Markdown/终端输出。
- 新增 `scripts/qlh_say.py` 与 `scripts/model_quotes.py` 薄入口；可直接从仓库根目录运行，不依赖已废弃的 `frontend`、现行 `frontend_cybergothic`、模型服务或网络。
- 语录卡片支持 `qlh.fun_quotes.v1` 输入校验、模型筛选、URL/地址/凭据脱敏、卡片与报告 digest；固定声明 `source=fixture`、`claim_scope=fixture_only`、`model_invoked=false`。
- 验证证据：`tests/test_harness_fun_cli.py` `10 passed`；harness 工具邻接回归 `226 passed, 1 skipped`。本票不调用 QW1.8B，不做模型质量结论。

本票完成答辩开场热场用的 ASCII/进度/语录卡片工具；下一票进入 `DOC-S1-GAP-01`。

### 已完成票 `DOC-S1-GAP-01`

- 修订 [小模型轻量推理 Harness 工作台调研与方案](小模型轻量推理harness工作台调研与方案.md) 的 `## 8. S1 实施记录`，明确当前仅有无模型 `RuleBasedSummarizer`，并补充“LLM 摘要 call 未实现（规划中）”及核查调研链接。
- 该修订只收口文档表述与证据链，不新增摘要模型、不改变 `ContextPolicy` 行为，不加载 QW1.8B、不联网。
- 定点验收确认 S1 实施记录、摘要核查结论和批次票据三处口径一致；下一张可执行纯软件票为 `RAG-BASE-01`。

### 已完成票 `RAG-BASE-01`

- 新增 `harness_workbench/tools/rag_baseline.py`：固定 6 份仓库相对路径文档与 30 条问题集，使用同一套 source reference 在主项目 `src.rag_store.RagStore` 和 harness `rag.store.RagStore` 两侧离线建库并执行 FTS5 检索。
- 报告 schema 为 `qlh.rag_baseline.v1`，只保留 corpus/case-set digest、问题 SHA-256、目标引用、命中位置与计数；默认两侧 `hit@5=1.000000`、`MRR=1.000000`，不写入问题原文、文档正文、临时绝对路径或模型信息。
- CLI：`python -m harness_workbench.tools.rag_baseline --json build/rag-baseline/latest.json --markdown build/rag-baseline/latest.md`；`harness_workbench.tools` 采用 lazy export，输入 schema 与绝对路径/遍历/重复标识 fail-closed。
- 验证证据：`tests/test_harness_rag_baseline.py` `8 passed`；本票不下载、不加载 QW1.8B、不联网，后续 RAG 票必须用该报告复测前后差异。

本票完成双侧召回基准冻结；下一票进入 `RAG-META-01`。

### 已完成票 `RAG-META-01`

- 主项目 `src/rag_store.py` 与 harness `harness_workbench/rag/store.py` 均新增元数据倒排表与索引，支持 `source/scope/type/tag/time` 五个字段；`tag` 支持多值，过滤条件通过 SQL `EXISTS` 下推到召回候选集。
- 两侧写入文档时同步维护 metadata index；旧 SQLite 通过版本标记自动回填，删除、revision 冲突、snapshot/cache 均保持一致性。harness `HybridRagRetriever`、`/v1/rag/*` 和主项目 `/api/rag/search` 透传 `metadata_filters`。
- 未知字段、空值、超限字段/值和非标量过滤 fail-closed；查询审计只记录过滤字段名，不记录问题文本或过滤值，避免将敏感元数据写入事件。
- 验证证据：RAG/harness 相关回归 `62 passed`，新增 metadata 过滤、revision 冲突、API 下推和 cache key 测试；本票不下载、不加载 QW1.8B、不联网。

本票完成结构化元数据索引与过滤下推；下一票进入 `RAG-CHUNK-01`。

### 已完成票 `RAG-CHUNK-01`

- 主项目 `src/rag_store.py` 与 harness `harness_workbench/rag/chunking.py` 统一支持 `fixed`、`paragraph`、`sentence`、`section`、`adaptive`、`semantic` 六种分块策略；`semantic` 在当前资源约束下明确采用可复现的标题/段落/标点规则，不加载模型或 embedding。
- 两侧均保留有界重叠窗口和原文 `start_offset/end_offset`，并将策略写入 `rag_chunks.granularity`；主项目旧 SQLite 初始化自动补列，重复 revision 在文本、元数据或策略变化时 fail-closed。
- FTS、CJK FTS、向量检索及主项目 API 引用结果均返回 `granularity`；harness source API、`list_chunks` 和命中对象透传策略信息。
- `run_rag_chunk_comparison()` 复用 `RAG-BASE-01` 的 6 份文档/30 条问题集逐策略对照；六种策略在主项目与 harness 两侧均为 `hit@5=1.000000`、`MRR=1.000000`，详情签名一致。
- 验证证据：`tests/test_rag_chunking.py` `10 passed`；主项目/harness/API RAG 回归 `44 passed`；六种策略均通过确定性、长度上限、偏移回指和策略冲突测试；本票不下载、不加载 QW1.8B、不联网。

本票完成双侧分块策略和粒度元数据落库；下一票进入 `RAG-QRW-01`。

### 已完成票 `RAG-QRW-01`

- 主项目 `src/rag_store.py` 与 harness `harness_workbench/rag/query.py` 均提供确定性查询规范化和有界改写：Unicode/空白归一、同义词、中文/英文术语映射、常见口语到规范词映射及 `and/or/以及/并且` 子查询拆分；改写次数默认 4、上限 8，未知/非法输入 fail-closed。
- FTS 路由对所有改写变体并行召回并按 chunk ID 去重；harness embedding 路由在单次 provider 调用中编码全部变体与候选，主项目向量路由与改写 FTS 候选做融合。两侧均使用加权 RRF/归一化混排，保留路由计数、命中去重和 FTS fallback。
- `RagSearchConfig`、harness `/v1/rag/search` 和主项目 `/api/rag/search` 均支持按请求/配置调节 `top_k`、每路候选数、改写次数与 FTS/vector 权重；缓存键包含改写计划与有效配置，避免调参串用旧结果。
- `run_rag_query_comparison()` 复用冻结的 6 份文档/30 条问题集，输出 `qlh.rag_query_comparison.v1`；主项目与 harness 原始/改写两侧均保持 `hit@5=1.000000`、`MRR=1.000000`，无模型/无网络。
- 验证证据：RAG/harness/API/基准回归 `67 passed`；harness 全量回归 `237 passed, 1 skipped`；本票不下载、不加载 QW1.8B、不联网。

本票完成双侧查询改写、多路召回与可调融合入口；下一票进入 `RAG-IDX-01`。

### 已完成票 `RAG-IDX-01`

- 主项目与 harness 各自落地 SQLite 多粒度索引表：配置分块粒度与 document/paragraph/sentence 视图并行写入，可按粒度列举和检索；保留原有 chunk/FTS/向量接口，不做静默截断。
- 新增独立于向量的关键词倒排表，记录 token、前缀和二词组条目；`keyword_search` 支持前缀、词组、粒度和既有元数据过滤。新增实体、关系表及 `graph_search`，按规则抽取实体/关系并沿边扩展候选。
- 调研前置结论：当前开发机只有 QW1.8B，且本轮明确不做模型冒烟/下载/联网；实体抽取采用可审计正则和共现/显式关系规则，模型抽取列为后续模型门票，不以规则结果冒充模型质量。
- 验证证据：双侧 RAG/API/索引回归 `68 passed`；harness 全量回归待本轮收口复跑；本票不加载模型、不联网。

本票完成双侧多粒度/关键词/轻量知识图谱索引；下一票进入 `RAG-RERANK-01`（如无可用小模型，继续保持规则/融合层，不启动模型门）。

### 已完成票 `RAG-RERANK-01`

- 主项目与 harness 在既有 FTS/向量 RRF 或加权融合之后增加有界重排层：先按 `rerank_candidate_k` 截取候选，再按词项覆盖、词组命中和标题命中计算可解释 lexical score；`rerank_weight` 与 `rerank_mode=lexical|none` 均可调。
- 重排结果保留 `fusion_score`、`rerank_score`、`rerank_final_score`、候选数和模式字段；缓存键包含重排配置，避免不同候选预算复用旧结果。无模型时不会调用或下载模型，后续交叉编码器可替换同一候选层。
- 验证证据：RAG/harness/API/索引/重排定向回归 `72 passed`；harness 全量回归 `239 passed, 1 skipped`；本票不加载 QW1.8B、不联网。

本票完成双侧候选集重排与可调截断；下一张可执行票进入 `HW-DSV4-9B-EVAL-01`，先做资源评估，不下载模型。

## 发散规划转票·批次 2（2026-09-09，发散工作池转票，**仅规划未落地**）

> 来源：[项目后续可做方向发散与子项目独立化评估-2026-09-09](项目后续可做方向发散与子项目独立化评估-2026-09-09.md) §2 的 N（补测/测度）、U（实用小工具）、F（趣味工具）——即截止本批尚未在任何计划登记的候选。全部为"规划票"：无论证、无实现、无排期承诺；实施时各自单独开工并附验收门。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 21 | `EX-QW3V2-01` | Qwen3-4B v2 口径补测（N1） | v2 全口径（loose+512）三轮标定，与 DS3（2/4×3、8/11×3）同口径对比 | Qwen3-4B Q4_K_M 工件（当前不可用，模型门后置） | 规划（模型门后置） |
| 22 | `EX-CTX-MEAS-01` | 上下文策略测度（N2） | 滑窗 vs STATE 摘要 vs 长期记忆 × 30 轮 fixture → 早期事实召回-预算曲线（产出论文/答辩图） | harness eval/replay + memory（U4 承接；纯软件优先） | **已完成（2026-09-11）** |
| 23 | `EX-SPEC-ROLE-01` | 投机解码角色实验（N3） | 7B 作 draft vs verify 的接受率/成本曲线（回答"7B 写草稿不改草稿"的量化版本） | speculative PoC + DS3/QW1.8B（7B 当前不可用，模型门后置） | 规划（模型门后置） |
| 24 | `TOOL-MODEL-CMP-01` | model_compare（U1） | 同题多模型输出矩阵（DS3/Qwen3-4B/R1/1.8B，md+json） | 4 模型夹具 + rubric（当前仅 QW1.8B，模型门后置） | 规划（模型门后置） |
| 25 | `TOOL-JUDGE-POLICY-01` | judge_policy_diff（U2） | 判题口径可视化：同一输出 v1 vs v2 判定对比（口径问题演示工具） | rubric v1/v2 + 标定输出 | **已完成（2026-09-11）** |
| 26 | `TOOL-MANIFEST-HLTH-01` | manifest_health（U3） | models/ 资产体检看板（清单/SHA/磁盘/缺口 vs .gitignore） | model_tools + 清单 | **已完成（2026-09-11）** |
| 27 | `TOOL-CTX-RESS-01` | ctx_ressure（U4） | ContextPolicy 压力测试 CLI（30 轮会话 → 折叠/记忆抽取/预算曲线 → 早召回报告） | harness context_engine + memory | **已完成（2026-09-11）** |
| 28 | `TOOL-REDTEAM-LAB-01` | red_team_lab（U5） | 红队样本交互沙盒（逐条演练拦截/放行与理由） | eval/red_team fixtures | **已完成（2026-09-11）** |
| 29 | `TOOL-TRACE-RPL-01` | trace_replay（U6） | 双机验收日志时间线回放（答辩展示辅助） | 项目进展记录 + 日志 | **已完成（2026-09-11）** |
| 30 | `TOOL-PROMPT-LAB-01` | prompt_lab（U7） | 多模板 A/B 渲染 CLI（PromptProfile 差异/长度对比） | harness adaptation | **已完成（2026-09-11）** |
| 31 | `TOOL-BENCH-LDG-01` | benchmark_ledger（U8） | 散落实验 JSON → 答辩引用汇总表（单机/双机/多模型自动聚合） | build/experiments/* | **已完成（2026-09-11）** |
| 32 | `TOOL-MODEL-CARD-01` | model_card_gen（U9） | 从 manifest/GGUF 元数据自动生成模型卡 markdown | manifest + GGUF 头 | **已完成（2026-09-11）** |
| 33 | `TOOL-DL-RUNNER-01` | download_runner（U10） | 幂等模型下载器（断点续传+自动重试+sha256 sidecar+完成后体检）——固化本次 DS3 下载经验 | huggingface_hub + 本次经验 | **已完成（2026-09-11）** |
| 34 | `TOOL-API-WB-01` | api_workbench（U11） | OpenAI 兼容客户端集合：harness `/v1` vs 主项目 `/api` 请求/耗时/错误码采样（契约漂移嗅探） | harness api_layer + 主项目契约 | **已完成（2026-09-11）** |
| 35 | `FUN-CLI-01` | 趣味 CLI（F1/F2 合并） | `qlh_say`（ASCII 横幅/模型语录/进度条彩蛋）+ `model_quotes`（固定趣味 prompt 输出语录卡片，脱敏） | 零依赖 | **已完成（2026-09-11）** |
| 36 | `HW-SUMM-01` | LLM 摘要器（上下文压缩建议落地） | 实现 `LLMSummarizer(SummaryProvider)`：summarizer 角色固定 Qwen3-0.6B（配置可换）→ 产 JSON STATE → `validate_state` 校验 → 失败/超时/非法 JSON fail-closed 回退 `RuleBasedSummarizer` + `context.summary_fallback` notice；作为 `HW-CTX-SQZ-01` 首增量 | harness chat 路径 + Qwen3-0.6B 工件（当前不可用，模型门后置）；依据 [上下文压缩核查调研](harness上下文压缩实现核查与摘要模型必要性.md) | 规划（模型门后置） |
| 37 | `DOC-S1-GAP-01` | harness 方案 S1 差异收口 | 在 harness 方案 S1 实施记录补一行"LLM 摘要 call 未实现（规划中，见上下文压缩核查调研）"，避免"摘要已完成"误读 | 一并随 `HW-SUMM-01` 前置（纯软件优先，可先独立收口） | **已完成（2026-09-11）** |

> 说明：批次 2 共 15 张（N 3 + U 11 + F 合并 1）；批次 2b（上下文压缩建议落地）2 张（`HW-SUMM-01`、`DOC-S1-GAP-01`，挂于 `FUN-CLI-01` 之后），连同批次 1 合计 **22 张票（已完成 18、规划 4）**，均不计入 §待完成统计的 15 张开发票。当前仅有 `QW1.8B`，后续执行顺序为：`RAG-IDX-01`；`EX-QW3V2-01`、`EX-SPEC-ROLE-01`、`TOOL-MODEL-CMP-01`、`HW-SUMM-01` 等模型门后置票不因 QW1.8B 冒充替代而启动。

> **本轮资源与执行约束（2026-09-11）**：开发机当前只有 `QW1.8B` 可用，缺少 Qwen3-4B、DS3-7B、Qwen3-0.6B 等本轮规划票所需工件；不做模型下载、模型冒烟或真实质量宣称。纯软件票允许使用固定 fixture、既有 QW1.8B 的协议/回放边界和静态 manifest；依赖缺失模型的票保持规划，不改写依赖来制造“可执行”。

## 发散规划转票·批次 3（RAG 召回率提升专项，2026-09-09，基准票已落地）

> 来源：用户给定方向（查询改写与多路召回 / 索引策略优化（多粒度·关键词·知识图谱）/ 分块策略 / 重排序 / 结构化元数据过滤；**明确不做 embedding 模型微调**）。定位：`HW-RAG-SQZ-01`（harness RAG 压榨）的专项分解，**同时落地主项目与子项目两端**——主项目 `src/rag_store.py`（`/api/rag/*`，FTS5+向量+ANN 门）与 harness `rag/`（含 `memory/retrieve`）**各自独立实现**（遵循"子项目不 import 主项目代码"契约），**共享同一设计、度量方法与基准**（同一问题集/hit@5/MRR，避免两套口径），允许各自差异化的索引细节（主项目复用既有 RAG-S0…S5D 门，harness 走 FTS5+可替换 embedding 契约）。**硬约束：不微调/不训练 embedding**，所有改进都在"检索管线"层（查询/索引/分块/排序/过滤）实现。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 38 | `RAG-BASE-01` | 召回基准冻结 | 冻结**主项目 + harness 双侧**检索基准（同一问题集 + 目标文档、hit@5/MRR 度量脚本、结果红脱敏报告）；每张下方票改动前后必须复测对比 | 主项目 `rag_store` + harness `rag/`（各自现有 FTS5/embedding 契约） | **已完成（2026-09-11）** |
| 39 | `RAG-QRW-01` | 查询改写与多路召回 | 查询改写（同义/子问题扩展/口语→规范）；多路召回（FTS5 关键词 + 向量 + 改写后查询并行）→ 合并去重；**top-k 与各路权重/次数可调**（召回次数调节入口） | `RAG-BASE-01` | **已完成（2026-09-12）** |
| 40 | `RAG-IDX-01` | 索引策略优化（多粒度/关键词/知识图谱） | 多粒度索引（文档级/段落级/句子级并行，粒度调节）；关键词索引（倒排/前缀/词组，独立于向量）；**轻量知识图谱**（实体/关系抽取 → SQLite 图结构，检索时沿边扩展；调研前置：实体抽取用现成小模型还是规则，先 H 调研再开工） | `RAG-BASE-01` | **已完成（2026-09-12）** |
| 41 | `RAG-CHUNK-01` | 分块策略 | 语义/重叠窗口/章节感知/自适应分块对照（现有 chunking.py 扩展），评估各策略 hit@5 差异；分块元数据随 chunk 落库 | `RAG-BASE-01` | **已完成（2026-09-11）** |
| 42 | `RAG-RERANK-01` | 重排序 | 两级：RRF/加权融合（已有混排思路）→ 轻量重排（交叉编码器或小模型打分，加载于检索后、候选集内）；候选集大小与重排截断可调 | `RAG-QRW-01`（多路产出）+ `RAG-IDX-01`（多粒度候选） | **已完成（2026-09-12）** |
| 43 | `RAG-META-01` | 结构化元数据过滤 | 元数据字段（source/scope/type/tag/时间）索引化 + 查询过滤器下推（语义过滤不进向量计算）；过滤表达式与默认值可配置 | `RAG-BASE-01` | **已完成（2026-09-11）** |

> 说明：批次 3 共 6 张（基准 1 + 方向 5，**各方向均为主项目 + harness 双端各自实现**），连同批次 1/2/2b 合计 **28 张票（已完成 11、规划 17）**，均不计入 §待完成统计的 15 张开发票。执行序：`RAG-BASE-01` → `RAG-META-01`（已完成，过滤下推）→ `RAG-CHUNK-01`（已完成，规则分块与粒度落库）→ `RAG-QRW-01`（已完成，改写与多路召回）→ `RAG-IDX-01`（已完成，规则索引与图扩展）→ `RAG-RERANK-01`（已完成，候选重排）；每票以 hit@5/MRR 提升 + 无静默截断为验收门，不达标不升级。

## 发散规划转票·批次 4（DeepSeek-V4 蒸馏候选，2026-09-10，**仅规划未落地**）

> 来源：2026-09-10 调研发现——V4 蒸馏系列已出现（教师 = **DeepSeek-V4-Flash**，学生 = **Qwen3.5 族**，与项目已有 `models/qwen3-5-2b` / `qwen3-5-9b` 底座同源）。与 DS3-0324-7B（V3 蒸馏，已替代 R1 判题模型）构成"下一代教师"对照。**两票均为规划**：2B 票按用户指示**等待启用**；9B 票**先立显存评估门槛**，通过才进入下载与实验。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 44 | `HW-DSV4-2B-01` | QwenSeek-2B 实验（V4 蒸馏 · 轻量候选） | 工件下载/登记：`faunix/QwenSeek-2B`（safetensors，学生=Qwen3.5-2B、Apache-2.0）+ `faunix/QwenSeek-2B-GGUF`；`import-model` + sha256 sidecar；B1 模板/thinking/架构探测（模型卡声明**默认非思考**、**不正式支持 Qwen3 thinking 软开关**——需实测确认；训练 4K vs 宣称 256K 上下文不一致，登记为已知差异）；v2 全口径（loose+512）三轮标定，对照 DS3-0324-7B **2/4×3、8/11×3** 与 Qwen3-4B 1/4；从节点轻载体评估（宣称 ~3.8GB VRAM / 2B） | 无（工件可直接获取）；**等待启用（用户指示）** | **等待** |
| 45 | `HW-DSV4-9B-EVAL-01` | Qwen3.5-9B-DeepSeek-V4-Flash 显存评估（前置门） | **先评估后下载**：对 `Jackrong/Qwen3.5-9B-DeepSeek-V4-Flash`（9B，另有 GGUF 与第三方 4bit 量化）做资源门测量——Q4_K_M（~5-6GB 权重）+ KV/上下文预算 vs 本机 **16GB RAM / RTX 4060 4GB**（参照基线：**Gemma4 12B Q4 此前勉强可拖动**；9B 预计轻于 12B 但须实测）；输出 CPU 推算 tok/s 与峰值 RSS 预估；**评估通过才进入下载/探测/三轮标定**（流程同 2B 票） | 本机实测（与 Gemma4 12B Q4 基线对照） | **交主节点（本机跳过）** |

> 说明：批次 4 共 2 张（规划 1、交主节点 1），均不计入 §待完成统计的 15 张开发票。共用风险备注：两模型均为社区产物（非官方蒸馏），质量与 thinking 行为须按项目惯例"真实下载 → 探测 → 三轮标定"验证，不得凭模型卡宣称；若 2B 探测确认"默认非思考且可判"，则与 R1 病根（`<think>` 关不掉）形成最轻量对照。

## 发散规划转票·批次 5（reasonix-codex-bridge 收口与主节点接线，2026-09-12）

> 来源：[reasonix-codex-bridge 完善方向](reasonix-codex-bridge完善方向-2026-09-12.md)。本批覆盖桥接层自身的零依赖回归、CLI 版本兼容、一次真实读调用，以及 Codex 配置写入的可回滚收口；桥接层仍不向子智能体授予写权限，不下载/加载本机模型。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 46 | `TOOL-RXB-T1` | bridge 自动化回归 | 独立子项目 `tools/reasonix-codex-bridge` 使用 Node 内置 `node:test` 覆盖配置解析三分支、`validateModelRef`、doctor `models[]/model` 归一化、Codex TOML 首尾/追加替换、configure 临时写入和 MCP `tools/list`/`reasonix_status` 会话；测试不联网、不依赖模型 | bridge 基线 `1d1e2a6` | **已完成（2026-09-12）** |
| 47 | `TOOL-RXB-T2` | CLI 版本兼容门 | 默认要求 Reasonix `>=1.38.6`；`configure verify` 对低版本 fail，server 默认拒绝启动；`REASONIX_MIN_VERSION` 显式放宽时打印 warning；版本探测失败标记 `versionCheck=unknown` 且不阻断 | `TOOL-RXB-T1` | **已完成（2026-09-12）** |
| 48 | `TOOL-RXB-T3` | 主节点真实链路冒烟 | 主节点以 `mode=inspect` 执行一次小任务，登记 CLI 版本、耗时、输出字节、是否截断和失败形态；成功回填基线，失败登记 §1.2，不把 fixture/配置检查写成真实调用 | `TOOL-RXB-T2` + 主节点可用额度 | **已完成（2026-09-12）** |
| 49 | `TOOL-RXB-C1` | profile 一致性 | `configure profile --create/--sync [--write]` 与 `verify` drift 检查；核对 profile model、read-only 与当前 bridge ref | `TOOL-RXB-T2` | **已完成（2026-09-12）** |
| 50 | `TOOL-RXB-C2` | Codex 写入健壮性 | 重复段合并、CRLF/LF、首尾段结构校验与临时文件原子替换；不可写时不半写 | `TOOL-RXB-T1` | **已完成（2026-09-12）** |
| 51 | `TOOL-RXB-C3` | doctor 结果缓存 | CLI mtime/version/抓取时间摘要缓存，默认 TTL 10 分钟，支持 `--refresh`，损坏回退重取 | `TOOL-RXB-T2` | 规划 |
| 52 | `TOOL-RXB-C4` | 环境摘要导出 | 脱敏导出/对照导入平台、CLI、provider/model、ref、profile；不含 key、完整 endpoint 或用户路径 | `TOOL-RXB-T2` | 规划 |
| 53 | `TOOL-RXB-R1` | 结构化调用日志 | 可选 JSONL 脱敏日志，记录 mode、根标签、步数、超时、退出码、耗时、输出字节和截断，不记录正文 | `TOOL-RXB-T3` | 规划 |
| 54 | `TOOL-RXB-R2` | 限额硬上限 | 配置覆盖 steps/timeout/output/queue，非法值回退；硬上限夹紧并反映到 `reasonix_status.limits` | `TOOL-RXB-T2` | 规划 |
| 55 | `TOOL-RXB-R3` | 队列观测 | `reasonix_status` 暴露 queueDepth/inFlight/lastRun 脱敏摘要；队列满错误带深度与重试提示 | `TOOL-RXB-T2` | 规划 |
| 56 | `TOOL-RXB-E1` | 受控方案模式 | 只读 `mode=plan` 透传机器可解析改动建议清单；桥接层无写路径，`implement` 继续禁用 | `TOOL-RXB-T2` | 规划 |
| 57 | `TOOL-RXB-E2` | ACP transport 设计 | 先登记会话生命周期、128MB 前 compact/rotate、stateless 回退和失败无副作用边界，不立即接生产路径 | `TOOL-RXB-T3` | 规划 |
| 58 | `TOOL-RXB-E3` | provider 能力透传 | status 暴露 contextWindow/vision/base_url_host；超 context window 的 task 在调用前拒绝或警告 | `TOOL-RXB-T2` | 规划 |
| 59 | `TOOL-RXB-E4` | 只读工具集扩展 | 按需增加只读 git log/diff 等工具，并在 profile 与文档登记；verify 列出实际工具集 | `TOOL-RXB-C1` | 规划 |
| 60 | `TOOL-RXB-G1` | CI | GitHub Actions 离线执行 node check、node test 和 README 链接检查 | `TOOL-RXB-T1` | 规划 |
| 61 | `TOOL-RXB-G2` | 版本与变更记录 | 子项目语义化版本、CHANGELOG 与首个 `v0.1.0` tag | `TOOL-RXB-T1` | 规划 |
| 62 | `TOOL-RXB-G3` | 跨平台验证 | WSL 或 CI 实跑 CLI 探测、启动自检、verify 和 POSIX terminate 分支 | `TOOL-RXB-T2` | 规划 |
| 63 | `TOOL-RXB-G4` | 主仓接线登记 | 主仓登记 Codex 配置、profile、CLI 与 workspace root，形成换机可照抄清单 | `TOOL-RXB-T2` | 规划 |

本批执行证据：`npm test` 为 `17 passed`，`npm run check` 三个 bridge 模块语法检查通过；T1/T2/C1/C2 使用临时目录和 stub，不访问网络、不启动本机模型。

`TOOL-RXB-T3` 验收：使用本机 Reasonix `v1.38.7` 与 `deepseek-worker`，通过 bridge 执行 ASCII-only 的真实 `mode=inspect` 只读任务；调用以退出码 `0` 返回非空、未截断的 worker 输出，耗时 `10.3s`，内容仅登记真实链路、耗时和输出形态，不作模型质量结论。此前一次 `max_steps=6` 调用在 5 轮工具调用后以退出码 1 暂停，失败形态保留，未将其伪造为成功。

`TOOL-RXB-C2` 验收：`validateCodexBlock` 对主段、env 段及必需键缺失 fail-closed；`upsertReasonixBlock` 合并重复目标段并保留无关段，覆盖首段/尾段及混合输入的 CRLF/LF；`atomicWriteFile` 使用同目录临时文件替换，Windows 目标先位移并在失败时恢复，测试确认无残留临时/旧文件；`codex --write` 仍先生成时间戳备份。

`TOOL-RXB-C1` 验收：新增 `profile` 预览/同步命令，默认不写盘；`--write` 调用 `reasonix subagent create/edit` 后回读 profile，校验 model 与 `read-only: true`，必要时补齐只读 guard；`verify` 对缺失、非法、model drift 或 read-only 缺失 fail-closed。覆盖引号/注释、flow/block `allowed-tools`、create/sync/write、漂移修复、缺失 profile 和非法 profile 名的离线回归 `15 passed`；本机真实 `profile --sync` 预览与 `verify` 均通过，未执行全局 profile 写入。曾通过 bridge 的只读 `mode=review` 请求审查本票，审查发现的问题已逐项修复；真实子 agent 未写入仓库。

## 发散规划转票·批次 6（主从分工：子智能体受控写入，2026-09-12）

> 来源：2026-09-12 需求——**主 agent 只负责指挥与检查，子 agent 真正干活**。当前子智能体是三重只读：profile frontmatter `read-only: true` 且 `allowed-tools` 无写工具（`read_file,grep,glob,ls,code_index`）、桥接层 `MODES` 只有 `inspect`/`review` 且 `mode=implement` 被硬拒绝、`reasonix_status.workerReadOnlyAssumed=true`。本批**有意改变批次 5 的"桥接层保持只读"约束**：写能力默认关闭，并在批次 5 的回归网（`T1`/`T2` 已完成）、profile 一致性（`C1`）与调用日志（`R1`）到位后逐张开放；每张票都必须满足"任何写动作可审计、可回滚、不越出显式授权路径"。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 64 | `TOOL-RXB-W1` | 受控写入通道 | 新增 `mode=implement`：双开关（`bridge.config.json` 的 `allowWrite: true` + 调用显式传 `mode`），`allowedPaths` 白名单（越界文件即拒绝并触发回滚），默认要求工作区干净（`requireCleanTree`），沿用 `allowedRoots`、步数与超时硬上限；开关关闭或未授权时行为与现状完全一致（`implement` 仍拒绝） | `TOOL-RXB-T2` + `TOOL-RXB-C1` | 规划 |
| 65 | `TOOL-RXB-W2` | 变更证据与回滚 | 每次写调用返回结构化变更集（涉及文件、增删行数、`git diff --stat`、文件哈希），**不返回文件正文**；提供仅针对本次调用产出的回滚入口，回滚由主 agent 显式触发；写调用按 `R1` 记脱敏日志 | `TOOL-RXB-W1` + `TOOL-RXB-R1` | 规划 |
| 66 | `TOOL-RXB-W3` | 可写 profile 与角色分工 | 新增写角色 profile（最小写工具集、不带 `read-only`），`configure profile --role read/write` 生成或同步；README 固化"主 agent 指挥 → 子 agent 干活 → 主 agent 检查"工作流与验收门（diff 审查 + 测试通过率 + 越界为零） | `TOOL-RXB-W1` + `TOOL-RXB-C1` | 规划 |

> 说明：批次 6 共 3 张（均规划）。执行序：`W1`（写入通道）→ `W2`（证据与回滚）→ `W3`（profile 与角色分工）；`C1` 已完成，`W1` 仍以前置 `R1`（脱敏日志）为开工门，并建议先由主节点完成 `T3`（真实读链路冒烟）——先证明读路径可用，再开写路径。写能力上线前必须保持 `mode=implement` 默认拒绝，任何放宽都要在 `reasonix_status` 中可见。

> 全表统计：计入前述已收口的 15 张审计/答辩票，当前共 **66 张票：已完成 44、规划 20、等待 1、交主节点 1**；本机不再启动 `HW-DSV4-9B-EVAL-01`，`TOOL-RXB-T3` 已完成真实读链路，本机下一张为 `TOOL-RXB-C3`，9B 资源评估仍保持后置。批次 6 的写能力默认关闭，`TOOL-RXB-C1`/`C2` 已完成，开工仍需 `TOOL-RXB-R1`。
