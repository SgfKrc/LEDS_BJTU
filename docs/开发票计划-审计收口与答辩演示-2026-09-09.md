# 审计收口与答辩演示开发票计划

> 更新日期：2026-09-09
> 当前票：`AUD-RT-01`（下一票）
> 排序原则：先做不依赖外部硬件的可复现演示入口，再关闭安全/发布门，最后执行真实运行与完整 UI 验收。

## 票队列

| 顺序 | 票号 | 主题 | 交付 | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 1 | `DEF-P1` | 一键演示模式 | fixture/live 双模式启动器、健康检查、脱敏报告、跨平台入口 | 无 | **进行中** |
| 2 | `AUD-AUTH-01` | 模型接口安全门 | 下载、注册、本地资产接口的认证/来源门设计与测试 | control/gateway 认证边界确认 | **已完成** |
| 3 | `AUD-PIN-01` | 远程工件身份固定 | 四个远程预设的 revision、文件清单和 SHA-256 审阅登记 | 发布源工件可访问 | **已完成** |
| 4 | `AUD-SBX-01` | MiniCPM4 探测沙箱 | `trust_remote_code` 探测迁移到低权限进程/容器 | 部署环境 | **已完成** |
| 5 | `AUD-RT-01` | 真实 runtime smoke | llama-cpp/MiniCPM4 模板、thinking、架构兼容真实冒烟 | 固定工件、运行时依赖 | 待开始 |
| 6 | `AUD-SW-01` | 真实模型切换验收 | load/switch 成功、失败、回滚和量化不匹配断言 | 固定工件、可用运行时 | 待开始 |
| 7 | `AUD-MCP-UI-01` | MCP 负向 UI 门 | 权限、未知工具、坏参数、后端业务错误和 SSE 失败态 | Harness UI | 待开始 |
| 8 | `AUD-CY-E2E-01` | CyberGothic 全量回归 | 完整 Playwright 矩阵和失败产物收口 | Edge/前端环境 | 待开始 |
| 9 | `DEF-P2` | 失败注入演示 | worker 退出/断连→重派→完成的可重复演示 | `DEF-P1`、本机双进程 | 排队 |
| 10 | `DEF-P3` | 分布式 benchmark | 固定负载、JSON/图表、单机/双机标识 | `DEF-P1`；双机数据后置 | 排队 |
| 11 | `DEF-P4` | 集群拓扑演示 | readiness 快照、节点状态、层段分配脱敏展示 | `DEF-P2`、现有画布 | 排队 |
| 12 | `DEF-A1` | 答辩故事线 | 5 分钟脚本、证据链接、翻车兜底 | `DEF-P1`～`P4` | 排队 |
| 13 | `DEF-A2` | 性能报告整理 | 将 benchmark/TTFT 结果生成结题表格 | `DEF-P3` | 排队 |
| 14 | `DEF-A3` | 现场检查清单 | 设备、电量、网络、模型、端口和恢复动作检查 | `DEF-P1` | 排队 |
| 15 | `DEF-A4` | 演示环境重置 | 仅清理本票产生的进程/临时状态，支持重复演示 | `DEF-P1` | 排队 |

## 已完成票 `AUD-AUTH-01`

验收结果：
- `src/model_api_access.py` 统一提供模型 API 来源门：默认只允许 loopback；跨机访问必须显式设置 `QLH_MODEL_API_TRUSTED_CIDRS`，无效/未配置时 fail-closed。
- 单体 `api_server` 与 `inference-svc` 的模型加载/切换、注册、下载、GGUF、本地资产接口均接入来源门；网关继续负责 Web Bearer 会话和 owner/admin 管理员角色。
- `gateway` 的 registry/GGUF/download 路由现在向 control-svc 透传 Bearer；`control-svc` 默认绑定 `127.0.0.1`，避免绕过网关直接暴露控制面。
- 测试证据：`tests/test_model_api_access.py` 5 passed；gateway 全量 122 passed；gateway/control TypeScript build passed；模型下载和 inference-svc 相关 Python 回归通过。
- 远程部署配置约束已明确：必须由 gateway 提供登录入口，或显式配置受控 Tailnet/CIDR，并在防火墙层限制 control-svc；不能仅依赖默认监听地址。

## 后续票 `DEF-P1`

验收条件：

- 默认 `fixtures` 模式只启动 `frontend_cybergothic`，不访问模型、网络或真实写接口。
- `live` 模式必须显式指定，后端和前端仅绑定 loopback，后端 `/api/health` 成功后才报告就绪。
- 每次运行生成 `build/defense-demo/latest.json`，不写绝对路径、凭据或模型输出。
- `--duration` 支持自动退出，便于 CI 和答辩彩排；进程退出时回收本票启动的子进程。
- Windows 和 Unix 均有薄入口，核心逻辑只维护一份 Python 实现。

本轮进展：

- `tests/test_defense_demo.py`：`4 passed`。
- fixture 彩排：3 秒自动退出，Vite 就绪，报告写入 `build/defense-demo/fixture-run.json`。
- live 彩排：后端 `/api/health` 与 Vite 均就绪；使用 loopback HTTP/TCP 端口，4 秒自动退出后无残留监听进程；报告写入 `build/defense-demo/live-run-final.json`。
- `DEF-P1` 仍保持“进行中”，因为预置对话/图像脚本和干净环境 2 分钟门尚未纳入本票的下一增量验收。

## 待完成统计

审计报告中的 8 项待完成工作已拆为 `AUD-AUTH-01`、`AUD-PIN-01`、`AUD-SBX-01`、`AUD-RT-01`、`AUD-SW-01`、`AUD-MCP-UI-01`、`AUD-CY-E2E-01` 七张票；认证/来源门单独作为第一张审计票，其余真实运行/自动化项目各一票。答辩工具另排 8 张票（`DEF-P1`～`P4`、`DEF-A1`～`A4`），合计 **15 张开发票**。

其中仍有外部条件的票：`AUD-RT-01`/`AUD-SW-01` 需要真实模型和运行时，`AUD-CY-E2E-01` 需要 Edge 环境；这些条件不满足时只能保持排队，不能用 fixture 冒充完成。当前 15 张票中 **3 张已完成、1 张进行中、11 张未开始**；原审计剩余的 4 项中，`AUD-PIN-01` 与 `AUD-SBX-01` 已关闭。

## `AUD-PIN-01` 完成记录

- 新增 `docs/agent_tool/model-artifacts/remote-model-pins-2026-09-09.json`，登记四个远程模型的官方仓库、完整 commit revision、允许文件集、必需文件和逐权重 SHA-256。
- `src/model_download_jobs.py` 对四个新 preset 强制读取并校验 pin；Hugging Face 下载固定 `revision` 与 `allow_patterns`，落盘后校验必需文件、精确权重集合和逐文件 SHA-256，失败时不发布目标目录。
- 回归证据：`tests/test_model_download_jobs.py` 为 `15 passed, 1 skipped`；未执行真实模型下载，避免在当前环境消耗大模型带宽和磁盘。

## 已完成票 `AUD-SBX-01`

- 新增 `scripts/model_tools/sandbox_runner.py`：Linux 使用 bubblewrap user/pid/network/mount namespace，macOS 使用只读禁网 profile，Windows 使用 restricted token、low-integrity token 和 Job Object；没有可用 OS 后端时明确返回 `os_sandbox_unavailable`，不降级为普通子进程。
- `small_model_probe` 先把非权重 metadata 和经允许的远程代码复制到一次性 snapshot；Windows snapshot/cache/temp 目录施加低完整性标签，worker 自身校验 sandbox 标识后才执行 tokenizer。
- 实机证据：当前 Windows 主机以 `windows-restricted-token-low-integrity` 启动 worker；MiniCPM4 本地模板探测在 sandbox 内完成，模板渲染通过、`thinking` 结果为 `accepted_but_undeclared`、未加载权重。
- 回归证据：`tests/test_small_model_probe.py` 与 `tests/test_model_probe_sandbox.py` 共 `9 passed`；Windows 低完整性本身不提供内核级禁网，因此仍强制 `HF_HUB_OFFLINE`/`TRANSFORMERS_OFFLINE`，真实 runtime 和网络隔离增强留在后续环境验收中。

## 发散规划转票（2026-09-09，**仅规划未落地**）

> 来源：2026-09-09 发散讨论（docagent 易用性/定点审查 + harness 上下文/RAG 压榨 + 榨干模式调研）。本批全部为"规划票"：无论证、无实现、无排期承诺；实施时各自单独开工并附验收门。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 16 | `DOC-ENV-01` | docagent 环境变量配置便利化 | `.env.docagent` 读取/校验/提示增强：字段级校验与默认值提示、脱敏检查（sk 不落日志/输出）、`--env` 显式指路与 profile 化配置、缺失时 fail-closed 中文提示 | docagent 独立仓库（已独立） | 规划 |
| 17 | `DOC-AUDIT-ENTRY-01` | docagent 指定条目定点审查 | 针对**指定文档条目**核对实际状态（如"某验收条目表述与实际证据不一致/文档落后"）：按条目/锚点定位 → 提取声明 → 对照 git 提交/测试证据/工作区状态 → 输出"表述 vs 实际"差异清单（复用 R1/R3 信号，但定点+可指定路径而非全库扫描） | `DOC-ENV-01`（配置入口）或独立 | 规划 |
| 18 | `HW-CTX-SQZ-01` | harness 上下文进一步压榨 | 分层预算扩展、更多压缩策略（verbatim/掩码/结构化 STATE 变体）、与长期记忆联动的折叠策略、超预算降级曲线 | harness S1/S8 | 规划 |
| 19 | `HW-RAG-SQZ-01` | harness RAG 进一步压榨 | 检索质量（chunking/混排）、embedding provider 可替换性落地、容量预算与引用边界强化、跨会话检索复用 | harness S4 RAG | 规划 |
| 20 | `HW-R1` | 榨干小模型潜力模式调研 | 上限研究问题清单与首批实验设计（模型上限构成/主导因素消融/多模型协同帕累托/v2 口径真实上限/公开证据对照） | 指向[发散文档 §2.4](项目后续可做方向发散与子项目独立化评估-2026-09-09.md)（H-R1） | 规划 |

> 说明：本批 5 张均为"规划"状态，不计入 §待完成统计的 15 张开发票；待具备实施条件时按队列顺序转为待开始。

## 发散规划转票·批次 2（2026-09-09，发散工作池转票，**仅规划未落地**）

> 来源：[项目后续可做方向发散与子项目独立化评估-2026-09-09](项目后续可做方向发散与子项目独立化评估-2026-09-09.md) §2 的 N（补测/测度）、U（实用小工具）、F（趣味工具）——即截止本批尚未在任何计划登记的候选。全部为"规划票"：无论证、无实现、无排期承诺；实施时各自单独开工并附验收门。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 21 | `EX-QW3V2-01` | Qwen3-4B v2 口径补测（N1） | v2 全口径（loose+512）三轮标定，与 DS3（2/4×3、8/11×3）同口径对比 | Qwen3-4B Q4_K_M 工件 | 规划 |
| 22 | `EX-CTX-MEAS-01` | 上下文策略测度（N2） | 滑窗 vs STATE 摘要 vs 长期记忆 × 30 轮 fixture → 早期事实召回-预算曲线（产出论文/答辩图） | harness eval/replay + memory（U4 承接） | 规划 |
| 23 | `EX-SPEC-ROLE-01` | 投机解码角色实验（N3） | 7B 作 draft vs verify 的接受率/成本曲线（回答"7B 写草稿不改草稿"的量化版本） | speculative PoC + DS3/QW1.8B | 规划 |
| 24 | `TOOL-MODEL-CMP-01` | model_compare（U1） | 同题多模型输出矩阵（DS3/Qwen3-4B/R1/1.8B，md+json） | 4 模型夹具 + rubric | 规划 |
| 25 | `TOOL-JUDGE-POLICY-01` | judge_policy_diff（U2） | 判题口径可视化：同一输出 v1 vs v2 判定对比（口径问题演示工具） | rubric v1/v2 + 标定输出 | 规划 |
| 26 | `TOOL-MANIFEST-HLTH-01` | manifest_health（U3） | models/ 资产体检看板（清单/SHA/磁盘/缺口 vs .gitignore） | model_tools + 清单 | 规划 |
| 27 | `TOOL-CTX-RESS-01` | ctx_ressure（U4） | ContextPolicy 压力测试 CLI（30 轮会话 → 折叠/记忆抽取/预算曲线 → 早召回报告） | harness context_engine + memory | 规划 |
| 28 | `TOOL-REDTEAM-LAB-01` | red_team_lab（U5） | 红队样本交互沙盒（逐条演练拦截/放行与理由） | eval/red_team fixtures | 规划 |
| 29 | `TOOL-TRACE-RPL-01` | trace_replay（U6） | 双机验收日志时间线回放（答辩展示辅助） | 项目进展记录 + 日志 | 规划 |
| 30 | `TOOL-PROMPT-LAB-01` | prompt_lab（U7） | 多模板 A/B 渲染 CLI（PromptProfile 差异/长度对比） | harness adaptation | 规划 |
| 31 | `TOOL-BENCH-LDG-01` | benchmark_ledger（U8） | 散落实验 JSON → 答辩引用汇总表（单机/双机/多模型自动聚合） | build/experiments/* | 规划 |
| 32 | `TOOL-MODEL-CARD-01` | model_card_gen（U9） | 从 manifest/GGUF 元数据自动生成模型卡 markdown | manifest + GGUF 头 | 规划 |
| 33 | `TOOL-DL-RUNNER-01` | download_runner（U10） | 幂等模型下载器（断点续传+自动重试+sha256 sidecar+完成后体检）——固化本次 DS3 下载经验 | huggingface_hub + 本次经验 | 规划 |
| 34 | `TOOL-API-WB-01` | api_workbench（U11） | OpenAI 兼容客户端集合：harness `/v1` vs 主项目 `/api` 请求/耗时/错误码采样（契约漂移嗅探） | harness api_layer + 主项目契约 | 规划 |
| 35 | `FUN-CLI-01` | 趣味 CLI（F1/F2 合并） | `qlh_say`（ASCII 横幅/模型语录/进度条彩蛋）+ `model_quotes`（固定趣味 prompt 输出语录卡片，脱敏） | 零依赖 | 规划 |
| 36 | `HW-SUMM-01` | LLM 摘要器（上下文压缩建议落地） | 实现 `LLMSummarizer(SummaryProvider)`：summarizer 角色固定 Qwen3-0.6B（配置可换）→ 产 JSON STATE → `validate_state` 校验 → 失败/超时/非法 JSON fail-closed 回退 `RuleBasedSummarizer` + `context.summary_fallback` notice；作为 `HW-CTX-SQZ-01` 首增量 | harness chat 路径 + 0.6B 工件（已下载）；依据 [上下文压缩核查调研](harness上下文压缩实现核查与摘要模型必要性.md) | 规划 |
| 37 | `DOC-S1-GAP-01` | harness 方案 S1 差异收口 | 在 harness 方案文档 S1 实施记录补一行"LLM 摘要 call 未实现（规划中，见上下文压缩核查调研）"，避免"摘要已完成"误读 | 一并随 `HW-SUMM-01` 前置 | 规划 |

> 说明：批次 2 共 15 张（N 3 + U 11 + F 合并 1）；批次 2b（上下文压缩建议落地）2 张（`HW-SUMM-01`、`DOC-S1-GAP-01`，挂于 `FUN-CLI-01` 之后），连同批次 1 合计 **22 张规划票**，均不计入 §待完成统计的 15 张开发票；优先度建议（取自发散文档 §5）：先 `EX-QW3V2-01` + `TOOL-JUDGE-POLICY-01`（答辩数据页材料）→ `TOOL-MANIFEST-HLTH-01` + `EX-CTX-MEAS-01/TOOL-CTX-RESS-01` → `TOOL-TRACE-RPL-01` + `TOOL-DL-RUNNER-01` → 其余按需。

## 发散规划转票·批次 3（RAG 召回率提升专项，2026-09-09，**仅规划未落地**）

> 来源：用户给定方向（查询改写与多路召回 / 索引策略优化（多粒度·关键词·知识图谱）/ 分块策略 / 重排序 / 结构化元数据过滤；**明确不做 embedding 模型微调**）。定位：`HW-RAG-SQZ-01`（harness RAG 压榨）的专项分解，**同时落地主项目与子项目两端**——主项目 `src/rag_store.py`（`/api/rag/*`，FTS5+向量+ANN 门）与 harness `rag/`（含 `memory/retrieve`）**各自独立实现**（遵循"子项目不 import 主项目代码"契约），**共享同一设计、度量方法与基准**（同一问题集/hit@5/MRR，避免两套口径），允许各自差异化的索引细节（主项目复用既有 RAG-S0…S5D 门，harness 走 FTS5+可替换 embedding 契约）。**硬约束：不微调/不训练 embedding**，所有改进都在"检索管线"层（查询/索引/分块/排序/过滤）实现。

| 顺序 | 票号 | 主题 | 交付（规划） | 依赖 | 状态 |
| ---: | --- | --- | --- | --- | --- |
| 38 | `RAG-BASE-01` | 召回基准冻结 | 冻结**主项目 + harness 双侧**检索基准（同一问题集 + 目标文档、hit@5/MRR 度量脚本、结果红脱敏报告）；每张下方票改动前后必须复测对比 | 主项目 `rag_store` + harness `rag/`（各自现有 FTS5/embedding 契约） | 规划 |
| 39 | `RAG-QRW-01` | 查询改写与多路召回 | 查询改写（同义/子问题扩展/口语→规范）；多路召回（FTS5 关键词 + 向量 + 改写后查询并行）→ 合并去重；**top-k 与各路权重/次数可调**（召回次数调节入口） | `RAG-BASE-01` | 规划 |
| 40 | `RAG-IDX-01` | 索引策略优化（多粒度/关键词/知识图谱） | 多粒度索引（文档级/段落级/句子级并行，粒度调节）；关键词索引（倒排/前缀/词组，独立于向量）；**轻量知识图谱**（实体/关系抽取 → SQLite 图结构，检索时沿边扩展；调研前置：实体抽取用现成小模型还是规则，先 H 调研再开工） | `RAG-BASE-01` | 规划 |
| 41 | `RAG-CHUNK-01` | 分块策略 | 语义/重叠窗口/章节感知/自适应分块对照（现有 chunking.py 扩展），评估各策略 hit@5 差异；分块元数据随 chunk 落库 | `RAG-BASE-01` | 规划 |
| 42 | `RAG-RERANK-01` | 重排序 | 两级：RRF/加权融合（已有混排思路）→ 轻量重排（交叉编码器或小模型打分，加载于检索后、候选集内）；候选集大小与重排截断可调 | `RAG-QRW-01`（多路产出）+ `RAG-IDX-01`（多粒度候选） | 规划 |
| 43 | `RAG-META-01` | 结构化元数据过滤 | 元数据字段（source/scope/type/tag/时间）索引化 + 查询过滤器下推（语义过滤不进向量计算）；过滤表达式与默认值可配置 | `RAG-BASE-01` | 规划 |

> 说明：批次 3 共 6 张（基准 1 + 方向 5，**各方向均为主项目 + harness 双端各自实现**），连同批次 1/2/2b 合计 **28 张规划票**，均不计入 §待完成统计的 15 张开发票。执行序：`RAG-BASE-01`（先立双侧度量）→ `RAG-META-01` + `RAG-CHUNK-01`（低成本先跑）→ `RAG-QRW-01` → `RAG-IDX-01`（知识图谱调研前置）→ `RAG-RERANK-01`（依赖前两者产出）；每票以 hit@5/MRR 提升 + 无静默截断为验收门，不达标不升级。
