# QLH 总体下一步计划

> 文档状态：现行（唯一总计划入口）
>
> 文档生命周期：**生效中（Active）**

> 2026-08-22 接口审查基线：旧 `frontend/` 终止支持、仅作对照；`frontend_cybergothic` 为唯一产品前端。`FE-AUD-01/02` 与 `CY-API-01/02/03/04/05/06/07` 已完成，下一票转 `AND-API-01`；前后端/Android 缺口与分期票以《前端安卓后端接口与功能缺口审查-2026-08-22》为准，先推进不依赖硬件的 `AND-API-01..05`、`BUNDLE-M1`、`EX-N3-PROD`、`DOCAGENT-M2-GATE/M3-QA`，硬件/网络验收继续后置。
>
> 创建日期：2026-07-28
> 更新日期：2026-08-22
>
> 最近复核：2026-08-21（QW1.8B Full Worker 重启/中断恢复与 Tailnet IPv6 短任务图通过；长时多轮、升级/断电、生产证书、SMTP、CUDA parity 和生产路由仍未通过）
>
> 下次例行复核：按仍开放的已知问题 #11 及外部验收清单推进；UX 前端开发票已收口，新增 UI 票须重新拆分排期。外部条件具备后进入 Android 真机 UI、Gemma 外部干净机、真实 SD→图像理解三轮标定、下一签名 Launcher 包下载进度和双机/CUDA/生产路由验收。无状态变化时最迟 2026-08-23 复核
>
> 适用范围：QLH 从当前 PoC/本机验证基线走向真机验收、可分发版本和后续探索的统一排期、依赖、退出条件与生命周期管理
>
> 上游事实入口：[项目进展与下一步计划](项目进展与下一步计划.md)；测试口径：[测试与评判标准](测试与评判标准.md)

> 说明：下方早期 P1 摘要和历史日志保留原始证据；当前排序、测试数字与下一票以本页最新的 `2026-08-22` 复核为准。

> **2026-08-20 双机验收结论**：在本地启动主节点、远端已启动从节点的条件下，双方源码 `HEAD=7b315321`，QW1.8B Safetensors 清单/权重一致。临时手动分层 `master 0-21 + client_TABLET-2TLUCNU8 21-24` 真实执行 PyTorch 前向，三次 `distributed_required` 客观 smoke 全部 `distributed_used=true` 且 `fallback=false`，从节点 7 个任务、0 错误，心跳窗口 64 次无重连/stall；因此 `L1-3` 的同模型层流水线数据面通过。验收后已恢复动态策略。
>
> 2026-08-22 CY-API-07 已完成本机开发：新增 SQLite 本地优先 storage health、固定客户端诊断和 speculative capability fail-closed 门；下一票转 AND-API-01。真实 control/gateway、外部 verify、双机与生产实验继续后置。
> 证据：typecheck/build、contrast `27/27`、Settings Playwright `3 passed`、API contract `9 passed`；ownership 扫描（含 gateway/control）backend `420`、frontend `284`、gap A `0`、gap B `156`。
> 已知问题 #22 的源码修复已完成：分布式请求会强制重新求解至少两节点的层计划，稳定返回拒绝原因，且不再把 `prepared` 迟到 ACK 记为错误；已有多节点计划会复用，不重复加载。该修复已随运行时开关同步到 Surface，并在 B3 IPv6 短任务图中完成自动 Full Worker 实测。`task_dispatch_enabled=false` 仍是生产准入边界，不绕过安全准入。中文采样质量、长时/断网恢复、IPv6-only 安装包、CUDA parity 和生产路由继续后置。

> **2026-08-21 B3/T-RACE 复核**：T-RACE-6-A/B 的主节点重启、运行中从节点中断和 Full Worker 恢复已通过；按要求跳过 T-RACE-6-C 的长时多轮、升级与断电恢复。B3 通过强制 Tailnet IPv6 注册后执行短 `dual_candidate`：`wf_330d0aa18e2c4a15aa83bb41bb85c9cc` 的远端 `candidate_a`、本地 `candidate_b/aggregate` 完成，`distributed_used=true`、`fallback=false`、重派 0，任务前后均由 Surface 健康接口与 TCP 表确认 IPv6。仍需为 IPv6 bootstrap 首选/重连顺序补策略门；IPv6-only 安装包、NW1.5/NW3 真实 443/TLS、中文质量、CUDA parity 和生产路由继续后置。

> **2026-08-16 最新复核与下一阶段**：G4.3.2B 的原生图片 smoke 已由 `3a3ee84/b41788c` 完成，不能再按历史“Blocked”排期；G4.4-G4.7 工件、产品化和 CUDA 包构建记录成立，但外部无 Ollama 干净机仍待验。KIP-11A/11B、KIP-12、KIP-13、KIP-14、KIP-15 和 KIP-16 开发门均已完成。QW3.18 的真实 sidecar PATCH 丢 ACK 续传、ledger 进度、重复 commit 幂等和 revoke 清理专项 `6 passed`，QW3 硬件无关开发链已收口；Python 全量 `2524 passed / 18 skipped`。当前不新增硬件无关 QW3 票，下一阶段转真实模型/CUDA/双机/生产路由与 EX-N3 三轮真实取数；所有未具备外部条件的路径继续 fail-closed。

> **2026-08-20 `CLUSTER-JOIN` 本机开发门完成**：入群流程已接入 Web/TUI：目标节点生成 `qlhjoinreq1` 请求码，主节点在明确 Auth App 审批接缝后签发短期 `qlhjoin1` client-only 授权，目标节点校验并消费后强制降级为从节点。主节点 issuer key、目标 pending key 和 nonce ledger 均写入用户自持 `qlh-control.sqlite3`；字符串与二维码共用同一 payload，未下发 cluster secret/TOTP seed/私钥。API/契约专项 `11 passed`，TUI `74 passed`，前端 build 通过；布尔审批开关 `QLH_CLUSTER_JOIN_BOOLEAN_APPROVAL` 默认关闭。真实 Auth App/TOTP 验证、SMTP/IMAP 自动回码、双机 TCP 换网、跨进程密钥保护和长时验收仍后置；下一票转 `RAG-S5D` 容量/长时任务/模型摘要变化与 ANN 决策。
> **2026-08-20 `RAG-S5D` 本机开发门完成**：容量摘要增加当前/陈旧 embedding 计数；`provider + model_id` 绑定冻结模型摘要，tag/摘要变化清理旧向量并使旧 queued/paused job fail-closed；SQLite 跨进程 lease、有限重试和身份/提交围栏回归；新增脱敏 `/api/rag/ann-decision`，默认 FTS/有界 cosine，未安装 `sqlite-vec` 为 `NO_GO`。RAG 定向 `.venv-test` `29 passed`。真实用户规模、provider 长时 soak、sqlite-vec benchmark、双机和生产性能后置，下一票回到其他不依赖硬件的计划项。

> **2026-08-21 投机解码三票 + 打包瘦身 + 前端复核**：SPC-CS/MEDS/REV 三票本机开发门完成（置信度调度 GammaPolicy + Session/run_speculative_chat 接线、Medusa 候选树逐位接受、置信驱动逆向回退），定向 `107 passed`，static 分布等价与 `static=None` 语义保留；TG-OPT-G5.3 解锁影子级动态故障注入闭环（白名单扰动、同 seed digest 重放、影子恒只读，真实双 Worker 竞速/唯一提交仍 Blocked）。打包瘦身改造：PyTorch 系移出安装包，`qlh-slim.spec` + `runtime_guard` 外部运行时 pip 引导（CPU 走 PyTorch CPU index、CUDA 官方源），页瘦身至引导器+源码+前端，真 PyInstaller 构建待打包环境。cyber-gothic 前端第 2 波（Account/Audit/ClusterAdmin、场景 canvas、p 级回归）同步入库。不宣称真模型加速收益、生产 ANN 性能或安装包已实构建。

> **2026-08-22 `CY-UX-FIX-01` 修复票完成**：修复 `frontend_cybergothic` Audit Node relay 对缺失 `timestamp` 聚合记录调用 `localeCompare` 导致的 render 崩溃；Downloads 接入 models 页面背景、揭示动效和统一面板层；Activity 日志改为固定 24 条分页，避免运行日志不断拉长。`typecheck`、production build、对比度 `27/27`、定向 Playwright `8 passed`。真实后端长时日志、低端设备滚动和发布包视觉验收后置。`frontend/` 继续冻结为对照。

> **2026-08-22 `FE-AUD-02` 开发门完成**：在 `api_gap_scan.py` 中增加 endpoint ownership 聚合和 `qlh.api_ownership.v1` CI JSON，独立输出 `domain`（product/internal_protocol/experimental）与 `frontend_status`（page/component/hook/client/unconsumed）；`--ownership` 提供人类可读表。三面真实扫描保持缺口 A `0`、软缺口 `0`，ownership 分类为 experimental `5`、internal protocol `120`、product `167`；专项 `.venv-test` `15 passed`。下一票转 `CY-API-01`，真实网关路由与运行时审计继续后置。
> **2026-08-22 `CY-API-01` 开发门完成**：`frontend_cybergothic` Account 页面与 API client 已接入 owner bootstrap、Auth App QR/字符串双下发、TOTP 首次确认、恢复码一次展示/轮换及管理员成员 provisioning；密钥/恢复码仅在当前页面内存态，未写浏览器持久存储。`qrcode` 本地依赖、typecheck、production build、contrast `27/27`、账户 Playwright `3 passed`。真实 control/gateway、OS credential store、首次安装和跨平台联调仍后置；随后进入 `CY-API-02`。
> `CY-API-02` 接线后的 canonical 静态复扫：后端 `417`、前端证据条目 `237`、缺口 A `0`、缺口 B `173`、软缺口 `0`；ownership 域分类仍为 experimental `5`、internal protocol `120`、product `167`。
> **2026-08-22 `CY-API-02` 开发门完成**：Cluster Admin 接入 current/activate 档案、advertised endpoint verify、pipeline capacity/layers 只读计划、Sidecar status/contracts 和 master 二次确认的 release/cancel；客户端保持只读。集群定向 Playwright `3 passed`，typecheck/build/contrast `27/27` 通过。真实 control/gateway、双机容量计划和 production Sidecar 继续后置；下一票为 `CY-API-03`。
> **2026-08-20 Worker/模型身份边界复核**：当前 `PC Full Worker` 只在本地完整模型就绪且不处于分层模式时公布一个活动模型身份；远端 Stage offer 会按精确 `model_id + engine + format + revision + sha256` 再校验。因此“单个 Full Worker 需要能加载完整模型”是当前运行时准入契约，不是分布式推理的普遍物理定律。任务图已经支持按 Stage 使用不同模型，模型切换需重新加载并刷新能力快照；PyTorch 层流水线则要求全链路同一工件/层布局，只做选择性层加载。跨模型仅通过 LLM→SD 等语义 Stage 边界传递受控文本或 artifact。无须再开不改变架构的优化票；从节点重启后进入 `L1-2/L1-3/L1-2B` 分路径分布式验收，多模型驻留或跨模型 hidden-state 适配另立架构票。

> **2026-08-17 `UX-07` 开发完成**：系统设置“外观”可选择/恢复用户对话头像；仅接受 1 MiB 内的 PNG/JPEG/WebP，作为 Data URL 存在当前浏览器 localStorage，不发送到用户主节点、网关或开发组。助手头像按主题固定使用 Vite 打包的用户提供 Logo：深色 `qlh.jpg`、浅色 `qlh-light.jpg`。浅色模式的文本/图片上传工具现在保持不透明的 SVG 描边和独立中性对比度，修复禁用态图标不可辨识。头像偏好与前端契约 `64/64`、设置与 UX Playwright `15/15`、生产构建通过；UX 前端开发票收口，Android 真机 UI 仍是 L0-2。

> **2026-08-17 `UX-06` 开发完成**：Playwright 已在 `1280x960` 与 `390x844` 复核工作区边界、无横向溢出和侧栏键盘可达性，并留存桌面/窄屏截图；前端契约 `60/60`、设置与 UX 回归 `14/14` 均通过。修复了窄屏侧栏收缩过渡期间聊天输入区溢出：侧栏在窄屏即时固定为 56 px 图标栏，文本框允许收缩并截断占位文本。Android Full/Lite Debug APK、androidTest Kotlin 源集与 JVM 单测均通过；但未安装到真机，亮暗主题、触控、软键盘/IME 与系统栏仍是 L0-2 外部验收。后续 UX-07 已完成。

> **2026-08-17 `UX-04` 开发完成**：Android Material 3 主题现与 PC 的白底黑字/黑底白字语义一致，`surfaceContainer*` 使用中性灰阶，成功/警告/错误仅作状态色；形状 token 收敛为 `4/8/12dp`，普通控件和卡片为 `8dp`，对话气泡、输入、会话图标和图像容器已接线。全部排版 token 字距为零，底部导航及系统状态/导航栏显式映射亮暗主题。Full/Lite Kotlin、androidTest 源集编译和 JVM 单测均通过（`174 passed`）。为修复既有构建阻塞，删除了零调用且引用从未定义认证 DTO 的 `AuthUiState.kt`；Android 认证仍保持 `AND-E-01` 待开发。随后 UX-05 已完成，真机视觉验收留给 `UX-06`，不以编译替代。

> **2026-08-17 `UX-05` 开发完成**：设置页将设备状态、主节点连接和推理模式前置；GPU、llama.cpp 后端、模型、上下文/KV 及系统资源细节默认收起，模型列表按需展开。原有 `showThinking` 已接入行内 `Switch`，数值状态保持统一右对齐，Full/Lite 分支和所有 API/数据行为不变。UX-06 已完成自动化视觉/构建回归；Android 认证 `AND-E-01` 仍独立待开发，真机视觉/触控继续后置。

> **2026-08-17 `MODEL-RUNTIME-UI-03` 开发完成**：用户主节点 SQLite 的每份 Sidecar 合同现在保存最多 24 条路径无关生命周期证据，列表只投影最近 8 条。证据限定时间、动作、阶段、代际、分段数、清理状态和归一化错误码，不保存异常原文、路径、权重、prompt 或 tensor；统一状态同时关联活跃合同。设置页显示计划代际、最近证据与恢复提示，模型卡按原始本地资产 ID 关联合同，修复 `local:` 展示 ID 不匹配。合同/Sidecar/容量专项 `21 passed`，前端单元 `60/60`、Playwright `13/13`、网关全量 `115 passed`、前端构建通过。Sidecar 控制面无硬件开发票至此收口；UX-04 至 UX-07 已完成，后续前端工作须按新问题重新拆票。真实权重、CUDA、双机和生产路由继续后置验收。

> **2026-08-14 `PT-PIPE-QW3.11` 开发门完成**：TCP server/client 为每次成功注册维护单调 `registration_epoch`；Qwen3 peer proof、transfer ticket、descriptor/receipt、network control 和 coordinator 授权共享同一 live epoch，重连后的旧 proof/ticket fail-closed。测试 helper 改为注册/撤销/重连事件投影；新增目标侧 `/network-control/consume`，目标 executor 在本地解析 artifact 并执行，控制面只返回 artifact SHA/bytes、hidden/KV shape/phase 摘要，不返回路径、ticket 或 tensor。QW3 网络/传输专项 `30 passed`，生产 `api_server` 路由仍未注册，真实双机/CUDA/异构 parity 和 sidecar 真模型执行继续后置。
>
> **2026-08-14 `PT-PIPE-QW3.12` 开发门完成**：新增 `Qwen3NetworkSidecarExecutor`，目标 sidecar 在本地解析 filtered assignment/hidden/KV artifact，decode 使用目标段 prefill KV；coordinator 以 `receiving -> committed -> consuming -> consumed` 状态机处理消费，重复合同幂等、变更合同拒绝。目标输出在本地复核后登记新的 path-free `output_reference`，绑定下一段拓扑；目标重启清理遗留消费工件，epoch 前进回收旧 transfer/输出，执行异常和取消不回退整模。QW3 专项 `144 passed`，真实模型、远端下一跳数据搬运、双机/CUDA parity 与生产路由继续后置。

> **2026-08-14 `PT-PIPE-QW3.13` 开发门完成**：源端 coordinator 新增认证 peer 绑定的有界 `output_reference` chunk reader，每块重新复核输出 bytes/SHA；摘要变化、路径失效、offset 越界和 peer 越界 fail-closed，并支持显式 output release。transfer client 新增 path-free `upload_chunks`，复用 offset/PATCH/commit 续传状态机；数据面新增二进制 output route，只返回 bytes 与摘要/offset/EOF headers。handoff transport 新增 `transfer_registered_output`，把 2/3 节点 output reference 接到下一目标，不交换路径，连接失败保留 `.part` 供重试，协议/摘要/拓扑错误定向回收。传输/网络专项 `36 passed`；持久 generation/KV ledger、真实模型/双机/CUDA parity、生产路由和生产准入继续后置。

> **2026-08-14 `PT-PIPE-QW3.14` 开发门完成**：新增用户主节点 SQLite `qwen3_network_ledger_v1`，仅保存 canonical contract/generation/phase、transfer 状态/确认 offset、KV shape/phase 和 output lease/next transfer 元数据，不保存路径、ticket 或 tensor。Scheduler 配置 network coordinator 时自动挂接；begin/commit/consume/progress/lease/commit-output/release 均同步投影。重启后旧 transfer/output 标为 `invalidated`，同合同可重新 activate，陈旧代际拒绝；prefill 消费后可结束阶段并进入 decode。handoff transport 可在第二次调用复用 pending plan，从目标确认 offset 续传；连接失败保留 lease/staging，永久错误清两端，重复 lease/commit 幂等，显式逐节点 release 完成。账本/网络专项 `44 passed`，QW3 全线 `153 passed`；真实模型、双机/CUDA parity、生产路由和准入继续后置。

> **2026-08-14 `PT-PIPE-QW3.15` 开发门完成**：`Qwen3NetworkHandoffTransport.execute_target_chain` 已把 2/3 节点 `consume → output_reference → transfer_registered_output → next consume → source release` 串成一次受合同约束的调用，覆盖 prefill/decode 和 KV generation；跨进程 helper 使用 synthetic target-local executor，控制面仍只返回 path-free 摘要，不返回路径、ticket 或 tensor。目标进程重启后从用户主节点 SQLite ledger 恢复时，遗留 output reference 被标记 `invalidated`，目标 staging 与消费工件清理，旧引用不可复活。`.venv-test` 下网络专项 `28 passed`，ledger/transfer/sidecar/state 合并 `53 passed`，QW3 全线 `156 passed`；此前模拟器嵌套 pytest 的缺依赖错误来自主环境而非 `.venv-test`，运行器已优先选择仓库 `.venv-test`。真实模型、TTL/peer epoch/重复提交/撤销的完整故障矩阵、生产路由、双机/CUDA parity 和生产准入继续后置。

> **下一票 `PT-PIPE-QW3.16`**：补齐独立进程故障矩阵（peer epoch 变化、ticket 过期、重复提交、中途撤销、断线重试）并核验 ledger terminal state 与 lease/staging 零残留；随后把测试 synthetic executor 替换为真实隔离 sidecar executor 的 CPU smoke。主运行时 `transformers==4.47.x`、`full_model_materialized=false`、无硬件开发门保持不变，真实双机/CUDA parity 与生产准入继续单独后置。

> **2026-08-14 `PT-PIPE-QW3.16` 开发门完成**：协调器新增 receiver TTL 终态回收，过期 transfer 会同步写入用户主节点 ledger 的 `expired`，peer registration epoch 前进会 fencing 活动 transfer 为 `invalidated`，状态路由触发清理并保证 `.part`/lease 不残留；独立进程覆盖过期、epoch 变化、旧引用不可复活和 terminal ledger。真实隔离 sidecar assignment CPU smoke 通过：Qwen3-4B `[0,1]+embedding` 选中 13 tensors、`979,779,072` bytes，sidecar `transformers 4.57.6`/`torch 2.13.0+cu126`/`accelerate 1.14.0`，KV prefill/decode 与 `full_model_materialized=false` 通过；该 forward 仍是受控 synthetic forward，不是完整模型质量验收。网络专项 `30 passed`，QW3 全线 `158 passed`。

> **下一票 `PT-PIPE-QW3.17`**：在独立进程中补断线后真实 `.part` offset/ledger progress 复验和重复提交/中途 revoke 的审计矩阵；将目标进程 synthetic executor 接到 `Qwen3NetworkSidecarExecutor`，先用真实 torch artifact 完成 2 节点 CPU prefill/decode，再评估 3 节点和真实 CUDA。主运行时 `transformers==4.47.x`、禁止 full-model fallback、真实双机/CUDA parity 与生产准入继续后置。

> **2026-08-14 `PT-PIPE-QW3.17` 复核通过并关闭**：独立 helper 已接入真实 `Qwen3NetworkSidecarExecutor` 和隔离 `.venv-qwen3-sidecar`，真实 torch artifact 的 2 节点 prefill/decode、KV、重复 consume 幂等、revoke 清理均通过；3 节点 output reference 级联、段间 release 和跨阶段 KV 副本也在 RAM 窗口内实测通过。复核发现真实模型测试在默认 `-n 4` 下会并行抢占内存，已加跨 xdist worker 文件锁并标记 `real_model`，修复后真实链专项 `39 passed / 1 skipped`，QW3 全线 `159 passed / 3 skipped`。sidecar worker 生命周期、错误码透传和 KV 副本清理均通过；真实 CUDA/双机 parity、生产路由和质量验收仍后置。

> **历史下一票 `PT-PIPE-QW3.18`（已于 2026-08-16 完成）**：补真实 helper 模式 PATCH 后断线续传，核对 `.part` 仅从 receiver 确认 offset 继续、ledger progress 单调且重复 status/commit 幂等；同时覆盖 revoke 中断后的真实 sidecar/lease 清理。合成网络矩阵继续保持，QW3 合成链收口后转五期 `PT-PIPE-MM1`。

> **2026-08-16 `PT-PIPE-QW3.18` 开发门完成**：真实隔离 sidecar helper 注入 PATCH 丢 ACK，重试仅从 receiver 确认 offset 续传；ledger `received_bytes` 单调，最终 artifact bytes/SHA 一致，重复 `commit_reference` 幂等；revoke 后 consume 工件、lease 和 staging 清理，旧 transfer 不可再次消费。`.venv-test` 真实 sidecar 专项 `6 passed`（约 62s）。该票只覆盖同机 loopback/隔离进程开发门，不宣称真实外机网络、CUDA parity 或生产路由；QW3 硬件无关开发链收口，转入外部验收。

> **2026-08-14 `PT-PIPE-MM1.1/.2` 开发门完成**：新增 path-free 多模态 profile/manifest、容量预算、visual→text handoff 和已提交 QW3 transfer reference 绑定；实际核对本地 Qwen3-VL-4B 与 Qwen3.5-2B config，禁止权重整模物化、路径/ticket/prompt/tensor 内容进入合同。MM1 定向 `6 passed`；QW3+MM1 联合回归 `169 passed / 1 skipped`。下一票 `PT-PIPE-MM1.3` 接入合成视觉组件生命周期与目标 consume，覆盖重试、取消、TTL/epoch、幂等和清理；真实视觉 forward、CUDA/双机 parity 和生产准入继续后置。

> **2026-08-14 `PT-PIPE-MM1.3` 开发门完成**：新增硬件无关 `Qwen3MultimodalSyntheticExecutor`，在目标 consume callback 内校验已提交 QW3 reference 与 MM1 visual→text binding，输出只携带 binding SHA 摘要；失败/取消沿用 coordinator cleanup，peer epoch 与 TTL 分别投影 `invalidated`/`expired`。MM1 runtime `3 passed`，QW3+MM1 联合 `172 passed / 1 skipped`。下一票 `PT-PIPE-MM1.4` 接入真实隔离 sidecar metadata；真实视觉 forward、CUDA/双机 parity 和生产准入继续后置。

> **2026-08-15 `PT-PIPE-MM1.4` 开发门完成**：`Qwen3MultimodalSidecarAdapter` 已包裹真实隔离 `Qwen3NetworkSidecarExecutor`，在 prefill/decode 控制结果中返回 path-free MM1 binding SHA，并转发 sidecar cleanup；主动返回的 hidden metadata 受 batch/hidden/dtype/device 校验。真实 `.venv-qwen3-sidecar` CPU smoke `1 passed`，MM1 runtime `4 passed`，QW3+MM1 联合 `174 passed / 1 skipped`。输入仍是受控 Qwen3 hidden torch artifact，不是视觉塔 forward。下一票 `PT-PIPE-MM1.5` 完成 processor/视觉 worker 离线预检合同。

> **2026-08-15 `PT-PIPE-MM1.5` 开发门完成**：新增 path-free processor/视觉 worker 离线预检。真实读取本地 Qwen3-VL-4B 与 Qwen3.5-2B 四类 JSON metadata，校验模型族、tokenizer/processor 类、patch/merge/temporal 几何和 image/video 像素/帧预算；Transformers 视觉 worker 只接收 `processor + vision_weights`，MTMD 只接收 `processor + mmproj`，文本权重/MTP 和本地路径均拒绝。安全策略固定 offline/local-only/network-disabled，`trust_remote_code=false`，权重与整模物化均为 false。MM1.5 定向 `15 passed`；QW3/MM1 联合 `190 passed`，`.venv-test` 4 workers 用时 78.67s；主 Python 无 pytest 仅为环境差异，不作为代码失败。下一票 `PT-PIPE-MM1.6` 做隔离 AutoProcessor 离线构造 smoke，真实视觉 forward、CUDA/双机 parity、语义质量和生产准入继续后置。

> **2026-08-15 `PT-PIPE-MM1.6` 开发门完成**：新增独立 multimodal processor controller/worker，真实隔离 `.venv-qwen3-sidecar` 对 Qwen3-VL-4B 与 Qwen3.5-2B 构造 `AutoProcessor`；实际类为 `Qwen3VLProcessor`、`Qwen2VLImageProcessorFast`、`Qwen3VLVideoProcessor`、`Qwen2TokenizerFast`，token/patch/temporal/merge 与 MM1.5 一致。pipeline sidecar 新增 Pillow/torchvision 依赖和就绪检查；worker 无网络、无远程代码、无权重物化，path-free response 带 cleanup 证据。MM1.6 定向 `8 passed`、MM1.5+MM1.6 `23 passed`；串行 QW3/MM1 全量 `198 passed`（111.12s）；并发模式下既有真实 sidecar RAM 资源门可能 skip。下一票 `PT-PIPE-MM1.7` 做 CPU 合成媒体预处理与张量摘要，真实语义/视觉塔/CUDA/双机仍后置。

> **2026-08-13 PyTorch 层流水线加载峰值 P0/P2 开发门完成**：Qwen/Qwen2 的 `load_layer_range` 已强制使用 `_LayerRangeLoadTracker`，所有 safetensors tensor 在读取前验证为分配层段 key；加载器未返回守卫、越界 key、缺失分配层、未知架构或裁剪层数不符均 fail-closed。meta 骨架、tensor 读取/安装、裁剪与清理边界记录 RSS/CUDA allocated 峰值增量，并通过模型信息投影 tensor 数和源/目标字节，不记录路径或权重 key。定向 `95 passed / 1 skipped`，Python 全量 `2105 passed / 8 skipped`。P1 通用契约已经可复用，但 Gemma 4/Qwen3 等具体 adapter 尚未实现；Surface 7/真实 7B 或 12B 的三轮峰值标定仍为 `Validating`，不阻塞其他开发票。
>
> **2026-08-13 `PT-PIPE-C1` 清单优先冷启动控制面完成**：新增不物化权重的 Safetensors 描述器，真实核验 Qwen3-4B、Qwen3-VL-4B、Qwen3.5-2B 的层数和逐组件字节；`prepare_pipeline_model` 与 `POST /api/models/prepare-pipeline` 可建立 `loaded=false / pipeline_prepared=true` 的 distributed-only 状态，Scheduler 不再依赖主节点先整模加载才能取得模型身份、层数和摘要，且该模式禁止未就绪时自动整模回退。现阶段仅 Qwen/Qwen2 执行器准入；三套 Qwen3 工件保持可发现但 fail-closed。定向回归：描述器/模型管理器 `99 passed / 1 skipped`，Scheduler `270 passed`。下一票 `PT-PIPE-C2` 实现集群总容量求解与 prepare/commit/abort 两阶段加载事务。

> **2026-08-13 `PT-PIPE-C2` 集群容量准入与两阶段加载事务开发门完成**：新增 metadata-only 容量求解器，按逐层真实字节、每节点 Final Norm/Embedding/LM Head、CUDA/CPU 运行扩张、reserve 和安全余量生成 all-or-nothing 连续分配；允许 master 零层控制面，单机均不足但集群合计足够时可形成 `aggregate_only` 方案。`GET /api/cluster/pipeline-capacity` 与层配置 API 共享事务代际；distributed-only worker 先 `prepare` 校验摘要/描述器/实时空闲容量，全部 prepared 后主节点本地 commit，再广播 `commit`，错误或断连广播 `abort/release`，禁止整模回退。新增合成容量、阶段 ACK、掉线和清理回归；Scheduler `279 passed`，ModelManager/容量 `102 passed / 1 skipped`，API 模型 `55 passed`。C2 仍不包含按需 shard（C3）、Qwen3 adapter 或真实多机峰值/结果等价验收。

> **2026-08-13 `PT-PIPE-C3` assignment manifest 按需工件同步开发门完成**：新增 assignment-scoped manifest 和 `/api/models/pipeline-assignment/{model_id}`，清单绑定 `config_id + plan_id + node_id + layer_range` 及完整模型 revision，仅下发本节点实际需要的 Safetensors shard/key、config 与支持文件；worker 使用 `.pipeline_assignments/<config>-<node>` 代际目录，复用 `.part`、Range、大小/SHA 和原子提交，不再要求 prepare 前镜像整模目录。接口仅接受可信模型 peer，并拒绝非当前 preparing 事务或不匹配的节点/层段合同；旧全量下载接口保留兼容。C3 定向 `12 passed`，Scheduler/API/安全/同步专项 `357 passed`，Python 全量 `2126 passed / 8 skipped`。视觉/MTP、Qwen3 adapter、真实跨节点传输/掉线验收仍未完成；下一票 `PT-PIPE-C3.1` 做缓存预算、代际回收和真实 Range 故障矩阵。

> **2026-08-13 `PT-PIPE-C3.1` assignment 缓存治理开发门完成**：新增缓存总预算、磁盘最小保留空间和 stale 代际策略；Scheduler 启动 reconcile 仅扫描受控 `.pipeline_assignments`，保护当前/下载中代际，清理无 marker/`.part` 废弃目录并按 LRU 回收超预算缓存。abort/release 现在精确清理 `model_id + config_id + node_id` 失败代际；过滤 index 写入校验大小/SHA，断点 Range 仍保持 `.part` 原子提交。缓存/同步专项 `11 passed`，全量基线 `2126 passed / 8 skipped`。真实大文件断网、磁盘 ACL 和多机结果等价仍后置；下一票切换 `PT-PIPE-QW3` Qwen3 隔离文本 adapter。

> **2026-08-13 `PT-PIPE-QW3` Qwen3 文本分层 adapter 开发门完成**：新增隔离 sidecar adapter/worker，保持主运行时 `transformers==4.47.x` 不变；sidecar 以 `transformers>=4.51`、离线只读和 `trust_remote_code=false` 为硬契约。已落地 `model.layers.N` 过滤物化、meta 骨架、分段前向、官方/legacy KV cache、causal mask/RoPE、LM Head、tied embedding 账本与 `enable_thinking=false` tokenizer 硬校验；未知 key、完整索引绕过、未隔离 runtime、thinking 标记和缺层均 fail-closed。定向回归 `18 passed`，容量/manifest 回归覆盖 tied embedding。当前隔离环境只有 `transformers 4.57.6` 与 tokenizer，缺 `torch/accelerate`，真实 Qwen3-4B smoke 和多机结果等价未发生，不能标记生产准入。

> **下一票 `PT-PIPE-QW3.1`**：按需安装执行 sidecar 依赖，生成 Qwen3-4B assignment manifest，先做资源门控与单段预检，再做两段/三段真实 prefill/decode、KV cache、峰值和 thinking-off smoke；失败只返回结构化拒绝，不回退主运行时整模加载。Qwen3-VL/Qwen3.5、真实跨节点和 PT-PIPE-V1 三轮标定继续后置。

> **2026-08-13 `PT-PIPE-QW3.1` 开发门完成**：新增 `qwen3-pipeline-smoke` 控制器/隔离 worker。worker 通过 Safetensors 无 Torch header 路径计算 assignment tensor 字节和 node-local CPU/CUDA 预算，资源不足返回 `resource_rejected`，Torch/Accelerate 缺失返回 `runtime_unavailable`；只有显式 `--execute` 且资源、依赖、隔离和 filtered-index 门全部通过才物化，禁止 full-model fallback。真实 `models/qwen3-4b` `[0,1]+embedding` 预检选中 13 tensor、`979,779,072` bytes、2 shards；当前 RAM 预算门通过，但 sidecar `transformers 4.57.6` 缺 `torch/accelerate`，故真实物化仍未发生。专项 `13 passed`，全量 Python `2140 passed / 8 skipped`（约 218 秒），`py_compile`/diff 检查通过。下一票 `PT-PIPE-QW3.2`：用户明确允许后安装平台匹配 sidecar 执行依赖，复用 C3 filtered assignment 做单段再两/三段真实 smoke。

> **2026-08-14 `PT-PIPE-QW3.2` 真实 sidecar assignment smoke 完成**：隔离 venv 已安装 `torch 2.13.0+cu126`、`accelerate 1.14.0`、`psutil 7.2.2`，新增 Torch CUDA index/spec 参数，主运行时 `transformers 4.47.1` 未变。临时 filtered assignment 使用硬链接优先、执行后清理；真实 CPU 首段/末段和 CUDA 中间段均通过，覆盖 tokenizer `enable_thinking=false`、prefill/decode KV（18→19 tokens）、RSS/CUDA 峰值、tied embedding logits 与 `full_model_materialized=false`。当前 CUDA 首段因可用 RAM 低于 assignment 预算结构化返回 `resource_rejected`，没有整模回退。QW3 smoke/adapter `9 passed`；下一票 `PT-PIPE-QW3.3` 做两/三段串联、hidden handoff 和跨节点 KV 合同，尚不宣称多机等价或生产准入。

> **2026-08-14 `PT-PIPE-QW3.3` 两/三段串联开发门完成**：新增 chain plan、hidden handoff 与 KV contract 的 fail-closed 校验，worker 对每段先做资源门，再按 prefill/release/decode 串行加载 filtered assignment。真实 Qwen3-4B 三段 CPU smoke `[0,12]+embedding / [12,24] / [24,36]+lm_head` 通过，prefill 18、decode 19 tokens，hidden handoff `[1,18,2560]`，6 次段加载均 `full_model_materialized=false` 并完成清理；专项 `18 passed`。下一票 `PT-PIPE-QW3.4`：接入 Scheduler prepare/commit 的 dry-run 协议与跨节点合同故障矩阵，仍不宣称真实双机或生产准入。

> **2026-08-14 `PT-PIPE-QW3.4` Scheduler dry-run 开发门完成**：新增独立 Qwen3 `prepare/prepared/commit/ready/abort/release` 状态机，canonical 合同锁定模型 revision、连续段、C3 manifest、hidden handoff 与 KV 摘要；64 KiB 合同/8 KiB ACK 上限并禁止 prompt/tensor/KV payload。故障矩阵覆盖篡改、容量漂移、KV 长度/设备/代际、重复 ACK、超时、断线、重试和 release。真实 Qwen3-4B 三段 manifest 合同为 2,959 bytes、3 条 prepare 消息，仅 header/hash，明确不发 TCP、不物化、不回退整模；扩展 `396 passed`，全量 `2165 passed / 8 skipped`。下一票 `PT-PIPE-QW3.5`：单机 loopback 绑定现有 TCP HMAC 和 assignment HTTP/Range，真实双机与生产准入继续后置。

> **2026-08-14 `PT-PIPE-QW3.5` 单机认证 loopback 开发门完成**：Qwen3 dry-run 已通过现有 TCP 注册 HMAC 与实际 loopback socket 双重准入，控制帧/ACK 额外绑定 peer、合同、代际、阶段、时间窗口和 nonce。worker 先验签 C3 assignment manifest，再以禁代理/禁跳转的 HTTP Range 仅读 Safetensors 头部；真实 Qwen3-4B shard 只读 20,008 bytes。Scheduler 的 ready 后断线、部分下发、重连与 release 清理已收口；专项 `48 passed`，Scheduler/TCP/API/manifest 扩大回归 `481 passed`，固定 4 worker 的完整 unit 通道 `2196 passed / 6 skipped`。全路径仍不物化权重、不回退整模；下一票 `PT-PIPE-QW3.6` 接隔离 sidecar 的 node-local 执行与清理，真实双机数据面和生产准入仍后置。

> **2026-08-14 `PT-PIPE-QW3.6` 隔离 sidecar 开发门完成**：新增有界 JSONL sidecar session 与持久 runtime worker，prepare 重算 assignment manifest/资源门并创建 filtered assignment，commit 物化已分配段，release/abort 释放 adapter、CUDA 缓存和 staging。Qwen3 合同只在显式 `execution_mode=node_local_sidecar` 中允许段物化，metadata-only 保持不变；断线会主动收回 sidecar。QW3 扩大回归 `488 passed`，固定 4 worker 的完整 unit 通道为 `2201 passed / 6 skipped`（约 86 秒）；真实双机 hidden tensor 数据面、CUDA 峰值和结果等价仍待验收，下一票 `PT-PIPE-QW3.7` 做单机多 sidecar hidden handoff/KV 执行。

> **2026-08-14 `PT-PIPE-QW3.7` 单机多 sidecar 开发门完成**：新增从 canonical node-local 合同创建 2/3 个 sidecar 的编排器，hidden/KV 通过本机受控 artifact 传递，JSONL 只携带引用、SHA-256/字节数和 shape/dtype/device/generation 合同；按 prefill/decode 顺序推进，每段持有独立 KV，摘要或代际不符即拒绝。取消、重启恢复、遗留工件回收和任一段失败时全量 abort 已覆盖；专项 `10 passed`，QW3 扩大回归 `495 passed`，完整 unit 通道 `2208 passed / 6 skipped`。真实 CUDA/双机结果等价、异构设备转换、网络重连与生产准入仍后置，下一票 `PT-PIPE-QW3.8` 接 Scheduler 本地链入口与 CPU parity gate。

> **2026-08-14 `PT-PIPE-QW3.8` Scheduler 本地链与 CPU parity 开发门完成**：新增主节点专用的 begin/prefill/decode/parity/release/cancel/status Scheduler/API，默认聊天和生产路由保持隔离并固定 `production_admitted=false`。chain/generation/cleanup/parity 仅以白名单元数据写入用户主节点 SQLite；服务重启可定向回收遗留工件，同合同与陈旧代际重放被拒绝。2/3 段合成 Qwen3-like CPU 整链/分段 logits、hidden/KV 和 artifact 证据通过，失败会整链取消且不回退整模；新增专项 `15 passed`、QW3 专项 `102 passed`、完整 unit `2223 passed / 6 skipped`。同时修复独立 Worker 对仓库 `scripts` 包路径的隐式依赖。下一票 `PT-PIPE-QW3.9` 只做认证网络工件票据、分块/续传/原子提交和 loopback 故障矩阵，不依赖第二台 CUDA PC；真实双机/CUDA parity 与生产准入继续后置。

> **2026-08-14 `PT-PIPE-QW3.9` 认证网络工件传输开发门完成**：Qwen3 专用 HMAC ticket 绑定 authenticated peer、canonical chain/generation/phase、相邻 segment、bytes/SHA、TTL/nonce；接收端以 4 MiB 有界块顺序写 `.part`，支持 offset 续传、逐块背压、最终摘要复核和原子提交，receipt/状态不泄露路径且 controller 不聚合完整 artifact。内部 FastAPI adapter 仅信任认证传输层注入的 peer identity；跨 peer/跨 session、截断、错序、变化重放、摘要不符、断线、取消和过期矩阵已覆盖。连接中断保留 `.part` 供恢复，协议损坏/取消/TTL 到期定向回收。新增专项 `14 passed`、QW3 专项 `107 passed`、完整 unit `2237 passed / 6 skipped`。该 router/runtime 仍为独立内部模块，未注册生产 `api_server`、未由真实 Scheduler peer 签发，不能标记双机/CUDA/生产准入。下一票 `PT-PIPE-QW3.10`：在活动 Scheduler 事务和 TCP HMAC peer 上接线，扩展 local/network artifact reference，并用同机多进程完成 2/3 节点 prefill/decode、重启/断线/陈旧代际/取消故障矩阵；真实外机验收继续后置。

> **2026-08-14 `PT-PIPE-QW3.10` Scheduler 认证网络接线与同机多进程链开发门完成**：新增请求级 peer HMAC proof、规范 JSON/SHA-256 绑定的内部 control router 和 `Qwen3LoopbackNetworkControlClient`；活动 coordinator 以 canonical chain、相邻 peer、generation/phase、transfer 状态互相 fencing，sidecar handoff 统一投影 path-free `local`/`network` reference。独立目标进程实际通过 control/data 两个 loopback 面运行 2/3 节点 prefill/decode，覆盖断线续传、目标失败、全链取消、阶段未完成拒绝、重启孤儿回收和陈旧合同拒绝；Scheduler 仅在显式配置 coordinator 时注册内部 router，`production_admitted=false`。网络专项 `14 passed`、QW3 `121 passed`、完整 unit `2252 passed / 8 skipped`。固定 allow-list 仅是测试 helper 对 live peer 的模拟；TCP 注册 epoch、目标端 sidecar 执行、真实双机/CUDA parity 和生产路由继续后置。下一票 `PT-PIPE-QW3.11`：接入真实 TCP peer 生命周期并把 network reference 消费下沉到目标端 sidecar。

> **2026-08-12 EX-N3 LLM 客观质量执行器与三轮基线标定完成**：按《自动化优化实验与报告方案》§6.2，新增本地 Qwen 1.8B Q4_K_M GGUF 执行器、版本化客观 rubric、模型/prompt/rubric SHA 锁、独立 `quality_gate`、`quality.required` 可选收口、输出目录运行锁和只读标定汇总器。生成正文与 rubric 答案均不进入记录；专项回归 `31 passed`。三条预注册 CPU 轮次均为正确率 `0/4`、格式率 `4/11`，因此依 §6.2.5 将原 `0.60/0.90` 临时值归档，建立 `ex-n3-qwen-q4-calibrated-v2` 可用性地板。该地板仅适用于锁定 Qwen Q4 工件，明确禁止作为产品质量认可、必需质量门或其他模型的阈值。终端超时造成第 3 轮有一条重复记录；汇总默认拒绝重复，本次显式选择时间戳最早记录并写入审计。`EX-N3` 保持进行中，下一开发范围是 SD 质量门采集与 Gemma 证据 schema 的独立准入，不自动启动受资源门阻止的 Gemma 推理。
>
> **2026-08-13 EX-N3-SD-N1 质量报告采集桥接完成**：新增只读 `experiment_sd15_quality_unit.py` 与 SHA 锁定的 schema v1 fixture plan。采集器须同时匹配报告 SHA-256、资产、工件和模式，结果只投影自动门 `passed/output_count/unique_output_count` 与人工审核状态/人数；prompt、图像、路径、图像哈希、审核者及报告原文不进入 result、记录或报告。核心质量门额外拒绝未由 plan 声明的 SD 资产。固定 fixture 经统一实验 CLI 实测为 `quality_gate=passed`；现有 90s 真机质量报告也已完成一次只读转换，得到自动门 `10/10`、人工审核 `passed/2`。本票不加载 Diffusers、不生成图，也不把既有验收重新宣称为新验收；新增定向 `5 passed`，EX-N3 质量专项累计 `36 passed`。下一票为 `EX-N3-GEMMA-S1`，仅冻结 Gemma 证据 schema、脱敏边界与静态回归，不绕过 RAM 门启动模型。
>
> **2026-08-13 EX-N3-GEMMA-S1 静态计数证据桥接完成**：质量记录新增兼容的 `qlh.experiment_quality.v2`：Gemma 只允许已锁定的 `model + judge_contract_id + judge_contract_sha256` 和主题命中/关键要素覆盖两组通过计数；模型、契约或计数不一致及 prompt、图像、路径、URL、模型输出、判题解释等字段均 fail-closed。无模型、无网络的 `experiment_gemma_quality_unit.py` 以 SHA 锁定契约和 fixture，统一 CLI 已产生 `quality_gate=passed` 的 v2 记录；该 fixture 的 `0.8/0.6` 仅验证数据通路，不能作为 Gemma 质量、图像语义或阈值标定结论。`gemma_judge` 在真实三轮取数和人工复核前被明确禁止设为 `quality.required=true`；本票与既有质量链路定向回归共 `43 passed`。下一项 EX-N3 真实多模态标定受 SD 工件、Gemma 执行资源与串行调度前置限制，暂不以静态 fixture 替代。

> **2026-08-12 外部干净 Windows Launcher/安装包验收通过，下载进度修复待发布复验**：外部干净真机的 Launcher 与安装包基础流程已验收通过。验收者发现下载期间没有可见进度，根因是 `update_core.download_asset()` 虽按 1MiB 分块写入并做大小/SHA-256 校验，却未输出进度事件，GUI 只显示固定阶段文字。现已在源码增加基于已验签清单 `size` 的 `downloading`、`verifying`、`reused` 事件；GUI 显示已下载量/总量/百分比和校验阶段，CLI/TUI 同步输出，GUI 更新按 100ms 节流。`.part`、大小上限、SHA-256、原子替换与安装前 Ed25519 门均未改变；相关专项与 Launcher/签名回归 `68 passed`。当前线上 `0.1.8.2` 不包含该修复，下一张发布票必须构建并签名新的 Launcher 包、发布后在外部干净 Windows 以较大资产复验下载、校验、缓存复用和安装前签名门。Linux `.deb`、真实模型会话、repair/保留数据重装与跨节点场景继续保留各自验收门。

> **2026-08-12 M1.3R 静态退场复核**：Scheduler 节点/容量/在线状态与动态分层已明确为运行时事实，身份和 HA 状态归主节点 SQLite；Python PG 访问层、历史脚本、control PG DAO/投影器和驱动依赖已删除，ModelHost/API 兼容标志与误导命名已收口。静态门禁止生产 `psycopg`、`QLH_DB_*`、`import db` 和 control PG driver 回流。扩大定向 `600 passed`，Python 全量 `1930 passed / 8 skipped`，control 构建及 `313/313` 通过。`M1.3R` 开发门完成；真实已登录 Tailnet 邀请/首次连接仍归外部网络联合验收，不阻塞当前下一票 `L1-2A`。

> **2026-08-12 L1-2A 任务链异构身份开发门完成**：`StageSpec` 新增可选 `model_identity`，每个 Stage 按自身身份优先、workflow 身份兜底；`StageRequest`、工作流快照和远端 Worker 精确匹配均沿用该生效身份，旧的同模型调用不变。自动 Worker 规划按 Stage 独立筛选候选和 fallback。Qwen-1.8B/Qwen-3B 两个 deterministic fake Worker 的双候选聚合、两个不同身份 remote Worker 的 API 自动规划和同模型回归通过，相关专项 `51 passed`。真实多机仍未宣称完成；LLM+SD 混合模板、v2/v3 协商转入下一票 `L1-2B`。

> **2026-08-12 L1-2B 受控 LLM→SD 混合链开发门完成**：新增唯一固定模板 `llm_sd15_v1`：集群内健康 v2 Full Worker 的 `image_prompt` Stage 以精确 `ModelIdentity` 生成受限 SD 1.5 提示词，再由不同节点的 v3 Diffusion Worker 按精确 artifact manifest 执行 `image_generate`。文本结果只在协调器内显式绑定到图像 Stage 的 `prompt` 根输入，v3 offer 保持空 `dependencies`，不破坏其 Blob-only 协议边界；`POST /api/diffusion/distributed/mixed` 仅主节点、双实验门和显式 Worker 选择下可用，歧义模型、同节点双角色、取消和失败均安全拒绝或回收。定向回归 `112 passed`、Python 全量 `1994 passed / 8 skipped`。本票不开放任意 DAG、外部 LLM 或真实多机准入；后者仍归 `L1-1/L1-2` 联合验收。

> **2026-08-10 MF-AUTH-N1W Web 首登复核**：前端认证门已覆盖 owner bootstrap、Auth App 本地二维码/字符串、首次确认、恢复码、登录、会话恢复和 logout；Bearer 只存 `sessionStorage` 且仅随认证/用户请求发送。gateway 公开认证能力并将 `auth/users` 固定路由到 control-svc；旧 Python 单体仅在明确 404 时保持原入口，能力探测 5xx/异常 fail-closed。前端单元 `29/29`、浏览器 `4/4`、gateway `107/107`、前后端构建通过、前端生产依赖审计 0。N1W 开发门完成，但 N1 仍为 `Validating`。

> **2026-08-10 MF-AUTH-N1A 业务授权与账户管理复核**：gateway 新增全局授权矩阵：公开认证、节点机器信任、已登录业务和 owner/admin 管理四级分流；通过 `/auth/session` 回 control-svc 校验 Bearer，`QLH_AUTH_REQUIRED` 支持显式 rollout 开关，节点注册/心跳/发现/模型文件分发不被用户会话误拦。control-svc 新增会话列表/撤销接口，用户列表返回 `totp_state` 与活跃会话数且不返回 token hash；admin 不得为 owner 替换 Auth App。Web 新增账户工作区，支持会话撤销、恢复码轮换、owner/admin 用户创建/编辑/暂停/启用、Auth App 二维码/字符串配置与一次性恢复码。control 全量 `313/313`、gateway `113/113`、前端 `29/29`、浏览器 `5/5`，构建通过，前端生产依赖审计 `0 vulnerabilities`。N1A 为 `Completed（开发门）`，N1 整体仍为 `Validating`。

> **2026-08-10 MF-AUTH-N2 API 阶段复核**：绑定 API 与本地主节点事务已通过契约 `3/3`，control `309/309`；该阶段只记录 control API，客户端接线由同日 N2 客户端复核覆盖，真实 Tailscale CLI/浏览器授权、旧节点实际撤销和双机实网仍未通过，故 N2 整体保持 `Validating`。

> **2026-08-10 MF-AUTH-N2 客户端接线复核**：账户工作区已接 Tailscale 当前绑定、官方 CLI/本机状态授权方式选择、pending 确认、稳定 `tailnet_id + tailscale_user_id + node_id` 录入、原子换网历史和撤销；API 客户端只提交稳定身份字段，不携带密码、token 或 `credential_ref`。前端单元 `30/30`、浏览器 `5/5`、构建通过；N2 客户端开发门完成，整体仍保持 `Validating`，真实 Tailscale CLI/浏览器、旧节点网络撤销和双机实网留最终联合验收。

> **2026-08-10 MF-AUTH-N2L 本机 Tailscale 状态适配复核**：control-svc 新增受认证保护的 `GET /auth/tailscale/local-status`，通过跨平台 `tailscale status --json` 只读探测本机状态；CLI 查找支持 `QLH_TAILSCALE_CLI` 覆盖、无 shell 执行、有界超时/输出、短缓存和并发合并。响应严格投影 `tailnet_id` 候选、`tailscale_user_id`、`node_id`、主机名、DNS 名称及受信 IPv4/IPv6 地址，明确排除 `loginName`、公钥、Peer/Health、stderr 和可执行文件路径；UI 仅自动填充候选，仍需用户显式发起 pending 与确认。N2L 开发门及 Windows 本机只读探测完成，真实绑定/换网、旧节点网络撤销、Linux/双机仍未通过，N2 整体保持 `Validating`。

> **2026-08-10 MF-AUTH-N1 Linux adapter 续票**：Linux Secret Service credential adapter 已实现并接入默认 `ModelCredentialStore`：通过 `secret-tool` 标准输入/输出保存密钥，SQLite/磁盘只保存随机 opaque handle，覆盖、删除、超时和服务不可用均 fail-closed。适配器契约测试 `2/2`，control 全量 `311/311`，构建通过；Web 接线已由后续 N1W 完成，Linux 实机 DBus/Secret Service 与完整安装首登仍待最终联合发布门，N1 保持 `Validating`。

> **2026-08-10 MODEL-TOOLS P0 复核**：`gguf_inspect`、`gguf_verify`、`models_sweep` 已落地到 `scripts/model_tools/`，统一入口为 `python scripts/model_tools.py inspect|verify|sweep`，均为只读并支持 `--json`。定向测试 `5/5`；真实 `H:\qlh_models` junction 根目录 sweep 发现 2 个 GGUF、5 个 SD manifest、2 个 PyTorch 目录，全部有效且 `orphan_candidates=0`。该阶段 Python 全量回归为 `1714 passed / 27 skipped`。P0 保持 `Validating`，剩余门槛是安装包/跨平台入口接线与发布路径复核；P1 后续状态由下方磁盘治理复核覆盖。

> **2026-08-10 MODEL-TOOLS P1 磁盘治理复核**：统一入口新增 `model_disk_usage` 与 `models_clean`。占用统计识别根 junction、按顶层资产汇总 logical/allocated/unique allocated bytes，并拒绝遍历嵌套 junction/reparse point；清理默认 dry-run，实际删除必须 `--apply --confirm CLEAN`。过期 `.part/.tmp` 与无目标 `.sha256` 才是默认可删项；cache/旧备份需额外类别开关，重复模型永远只列 review。定向测试 `11 passed / 1 skipped`、Python 全量 `1720 passed / 28 skipped`；真实 `models` junction 指向 `H:\qlh_models`，只读统计 204 文件、41,238,213,220 bytes，dry-run 发现 8 个 stale cache 且默认安全删除数为 0，本票未删除资产。P1 磁盘治理开发门完成；下一票进入 `sd15_prompt_batch` / `sd15_sampler_matrix` CUDA 侧车批测。

> **2026-08-10 MODEL-TOOLS P1 SD 批测复核**：统一入口新增 `sd15_prompt_batch` 与 `sd15_sampler_matrix`，支持固定矩阵、PNG/contact sheet、JSON 报告和自动图像门；引擎请求级支持 Euler/DDIM 且恢复原 scheduler。CUDA 侧车真实 smoke：原版 SD 1.5 4-step Euler+DDIM 共 2 输出，另跑 2 prompts×1 seed 共 2 输出，均自动门通过、hash 唯一、CLI `exit_code=0`。相关定向回归 `91 passed / 6 skipped`、CUDA 侧车恢复 `2/2`、Python 全量 `1723 passed / 30 skipped`。P1 SD 批测开发门完成；完整 10-seed矩阵、人工目视、安装包/跨平台入口仍为 `Validating` 外部门。

> **2026-08-10 MODEL-TOOLS P2 同步检查复核**：统一入口新增 `models_sync_status inventory|compare`。schema v1 清单只包含相对资产 ID、类型、大小、文件数与摘要，不输出根路径；默认结构摘要快速巡检，`--full-hash` 才做内容摘要，模式不同拒绝混比。比较结果固定为缺少/多出/不一致，CLI 退出码 `0/1/2` 区分一致/差异/无效输入；外来清单 fail-closed，嵌套 junction 不跟随。定向 `24 passed / 2 skipped`，Python 全量 `1728 passed / 31 skipped`；真实 `models` 与 `H:\qlh_models` 均为 10 个顶层资产且结构比较一致，输出无绝对路径。开发门完成，真实双机/跨平台仍属 `Validating`；下一票 `llm_smoke_matrix`。

> **2026-08-10 MODEL-TOOLS P2 LLM 冒烟矩阵复核**：统一入口新增 `llm_smoke_matrix`，固定中文/英文/精确 JSON/中等上下文 marker 四项；每个模型格式走独立离线子进程、临时 HF 缓存、有界 token/单元数/超时，资源不足先拒绝，不通过 OOM 探测。报告不含生成原文或模型绝对路径；执行门与覆盖门分离。Qwen 1.8B GGUF+Safetensors 为 `2/2 units、8/8 jobs` 通过；完整注册表发现 10 单元，实际执行 2 个均通过，6 个缺资产、DeepSeek 7B 两格式分别因 RAM/VRAM 余量不足跳过，故 `execution_gate=true / coverage_complete=false`。定向 `29 passed / 2 skipped`、Python 全量 `1738 passed / 31 skipped`。开发门完成，结果见下条 GGUF 预检复核。

> **2026-08-10 MODEL-TOOLS P2 GGUF 转换预检复核**：统一入口新增 `gguf_convert`/`gguf-convert`，支持注册表 `--model-id` 或显式 `--source`，预检阶段只执行只读工具链/架构/源资产 index/空间/目标路径检查和 dry-run，当时不提供 `--apply`，不创建 staging 或调用转换器。F16/BF16/F32/Q8_0 规划为单步 HF→GGUF，Q4/Q5/K 系列规划为 F16→`llama-quantize` 两步，报告不保存绝对路径且目标已存在 fail-closed。Qwen 1.8B F16 真实预检 `exit=0`，两片权重 `3,673,678,272` bytes、index 完整、转换器/依赖/架构/空间门通过；Q4_K_M 因缺 `llama-quantize` 正确 `exit=1 / quantizer_missing`，未写入工件。MODEL-TOOLS 定向 `40 passed / 2 skipped`、Python 全量 `1744 passed / 31 skipped`。第一阶段开发门完成，执行事务已由下条完成。

> **2026-08-10 MODEL-TOOLS P2 GGUF 转换执行事务复核**：`gguf_convert` 默认保持 dry-run，写入必须显式 `--target --apply --confirm CONVERT`；预检、同卷 staging、离线无 shell 子进程、有界超时、Q4/Q5 的 F16 中间件检查、最终 GGUF 结构+全 SHA256 校验均已落地。发布采用摘要旁车先于 GGUF 的同卷无覆盖硬链接，目标或旁车竞态出现时不覆盖，失败/超时/取消均清理。合成真实子进程覆盖 F16 与 Q4_K_M 成功、失败、超时、取消和竞态，定向 `48 passed / 2 skipped`；Python 全量 `1752 passed / 31 skipped`。真实 Qwen 只做安全预检和未确认执行拒绝，没有转换或生成新模型。量化器固定/打包开发票已由下条完成；真实转换仍必须另获用户批准。

> **2026-08-10 MODEL-TOOLS P2 llama-quantize 固定与分发复核**：新增机器锁与 `scripts/build_llama_quantize.py`，固定 llama.cpp `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`，无网络构建 CPU-only 静态量化器；Windows 本机实际构建/帮助 smoke/PE 系统依赖检查/manifest 与确定性 ZIP 均通过，EXE SHA256 `d91893de6d9b6bede05fcd927f7762b37896779f8cfb337609a3dccea7e30794`，ZIP SHA256 `e3361e96b9cc6b1ab060934f01ca9d45a2797deb1d49ef3ce9280ccea1002b32`。`gguf_convert` Q4_K_M dry-run 已报告 `managed_package/verified`，真实 Qwen 未转换。Linux 构建脚本和 `.deb` 安装路径已接线，但 Linux 真机编译、运行库、安装和执行证据仍待设备；下一票为 Linux 真机工具链验收，随后才申请真实转换批准。

> **2026-08-10 L4-SD1.5 P1 / SD-N3.4 真实 Worker 复核**：新增 `scripts/smoke_sd15_distributed_worker.py`，以临时集群密钥、临时状态目录和 Hub 强制离线模式启动主节点协调器与独立真实 Diffusers Worker。RTX 4060 Laptop GPU 上固定原版 SD 1.5 完成 seed `19950101`、512×512、4-step 单 `image_generate` Stage：v3 TCP 控制、认证 HTTP 分块、协调器 CAS、TaskGraph commit 全部通过；去噪 6.000 秒，端到端 15.765 秒，PNG 360,858 bytes，SHA256 `86665855…05bfa`，第二次运行 SHA 一致。报告不保存模型绝对路径、密钥、grant 或 lease；SD 分布式定向回归 `94/94`，Python 全量 `1765 passed / 31 skipped`。本票只关闭本机真实双进程开发门，`SD-N3.4` 仍为 `In Progress`；下一票是迟到结果/journal 恢复 fencing，之后才进入两台物理 PC 与 SD-N4 多 seed fan-out/fan-in。

> **2026-08-11 L4-SD1.5 P1 / SD-N3.4 恢复 fencing 复核**：TaskGraph 对已返回但未提交的 Provider 结果增加幂等 disposer；远端 Diffusion 在 ingest 后遇到取消/lease 过期会立即回收 coordinator CAS；精确 owner scope 清理支持撤销旧 lease；SQLite journal 恢复的 `result_ready` 图像工作流由启动同步回收遗留输出。新增取消/ingest 竞态、跨 owner 清理、SQLite journal 恢复用例，扩大定向回归 `133/133` 全部通过，Python 全量 `1770 passed / 31 skipped`。`SD-N3.4` 开发门完成但不等价于双机分布式发布；下一票转两台物理 CUDA PC 的 `SD-N4` 固定四 seed fan-out/fan-in、网格聚合和真实参与节点统计。

> **2026-08-11 L4-SD1.5 P1 / SD-N4 非硬件开发门完成**：新增固定四 seed `image_generate` fan-out 与主节点 `image_grid` fan-in，精确 manifest Worker 池采用稳定轮转与 fallback；单 Worker 明确降级为串行批次。2×2 网格和四张成员图写入同一 owner scope，API 返回每张图的实际 winner `stage/provider/node/attempt`，仅至少两个实际 Worker 节点参与时标记 `distributed=true`。单 seed 失败、取消、父引用、owner 清理和 journal 恢复均有自动化覆盖，扩大相关回归 `220/220`，Python 全量 `1777 passed / 31 skipped`。因缺少第二台 CUDA PC，跨机吞吐、断网/OOM/重连和 UI 发布门保持 `Blocked`；当前执行票切换为 `UP-N6.0` 启动器安装清单。

> **2026-08-11 UP-N6.0 安装完整性基线开发门完成**：新增严格 schema v1 `manifest/install-manifest.json`，对最终安装树的规范路径、大小、SHA-256 与资产 kind 进行 UP-N2 Ed25519 签名；链接/reparse、路径逃逸、大小写碰撞、未知字段和用户数据根全部 fail-closed。CPU/CUDA/Launcher/Linux 构建缺 `QLH_SIGNING_KEY` 即拒绝发布；Windows Inno `ssPostInstall` 与 Linux `postinst` 验签，Launcher 远端 A/B 自更新强制签名清单并绑定版本/变体/包身份。相关回归 `111/111`、Python 全量 `1795 passed / 31 skipped`，冻结验签 EXE 已以临时授权链实测。Windows/Linux 干净机安装仍待验收；当前执行票为 `UP-N6.1 verify`，只读 quick/full/deep 不扫描用户数据。

> **2026-08-11 UP-N6.1 runtime verify 开发门完成**：`install_manifest.py` 新增只读 `verify --level quick|full|deep [--json]`，`qlh-launcher verify` 与 `bjtu verify` 均已接线；签名先行，quick 哈希关键启动文件，full 对小文件全哈希并对大文件做大小/固定区段可读性检查，deep 全量 SHA-256。schema v1 无签名抽样摘要，full 不宣称发现所有同尺寸篡改，须用 deep 得到完整防篡改结论。验证仅按签名清单枚举程序路径，用户数据根零遍历；Launcher 启动调用抛错后自动 quick。故障注入与打包链路 `119/119`、Python 全量 `1803 passed / 31 skipped`。Windows/Linux 干净机安装仍待验收；当前执行票为 `UP-N6.2 diagnose`。

> **2026-08-11 UP-N6.2 diagnose 开发门完成**：新增 `diagnose.py` 与 `qlh-launcher diagnose`/`bjtu diagnose`，以 quick 结果和本机受限探测映射 10 类症状。仅 CUDA 变体调用无 shell、3 秒超时的 `nvidia-smi`；磁盘/权限检查只读取系统元数据；网络、模型和 DLL 问题只匹配本地错误症状。原始错误文本不落盘，诊断不联网、不修复、不扫描模型或用户数据；UP-N4 诊断 ZIP 仅附状态目录内、去安装根和失败路径且经脱敏的 JSON 摘要。打包链路 `126/126`、Python 全量 `1810 passed / 31 skipped`。Windows/Linux 干净机安装仍待验收；当前执行票为 `UP-N6.3 repair`。

> **2026-08-11 UP-N6.3 repair 开发门完成**：新增 `repair.py`、`qlh-launcher repair`/`bjtu repair` 与 GUI/TUI 明确确认入口。修复先以本机签名基线执行 deep 复验，只接受 `missing/size/hash` 的已列出程序文件；再要求同版本、UP-N2 已验签的 `kind=repair-index`，外层资产 SHA-256 固定索引，索引逐项反校本机 `path/size/sha256`，载荷不得离开索引 origin。全部载荷预下载校验后才写入同目录临时文件并原子替换，旧文件进入状态目录 `.bak`，最终 deep 复验失败会回滚。超过 10 文件或 64MiB、签名无效、越界索引及非可修复类别均 fail-closed 且不写用户数据。相关回归 `132/132`、Python 全量 `1819 passed / 31 skipped`；Windows/Linux 干净机安装和真实发布源验收仍待执行；当前执行票为 `UP-N6.4 数据保留重装`。

> **2026-08-11 UP-N6.4 数据保留重装开发门完成**：新增 `data_retention.py`、`qlh-launcher`/`bjtu` 的 `data-status`、`retain-data --yes`、`reassociate-data --yes`、`reinstall --yes`，以及 GUI/TUI 明确确认入口。只允许 `models`、`chat_history`、`logs`、`config`、`local_docs` 五个固定目录在同一文件系统内 `os.replace` 原子迁移；链接/reparse、根嵌套、非目录、目标冲突和跨卷全部拒绝，不扫描内容、不合并、不覆盖，标记写入外置 `.qlh-retention.json`，失败尽力回滚。Windows Inno 在卸载前迁出、`ssPostInstall` 回迁；Linux `.deb` 在 `prerm`/`postinst` 使用 `/var/lib/qlh-edge-inference/data` 完成同一事务。`reinstall` 保留后仅复用已验签安装流，绝不放行 `--allow-unsigned`。定向回归 `58 passed / 1 skipped`、Python 全量 `1826 passed / 32 skipped`，两份 Inno 脚本 `ISCC /O-` 与 Linux shell 语法通过；干净机端到端门仍待执行。当前唯一可执行下一票为 `L4-ENV-REG` 打包期 TUI 环境变量注册。

> **2026-08-11 L4-ENV-REG 开发门完成**：Windows CPU/CUDA 安装器新增默认可取消的 `envreg` 任务和 `/ENVREG=0|1`；仅向 `HKCU\Environment\Path` 追加精确安装目录，并记录包归属，卸载不会删除用户原有路径。Linux `.deb` 新增 `/usr/sbin/qlh-env-register`；默认安装不改环境，只有 `QLH_ENVREG=1` 或 helper 显式启用时才原子生成 `/etc/profile.d/qlh.sh`，常规 remove 删除 profile、purge 删除选择状态。未写入端口、模型路径、密钥或代理。专项 `3 passed`、完整 Python `1829 passed / 32 skipped`、Windows 两份 `ISCC /Q /O-` 和 Linux `bash -n` 通过；Windows/Linux 干净机安装、登录 shell 与 remove/purge 仍待最终发布门。T9.6 已进入开发接线，结果见下一条。

> **2026-08-11 L4-TUI-CHAT T9.6 开发接线完成**：`requirements-cpu.txt` 锁定 `textual==8.2.8`，CPU/CUDA 构建在签名清单前生成 `QLH-TUI-Chat/QLH-TUI-Chat.exe` 控制台伴随程序，`bjtu.bat chat` 只调用包内载荷；Linux `.deb` venv 安装同一依赖，`bjtu chat` 直接执行包内 `src/tui_chat.py`。静态契约和 TUI/入口/清单回归 `91/91`，独立 PyInstaller Windows 冒烟 `--help` 通过（89 文件、约 29 MB）。原下一票 T9.6-R 的推进结果由下一条覆盖；默认入口仍保持冻结。

> **2026-08-11 L4-TUI-CHAT T9.6-R Windows 发布构建门完成**：专用 CPU/CUDA venv 已实际完成主程序、包内 `QLH-TUI-Chat`、清单工具、Ed25519 清单和 Inno Setup。CPU deep 为 `5015 checked / 5014 hashes / 0 failed`，Setup `185067509` bytes（SHA-256 `c2151a5b...e98abb`）；CUDA deep 为 `4736 checked / 4735 hashes / 0 failed`，Setup `1496011277` bytes（SHA-256 `0a7e1af1...bb4ee7`）。两变体 `--help` 与 3 秒 headless fixture 均通过。构建期间修复了批处理非交互退出、发布附带文件未复制却进入清单、重复 include，以及 CUDA 包错误强依赖物理 GPU 的门控。Linux 新增原生工具链 `--preflight-only`；当前 WSL 明确缺 `node/cmake/c++/make` 且 `npm` 指向不可用的 Windows 路径，故 `.deb` 未构建。下一票 `T9.6-R2` 只做 Windows 干净机安装/卸载/fixture/真实后端与 Linux 真机或完整 Linux 环境 `.deb` 门；默认入口继续冻结。

> **2026-08-11 L4-TUI-CHAT T9.6-R2 Windows 开发机实装门完成**：CPU/CUDA 修正版 Setup 已分别在 `%LOCALAPPDATA%` 隔离同卷目录完成安装、安装期 deep、独立 deep、`bjtu chat --help`、冻结 EXE fixture 启动、五类数据卸载保留和重装关联。实装修复了批处理括号块变量提前展开、Inno 外部 `Source` 覆盖签名树、安装期只验签不验文件三项缺陷；新 CPU Setup `185050756` bytes（SHA-256 `db5d3174...c4623bf`），CUDA Setup `1495975790` bytes（SHA-256 `f3c8bd93...f51b92eb`）。真实聊天后端/TUI 契约 `34 passed`，打包/清单定向 `25 passed`。跨卷 `G:` 应用根到 `C:` 保留根按现行规则安全拒绝且数据完整，但构成自定义非系统盘安装的卸载阻塞；下一可执行票切换为 `UP-N6.4W` 事务化跨卷保留。外部干净 Windows、Linux 原生 `.deb`、安装包真实模型会话和默认入口仍未完成。

> **2026-08-11 UP-N6.4W Windows 跨卷保留开发机门完成**：`data_retention.py` 保留同卷 `os.replace` 快路径，并新增外置 journal、目标空间预检、4MiB 有界复制、逐文件大小/SHA-256、完整 staging 验证、整批提交后源隔离删除和 `copying/prepared/committed/deleting_source` 恢复。Windows CPU/CUDA 构建在清单签名前冻结 `QLH-Data-Retention.exe`，Inno 卸载前保留、安装 deep 后回迁。故障注入、冻结 helper 真实 `G:`↔`C:` 往返和 CPU Inno 安装→跨卷卸载→重装回迁均通过，源内容、marker、临时物和既有主节点状态边界已复核；最终 CPU/CUDA deep `5016/5016`、`4737/4737`，Setup 分别为 `193205616` bytes（SHA-256 `fb3a4157...2ba02b7`）和 `1504200870` bytes（SHA-256 `021a3514...63106f`），Python 全量 `1836 passed / 33 skipped`。当前下一票回到 `T9.6-R2-EXT` 外部干净 Windows/Linux、环境变量、新 shell 与真实模型会话门。

> **2026-08-11 排期切换：NW1.1-NW1.4 本机路径观测门**：`T9.6-R2-EXT`、Linux `.deb`、真实 Tailscale/DERP、双机 SD 和 Android 真机均因缺外部真机保持 `Blocked`，不以开发机或模拟器伪造外部证据。为保持可验证开发，已执行抗弱网专项的受信路径事实源、心跳质量窗口、单体 API/TUI 只读投影和 loopback 回归。该支线不扩展被冻结的微服务并行拓扑、不修改调度决策或现有 TCP 消息帧；真实路径采样仍单列 `NW1.5` 环境门。

> **2026-08-11 NW1.1 路径事实源与脱敏分类完成**：新增独立 `network_path.py`，只接受显式 `TrustedEndpoint`，以参数数组、0.1-10s 超时和 1MiB 输出上限读取可选 `tailscale status --json`/`netcheck`；CLI 缺失、超时、非零、无效 schema 均结构化降级。路径只分类 `lan_direct/public_tcp_direct/tailscale_direct/derp/gateway_relay/unknown`，public view 仅保留地址类别和聚合事实，不暴露节点地址、MagicDNS 名、DERP region 或 CLI 原文；TCP probe 只连接一个受信端点，不扫描网段。18 个新 fixture 与地址/TCP/bootstrap 扩大回归 `107 passed`，本票未访问真实网络。下一票 `NW1.2` 有界心跳质量窗口。

> **2026-08-11 NW1.2 心跳质量窗口完成**：`network_path.py` 新增线程安全、generation-fenced 的 64 项 RTT/事件窗口，冻结 nearest-rank RTT p50/p95、相邻 RTT 差 jitter p95、连续/窗口内 stall 与 reconnect；`TCPClient` 保留原 EWMA `avg_rtt_ms` 并提供 schema v1 只读 snapshot。时钟回退、超 120s、畸形、重复、迟到和旧代次 ACK 不进入指标，也不改变消息帧、重连、离线或调度。扩大回归 `120 passed`；Python 全量为 `1865 passed / 33 skipped / 2 failed`，仅两项 Linux env-register 测试在 Windows 误用 WSL `bash.exe` 后无法访问 `/g/...`，保留为 Linux 真机联合验收环境门。下一票 `NW1.3` 单体 API/TUI 只读投影。

> **2026-08-11 NW1.3 单体 API/TUI 只读投影完成**：新增无外部 I/O 的 client→master `network_path` 安全投影，当前 `/api/cluster/status` 与主节点条目按 schema v1 可缺省返回；旧响应键不删除，质量只保留聚合白名单。已注册 LAN/公网连接作为被动可达证据，Tailscale 没有 CLI status 路径事实时仍为 `unknown/degraded`，不误报 direct/DERP。TUI 节点屏、`/nodes` 和 Web 管理表仅显示类别、可用性、p95/jitter/stall/reconnect，不读取 endpoint host/Tailnet/region。新增 7 项契约，扩大回归 `512 passed`、前端 `30/30`、构建通过；Python 全量 `1879 passed / 26 skipped / 2 failed`，仅同两项 WSL/Linux 环境门。下一票 `NW1.4` loopback 与诊断边界收口。

> **2026-08-11 NW1.4 本机回归与诊断边界完成**：单进程 loopback TCP 在同一连接验证注册、手工心跳 ACK、取消与结果回传的既有消息语义，质量窗口只增加只读样本。`sanitize_network_path_view()` 成为 API 路径投影和 `network_path_diagnostic_json()` 的统一白名单边界；运行时投影不执行 CLI/probe/外部 I/O，异常日志只含类型。路径/TCP/调度/API/TUI 扩大回归 `515 passed`，专项 `109 passed`；全量 `1882 passed / 26 skipped / 2 failed`，仍仅 Windows WSL 无法访问 `/g/...` 的 Linux env-register 环境门。NW1.1-NW1.4 至此完成；下一项 `NW1.5` 仍因真实 Tailnet/双端网络环境缺失而 `Blocked`。

> **2026-08-11 排期切换：L4-GEMMA4 G4.1/G4.2 完成，G4.3 为候选下一票**：官方 Ollama `gemma4:12b`（ID `4eb23ef187e2`、7.6GB、Q4_K_M）经仅作用于下载子进程的 `127.0.0.1:7897` 代理完整拉取，Ollama 0.32.8 确认 completion/vision/audio/tools/thinking。QLH 的有界 `image_data_urls`、OpenAI 结构化 `image_url`、可选 `QLH_EXTERNAL_REASONING_EFFORT`、显式外部作用域门和禁止丢图回退现已由 Web/TUI 使用：浏览器内存预览与删除、4 图/8MiB/16MiB 限制、TUI 本地路径队列均落地，原图不写入历史/SQLite/localStorage。真实 Edge → QLH → `gemma4:12b` 返回戏剧悲喜面具描述；Python 定向 `179 passed`、Web 桩契约 `1 passed`、真实浏览器 `1 passed`，全量在仅测试进程 `NO_PROXY=*` 下为 `1915 passed / 26 skipped`。下一票仅将原生 llama.cpp、音频、分布式和 8K/16K 资源门列 G4.3 Candidate，不承诺已支持。

> **2026-08-11 L4-GEMMA4 G4.3.1 原生绑定/ABI 预检完成**：`scripts/model_tools.py gemma4-native-probe` 新增独立、只读、无网络 worker；它报告项目 llama.cpp 锁定 revision、当前 `llama-cpp-python` 版本及 MTMD 必需 ABI，工件预检必须显式给出 GGUF/mmproj 与两个 SHA-256，摘要不匹配、缺文件或初始化失败均 fail-closed，报告不回显用户绝对路径。开发机实测项目 revision `47e1de77…`、`llama-cpp-python 0.3.28`、MTMD ABI 完整；因没有冻结工件，结果正确为 `binding_ready / gate_passed=false`，不宣称原生图像或音频支持。专项 `56 passed / 2 skipped`，全量 `1934 passed / 13 skipped`；下一票 G4.3.2 为固定工件、隔离初始化与图片语义对照。

> **2026-08-11 L4-GEMMA4 G4.3.2A 官方工件身份与资源准入完成**：新增 `gemma4_native.lock.json`、`gemma4-native-assets` 和 `gemma4-native-ollama-probe`。本机已存在的官方 Ollama `gemma4:12b` manifest、Apache-2.0 许可、主 GGUF（7,381,382,048 bytes）和 mmproj（175,115,584 bytes）均通过完整 SHA-256 与 Gemma 4 元数据配对校验，复用原工件且不下载第二份模型；任何不一致均 fail-closed，报告不输出用户路径。组合预检在导入或初始化 `Llama` 前先执行资源门；两次实测可用 RAM 约 5-6GiB，低于约 8.74GiB 保守门，正确返回 `resource_rejected / insufficient_ram`，没有模型加载、MTMD 初始化或图片处理。定向 `59 passed / 2 skipped`，当前全量 `1942 passed / 8 skipped`；下一票 G4.3.2B 只在可用 RAM 达门且 Ollama 未驻留后进行 CPU-only 初始化与固定图片语义对照。

> **2026-08-12 已知问题与排期复核**：问题 #1/#4/#6/#7 保持关闭。问题 #8 的身份重置、地址筛选和 `M1.3R` 静态退场开发门均已完成，仍只因真实已登录 Tailnet 邀请/首次连接未验而保持 `Validating`。问题 #3（分层 worker opt-out）与 #5（IPv6）已有实现和自动门，分别等待新 PC 包双机矩阵、真实 IPv4-only/IPv6-only/双栈与 Tailnet/安装包矩阵。问题 #2 的 `L1-2A` per-stage 身份与 `L1-2B` 固定 LLM→SD 跨通道模板均已完成自动化开发门；真实 Worker 派发仍保持实验默认关闭，留 `L1-1/L1-2` 联合验收。

> **2026-08-12 SIM 无硬件前置队列（开发完成）**：`SIM-N1` 至 `SIM-N6` 已按序完成，只补齐生产契约的确定性预验证，不模拟或宣称物理设备、真实模型、真实网络或安装包验收。`SIM-N6` 提供跨平台 `scripts/run_simulation.py`：默认 `quick` 固定运行 N1-N5，`extended` 追加相邻生产契约，`full` 才显式运行整个 `tests`；schema `qlh.simulation_runner.v1` 只输出聚合 pytest 计数和既有脱敏证据。真实验收仍统一归 `L1-1/L1-2/L1-3`。

> **2026-08-12 SIM-N1 确定性任务图仿真基座完成**：新增 `TaskGraphSimulationHarness`，在不启动后端、不打开端口、不创建子进程、不加载模型的前提下，直接复用生产 `TaskGraphCoordinator`、`ProviderRegistry` 与 reservation/lease/fencing 契约。固定覆盖异构双 Worker 成功、一次 Worker 失联后的 retry-safe fallback、运行中取消三种场景；schema v1 仅投影 Stage/attempt/provider 状态、节点逻辑 ID、重试码和槽位收敛，不保存提示词、模型输出正文、路径或密钥。专项 `68 passed`，Python 全量 `2001 passed / 8 skipped`。该证据仅为 L1 预验证，不替代物理节点、网络、性能或安装包验收；下一票 `SIM-N2`。

> **2026-08-12 SIM-N2 v2 Worker 控制面故障矩阵完成**：新增 `TaskWorkerControlSimulationHarness`，在同一进程直接复用生产 `TaskWorkerControlPlane`、`RemoteFullWorkerProvider` 与 v2 消息校验。固定覆盖 hello 重放/冲突、精确模型身份拒绝、错误 lease epoch fence 后的结果重放、断线后的迟到结果、取消确认和有效续租；schema v1 仅记录消息类型、稳定错误码、终态和槽位收敛，不保存 root input、输出正文、路径或密钥，并显式声明 `network_io=false`、`real_model_loaded=false`、`physical_nodes=false`。专项 `118 passed`；真实 TCP、物理 Worker、模型和性能验收仍未发生。下一票 `SIM-N3`。

> **2026-08-12 SIM-N3 v3 图像数据面恢复矩阵完成**：新增 `DiffusionDataPlaneSimulationHarness`，直接复用生产 v3 manifest 准入、`PersistentImageBlobStore` SQLite/CAS、`DiffusionCoordinatorRuntime` 的 owner 隔离、取消清理与重启恢复逻辑。固定覆盖未声明 artifact manifest 拒绝、chunk 重放/冲突与同 owner CAS 去重、跨 workflow owner 隔离、取消后的未提交输出清理，以及重启后撤销 lease 并回收失败 workflow scope。每个场景在临时本地状态目录中运行并清空，schema v1 不保存 blob ID、图像字节、root input 或 transfer grant，显式声明无网络 I/O、子进程、模型和物理节点。专项 `36 passed`；此票不替代 HTTP/TCP、真实 Worker、跨机传输、模型、性能或安装包验收。下一票 `SIM-N4`。

> **2026-08-12 SIM-N4 固定 LLM→SD 跨通道故障收敛完成**：新增 `MixedWorkflowSimulationHarness`，直接复用唯一生产入口 `_run_distributed_mixed_generation` 和 `llm_sd15_v1`。固定覆盖 v2 文本到 v3 根输入的本地绑定、文本模型歧义、SD artifact 歧义、同节点双角色冲突、超过 1000 字符的提示词输出，以及图像阶段取消后的 workflow CAS scope 清理。所有场景在临时 SQLite/CAS 状态中运行并回收；schema v1 仅投影稳定错误码、终态、布尔契约和空闲存储摘要，不保存 prompt、blob ID、图像字节或 grant。专项 `83 passed`；不开放任意混合 DAG，也不替代真实 v2/v3 Worker、网络、模型、性能或安装包验收。下一票 `SIM-N5`。

> **2026-08-12 SIM-N5 有界并发与容量压力矩阵完成**：新增 `CapacitySimulationHarness`，以事件闸门直接复用生产 `TaskGraphCoordinator`、Provider reservation/fallback 与 SQLite/CAS 容量回收。固定覆盖全局两槽对三个 ready Stage 的上限、单 Provider 一槽串行化、外部占用 primary 时以稳定 `all_providers_busy` 切换 pure Stage fallback、并行取消后所有 reservation 归零，以及 leased CAS 对容量的阻塞和显式回收。报告 schema v1 只记录终态、槽位计数、稳定错误码和空闲存储摘要，明确 `performance_claim=false`，不含输入、输出、blob ID 或耗时指标。专项 `56 passed`；不等价于吞吐、时延、OOM、真实网络或真实多机压力验收。后续 `SIM-N6` 已完成。

> **2026-08-12 SIM-N6 CLI/CI 分层运行器完成**：新增 `scripts/run_simulation.py`，默认 `quick` 固定运行 SIM-N1 至 N5，`extended` 显式增加 TaskGraph/v2-v3 Worker/diffusion 相邻契约，`full` 显式对 `tests` 运行 pytest。输出 schema `qlh.simulation_runner.v1` 只含相对目标、pytest 聚合计数和既有脱敏证据；stdout 可直接解析为 JSON，文件输出以原子替换写入工件目录。快检实测 `41 passed`，且不将物理节点、模型、网络、性能或安装包标为已验收。

> **2026-08-12 开发完成度复核**：SIM-N1 至 N6、测试改进 Q1 至 Q22、L1-2A/B 及各无硬件开发门均已从“可立即编码”队列移出；剩余多数是外部环境验收。确认 `TG-OPT-G0` 可在当前开发机实施：它只增加 TaskGraph 的逻辑图/优化图/分析视图/attempt 图类型边界、稳定脱敏快照和不变量，不改调度或执行图。`MT-LORA-N0` 为后续低优先级只读工具票；`EX-N3` 仍须先冻结质量指标。完整盘点见[开发完成度盘点](开发完成度盘点.md)。

> **2026-08-12 TG-OPT-G0 开发门完成**：新增纯模块 `src/task_graph_optimization.py`，从 `StageSpec` 和既有工作流快照只读投影 `logical_dag`、`optimized_dag`、`analysis_view`、`attempt_graph`、`provider_topology` 五类 schema v1 视图。投影采用独立 `stage:`/`attempt:`/`provider:`/`edge:` 命名空间、canonical JSON SHA-256 digest 和白名单 trace；对图类型混用、原始正文、错误、路径、URL、blob/grant/密钥字段 fail-closed。它没有接入 Provider 选择、ready-Stage 调度、journal 或既有 `get()`/`list()`；专项 `33 passed`，TaskGraph API/并行/fencing/review 回归 `71 passed`。

> **2026-08-16 TG-OPT-G1 开发门完成**：新增 shadow-only `optimize_task_graph()`，对 `logical_dag` 生成 canonical 排序后的 `optimized_dag` 候选、稳定 optimizer digest、白名单 trace 和不可变 fan-out payload 引用规划。只裁剪纯 Stage 及其纯上游的不可达节点；副作用边界保留并拒绝裁剪。未接入 Coordinator、Provider 选择、ready 队列、journal 或 runtime 执行；专项 `10 passed`，TaskGraph API/并行/fencing/review 回归 `99 passed`。下一票转 `TG-OPT-G2`，安全传递约简和 payload 引用运行时准入仍需语义等价门。

> **2026-08-16 TG-OPT-G2.1 开发门完成**：依赖边新增 schema v1 语义合同，显式区分纯依赖、input binding 和 partial join；optimizer 升级为 `task-dag-opt-v2`，安全传递约简规则默认关闭，仅在显式启用且存在语义等价纯路径时删除冗余直边，并同步候选依赖/最少成功数。结果始终保留完整逻辑 DAG 回退，binding、partial、合同缺失和未知规则均 fail-closed。专项 `16 passed`，任务图相关联合回归 `193 passed`；仍未接 runtime。下一票为 `TG-OPT-G2.2` 不可变 payload reference 合同与受控本地物化。

> **2026-08-16 TG-OPT-G2.2 开发门完成**：新增 schema v1 不可变 payload reference 和独立本地 `TaskPayloadStore`，引用只含内容摘要、大小、媒体类型、数据作用域与生产/消费 Stage；相同内容跨 scope 不复用。fan-out 计划可绑定到受控根目录的原子 object/manifest，授权 consumer 仅在上下文内短暂物化并自动清理；活动租约阻止 release，越权、超限、篡改、合同不匹配均 fail-closed。定向 `24 passed`，任务图相关联合 `201 passed`。该票未接 Coordinator/journal/跨进程 CAS 或 Worker；下一票为 `TG-OPT-G2.3` 固定模板 shadow 消费、对照指标与回退演练。

> **2026-08-16 TG-OPT-G2.3 开发门完成**：新增固定模板 shadow 消费器和 schema v1 报告，准入模板只比较 `logical_dag` 与优化候选，执行选择始终保持逻辑图。报告记录稳定结构/载荷指标和摘要，不含 payload 正文、路径或原始异常；开发门扇出模板验证一份不可变引用供两个消费者使用，模板结构/规则/投影/优化/绑定失败均稳定回退。定向 `33 passed`，任务图相关联合 `210 passed`。G2 开发链收口，下一票 `TG-OPT-G3.1` 只实现 Stage 语义指纹和 `determinism/share_policy` 合同基线，不做实际合并或 runtime 接线。

> **2026-08-16 TG-OPT-G3.1 开发门完成**：新增 Stage 语义合同与 shadow 共享分析模块，输入和配置只以摘要参与指纹，严格绑定输入/输出 schema、完整模型身份、Provider 要求、数据作用域、副作用、确定性和共享策略。只有同指纹、纯 `transform`、确定性、无副作用且明确允许共享的 Stage 对才判定为候选；随机候选、跨 scope 和任一语义维度差异均 fail-closed。结果只包含稳定指纹、逐对判定和脱敏 reason code，不改执行 DAG。专项 `16 passed`，任务图相关联合 `226 passed`。下一票为 `TG-OPT-G3.2` shadow 合并演练，生产 runtime 仍关闭。

> **2026-08-16 TG-OPT-G3.2 开发门完成**：新增 shadow-only 公共子任务合并候选模块。两个或多个同指纹纯 `transform` 可投影为 `shared:<fingerprint>`，下游依赖重绑定到共享 Stage，并用 provenance 保留全部原始 Stage ID；执行选择仍固定为逻辑 DAG。若合并会压缩 join 输入、碰撞不同边合同、形成自依赖或占用既有共享 ID，候选整组回退并记录脱敏原因。专项 `10 passed`，G1-G3.2 全链 `59 passed`，任务图相关联合 `236 passed`。下一票为 `TG-OPT-G3.3` 连续纯变换融合与失败/provenance 映射，生产 runtime 继续关闭。

> **2026-08-16 TG-OPT-G3.3 开发门完成**：新增 `task_graph_fusion.py`、Stage 融合合同和 shadow 候选 schema。仅准入单出→单入、同 Provider/模型/scope、schema 连续、无 binding/副作用/屏障且四类原 Stage 映射完整的纯 `transform` 链；安全链投影为 `fused:<chain_digest>`，每个源 Stage 都保留失败、取消、日志、计费 boundary ID。fan-in/fan-out、Provider/模型/scope/schema 切换、checkpoint/commit/billing、随机 Stage 或映射缺失均 no-op/拒绝，逻辑 DAG 始终是执行选择。专项 `22 passed`，G1-G3.3 全链 `81 passed`，任务图相关联合 `258 passed`。没有接真实融合执行器或 runtime 故障回写；下一票为 `TG-OPT-G4.1` 支配/后支配与关键路径只读分析基线。

> **2026-08-16 TG-OPT-G4.1 开发门完成**：新增 `task_graph_analysis.py`、有界 Stage 估时合同和结构分析报告。支配/后支配集合与树覆盖单入口/出口；多入口/出口只使用分析级 `virtual:entry/exit`，原逻辑 DAG 不变。完整合法的 lower/expected/upper 区间可生成确定性静态关键路径、最早开始/完成和剩余时长；合同缺失、覆盖不全、重复、越界或带敏感字段时，报告保留结构分析、隐藏输入并将关键路径置为 unavailable，排序策略始终为 `topological`。专项 `17 passed`，G1-G4.1 全链 `98 passed`，任务图相关联合 `275 passed`。未接 ready queue/Scheduler，也未形成真实 P95 结论；下一票为 `TG-OPT-G4.2` 离线 shadow 排序模拟与饥饿保护基线。

> **2026-08-16 TG-OPT-G4.2 开发门完成**：新增 `task_graph_ordering.py` 和离线模拟 profile/report schema。同一 G4.1 分析在 lower/expected/upper 场景中分别按普通拓扑和关键路径优先策略模拟，报告保存 Stage 派发/ready/start/finish、等待、绕过次数、完成/等待 P50/P95、slot 利用率及策略差值。关键策略以 `max_ready_bypass` 限制连续 ready Stage 被绕过的派发次数，达到阈值即优先派发；这不是毫秒级等待 SLA。双并发固定 fixture expected `31→22 ms`、上界/P95 `36→26 ms`，单并发 makespan 不变，仅构成离线开发证据。专项 `19 passed`，G1-G4.2 全链 `117 passed`，任务图相关联合 `294 passed`。实际选择仍为 `topological`，未接 Scheduler 或真实 P95；下一票 `TG-OPT-G4.3` 生成检查点/级联取消建议合同。

> **2026-08-16 TG-OPT-G4.3 开发门完成**：新增 `task_graph_resilience.py` 和只读 resilience profile/report schema。检查点建议仅准入纯 Stage，以支配范围、前缀重算成本、受影响工作量和关键路径成员排序；不合格 Stage 获得稳定 rejection。取消建议只面向未开始 Stage，逐层按 `minimum_successful_dependencies` 计算剩余成功容量，required join 会级联，仍满足阈值的 partial join 保留。报告绑定逻辑图、G4.1、G4.2 三重 digest，记录 post-dominator 边界、side-effect 影响、成本/范围摘要；任何证据 unavailable/mismatch 均整票回退，且 `runtime_actions_enabled=false`。专项 `20 passed`，G1-G4.3 全链 `137 passed`，任务图相关联合 `314 passed`。下一票 `TG-OPT-G5.1` 先审计已有 attempt/epoch/winner/fallback 不变量，不重复开发现有运行时。

> **2026-08-16 TG-OPT-G5.1 开发门完成**：新增只读 `task_graph_attempt_audit.py` 与 `qlh.task_graph_attempt_audit.v1`。审计安全 attempt_graph 和可选 SQLite journal 事实，核对 epoch/winner/Provider/fallback/retry/拒绝/租约/工作流计数，以及重启后的 terminal、连续 sequence、唯一 recovery 事件和恢复计数；报告不携带 error/output/root_input/request/lease/reservation 原文，且运行时动作恒关闭。专项 `14 passed`，与 TaskGraph/fencing/journal/optimizer 联合 `140 passed`，完整任务图联合矩阵 `330 passed`，未发现可证明缺口；G5.2 记为 No-Go，不凭空增加修复。

> **2026-08-16 TG-OPT-G5.2 No-Go 证据升级并复核**：新增 `test_task_graph_fault_injection.py` 的 12 项动态矩阵。真实 Coordinator 对旧/错 epoch、stale lease、Provider/attempt 归属、终态、非法 schema、winner 后第二结果等非法提交全部拒绝，精确重复保持幂等；注入恢复合法状态后均通过 G5.1 审计。复核修正了原矩阵中 M5 未真正回放旧 epoch、M11 未断言 `attempt_not_running`、M4 过度称为“双 winner”的问题。专项 `12 passed`，G5.2 定向联合 `37 passed`，完整任务图联合矩阵 `342 passed`。当前没有并发 speculative attempt，故双 winner 竞速仍属于 G5.3 外部门；G5.2 继续 No-Go，不修改运行时。

> **2026-08-12 MT-LORA-N0 开发门完成**：统一 MODEL-TOOLS CLI 新增 `sd15_lora_inspect` / `sd15-lora-inspect`。它只读取单个 Safetensors 的长度前缀和最多 16 MiB JSON header，精确校验 dtype/shape/data range、重叠和根目录边界；不导入 Torch/Diffusers/Safetensors，不读取权重区，也不初始化 CUDA。报告只保留 LoRA 结构计数、组件摘要、数值训练参数及训练集/触发词聚合，不输出元数据原文、文件名、路径、未知键名或可逆摘要。专项 `48 passed / 2 skipped`；真实 LoRA 的兼容性、合并和质量不由此票宣称。

> **2026-08-12 EX-N3-S0 质量证据开发门完成（后续更新）**：`scripts/experiment_core/quality.py` 冻结 `qlh.experiment_quality.v1`，collector 从结果文件的可选 `quality_evidence` 只提取固定、可审计的 LLM 计数式证据和 SD 质量门安全引用，并从 `metrics` 中剥离原始载荷。LLM 证据必须匹配当前 plan 的 prompt set ID/SHA-256；SD 适配器只保留资产/工件标识、自动门状态和计数、人工审核状态/人数，不复制 prompt、图像/路径、审核人或错误原文。缺失明确记为 `not_collected`，不合规输入只记为脱敏 `invalid`。`report.md` / `summary.json` 已成为首个消费者。后续 EX-N3 LLM 质量执行和三轮阈值修订已完成，但弱 Qwen Q4 校准地板不改变性能 gate，也不得作为质量通过结论。

**当前可执行队列**：`EX-N3-GEMMA-S1` 已完成，当前没有可安全推进的 EX-N3 实现票。下一项为真实 SD→Gemma 三轮标定：先满足固定 SD 工件、可调用的 Gemma 路线 A、RAM/显存资源和 SD/Gemma 串行调度，再按 §6.2.5 取数与人工复核；在此之前不得以静态 fixture 取代模型验收，也不得启用 `quality.required=true`。`TG-OPT-G4.3` 和 `TG-OPT-G5.1` 的只读开发门均已完成，G5.2 因未发现可证明缺口记为 No-Go，G5.3 继续 Blocked。外部环境恢复后统一执行 `L1-1/L1-2/L1-3`、问题 #3/#5/#8、认证 N1/N2、`T9.6-R2-EXT`，并对任务图执行真实 Worker 断线/重启 journal 验收。

**Qwen 支线（文档与调研完成）**：`QW3-S0` 已冻结 Qwen3-4B、Qwen3-VL-4B 与 Qwen3.5 的官方工件、thinking 控制和运行时边界；下一票 `QW3-D1` 是受控下载/登记而非运行时支持。先逐一 resolve，冻结 revision、许可证、文件清单、大小和 SHA-256；再顺序传输 Qwen3-4B 的官方 Safetensors 与 Q4_K_M。仅在前者通过后，才传输 Qwen3-VL-4B 的 Safetensors、语言 GGUF 与 `mmproj` 工件集。下载可使用 `QLH_HTTP_PROXY` 或用户持久化的 7897 代理，仍遵循 `环境代理 > 用户代理 > 直连`、Range 续传和重新 resolve 规则。Qwen3/Qwen3.5 不得因此直接进入主应用：主 `transformers` 仍为 4.47.x，Qwen3 Safetensors 需 `>=4.51` 隔离 sidecar，Qwen3.5 需官方要求的最新 Transformers；三轮质量标定和正确率门恢复继续后置。

> **2026-08-10 当前排序复核**：`M1.3`、`MF-AUTH-N1W`、`MF-AUTH-N1A`、`MF-AUTH-N2` 客户端接线和 `MF-AUTH-N2L` 开发门已完成，`MF-AUTH-N1/N2` 整体保持 `Validating`。SQLite schema v7 包含 `storage_retirement`、本地认证会话哈希和登录锁定状态；owner bootstrap、TOTP 双下发、恢复码、登录/撤销、用户管理、业务 API 授权矩阵、账户管理 UI、Tailscale 绑定/换网、本机状态安全投影、审计和停用会话撤销均已有自动化证据。当前相关基线为 Python `1714 passed / 27 skipped`、control `317/317`、gateway `113/113`、前端 `30/30`、浏览器 `5/5`。当前执行维持 `MF-AUTH-N2` 外部验证；Linux 实机 Secret Service/DBus、完整安装首登、纯 IPv6/混合双栈、真实 Tailscale 和双机撤销因真机未到位，集中到最终跨平台联合验收。

**L4-MODEL-FLEET 当前票**：`M1.3 Completed` → `MF-AUTH-N1W Completed（开发门）` → `MF-AUTH-N1A Completed（开发门）` → `MF-AUTH-N2L Completed（本机只读开发门）` → `MF-AUTH-N2 Completed（客户端开发门）/Validating（真实环境）`；并行 `L4-MODEL-TOOLS` 已完成 P0/P1/P2 当前本机门及只读 `MT-LORA-N0`。`L4-GEMMA4` 的 G4.1 后端/官方模型门、G4.2 Web/TUI 图片交互、G4.3.1 原生绑定/ABI 预检和 G4.3.2A 工件身份/资源准入均完成；G4.3.2B 原生初始化与图片语义因本机可用 RAM 未达约 8.74GiB 门而 `Blocked`，G4.3.3 音频/分布式/8K-16K 资源与打包维持 Candidate。认证与 Tailscale 的 Linux/双机/安装包证据、MODEL-TOOLS Linux 工具链及 Gemma G4.3 后续原生路径继续等待各自环境门。

**L4-SD1.5 当前票**：P1 的 `SD-N4` 非硬件开发门已完成：固定四 seed DAG、精确 manifest Worker 池、单节点串行降级、主节点 2×2 CAS 网格、实际 winner 统计和失败/取消/重启清理均已落地，相关扩大回归 `220/220`，Python 全量 `1777 passed / 31 skipped`。物理双机吞吐、网络/OOM/重连和 UI 发布验收因缺设备保持 `Blocked`，实验 API 与 UI 继续默认关闭。`UP-N6.4W` Windows 跨卷保留开发机门与 `NW1.1-NW1.4` 本机路径观测均已完成；外部发布安装门、第二台 CUDA PC 和真实 Tailnet 环境仍待恢复。

本文是下一步工作的**唯一排序入口**。专项计划继续保存技术细节和验收方法，但不得自行改变这里的优先级或完成状态；如有冲突，以源码、最新测试证据和本文依次为准。

---

## 1. 生命周期规则

### 1.1 文档生命周期

```text
草案 Draft → 生效 Active → 冻结 Frozen → 历史 Archived → 废弃 Retired
                    └──────────────► 被新版取代 Superseded
```

| 状态 | 进入条件 | 维护要求 | 退出条件 |
|---|---|---|---|
| 草案 `Draft` | 目标和验收口径尚在讨论 | 不作为排期或能力承诺 | 负责人、依赖、判据确定后转 Active |
| 生效 `Active` | 已作为统一执行入口 | 每次状态迁移同步更新日期、证据和变更记录 | 阶段暂停、归档或由新版接替 |
| 冻结 `Frozen` | 外部依赖长期不可用，或止损判据触发 | 保留原因和恢复条件，不继续投入实现 | 恢复条件满足后转 Active，或转 Archived |
| 历史 `Archived` | 所辖工作全部完成/取消，内容仅供追溯 | 不再修改当前状态，只允许勘误 | 无；必要时转 Retired |
| 被取代 `Superseded` | 新总计划已生效 | 文件头必须链接替代文档 | 清理周期内转 Archived/Retired |
| 废弃 `Retired` | 已无有效引用价值 | 从 README 推荐索引移除 | 按维护规则人工删除 |

本文件当前为 `Active`。当 L0–L4 全部进入 `Completed / Cancelled / Frozen`，或项目发布新的总计划时，本文件必须迁移为 `Archived` 或 `Superseded`，不得继续以“现行计划”存在。

### 1.2 工作项生命周期

```text
候选 Candidate → 就绪 Ready → 进行中 In Progress → 验证中 Validating → 已完成 Completed
                         │              │
                         ├→ 阻塞 Blocked┘
                         ├→ 冻结 Frozen
                         └→ 取消 Cancelled
```

| 状态 | 判定规则 |
|---|---|
| 候选 `Candidate` | 有价值，但不在当前承诺范围，或前置决策尚未完成 |
| 就绪 `Ready` | 前置条件齐备，可立即开始 |
| 进行中 `In Progress` | 已有实际实现、测试或联调活动；只有明确证据才能进入 |
| 阻塞 `Blocked` | 目标仍有效，但缺硬件、外部端点、权限或上游产物 |
| 验证中 `Validating` | 实现已完成，正在执行完成判据；不得提前写“已完成” |
| 已完成 `Completed` | 本文所列完成判据全部满足，并有可定位证据 |
| 冻结 `Frozen` | 止损判据触发或短中期不再投入；必须写明恢复条件 |
| 取消 `Cancelled` | 目标不再需要；必须记录替代方案或取消理由 |

状态迁移必须同时更新：本表状态、证据链接/命令、阻塞原因、下一动作、最近复核日期。代码合入不等于完成；自动化通过也不能替代要求中的真机、真实模型、安装包或目视验收。

### 1.3 改动分级与门限（2026-08-12 决策，防过度设计）

门体系复盘（2026-08-12）：56 个"开发门"、60+ 专项子票、64 处登记点，存在对低风险改动套发布级流程的过度设计（"1+1=2 也要哈希和烟测"）。自本文档起，所有工作项按**改动分级**确定门槛与登记方式：

| 级别 | 适用 | 门槛 | 登记 |
|---|---|---|---|
| **trivial** | 只读工具、常量/文档/单文件小修、测试改进 | 定向测试通过（全量不强制）；无签名/清单/目视要求 | 变更记录单条，不写复核段 |
| **常规** | 功能实现、协议改动、新端点 | 定向 + 全量回归 + 冒烟（必要时） | §7 变更记录一处 |
| **发布级** | 安装包、签名/授权链、资产生成与对外分发 | 清单/deep/验签/双人目视全套 | 完整登记（专项 + §7） |

配套规则：

1. **登记合并**：§2.1 增量复核段只保留"当前票指针"，历史统一进 §7 变更记录，不再为每个子票双段登记；
2. **工具类降级**：`gguf_convert` 保留"校验 + 原子落位"，移除摘要旁车先行/无覆盖硬链接/竞态回收（单用户单机工具无并发写者）；只读工具（`gguf_inspect`/`gguf_verify`/`models_sweep`/`models_sync_status` 等）默认按 trivial 执行；
3. **提示词集校验降频**：实验 manifest 与 `ps-v1` 不匹配从"拒绝运行"降为"警告 + 记录"（探索期）；发布级实验可恢复严格模式；
4. **历史证据不删**：已建门及其复核记录保留为审计轨迹，随登记合并自然收敛，不回改历史状态。

---

## 2. 当前基线与总目标

### 2.1 2026-07-28 基线

- Python 全量回归：收集 1056 项，`1030 passed / 23 skipped / 3 failed`；3 项为关闭端口时“不可达/超时”错误分类差异。
- 前端：`9/9` 测试通过，生产构建成功。
- Android：Full Debug Kotlin 编译成功；APK 组装、安装、Lite 编译和真机目视验收未完成。
- PC Full Worker：本机门槛通过，仍停在 TC-N2.4 物理设备准入；`adapter_connected=false`、`task_dispatch_enabled=false` 保持。
- PyTorch 层间流水线、控制面与 TUI：有单机/双进程证据，没有真机双设备验收。
- TP 孤岛与外部 Provider：阶段 1 PoC 已实现，没有真实 vLLM/SGLang 持续负载与收益证据。
- 投机解码：实验端点可用，未接生产解码循环；`PagedKVCache.truncate(n)` 已实现并验证，真实 draft/verify 与生产接线仍未完成。
- 本轮未重新生成 PC/Linux/Android 分发包。

2026-07-31 最新复核：全量回归收集 1089 项，`1066 passed / 23 skipped`，无失败；L0-1 的关闭端口跨平台错误分类和 L0-4 的 KV 尾部回滚已经收口。SD 1.5 侧车的单机量化和 QKV 融合已完成真实 CUDA 验证，但分布式 Worker 仍未接入。以上 2026-07-28 条目保留为进入本计划时的历史基线。

2026-08-06 最新复核：**微服务架构改造**（独立专项，~~Active~~；**2026-08-09 已冻结主线**，见 §4.6 与[专项计划](微服务架构改造计划.md)顶部冻结决定）阶段 0/1 主体完成、阶段 2 的 2.1-2.4 完成（网关端点 96/96 可达、静态托管、TUI T1-T8）、阶段 3 的 3.1+3.2 全部 9 域完成（control-svc 契约 136/136；2026-08-05 双网关 contract_diff 收敛 61/26，修复 models/load 白名单、models/switch 注册校验、cancel 格式校验与二进制透传 4 项真实缺陷）；2.5/3.3 删除动作按并行共存策略冻结至清理阶段 §5.1。TUI 命令系统扩至 27 条（73 用例，2026-08-05）。SD 1.5：`SD-N1` 于 2026-08-05 标记 `Completed`，`SD-N2`、`SD-N5.1` 和 `SD-N5.1A` 为 `In Progress`；img2img 与 IP-Adapter `reference` 的引擎/服务/API/前端及双模型完整自动质量/显存门均已通过，报告均于 2026-08-06 双人目视签字完成（Siegfried Kkm./浅草爱音，status=passed）。IP-Adapter 已冻结并下载 `h94/IP-Adapter@018e4027...` 资产，稳定 SHA `671c7452...e94c4`；原版 36/36、90s 35/36 有效图达到每档至少 10 张要求，唯一 safety 黑图留证且不参与质量计数；动态 image encoder 的 CPU-offload hook 和安装包 tqdm 无控制台问题已实机修复。以上条目均有专项计划与测试证据可定位，不再重复基线数字。

2026-08-07 增量复核：`SD-N5.2` 局部重绘进入 `Completed`。固定 `stable-diffusion-inpainting@8a4288a...` 的 2,742,261,613-byte FP16 集合并下载校验，稳定 artifact SHA 为 `ddd6d69a...1c605`；专用 9-channel pipeline、source/mask 双租约、API/网关和 Web 遮罩编辑器均已接通。RTX 4060 Laptop GPU 上 10 seed 的全黑/局部白/全白 mask 语义与显存门 10/10 通过，连续 allocated span 为 0、峰值 reserved 约 3.40 GiB；Edge 150 真实画布上传与 inpaint 任务通过。

2026-08-07 SD-N5.3 增量复核：完整 InstructPix2Pix 与 ControlNet IP2P 已完成同源图三指令真实对照，默认路线冻结为 `timbrooks/instruct-pix2pix@31519b5c...`（MIT，稳定 artifact SHA `a6626f7f...c872ab`），ControlNet 保留为未暴露实验候选。专用 Inspector/执行器、API/网关/Web 指令编辑入口和独立 `image_guidance_scale` 已贯通；固定 10 条指令自动门 10/10、0 safety、10 张唯一、连续 allocated span 为 0，峰值 reserved 约 3.43 GiB，Edge 150 真实链路通过。`SD-N5.3` 仍为 `In Progress`，双人目视签字已于 2026-08-07 完成（Siegfried Kkm./浅草爱音，报告 status=passed），不额外承诺能力。

2026-08-07 SD-N3.0 增量复核：持久化图像 Blob 基础设施和显式 Task Worker v3 图像 schema 已落地。Blob 层使用 SQLite WAL 元数据与内容寻址对象，具备分块上传、SHA/MIME/像素校验、owner/父引用、attempt lease、短期 HMAC grant、容量/TTL 回收和事务提交后 GC；协议限定专用 `pc_diffusion_worker`、`diffusers_sd15`、三类图像 Stage、artifact manifest 和不可变 Blob descriptor。现有文本 Worker 适配器仍固定 v2，v3 数据面、`RemoteDiffusionProvider`、TaskGraph 执行和真实 PC Worker 均未接通，因此不得宣称分布式图像可用。下一步为 SD-N3.1 受控 HTTP 数据面与 v3 协商。

2026-08-07 SD-N3.1（数据面与 wire schema）增量复核：主 FastAPI 已挂载不进入 OpenAPI 的内部 Blob 数据面；仅在 32+ byte 集群密钥可用时，于 `STATE_DIR/diffusion/distributed_blobs` 创建 SQLite 内容寻址 store。上传/下载分别绑定 attempt + upload session 或 attempt + Blob lease，并以短期 HMAC Bearer grant 授权；分块重放、重复 commit、范围下载、错误 scope 和过期 grant 均有自动化覆盖。v3 `stage_offer`/`stage_result` 的 transfer plan 严格覆盖对应 descriptor 且纳入摘要，输入由协调器供 Worker 拉取，输出由 Worker 供协调器拉取。v3 adapter、远端 Provider、TaskGraph 执行和真实 Worker 仍未接通，不能标记为分布式图像可用。

2026-08-07 SD-N3.1（控制面 library）增量复核：独立 v3 `DiffusionCoordinatorControlPlane`/`DiffusionWorkerAdapter` 已覆盖 image Worker hello/capability snapshot、单 active offer、accepted/terminal replay、busy/expired lease 拒绝、取消首终态 fencing、重复取消幂等和安全错误回执；fake executor 已完成单 `image_generate` 代码级闭环，文本 Worker v2 回归不变。该模块没有接入 transport、`RemoteDiffusionProvider`、TaskGraph 或实际 Blob 拉取/上传，状态刻意保持 `adapter_connected=false`、任务调度关闭，不能标记为分布式图像可用。

2026-08-07 SD-N3.2（隔离 Provider 与 Blob 回传）增量复核：`RemoteDiffusionProvider` 只接受 v3 image Worker、显式 artifact manifest 和显式 dispatch，使用 result ingestor 在内存中消费 output transfer plan，禁止将 HMAC Bearer grant 写入 `StageResult.metadata` 或 journal。`DiffusionBlobTransferClient` 只使用标准库 HTTP，分块校验 range、MIME、响应 SHA、总长度和总 SHA，`publish_output()` 为 Worker 结果建立本节点 descriptor 与 attempt-scoped 下载授权。fake transport 已验证 Worker PNG 发布、Provider 结果 fencing、内部 HTTP 分段拉取和协调器本地 store 落盘；篡改 range fail-closed。Scheduler/TCP、真实 Diffusers Worker、TaskGraph journal、UI 和物理设备均未接通，运行时仍保持 dispatch 关闭，不能标记为分布式图像可用。

2026-08-07 SD-N3.3（受控 TCP 与 Worker bridge）增量复核：v3 图像控制面默认关闭，开启后仅允许已注册 PC client 通过既有 `TASK_WORKER` TCP 帧进行 hello；主节点仍拒绝全部 v3 Stage 响应、不会创建 image Provider 或开放调度。客户端可由 API 生命周期显式安装 adapter，处理 `hello_ack/offer/renew/cancel`，断线会取消 active stage 并使协商失效。`DiffusionWorkerRuntime` 仅对已加载且 SHA 明确的本机 SD 基础工件广告 `image_generate`，以本地可取消 job 运行，成功结果发布到带 attempt HMAC grant 的持久 Blob 数据面；取消、缺 data plane URL 或 artifact 不匹配均 fail-closed。没有 Provider、TaskGraph、双进程或物理设备验收，仍不得标记为分布式图像可用；下一步为 SD-N3.4 主节点 result ingestor/Provider 的显式接线。

2026-08-07 SD-N3.4 增量复核（`In Progress`）：API 只有在实验开关、master 角色和认证数据面同时成立时才向 Scheduler 注入 coordinator result ingestor；已协商 Worker 随后绑定为 `RemoteDiffusionProvider`，并在 TaskGraph 开启时按稳定 ID 注册。默认关闭的单 `image_generate_v1` 实验接口已接入，支持显式 artifact 选择、单 Stage workflow、workflow 取消和 coordinator CAS owner 读取；TCP 心跳会刷新图像 Worker 健康时间。Scheduler 级 fake 闭环、API workflow/取消/工件歧义/CAS owner 回归以及独立 Worker 子进程的真实 TCP+FastAPI HTTP 固定 PNG 回传均通过；子进程仍使用确定性 fake SD executor。真实 Diffusers/GPU Worker、迟到结果/journal 恢复、物理设备和多 seed fan-out/fan-in 仍无证据，因此不得标记 SD-N3.4 完成或宣称分布式图像已发布。
2026-08-09 L0-5 收口：两个 v2 测试调用（`tests/test_external_provider.py::test_model_identity_external_api_accepted` 与 `tests/test_speculative.py::test_speculative_model_identity_accepted_by_validators`）的 `_validate_capabilities(..., version=2)` 已正式落地；干净工作区全量 `pytest -q` 实测 **1657 passed / 7 skipped / 0 failed**（2026-08-09，收集 1664 项），L0-5 转 `Completed`，发布门解锁该项。专项基线：Launcher/Updater `82 passed`，MODEL-FLEET/control-svc 全量 `274/274 passed`，gateway `104/104 passed`，前端单元 `27/27 passed`，模型工件浏览器 mock `1/1 passed`。

### 2.2 总目标

按顺序达成四个生命周期门：

1. **基线可信**：测试结果可重复，3 个环境敏感失败有稳定结论，文档口径统一。
2. **真机可验**：控制面、TUI、层间流水线和 PC Full Worker 在物理设备上完成故障与性能验收。
3. **版本可交付**：Android Full/Lite、PC 集显/独显和 Linux 包按同一版本构建并完成最小安装验证。
4. **扩展可决策**：外部 Provider、TP 孤岛和投机解码用真实模型数据决定生产化、冻结或取消。

---

## 3. 总体路线与阶段门

```text
L0 基线收口
  ├─ 测试错误分类稳定
  ├─ Android UI 运行验收
  └─ 文档与证据统一
          │
          ▼
L1 真机准入 ──► L2 可交付版本 ──► L3 生产化扩展
  │                    │                    │
  ├─ 控制面/E0        ├─ 全平台构建       ├─ 外部 Provider
  ├─ PC Worker N2.4   ├─ 安装/升级验证    ├─ TP 孤岛
  ├─ 层间流水线       └─ 发布清单         └─ 投机解码止损
  └─ TUI
                                               │
                                               ▼
                                      L4 后续探索（按门槛进入）
```

| 阶段 | 生命周期 | 进入门槛 | 退出门槛 |
|---|---|---|---|
| L0 基线收口 | `Active` | 当前即可执行 | L0-1、L0-2、L0-3、L0-5 全部 Completed |
| L1 真机准入 | `Blocked` | 物理从节点恢复并具备同版本模型工件 | L1-1～L1-4 全部 Completed |
| L2 可交付版本 | `Candidate` | L0 完成；涉及分布式包时还需 L1 完成 | 各目标包构建、安装、冒烟、版本/哈希记录齐全 |
| L3 生产化扩展 | `Candidate` | 真实外部端点/多卡环境可用，L0 完成 | 每条路线分别作 Promote / Freeze / Cancel 决策 |
| L4 后续探索 | `Candidate` | 对应 L1/L3 门槛满足 | 专项判据通过；不占用 L0/L1/L2 关键资源 |

---

## 4. 工作分解与当前状态

### 4.1 L0：基线收口（当前最高优先级）

| ID | 工作项 | 当前状态 | 完成判据 | 下一动作 |
|---|---|---|---|---|
| **L0-1** | 稳定关闭端口错误分类测试 | `Completed` | 3 个失败在支持环境稳定通过；明确 `ConnectError` 与 `TimeoutError` 的跨平台契约；全量回归无未解释失败 | 保留确定性异常映射单测；后续 HTTPX 或代理配置升级时重跑端到端关闭端口用例 |
| **L0-2** | Android UI 完整验收 | `Validating` | Full/Lite 均 assemble 成功；安装 Full APK；亮/暗/跟随系统及三屏清单通过；保留构建日志与截图 | 2026-08-03 `assembleFullDebug` + `assembleLiteDebug` 构建成功（Full ~29.9 MB / Lite ~18.4 MB）；剩模拟器/真机逐项目视核对 |
| **L0-3** | 文档与结题口径收口 | `Validating` | README、总计划、进度快照、测试标准和专项文档状态一致；无失效链接；不含真实凭据；测试数字来自最新实测 | 2026-08-12 已复核 Q22 与问题 #2：`L1-2A/B` 自动化开发门完成，真实环境门未越级关闭；当前 Python 全量 `2085 passed / 8 skipped`（2026-08-13 实测）；后续实现变更仍须同步复核 |
| **L0-4** | `PagedKVCache.truncate(n)` | `Completed` | 尾截断、尾页 `used` 回退、空页回收完备；往返一致性与页计数无泄漏测试通过 | 保持独立能力边界；真实 draft/verify 达到准入门后再评审生产解码接线 |
| **L0-5** | 全量回归协议校验失败收口 | `Completed`（2026-08-09） | 两个 v2 测试调用在代码中明确传入 `version=2`，随后以干净工作区重跑 `pytest -q` 达到 `1650` 项、`0 failed` | 已落地：`tests/test_external_provider.py` 与 `tests/test_speculative.py` 的 `_validate_capabilities(..., version=2)` 入库；全量实测 `1657 passed / 7 skipped / 0 failed`（2026-08-09） |

### 4.2 L1：真机准入（硬件恢复后立即启动）

| ID | 工作项 | 当前状态 | 完成判据 | 下一动作 |
|---|---|---|---|---|
| **L1-1** | 真机双设备控制面 + E0 | `Blocked` | 注册、心跳、断线、重连、主节点重启全过；直连/DERP 路径、RTT、吞吐有记录；无僵尸连接、误踢或前端假死 | 等从节点恢复；提前冻结版本、模型和故障注入脚本 |
| **L1-2** | PC Full Worker TC-N2.4 | `Blocked` | Qwen/DeepSeek 真实模型、错/缺模型、切换、断网重派、重启、长文本与连续任务、双端安装包全部过门 | L1-1 后按《任务链下一阶段实施计划》执行；未通过前保持准入关闭 |
| **L1-2A** | 任务链 per-stage 模型身份（已知问题 #2） | `Completed`（无硬件开发） | `StageSpec` 可选身份覆盖 workflow 默认；每个远端 Stage 按自身身份匹配 Provider；fake Worker A/B 异构链通过；无身份模板及层间/TP 同模型约束不回归 | 已由 `L1-2B` 使用；真实派发仍保持实验默认关闭 |
| **L1-2B** | 固定 LLM→SD 跨通道任务模板（已知问题 #2） | `Completed`（无硬件开发） | v2 `image_prompt` 精确模型 Worker 与 v3 artifact manifest 图像 Worker 通过固定 `llm_sd15_v1` 链完成；文本仅本地绑定为图像根输入；取消/失败清理与同节点角色冲突均有覆盖 | 不开放任意 DAG 或外部 LLM；物理双节点、断网/重启/长时与安装包验证并入 `L1-1/L1-2` |
| **L1-3** | PyTorch 层间流水线真机验收 | `Blocked` | 13 行矩阵与 6 类证据齐全，至少证明性能或容量一项收益 | 与 L1-2 共用设备窗口，分别留存执行模式和参与节点证据 |
| **L1-4** | TUI Windows/Linux 真机验证 | `Blocked` | 远程主节点、7 屏数据、连接流程和 `--plain` 降级在真实环境通过 | L1-1 在线后执行 |

### 4.3 L2：可交付版本

| ID | 工作项 | 当前状态 | 完成判据 | 下一动作 |
|---|---|---|---|---|
| **L2-1** | Android Full/Lite 交付包 | `Candidate` | 两 flavor APK 构建、签名策略确认、干净设备安装/升级/启动/聊天冒烟通过 | L0-2 后冻结 Android 版本 |
| **L2-2** | Windows 集显/独显安装包 | `Candidate` | 干净机安装、启动、模型选择、本地聊天、卸载通过；分布式声明与实际能力一致 | L0 完成后构建；若宣称 Worker 可用则必须等待 L1-2 |
| **L2-3** | Linux `.deb` | `Candidate` | 安装、systemd 启停、日志、TUI、本地聊天与卸载通过 | 与 L2-2 使用同一版本和发布清单 |
| **L2-4** | 发布证据包 | `Candidate` | 版本号、commit、依赖锁、文件哈希、测试摘要、已知限制、回滚方法齐全 | 三类产物完成后统一生成，不手工散落记录 |
| **L2-5** | 独立 Launcher/Updater 干净机发布验收 | `Validating` | Launcher Setup 与主应用 Setup 独立安装；Windows 主应用发现/切换/失败回滚、Linux `.deb` 安装、断网和权限场景通过；保留版本/签名/哈希/回滚证据 | 依赖现有 `UP-N4` ZIP 与发布清单；Android 更新另归 UP-N5 |

### 4.4 L3：生产化扩展（可与 L1 并行，但不得抢占关键路径）

| ID | 工作项 | 当前状态 | 生命周期出口 | 完成/止损判据 |
|---|---|---|---|---|
| **L3-1** | 路线 A：真实 TP 孤岛 | `Blocked`（缺多卡环境） | `Completed` 或 `Frozen` | 容量收益或 P50 ≥20% 改善，故障重编排通过；llama.cpp RPC 30 分钟不稳定则该变体 Frozen |
| **L3-2** | 路线 B：外部 Provider 生产化 | `Blocked`（缺真实端点） | `Completed` 或 `Frozen` | 会话粘性、费用/token 上限、前端显式开关和 fail-closed 全部完成；真实端点稳定性达标 |
| **L3-3** | 路线 C：真实 draft/verify | `Blocked`（缺真实同族 draft/verify 模型与端点） | `Completed`、`Frozen` 或 `Cancelled` | 若 `tokens_per_round ≤ 1.5`，或 `RTT > tokens_per_round / 本地 tok/s`，冻结 C-1；否则再进入生产接线评审 |

### 4.5 L4：后续探索

以下项目保持 `Candidate`，只有前置阶段完成后才能提升为 `Ready`。**优先级标注**：`P1`=近期推进（已有实现/正在实施，完成判据明确）；`P2`=调研结论已形成、待前置条件（真机/模型/打包阶段）或资源就绪后实施；`P3`=等待维护窗口/低紧迫度，不影响关键路径。

> **2026-08-11 状态覆盖**：本节早期 SD 摘要中的“SD-N3.1 未接 Provider/TaskGraph/PC Worker”已被后续增量记录取代。SD-N4 非硬件开发门已完成，但两台物理 PC 与发布门仍阻塞，不能宣称分布式图像已发布。TUI 专项已完成 T9.6 开发接线、T9.6-R 发布构建、T9.6-R2 Windows 开发机实装和 UP-N6.4W 跨卷保留；开发机已补真实 Qwen INT4 `interactive` SSE/Textual 本地会话与冻结聊天程序/签名树预检，但外部干净机 Setup、新 shell/环境变量、Linux `.deb`、安装包真实模型会话和默认入口仍待。

- **[P1] L4-SD1.5：Diffusers SD 1.5 图像引擎、编辑与分布式批次展示**：仍为 `Candidate`；`SD-N1` 本地单机引擎/API 已于 2026-08-05 `Completed`，`SD-N2` 于 2026-08-11 `Completed`（含正式离线资产包：五资产 15 GB 真实打包、导入闭环 5/5、离线 CUDA 冒烟通过），`SD-N5.2` 于 2026-08-07 `Completed`，`SD-N3`、`SD-N5.1`、`SD-N5.1A` 和 `SD-N5.3` 为 `In Progress`。原版 SD 1.5 已在 RTX 4060 Laptop GPU 8 GB 上完成 10 轮真实生成，U-Net Linear 8-bit 量化和 Diffusers QKV 融合也已验证。主节点 Web 图像工作区现已接通本机资产登记、profile 加载、LLM/SD 显式切换、固定 preset、step 进度、取消和 PNG 生命周期。90s DreamBooth `aa8a082c...` 的 4.87 GB 组合资产已实际下载并完整校验，固定十种子自动门第二轮 10/10 通过，安全检查 0 次触发。**img2img 已于 2026-08-06 实现后端全链路和前端工作区，并修复生成结果续编的 Blob purpose/owner 契约；真实 inference-svc 生命周期门与 Edge 两轮结果续编点击链路通过。固定源图下原版 28 steps、90s 40 steps 各完成 10 seed × 0.25/0.55/0.85 共 60 张，自动完整性、唯一性、安全和连续显存门全部通过；两份报告于 2026-08-06 双人目视签字完成（Siegfried Kkm./浅草爱音，status=passed）。** **`SD-N5.1A` 已冻结下载 `h94/IP-Adapter@018e4027...`，接通本地识别、API/引擎生命周期和 Web `reference` 入口，并修复动态 ViT-H image encoder 未进入 Accelerate offload hook 及 tqdm 无控制台问题。固定人物参考图下，原版 28 steps 36/36 有效，90s 40 steps 35/36 有效且 safety 黑图留证；两份完整自动报告于 2026-08-06 双人目视签字完成（Siegfried Kkm./浅草爱音，status=passed），峰值 reserved 约 2.60 GiB；主要要素保持已目视确认，不承诺精确身份锁定。** **`SD-N5.2` 已固定下载 `stable-diffusion-inpainting@8a4288a...`（稳定 SHA `ddd6d69a...1c605`），接通专用 9-channel pipeline、source/mask 双租约、Web 黑白遮罩编辑器和真实 Edge 链路；10 seed × 20 steps 的 3 黑/4 局部白/3 全白矩阵 10/10 通过，0 safety flag，连续 allocated span 为 0，峰值 reserved 约 3.40 GiB。** **`SD-N5.3` 已冻结完整 InstructPix2Pix `31519b5c...`（稳定 SHA `a6626f7f...c872ab`），接通专用执行器、API/网关/Web 与独立 guidance；10 条固定指令自动门 10/10、0 safety、显存门和 Edge 链路通过，双人目视签字已于 2026-08-07 完成（Siegfried Kkm./浅草爱音，报告 status=passed）；ControlNet IP2P 保留为实验候选。** 正式离线资产包已完成（2026-08-11，五资产 15 GB，见[SD 1.5 离线资产包与签名源站发布计划](SD%201.5离线资产包与签名源站发布计划.md)）；SD-N3.1 已完成持久内容寻址 Blob store、受控 HTTP 数据面、v3 transfer plan schema 和仅限 fake executor 的 Worker/协调器控制环，但尚未接 transport、实际 Blob 拉取/上传、`RemoteDiffusionProvider`、TaskGraph 执行或 PC Diffusion Worker。编辑路线按 SD-N5.0 公共输入 blob/API（已完成）、SD-N5.1 图生图、SD-N5.1A IP-Adapter 参考图、SD-N5.2 专用 inpaint、SD-N5.3 InstructPix2Pix/ControlNet IP2P、SD-N5.4 分布式接入推进；不得用普通 img2img 或图片描述文本冒充人物一致性，也不得把 IP-Adapter 宣传为精确身份锁定。跨 PC 展示仍依赖 L1-1、L1-2 和新的图像 Stage/blob 协议，第一阶段只做完整 Worker 的任务级 fan-out，不拆单张图的 U-Net 去噪循环。细节见 [SD 1.5引擎与分布式图像生成实施计划](SD%201.5引擎与分布式图像生成实施计划.md)。
- **[P2] L4-QUANT-LOWBIT：2/3-bit 与 4-bit 量化路线评估**：当前 PyTorch 仅支持 FP16/INT8/NF4 4-bit，不能把 `int2`/`int3` 当作 bitsandbytes 开关；2/3-bit 正式只面向计划中的 14B 及以上模型（Qwen2.5-14B、DeepSeek-R1-Distill-Qwen-14B/32B、DeepSeek V2-Lite/Coder V2-Lite 等），Qwen-1.8B/1.5B/7B 只保留转换和 loader smoke 入口，日常不建议给小模型降智。第一阶段从原始 F16/BF16 直接生成 GGUF `Q3_K_M`、`Q2_K`、`IQ2_XXS`、`IQ2_XS`，与 F16、`Q4_K_M` 做质量、有效 bpw、内存、速度和 Android 兼容性对照。2-bit 不因“浮点”而默认比 4-bit 精度高，AQLM/QuIP# 属于独立 sidecar，BitNet b1.58 属于重新训练的三值模型；详细阶段、artifact 身份、分布式边界和止损门见 [2-bit、3-bit 与 4-bit 量化调研与实施计划](2bit与4bit量化调研与实施计划.md)。
- **[P1] L4-MODEL-FLEET：一键模型部署与自治集群**：`Active / Candidate`。无硬件批次 M0.1-M5.0、M2.4-M2.5 Web 控制面和 M3.1-M3.3 真实网络/运行时/恢复门已完成：契约 `25/25 + 25/25`、control-svc `301/301`、gateway `104/104`、前端单元 `27/27`、浏览器 mock `1/1`。除 DeepSeek 7B GGUF 外，固定 `Qwen/Qwen2.5-0.5B-Instruct@7ae5576…` 已经 7897 完整拉取 10 文件/999,604,126 bytes，工件 `f8dcc256…bbe9c` 在 CUDA BF16 两次离线加载 `ready`（494,032,768 参数，2,534/1,674ms）并通过本地 deployment prepare。M3.3 在 393,216 bytes 强杀首个 control 后由第二进程从同偏移续传；可控代理在 196,608 bytes 断开并短时 503，同一 job 第 3 次尝试恢复，永久不可用则明确 `transfer_unavailable`。显存支线 `MF-MEM-N0/N1/N2`、N2R 真实 NF4 基线、N2F 固定 64 MiB bridge/取消/长输出门和 N2P Windows 真实延迟决策均已完成；N2P 已冻结双槽 host prefetch 默认优化，不进入默认吞吐路径。认证支线 `MF-AUTH-N0` 已完成文档调研：QLH 用本地 Auth App/TOTP 登录，认证/用户/绑定数据只由用户主节点持有；Tailscale 只做稳定 ID 绑定和网络授权，不是 QLH 的通用 IdP。存储主线 `M1.2` 七片及双平台隔离环境门已完成：Python/Node 共用用户可写 SQLite，Windows 11 的干净目录、损坏活动库、NTFS 写入 deny ACL，以及 WSL2 Ubuntu 22.04 ext4 的干净目录、损坏库和 `0555/0444` 权限门均通过并 fail-closed。网络支线 `NET-N1` 已完成外部入口本地双栈修复：TCP、Python API、control-svc、gateway、分发服务及 PC/TUI/Android IPv6 URL authority 已统一，Windows 真实进程与 Windows/WSL socket 门通过；B3 Tailnet IPv6 短实跑已通过，IPv6-only 安装包、IPv4/IPv6 重连首选和生产路由仍处于 `Validating`。总体下一票仍执行 `M1.3` 远端 PostgreSQL 退场准备，之后进入 `MF-AUTH-N1/N2`；网络实机门在设备/网络可用时并行补齐。独立 `.qlhbackup` 只包含 SQLite，`.qlhmigrate` 才携带实际引用 blob，两者都不迁移 OS 凭据密文；当前没有登录 API、TOTP 生成或真实 Tailscale 授权。远端数据库默认关闭。UVM 仍仅在原生 Linux + 32 GB RAM 条件满足时进入 `MF-MEM-N3` 能力门。Windows/Linux 完整安装、首次认证、gated/认证代理、非 Windows credential adapter、真实 Tailscale、Auth App 实现与 PC Worker 仍属外部门/实现门。详见[专项计划](一键模型部署与自治集群远期计划.md) §10、§11、§16.0。
- **[P2] L4-TUI-CHAT：简化终端对话页**：`Candidate`（T9.0-T9.5、54/54 终端布局、T9.6 接线、T9.6-R Windows 发布构建和 T9.6-R2 Windows 开发机实装门已完成）。Windows CPU/CUDA 签名树、deep 校验、Inno Setup、包内 TUI help/headless fixture、隔离安装/卸载保留/重装关联均有实际证据；本机 Qwen INT4 `interactive` SSE 与 Textual 实页会话、当前 CPU 签名树 `5016/5016` deep 和冻结 TUI `--help` 也已复核。Linux 使用包内 venv，原生工具链预检已接线。仍缺外部干净 Windows、Linux `.deb` 构建安装、**安装包**真实模型会话、inference-svc interactive 历史事务提交、物理从节点指标一致性和默认入口决策。下一票仍为外部环境门 `T9.6-R2-EXT`，详见 [TUI 适配与聊天页实施计划](TUI适配实施计划.md) §9。
- **[P2] L4-任务图图树变换：任务图图树变换调研与实施**：`TG-OPT-G0/G1/G2.1/G2.2/G2.3/G3.1/G3.2/G3.3/G4.1/G4.2/G4.3/G5.1 Completed（开发门）`，`TG-OPT-G5.2 No-Go`，`TG-OPT-G5.3-P0 Completed（只读候选门）`，完整 `TG-OPT-G5.3 Blocked`。调研结论与实现结果见[任务图图树变换调研与实施计划](任务图图树变换调研与实施计划.md)：现已具备语义约简、payload reference、Stage 共享/合并/融合、支配/后支配、静态关键路径、离线排序模拟、检查点/取消建议、attempt 图一致性审计和推测执行候选成本/资源门；均不改变实际 DAG、attempt 或 runtime。完整 G5.3 仍需真实双 Worker 竞速与唯一提交验收。
- **[P2] L4-重模型实验：DeepSeek 重模型实验支持**：`Candidate`（部分实施）。模型注册槽位已落地（`model_config.py`），**真实加载与分布式兼容仍需逐模型验收**；目标 Qwen2.5-14B/DeepSeek-R1-Distill 系列 14B/32B，依赖 14B+ F16 工件下载与高显存环境；验收通过前不得宣称重模型可用。详见 [重模型实验支持计划-DeepSeek](重模型实验支持计划-DeepSeek.md)。
- **[P1] L4-GEMMA4：Gemma-4-12B 多模态支持**：`Active（G4.1/G4.2/G4.3.1/G4.3.2A/G4.3.2B/G4.4/G4.6 Completed；G4.5 产品代码与 CUDA 包构建完成，外部干净机 Pending）`。官方 `gemma4:12b` 已通过本地代理下载并完成 Ollama 原生文本/图像、QLH `external_api` 与真实 Edge 浏览器图片会话；G4.3.2B 原生图片 smoke、G4.4 独立工件、G4.5 产品代码/CUDA 包构建和 G4.6 开发机对照已完成。KIP-12 已关闭原生 binding 身份回退风险；外部无 Ollama 干净机、音频、8K/16K、真实分布式和生产准入继续后置，PyTorch 层真实准入风险见 #11。详见 [Gemma-4-12B多模态支持方案](Gemma-4-12B多模态支持方案.md)。
  G4.3.1/G4.3.2A 的 binding/工件/资源预检和 G4.3.2B CPU-only 初始化与固定图片语义对照均已完成；G4.4-G4.6 后续实现已接续到受管独立工件、产品路径和 CUDA 包，KIP-12 冻结 manifest/marker 开发门已关闭。外部无 Ollama 安装包、音频、8K/16K 和真实分布式继续由发布/验收票处理。
- **[P2] L4-仿真测试：分布式推理仿真测试矩阵**：`Candidate`。真机缺位期间以仿真验证分布式路径（[分布式推理仿真测试计划](分布式推理仿真测试计划.md)），与 L1 验收矩阵共用同一套断言口径；可在 L1 阻塞期先行完善矩阵并作为 L1-1/L1-2 的预验证工具。
- **[P1] L4-EXPERIMENT-AUTO：全自动优化实验与报告流水线**：`Candidate`（EX-N0/EX-N1 已完成 2026-08-09，EX-N2 起接入首批实验）。固定提示词集已冻结（`ps-v1-zh-en-code` 30 条，SHA-256 锁定），`scripts/run_experiments.py` + `experiment_core/`（plan/scheduler/runner/collector/report）已实现：串/并行调度与 `(key,value)` 资源互斥（`any` 通配共享池）、超时重试留痕、断点续跑、gate 三态判定、统一 schema v1 落盘与对照表报告；`tests/test_experiment_runner.py` 17 用例全绿。EX-N1 后新优化实验一律经脚本取数，手跑数据不得进入正式报告；作为 L4 各项（量化、层流水线、投机解码、SD 参数等）Go/No-Go 与止损判据的数据来源。
- **[P2] L4-MODEL-TOOLS：模型通用小工具集**：`Validating`。P0/P1/P2 工具、Windows `llama-quantize` 受管包、`gguf_convert` 安全事务和只读 `sd15_lora_inspect` 均已落地到统一 CLI。LoRA 检查器只读取有 16 MiB 上限的 Safetensors header，报告不回显训练集、触发词、文件名或路径；专项 `48 passed / 2 skipped`。GGUF 写入仍要求精确确认、同卷 staging、结构/摘要校验和无覆盖发布。剩余 Linux 真机量化器/`.deb` 验收、完整安装/跨平台入口、真实双机、真实转换批准及 `sd15_merge` 评审均非当前可直接编码票。详见 [模型通用小工具调研与方案](模型通用小工具调研与方案.md)。
- **[P2] L4-ENV-REG：TUI 环境变量注册（打包期）**：`Completed（开发门，2026-08-11）`。Windows CPU/CUDA Inno 安装器实现默认可取消任务和 `/ENVREG=0|1`，通过 `HKCU\Software\QLH\EnvironmentRegistration` 精确追踪包拥有的 PATH 段；Linux `.deb` 以 `/usr/sbin/qlh-env-register` 和 `/etc/profile.d/qlh.sh` 实现显式 opt-in，默认安装不改环境，remove/purge 区分 profile 与选择状态。仅注册命令 PATH，不写入任何配置或密钥。专项 `3 passed`、完整 Python `1829 passed / 32 skipped`、两份 Inno 编译和 Linux shell 语法通过；Windows/Linux 干净机实际安装、卸载和新会话门仍属最终发布验收。详见 [TUI 环境变量注册优化方案](TUI环境变量注册优化方案.md)。
- **[P3] Android 版本远期计划（分布式部分）**：`Candidate`。规划与技术预研完成（[Android版本远期计划](Android版本远期计划.md)），分布式部分全部未实施；UI 层重构已完成并通过 Full Debug Kotlin 编译。完整 Worker 依赖 L1-2，且不得改变 Android 不参与 PyTorch 层拆分的边界。
- **[P3] Android llama.cpp Submodule 迁移**：`Completed`（2026-08-08）。llama.cpp 已由 vendored 源码迁移为 gitlink `47e1de77`，Full Release 构建通过；迁移方案已标记废弃（待手动删除），版本和维护事实源改由 `android/app/src/main/cpp/LLAMA_CPP_VERSION.md` 承担。迁移后真机 Full/Lite 回归仍属于 L2-1/P0-1。
- **[P3] PC 与 Android 交互体验优化（待拆分）**：`Candidate`。历史参考/待拆分（[PC与Android端交互体验优化计划](PC与Android端交互体验优化计划.md)）；拆分完成后按优先级重新评估。
- E3 混合任务链：依赖 L1-2，且 L3-1/L3-2 至少一条通过。
- 超节点画像与 A-1 层段孤岛：依赖 L3-1 通过真实收益门槛。
- 自建 verify 后缀端点：依赖 L3-3 未触发止损。
- TC-N3.1 第二个固定 DAG 模板：依赖 L1-2。
- 激活/KV INT8 传输量化：依赖 L1-3 的真机基线。
- Android 完整 Worker：依赖 L1-2，且不得改变 Android 不参与 PyTorch 层拆分的边界。

### 4.6 并行工程工作流（独立专项，Active，不改变 L0-L4 优先级）

以下工作流按各自专项计划独立推进并维护状态，本表只登记入口与当前状态；不占用 L0/L1/L2 关键资源，不得改写 L0-L4 的完成判据。

| 工作流 | 专项计划 | 当前状态（2026-08-09） | 下一步 |
|---|---|---|---|
| 微服务架构改造 | [微服务架构改造计划](微服务架构改造计划.md) | **已冻结（2026-08-09）**：阶段 0/1 主体、阶段 2 的 2.1-2.4、阶段 3 的 3.1+3.2 全部 9 域已完成（control-svc 契约 136/136）；但所有迁移域从未接管实际运行路径——`start_backend.bat` 仍启动 Python 单体、前端直连 :8000、gateway/scheduler-svc/inference-svc 仅存在于契约测试；Python 侧功能持续迭代形成双基线漂移。冻结主线（3.4 验收、清理阶段 §5.1、阶段 4），待 Python 功能面收敛后重启；MODEL-FLEET 在 control-svc 上的开发不受影响 | 冻结期内不推进 3.4/§5.1/阶段 4；恢复条件：Python 功能面进入稳定窗口后重新对齐契约、重跑 3.4 Go/No-Go（见专项计划顶部冻结决定） |
| TUI 管理终端 | [TUI 适配与聊天页实施计划](TUI适配实施计划.md) | T1-T8、T9.0-T9.5、T9.6 接线和 T9.6-R Windows CPU/CUDA 发布构建门完成；签名 deep、Inno 与包内 TUI fixture 已通过 | T9.6-R2：Windows 干净机安装/卸载/真实后端，Linux 原生 `.deb` 构建安装；真实使用窗口后决策默认入口 |
| 模型部署与自治集群 | [一键模型部署与自治集群远期计划](一键模型部署与自治集群远期计划.md) | P1 Active/Candidate：`M1.3`、`MF-AUTH-N1W`、`MF-AUTH-N1A`、`MF-AUTH-N2L` 和 `MF-AUTH-N2` 客户端开发门已完成；SQLite schema v7、control 本地认证/Tailscale 绑定、本机状态安全投影、Windows DPAPI/Linux Secret Service adapter、Web 首登/账户管理/换网 UI 和 gateway 全局授权矩阵均已落地。`MF-AUTH-N1/N2` 整体保持 `Validating`。Python `2085 passed / 8 skipped`（2026-08-13）、control `313/313`、gateway `114/114`、前端 `38/38`、浏览器 `5/5` | 当前执行 N2 真实 Tailscale/跨平台联合验收；Linux Secret Service/DBus、完整安装首登、纯 IPv6/混合双栈、旧节点撤销和双机实网集中到最终验收；暂不开放 Tailscale OAuth |
| 抗弱网通信 | [抗弱网通信协议专项计划](抗弱网通信协议专项计划.md) | **NW1.1-NW1.4、NW3.1、NW4 本机开发门 Completed；NW1.5 In Progress；NW3.0 契约骨架、NW4.1-F1 fake transport、T-RACE-0 至 T-RACE-5、CLUSTER-JOIN-S0、SYNC-SSH-S0/S1/S2 Completed（2026-08-20）**：Transport v2 envelope、三通道边界、有界 ACK 窗口、稳定故障矩阵、circuit breaker、确定性内存链路、本地 WSS TLS/HMAC/帧 fencing 和 TCPClient/Scheduler 可插拔运行时桥接已通过；默认仍为 Legacy TCP。真实 WSS endpoint、生产证书、443/TLS、双端流量、代理兼容和默认 v2 切换继续后置 |
| 独立启动器与更新 | [独立安装包启动器与自动更新方案](安装包自动更新引导器方案.md) | UP-N0/UP-N1 源码纵切、UP-N2 Ed25519 验签/key rotation、UP-N3 版本目录 staging/current/previous/recover/rollback、UP-N4 Launcher A/B 自更新/健康探针/回滚/诊断已实现；Linux 接线完成；Launcher ZIP 已实际构建并验证发布链路；**UP-N6.0-N6.4W、L4-ENV-REG、T9.6 与 Windows 开发机发布门完成（2026-08-11）** | 下一票 `T9.6-R2-EXT`；Windows/Linux 外部干净机、真实修复发布源、环境变量/新 shell 与真实模型会话端到端验收合并执行 |

---

### 4.7 2026-08-19 可并行开发票（文档分期）

| 优先级 | 票 | 状态 | 范围与边界 |
|---|---|---|---|
| P0 | `NW4.1-F1` | Completed（本地 fake transport 门） | Transport v2 envelope、control/stream/blob、ACK/背压/deadline/circuit breaker、可控内存链路和异常注入；已由 NW4 完成本机 TCPClient/Scheduler 可插拔桥接，真实流量仍后续 |
| P0 | `NW4` | Completed（本机运行时接线门） | `TransportRuntimeBridge` 可按 peer 注入 Scheduler/TCPClient；Legacy 帧仅做 envelope/代际/失败观测，显式 fake v2 支持收发/窗口/熔断；新增 `3 passed`，Transport/TCP 回归 `111 passed`；真实 WSS/443、双机流量和默认 v2 路由后置 |
| P0 | `T-RACE-0` | Completed（本机审计） | 完成注册、ACK、层配置、lease、SQLite 和补丁同步状态盘点；修复旧接收线程迟启动时误关闭新 socket 的竞态 |
| P0 | `T-RACE-1` | Completed（测试基础） | `DeterministicClock`、`EventBarrier`、fake link 的无 sleep 时序骨架；随机压力和业务状态矩阵仍后续 |
| P0 | `T-RACE-2` | Completed（本机确定性矩阵） | 并发注册、REGISTER/ACK/业务帧乱序、旧 socket 迟到、发送/关闭交错、重复连接和 ACK barrier |
| P0 | `T-RACE-3` | Completed（本机确定性矩阵） | 层 reserve/prepare/ACK/release、generation 围栏、任务 epoch/winner/取消/lease expiry；调度/任务专项 `343 passed` |
| P0 | `T-RACE-4` | Completed（本机故障矩阵） | SQLite WAL FULL、整轮对话/消息计数原子提交、子进程退出恢复、operation_id 幂等重放；签名补丁精确 SHA、原子状态 journal、半应用同目标重放和新目标 fencing；定向回归 `588 passed` |
| P0 | `T-RACE-5` | Completed（本机随机压力门） | 三轮固定 seed、每轮 48 个，共 144 个；Transport/SQLite/任务图随机故障、同 epoch winner 与失败证据；`.venv-test` `3 passed / 43.26s` |
| P1 | `CLUSTER-JOIN-S0` | Completed（本机契约门） | Ed25519 client-only grant、请求摘要/目标公钥/TTL/nonce 绑定、字符串/QR 同编码、SQLite WAL 原子 nonce ledger；Auth App/TOTP 由控制面提供 approval gate，Web/TUI、SMTP 和真实跨机后置 |
| P1 | `CLUSTER-JOIN` | Completed（本机控制面/前端门，2026-08-20） | `/cluster/join/request|grant|consume`、SQLite issuer/pending key、Web 字符串/二维码、TUI `/join`；消费后强制 client；API/契约 `11 passed`、TUI `74 passed`、前端 build 通过；布尔审批默认关闭 | 实际 Auth App/TOTP、SMTP/IMAP、双机 TCP 换网、OS 密钥保护和长时验收后置 |
| P1 | `SYNC-SSH-S0` | Completed（本机契约门） | strict Ed25519 host key/fingerprint、固定 helper、无密码/无首次信任 argv、签名帧上限、maintenance lock 和稳定 retry/fallback matrix；`.venv-test` `11 passed` |
| P1 | `SYNC-SSH-S1` | Completed（本机 fake loopback 门） | `FakeSshServer` + 固定 helper；真实临时 Git fetch/reset、签名/受管 origin 复核、dirty/锁/断连/重放/commit-bound 幂等 ledger；专项 `9 passed` |
| P1 | `SYNC-SSH-S2` | Completed（真实双机门） | 主节点受控 client/签名 frame 与从节点专用 key/forced-command/helper 已完成；真机 `status/fetch/apply/verify` 顺序通过，README 换行脏变更已由从节点确认并清理，未触碰用户数据 |
| P1 | `RAG-S0` | Completed（本地主节点门） | SQLite WAL/FULL schema、资料边界、敏感字段拒绝、版本幂等导入、删除/重建基础已完成；`.venv-test` `11 passed` |
| P1 | `RAG-S1` | Completed（本地主节点门） | SQLite FTS5 检索、访问范围硬过滤、重复导入/删除重建索引一致性与查询审计；`.venv-test` `13 passed`，不接 Web/TUI 和 embedding |
| P1 | `RAG-S2` | Completed（本地 provider 门） | 统一 embedding 契约同时支持 Ollama `/api/embed` 与显式原生 llama.cpp；批量上限、超时/不可达/拒绝、有限值和维度校验已完成；`.venv-test` `26 passed`，不自动下载模型 |
| P1 | `RAG-S3` | Completed（本地有界向量门） | SQLite float32 向量存储、模型摘要/维度绑定、活动 revision 过滤、cosine 扫描预算、FTS+向量混排及超预算 FTS fallback；本机 `nomic-embed-text:latest` 768 维真实 smoke 通过 |
| P1 | `RAG-S4` | Completed（本地产品接线门） | 新增 `/api/rag/health`、`/sources`、`/search`、`/rebuild` 和 source 删除；设置页知识库工作区展示引用摘要、健康计数、FTS5 重建与检索；`.venv-test` `31 passed`，前端 Vite build 通过 |
| P1 | `RAG-S5A` | Completed（可恢复任务与容量门） | SQLite `rag_jobs` 保存脱敏 chunk 快照、模型摘要和游标；embedding 每批事务提交、进程内互斥、取消/恢复幂等；新增 `/api/rag/capacity` 与 embedding job 控制面；`.venv-test` RAG 定向 `25 passed` |
| P1 | `RAG-S5B` | Completed（代码门与真实质量门） | 固定 30 条标注查询质量门、查询脱敏、跨进程 lease、provider 可重试故障恢复和旧库迁移；S5C 修复后多源真实 smoke `hit@5=0.966667`、`citation_rate=1.0`、`MRR=0.609444`，达到基线 |
| P1 | `RAG-S5C` | Completed（本地质量门） | 重叠/边界感知 chunk、NFKC 查询规范化、中文二元词 FTS 回退、稳定 FTS/向量混排；容量、模型摘要变化、长时任务和 ANN/`sqlite-vec` 后置验收 |
| P1 | `RAG-S5D` | Completed（本机开发门，2026-08-20） | 容量摘要增加当前/陈旧 embedding 计数；`provider + model_id` 绑定冻结模型摘要，tag/摘要变化清理旧向量并使旧 queued/paused/running job fail-closed；SQLite 跨进程 lease、有限重试和身份/提交围栏回归；新增脱敏 `/api/rag/ann-decision`，默认 FTS/有界 cosine，未安装 `sqlite-vec` 为 `NO_GO`。RAG 定向 `.venv-test` `30 passed` | 不宣称生产 ANN 性能；真实用户规模、provider 长时 soak、sqlite-vec 基准和双机仍后置 |
| P1 | `NW3.1` | Completed（本机开发门） | `src/wss_loopback.py` 提供临时本地 CA/服务器证书、loopback-only WSS、TLS peer 指纹校验、HMAC challenge、TransportEnvelope echo、generation/sequence fencing；专项 `6 passed`。不接入默认 Legacy 路由、不代表生产证书 |
| P1 | `SPC-CS/MEDS/REV` | Completed（本机开发门，2026-08-21） | 投机解码增量三票：SPC-CS 置信度调度（GammaPolicy/AdaptiveGammaPolicy + Session `gamma_mode` + run_speculative_chat 接线）、SPC-MEDS 半自回归（Medusa 候选树逐位接受+修正、eheads=1 严格等价）、SPC-REV 置信驱动逆向回退；static 默认与分布等价保留。定向 `107 passed`；真模型加速收益后置 |
| P1 | `TG-OPT-G5.3-注入闭环` | Completed（本机开发门，2026-08-21） | G5.3 完整版解锁影子级动态故障注入：白名单扰动（尾延迟/成本/准入/空闲 worker/故障域），实际类型 fail-closed、未知键拒绝，同 seed digest 可复现重放、影子恒不启运行时动作。专项 `10 passed`；真实双 Worker 竞速/唯一提交/取消 loser/尾延迟收益仍 Blocked（双机窗口） |
| P1 | `PACK-SLIM` | 本机开发门完整（2026-08-21），真构建待打包环境 | PyTorch 系移出安装包：`packaging/qlh-slim.spec` 只打 qlh_launcher + `_internal/src` + `frontend/dist` + runtime 清单 + pubkeys；`runtime_guard` 每次启动 probe 缺失则 pip 引导外部 runtime venv（CPU 单独 `--index-url .../whl/cpu`、其余默认 PyPI；CUDA 官方源；proxy 可配），以 `_internal` 为 cwd 源码跑 uvicorn。专项 `64 passed`（含 launch/proxy/probe）；体积 CPU 734MB/CUDA 1.7GB+ → 几十 MB；真 PyInstaller 构建 + 首启引导实跑待打包环境 |
| 后置 | `T-RACE-6-C`、真实 WSS/cert | Environment gate | 长时多轮/升级/断电、可达 443/域名或 SMTP；B3 IPv6 短实跑已通过，不阻塞上述本地开发 |

## 5. 责任、依赖与执行节奏

| 工作流 | 主责 | 关键依赖 | 周期输出 |
|---|---|---|---|
| L0-1、L1、L3-1 | 分布式架构组 | PC 从节点、Tailscale、多卡环境 | 测试日志、故障记录、性能表、准入结论 |
| L0-4、L3-3、L4 量化 | 模型优化组 | 真实模型、共享 tokenizer、verify 端点 | 正确性测试、基准、止损结论 |
| L4-SD1.5 | 模型优化组 + 分布式架构组 + 前端组 | CUDA PC、SD 1.5 资产、图像 blob 协议、物理 Worker | 固定 preset、单机/分布式图像、吞吐与故障证据 |
| L4-QUANT-LOWBIT | 模型优化组 + GGUF/Android 维护者 | 14B+ F16 源工件、锁定 llama.cpp、校准集、真实设备 | Q2/Q3/Q4 对照、有效 bpw、质量/速度/内存门、Go/No-Go |
| L4-MODEL-FLEET | 控制面组 + 模型优化组 + 分布式架构组 | M0.1-M5.0、M2.4-M2.5、M3.1-M3.3、MF-MEM-N0-N2P、M1.2/M1.3 已完成；`MF-AUTH-N1W`、`MF-AUTH-N1A`、`MF-AUTH-N2L` 和 `MF-AUTH-N2` 客户端开发门已完成 Web 首登、会话/角色授权、账户管理、本地 QR/字符串、本机只读状态投影、绑定/换网 UI 和 gateway control-svc 接线。`MF-AUTH-N1/N2` 整体仍为 `Validating`；认证资产只属于用户主节点 SQLite/OS credential store | 当前执行 N2 真实 Tailscale/跨平台联合验收；Linux/完整安装/双栈双机证据统一留到真机到位后的最终验收，当前 Windows 不模拟 UVM 或 Linux 凭据门通过 |
| L4-TUI-CHAT | TUI/前端组 + 推理契约维护者 + 分布式架构组 | chat interactive SSE 契约、Textual 可选环境、local 模型、物理 Worker | 真流式会话、取消/恢复、local/distributed 真实指标、打包与旧 TUI 回归证据 |
| 抗弱网通信 | 分布式架构组 + 网络运维 | Tailscale 客户端、自建 DERP、校园网与非受限网络对照环境 | 脱敏路径画像、DERP SLO/切换记录、WSS/netshim PoC 报告、弱网回归数据 |
| 独立启动器与更新 | 打包维护者 + 前端与文档组 | Windows/Linux 打包环境、发布清单、代码签名材料 | Launcher/主应用独立安装、清单签名、故障注入、回滚和发布证据 |
| L0-2、L0-3、L2、L3-2 前端 | 前端与文档组 | Android 环境、打包环境、发布版本 | APK/安装包、截图、索引、发布说明 |

执行节奏：

1. 每次开始工作前确认该项为 `Ready` 或 `In Progress`，并记录所用 commit、环境和模型工件。
2. 每次合入后只把状态推进到 `Validating`；完成判据全过后才能转 `Completed`。
3. 每周至少一次复核 Blocked 项；阻塞解除后按 L0 → L1 → L2 顺序抢占资源。
4. 真实设备或外部算力窗口到位时，优先运行预先冻结的验收矩阵，不在窗口内临时扩需求。
5. 任何止损判据触发后立即转 `Frozen`，记录数据和恢复条件，不以“已投入很多”为由继续。

---

## 6. 发布门与不可越过的边界

以下任一条件成立时，不得宣称“生产可用”或“分布式已完成”：

- Python 当前全量回归为 `1942 passed / 8 skipped`（2026-08-11，测试进程 `NO_PROXY=*`）；早期 `1720 passed / 7 skipped / 0 failed` 仅保留为 M1.2 第六片的历史证据，不能再作为发布基线。
- TC-N2.4 未通过，却把 `adapter_connected` 或默认任务派发状态改为 true。
- 只有单机双进程证据，却宣称真机分布式验收完成。
- Android 只有 Kotlin 编译证据，却宣称 APK/交互验收完成。
- TP 孤岛、外部 Provider 或投机解码只有 mock/实验端点证据，却宣称有性能收益。
- 路线 B/C 未经用户显式授权让内容出集群，或把默认数据作用域改为 `allow_all`。
- 发布包没有对应 commit、依赖版本、哈希、测试摘要和已知限制。

---

### 2026-08-18 开发优先复核（验收后置）

本轮按“先完成可脱离硬件的代码，再统一外部验收”复核开发票：任务图变换已完成至 G5.2，并完成 G5.3-P0 只读候选门；QW3/MM1、Gemma、模型工具、UX 和 KIP 开发链均已收口；真实模型采样质量、CUDA、双机、生产路由、Android 真机与大文件/断网场景继续保留为后置验收。完整 `TG-OPT-G5.3` 仍因双 Worker 竞速/唯一提交条件阻塞，不将 P0 静态候选门伪装成生产推测执行。

Android 清单复核发现 `AND-F-01` 已由 `f05bcf3` 交付，旧文档状态已修正；本轮完成 `AND-E-01` 客户端开发门：Keystore 会话存储接入 `ApiClient`，登录/会话查询/登出、Bearer 注入、401 清理和 Authorization 日志脱敏均已覆盖，JVM contract tests 通过。真实 auth API 和真机安装验收后置。当前无其他明确不依赖硬件且未被外部条件阻塞的 P0/P1 开发票，后续转统一外部验收；如出现新的纯软件缺口，按同一拆分规则单独立票。

## 7. 变更与归档记录

| 日期 | 生命周期事件 | 说明 |
|---|---|---|
| 2026-08-11 | `L4-ENV-REG 开发门完成` | Windows CPU/CUDA 安装器新增可取消的 `envreg` 任务、`/ENVREG=0|1`、当前用户 PATH 精确归属追踪和 `ChangesEnvironment=yes`；Linux 包新增显式 opt-in 的 `qlh-env-register`、持久选择和 profile 生命周期。只写 PATH，不写端口、模型位置、密钥或代理。专项 `3 passed`、完整 Python `1829 passed / 32 skipped`，两份 Inno `ISCC /Q /O-` 与 Linux shell 语法通过；跨平台干净机安装、卸载和新 shell 仍待最终发布验收。下一票 `L4-TUI-CHAT T9.6`。 |
| 2026-08-11 | `UP-N6.2 diagnose 开发门完成` | 内置 10 类本地症状知识库、签名 quick 证据、CUDA 无 shell 驱动探测、磁盘/权限只读检查、CLI/JSON/GUI/TUI 与脱敏 ZIP 摘要落地；错误文本不落盘，模型/用户数据零扫描。打包链路 `126/126`，Python 全量 `1810 passed / 31 skipped`；下一票 `UP-N6.3 repair`，干净机安装仍待验收。 |
| 2026-08-11 | `UP-N6.1 runtime verify 开发门完成` | 新增签名先行的 quick/full/deep 只读校验、稳定 JSON/退出码、`qlh-launcher verify`/`bjtu verify` 路由和启动调用异常后的 quick。只枚举签名程序路径；full 的大文件采样仅证明可读性，deep 才提供完整哈希防篡改。故障注入与打包链路 `119/119`，Python 全量 `1803 passed / 31 skipped`；下一票 `UP-N6.2 diagnose`，干净机安装仍待验收。 |
| 2026-08-11 | `UP-N6.0 安装完整性基线开发门完成` | 新增签名 schema v1 安装清单、构建期 fail-closed、Windows Inno/Linux `postinst` 验签、Launcher 远端 A/B 自更新签名与身份绑定；用户数据根被扫描/契约双重排除。相关回归 `111/111`、Python 全量 `1795 passed / 31 skipped`，冻结验签 EXE 已实测；下一票 `UP-N6.1 verify`，干净机安装仍待验收。 |
| 2026-08-11 | `L4-SD1.5 P1 / SD-N4 非硬件开发门完成` | 固定四 seed DAG、精确 manifest Worker 池、单节点串行降级、主节点 2×2 CAS 网格、实际 winner 统计与失败/取消/重启清理落地；扩大相关回归 `220/220`，Python 全量 `1777 passed / 31 skipped`。双物理 CUDA PC 的吞吐、故障和 UI 发布验收保持 `Blocked`；当前执行入口转 `UP-N6.0`。 |
| 2026-08-11 | `L4-SD1.5 P1 / SD-N3.4 恢复 fencing 开发门完成` | 未提交 Provider 结果 disposer、ingest 后取消/lease 回收、精确 owner scope 清理和 SQLite journal 重启恢复清理落地；扩大定向回归 `133/133`，Python 全量 `1770 passed / 31 skipped`。下一票进入两台物理 CUDA PC 的 SD-N4。 |
| 2026-08-10 | `L4-SD1.5 P1 / SD-N3.4 真实 Worker 开发门通过` | 新增离线双进程 smoke，真实 Diffusers/GPU Worker 经 v3 TCP 与认证 HTTP 完成单 Stage、协调器 CAS 和 TaskGraph commit；4-step PNG 360,858 bytes，SHA256 `86665855…05bfa`，重复运行一致，定向 `94/94`。SD-N3.4 仍待迟到结果/journal 恢复和两台物理 PC；下一票先做恢复 fencing。 |
| 2026-08-10 | `L4-MODEL-TOOLS P2 llm_smoke_matrix 开发门完成` | 固定中文/英文/JSON/上下文四项、有界隔离 worker、临时离线 HF 缓存、资源预检和执行/覆盖双门落地；Qwen 1.8B GGUF+Safetensors `2/2 units、8/8 jobs` 通过。注册表发现 10 单元，2 通过、8 跳过、0 失败，覆盖不完整。定向 `29 passed / 2 skipped`、Python 全量 `1738 passed / 31 skipped`；下一票 `gguf_convert` 第一阶段。 |
| 2026-08-10 | `L4-MODEL-TOOLS P2 gguf_convert 第一阶段开发门完成` | 新增只读 `gguf_convert`/`gguf-convert` dry-run：注册表或显式 source 二选一，校验 config/tokenizer/index、架构注册、Python 依赖、转换器/`llama-quantize`、空间、目标冲突和源边界；F16 单步与 Q4_K_M 两步计划分开，目标不存在才允许未来发布。Qwen F16 预检通过，Q4_K_M 因缺量化器阻塞；定向 `40 passed / 2 skipped`、Python 全量 `1744 passed / 31 skipped`。执行事务已由同日下一条完成。 |
| 2026-08-10 | `L4-MODEL-TOOLS P2 gguf_convert 执行事务开发门完成` | 新增 `--apply --confirm CONVERT` 显式执行、同卷 staging、离线无 shell 子进程、超时/取消回收、Q4 两阶段中间件检查、最终 GGUF+SHA256 校验及旁车先行的无覆盖硬链接发布；合成 F16/Q4 成功和失败/超时/取消/竞态均通过。定向 `48 passed / 2 skipped`、Python 全量 `1752 passed / 31 skipped`；真实 Qwen 未转换。量化器固定/打包已由同日下一条完成，真实转换另需批准。 |
| 2026-08-10 | `L4-MODEL-TOOLS P2 llama-quantize 固定与分发开发门完成` | 机器锁固定 llama.cpp revision；新增无网络 CMake 构建、CPU-only 静态 Windows 包、manifest/逐文件 SHA256/确定性 ZIP、受管发现与篡改 fail-closed；Windows EXE `9,532,946` bytes，SHA256 `d91893de…30794`，ZIP SHA256 `e3361e96…0b32`。Qwen 1.8B Q4 dry-run `valid=true / managed_package / verified`，真实 Qwen 未转换。工具链定向 `73 passed / 2 skipped`，Python 全量 `1760 passed / 31 skipped`。Linux 构建和 `.deb` 接线完成但无 Linux 真机证据，下一票 Linux 工具链验收。 |
| 2026-08-10 | `L4-MODEL-TOOLS P2 models_sync_status 开发门完成` | 新增只读 inventory/compare 协议、结构/完整 SHA256 双模式、外来清单校验、缺/多/不一致分类与 CLI `0/1/2` 退出码；真实 `models` 与 `H:\qlh_models` 结构比较 10/10 一致且输出无绝对路径。定向 `24 passed / 2 skipped`、Python 全量 `1728 passed / 31 skipped`；真实双机/跨平台仍为 `Validating`，下一票 `llm_smoke_matrix`。 |
| 2026-07-28 | `Draft → Active` | 建立唯一总计划入口；吸收原进度快照中的 P0/P1/P2，新增 L0–L4 阶段门、工作项生命周期、发布门与归档条件 |
| 2026-07-30 | 新增 `L4-SD1.5` | 完成 SD 1.5、DreamBooth 演示模型、ControlNet 单文件和扩散模型分布式方案调研；登记为 L4 Candidate，细节由专项计划维护 |
| 2026-07-30 | `SD-N0`、`SD-N1` 验证 | 固定 SD 1.5 FP16 快照并完成资产识别；在 RTX 4060 Laptop GPU 8 GB 上以 `diffusers 0.35.2` + `transformers 4.47.1` 完成本地 512x512/28 steps 真实生成基线。L4-SD1.5 仍是 Candidate，尚未接入 API、Worker 或跨 PC 分布式。 |
| 2026-08-05 | `SD-N1 In Progress → Completed` | 新增本地资产登记、LLM/SD 生命周期互斥、异步生成 job、可取消 step、临时 PNG blob，以及单体 FastAPI / inference-svc / Nest 网关契约；自动化覆盖并发与错误边界。RTX 4060 上通过真实 inference-svc 路由完成 512x512 PNG、step 取消、SD 卸载与 Qwen 1.8B GGUF 重载。UI、持久 blob、Worker 和分布式 SD 仍属后续阶段。 |
| 2026-07-30 | 新增 `L4-QUANT-LOWBIT` | 完成 2/3-bit 与 4-bit、GGUF IQ2/Q3、AQLM/QuIP# 与 BitNet 边界调研；明确低比特正式只面向 14B+，小模型仅保留 smoke 入口，登记为 L4 Candidate，细节由专项计划维护 |
| 2026-08-05 | 新增 `L4-MODEL-FLEET` | 完成 Ollama/Hugging Face/GGUF/Tailscale 与本地数据库接管调研；规划内容寻址工件库、一键下载部署、多集群档案，以及主节点 SQLite + outbox / 远端 PostgreSQL 投影架构，登记为 L4 Candidate |
| 2026-08-05 | 新增 `L4-TUI-CHAT` | 在已完成 T1-T8 管理 TUI 基线上规划第 8 个简化聊天入口；采用可选 Textual/httpx 环境，先统一真流式/持久化/取消契约，再接 local/distributed 路由和真实参与指标 |
| 2026-07-30 | `L0-1 Ready → Completed` | 明确 `ConnectError → Unreachable`、`ConnectTimeout → Timeout` 契约；关闭端口端到端用例允许平台合法差异，新增确定性分类测试；全量回归 `1046 passed / 23 skipped`。 |
| 2026-07-30 | `L0-4 Ready → Completed` | 新增 KV 尾部 token 回滚、尾页 `used` 回退、空页复用、容量原子拒绝和固定种子随机状态机测试；CUDA 快速冒烟 `62/62`，全量回归 `1054 passed / 23 skipped`。同时将孤岛耗时统计改用单调时钟，消除短请求偶发 `0 tok/s`。 |
| 2026-08-05 | 微服务阶段 3.2 完成 | 控制面 9 域全部迁入 control-svc（契约测试 136/136）；双网关 contract_diff 收敛 61/26，修复 4 项真实缺陷（models/load 白名单、models/switch 注册校验、cancel 格式校验、二进制透传）；2.5/3.3 删除动作按并行共存策略冻结至清理阶段。登记见 §4.6。 |
| 2026-08-05 | TUI 指令集交付 | `/` 命令系统扩至 27 条并发布全量参考文档，`tests/test_tui_commands.py` 73 用例对齐；q 键优雅退出与单命令模式。 |
| 2026-08-06 | T9 聊天页 T9.0-T9.5 完成 | interactive 契约/routing_preference/SSE parser/Textual 聊天页/会话管理/错误恢复落地；`bjtu chat` 可用；相关回归 126+ 用例与全量 1416 passed；T9.6（打包/默认入口）与分布式真机验收待后续。登记见 §4.6。 |
| 2026-08-06 | `L0-3` 文档收口首轮 | 总体计划同步微服务/TUI/SD 进展（§2.1、§4.5、§4.6、§7）；SD 实施计划状态表更新 img2img 为"已实现待验收"；README 文档索引补登记仍在执行的专项计划；链接与凭据检查通过（本项目文档无失效链接、git 未跟踪凭据文件）。`L0-3` 推进至 `Validating`。 |
| 2026-08-06 | 管理员收件邮箱可配置 | SMTP 收件人从环境变量扩展为 node_config.json 可配置（UI/API 双入口，空串清除回退环境变量）；所有告警与测试邮件动态读取收件人；单体与 scheduler-svc 双端端点。 |
| 2026-08-06 | L4 候选补登记与优先级标注 | §4.5 新增 `P1/P2/P3` 优先级口径，并登记 7 项"调研结论已形成/未实施或未完全实现"工作项：任务图图树变换（调研完成未实现）、DeepSeek 重模型实验（槽位落地待逐模型验收）、分布式仿真测试矩阵（真机缺位期验证支撑）、TUI 环境变量注册（随 L2 打包落地）、Android 版本远期计划分布式部分、Android llama.cpp Submodule 迁移（待维护窗口）、PC/Android 交互体验优化（待拆分）；原 L4-SD1.5/QUANT-LOWBIT/MODEL-FLEET/TUI-CHAT 同步标注优先级。 |
| 2026-08-09 | `L4-EXPERIMENT-AUTO EX-N0/EX-N1` | 提示词集 v1 冻结（`ps-v1-zh-en-code` 30 条，SHA-256 `8cc555f5…`）；`run_experiments.py` + `experiment_core/` 框架落地：资源互斥调度（`(key,value)`/`any` 共享池）、超时重试留痕、断点续跑、gate 三态、schema v1 记录与对照表报告；`tests/test_experiment_runner.py` 17/17。 |
| 2026-08-09 | `L4-EXPERIMENT-AUTO EX-N2` | 首批 LLM 量化/编译对照接入：`experiment_llm_unit.py` + `plan-quant-qwen-1.8b-v1`（4 单元）。RTX 4060 实测：fp16 29.08 tok/s；compile 持平（-1.96%）；int8 -69% failed；int4 -3.4% 且显存 -50%（passed）。跨轮波动已记录；框架补充：resume 只跳过 passed（failed 可重跑）、重跑替换记录不重复。 |
| 2026-08-10 | `MF-AUTH-N1W Completed（开发门）` | Web 首登/登录/恢复码/logout 与本地 QR 接线完成；gateway 提供能力协商并强制 auth/users 走 control-svc，旧单体明确兼容、5xx fail-closed。前端 `29/29`、浏览器 `4/4`、gateway `107/107`。下一票 N1A；Linux/安装包/双机证据集中到最终跨平台联合验收。 |
| 2026-08-10 | `MF-AUTH-N1A Completed（开发门）` | gateway 全局公开/机器/已登录/manager 授权矩阵与 Bearer 校验完成；control 会话列表/撤销、用户元数据、admin-owner TOTP 边界和账户管理 UI 完成。control `313/313`、gateway `113/113`、前端 `29/29`、浏览器 `5/5`。下一票 N2 客户端绑定/换网；Linux/安装包/双机证据集中到最终跨平台联合验收。 |
| 2026-08-10 | `MF-AUTH-N2 客户端开发门完成` | 账户工作区接入绑定状态、CLI/本机状态授权选择、pending 确认、稳定 ID 换网历史和撤销；前端 `30/30`、浏览器 `5/5`，构建通过。N2 整体仍待真实 Tailscale CLI/浏览器、旧节点网络撤销和双机实网联合验收。 |
| 2026-08-10 | `MF-AUTH-N2L 本机状态适配开发门完成` | 新增认证保护的本机 `tailscale status --json` 只读适配器、跨平台 CLI 查找、超时/输出边界、短缓存/并发合并和安全字段投影；Web 可读取并自动填充候选身份，但仍要求 pending/confirm。N2L 契约与 Windows 本机探测通过，真实绑定/换网和跨平台联合验收仍待完成。 |
| 2026-08-08 | 独立 Launcher/Updater 基础纵切 | 建立与主应用 Setup 分离的 Bootstrap：GUI/TUI 双前端、BJTU update/version、并行清单、四段版本、CPU/CUDA 显式选择、SHA-256 下载和 `/latest.json`；状态为源码实现待打包实机，发布签名、原子版本切换与回滚仍 Pending。 |
| 2026-08-08 | 独立 Launcher 安装事务验证 | 独立 Setup 在工作区隔离目录完成静默安装、冻结 EXE 清单检查与静默卸载；验证不触及主应用 CPU/CUDA 构建目录。Windows 主应用发现和 Linux `.deb` 干净机验收仍待完成。 |
| 2026-08-08 | UP-N2 可信发布实现与验收 | Ed25519 清单签名/key rotation 全链落地：`packaging/signing.py`（keygen/authorize/sign/verify）+ 内置 `pubkeys` 信任集（root + release-20260809，授权链可追溯）；`update_core`/`updater`/`serve`/`qlh_launcher` 接入验签门控，GUI/TUI 安装不再传递 `--allow-unsigned`；篡改清单/签名、未知 key、伪造授权、过期 key 均 fail-closed（`tests/test_signing.py` 26 用例 + 回归 72 用例全绿）。**验收签字：Siegfried Kkm./浅草爱音（2026-08-08，结论 passed）**。 |
| 2026-08-08 | UP-N4 Launcher ZIP 构建与发布链路实测 | `build-launcher.bat` 产出 `packaging/dist/QLH-Launcher-v0.1.8.1.zip`（16,158,275 bytes，1013 成员，含 `QLH-Launcher.exe` + `health.ok`，版本号读 `packaging/version.txt`）；签名源站（`QLH_SIGNING_KEY`）`/latest.json` 发布 `kind=launcher` 资产，`launcher-check` 真实 HTTP 验签通过；`launcher-download → stage → 隔离子进程 health 探针 → activate → rollback → 再切换` 双槽闭环实测通过；serve.py 修复 Windows GBK 控制台 emoji 崩溃（stdout 重配 UTF-8）。剩余：Windows 安装后真实切换/回滚与 Linux `.deb` 干净机验收。 |
| 2026-08-08 | 新增抗弱网通信工作流 | 基于 `tailscale status/netcheck` 脱敏实测与官方协议资料建立专项计划：区分 UDP 直连失败和公共 DERP TCP/TLS 可达性问题；当前自建 DERP 为 Active 可用性基线，NW1/NW2 Ready，应用层 WSS、Transport v2 与 UDP-over-WSS sidecar 仍为 Candidate。 |
| 2026-08-08 | 补充主节点 TCP 数据面决策 | 明确 WireGuard 无原生 TCP 模式；若主节点 TCP 443 可达，QLH 直连 WSS 可使推理 payload 绕开 DERP。双方均在严格 NAT 后时，DERP 仅做会合仍需独立 ICE-TCP/simultaneous-open 实验，失败后任何可靠方案仍需要有容量的中继。 |
| 2026-08-08 | L0-5 新增：全量回归基线重新核对 | 全量 Python 收集 1650 项，`1641 passed / 7 skipped / 2 failed`；失败为外部 Provider 与投机解码测试调用 `_validate_capabilities()` 缺少 `version`，发布门保持未通过。增量 Launcher/Updater `82 passed`、control-svc `205 passed`、gateway `101 passed`。 |
| 2026-08-08 | `L0-5 临时验证，保持 Validating` | 在未保留代码改动的临时工作区为两个 v2 测试调用补齐 `_validate_capabilities(..., version=2)`；全量 Python 回归可达 `1643 passed / 7 skipped / 0 failed`，正式仓库仍为 `1641 passed / 7 skipped / 2 failed`。 |
| 2026-08-09 | `L0-5 收口 Completed` | 两个 v2 测试调用显式 `version=2` 落地（`tests/test_external_provider.py`、`tests/test_speculative.py`）；干净工作区全量 `pytest -q` 实测 **1657 passed / 7 skipped / 0 failed**（收集 1664 项），L0-5 关闭，发布门解锁该项。 |
| 2026-08-08 | Android submodule 与 Launcher 发布证据更新 | llama.cpp vendored → gitlink `47e1de77`，Full Release 构建通过；Launcher ZIP 签名源站发布与 A/B 双槽回滚闭环通过；Windows/Linux 主应用干净机、Android 真机回归和主应用聊天依赖仍待完成。 |
| 2026-08-08 | `L4-MODEL-FLEET P2 → P1` | 启动无硬件并行批次，基线为 Python schema `17/17`、control-svc `64/64`。 |
| 2026-08-08 | `L4-MODEL-FLEET MF-N1 → M5.0` | 完成 M0.1-M5.0 本地门：Python schema `25/25`、control-svc 全量 `225/225`；新增兼容清单/时序 fixture、恢复补投、批量迁移 CLI、来源/预检、profile 选择/发现和部署模拟器。真实 PG/Hub/Tailscale/PC Worker 仍保留后续环境门。 |
| 2026-08-08 | `L4-MODEL-FLEET M2 实际迁移` | 8 个内置条目/10 个预期来源完成真实运行：2 个模型 ID 的 Safetensors+GGUF 共 4 个工件、46 个 blob 成功，6 个条目因源路径缺失登记为 `missing`；GGUF 多 MiB 元数据预算修正后重试通过，控制面门继续收口。 |
| 2026-08-08 | `L4-MODEL-FLEET M3 安全门与真实小工件续传` | Windows DPAPI、Bearer、gated 双重门和 Git 风格代理完成；resolver 获取 blob size/LFS SHA，downloader 严格校验 206/Content-Range。7897 环境/用户代理均完成 456318-byte 完整下载、16384-byte 中断、Range 恢复和 SHA 一致；生产依赖审计 0 vulnerability。 |
| 2026-08-08 | `L4-MODEL-FLEET M2 runtime sidecar` | 独立 Python loader、Node 超时/崩溃回收、硬链接运行时视图与 SQLite v2 状态登记完成；DeepSeek 7B GGUF SHA 复核后真实加载 `ready`（2,720ms），Safetensors 按内存安全余量登记 `resource_rejected`；全程离线且未用代理。 |
| 2026-08-09 | `L4-MODEL-FLEET M2 runtime admission + M2.4 UI` | 在 2026-08-08 准入接线基础上完成 Web 工件控制面：alias/存储/runtime 汇总、pull 列表、网关精确透传，以及导入/复检/失效/取消/用户代理交互；control-svc `265/265`、gateway `103/103`、前端单元 `27/27`、浏览器 mock `1/1`，本阶段离线且未用代理。下一票为真实模型权重完整 pull + Safetensors 适配主机。 |
| 2026-08-09 | `L4-MODEL-FLEET M2.5 来源/凭据与预检 UI` | 新增凭据无密文状态列表与 gateway `resolve/credentials/licenses` 透传；Web 支持来源编辑/禁用/恢复、DPAPI 凭据只写/删除、许可接受/撤销，并强制按当前 `source_id` 重新 resolve 后确认拉取。control-svc `265/265`、gateway `104/104`、前端 `27/27`、浏览器 mock `1/1`，本阶段未使用代理。下一票收窄为真实模型权重完整 pull + Safetensors 适配主机。 |
| 2026-08-09 | `L4-MODEL-FLEET M3.2 真实权重与 Safetensors 准入` | 固定 Qwen2.5 0.5B revision `7ae5576…`，经 7897 完整拉取 10 文件/999,604,126 bytes 并得到工件 `f8dcc256…bbe9c`；修复整块哈希、pull 缺 Safetensors inspection 与已有 LFS blob 未复用。494,032,768 参数在 CUDA BF16 两次离线加载 `ready`（2,534/1,674ms），本地 prepare=`ready`；control-svc `267/267`、schema `25/25`、生产依赖 0 vulnerability。下一票 M3.3 真实进程重启 partial 恢复 + 代理掉线。 |
| 2026-08-09 | `L4-MODEL-FLEET MF-MEM-N0 显存分页调研` | 本机 RTX 4060 Laptop 8 GB/WDDM 为 `managedMemory=1` 但 `concurrentManagedAccess=0`，GPU 目标 prefetch/advise 均不满足能力门；256 MiB pinned 实测 H2D 12.142 GB/s、D2H 13.033 GB/s。决策为 Windows/WSL UVM `No-Go`、显式 pinned-memory layer pager `Go`、原生 Linux UVM 主动预取 `Conditional Go`；下一票 `MF-MEM-N1` 可与 M3.3 并行，详见专项 §7.6。 |
| 2026-08-09 | `L4-MODEL-FLEET M3.3 真进程重启与代理掉线` | 新增瞬时 fetch/stream/408/425/429/5xx 最多 4 次有限重试、严格 Range 续传、持久恢复观测和 `transfer_unavailable` 终态；响应头超时不会截断已建立的慢速大文件流。远程源继续强制 HTTPS，仅 loopback 允许 HTTP。真进程 smoke 在 393,216 bytes 强杀首个 control 后由第二进程从同偏移续传；代理在 196,608 bytes 断开并短时 503，同一 job 第 3 次尝试恢复，两个 4 MiB 固定工件 size/SHA 一致。control-svc `274/274`，生产依赖 0 vulnerability；下一票 `MF-MEM-N1`。 |
| 2026-08-09 | `L4-MODEL-FLEET MF-MEM-N1 固定 CUDA transport` | 新增独立 PyTorch CUDA harness 和 EX-N1 12 单元矩阵：64/256/1024 MiB × pageable、pinned 单槽、双槽、三槽，两轮均 `12/12 passed`，checksum 与 H2D/D2H、pipeline overlap、stall P50/P95/max、pinned/VRAM 峰值均记录。pinned raw H2D 约 `11.8-12.2 GB/s`、D2H 约 `12.6-13.1 GB/s`；双槽 pipeline 增益不足 2% 且 P95 stall 放大，三槽冻结。下一票 `MF-MEM-N2` 64 MiB 双槽隔离 sidecar。 |
| 2026-08-09 | `L4-MODEL-FLEET MF-MEM-N2 合成 explicit layer pager` | 新增隔离 `scripts/model_fleet_explicit_layer_pager.py` 与 EX-N2 4 单元计划；64 MiB 双槽有界 host 预取、显式 copy/compute event、CUDA batch=1/4 输出 digest 一致、取消第 2 层后回收 allocated/reserved=0、CPU equality control 均通过，计划 `4/4 passed`，专项测试 `6/6`。Windows WDDM 两项完成样本 overlap=0%，不宣称吞吐优化，未接生产 runtime。下一票 `MF-MEM-N2R` 真实 INT8/NF4 工件对照。 |
| 2026-08-09 | `L4-MODEL-FLEET MF-MEM-N2R 真实 7B NF4 placement 与层迁移` | 固定 `deepseek-r1-distill-qwen-7b` Safetensors 15,231,233,024 bytes；artifact gate `3/3 passed`，真实 NF4 resident/`device_map=auto`/context 批量对照 `6/6 passed`。context=128 batch=1 两种 placement digest 一致（4.62/4.54 tok/s）；context=2048 resident batch=1 为 2.23 tok/s、auto batch=4 为 1.76 tok/s；模块级 hook 28 层 page-in/out 各 28 次、H2D/D2H 各 3.039 GiB。CPU-only 因可用 RAM 不足明确拒绝；固定 64 MiB bridge、长上下文和取消尚未关闭。下一票 `MF-MEM-N2F`。 |
| 2026-08-09 | `L4-MODEL-FLEET MF-MEM-N2F 真实固定 tile/取消/长输出` | 新增 `explicit-layer-pager-64` 与 `plan-model-fleet-n2f-fixed-tile-v1.json`；64 MiB × 2 bridge 计划 `3/3 passed`，ctx128 完整、ctx128/8192 layer-boundary cancellation 均通过，补充 output 32/128 完成，固定分块 12/336/10,752/43,008，VRAM allocated/reserved 约 2.75/5.63 GiB，bridge 峰值模块 116.5 MiB。专项回归 `9/9`；桥接只限制单次传输块，不等于完整显存上限，未接生产 runtime。下一票 `MF-MEM-N2P` 预取/重叠和延迟决策。 |
| 2026-08-09 | `L4-MODEL-FLEET MF-MEM-N2P 真实预取/延迟决策` | 新增预取距离 0/1、host staging overlap、CUDA stream overlap、TTFT/inter-token 统计和 `plan-model-fleet-n2p-latency-v1.json`；resident/serial pager/distance=1 计划 `3/3 passed`，同 digest `e958d843…`。distance=1 host staging overlap 100% 但 CUDA H2D overlap 0%，E2E 约慢 1.0%、TTFT 约慢 6.7%、inter-token P95 仅改善约 3.6%，取消回收补充门通过；结论 `Freeze` 双槽 host prefetch 默认优化，保留显式 pager 容量 profile。下一票仅在原生 Linux + 32 GB RAM 条件满足时进入 `MF-MEM-N3`。 |
| 2026-08-09 | 新增 `L4-MODEL-TOOLS` | 完成模型通用小工具调研（[模型通用小工具调研与方案](模型通用小工具调研与方案.md)）：GGUF 元数据查看/完整性校验、全库体检、junction 感知磁盘治理、SD 批量提示词/采样器对比、GGUF 量化转换、SD 融合、多节点同步检查、全模型冒烟矩阵共 11 项；登记为 L4 Candidate。P0 三件套（`gguf_inspect`/`gguf_verify`/`models_sweep`）只读零风险、复用现有 SHA256 基建，不占用 L0-L4 关键路径，可在并行工作流窗口随时启动。 |
| 2026-08-09 | 新增 `L4-GEMMA4` | 完成 Gemma-4-12B 多模态（图生文）调研与接入设计：本机 Ollama 已实测 Q4_K_M 可运行但缺视觉 projector；vendored llama.cpp 已支持 `LLM_ARCH_GEMMA4`。登记为 L4 Candidate：路线 A 经 `external_api` 对接 Ollama OpenAI 兼容端点（零新引擎零下载），路线 B 为 llama.cpp 原生 mmproj 接入；验收基线为"图像输入 → 正确图生文描述"。详见 [Gemma-4-12B多模态支持方案](Gemma-4-12B多模态支持方案.md)。 |
| 2026-08-11 | `L4-GEMMA4 G4.1` 后端与官方模型门完成 | 原调研中的零下载、12B 模态和原生绑定结论已按官方资料与实测修正。官方 `gemma4:12b` 经 7897 子进程代理下载，QLH `external_api` 完成结构化图片、reasoning、数据作用域、格式/大小限制、禁止丢图回退及零图片持久化；定向 `160 passed`、真实图像调用通过；全量在仅测试进程 `NO_PROXY=*` 下为 `1908 passed / 26 skipped / 2 failed`，失败仍仅为当前 WSL 无法访问 `/g/...` 的 Linux env-register 环境门。当前下一票 G4.2 Web/TUI 图片交互；G4.3 原生/音频/分布式保持 Candidate。 |
| 2026-08-11 | `L4-GEMMA4 G4.2` Web/TUI 图片交互完成 | Web 本地内存图片队列完成 MIME/magic/4 图/8MiB/16MiB 边界、预览/删除/发送时清理，带图自动选 external API；TUI `/image`、`/images`、`/image-clear` 与同一校验口径落地。桌面/移动桩浏览器 `1 passed`，真实 Edge → QLH → `gemma4:12b` 返回悲喜面具描述；Python 定向 `179 passed`，全量 `1915 passed / 26 skipped`。下一票 G4.3 Candidate。 |
| 2026-08-11 | `L4-GEMMA4 G4.3.1` 原生绑定/ABI 预检完成 | 新增 `gemma4-native-probe` 独立只读 worker：绑定 ABI 与版本检查不等于模型能力；只有显式 GGUF/mmproj 双 SHA 校验后才可 CPU-only 初始化 MTMD，路径不回显、异常 fail-closed。开发机为 lock `47e1de77…` / binding `0.3.28` / MTMD ABI 完整，但未给工件所以 `binding_ready / gate_passed=false`；专项 `56 passed / 2 skipped`，全量 `1934 passed / 13 skipped`。下一票 G4.3.2 固定工件与图片语义对照。 |
| 2026-08-11 | `L4-GEMMA4 G4.3.2A` 官方工件身份与资源准入完成 | 锁定本机官方 Ollama `gemma4:12b` 的 manifest、Apache-2.0、主 GGUF/mmproj 双 SHA、layer `from` 关系和 GGUF 元数据；`gemma4-native-assets --full-hash` 通过，不复制/下载第二份模型。`gemma4-native-ollama-probe` 先做资产门、再于 `Llama`/MTMD 初始化前检查 RAM；两次实测可用约 5-6GiB、小于约 8.74GiB，安全返回 `resource_rejected / insufficient_ram`，未加载模型或处理图片。专项 `59 passed / 2 skipped`，当前全量 `1942 passed / 8 skipped`；G4.3.2B 等待 RAM 门满足后执行 CPU-only 初始化和固定图片语义对照。 |
| 2026-08-09 | 新增 `UP-N6` 启动器完整性检查与疑难解答 | 调研完成（[启动器完整性检查与疑难解答方案](启动器完整性检查与疑难解答方案.md)）：install-manifest 安装清单 → `verify` 分层完整性检查（quick/full/deep）→ `diagnose` 症状知识库 → `repair` 自动修复（单文件重拉、阈值升级）→ 兜底为**保留数据的卸载重装**（数据迁移至 `%LOCALAPPDATA%` + 自动重装 + 重新关联，models/chat_history 零丢失）；所有路径受 UP-N2 签名门控、不触碰用户数据。登记为 UP-N6.0-N6.4，待实施。 |
| 2026-08-09 | **冻结微服务架构改造主线** | 现状核对结论：微服务"实现超前于应用"——迁移域契约测试全绿但从未接管实际运行路径（`start_backend.bat` 仍启动 Python 单体、前端直连 :8000），Python 侧功能持续迭代导致双基线漂移。冻结 3.4 验收/清理阶段 §5.1/阶段 4，待 Python 功能面收敛后重启；MODEL-FLEET 在 control-svc 的开发不受影响。登记见 [微服务架构改造计划](微服务架构改造计划.md) 顶部与 §4.6。 |
| 2026-08-09 | `L4-MODEL-FLEET MF-AUTH-N0` | 完成 Auth App/TOTP 与本地主节点用户管理调研：RFC 6238 + `otpauth://` 字符串/二维码双下发；推荐外部 Aegis（GPL-3.0，QLH 不内嵌）；Tailscale 官方不是 IdP，OAuth clients 是 API client credentials，OAuth Apps 设备授权当前 alpha 且单 tailnet。方案改为 QLH 本地 TOTP 登录、稳定 `tailnet_id + tailscale_user_id` 绑定和 pending/confirm/revoke 换网；认证域不投影开发组/远端 PostgreSQL。下一票 `MF-AUTH-N1`。 |
| 2026-08-09 | `L4-MODEL-FLEET 存储主权重排` | 将 `M1.1` 定义为现有 SQLite/远端 PG 兼容基线，新增 `M1.2` 主节点 SQLite 资产自持与加密备份/恢复、`M1.3` 远端 PostgreSQL 退场准备。默认 `local_only`，远端仅用户主动发起单向兼容导出，最终可删除且不影响核心运行；认证、模型、工件、集群、任务、会话和备份均属于用户主节点资产。下一票 `M1.2`。 |
| 2026-08-09 | `L4-MODEL-FLEET M1.2 实施首片` | control-svc 默认 `local_only`，设置写入改为主节点 SQLite，旧 PostgreSQL projector 在未显式启用时不启动；新增 scrypt + AES-256-GCM 用户加密 SQLite 备份、quick-check、恢复前验证和失败回滚，`storage:backup` CLI 支持 backup/verify/restore。控制面全量回归 `277/277`；剩余核心域统一迁移、artifact manifest 和跨平台干净机验收仍未完成。下一票继续完成 `M1.2`，不是进入认证。 |
| 2026-08-09 | `L4-MODEL-FLEET M1.2 核心域第二片` | SQLite v3 新增 `sessions`、`session_messages`、`workflow_journal`、`review_tickets`；control-svc 默认写入主节点 SQLite，旧 JSON 首次启动只读导入且原文件保留。跨实例持久化/导入测试通过，control-svc `280/280`；下一票仍为 M1.2。 |
| 2026-08-09 | `L4-MODEL-FLEET M1.2 artifact/引用第三片` | SQLite v4 新增 `artifact_manifests`；manifest 写入 fail-closed 校验路径、摘要、聚合 digest 和 blob，启动执行 SQLite↔文件系统双向 reconcile。backup/verify/restore dry-run 新增 manifest payload、deployment/runtime 引用和 `--model-store` 外部 blob 校验；SQLite 备份不含 blob 字节。control-svc `283/283`、gateway `104/104`；下一票继续认证域、Python 主运行路径、blob 迁移包和跨平台故障门。 |
| 2026-08-09 | `L4-MODEL-FLEET M1.2 本地资产迁移第四片` | 新增 `.qlhmigrate` 流式 scrypt + AES-256-GCM 包及 `package/verify-package/restore-package`；从 SQLite 快照收集实际引用 blob，验证包头 AAD、逐条大小/SHA、写入期二次摘要并重建 manifest。空间不足、篡改、缺 blob、不可创建目标、SQLite 阶段失败及切换后 reindex 失败均在切换前拒绝或回滚 SQLite/模型目录；control-svc `290/290`、gateway `104/104`。后续认证存储第五片已完成。 |
| 2026-08-09 | `L4-MODEL-FLEET M1.2 认证资产存储第五片` | SQLite v5 新增本地用户、TOTP `secret_ref`、恢复码 scrypt/Argon2id 哈希、本地审计和 Tailscale binding；active/pending 唯一约束、TOTP 轮换、换网冲突保留旧 active、重启和 `.qlhmigrate` 恢复均通过，认证操作不写 outbox。迁移包不携带 DPAPI/系统凭据密文，当前没有登录 API、TOTP 生成或真实 Tailscale 授权；control-svc `295/295`。下一票收口 Python 主运行路径。 |
| 2026-08-09 | `L4-MODEL-FLEET M1.2 Python 主运行路径第六片` | Python `local_store` 改为共享 WAL SQLite，Node/Python 默认路径统一并兼容 cwd 旧库；不修改 `PRAGMA user_version`，旧 JSON 只读导入一次且保留。单体 API 与 inference-svc 启动执行可写门，会话/设置/健康/推理历史本地优先，旧 PostgreSQL 仅兼容导出；测试隔离不写用户数据库。Python `1720 passed / 7 skipped`、control-svc `297/297`、gateway `104/104`，control 构建通过。下一票为 Windows/Linux 干净机、活动库损坏和真实只读 ACL 门。 |
| 2026-08-09 | `L4-MODEL-FLEET M1.2 双平台隔离环境第七片` | 新增 Python/Node 存储探针与跨平台编排脚本。Windows 11 通过干净目录、损坏库和 NTFS 写入 deny ACL；WSL2 Ubuntu 22.04 ext4 通过干净目录、损坏库和 `0555/0444` 权限门；两端共库保持 schema v5，损坏/只读均 fail-closed。修正 SQLite 健康状态，临时 ACL、Node 和运行目录全部清理，未使用 7897。control-svc `297/297`、构建通过，Python 定向 `116/116`。`M1.2` 关闭，下一票 `M1.3`。 |
| 2026-08-09 | `L4-MODEL-FLEET NET-N1 IPv4/IPv6 外部入口双栈` | TCP 数据面、Python API、control-svc、gateway、独立分发服务和 PC/TUI/Android URL authority 完成本地双栈改造；Tailscale ULA `fd7a:115c:a1e0::/48`、IPv4-mapped IPv6、MagicDNS/广告地址契约已覆盖。Windows 真实三服务均接受 `127.0.0.1`/`::1`，Windows/WSL `socket-helper` 同端口双监听通过；Python `1729 passed / 7 skipped`、control-svc `301/301`、gateway `104/104`、Android Full Debug Kotlin 编译通过。状态保持 `Validating`，剩余双机纯 IPv6/混合网、真实 Tailscale IPv6 与安装包门。总体下一票仍为 `M1.3`。 |
| 2026-08-11 | 图像离线资产导入与节点发现提示修复 | 图像资产卡不再复用原版 SD 的全局路径：catalog 返回每卡 `local_dir`，旧前端错误提交其他受管目录时后端也会纠正到当前 asset。通用“检查并登记”统一按应用根目录解析，消除服务从 `src` 启动时的不存在目录/`unknown`。从 `src` 模拟旧前端对五个资产均提交原版路径，完整验证全部通过（原版/90s/IP-Adapter/inpaint/Pix2Pix：`16/16/3/16/16` 文件）；新源码后端 HTTP 复测确认五个目录均返回，90s 旧路径请求被校正并成功。邀请接口移除旧 `db_registered`，前端说明改为主节点本机 SQLite、已连接节点本机 bootstrap 恢复和同一 Tailnet 自动发现；首次未组网设备先完成 Tailnet，地址仅作可信 bootstrap 辅助。资产/服务/API 定向 Python `60 passed`、全量 Python `1916 passed / 8 skipped`、前端单元 `38/38`、生产构建通过；总体下一票不变，仍为 `L1-2A`。 |
| 2026-08-11 | `M1.3R` 身份与广告地址退场修复 | 主节点 MAC 身份由远端 PG 兼容路径迁入用户自持 `qlh-control.sqlite3`，首次绑定与重置均立即生效；启动不再注册/心跳/重连旧 PG。`detect_lan_ip()` 只接受实际分配的 Tailscale IPv4/ULA，拒绝未登录时的 `169.254/16`、WSL/Hyper-V 等虚拟接口，当前未登录 Tailnet 时实测回退到可达 WLAN。邀请返回 `master_host_source`，发现与断线健康检查只使用本机配置/Tailnet/TCP，legacy-control 健康桩改为 `local_only`。本地存储、TCP、Scheduler 定向回归 `362 passed`，前端单元 `38/38`；剩余 Scheduler 静态兼容分支、`src/db.py` 与历史 PG 工具进入 `M1.3R` 后续收口，已登录 Tailnet 真实邀请/首次连接仍待环境恢复。 |
| 2026-08-12 | `M1.3R` 静态退场开发门完成 | Scheduler 节点/分层状态已归运行时或主节点 SQLite；Python/control PG 驱动、DAO、投影器、历史脚本与兼容标志删除，静态防回归门落地。扩大定向 `600 passed`，Python 全量 `1930 passed / 8 skipped`，control 构建及 `313/313` 通过。真实 Tailnet/LAN 验收留外部门；下一票 `L1-2A`。 |
- 2026-08-15 MM1 支线复核修正：确认 MM1.15 原实现会先完整加载 safetensors 分片再过滤，已改为 index/header + `safe_open.get_tensor()` 逐张量加载并加入键/形状/shard 安全校验。确认 MM1.18 把完整 Qwen3-VL 模型物化后仍标记 `full_model_materialized=false`，该完成结论撤回；默认路径现在 fail-closed，只有 `allow_full_model_materialization=true` 的研究请求才允许完整模型 smoke，成功结果明确标记为 true。MM1.8-11 账本、容量、placement 和 media reference 校验已收紧，新增回归 `45 passed`，合同/运行时 `25 passed`。MM1.18 不进入生产准入或跨节点 handoff，下一票为 MM1.19。
- 2026-08-15 `PT-PIPE-MM1.19` 开发门完成：真实语义 smoke controller/worker 支持最多 4 张图单次模型加载、逐图关键词基线（可区分 sd-001/90s 风格）、SHA/时延和 path-free RSS/RAM/CUDA 资源观测；生产路由评估固定为原生 sidecar fail-closed、条件式 external_api 基线。新增 MM1 review 回归后专项 `8 passed`，MM1 合同/运行时联合回归 `72 passed / 2 skipped`；当前机可用 RAM 约 6.5 GiB，真实双图请求在加载前 `resource_rejected/insufficient_ram`，实机数据待资源恢复。下一票为 `PT-PIPE-MM1.20` 分段文本执行器前置与无峰值 handoff 门。
- 2026-08-15 `PT-PIPE-MM1.20` 开发门完成：新增视觉特征到首文本段的 path-free staged contract，绑定 2/3 段拓扑、manifest/assignment 身份、逐段权重/激活/容量、visual/text token 布局、sequence budget 和下一段 hidden 请求；CPU fixture 覆盖执行、失败与 cleanup，且明确保持 `text_weights_loaded=false`、`segment_materialized=false`、`full_model_materialized=false`。专项 `7 passed`，MM1 联合 `80 passed / 1 skipped`。下一票为 `PT-PIPE-MM1.21` 首段 sidecar 本地 artifact 适配；真实文本权重/forward、CUDA、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.21` 开发门完成：新增 MM1 首段 path-free 本地 artifact 绑定合同，区分媒体摘要与序列化 `.pt` 文件证据；新增首段 sidecar 适配器，严格执行 assignment/scope/输入输出 SHA/hidden handoff 校验及 prepare → commit → prefill → release/abort。输出只以本机 `artifact_id` 保留，控制结果不含路径；取消、prefill/证据异常和 cleanup 不完整均删除本票输出并 fail-closed。专项 `9 passed`，排除 `real_model` 的稳定联合回归 `91 passed`；含真实模型门最近一次 `94 passed / 2 skipped`（MM1.17 RAM 门、MM1.18 显式 opt-in）。下一票为 `PT-PIPE-MM1.22` 本机 2/3 段 sidecar artifact 串联；真实权重、CUDA、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.22` 开发门完成：新增 `Qwen3MultimodalMultiSidecarAdapter`，把 MM1 staged、首段 artifact binding 与本机 2/3 段 multi-sidecar 串联；首段输出直接进入下一段，逐段 session assignment、hidden size、输出 SHA、local reference、KV/generation 和最终输出摘要均校验，通用链在下一段启动前拒绝损坏 handoff。取消、中段失败、重启残留和最终 artifact 清理均覆盖。专项 `7 passed`，通用 multi-sidecar `5 passed`，MM1/sidecar/network 联合 `138 passed / 1 skipped`。下一票为 `PT-PIPE-MM1.23` 2/3 段 prefill → decode/KV 生命周期；真实权重/forward、CUDA、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.23` 开发门完成：新增 decode 输入 artifact 合同，绑定 generation、batch/token、prefill 总长度与 decode 增量长度；MM1 multi-sidecar 在同一生命周期内执行 prefill → decode，逐段复用并核验 KV，通用链新增 `input_sequence_length` 以区分 decode hidden handoff 和 KV 总长度。输入/KV/合同篡改、generation 不一致、取消、异常 abort、重启和最终清理均覆盖。专项 `13 passed`，MM1/sidecar/network 联合 `144 passed / 1 skipped`。下一票为 `PT-PIPE-MM1.24` decode 结果消费与 path-free 质量摘要；真实 forward/logits 质量、CUDA、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.24` 开发门完成：新增本地 decode artifact consumer，以受控 root、committed 类型、实际大小/SHA 和 weights-only CPU 加载为入口门；严格校验 logits/hidden rank、batch/token、dtype/device/finite 及 KV 层数、完整长度和 generation，控制结果只保留 path-free 质量摘要。重复消费默认拒绝，reset 不取得文件所有权，最终清理仍归 MM1 adapter。MM1.24 新增 `9` 个回归，MM1.22-24 专项 `22 passed`，MM1/sidecar/network 联合 `153 passed / 1 skipped`。下一票为 `PT-PIPE-MM1.25` 有界多步 decode 与逐段 KV 原子滚动；真实文本质量、CUDA、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.25` 开发门完成：通用 2/3 段链支持连续 decode，新一代所有 segment 成功后才原子切换并删除旧 KV，generation/sequence 单调门、远端 node-local KV 副本滚动与陈旧水位拒绝均已落地。MM1 adapter 新增 batch=1 单 token greedy/EOS/上限/取消循环；token ID 只写本地临时 `input_ids`，控制面使用常量级 trace SHA 与最终质量摘要，不返回 ID、tensor 或路径。MM1.25 新增 `6+2` 个用例，MM1.22-25 专项 `28 passed`，sidecar/multi/network `44 passed`，最近联合 `161 passed / 1 skipped`（唯一 skip 为完整模型显式 opt-in）。下一票为 `PT-PIPE-MM1.26` 本地生成 token 账本与隔离 tokenizer 最终解码；真实语义、sampling、CUDA/双机和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.26` 开发门完成：逐步生成 token 仅落到主节点本地 JSON ledger，固定 schema/canonical digest、artifact SHA、水位与 `4096` token/`256 KiB` 上限，控制面不携带 token ID 或路径；ledger 与 `.pt` 工件由 adapter 原子提交并统一清理。隔离 tokenizer worker 强制专用 Python、离线、`local_files_only=true`、`trust_remote_code=false`，严格验证账本后只返回 UTF-8 文本及 SHA/字节证据。MM1.26 新增 `7` 项回归，专项 `35 passed`，MM1/sidecar/network 联合 `166 passed / 3 skipped`；跳过项为 RAM 门和完整模型显式 opt-in。下一票为 `PT-PIPE-MM1.27` 本地 sampling 合同与可复现生成；真实语义、CUDA/双机、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.27` 开发门完成：有界 decode 支持严格 temperature/top-k/top-p/seed 校验，temperature=0 保持 greedy，正温度使用 CPU 本地 seeded `torch.Generator`，不改变全局 RNG；top-k/top-p、非有限 logits、EOS/上限/取消均在本地 data plane 处理。ledger 绑定 sampler canonical digest 与逐步 draw evidence，worker 复核 sampler/record/artifact SHA，控制面只返回配置摘要和 draw 数。新增 sampling 专项回归，最新 MM1/sidecar/network 联合 `176 passed / 3 skipped`（RAM 门与完整模型显式 opt-in）。下一票为 `PT-PIPE-MM1.28` sampling 质量摘要与受控重放边界；真实语义、CUDA/双机、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.28` 开发门完成：sampling 每步质量在本地压缩为候选数、熵、置信度、top-p 截断水位及滚动 SHA，聚合摘要与 ledger 的 sampler/artifact 证据绑定；隔离 worker 新增只读 replay operation，验证 ledger、sampler/quality digest、链身份和水位，不加载 tokenizer、模型或完整 RNG。旧版 ledger 仍可 decode，篡改、缺失、过期/期望摘要不符均 fail-closed。新增 quality/replay 集成回归，定向 `46 passed`，MM1/sidecar/network 联合 `177 passed / 3 skipped`。下一票为 `PT-PIPE-MM1.29` sampling 策略快照与路由接线；按当前硬件无关开发队列，验收前剩余 `1` 张开发票，验收后问题修复另计；真实语义、CUDA/双机、跨节点和生产准入继续后置。
- 2026-08-15 `PT-PIPE-MM1.29` 开发门完成：sampling policy snapshot 固定策略版本、policy ID、issued/expiry、replay 权限、route scope，并以 canonical SHA 绑定 sampling；旧客户端无快照时仅允许 greedy，隐式正温度 sampling 在 prepare 前拒绝。adapter、ledger、worker/replay 均投影并校验 path-free 策略摘要，过期/篡改/权限关闭/期望 policy SHA 不符均 fail-closed。新增策略与路由回归，定向 `48 passed`，最新 MM1/sidecar/network 联合 `181 passed / 1 skipped`（唯一 skip 为 MM1.18 完整模型显式 opt-in）。MM1 硬件无关开发队列已完成，下一阶段转向 `L4-GEMMA4 G4.3.2B`；真实模型采样质量、CUDA/双机、跨节点和生产准入仍后置。
- 2026-08-16 G4.3.2B 后续提交复核：确认 G4.3.2B 已于 `3a3ee84/b41788c` 完成，上一条“下一阶段转向 G4.3.2B”随本次复核失效。G4.4-G4.7、QW3/MM1、MODEL-TOOLS P5-P8 与 EX-N3 后续实现已对照源码和测试；新增已知问题 #11-#16。KIP-11A 已完成官方 Gemma 4 descriptor/assignment/隔离选择性物化合同，下一票为 KIP-11B；此后依次执行 KIP-12 至 KIP-16，外部真机与生产验收仍后置。
- 2026-08-16 `KIP-11B` 开发门完成：新增独立 `.venv-gemma4-pipeline`（Transformers `5.10.1`，不复用 `.venv-gemma4-native`），Gemma4PipelineAdapter 保持官方绝对层号并实现 full/sliding mask、跨段 prefill、连续 decode、native Cache/shared-KV；新增 path-free contract、sidecar controller/worker、2/3 段 multisidecar 与 Scheduler 本地显式 opt-in。KIP-11B 专项 `18 passed`；官方 Transformers 5.10.1 CPU tiny 整模 vs 两段 prefill/decode logits 最大绝对误差均为 `0`。主运行时 `pipeline_runtime_supported=false`，真实 23.9GB 工件、CUDA/双机、网络/恢复和生产路由仍后置。下一票转 `KIP-12`，修复 Gemma native binding fail-open。
- 2026-08-16 `KIP-12` 开发门完成：在既有路径归属门上新增 `gemma4_native_binding.lock.json` 与包内 marker，固定 `llama-cpp-python 0.3.28`、llama.cpp `47e1de77…`、MTMD patch SHA 和七项 ABI 摘要；构建脚本生成 marker 前复核仓库输入，`qlh-cuda.spec` 复核并携带 lock/marker，运行时字段篡改或缺失均在首次 `Llama` 前 fail-closed。组合回归 `138 passed / 1 skipped`，开发机真实受管 binding 通过。下一票转 `KIP-13`；完整 CUDA 安装包/外机验收仍后置。
- 2026-08-16 `KIP-13` 开发门完成：P6 同模型复用改为 schema v1 加载指纹，绑定 registry artifact/SHA/revision/quantization、规范化制品路径与 stat/SHA sidecar、最终 engine、请求量化和稳定 device/offload profile；完整加载后才提交摘要，卸载、distributed-only 和层段状态清除摘要，孤岛引擎不复用。registry 换路径、同路径换 SHA/revision、engine/有效 profile 变化均强制 reload，只变 free VRAM 不抖动；公开状态仅返回 `sha256:<digest>`。ModelManager/API 定向 `167 passed / 1 skipped`，Python 全量 `2510 passed / 18 skipped`（87.78s）；下一票转 `KIP-14` P7 日志认证、IPv6 URL、`--lines` 与有界并发，真实模型/CUDA/双机/安装包仍后置。
- 2026-08-16 `KIP-14` 开发门完成：P7 CLI 改用结构化 IPv4/IPv6 authority 与 `urlencode`，统一发送 `X-QLH-Log-Token`，`--lines` 同步约束请求和显示；API 日志聚合改为 `asyncio.to_thread` + 最大 8 路并发 + 3 秒总 deadline，部分节点失败/超时保留确定性结果，空 worker 集合安全。专项 `62 passed`，Python 全量 `2518 passed / 18 skipped`（91.40s）；下一票转 `KIP-15` P8 导入事务、模型类型和代理治理，真实 Tailnet/安装包仍后置。
- 2026-08-16 `KIP-15` 开发门完成：P8 导入器新增共享模型布局预检、非空权重/`config.json`/GGUF 存在性和后缀校验、相对路径/大小/SHA/revision manifest、同卷 staging→原子发布和注册前二次预检；远程下载支持 `--proxy` 与 `QLH_HTTP_PROXY`/`QLH_HTTPS_PROXY`，HF/ModelScope 使用隔离代理环境。SQLite 仅在预检成功后写入准确 `model_type`、artifact/manifest SHA；注册失败清理远程发布目录。定向 `120 passed / 2 skipped`，Python 全量 `2524 passed / 18 skipped`（`.venv-test`，约 88s），静态编译和差异检查通过。真实源/大权重 loader、CUDA/双机/安装包验收仍后置；下一票为 `KIP-16` EX-N3 required 质量门。
- 2026-08-16 `KIP-16` 开发门完成：EX-N3 required gate 强制人工审核声明与三轮 calibration，collector/quality/real judge 统一透传 `manual_review` 状态和人数，缺失审核或轮次声明直接拒绝；总 gate 实跑 `3/3 passed`，人工复核确认通过，质量专项 `20 passed`（历史质量专项累计 `53 passed`）。reviewer 身份按脱敏纪律不写入证据，重复身份校验以人数门替代；真实 SD→Gemma 三轮取数后再升级生产质量门。当前开发票转入外部真实模型/CUDA/双机/生产路由验收。
- 2026-08-21 投机解码三票 + TG-OPT-G5.3 注入闭环 + 打包瘦身批次登记：`SPC-CS/MEDS/REV` 本机开发门完成（GammaPolicy 置信度调度、Medusa 候选树、置信驱动逆向回退，定向 `107 passed`）；`TG-OPT-G5.3` 解锁影子级动态故障注入闭环（白名单/类型校验/digest 重放/影子只读，真实双 Worker 仍 Blocked）；RAG 后置可准备（provider 长时 soak harness + sqlite-vec ANN 决策，见 `src/provider_soak.py`/`scripts/rag_ann_benchmark.py`）；**打包瘦身**：PyTorch 系移出安装包（`packaging/qlh-slim.spec` + `runtime_guard` 外部运行时 pip 引导，CPU 单独 PyTorch CPU index / CUDA 官方源），专项目标 `64 passed`，真 PyInstaller 构建 + 首启引导待打包环境；`test_task_graph_gate_off` 测试自包含修复（node_config feature-gates 隔离）。cyber-gothic 前端第 2 波入库。不宣称真模型加速收益、生产 ANN 性能或安装包实构建。
- 2026-08-19 集群接入、稳定性与本地 RAG 调研收口：新增《集群接入稳定性与本地RAG实施计划》，规划一次性手动入群（目标节点强制 client）、Auth App/TOTP + 可选邮件批准、SSH 补丁传输、SQLite FTS5/可选向量检索和 `T-RACE` 竞态矩阵。NW4.1 Transport v2 与 NW3.1 本地自签名 WSS loopback 已写入弱网专项；均仅为文档门，未宣称运行时接线或真实证书/双机完成。
- 2026-08-19 `T-RACE-0` 开发门完成：完成连接、注册、层配置 ACK、任务 lease、SQLite 与补丁同步状态审计；修复 TCPClient 旧代际接收线程延迟启动时读取并关闭新 `self.sock` 的竞态，线程现显式绑定 generation 对应 socket。`.venv-test` 定向 TCP/Transport/网络路径回归 `146 passed`；下一票为 `T-RACE-2` 本机确定性 TCP 生命周期矩阵，真实从节点长连/断网验收仍后置。
- 2026-08-22 前端/安卓接口与全量文档审查收口并推进 `CY-API-01/02`：确认旧 `frontend/` 终止支持、仅作对照，`frontend_cybergothic` 成为唯一产品前端；`FE-AUD-01/02`、`CY-API-01` 与 `CY-API-02` 已完成。`CY-API-02` 接入 Cluster Admin 档案切换、端点检查、容量/层计划和 Sidecar 合同投影，定向 Playwright `3 passed`、typecheck/build/contrast `27/27`；真实 control/gateway、双机与生产 Sidecar 后置。下一票为 `CY-API-03`，随后按 `CY-API-04..07`、`AND-API-01..05`、`BUNDLE-M1`、`EX-N3-PROD`、`DOCAGENT-M2-GATE/M3-QA` 分期推进；硬件/外部环境阻塞项不计入本轮开发队列。文档链接检查 `70` 份、`482` 条相对链接全部通过；完整度快照和文档索引已同步。

## 2026-08-22 计划推进记录：CY-API-03
Models 页面已经完成资产治理闭环的本机开发：模型注册/移除、manifest 与文件流查看、活动 assignment 摘要、部署模拟状态机均已接线；主节点权限、内置模型保护和 sha256/活动节点前置条件已实现。真实模型文件、CUDA、双机和生产路由不在本票验收范围。总体下一票为 `CY-API-04`，随后推进 `CY-API-05..07`、Android adapter、BUNDLE-M1、EX-N3-PROD 和 DOCAGENT QA；硬件阻塞项继续后置。
## 2026-08-22 计划推进记录：CY-API-04

CY-API-04 已完成本机开发门。`frontend_cybergothic` 的 Settings/Local RAG 现在具备容量预算、ANN 决策和可恢复 embedding job 控制：创建任务必须绑定模型 SHA256，可查询/运行单批/取消，容量和 ANN 结果带有界解释；`NO_GO` 与 `production_approved=false` 在界面上保持 fail-closed。真实 Ollama `nomic-embed-text:latest`、sqlite-vec benchmark、长时 soak、双机和生产性能验收继续后置。

验证结果：typecheck、production build、contrast `27/27`、RAG 设置页 Playwright `2 passed`；接口扫描 backend `417`、frontend `261`、gap A `0`、gap B `165`。下一票为 `CY-API-05`，接入任务图/工作流状态和取消控制面；硬件、真实网络、外部模型质量不阻塞本机接口开发。

## 2026-08-22 计划推进记录：CY-API-05

CY-API-05 已完成本机开发门。Image Studio 现在覆盖本地输入 Blob 上传、img2img/inpaint、资产检查/登记/导入/下载/状态、许可证确认，以及实验门控制下的分布式单图和四 seed grid；workflow 与远端 Blob 结果以可追踪摘要呈现。真实 CUDA、双机 data plane、长时取消/重试和生产质量继续后置。

验证结果：typecheck/build、contrast `27/27`、Image Studio 定向 Playwright `3 passed`、全量 CyberGothic `62 passed`、后端 diffusion API `13 passed`；接口扫描 backend `417`、frontend `278`、gap A `0`、gap B `157`。下一票为 `CY-API-06`，进入 Tasks 的 Stage/attempt/worker 详情与审计控制。

## 2026-08-22 计划推进记录：CY-API-06

CY-API-06 已完成本机开发门：Tasks 工作流选择后读取 `/api/workflows/{workflow_id}`，呈现 Stage 输入绑定、依赖、输出大小/SHA、attempt/worker/lease、稳定错误码、retry/reassignment/result rejection、journal 计数和 Audit 索引；取消复用既有接口，未伪造手动重派或优化执行按钮。单体 `api_server.py` 新增 `/api/auth/capability`，直连单体返回明确 `required=false/available=false`，Account 不再因 404 显示 capability unavailable；gateway/control-svc 仍负责真实 `local_totp`。验证：Tasks Playwright `3 passed`、API contract `.venv-test` `7 passed`、typecheck/build/contrast `27/27`；扫描 backend `418`、frontend `280`、gap A `0`、gap B `156`、soft `0`。真实 Auth control-svc、双机 Worker、长时重试和优化 shadow 后置，下一票为 `CY-API-07`。
