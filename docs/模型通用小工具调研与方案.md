# 模型通用小工具调研与方案

> 本文调研"给项目现有模型提供通用小工具"可以做什么、哪些值得做，
> 盘点现状避免重复造轮子，按价值给出优先级与实施建议。
> 状态：P0/P1 与 P2 `models_sync_status` / `llm_smoke_matrix` / `gguf_convert` 执行事务已实施并验证；`llama-quantize` Windows 受管包已实际构建，Linux 构建/安装接线已完成但真机产物仍为 `Validating`；完整安装/跨平台、真实双机与未落盘模型覆盖仍为 `Validating`。

## P2 LLM 冒烟矩阵实施复核（2026-08-10）

- 统一入口新增 `python scripts/model_tools.py llm_smoke_matrix`。固定 `qlh-llm-smoke-v1` 四项：中文、英文、精确 JSON 对象和中等长度上下文 marker；默认每项最多 32 个新 token、每个“模型×格式”独立子进程和 180 秒超时，矩阵最多 32 个单元，参数上限不可绕过。
- 发现事实源为 `src/model_config.py` 注册表；`both` 模型分别形成 GGUF/llama.cpp 与 Safetensors/PyTorch 单元。缺资产、CUDA 不可用或 RAM/VRAM 余量不足显式记 `skipped`，不通过 OOM 探测；某一模型加载失败或某条生成失败不会阻断后续单元。执行门和覆盖门分开：退出 0 只表示所有实际执行单元通过，`coverage_complete=false` 时不得宣称全注册表通过。
- worker 强制离线，Hugging Face 动态模块/Hub 缓存重定向到单元临时目录并自动清理；除显式 `--output` 外不持久写入。报告不保存生成原文或模型绝对路径，只保存模型 ID、格式、耗时、输出字符数、SHA256 与语言/JSON/marker 门结果；输出路径落入模型根目录时拒绝。
- 真实证据：Qwen 1.8B 的 GGUF 与 Safetensors 两格式均通过，`2/2 units`、`8/8 jobs`，JSON 可直接解析且 marker 正确；注册表有界扫描发现 10 个单元，实际执行 2 个并全部通过，6 个因资产未落盘跳过，DeepSeek 7B GGUF/Safetensors 分别因当前 RAM/VRAM 余量不足跳过，0 failed，故 `execution_gate_passed=true / coverage_complete=false`。工具定向 `29 passed / 2 skipped`，Python 全量 `1738 passed / 31 skipped`。
- `gguf_convert` 预检与执行事务均已完成；Windows `llama-quantize` 受管包已固定并打包，下一票是 Linux 真机工具链验收，之后才在用户明确批准目标、空间和耗时预算时执行真实模型转换验收。`sd15_merge` 继续等待写入事务和资产门评审。

## P2 GGUF 转换执行事务实施复核（2026-08-10）

- dry-run 仍为默认行为；实际转换必须同时给出显式 `--target --apply --confirm CONVERT`。缺少精确确认或参数无效时 CLI 返回 2 且不写文件；执行前重新运行完整预检，目标父目录必须已存在，目标 GGUF 及相邻 `.sha256` 均必须不存在。
- 转换在目标同卷的 `.qlh-gguf-staging-*` 临时目录中运行，子进程不经 shell，强制 Hugging Face/Transformers 离线并受 `--timeout-seconds` 限制。F16/BF16/F32/Q8_0 走单阶段；Q4/Q5/K 系列先生成 F16 中间件并完成 GGUF 结构检查，再调用 `llama-quantize`。
- 最终 staging 工件必须通过 GGUF 结构解析和全文件 SHA256 校验。发布顺序固定为“摘要旁车先、GGUF 后”，两者都以同卷无覆盖硬链接落位；目标在转换期间被其他进程创建时保持原文件不变，禁止使用可覆盖 rename。发布成功时可见 GGUF 已有摘要旁车；任一阶段失败、超时、取消或竞态均回收 staging 和未完成发布。
- 合成真实子进程证据覆盖 F16 单阶段成功、Q4_K_M 两阶段成功、SHA256 旁车可复验、转换器失败、超时、取消和目标竞态。MODEL-TOOLS 与同步定向为 `48 passed / 2 skipped`；本票代码落地后的 Python 全量为 `1752 passed / 31 skipped`。
- 对真实 Qwen 1.8B 只执行安全门：F16 dry-run 通过；Q4_K_M 因缺 `llama-quantize` 阻塞；未确认的 F16 `--apply` 返回 2 且没有目标、旁车或 staging。**本票没有执行真实 Qwen 转换，也没有生成新的真实模型工件。**
- 固定/打包票已由下节完成 Windows 开发门；下一票在 Linux 真机上执行同一 revision 的构建、运行库和 `.deb` 验收。跨平台门关闭后，再单独申请用户批准，以明确目标路径、空间/时限预算执行真实 F16/Q4 转换，验收摘要、加载冒烟和清理。

## P2 llama-quantize 工具链固定与分发复核（2026-08-10）

- 新增机器锁 `scripts/model_tools/llama_quantize.lock.json`，固定 llama.cpp commit `47e1de77aa0f06bf73cfd8c5281d95979f89fcbe`、CMake target `llama-quantize`、`windows-x86_64`/`linux-x86_64` 目标和可执行文件名；父仓库构建脚本不会修改上游 submodule。
- 新增 `python scripts/build_llama_quantize.py`：无网络、无 shell，使用 CPU-only、静态库、关闭 CUDA/native/OpenMP/UI/HTTPS 的 CMake 配置；Windows GNU 构建额外静态链接 GCC/C++ 运行库，并在打包前检查 PE 依赖只剩系统 DLL。受管包包含可执行文件、上游 MIT 许可证、manifest、逐文件 SHA256 和确定性 ZIP/ZIP SHA256。
- Windows 本机实际构建通过：revision 与锁一致，`--help` 暴露 `Q4_K_M`，EXE `9,532,946` bytes，SHA256 `d91893de6d9b6bede05fcd927f7762b37896779f8cfb337609a3dccea7e30794`；ZIP SHA256 `e3361e96b9cc6b1ab060934f01ca9d45a2797deb1d49ef3ce9280ccea1002b32`。未发现 `libgcc/libstdc++/libwinpthread` 非系统依赖。
- `gguf_convert` 现在优先发现并验证受管包，报告 `provenance=managed_package / verification=verified / revision / sha256`；显式 `--quantizer`、`QLH_LLAMA_QUANTIZE` 或 PATH 工具仍可用，但标记为 `unmanaged`，不伪装成锁定发布工具。manifest 被篡改、revision 漂移、文件增删或摘要不一致时 fail-closed。
- Windows PyInstaller spec 已把验证后的工具包放入 `model-tools/llama-quantize/windows-x86_64`；Linux `.deb` 构建脚本会先调用同一构建器，并把 Linux 受管包放入 `/opt/qlh-edge-inference/model-tools/llama-quantize/linux-x86_64`。本机没有 Linux 真机，因此 Linux 编译、`ldd` 运行库验证、`.deb` 安装和执行仍未宣称完成。
- 当前 Qwen 1.8B Q4_K_M dry-run 已从 `quantizer_missing` 变为 `valid=true`，Q4 两阶段计划和受管量化器摘要均通过；仍未执行真实转换，目标 GGUF、`.sha256` 和 staging 均不存在。工具链定向 `73 passed / 2 skipped`，Python 全量 `1760 passed / 31 skipped`。
- 下一票是 Linux 真机执行同一 revision 的构建/运行库/`.deb` 验收；跨平台门关闭后，才申请用户明确批准的真实 Qwen F16/Q4 转换和加载冒烟。

## P2 GGUF 转换预检实施复核（2026-08-10）

- 统一入口为 `python scripts/model_tools.py gguf_convert`（别名 `gguf-convert`），支持注册表 `--model-id` 或显式 `--source` 二选一，目标通过 `--target` 指定；不指定 `--apply` 时只生成 JSON/人类可读 dry-run，不创建目标、不创建 staging、不调用转换器。
- 预检读取 `config.json`、`tokenizer_config.json`、Safetensors/bin 权重和分片 index，验证 architecture 是否被 vendored `convert_hf_to_gguf.py` 注册；工具链检查转换脚本、Python 依赖、`llama-quantize`、目标路径/目标冲突、源目录边界、目标卷空间和只读可写性。
- 输出类型按实际 llama.cpp 流程拆分：F16/BF16/F32/Q8_0 可单步转换；Q4/Q5/K 系列先转换 F16 再调用 `llama-quantize`。计划以占位符命令输出，不保存绝对路径；dry-run 报告固定 `read_only=true / writes_performed=false / apply_supported=true`，目标或摘要旁车已存在时 fail-closed。
- 真实证据：Qwen 1.8B Safetensors 两片、`3,673,678,272` weight bytes、index 引用完整；F16 预检 `exit=0`、转换器/依赖/架构/空间门均通过，估计输出 `3,673,678,272` bytes、所需空闲约 `4,478,984,640` bytes，当前盘余量约 `80,245,624,832` bytes。Q4_K_M 正确生成两步计划，但因当前没有 `llama-quantize` 以 `quantizer_missing` 阻塞，未写入任何工件。
- 第一阶段自动化证据为 MODEL-TOOLS 与同步定向 `40 passed / 2 skipped`、Python 全量 `1744 passed / 31 skipped`；执行事务的当前证据和下一票以紧邻上节为准。

## P2 多节点模型同步检查实施复核（2026-08-10）

- 统一入口新增 `python scripts/model_tools.py models_sync_status inventory|compare`，支持“本地根目录/已保存清单”任意组合比较；工具不下载、不复制、不删除，也不写模型目录，只有显式 `--output` 才原子写出用户指定的 JSON 文件。
- 清单协议为 schema v1，以顶层相对 `asset_id` 记录资产类型、逻辑字节、文件数、结构摘要和可选内容摘要；不包含扫描根、盘符或绝对路径。默认 `structure` 只读取路径/大小，`--full-hash` 才读取文件内容。不同 hash mode 拒绝混比，避免把结构一致误报为内容一致。
- `compare` 将结果分成 `missing_on_peer`、`extra_on_peer`、`mismatched`，退出码固定为 `0=一致`、`1=存在差异`、`2=清单/参数无效`。外来清单须通过 schema、单段相对 ID、唯一性、计数和摘要格式校验；嵌套 junction/reparse point 一律不跟随。
- 自动化证据：`tests/test_model_tools.py + tests/test_model_sync.py` 为 `24 passed / 2 skipped`，全量 Python 为 `1728 passed / 31 skipped`。真实 `models` junction 与 `H:\qlh_models` 快速清单均识别 10 个顶层模型资产，结构模式比较 `in_sync=true`，两次 CLI 均退出 0；输出扫描未发现盘符/绝对路径。本票未做 41.24 GB 全内容重哈希，也未替代真实双机/跨平台验收。
- P2 `llm_smoke_matrix` 已由上节完成；同步检查的真实双机门仍保留，不由单机冒烟替代。

## P1 SD 批测实施复核（2026-08-10）

- 统一入口新增：`python scripts/model_tools.py sd15_prompt_batch` 与 `sd15_sampler_matrix`，均支持 `--json`，输出 PNG、contact sheet 和原子写入的 `report.json`。
- `sd15_prompt_batch` 固定 prompt×seed 矩阵，最多 64 张；`sd15_sampler_matrix` 固定 scheduler×steps 矩阵，最多 64 张。每项记录 scheduler、steps、seed、耗时、PNG SHA-256、尺寸/熵/通道标准差和 safety flag。
- 批测复用 `SD15Engine`、资产 manifest 全量校验和现有质量门；资产无效时 fail-closed，不下载、不写模型目录、不接主节点 SQLite。引擎请求级 scheduler 新增 `EulerDiscreteScheduler` 与 `DDIMScheduler`，任务结束恢复原 scheduler。
- 真实 CUDA 侧车：Torch `2.5.1+cu121` / RTX 4060 Laptop / Diffusers `0.35.2`；原版 SD 1.5 4-step sampler smoke（Euler + DDIM，2 输出）和 prompt smoke（2 prompts × 1 seed，2 输出）均自动门通过、图像 hash 唯一、CLI `exit_code=0`。完整 10-seed质量门与人工目视不由本票替代。
- 自动化证据：MODEL-TOOLS/SD 相关定向回归 `91 passed / 6 skipped`，CUDA 侧车 scheduler 恢复 `2/2`；Python 全量基线 `1723 passed / 30 skipped`。

## P1 磁盘治理实施复核（2026-08-10）

- 统一入口新增：`python scripts/model_tools.py model_disk_usage <root>` 与 `python scripts/model_tools.py models_clean <root>`，别名为 `disk-usage`、`clean`。
- `model_disk_usage` 按顶层模型/目录统计文件数、逻辑字节、allocated bytes、去重后的 allocated bytes 和扩展名；识别根 junction，遍历时不跟随嵌套 junction/reparse point。
- `models_clean` 默认只读 dry-run；过期 `.part`/`.tmp` 和无目标 `.sha256` 才是默认可清理候选。`.cache`、`models_old_backup` 只列为 `review`，必须分别加 `--include-caches`/`--include-old-backups` 后才可能删除；重复模型内容只列为 review，永不自动删除。
- 任何实际删除必须同时提供 `--apply --confirm CLEAN`，并再次校验目标仍位于用户指定根目录内、不是 junction/reparse point；缺少确认或路径安全校验失败时 fail-closed。
- 自动化证据：`tests/test_model_tools.py` 为 `11 passed / 1 skipped`；跳过项仅为 Windows 无目录 symlink 权限。Python 全量为 `1720 passed / 28 skipped`。真实 `models` junction / `H:\qlh_models` 只读统计识别 204 个文件、约 41.24 GB logical/allocated；dry-run 发现 8 个 stale cache，默认 `safe_to_delete=0`，未删除任何资产。
- 本票仍不进入默认推理路径；SD `sd15_prompt_batch` 与 `sd15_sampler_matrix` 下一票，需要 CUDA 侧车环境。

## P0 实施复核（2026-08-10）

- 统一入口：`python scripts/model_tools.py inspect|verify|sweep`；别名分别为 `gguf_inspect`、`gguf_verify`、`models_sweep`。
- 实现位置：`scripts/model_tools/`。工具保持只读，支持人类输出与 `--json`；GGUF 解析不依赖重量级第三方包，校验复用现有 SHA256 基建，SD manifest 校验复用 `src/diffusion/assets.py`。
- 自动化证据：`tests/test_model_tools.py` 定向 `5/5`；P0 关联回归 `27/27`；Python 全量 `1714 passed / 27 skipped`。
- 真实资产证据：对 junction 根目录 `H:\qlh_models` 执行 sweep，发现 GGUF×2、SD manifest×5、PyTorch 目录×2，全部有效，`orphan_candidates=0`。
- 当前边界：仍处于 `Validating`，需补安装包/跨平台入口接线和发布路径复核；P1/P2 不在本票内，也不进入默认推理路径。

---

## 1. 现状盘点

### 1.1 模型资产（models/，约 5.8GB）

| 类别 | 资产 | 大小 | 说明 |
|---|---|---|---|
| GGUF（llama.cpp） | `DeepSeek-R1-Distill-Qwen-7B-Q4_K_M.gguf` | 4.68GB | 带 `.sha256` |
| GGUF（llama.cpp） | `Qwen-1_8B-Chat.Q4_K_M.gguf` | 1.24GB | 带 `.sha256` |
| PyTorch 完整模型 | `deepseek-r1-distill-qwen-7b/` | — | safetensors 分片 |
| PyTorch 完整模型 | `qwen-1_8b-chat/` | — | safetensors 分片 |
| SD 1.5 diffusers | `sd15-original-v1/`、`sd15-90s-retrovers-v1/` | 各 ~4.9GB | 基座 + DreamBooth |
| SD 1.5 辅助网络 | `sd15-inpaint-v1/`、`sd15-instruct-pix2pix-v1/`、`sd15-ip-adapter-v1/`、`sd15-controlnet-ip2p-v1/` | 数 GB | 专用 pipeline / adapter |

### 1.2 已有工具链（scripts/ 与 src/）

| 已有能力 | 位置 | 覆盖 |
|---|---|---|
| bitsandbytes 加载时量化验证 | `scripts/quantize_model.py` | 仅 PyTorch 模式切换，**不是 GGUF 转换** |
| 统一性能基准 | `scripts/benchmark_all.py`、`benchmark_compile.py`、`sse_ttft_compare.py` | LLM 量化组合 + 编译 + SSE TTFT |
| SD 资产下载/校验/检查 | `scripts/download_sd15.py`、`src/diffusion/assets.py`、`artifacts.py` | 固定 revision + SHA256 强校验 |
| SD 质量门 | `scripts/quality_gate_sd15*.py`、`smoke_sd15.py`、`validate_sd15_api_lifecycle.py` | 固定 seed 生成、语义门、生命周期 |
| 模型注册表导出 | `scripts/export_model_catalog.py` | BUILTIN_MODELS → catalog seed |
| SHA256 计算 | `src/model_sync.py`、`src/diffusion/*` | 文件级/目录级 |
| 模型下载引导 | `src/model_downloader.py` | 缺模型时引导 ModelScope/HF |
| KV 缓存测试 | `scripts/test_kv_cache.py` | llama.cpp KV 生命周期 |
| 实验框架 | `scripts/experiment_core/`、`run_experiments.py` | 自动化优化实验 |

### 1.3 结论：已覆盖 vs 缺口

已覆盖：LLM 性能基准、SD 下载/校验/质量门、SHA256、模型注册、下载引导、KV 测试。
**缺口集中在"模型文件本身的运维与体检"**——GGUF 元数据查看、GGUF 转换、完整性批量校验、磁盘/清理、SD 融合、提示词批测等。

---

## 2. 候选小工具清单（按类别）

### 2.1 GGUF 文件工具（当前缺口最大，P0）

GGUF 是二进制格式，元数据（架构、量化类型、上下文长度、词表大小、张量清单）内嵌在文件头，llama.cpp 加载失败时很难人工排查。三个最值得做的工具：

| 工具 | 功能 | 依赖 | 工作量 |
|---|---|---|---|
| **`gguf_inspect` 元数据查看器** | 读取 GGUF 头：`general.architecture`、`general.name`、量化类型（从文件名之外直接读 `block_count`/`quantization_version` 推导）、上下文长度、词表类型与大小、张量名/形状/类型列表 | `gguf`（llama.cpp 官方 Python 包，轻量）或手工解析 header | 小（0.5-1 天） |
| **`gguf_verify` 完整性校验** | ① 比对 `.sha256` 侧车文件；② 无侧车时用 `gguf` 包完整解析张量元数据（能解析 = 结构未损坏）；③ 可选做张量字节级哈希抽样 | 无额外依赖 | 小（0.5 天） |
| **`gguf_convert` 量化转换器** | 已完成只读源/工具链/空间/路径预检，以及显式确认下的 Q4_K_M / Q8_0 / F16 staging→校验→无覆盖发布事务；Windows 量化器受管包已验证 | llama.cpp `convert_hf_to_gguf.py` + 固定 `llama-quantize`（Linux 真机包待验收） | 中（Windows 开发门完成，Linux 分发与真实模型验收待做） |

> 价值场景：下载的模型报"加载失败"时先 `gguf_inspect` 看量化/架构是否与引擎预期一致；`gguf_verify` 快速区分"文件损坏"与"引擎问题"；`gguf_convert` 支持把新模型按设备显存降量化，无需重新下载。

### 2.2 模型库运维工具（P1）

| 工具 | 功能 | 依赖 | 工作量 |
|---|---|---|---|
| **`models_sweep` 全库体检** | 遍历 `models/`：GGUF 查侧车哈希、diffusers 目录对照 manifest SHA256（复用 `src/diffusion/assets.py` 的 `verify_asset_directory`）、报告孤儿文件（`.part`、`.tmp`、无主 `.cache`）、输出人类可读 + JSON 报告 | 复用现有 `compute_file_sha256`、`verify_asset_directory` | 中（1-2 天） |
| **`models_clean` 磁盘清理助手** | `--dry-run` 列出可安全删除项（孤儿 `.part`、`.cache`、旧模型备份 `models_old_backup`、重复 sha256 文件），确认后才删；**必须感知 junction**（当前 `models/` 是 H 盘 junction，不能误删联接本身） | 无 | 小-中（1 天） |
| **`models_sync_status` 多节点模型同步检查（已实施）** | 对比主/从节点模型清单与哈希，输出"缺、多、不一致"三类差异；只读不改，为分布式部署前体检 | 复用现有 SHA256 与模型目录约定 | 中（已完成开发门） |
| **`model_disk_usage` 占用统计** | 按模型/子目录统计实际占用，区分 junction 目标盘占用（`du` 对 junction 显示 0 的坑，需 `os.stat` + `Path.resolve()` 处理） | 无 | 小（0.5 天） |

### 2.3 SD 1.5 图像工具（P1-P2）

| 工具 | 功能 | 依赖 | 工作量 |
|---|---|---|---|
| **`sd15_merge` 模型融合器** | checkpoint/LoRA 融合：基座 + LoRA → 合并 safetensors，产出新 asset 目录并按现有 SHA256 约定登记 | `diffusers`、`safetensors`（CUDA 侧车环境） | 中（2-3 天） |
| **`sd15_prompt_batch` 提示词批量测试** | 固定 seed/step/sampler，批量跑 prompt 矩阵并生成对比网格图 + 报告（对齐 `quality_gate_sd15.py` 的报告格式） | 复用 `smoke_sd15.py` / quality_gate 基建 | 中（2 天） |
| **`sd15_sampler_matrix` 采样器对比** | 同一 prompt 下 Euler/DDIM/DPMSolver 等采样器 + step 数矩阵，输出直观对比图，用于 preset 调参 | 同上 | 中（2 天） |
| **`sd15_lora_inspect` LoRA 元数据查看** | 读 safetensors 头：`ss_*` 元数据（训练集、触发词、学习率）| `safetensors` 头解析即可，无需 GPU | 小（0.5-1 天） |

> 注：SD 侧尽量复用现有 CUDA 侧车虚拟环境（`.venv-packaging-cuda`）与质量门报告登记机制，不污染主解释器。

### 2.4 LLM 运行时体检工具（P2）

| 工具 | 功能 | 依赖 | 工作量 |
|---|---|---|---|
| **`llm_smoke_matrix` 全模型冒烟矩阵（已实施）** | 对注册表里每个模型（GGUF + PyTorch）跑固定中文/英文/中等上下文/JSON 输出，汇总可加载、可生成与格式门；隔离超时和资源预检 | 复用 `ModelManager` 双引擎加载路径 | 中（已完成开发门） |
| **`tokenizer_stats` 分词器统计** | 输出词表大小、BOS/EOS/PAD 是否齐全、中文字符编码效率（每字 token 数） | `transformers` / llama.cpp tokenizer | 小（0.5 天） |

---

## 3. 优先级与实施建议

### 3.1 推荐路线

| 优先级 | 工具 | 理由 |
|---|---|---|
| **P0（先做）** | `gguf_inspect` + `gguf_verify` | 缺口最大、工作量最小、立即提升排查效率；纯标准库/官方 `gguf` 包，无环境风险 |
| **P0** | `models_sweep`（含 junction 感知） | 模型已迁 H 盘，全库体检能发现迁移残留与损坏，复用现有 SHA256 基建 |
| **P1** | `models_clean`、`model_disk_usage` | 磁盘治理刚需，注意 junction 安全 |
| **P1** | `sd15_prompt_batch` / `sd15_sampler_matrix` | 直接服务 preset 调参与质量门，复用 quality_gate 报告格式 |
| **P2** | `models_sync_status` / `llm_smoke_matrix` / `gguf_convert` 预检与执行事务（开发门已完成）；Windows `llama-quantize` 已固定/打包，下一票 Linux 真机工具链验收；经明确批准再做真实转换验收；后续 `sd15_merge` | 转换/融合需工具链、空间、校验、无覆盖发布和人工批准门 |

### 3.2 通用原则

1. **只读工具优先**：inspect/verify/sweep/status 全部只读，无副作用，先落地建立信心。
2. **复用而非重造**：SHA256 用 `src/model_sync.py`；SD 校验用 `src/diffusion/assets.py:verify_asset_directory`；报告格式对齐 quality_gate 系列；模型注册用 `src/model_config.py` BUILTIN_MODELS。
3. **CLI 优先，输出双格式**：人类可读表格 + `--json`（对齐 `export_model_catalog.py` 与 `quality_gate` 的报告约定，未来可被自动化实验框架消费）。
4. **junction 安全**：`models/` 现为 H 盘 junction，任何删除/统计工具必须 `os.path.islink()` 或 `Path.is_junction()` 判断，只操作 junction 内部文件，绝不删联接本身。
5. **环境隔离**：GGUF 纯解析工具只用标准库 + `gguf` 包（轻量）；SD 工具走 CUDA 侧车；不往主解释器塞重依赖。

### 3.3 落地形态建议

统一入口：`scripts/model_tools/` 目录 + 一个 `model_tools.py` 子命令分发（`python scripts/model_tools.py inspect models/xxx.gguf`），或直接复用 TUI 指令集模式注册进 `bjtu` 命令体系（对齐 `docs/TUI指令集.md`）。P0 两个工具建议一次性提交。

---

## 4. 参考

- GGUF 规范：llama.cpp `gguf-py`（`gguf` Python 包）提供 `GGUFReader` 读取头部与张量元数据
- 现有 SHA256 基建：`src/model_sync.py`（`compute_file_sha256` / `compute_model_sha256`）
- SD 资产校验：`src/diffusion/assets.py`（`verify_asset_directory`，逐文件大小 + 权重 SHA）
- 报告格式约定：`scripts/quality_gate_sd15.py` 的 `--review-report` 登记模式、`docs/自动化优化实验与报告方案.md`
