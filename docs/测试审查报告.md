# 测试审查报告（2026-08-17，新分类法）

> 状态：现行（审查快照）
>
> 更新日期：2026-08-17
> 审查对象：`tests/`（189 个 tracked 文件）+ control/gateway/android 测试，窗口 2026-07-20 ~ 2026-08-17（440 提交，179 个测试相关提交）
>
> **分类方法**：为避免杀虫剂效应，本次**不使用**既有分类（断言质量/覆盖率盲区/可靠性/非 Python 层），改用三个新维度：
> ① **修复驱动**（修复提交是否带"先红后绿"复现测试）；② **生命周期覆盖**（新功能与测试的同步度与行为深度）；③ **价值密度 × 维护成本**（测试的 git 修改频率、壳测试判定、重复/参数化/耦合）
>
> 安卓端按审查顺序**放最后**（GPT 组活跃期，只做浅层只读评估，待其交付后深查）

## 1. 总体结论

**中上水准**：核心产品修复 ~79% 带测试且多为真复现断言；六大新功能主题（补丁分发/离线整合包/文档 agent/QW3/判题恢复/TaskWorker）测试与功能同批落地、行为级深度；**零壳测试**（无只测导入/常量的用例）；参数化与耦合健康。主要短板集中在**工具代码盲区**与**两个高风险裸修复**。

## 2. 维度一：修复驱动（37 个产品修复）

| 指标 | 数值 |
|---|---|
| 产品代码修复提交 | 37 |
| 同提交改测试 | 27（73%）；剔除低价值项后 **79%** |
| 未改测试 | 10（2 个高风险） |

### 🔴 高风险裸修复（建议优先补测）
| 提交 | 修复点 | 风险 |
|---|---|---|
| `6baab87` | 推理核心 load 路径 4 缺陷（use_compile 传参 TypeError、model_loaded 状态、remote-model 缓存超时） | 核心运行时零回归防护 |
| `9a71ccb` | 网关异常过滤器 request_id fallback | 错误路径门面无对应用例 |

### 🟠 工具代码系统性盲区
`scripts/model_tools/`（gguf_convert/import_model）**仓库内零测试**，近 30 天 3 连发修复均裸奔（`2fe47a6`→`8129806`→`3e26a35`）；其中补丁持久化新增"自动应用"分支最易回归。`contract_diff.py`、`serve.py` GBK 修复亦无测试。

### ✅ 好的修复测试（16 个）
`bccf6a8`（含 `pytest.fail` 拒绝旧行为）、`1e86ec1`（docstring 明言"修复前必崩"+真实执行）、`539a7e1`（cancel/泄漏/缺字段三面断言）、`cacdc27`（403 语义）、`fee20e0`（Monte-Carlo 分布等价）等。

## 3. 维度二：生命周期覆盖（6 大新功能主题）

| 主题 | 评级 | 证据 |
|---|---|---|
| 补丁分发/监听（M1/M2） | **完整** | 源+测同 commit；真实 Ed25519 验签闭环、篡改拒绝、重试 fail-closed、dry-run 副作用断言 |
| 离线整合包（去重/分卷/归档） | **完整** | 同 commit + 6 个后续修复补测；dedup 只存一份、junction 保护、prune dry-run 不删 |
| 文档 Agent（M1/M2） | **完整** | 7 个测试文件；R1-R5 双面、密钥脱敏、缓存命中、原子替换 |
| QW3 系列（~40 提交） | **完整** | 24 个 test_qwen3_*；真实 config 契约、事务状态机、版本互斥 |
| 判题恢复（EX-N3） | **完整** | rubric SHA 漂移拒绝、required/isolated 语义、0.15 floor 边界、KIP-16 强制门 |
| TaskWorker | **完整**（control_simulation 偏薄） | v3 版本协商/防降级/篡改拒绝；62 行 control_simulation 只断言摘要契约 |

**薄弱点**：服务入口薄壳层（`src/db.py`、`scheduler_svc_main.py`、`inference_client.py` 等）无同名专测（靠间接覆盖）；真实模型通道（qw3 worker、gemma4 smoke、judge_real）不进常规 pytest（环境依赖盲区，非缺陷）；`scripts/` 验证/benchmark 类工具 0 测试（`contract_diff.py`、`check_readme_l10n.py` 等，工具性可接受）。

## 4. 维度三：价值密度 × 维护成本

### 价值密度（189 文件，窗口修改频率）
- **0 次修改 = 0 个**；1 次 = 88（46.6%，多为 7-8 月新落地文件，观察期不足，非废弃）
- 抽查 7 个"创建即唯一提交"文件：**全部非壳**（行为断言）
- **主动价值管理存在**：`f056e0a` 整文件删除测死代码的 `test_db_config.py`；`02bef2e` 把 13 个恒跳过 `requires_db` 用例改写为 SQLite 真实用例
- 峰值：`test_scheduler.py` 25 次、`test_inference_service_protocol.py` 24 次（高价值/高维护伴生）

### 维护成本
| 项 | 发现 | 处置建议 |
|---|---|---|
| 重复 helper | `_single_stage`（fencing/fault_injection 两处，参数默认已漂移）、`_make_tiny_model`（两处实现不同） | 收敛到 `tests/helpers/` |
| 参数化 | 79 处，case 数 3-10，**无滥用** | 无需处理 |
| 超大文件 | 18 个 >800 行（`test_scheduler.py` **6521 行** ≈ 140 用例）；**无超长函数**（单函数 ≤100 行） | 体积管理可选 |
| 耦合 | 仅 1 个 module-scope 可变服务（`live_inference_svc`，低风险）；无固定端口/session scope | 无需处理 |

## 5. 安卓端（最后，浅层只读）

> **审查顺序放最后**：GPT 组正在安卓活跃开发（A1 图像理解/B Worker 接线），本次仅浅层只读评估，未深查其进行中代码。

- JVM 单测：5 个契约文件（ApiClient/NetworkAddress/MainViewModelLogic/WorkerProtocol/WorkerClientState）+ 2 个 androidTest UI——**够用**（纯逻辑覆盖良好）
- **盲区**：UI 图像语义链（A1）、C++ JNI 侧无自动化验证文件（instrumented 仅 2 个）
- **建议**：待 GPT 组交付 A1/B Worker 后，对 `android/app/src/test/` 与 androidTest 做一次深度审查（届时仓库状态稳定）

## 6. 优先行动清单

| 优先级 | 行动 | 归属 |
|---|---|---|
| P0 | 补 `6baab87` 加载路径回归测试（推理核心） | 主开发 |
| P0 | 补 `9a71ccb` 网关 request_id fallback 用例 | 主开发 |
| P1 | `scripts/model_tools/` 建测试（gguf_convert 补丁持久化/import_model 子进程） | 主开发 |
| P1 | 收敛 task_graph 系重复 helper 到 `tests/helpers/` | 可选 |
| P2 | 88 个"1 次"文件 2 个发布周期后复评 | 定期 |
| P2 | `test_scheduler.py` 按类域拆分（体积管理） | 可选 |
| 待办 | 安卓端深度审查（GPT 组交付后） | 后续 |

## 7. 变更记录

| 日期 | 内容 |
|---|---|
| 2026-08-17 | 新分类法审查（修复驱动/生命周期覆盖/价值密度×维护成本）；安卓端最后浅层评估；结论中上，P0 两项裸修复待补测 |
