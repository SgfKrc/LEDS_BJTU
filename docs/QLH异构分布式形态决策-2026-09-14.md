# QLH 异构分布式形态决策（2026-09-14）

> 状态：**生效**（v5；llama.cpp 模型分片/RPC 主攻 / PyTorch PC 对照 / 任务图临时回退）。
> 关联：[QLH 定位与模块归属终局决策](QLH定位与模块归属终局决策-2026-09-14.md)（v5 主线）、[TP 孤岛接入指南](TP孤岛接入指南.md)、[张量并行外部辅助与混合拆分调研方案](张量并行外部辅助与混合拆分调研方案.md)。

## 1. 调研结论（2026-09-15；Reasonix 只读审计 + 官方来源）

1. **llama.cpp RPC 存在跨机权重/KV 分布能力，但尚未达到生产支持**：主 host 可通过 `--rpc host:port,...` 使用多个 `ggml-rpc-server` worker，权重和 KV 可按设备内存比例分布；官方仍将 RPC 定性为 proof-of-concept、fragile、insecure，并明确不应暴露到开放网络。QLH 将它列为主线攻关对象，但在分片驻留、身份、传输保护、租约、回滚和 soak 门通过前，不宣称已接入生产。
2. **llama.cpp 的本地多设备 split 与 RPC 必须分开**：server 当前有 `--split-mode layer/row/tensor`；layer 是默认的 pipeline 形态，tensor 仍是实验性路径。它们证明的是同一运行时内的模型分割能力，不等于 QLH 已获得跨生态、异构网络的稳定分布式协议。
3. **Android 的 PyTorch 生态路径是 ExecuTorch AOT，而不是完整 PyTorch 运行时**：官方 ExecuTorch Qwen3 示例覆盖 Qwen3 0.6B/1.7B/4B，Android 通过 AAR 和 XNNPACK/Vulkan 等后端运行；代价是导出链和模型集合受限。它是远期可选引擎，不改当前 L=GGUF、D=PyTorch 的双档边界。
4. **跨框架层接力（PyTorch ↔ llama.cpp 一次 forward）没有本项目可直接采用的中间张量交换标准**，工程上等于另造分布式引擎；当前不做 GGUF 到 PyTorch `LAYER_FORWARD` 的接力。
5. **任务图/整请求级分发只能作为迁就型回退**：OpenAI 兼容 HTTP 是最低公共层，任务池可以承载 Android/GGUF 轻节点，但它要求完整模型可在某个执行节点装下，不能实现“单机装不下、各节点只装一部分”的核心价值；主线不再把它当作模型分布式形态。
6. 本地核对：项目已有三种代码形态（PyTorch 层拆分、TP 孤岛、任务池）；Android Full 当前使用本地 llama.cpp/HTTP 任务路径，构建未启用 `GGML_RPC=ON`，因此不能把 RPC 或 Android Layer Worker 写成已交付能力。

### 1.1 Silica Cluster 外部复核

- [Silica Cluster](https://github.com/ShintoChakkiath/Silica-Cluster-Decentralized-Mobile-AI) 确实把 Android 设备包装成 `libllama.so` 主节点或 `librpc.so` worker，并通过标准 llama.cpp `--rpc` 连接远端节点。
- [BinaryRunner.kt](https://raw.githubusercontent.com/ShintoChakkiath/Silica-Cluster-Decentralized-Mobile-AI/main/app/src/main/java/io/github/shintochakkiath/silicacluster/BinaryRunner.kt) 的“精确层拆分”实际是 `-ngl N`；没有完整层数时则使用 `--tensor-split` 比例。按 llama.cpp 官方语义，`-ngl` 是设备上限层数，`--tensor-split` 是各设备比例，不是 QLH 的跨节点连续 `layer_range` 合同。
- [SilicaService.kt](https://raw.githubusercontent.com/ShintoChakkiath/Silica-Cluster-Decentralized-Mobile-AI/main/app/src/main/java/io/github/shintochakkiath/silicacluster/SilicaService.kt) 还包含完整模型上传到 worker 后再启动引擎的路径，因此它验证的是 Android 承载 GGUF/RPC 的可行性，不是模型层段只持有、带身份校验和可恢复租约的分布式层流水线。
- **结论（2026-09-15 用户裁决后改写）**：Silica 的价值不只在 Android，而在于它给出了“llama.cpp host + RPC backend + Android worker”的攻关入口。QLH 主线将 llama.cpp 模型分片/RPC 作为生产主攻，先完成 PC 单机、同机双进程模拟，再做 PC/Android 真机；PyTorch 降为 PC 对照，任务图降为完整模型回退。QLH 要补的是把“比例/上限分配”升级为**可校验的部分驻留合同**，并补上身份校验、传输保护与租约。Silica 为 AGPLv3，**不复制其代码**，只借鉴协议边界与验证方法。

## 2. 三形态定案（主线排序）

> 2026-09-15 v5 更新：Android RPC 并入 llama.cpp 模型分片主形态；PyTorch 仅 PC 对照；任务图保留为临时回退。

| # | 形态 | 适用节点 | 实现载体 | 语义 |
| --- | --- | --- | --- | --- |
| 1 | **llama.cpp 模型分片/RPC（主线）** | PC、Android、CPU/弱 GPU 节点 | 本地 `llama-cpp-python`；分布式 `llama-server`/`ggml-rpc-server`；QLH 分片合同 | **同一 GGUF 的模型级分片**：目标是单节点装不下、各节点只承载部分数据；Silica 提供 Android worker 参考 |
| 2 | **PyTorch 层流水线（PC 对照）** | 仅 PC/工作站 | `model_module.py` + `tcp_comm.py` + `qwen3_pipeline_*`/`inference_service` | 用于正确性、性能和容量对照；不进入边缘、默认路由或正式发行 |
| 3 | **任务图/整请求（临时回退）** | 无法参加模型分片的异构节点 | `task_worker_protocol.py`/`task_provider.py`/`task_chain` | 要求某个节点装下完整模型；只做兼容回退与对照，不代表模型已被分布式拆开 |

### 2.1 llama.cpp 分片主路径的身份与可用性（**auth 与可用性策略就落在这一层**）

llama.cpp 分片主路径是“跨设备参与同一模型计算”，因此它必须自带**参与资格**与**故障语义**。这两件事**不是可选项**，也不能等到接入生产才补——它们是这层的地基。QLH 侧**已有可用件**，不需要从零造：

| 维度 | 要求 | QLH 已有 / 复用 | 对应票 |
| --- | --- | --- | --- |
| **节点身份（auth）** | 每个 RPC worker 持有可验证身份；入群授权一次性、不可重放 | `src/cluster_join.py`：Ed25519 节点密钥 + 一次性 nonce 消费 + 300s 短 TTL grant + 请求摘要防篡改 | `AUTH-TOTP-01/02` |
| **人类授权（auth）** | 签发 / 转让 / 移除等高危动作必须由人确认一次 | **TOTP 一步确认**：authapp 输入一次 6 位码即视为审核通过，替代原邮件审核与"布尔审批接缝" | `AUTH-TOTP-01/02` |
| **分片租约** | worker 的“我持有哪些模型数据/计算分片”必须是**有时限、可回收**的租约，而非一次性分配 | `shard_spec`/分片摘要 + 租约字段（语义对齐任务图租约） | `CORE-LLAMA-PC-RPC-01` |
| **故障恢复** | worker 掉线 → 重新分配分片；恢复后**不双写** | 心跳 + 重分配（PC/Android 阶段验证） | `CORE-LLAMA-PC-RPC-01` |
| **防脑裂（fencing）** | 备用主节点接管后，旧主**必须立即失效** | **epoch/term 单调递增 + fencing**（旧主凭据作废） | `HA-STANDBY-01` |
| **备用主节点 / 快转让** | 主节点不可用时可接管，且要**秒级**（取代 48h 投票） | **TOTP 快转让**（`epoch += 1`）；明确**不做**完整 Raft/quorum | `HA-STANDBY-01` |
| **参与资格** | 按**能力标签**判定，不按硬件型号硬编码 | 能力标签 + `votes`/`weight` 分离（MongoDB 副本集模式） | `REVIEW-ROLE-01` |
| **传输保护** | RPC 通道不得裸奔（上游 llama.cpp RPC **未加密**） | 限定内网 / 隧道（Tailscale 等），不新增公网暴露面 | `CORE-LLAMA-PC-RPC-01` |

**与既有文档的关系**：这一节补的是此前缺失的一环 —— 过去只讨论"**能不能拆**"，没写"**拆完谁有资格参与、节点掉了怎么办、主节点挂了谁接管**"。方案与业内对照见[分布式组网审核与可用性重构调研](分布式组网审核与可用性重构调研-2026-09-15.md)，落地顺序以其中 §4 行动表为准。

## 3. 选型规则（形态怎么挑）

- **输入**：`device_profiler` 画像（显存/内存/算力/平台）+ 节点能力表（能否装 torch、互联带宽）；
- **规则**：
  - 能运行 GGML/RPC 且可证明部分驻留的 PC/Android 节点 → ① llama.cpp 模型分片；
  - 仅能运行 PyTorch 的 PC 节点 → ② 参考实验，不作为生产分片节点；
  - 无法参与模型分片但能运行完整模型的节点 → ③ 临时任务图回退；
- **协议纪律**：模型粒度优先使用 llama.cpp 分片合同；整请求 HTTP/任务协议只作回退；**禁止跨框架 hidden-state 接力**。

## 4. llama.cpp 模型分片攻关与验收顺序

- **G0 断点门**：先做功能裁切/重写后的入口、import、schema、测试、资源和外置子模块合同检查；TUI、模型下载/选择、API、llama 单机入口不能断。
- **G1 单机门**：PC 单进程 llama.cpp 跑通模型舰队代表工件，记录模板/thinking、流式、质量、RSS/PSS、冷启动和吞吐；默认路径不导入 torch。
- **G2 双进程门**：同一台机器启动 **1 个 llama host + 1 个 `ggml-rpc-server` worker**，先用 loopback 验证 RPC。必须证明各进程/后端只持有分配部分，合计可承载单进程装不下的模型；禁止以“两个进程各自加载完整 GGUF”通过。
- **G3 PC 真机门**：跨 PC 验证模型身份/摘要、分片分配、租约、epoch fencing、断线重分配、内存收益、正确性和吞吐；RPC 上游未加密，必须限制在内网/隧道。
- **G4 Android 门**：借鉴 Silica 的 `librpc.so`/`ggml-rpc-server`，完成 Android worker 交叉编译、真机接入、热/能耗、断线和安全验证；Android UI 不回流主仓。
- **生产准入**：G0→G2 未通过不得做真机网络实测；G3 未通过不得默认开启 RPC；G4 是 Android 生产路径的必要门。所有阶段保留 llama 单机回退和 PyTorch PC 对照。

## 5. ExecuTorch（主线外研究，记录）

- **触发条件**：安卓侧需要 PyTorch 生态模型，或 GGUF 覆盖不足时评估；
- **已知**：Qwen3 0.6B/1.7B/4B 官方支持、Android AAR、AOT 导出链、性能同量级；代价 = 模型集受限与导出工程量；
- **当前动作**：不进入主线发行或默认运行时；只作为端侧 AOT 研究对照。Android 主线优先走 llama.cpp/GGUF RPC。

## 6. 不变式

1. 生产引擎为 llama.cpp/GGUF；PyTorch/Safetensors 仅作 PC 对照，不能进入边缘安装、默认路由或正式发行；
2. 已有代码、模拟和受控验证不能替代 Android 真机、真实模型、长时和生产网络验收；
3. 任务图是完整模型回退，不等价于模型分片；跨框架层接力不做；llama.cpp 分片适配允许作为本项目核心技术攻关。
4. 分布式真机实测前必须完成单机与同机双进程模拟，且功能裁切/重写断点和子模块低耦合门必须先通过。

## 7. 官方来源与证据边界

- [llama.cpp RPC README](https://github.com/ggml-org/llama.cpp/blob/master/tools/rpc/README.md)：确认 `ggml-rpc-server`、`--rpc`、按设备内存分布权重/KV 的能力，同时确认 RPC 的 proof-of-concept、fragile、insecure 定性。
- [llama-cpp-python server settings](https://github.com/abetlen/llama-cpp-python/blob/main/llama_cpp/server/settings.py)：确认 Python server 暴露 `rpc_servers`、`n_gpu_layers`、`split_mode` 和 `tensor_split` 参数；因此 PC 适配可以先复用 Python server/HTTP 合同，底层 RPC 仍需以 llama.cpp 原生行为实测为准。
- [llama.cpp server README](https://github.com/ggml-org/llama.cpp/blob/master/tools/server/README.md)：确认 `--split-mode layer/row/tensor` 的本地 split 语义，以及 tensor 路径的实验性边界。
- [Silica Cluster README](https://github.com/ShintoChakkiath/Silica-Cluster-Decentralized-Mobile-AI) 与 [BinaryRunner.kt](https://raw.githubusercontent.com/ShintoChakkiath/Silica-Cluster-Decentralized-Mobile-AI/main/app/src/main/java/io/github/shintochakkiath/silicacluster/BinaryRunner.kt)：确认 Android `libllama.so`/`librpc.so` 与标准 `--rpc` 接线；其 AGPLv3 代码不直接复制。
- [Qwen3 官方仓库](https://github.com/QwenLM/Qwen3) 与 [ExecuTorch Qwen3 Android 示例](https://github.com/pytorch/executorch/blob/main/examples/models/qwen3/README.md)：确认 Qwen3 的 GGUF 生态路径和 Qwen3 0.6B/1.7B/4B 的 ExecuTorch AOT/Android 示例。
- [ExecuTorch Android 使用文档](https://docs.pytorch.org/executorch/stable/using-executorch-android.html)：确认 Android AAR 运行时；[Android 后端文档](https://docs.pytorch.org/executorch/stable/android-backends.html)：确认 XNNPACK、Vulkan、Qualcomm 等可选后端。

这些来源证明的是上游能力和限制，不证明 QLH 已完成 Android 真机、GGUF RPC、真实多节点或生产网络验收。QLH 的发布口径必须以本地工件、实际参与节点、可复现命令和质量/性能证据为准。

## 变更记录
| 日期 | 变更 |
| --- | --- |
| 2026-09-15 | 官方 RPC/server、ExecuTorch/Qwen3 文档与 Reasonix 只读审计复核：RPC 准入仍未完成，Android 尚未启用 RPC，真实硬件证据不提前宣称通过；补充官方来源与证据边界 |
| 2026-09-15 | 外部复核 Silica Cluster：确认 Android + llama.cpp RPC 候选真实存在；因其使用标准 offload/split 且缺少 QLH 分片合同，纳入 RPC 候选验证；该记录随后由 v5 主线裁决取代 |
| 2026-09-15 | **v5 主线裁决**：llama.cpp 模型分片/RPC 升为生产主攻；验收顺序固定为断点/低耦合 → PC 单机 → 同机双进程 → PC 真机 → Android；PyTorch 降为 PC 对照，任务图降为临时整模回退 |
