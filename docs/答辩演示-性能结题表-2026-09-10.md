# 答辩演示性能结题表

> 票号：`DEF-A2`  
> 报告口径：控制面 fixture 与真实模型指标严格分表，禁止跨表替代  
> 当前结论：P3 TaskGraph 控制面数据可引用；真实模型 TTFT/tok/s 与物理双机数据按输入状态明确标记

## 数据来源与完整性

| 数据源 | SHA-256 | 状态 | 可支持结论 |
| --- | --- | --- | --- |
| `build/defense-benchmark/fixture-single-host-final.json` | `10bc4f1833dbed71480e39f8aac8e474a99f6e24fd10453572fca6b22935ef7a` | `PASSED` | 单机 TaskGraph 控制面 fixture 延迟与串行任务率 |
| `scripts/demo/real-model-performance-not-run.json` | `01ec7a67bde11b8c26e2765bea4c41b556a3c5c1fbc43adc439df626ab18cbb4` | `NOT_RUN` | 性能不可用；`AUD-RT-01` / `AUD-SW-01` 功能验收已完成，但未按本表性能 schema 采样 |

- P3 采集时间：`2026-09-10T07:18:41.230515+00:00`。
- P3 workload：`def-p3-control-plane-v1`，12 次计量 + 2 次预热，每轮 1 个 `full_inference` fixture stage，固定载荷 288 bytes。
- Workload SHA-256：`638e4ef8d5e15b7828c8593e180449599a7f9d630b7ba10f3ac310345e9b09df`。
- 真实模型性能身份：`NOT RUN`。当前机器已完成真实模型功能性加载/生成/切换验收，但没有按本报告 schema 采集 TTFT、inter-token、tokens/s、峰值资源和完整环境身份，因此不得把功能 smoke 推导为性能数据。
- `scripts/sse_ttft_compare.py` 的控制台文本缺少本报告要求的完整身份、环境、拓扑和 JSON 契约，因此未被静默采纳。

## 表 1：TaskGraph 控制面 fixture

> 单位中的 `tasks/s` 是串行 fixture workflow 完成率，不是 tokens/s；两组均为 `host_count=1`。

| Series | Topology | Hosts | Processes | Samples | Median (ms) | P95 (ms) | Serial tasks/s |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: |
| `in_process_fixture` | `single_host_single_process` | 1 | 1 | 12 | 1.553 | 1.963 | 702.519 |
| `loopback_worker_fixture` | `single_host_dual_process` | 1 | 2 | 12 | 2.348 | 3.593 | 396.710 |

可引用结论：同一固定 workload 已分别走进程内 provider 与真实 loopback worker 进程路径，可比较本机控制面/进程通信开销。

不可引用结论：不得从本表推导模型 TTFT、decode tokens/s、生成质量、物理双机收益或模型加速比。

## 表 2：真实模型时延与吞吐

| Metric | Median | P95 | Evidence state |
| --- | ---: | ---: | --- |
| TTFT (ms) | `NOT RUN` | `NOT RUN` | `real_model_environment_unavailable` |
| Inter-token latency (ms) | `NOT RUN` | `NOT RUN` | `real_model_environment_unavailable` |
| E2E latency (ms) | `NOT RUN` | `NOT RUN` | `real_model_environment_unavailable` |
| Decode (tokens/s) | `NOT RUN` | `NOT RUN` | `real_model_environment_unavailable` |

## 表 3：真实模型资源

| Metric | Value | Evidence state |
| --- | ---: | --- |
| Peak memory (GB) | `NOT RUN` | `real_model_environment_unavailable` |
| Model load (s) | `NOT RUN` | `real_model_environment_unavailable` |

## 表 4：结论资格矩阵

| 结论 | 当前是否可用 | 原因/边界 |
| --- | --- | --- |
| 固定负载下 TaskGraph 控制面 fixture 数值 | 是 | P3 schema、workload SHA、样本数、拓扑和声明门均通过 |
| 第二个真实 worker 进程参与控制面路径 | 是 | `single_host_dual_process`，worker exit code 为 0 |
| 真实模型 TTFT / inter-token / E2E / tokens/s | 否 | 功能验收已完成，但本表要求的性能采样仍为 `NOT RUN` |
| 物理双机性能 | 否 | 不可用；`physical_dual_host_data_pending` |
| 模型质量或通用加速比 | 否 | 本报告不采集输出质量，也不把异类指标作加速比 |

## 结题摘要

1. `DEF-P3` 的两组数字只进入“控制面 fixture”表，标签、单位和拓扑保持完整。
2. 当前真实模型性能输入为 `not_run`；`AUD-RT-01`/`AUD-SW-01` 的功能验收不能补齐任何 `NOT RUN` 性能单元格。
3. 只有未来输入通过 `qlh.real_model_performance.v1` 的模型工件、环境、拓扑、负载、事件计数和声明门校验后，表 2/3 才会出现真实数值。
4. 当前物理双机性能结论：不可用；`physical_dual_host_data_pending`。

## 实现与契约证据

- [A2 汇总器](../scripts/perf_report.py)
- [真实模型 NOT RUN 输入](../scripts/demo/real-model-performance-not-run.json)
- [P3 benchmark 实现](../scripts/demo/defense_benchmark.py)
- [A2 正负向契约测试](../tests/test_defense_performance_report.py)

## 重建命令

```powershell
scripts\demo\performance_report.bat --benchmark build/defense-benchmark/fixture-single-host-final.json
```

该命令只读取 JSON 并生成 Markdown/校验报告，不启动后端、不访问网络、不加载模型。
