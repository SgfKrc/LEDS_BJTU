# QLH 定位与模块归属终局决策（2026-09-14）

> 状态：**生效**（v4；本文与《主线开发计划》《支线开发计划》共同构成当前边界）。本文档是归属裁决依据，冲突表述以本文为准。
> 创建日期：2026-09-14（v4 重整）
> 依据：本地审计、功能存废清单、分布式形态调研、用户最新指示（主仓聚焦分布式推理优化；必须保留边缘版；TUI 为主交互；非核心功能独立并迁移至侧车/Koakumix）。

## 1. 主项目定位（最终裁定）

**主项目 QLH = 轻量分布式推理系统：引擎（数据面）+ 管理器（控制面）+ TUI/合同/验收基建，同仓同项目。**

- **一句话**：在边缘设备（无/弱 GPU）上跑轻量模型，并把 PC/工作站节点组织成可验证分布式推理系统——**数据面（双引擎）**：**L 档 = GGUF/llama.cpp 轻量引擎**（单机推理 + TUI 对话；无 torch，120MB venv 实测）与 **D 档 = PyTorch 分层分布式引擎**（层拆分 / 层流水线 / 多节点；**llama.cpp 不能直接替代现有 PyTorch 层流水线，PyTorch 不抛弃**）——**按设备画像选档**；**控制面**负责调度、集群、节点和能力合同。
- **主交付（重心）**：**TUI 对话与集群控制**——`qlh chat` 使用稳定协议调用本地或远端引擎；`qlh_edge` 提供最小 HTTP。Web/Android 是外部产品壳，不再定义主项目交付。
- **拆分原则**：控制面与数据面仍同仓分层；产品壳、知识库、图像生成、发布和运营工具独立为支线/子模块，Koakumix 接收小模型增强和生图侧车。
- **纪律**：控制面与数据面在**代码组织上分层**（目录/包可辨识、依赖单向：控制面可调用数据面，反之禁止），**不拆仓、不拆 CI**；产品壳与知识库不再占主仓。新增功能准入问句："它服务这套推理系统（引擎或管理器）吗？"

## 2. 归属定案（两部分同仓 + 支线独立）

| 归属 | 层 | 角色 | 当前代码映射（文件级） |
| --- | --- | --- | --- |
| **主仓·数据面** | **QLH Engine（双档）** | **L 档（GGUF/轻量）**：跑模型 + 优化 + 对话面；**D 档（PyTorch/分布式）**：分层分布式推理 | **L 档**：`llama_engine.py` · `qlh_edge.py` · `paged_kv_cache.py` · `cache_unit_layout.py` · `speculative.py` · `tui_chat.py`/`tui_sse.py`/`tui_shared.py`（**TUI 对话**）· 量化工具 · 模型资产面。**D 档**：`model_module.py`（层拆分/张量放置）· `tcp_comm.py`（张量传输）· `qwen3_pipeline_*`/`qwen3_multimodal_*`（流水线/多模态运行时）· `task_worker_protocol.py`/`task_provider.py` · `inference_service/`（流水线宿主）· `schemas/` 引擎契约。**选档 = `device_profiler.py` 画像**（显存/内存/算力 → L/D） |
| **主仓·控制面** | **QLH Control** | 管理器：调度/集群/节点/能力画像/任务租约/降级 | `scheduler.py` · `tcp_comm.py` · `device_profiler.py` · `scheduler_svc_http.py` · `api_server.py` 的核心控制端点 · 任务链协议（管理侧）；邮件通知和运营面只保留合同，按支线票迁移 |
| **主仓·交互/验证** | **QLH TUI + evidence** | 主入口、模型舰队选择、能力显示、推理优化质量门 | `tui_chat.py`/`tui_sse.py`/`tui_shared.py`、模型 manifest/profile、必要实验与回归；演示 UI、图表和环境编排外置 |
| **支线** | **产品壳/侧车** | 把核心组装为可选产品或扩展能力 | Web/Android/packaging、Koakumix harness/生图、Patchouli/RAG、docagent、reasonix bridge、spawnledger、演示/网络/发布工具；具体迁移见支线计划 |

> 注（v2 修正）：首版曾把控制面列为"拆出模块"——**修正为同仓两部分**（控制面/数据面分离是**架构分层**，不是仓库拆分）。
> 注（v3 修正）：数据面为**双引擎**——**PyTorch 不抛弃**（llama.cpp 不能做分层分布式推理）；L 档（GGUF，无 torch）与 D 档（PyTorch）**按设备画像选档**；边缘基线（≤300MB venv）仅约束 **L 档**，D 档的 torch 依赖是分层分布式能力的正当成本。
> 注（v4 重整）：主仓不再以“产品壳唯一拆出物”为边界。主仓只保留推理核心、控制、TUI、模型合同和质量证据；所有非核心功能均按支线计划独立，生图唯一归 Koakumix。

## 3. 支线角色表（全部敲定）

| 支线 | 角色 | 现状/去向 |
| --- | --- | --- |
| **Koakumix**（harness） | 小模型增强对话工作台、可选多模态增强、图像生成和 MCP 侧车 | 独立维护；通过 API/模型合同接入 |
| **docagent** | 文档维护扫描器 | 已独立（子模块） |
| **reasonix-codex-bridge** | Codex↔Reasonix 受控桥 | 已独立（子模块） |
| **spawnledger** | 进程归属票据工具 | 已独立（子模块） |
| **Patchouli** | 文档库管理 TUI（规划） | 独立计划 |
| **SD 生图服务** | 图像生成（引擎远程调用它） | 服务化中（并行线在途） |
| **知识库/RAG 服务** | 文档检索（与 Patchouli 合流） | 已裁决独立 |
| **QLH 产品壳（Web/Android）** | 可选用户界面和端侧载体 | 独立仓；只消费 API/任务/模型合同 |
| **发布与运维工具** | 打包、更新、离线资产、演示、SSH、网络探针 | 独立工具仓；不成为主线运行时依赖 |
| **实验体系（EX-N3/度量）** | 引擎的**质量与优化验证基建**——**留主仓** | 不拆 |

## 4. 分层与接口（同仓两层怎么协作）

```
[主仓·控制面 QLH Control]                [主仓·数据面 QLH Engine]
  调度/集群/节点/控制入口  ──任务(REST/任务链协议)──▶  跑模型（L/D 双引擎+优化）
  设备画像/合同/降级                                    TUI 对话（qlh chat）
        │                                                  │
        └────── 契约：schemas/（推理请求/结果/任务协议）──────┘
[产品壳支线] Web/Android/Koakumix ──▶ 可选用户体验（经版本化合同）
```

- **依赖方向（强纪律）**：控制面 → 数据面**单向**（管理器调用引擎执行任务；**引擎不得 import 控制面**）；同仓不同目录，可用一次性测试断言守住（import 检查）。
- **数据面对外**：`qlh chat`（TUI 对话，主交付）、`qlh_edge`（最小 HTTP）、models 资产面；
- **控制面对外**：cluster/scheduler/节点/审批面（`api_server` 控制侧端点）。

## 5. 实施路径（三轮，聚焦主交付）

| 阶段 | 动作 | 验收 |
| --- | --- | --- |
| **P1 TUI 对话**（主做，立即） | `qlh chat` 打通：TUI 对话接入引擎（复用 `tui_chat`（715 行零第三方）+ llama.cpp 本地直连；或极简新写） | 一条命令本地多轮/流式对话；venv ≤300MB（边缘基线） |
| **P1.5 双档与画像**（v3 新增） | 引擎双档显式化：`device_profiler` 画像 → 推荐/选择 L 档（GGUF）或 D 档（PyTorch）；TUI/`qlh_edge` 状态中显示当前档 | 不同画像设备自动/显式选对档；L 档无 torch、D 档分层分布式路径可用 |
| **P2 同仓分层整理**（可选，按需） | 目录级可辨识（如 `src/engine/` 与 `src/control/` 归档整理）+ import 单向断言测试；`api_server` 控制面端点归位 | 引擎目录零控制面 import；测试守住 |
| **P3 侧车与产品壳外置** | Web/Android/packaging、Koakumix/Patchouli、演示/发布工具按支线计划迁移 | 主仓 = 引擎 + 控制面 + TUI + 契约 + 必要验证 |

> 注：首版的"管理包化/独立仓"路径（P2/P3）**撤销**——控制面留主仓；未来若出现触发条件（独立交付/宿主外使用/CI 冲突），再按候选计划四条件评估。

## 6. 不变式（本次决策不改变的事）

1. Koakumix/docagent/bridge/spawnledger 的既有独立状态与纪律不变；
2. 实验体系（fixtures/experiments/MANIFEST/EX-N3 门）留主仓（引擎质量基建）；
3. 边缘基线（≤300MB venv / ≤15s 冷启动 / 无 torch / TUI 可用）为数据面交付标准；
4. 文档双轨归档（docs/archive、_archive）与"先归档后删"纪律不变；
5. 控制面内部（scheduler 等）不因本决策改动行为；分层整理只动组织不动语义。

## 变更记录
| 日期 | 变更 |
| --- | --- |
| 2026-09-14 | 首版（用户"必须敲定"）：主项目定位=轻量推理引擎；三分（引擎=主仓 / 管理=拆出 / 产品壳=支线） |
| 2026-09-14 | **v2 修正（用户）**：引擎与管理器**同仓同项目**（控制面/数据面分层不拆仓）；**产品壳为唯一拆出物**；**主交付=TUI 对话**；路径改三轮（P1 TUI → P2 同仓分层 → P3 产品壳外置） |
| 2026-09-14 | **v3 补记**：`frontend_cybergothic` **放弃维护**——108 个跟踪文件移出 git（本地保留 135M 含 dist + gitignore）；打包 spec/launcher、`api_server` 静态托管与 4 个测试均读本地路径，继续可用；干净克隆缺前端为预期代价 |
| 2026-09-14 | **v3 修正（用户）**：**PyTorch 不抛弃**——llama.cpp 不能做分层分布式推理，数据面为**双引擎**（L 档 GGUF 轻量 / D 档 PyTorch 分层分布式），**按设备画像选档**；新增路径 P1.5（双档与画像） |
| 2026-09-14 | **v4 重整（用户）**：主线聚焦分布式推理优化并保证 Edge L 档；主交互收敛为 TUI；产品壳、知识库、生图、发布和运维工具独立，生图唯一归 Koakumix；新增主线/支线双计划 |
