# 主线票据 CORE-EDGE-01（2026-09-15）

> 状态：**已完成**
>
> 目标：把 Edge L 档从已有源码基线收敛为可安装、可预检、可回归的最小运行时。

## 交付

- `requirements-edge.txt` 锁定已实测的 `llama-cpp-python`、FastAPI、Uvicorn、psutil、httpx 和 numpy；不复用主环境依赖。
- 根入口 `qlh_edge.py` 支持 `--help`、`--host`、`--port`，并暴露 `qlh_edge:app` 供 Uvicorn 使用。
- `scripts/edge_preflight.py` 只使用标准库，检查 Python 环境体积、导入冷启动、必需模块、禁用模块和最小路由合同。
- `tests/test_qlh_edge.py` 覆盖健康/状态接口、模型缺失错误、GGUF 懒加载、禁用导入门、CLI 帮助和开发环境预检。
- `docs/边缘设备模拟环境计划-2026-09-15.md` 与 README 已改为真实的 Edge 独立安装和验收命令。

## 验收证据

| 检查 | 结果 |
| --- | --- |
| `.venv-edge` 体积 | `112.66 MB / 300 MB` |
| `qlh_edge` 导入冷启动 | `0.2914 s / 15 s` |
| 禁用模块 | `torch`、`transformers`、`accelerate` 等均未安装且未导入 |
| 必需模块与路由 | 全部通过；`/health`、`/status`、`/generate` 存在 |
| 定向回归 | `6 passed` |
| CLI | `qlh_edge.py --help` 退出码 `0` |

## 边界

本票只固化 Edge L 的 GGUF/CPU 最小路径。TUI 在主环境单独运行，模型文件仍由用户选择或下载；
无模型时服务可以启动并返回明确的 `503`，不会把模型存在性变成进入主程序的前置条件。
