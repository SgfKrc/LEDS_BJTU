# QLH 单机工作整理与分布式路线盘点（2026-09-16）

> **背景**：从节点（Surface）暂时不可用，跨机验证无法进行 → 本节把**可在单机推进的工作**整理成清单，
> 并盘点**几条分布式路线的真实实现情况**。
>
> **下一步建议（用户提名）**：**重写 TUI**（P1–P4）。前置条件（耦合度票）已由并行线完成，见 §3。

---

## 1. 分布式路线盘点（五条，按落地程度排序）

| 路线 | 机制 | **实现状态** | 关键代码/证据 | 缺什么 |
| --- | --- | --- | --- | --- |
| **D. PyTorch 层流水线（D 档）** | 层拆分 + tensor 放置 + 流水线运行时 | ✅ **已实现，是现行生产路径** | `qwen3_pipeline_*`（**19 个文件**）、`pipeline_capacity/assignment_manifest/model_descriptor`、`tcp_comm.py` | 真实标定 + 非均匀层分配；多请求流式交叠 |
| **B. 跨框架逐层接力（D→L）** | torch 上游算前 k 层 → hidden → llama.cpp 裁层续算 | ✅ **正确性已打通 + 性能已收口** | `relay_contract.py` / `relay_planner.py` / `relay_transport.py`；`CORE-RELAY-XFRAME-01` | ⚠️ **性能不具优势**（最优 333 ms/步 vs llama.cpp 原生 13.5 ms/步，差 **≈25×**）。⚠️ 口径注（2026-09-19 核）：参照不唯一 —— `13.5 ms/步` 下 **≈25×**（`333 ÷ 13.5 = 24.7`）、`-ngl 24` 的 `26.6 ms/token` 下 **12.5×**；见[跨框架层接力-实验数据汇总](../relay/跨框架层接力-实验数据汇总.md) §F/§H。→ 定位为**能力组合** |
| **C. 层流水线（按需加载）** | 上游只加载自己那段（**不载整模**）+ 下游裁层 GGUF | ✅ **端到端已验证**（64/64 一致） | 本文档 §14；`layer_pipeline_upstream.py` | 单层粒度受架构约束；实用粒度是多层层段 |
| **A. llama.cpp 模型分片 / RPC** | 借用别的机器的 GPU（**借后端**，非集群编排） | ⚠️ **规划中（Silica 式为参考，未落地）** | `llama_rpc_contract.py` / `llama_rpc_planner.py` / `cluster_transport.py`；票 `CORE-LLAMA-PC-RPC-01` / `-SIM-01` / `-ANDROID-01` | 整条链路未跑通；⚠️ RPC 上游定位是"借 GPU"而非"多机合跑一个模型" |
| **E. ExecuTorch（端侧）** | `.pte` 导出 → 真机 → 与 GGUF/JNI 同量化对照 | ⚠️ **实验立项，未执行** | 票 `CORE-ET-EXP-01` | 导出与真机数据（**Android 设备也是单机可做，不依赖 Surface**）|

### 1.1 三条重要关系

1. **B 与 C 是孪生的**：同一套「层切分 + hidden 传递」机制；区别只在**是否按需加载权重**
   （B 的上游仍载整模，C 不载）。**B 的"手动 4 层前向"正是 C 的上游实现。**
2. **C 是 D 的"跨框架版"**：D 档用 PyTorch 做层流水线（已有生产路径），C 把它扩展到
   **一侧 torch + 一侧 llama.cpp**。
3. **A 与 B/C 不冲突但不同层**：A 解决「单机装不下 → 借别的机器」；
   B/C 解决「两侧异构引擎协作」。按既有裁决，**Relay 默认关闭、不替代 RPC**。

### 1.2 ⚠️ 性能上的诚实结论（本轮实测）

跨框架接力（B/C）在**单序列**上**不具备性能优势**，原因是 **batch=1 是 memory-bound**：
上游 torch-GPU **9.2 ms/层** ≈ 下游 llama.cpp-CPU **8.3 ms/层** ——
**决定速度的是"搬运多少字节"，不是"CPU 还是 GPU"**。

**→ B/C 的价值在「能力组合」与「容量」，不在「提速」**（详见 §14 / 性能汇总报告）。

---

## 2. 单机可推进的工作清单

> 全部**不依赖 Surface**，本机（RTX 4060 8GB / 16GB RAM）可独立完成。

### 2.1 交付与工程（优先级高）

| # | 工作 | 为什么现在做 | 依赖 |
| ---: | --- | --- | --- |
| **1** | **TUI 重写 P1–P4** | ✅ 主路径已完成；统一入口、聊天屏、PipelineLayout 观测已在本机验证 | 见 §3 |
| 2 | 补主仓 `llama_engine` 已暴露参数的**调用方接入** | 能力已具备（本日新增 `**llama_kwargs`），缺应用 | 无 |
| 3 | 文档库收口（本轮 7 份新文档已交叉引用） | 避免文档漂移 | 无 |

### 2.2 分布式（单机可做部分）

| # | 工作 | 说明 |
| ---: | --- | --- |
| 4 | **`CORE-LLAMA-SIM-01`**（同机双进程）| **1 个 llama host + 1 个 `ggml-rpc-server` worker**，同机即可验证部分驻留/容量合并/断开回退 |
| 5 | **`CORE-ET-EXP-01`**（ExecuTorch）| 导出 `.pte` **在本机可做**；真机阶段需 Android 设备（非 Surface） |
| 6 | D 档三改进（真实标定 + 非均匀层分配 → 多请求流式交叠 → 会话级 KV 复用）| 全在单机；`pipeline_capacity` 已有基础 |

### 2.3 实验（单机可做）

| # | 工作 | 预期价值 |
| ---: | --- | --- |
| 7 | **长上下文（≥2K）复测全链路** | 长上下文下 KV 与算子权重会变化（WSL2 的 fla 收益正是在长序列才显现）|
| 8 | **投机解码**（`draft_model` 参数已在 0.3.35 暴露）| 唯一未验证的加速方向；接受率是独立前提 |
| 9 | 单层切分的架构约束修复 | 价值较低（多层层段已够用）|

---

## 3. 下一步：TUI 重写（用户提名，本机可全流程验证）

### 3.1 现状（均有实证）

| 项 | 值 |
| --- | --- |
| 源码 | `tui_admin.py` 管理/聊天容器（7 个管理屏 + `ChatScreen`）/ `tui_shared.py` / `tui_splash.py` / `tui_sse.py`；旧 `tui_chat.py` 仅保留 fixture 兼容 |
| **入口** | **统一**：`qlh` 无参与 `qlh chat` 调用 `tui_admin.main()`，本机 supervisor 在同进程启动后端 |
| 聊天页依赖 | **标准库**（复用 `tui_sse` 解析）；Textual 仅存在于旧兼容页 |
| 端点覆盖 | 历史盘点 **41/120**；当前以聊天、分布式布局和边缘依赖边界验收 |

### 3.2 迁移路径（方案已定：`docs/TUI重写方案-2026-09-16.md`）

| 阶段 | 内容 | 状态 |
| --- | --- | --- |
| **P0** | `tui_splash.py`（灰蓝、alt screen）+ `APP_TITLE="Koakuma"` / `APP_SUBTITLE="leds-bjtu"` + `--no-splash` | ✅ **已完成**（TUI 测试 85 passed）|
| **P1** | `qlh`（无参）走一键路径；后端进程内线程启动 | ✅ 已完成 |
| **P2** | 聊天页并入统一 TUI（零依赖重写，复用 `tui_sse`）| ✅ 已完成 |
| **P3** | 分布式核心观测并入现有页；RAG/外置 Runtime/Registry 不回流主仓 | ✅ 核心范围已完成 |
| **P4** | 主路径退出 Textual 依赖；旧 `tui_chat.py` 保留 fixture 兼容 | ⚠️ 兼容收口中 |

### 3.3 为什么前置条件已解除

原计划「P1–P4 搁置等后端耦合度修完」——**该前置已由并行线完成**：

| 票 | commit | 内容 |
| --- | --- | --- |
| `CORE-TRANSPORT-PORT-01` | `59198a8` | 控制面传输端口收口 |
| `CORE-KOAKUMA-ENGINE-01` | `2d788b8` | 引擎合同与 backend 选择 |
| （登记） | `50e0244` | 两票交付登记 |

**→ P1-P3 已完成，P4 仅保留兼容 fixture；下一阶段转入真实模型/活动布局和分布式引擎验收，不再扩张主仓 TUI 页面范围。**

---

## 4. 未验证声明

1. 本文档为**盘点与整理**，未新增实验（所引数据均来自同日各专项文档）；
2. **路线 A（llama.cpp 分片/RPC）的"规划中"状态**依据是代码/票的存在与既有调研结论，
   **未在本轮实际运行验证**；
3. **路线 E（ExecuTorch）**仅为立项状态，**未执行任何导出或真机测试**；
4. **跨机验证完全未做**（从节点不可用）；
5. TUI 不再以覆盖端点总数作为完成指标；当前定向回归与本机后端 supervisor 烟测已见 [TUI重写方案](archive/tui/TUI重写方案-2026-09-16.md)。
