# 主线票据 CORE-TUI-01

> 状态：已完成（2026-09-15）
>
> 归属：QLH 主仓主线，本机主节点

## 交付

- 新增 `qlh.py`、`qlh.bat`、`qlh.sh`，提供跨平台终端入口：`qlh chat`、`qlh admin`、`qlh status`、`qlh models`。
- `/models` 现展示模型舰队、本地可用状态、可用格式、推理引擎、显存预算、当前模型和设备档位。
- 新增 `/model fleet` 和 `/model select <模型ID>`；选择沿用后端 `/models/switch`，保留量化/引擎覆盖和失败回滚。
- 邮件通知全链路裁撤：实现、配置项、生命周期轮询、健康告警、审查通知、邮件投票端点、TUI 菜单动作和专门测试均移出主仓；源文件和专门测试保留在 `_to_delete/migration/`。
- 审查工单保留 TUI/API 查询、投票、过期和 SQLite 持久化能力，不再携带通知状态。

## 验收

- 运行命令：`.venv-test\Scripts\python.exe -m pytest -q tests/test_tui_commands.py tests/test_qlh_cli.py tests/test_review.py tests/test_api_security_boundaries.py`
- 结果：`123 passed`。
- 静态检查：上述变更涉及的 Python 文件 `py_compile` 通过；运行时代码和 TUI 走查脚本不再引用邮件实现，API 测试仅保留“裁撤路由不得复活”的负向守卫。
- 高危文件操作先完成目标路径干跑，再移动至 `_to_delete/migration/`；未直接物理删除。

## 边界

`qlh chat` 是可选 Textual 聊天壳入口；边缘最小环境仍以标准库 `tui_admin --plain` 和后续 `CORE-EDGE-01` 的无 torch 门为准。没有本地模型时，主程序仍可进入 TUI，模型下载/选择由模型舰队接口处理。

## 后续事项

- `SIDE-DOC-01`：迁移后活动文档链接检查仍有 77 条历史/外置材料断链；旧材料已保存在 `_to_delete/docs`，不恢复旧入口，后续按支线计划重写当前链接。
