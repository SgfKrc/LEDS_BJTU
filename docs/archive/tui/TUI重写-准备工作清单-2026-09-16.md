# TUI 重写：准备工作清单（2026-09-16）

> **背景**：用户裁定 **TUI 重写先做准备工作** —— 因为**耦合度修复仍在进行**
> （已到**第三修复票**，后面还有若干），P1 所需的 `api_server` / `model_host` /
> `config` / `scheduler` **正在被并行线修改**，此时动 P1 会冲突。
>
> 本清单的作用：**把不被阻塞的准备项做完**，使 P1–P4 在耦合度解冻后可立即推进。
>
> **关联**：[TUI 重写方案](TUI重写方案-2026-09-16.md)（P0–P4 路径）、
> [单机工作整理与分布式路线盘点](../distributed/单机工作整理与分布式路线盘点-2026-09-16.md)

> **状态更新（2026-09-17）**：本文原定的准备项已转化为实现并完成定向验证。`ChatScreen` 已注册，P1 本机 backend supervisor 已落地，`DistributedScreen` 已接入 `PipelineLayout`。当前定向回归为 **173 passed**；本文后续“待做/未验证”内容保留作历史盘点，现行口径以 [TUI 重写方案](TUI重写方案-2026-09-16.md) 和 [TUI 使用指南](TUI使用指南.md) 为准。

---

## 1. 现状（均有实证）

| 事实 | 证据 |
| --- | --- |
| 一键启动要**两个窗口** | `start_tui.bat`：`start "QLH 后端 API" cmd /k "... python src\api_server.py"` |
| 聊天与管理是**两个 TUI** | `tui_chat.py`（715 行）vs `tui_admin.py`（2913 行）|
| **两个第三方依赖** | `tui_chat.py` 依赖 **Textual**（L15）**与 `httpx`**；缺 Textual 时**静默退出** |
| 入口语义曾不统一 | `qlh` 无参与 `qlh chat` 已统一进入聊天屏；`qlh status/models` 仍保持不自动启动后端的单命令语义 |
| 动画只覆盖一条路径 | `tui_splash.py`（灰蓝 alt screen）仅接在 `tui_admin.py` |
| **测试基线** | `tests/test_tui_{chat,commands,shared,sse}.py` → **139 passed**（10.27s）|

### 1.1 关键发现：`tui_admin.py` 的抽象已足够

```
APP_TITLE / APP_SUBTITLE          （P0 已完成）
ApiClient / AnsiTerm / BaseUI / TermUI / PlainUI
Screen（基类）
  ├─ DashboardScreen   ├─ NodesScreen    ├─ DistributedScreen
  ├─ QueueScreen       ├─ DeviceScreen   ├─ LogsScreen   └─ SettingsScreen
```

**→ P2 只需新增一个 `ChatScreen`，无需改动既有架构。**

### 1.2 关键发现：`tui_sse.py` 已可复用

其 docstring 明确：「该模块**不依赖 httpx/Textual**，可在任意线程或 async 循环中使用」。
→ **SSE 解析器直接复用；P2 只需替换传输层。**

---

## 2. 阻塞边界（本清单的核心）

| # | 准备项 | 是否被耦合度修复阻塞 | 触碰的并行线文件 |
| ---: | --- | --- | --- |
| **1** | **P2：`ChatScreen` 零依赖实现** | ❌ **不阻塞** ✅ | **无**（新增文件 + 只读 `tui_sse`）|
| 2 | 历史端点覆盖盘点（41/120） | ✅ 已完成；不作为当前完成指标 | 无 |
| 3 | 页面注册骨架 / 路由设计 | ✅ 已完成 | `tui_admin.py` / `tui_chat_screen.py` |
| 4 | 测试基线固化 + 回归脚本 | ✅ 已完成 | `tests/test_tui_*.py` |
| 5 | **P1：后端进程内线程化** | ✅ **已完成** | `tui_backend.py`；后端原模块保持不改 |
| 6 | **P4：退役 `tui_chat.py`** | ⚠️ 主路径已完成，fixture 兼容保留 | `tui_chat.py`（自身）|

**并行线正在修改的文件（本清单明确不碰）**：
`src/api_server.py`、`src/config.py`、`src/inference_client.py`、`src/inference_service/routes.py`、
`src/model_host.py`、`src/paged_kv_cache.py`、`src/scheduler.py`、`tests/*`（若干）、`docs/*`（若干）。

---

## 3. 准备项 1：`ChatScreen` 的具体设计（不阻塞，可立即开始）

### 3.1 依赖对比

| 能力 | 现（`tui_chat.py`）| 目标（`ChatScreen`）|
| --- | --- | --- |
| UI 框架 | **Textual** | **stdlib**（复用 `tui_admin` 的 `AnsiTerm` / `Screen`）|
| HTTP 流式 | **httpx** | **`urllib.request`**（流式 `read`）|
| SSE 解析 | `tui_sse.SSEDecoder` | **同一模块（不变）** |

### 3.2 端点契约（只有 3 类）

```
POST /api/chat/stream                        （streaming_mode=interactive）
POST /api/chat/generations/{id}/cancel
GET/POST /api/sessions*
```

事件序列：`start → token* → (done | error | cancelled)`

### 3.3 边界约束（沿用 `tui_chat.py` 的设计边界，不放松）

- 模型输出经 Markdown 渲染，**不执行内容**；ANSI 注入由渲染层过滤；
- **只以 `done` 事件 metrics 展示执行模式，不推断分布式**；
- 不与管理 TUI 的手写 ANSI 主循环冲突（统一后二者同进程，但 `Screen` 之间互斥）。

### 3.4 为什么现在做它是安全的

- **新增文件**（如 `src/tui_chat_screen.py`），**不改任何现有入口**；
- 只**只读**引用 `tui_sse.SSEDecoder` 与 `tui_admin` 的工具函数；
- **不触碰**并行线正在改的 8 个文件；
- 可与 P1 解耦，**独立测试**（可用 `--fixture` 回放，见 `tui_chat.py` 既有的 fixture 模式）。

---

## 4. 准备项 2/3/4：盘点与骨架

| 项 | 产出 |
| --- | --- |
| **端点覆盖盘点** | ✅ **已完成（本次实跑）**，见 §4.1 |
| **页面注册骨架** | ✅ `SCREEN_CLASSES` 数据驱动注册，现有 7 个管理屏 + `ChatScreen` 共 8 屏 |
| **测试基线** | ✅ 聊天、命令、共享协议、后端启动与 CLI 定向回归 **173 passed** |

### 4.1 历史端点覆盖盘点（2026-09-16 实跑结果）

**服务端定义：125 个 `/api/*` 端点**（从 `src/` 提取）；**TUI 覆盖：41 个（32.8%）**。

按域分布：

| 域 | 端点总数 | TUI 已覆盖 | 缺口 |
| --- | ---: | ---: | ---: |
| **cluster** | **51** | 24 | 27 |
| **models** | **21** | 8 | 13 |
| **rag** | **11** | **0** | **11** ← P3 主要目标 |
| logs | 9 | 2 | 7 |
| chat | 5 | 2 | 3 |
| workflows | 4 | 1 | 3 |
| sessions | 4 | 0 | 4 |
| device | 3 | 3 | 0 |
| **users / auth / bootstrap / user** | **7** | **0** | **7** |
| conversations / experimental | 4 | 0 | 4 |
| **合计** | **125** | **41** | **84** |

**未覆盖的主要域**：`rag`（11）、`models` 剩余（13）、`users/auth`（7）、`sessions`（4）。其中 RAG/外置 Runtime/Registry 不再作为主仓 TUI P3 目标。

**⚠️ 与既有文档的差异**：`TUI重写方案-2026-09-16.md` 记「41/120 → P3 后 ~70/120」；
本次实跑得到的分母是 **125**（非 120），覆盖数 **41** 一致 —— 差异来源未核对
（可能是提取正则的覆盖范围不同，或期间新增了端点）。

**覆盖统计口径**：本次用 `grep -rhoE '\.(get|post|put|delete)\("/...' src/tui_*.py` 提取
TUI 侧调用（`ApiClient` 的路径**不含 `/api` 前缀**，`base_url` 已含）；
服务端用 `grep -rhoE '"/api/...' src/` 提取。**两者都可能漏掉动态拼接的路径**。

---

## 5. 明确不做（本轮）

1. P1 已完成，不再受原并行线阻塞；
2. `tui_chat.py` 保留为 `--fixture` 兼容路径，主入口不再依赖它；
3. **不引入任何新第三方依赖**（保持 L 档零依赖边界）；
4. **不改 `/api/*` 契约**。

---

## 6. 历史未验证声明

1. 本文档原为**准备与盘点**；实现状态已由 2026-09-17 更新说明覆盖；
2. **测试基线 139 passed 是历史实跑结果**；而 `TUI重写方案` 中记的「85 passed」为 P0 当时的值，
   两者差异说明**测试在 P0 之后已有增长**（未逐项核对新增了哪些用例）；
3. **端点覆盖 41/120** 是历史盘点数字，不再作为当前 TUI 完成指标；
4. **耦合度修复的进度**（"已到第三修复票"）来自用户口述，**未从代码/票表核实**；
5. `ChatScreen` 的工作量估计已被实际实现与测试结果替代。
