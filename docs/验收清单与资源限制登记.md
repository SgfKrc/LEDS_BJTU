# 验收清单与资源限制登记

> 状态：**现行**
>
> 更新日期：2026-09-21

> 文档生命周期：**现行（Active）**
>
> 创建日期：2026-08-13
> 更新日期：2026-09-16
> 适用范围：登记 QLH 各项待验收/受限能力的状态、所需设备与生命周期；供开发组与外部持有者分配验收任务
>
> 生命周期约定：`规划`（未实施）→ `Active`（实施中/可验）→ `Validating`（部分门过、待完整验收）→ `已验收`（验收完成）→ `Blocked`（受资源/环境硬阻塞）→ `已归档`（不再跟进）

> **D29 当前口径覆盖（2026-09-21）**：本文件下方保留了 D29 的历史流水记录，其中的“生产准入仍 fail-closed”“Windows 只能通过 WSL2 使用 Triton/fla”“21.3 s / 333 ms/步为当前主仓基线”等表述不得再作为现状引用。当前有效数据、引擎口径和待重跑矩阵以[跨框架接力当前有效基线与后续优化计划](跨框架接力-当前有效基线与后续优化计划-2026-09-21.md)为准：正确性证据与速度路由分开；Windows 原生 `triton-windows==3.8.0.post28` 已有本地实测但无 CI 编译保障；qwen3.5 容量扫描的 K=8/16/20 正确性**已于 2026-09-21 重跑，全部 32/32**，且同一公共接口下的双主仓端到端矩阵（两模型 × 切点/负载/batch/混合精度）**27/27 逐 token 一致**（驱动 `build/cross-framework-layer-poc/relay_mainrepo_both.py`，明细见容量收益实测 §9）。

---

## A. 硬件限制类

> 本机基线：16GB RAM / RTX 4060 Laptop 8GB VRAM。**开发组手中无大内存、高显存设备，无 Linux 真机**；下列项目除 A1 外均需外部设备。

| # | 项目 | 所需设备 | 生命周期 | 备注 |
|---|------|---------|---------|------|
| A1 | G4.3.2B Gemma 4 原生绑定图片语义（MTMD handler 初始化 + 图片处理） | 本机（条件性） | **`已验收`（2026-08-13 原生图片语义通过）** | 资源门（RAM 10.80GiB）+ 初始化门（probe `ready_for_image_smoke`）后，**重编 47e1de77 绑定 + 补 MTMD 图像管线**（`llama-cpp-python-mtmd.patch` + `.venv-gemma4-native`），`gemma4_native_image_smoke.py` 实测固定测试图输出语义正确描述（two red apples / weathered wooden surface）。已知小项：输出含 thinking 前缀未剥离（后续可加）。**2026-08-16 独立复验通过**：可用 RAM 9.73GB（>8.74GiB 门），MTMD 初始化 vision=True、图像编码+解码（2273+2036ms）、生成 17 tokens/16.3s，输出与验收语义一致且**无思考段残留**。G4.3.1/2A 均已通过 |
| A2 | DeepSeek-7B 冒烟矩阵（GGUF + Safetensors 两格式） | 大内存/高 VRAM | `Blocked` | 本机 RAM/VRAM 余量不足被正确拒绝（llm_smoke_matrix 资源门） |
| A3 | SD img2img / IP-Adapter 全矩阵质量门 | 8GB+ VRAM CUDA | `已转移` | QLH 主项目已裁撤；历史证据与后续实现归 Koakumix 管理 |
| A4 | 分布式真机：双机四 seed fan-out/fan-in、网格聚合、真实参与节点统计（历史 SD-N4）；异构任务链（L1-2B） | **两台物理 CUDA PC（特定时段可用）** | `部分转移` | 异构任务链仍按任务链专项维护；SD 多 seed/图像聚合不再属于 QLH，历史开发门不代表当前生图支持 |
| A5 | Qwen3-VL-8B 接入 | **硬件阻塞**（内存条待购，暂买不起） | `Blocked`（备注） | **Qwen3-VL-4B 已完成**（工件下载 + Ollama J1 判题实跑）；**8B 跳过单机验收**：留待分布式网络环境直接硬上 8B（多节点合计内存满足 6.1GB+ 标称后），单机内存门不再单独排期 |
| A6 | gemma4 8K/16K 上下文、音频、分布式推理 | 大显存/大内存 | `规划` | 资源门后置；当前仅承诺 2K/4K smoke |

## B. 网络限制类

| # | 项目 | 限制 | 生命周期 | 备注 |
|---|------|------|---------|------|
| B1 | 模型工件下载（HF/ModelScope/Ollama） | 本机外网直连受限，需 7897 代理 | `Active` | gemma4:12b 已通过代理完整拉取并验证；其余下载同路径可行 |
| B2 | 签名源站接入（Launcher 更新源 GitHub + Gitee 镜像） | 外部源站 | **`已验收`（2026-08-16 外部验证通过）** | `DEFAULT_UPDATE_SOURCES` 含 Gitee 镜像兜底 + `fetch_latest` 自动 tag 填充，已由外部实测通过，无需再排期 |
| B3 | 双机纯 IPv6 / 混合网组网（Tailscale） | 第二台真机 + Tailnet（**特定时段可用**） | **`已验收`（2026-08-21 双机 IPv6 实跑通过）** | 本机双栈门已通过；连接前只读门 `cluster_node_preflight.py` 已覆盖 Tailscale daemon/在线态、Tailnet IPv6、明确主节点 API/TCP 端点和 CPU sidecar。**2026-08-21 实跑**：Surface 经 `fd7a:115c:a1e0::…8888` 注册，健康接口与 Windows TCP 表均确认纯 IPv6 连接；工作流 `wf_330d0aa18…` 远端 `candidate_a`/本地 `candidate_b+aggregate` 完成、`distributed_used=true`/`fallback=false`/重派 0，任务后连接仍 IPv6。**2026-08-23 `NET-DUALSTACK-PREF-01` 收口**：显式 `/api/cluster/connect` 成功后持久化首选 endpoint，启动覆盖陈旧 `.env`，自动回退顺序固定「首选 → bootstrap 原地址 → Tailnet」；scheduler `284 passed` + 双栈专项 `36 passed`。`长时/多轮与 IPv4/IPv6-only/双栈安装包矩阵（E2）按双机/IPv6 可用时段排期验收` |
| B4 | Windows/Linux 安装包真实网卡双栈行为 | 真实网络 + 安装包 | `Validating` | 独立分发服务行为与开发环境一致性待验 |
| B5 | 身份重置 SQLite 实机复验 | 已登录 Tailnet 的实机 | `Validating` | 本机 SQLite 身份重置与地址筛选已实现 |
| B6 | 更新器 GitHub release 下载链路 | 外网 | **`已验收`（2026-08-13 外部验证通过）** | 已由外部人员实机验证可下载通过，无需再排期 |

## C. 待核验验收项（无硬性资源障碍，但未走完验收）

| # | 项目 | 验收方式 | 生命周期 | 备注 |
|---|------|---------|---------|------|
| C1 | Linux 真机全家桶：`.deb` 构建/安装/运行、`qlh-launcher` 三模式、`env-register` Linux 脚本、Linux 双栈 | Linux 真机 | `Validating` | 构建脚本与安装接线完成，真机证据待设备 |
| C2 | T9.6-R2-EXT 外部干净 Windows/Linux、新 shell、真实模型会话门 | 外部干净机器 | `Validating` | 本机开发门过（TUI 走查 54/54）；外机会话待验 |
| C3 | EX-N3 质量总 gate 实跑（`quality.required=true`） | 本机即可 | **`已验收`（历史记录；当前收敛为文本与多模态理解）** | QLH 当前质量门不再包含 SD；历史 SD/Gemma 记录只保留追溯，不作为主项目生图能力 |
| C4 | 下一签名 Launcher 包外机复验 | 外部机器 | `Validating` | 等签名包下载进度 |
| C5 | Android Edge 本地推理真机会话（SAF 模型目录 + llama.cpp 原生） | Android 真机 | **`已验收`（2026-08-13 真机多轮实测）** | Edge runtime Release APK 构建/安装 ✓；SAF 外部目录选择/扫描/加载（含 `/proc/self/fd` 与回退缓存副本）✓；本地推理有效回答、断网可用、本地/集群切换与 StatusChip 显示 ✓；卸载保留/重装复用（MVP 标准①②）✓；失败路径明确提示不崩溃 ✓；暗色模式与长消息气泡目视 ✓。核验清单见验收清单复核讨论，全部通过 |

## D. 本机可立即验收（无限制）

| # | 项目 | 生命周期 | 备注 |
|---|------|---------|------|
| D1 | Python 全量测试（**2026-09-24 复跑：`3555 passed / 25 skipped`，`-n 4`、292.6 s、`0 failed`**；此前快照 3032/19 @ 2026-08-20 ⇒ **+523**）、control/gateway/frontend/Android 套件 | `Active` | 随时可跑（★ R-R10 复跑；注意此时并行组有 9 个在制品在工作区，全量仍 0 failed） |
| D2 | EX-N3 标定证据：多模态理解判题与历史 SD 记录 | `已验收` | 当前仅维护文本/多模态理解；SD 十种子门与双人目视是 Koakumix/历史证据，不再属于 QLH 主项目 |
| D3 | Launcher/Setup 构建、Ed25519 签名、清单校验（manifest 5015 文件） | `Active` | 0.1.8.2 全套已构建 |
| D4 | 仿真套件（容量/任务链/worker harness） | `Active` ⚠️ | ★ **R-R10 复跑：8 场景 `0/8` 通过（请求级 `3/27`）** —— 根因是 `tests/simulation/framework.py:34` 默认 **`model = "qwen-1.8b"`**（**已退役模型，本机无此工件**）⇒ 后端起不来 ⇒ 每条请求 **60 s 超时**；`stress_test` 更是 `0.0s` 立即失败。**不是环境慢**（harness 自述 `real_model_loaded: False`）。**顺带修**：`tests/run_simulation_tests.py` 缺 UTF-8 容错 ⇒ 打印 `✗` 抛 `UnicodeEncodeError` **中断整套仿真**（修前只跑完 1 个场景，修后 8 个全部跑完）。**待办**：把默认模型改为现行档（属配置决策） |
| D5 | 多模态判题器（Ollama gemma4 本机实跑） | `Active` ⚠️ | ★ **R-R10 核实**：Ollama 服务在跑（`/api/tags` → `200`）且 `gemma4:12b`（7.6 GB）在位；但 `scripts/experiment_gemma_judge_real.py` 的 `--prompts` 要求的是**图片清单**（每项含 `image` + `prompt` + `key_elements`，逐图判题），而仓库现有的 `fixtures/prompt_sets/*.jsonl` 是**纯文本** prompt 集（无 `image`）⇒ **缺判题图片集**（属需人审的基线工件，同 D15）。**本轮未复跑** |
| D6 | A1 条件性验收：空闲时腾出 RAM 后初始化 MTMD handler + 图片语义 | **`已验收`（见 A1，2026-08-13）** | ★ **R-R10 核实**：A1 本体在 A 段已标 **`已验收`（2026-08-13 原生图片语义通过）**，故本行不再挂 `Active`；复验仍受 A1 的资源门约束（空闲 RAM 10.80 GiB + probe `ready_for_image_*` 初始化门） |
| D7 | 投机解码本机门（SPC-CS 置信度调度 / SPC-MEDS 半自回归 / SPC-REV 逆向回退） | `Active` | 专项定向 `107 passed`（含 SPC-* + TG-OPT + RAG 后置），分布等价与 static=None 语义已验证；真模型加速收益后置 |
| D8 | TG-OPT-G5.3 影子级动态故障注入 + 复验闭环（同 seed digest 可复现、影子恒只读） | `已归档`（不再跟进；重启需路线决策） | 本机影子级复验专项 `10 passed`，代码与测试保留。**真实双 Worker 竞速 / 唯一提交 / 取消 loser / 尾延迟收益**（同一 Stage 由 `local_full_model` × `remote_full_worker` 并发、`single_winner` 仲裁）的卡点**不是设备**（2026-09-23 核实）：① 任务图线的调研/实施文档已**全部归档**（现行 `docs/*.md` 零命中 `任务图` / `task_graph` / `TG-OPT`）；② `src/task_graph_speculation.py` / `task_graph_shadow.py` **无产品路径消费者**（引用者仅自身 + 测试）⇒ 从未接线；③ 主线计划 `CORE-TASK-FALLBACK-01`（原 `CORE-POOL-01`）已把「任务图 / 整请求分发」定性为**临时迁就回退 + 低优先级**，并**禁止扩展为模型分片替代物**。⇒ **双机窗口不解锁本项** |
| D9 | RAG 后置可准备：provider 长时 soak harness（确定性 digest + 注入故障）+ sqlite-vec ANN 决策基准（默认 FTS5/有界 cosine，未装 sqlite-vec 为 `NO_GO`） | `Active` | ★ **R-R10 复跑（2026-09-24）**：`scripts/provider_soak_harness.py` —— **干净档** `--iterations 40`（无注入）⇒ `ok=40 fail=0 timeout=0`、`exit=0`；**注入档** `--failure-rate 0.2 --hang-rate 0.1 --seed 7` ⇒ `ok=26 fail=8 timeout=6 recovered=8`、`exit=0`（注入的失败/挂起**全部被识别并恢复**，无泄漏）。专项 `test_provider_soak` 等 **27 passed**。真实规模 soak 与多源真实质量仍后置 |
| D10 | 瘦身安装包（外部运行时依赖）：`qlh-slim.spec` 构建 + 首次启动 pip 引导（CPU 走 PyTorch CPU index / CUDA 官方源，`_internal` 下源码跑 uvicorn） | `Active` | 单元/静态验证通过（64 项专项；含 launch/proxy/probe）；**真 PyInstaller 构建 + 首启引导实跑待打包环境** |
| D11 | 模型一键下载与搜索（P0a 预设下载 job + P0b cybergothic 页面 + P1a HF/ModelScope 搜索）：`model_download_jobs.py` job 状态机/SQLite/资源门/SHA 校验，`GET /api/models/presets|downloads|search`；ModelDownloadsPage + e2e | `Active` | service 8 + API 6 + 搜索双组测试通过、前端 typecheck/build 通过；**真实公网下载→校验→注册→冒烟后置**（预设 hf_repo/SHA 待联机核实） |
| D12 | 接口契约对齐审计工具（FE-AUD-01/02）：`api_gap_scan.py` canonical（仅 cybergothic）默认 + `--include-legacy-frontend` 对照 + `--ownership` 归属表（qlh.api_ownership.v1） | `Active` ⚠️ | ★ **R-R10 复跑（2026-09-24）**：`backend=97 / frontend=247` ⇒ **缺口 A=118、缺口 B=88**（`exit=3`），与原先记录的「canonical 214 / legacy 161、**缺口 A=0**」**不符**。**两个口径陷阱**：① 必须带 `--include-gateway --include-control`，否则 `/api/auth/*` 等 control 端点**全部被误判成缺口 A**（实测 118 条假缺口）；② `CORE_ROOT` 是**单点**解析 `REPO_ROOT.parent/"qlh"`（"外置壳与主仓平级"的布局）⇒ **在主仓内直接跑会指到 `<repo>/qlh`（不存在）⇒ 后端扫成 0 ⇒ 每条前端调用都成"缺口 A"**；当前须显式 `QLH_CORE_ROOT=<repo>`（更长久的修法是改多点探索）。缺口是否真实存在**待裁定**。专项 `15 passed`；动态调用与运行时 404 仍属运行时审计 |
| D13 | Android presence 租约 + Full Worker 客户端全链路（`AndroidPresenceStateMachine`/`AndroidPresenceService`/`AndroidFullWorkerStageExecutor`/`AndroidWorkerCapabilities` + JNI + 后端 scheduler/api_server 配套） | `Active` | Kotlin 单测 + scheduler/task_worker 回归通过；**真实主节点/Android 真机与 Full Worker offer→result 后置** |
| D14 | Android 控制面 `AND-CTRL-01..05`（只读概览/舰队/审计/账户会话 + 受限管理入口）与前置契约 ②③④（管理授权矩阵/审计/一次性确认令牌） | `Active` | control 38 suites/326、gateway 9 suites/121、Android `ApiClientContractTest` 42 用例全绿；confirm token 仅在请求内流转；**真实 control/gateway、真机联调与审计落盘后置** |
| D15 | 文档质量/实验质量/资产工具：docagent 检索·语义质量门、实验质量生产门（`production_quality`）、离线资产一键整合包（`build_offline_bundle`） | `Active` ⚠️ | ★ **R-R10 复跑（2026-09-24）**：① **实验质量生产门 + docagent 专项测试 `27 passed`**；② `build_offline_bundle.py --all --preflight` **跑通并给出容量需求** —— pc payload **102.7 GiB** / android **10.0 GiB**，而本机 **G: 12.8 GiB、C: 72.0 GiB** ⇒ **磁盘不足**（属资源限制，见 C 段）；③ 两个 docagent 门（`semantic_gate` / `retrieval_quality_gate`）用仓库现有 fixture 实跑均为 **`invalid`**：`sample_not_found:qwen38-partial-plan` / `target_not_found:cluster-rag-boundary` ⇒ **fixture baseline 指向的文档已不在文档库**（fixture 名即 "reviewed baseline"）⇒ 需文档维护方重审后更新，**不能靠改门禁绕过** |
| D16 | `CY-PKG-01` 外置产品壳/发布包维护：`qlh-shell` 与 `qlh-release` 独立消费主仓 API/合同 | `支线` | 主仓 API 不再挂载 `frontend_cybergothic/dist`；外置打包、WebView2、Linux `.deb` 和安装包首启由支线自行验收 |
| D17 | `CORE-CUTOVER-01` / `SIM-L0`：功能裁切/重写后的入口、TUI/API/模型合同连通性与主仓/外置仓低耦合 | `已完成（2026-09-15）` | 主 API 不再挂载外置 Web；生图、邮件和工具侧车不回流；无 torch Edge 入口、能力合同和原生 RPC worker 生命周期面可启动；定向回归 `28 passed`，Edge 预检通过 |
| D18 | `CORE-LLAMA-SINGLE-01` / `SIM-L1`：Edge/PC llama.cpp/GGUF 单机真实模型基线 | `已完成（2026-09-15）` | Qwen2.5-0.5B/Qwen3-0.6B/MiniCPM4-0.5B 真实 llama.cpp 加载、完整/流式非空输出；Qwen3 `thinking_controlled=true`；Edge 无 torch 预检通过；单机基线命令记录 RSS/冷启动/吞吐。DS3-7B 性能实测因本机内存不足后置，不宣称已完成 |
| D19 | `CORE-LLAMA-SIM-01` / `SIM-L2`：Edge/PC 同机 llama host + `ggml-rpc-server` worker 双进程模型分片 | `已完成（2026-09-15，受控容量门）` | DS3-7B `12/29` 层、CPU/RPC buffer 分离、受控 worker budget `2048MiB` 下 backend 驻留合并 `3853.60MiB`、请求/断开/CPU 回退闭环；已排除 worker 整模复制。物理单进程 OOM、跨机与生产 RPC 仍后置 |
| D20 | `CORE-LLAMA-PC-RPC-01`：Surface 从节点 CPU worker 的 PC 真机 RPC 与 SSH 隧道对照（历史 Qwen1.8B 基线） | `已完成（2026-09-16，历史证据）` | 本机/远端 Qwen1.8B SHA256 一致；画像驱动自动选 `1/25` 层，SSH 隧道 + 远端 CPU worker 返回 `Okay`，RPC0 驻留 243.44MiB、远端 RSS 250.71MiB，断线后本地回退成功，旧 lease 提交被 epoch fencing 拒绝。最终实测分布式请求 657ms、CPU 回退 313ms，当前不宣称吞吐收益；该模型已退役，不得作为当前模型或硬件对照基线，直连长时与更大模型容量收益后置 |
| D21 | `CORE-RELAY-01`：L→L 裁层 GGUF + hidden/`embd` 逐 token 接力架构验证与主仓合同 | `已完成（2026-09-16；生产接线后置）` | Qwen3.5-2B 同机单进程，整模型 layer 0..3 输出注入裁层模型，贪心生成 `16/16` 步一致，三次运行未分叉/未 OOM；主仓合同区分逻辑模型摘要与裁层 artifact 摘要，强制裁层 block_count/MTP 布局，探针拒绝短/伪造 token 序列并可实测单机回退。**原先记录的"`embd` 路径进程内 bitwise 非确定"已查明为上游 #28963 的调用方 `pos` 越界读（见 D22 与《上游llama-embd注入非确定性》§13）；修复后 141 步全一致**。D→L、跨进程/网络、长序列和采样仍归 `CORE-RELAY-XFRAME-01`，不进入 Edge 默认路径 |
| D22 | `CORE-RELAY-XFRAME-01`：D→L/跨边界 Relay 的实验准入与失败回退门 | `实验轨道已收口（2026-09-16）—— 根因定案为上游 #28963 的 `pos` 越界读且修复已验证；五条 D→L 等价证据齐备；**2026-09-21 准入判定改为「正确性已验证即准入」**（用户裁定 `dec-6d91cd100fecc798`：容量性能同样算性能，D→L 的独有优势是「单机装不下的模型可运行」）；容量收益实测已完成（两段 1.547–1.568×，最优 `K=N/2`）；收口结论见《[跨框架层接力重启评估](archive/relay/跨框架层接力重启评估-2026-09-15.md)》§7.13` | `RelayXFrameRequest` / `admit_relay_xframe` 与 `scripts/llama_relay_xframe_probe.py` 已落地（含 `tolerant_verdict` 诊断档）：PC 显式 loopback/SSH 隧道才进入实验范围；跨引擎、公共网络、非法长度、NaN/非零采样参数 fail-closed，统一声明单进程 llama.cpp 回退。**根因（上游给出 + 本仓库独立验证）**：`llama_batch_allocr::ubatch_add` 在 **M-RoPE 模型 + `embd` 批次**时读 `batch.pos[0 .. n_pos_per_embd*n_tokens)`，而 `llama_batch_init` 只分配 `n_tokens` 个 `pos` → **调用方 `pos` 数组的堆越界读**（上游 valgrind 实证；与 `pos == NULL` 的 #28902/#28910 孪生）。**调用方修法**：`pos` 按 `n_pos_per_embd * n_tokens` 分配、每段填 `pos[j*n+i]=i`（Qwen3.5 为 4 段）。**修复后实测（Windows / MSYS2 UCRT64 / Qwen3.5-2B f16 / CPU）**：`embd` 同输入连跑两次 `126/141` → **`141/141`（cosine min `1.000000`）**；`embd` vs token 路径 `132/141` → **`141/141`**；`llama-relay-gen` L→L 141 步（修复前第 46/51 步分叉）→ **`141/141`（2/2）**。**因此此前"固有非确定性 / 长序列拒绝 / 16 步边界 / top-1↔top-2 判据不可达"的结论全部作废**；`top_k_tolerant_argmax` 降为**诊断档**，不作准入依据。**正式 D→L 证据（2026-09-16 修复后重跑，五条）**：① **prefill**（同机跨进程文件交接）—— 官方 probe 判定 **`evidence_accepted`**，严格 `per_token_argmax` **`141/141`**、`cosine_min 0.99999954`、B 侧连跑两次一致；② **逐步生成**（同机 stdio）—— `llama-relay-gen-dl` + `drive_dl_relay.py`，**141 步逐 token 完全一致**；③ **采样矩阵** —— `T=0` 确定（141/141）；`T>0` 两侧各自采样必然第 2 步分叉，且"共享 seed"非充分条件（`top_p=1` 同 seed 仍 30/141），只有截尾 `top_p<1` 后才一致 → 约束实质是"**禁止两侧各自采样**"；④ **f32 权重对照** —— 整模型/裁层均 f32，**`141/141`**、`cosine_min 0.999999`（7.8 GB mmap，实测内存无压力）；⑤ **跨机（SSH 隧道）逐步生成** —— A=PyTorch 在**本机**、B=裁层 llama.cpp 在 **Surface 从节点**，经 `scripts/llama_relay_tcp_bridge.py`（远端 loopback 监听）+ `ssh -L` 隧道 + `RelayTcpClient`（强制 loopback-only）+ `drive_dl_relay_tcp.py`，**141 步逐 token 完全一致**（`RESULT: 全部 141 步一致`）。报告：`local_docs/CORE-RELAY-XFRAME-01-dl-{fixed-prefill,stepwise,sampling-matrix,ssh-tunnel}-*-2026-09-16.json`。**仍待完成**：长时运行、n 段外推验证、不可分层张量缩减、远端资产分发自动化 |

| D23 | `CORE-TRANSPORT-PORT-01`：控制面与 TCP 传输实现的低耦合端口 | `已完成（2026-09-16）` | `src/transport_port.py` 提供服务器/客户端、协议枚举、张量编解码、集群配置和本机网络信息的适配入口；`scheduler.py` 不再直接导入 `tcp_comm`、构造 `TCPServer` 或调用旧张量编解码器；静态边界断言与动态替身回归已补齐。 |
| D24 | `CORE-KOAKUMA-ENGINE-01`：Koakuma backend 合同、注册表、节点画像选择与能力探测 | `已完成（2026-09-17）` | CUDA 画像默认 PyTorch、无 CUDA 默认 llama.cpp、显式 llama.cpp 可退化；`scheduler.py`、`api_server.py`、`inference_service/engine_host.py` 不再读取 `_engine_type`，层拆分通过 `FORWARD_LAYERS` 能力探测。定向回归 `592 passed, 1 skipped`，动态配置修复后复核 `309 passed, 1 skipped`。 |
| D25 | `CORE-NO-TORCH-GUARD-01`：L-tier 无 torch 硬门与分页 KV 惰性依赖 | `已完成（2026-09-17）` | `paged_kv_cache.py` 移除顶层 torch 依赖；默认 `llama_cpp` 配置不导入 torch 进行设备探测，显式 `pytorch`/`auto` 保留探测。无 torch 阻断子进程完成 API 冷启动、假 GGUF 单轮推理和 `tui_admin.main(--plain)` 启动（该用例随后随旧 TUI 归档改由只读薄层承担：`tui_commands.main(["help"])`，2026-09-17）；真实安装 torch 的默认 GGUF 导入检查确认 `sys.modules` 不含 torch。定向回归 `7 + 22 + 5 passed`。 |
| D26 | `CORE-HIDDEN-CALLBACK-01`：调度隐藏回调显式化 | `已完成（2026-09-17）` | `SchedulerCallbackSet` 原子注入 API composition root 的 7 项依赖，`ModelHost`/`Scheduler`/远程 `InferenceClient` 都通过公开、类型化 bundle 消费；`_ATTACHED_ATTRS`、`get_attachment`、`.attach(` 与 scheduler 的 `self._host._` 均已清零。乱序装配、本地 task-worker 与 scheduler-svc 回归 `53 + 7 passed`。 |
| D27 | `CORE-DATAPLANE-NO-WRITEBACK-01`：数据面不得反写控制面对象 | `已完成（2026-09-17）` | `tcp_comm.py` 不再反射 callback owner 或写回调度器字段；消息/断连/注册确认事件在 I/O 线程启动前显式安装。调度器以线程局部 source context 绑定实际接收连接的 server，双 `TCPServer` 并发无串台，默认 server 不被回调改写；`callback_owner` 静态命中为 0。回归 `103 + 285 + 107 + 108 passed`。 |
| D28 | `CORE-LAYER-CONTRACT-01`：层段统一合同与顶层资源透明性 | `已完成（2026-09-17）` | `PipelineNode`/容量/工件/布局合同统一容量计划、assignment manifest、Relay handoff 和 RPC lease；半开区间必须完整覆盖、不重叠、不空洞，embedding/LM Head、模型摘要、架构和 GGUF block/MTP 几何全部 fail-closed。已准入容量计划附 `pipeline_layout`；只读 `/api/cluster/resources` 与 TUI `/nodes` 仅投影在线集群 CPU/RAM/GPU 统计，不含地址或本地路径。回归 `39 + 115 + 286 passed`。 |
| D29 | `CORE-RELAY-XFRAME-02`：跨框架接力性能优化（切点重搜 / 下游加速 / 批量交叠 / 混合精度 / 加载与算子） | `进行中（2026-09-21 更新：P0 双主仓端到端矩阵 27/27 通过 + qwen3.5 四切点重跑 32/32）—— 生产准入按「正确性已验证即准入」，速度只影响默认路由倾向` | **现状基线**（gen=64，逐 token 一致）：累进 182 s → **21.3 s（8.5×，同进程 + 双侧 KV + 上游手动 4L ≈ 333 ms/步）**，对照 llama.cpp 原生整模 GPU 约 **13.5 ms/步**，**仍慢约 25×**（`333 ÷ 13.5 = 24.7`；⚠️ 参照不唯一，`-ngl 24` 的 `26.6 ms/token` 口径下为 12.5×）；五段剖析 **99.3% 是两侧实际计算**（上游 66.5% / 下游 32.9%），通信与同步仅 **0.34%**。**优化项（按优先级；每项均需实测才能改口径）**：**P0 切点重搜**——现为固定 4 层（`L0-3` 仅占权重 10.1%，`L4-23` 占 50.3%），应按两侧实测单位层耗时求解最优切点；判据：给定设备对，搜索使 64 步墙钟最小的切点并落可复算报告。**P1 下游 GPU**——`--gpu-layers` 已实测 2.18×，但同进程叠加阻塞于 **CUDA 版 `llama-cpp-python`（未构建）**；判据：同进程 + KV + 最优切点下 offload 后墙钟下降且仍 64/64。**P2 批量交叠 / 批量验证**——把单序列延迟转为吞吐（上游跑 batch B 时下游跑 batch A）；投机解码门槛为"批量验证 k token < k × 单 token"，llama.cpp prefill 分块实测 3.3× 说明前提可能成立，但需过接受率关。**P3 混合精度与量化误差累积**——上游 f16 + 下游 Q4_K_M，**业界无公开测量**；判据：给误差累积曲线与 argmax 一致率。**P4 加载开销 ⇒ ✅ 已完成（复核 2026-09-19）**——该修法**当时已落地**（`同进程双后端接力实现与性能-2026-09-16.md:658` 已写「避免随机初始化 737M 参数；初版不用 meta 时加载要 29 s」；代码侧 `model_module.py:1879` 与 `qwen3_pipeline_adapter.py:211/293` 均已 `init_empty_weights()` + `from_config`）。**复核实测**：主仓 `load_layer_range`（qwen3-5-2b/24 层）**3.59 s** vs **不用 meta 的对照 27.36 s**（慢 **7.63×**）⇒ **已避免随机初始化**；参数与 ckpt 数值等价（`max_abs_diff 2.98e-08`、abs mean 完全相同；**非逐位相同**仅因 ckpt 是 **BF16** 而主仓按设计载成 **FP16**）。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-p4-and-e2e-compile-2026-09-19.json`。**★ 端到端编译收益（新，2026-09-19）**：主仓 `forward_layers` 开 `USE_MONOLITHIC_FORWARD`（编译 `_LayerLoop`）在 **qwen3-5-2b（hybrid）24 层 / gen=64** 上实测 **51.259 → 19.002 ms/step（2.697×）**，**逐 token 序列与 eager 完全一致**（sha `103db8e2e023f283`），且**方差大幅下降**（std 3.6 → 0.05）；口径 = 上游 torch 段、同进程、不含 llama.cpp 下游（每配置独立子进程 ×3，因同进程重建会显存累积 OOM）。⚠️ 与 B14 旧值（hybrid 1.269×）**口径不同不可直接比较**。**P5 上游算子**——fla/triton 在 Windows 无构建（WSL2 已通）；判据：上游固定最优 kernel 配置后无回退告警。**明确不做（已被数据否定）**：减少同步次数、复用 `llama_batch`、朴素截断上游层、把 relay 逻辑下沉 C++ 以减少 Python per-step 参与（合计 0.34%）。**★ D→L 端到端 + 上游编译层循环（2026-09-19）**：主仓上游（12 层）+ 裁层 GGUF 下游（12 层）真接力跑通，合计 **≈28.1 ms/step**（上游 13.9 / 下游 14.3，两侧基本打平），**6/6 次 token 均与纯 llama.cpp 整模一致**。⚠️ **否定结论**：上游开 `USE_MONOLITHIC_FORWARD` 后**反而慢约 6×**（84.2 vs 13.9 ms/step）——与 2B/24 层上实测的 **2.70× 快**直接矛盾 ⇒ **编译层循环收益强依赖模型规模**，**「2.70× 可直接搬到真实链路」不成立**，须按规模分别验证。**副发现（用户提醒核实）**：主仓**本就同时有 PyTorch 与 GGUF 两引擎**，上游走 `model_module` **已有**；但下游 `llama_engine` **没有「从 hidden 续算」入口**（其 `embd` 注入仅见于 `chat_image` 的 MTMD）⇒ 与 **D21「生产接线后置」**吻合 ⇒ **本实验下游不得不用裸 `llama_cpp`，已显式记录该偏离**；建议登记「给 `llama_engine` 加层段续算接口」。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-relay-dl-monolithic-2026-09-19.json`。**★ 主仓优化开关对齐 + 编译规模门（2026-09-19）**：① **显式 `attn_implementation="sdpa"`**（P6 实测 f16+sdpa 1.665 ms/层为最佳，而此前**全仓无此参数**、依赖库默认）—— 加在 `from_config`（2 处）与 `load_kwargs`（1 处）；② **编译规模门 `COMPILE_MIN_PARAMS = 1.5e9`**（用户裁定）：`_maybe_apply_compile()` 现在按 config **估算整模参数量**（`layers×(4h²+3hi)`，**不能用 `sum(p.numel())`** —— 层拆分只物化部分层会误判），低于 1.5B 直接跳过并告警。依据：0.5B/12 层开编译层循环**反而慢约 6×**，而 2B/24 层**快 2.698×**；1B 以内即便真能优化也碰到边际效应。**验证**：`tests/test_model_module.py` **123 passed**。**★ 主仓接力接线（下游）⇒ ✅ 已完成（2026-09-19）**：`llama_engine` 新增 `LlamaCppEngine.forward_layers_from_hidden(hidden, n_past)` —— 把上游 hidden 注入 `llama_batch.embd`，只跑本模型（**裁层 GGUF**）的层并返回末位 logits；含 **n_embd 宽度校验**。⇒ **「上下游都走主仓引擎」成立**（上游 `model_module.forward_layers` + 下游本方法）。**实测确认取法**（此前卡点，记下来避免重踩）：① `LlamaCppEngine.is_loaded` 是 **bool 属性、不是方法**；② 原生 handle：`llm._model` 是 `LlamaModel` **包装**、**不能**直接喂 `llama_*` C 函数（报 `ctypes.ArgumentError: wrong type`）——原生 `llama_model_p` 是它的 **`.model`**；原生 context 是 `llm._ctx.ctx`。③ 参考 `build/cross-framework-layer-poc/relay_mainrepo_upstream.py:171-179`。**验证**（`out/qwen25-05b-f16-cut-k12.gguf`，12 层裁层）：`n_embd=896 n_vocab=151936` ⇒ 返回 `logits shape=(151936,) float32 finite=True`；宽度不匹配被拒绝；`pytest -k llama` **87 passed / 1 skipped**。另：**实验用 llama 引擎基本是最优配置**、也是主仓 PyTorch 要追赶的对象；接下来再考虑**交换上下游**（llama 上游 + torch 下游）等既有待办。**★ 交换上下游实验（L→D vs D→L，2026-09-19）**：两端均走主仓（上游/下游分别用 `ModelManager` 与 `LlamaCppEngine`），0.5B/切点 12/gen=32。**D→L 数值正确**（token 与纯 llama 整模一致）。⚠️ **L→D 数值未对齐 ⇒ 其性能数字不得引用**：跑得通且测得 **35.342 vs D→L 47.501 ms/step（0.744×，快 25.6%）**，但 **token 与整模不一致**。直接比对两个「上游 hidden」得 **形状一致、内容完全不同**（`max|diff|=1641.85`、**cosine=-0.005258**、尺度差 **3.8×**）⇒ **不是精度误差，是「第 N 层输出」的定义不同**。**下一步**：把 llama 的 embeddings 与 torch 的 第 1..N 层 hidden **逐一比对**，定位它实际对应哪层/差在哪个算子，再决定形态。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-swap-ld-2026-09-19.json`。**★ 交换上下游实验结果 ⇒ 形态结论：D→L（2026-09-19）**：L→D **跑得通且看似更快**（32.629 vs D→L 40.071 ms/step = 0.814×），但 **token 与整模不一致 ⇒ 该数字不可用**。**根因闭环**（四步）：① 比两个「上游 hidden」得 **cosine=-0.005258**；② 查裁层方向 —— `cut_layers_generic.py` 的 `--k` 实为「**丢前 K**」⇒ `cut-k12` 是 **后 12 层**（已加 `--keep-head` 并生成正确工件，但换用后**仍不一致**）；③ **决定性**：用**完整整模**排除裁层变量后，llama 的 embeddings **只与「跑完全部 24 层」相似**（cosine **+0.9225**），1/4/8/12/16/20 层全部 ≈0.05~0.09 ⇒ **`llama_get_embeddings_ith` 取的是「最后一层之后的输出」，不是「第 k 层 raw hidden」**；④ 差值未到 1.0 的可能原因：`head12` 保留了**原 24 层模型的 `output_norm.weight`**，而 torch `has_lm_head=False` 返回**未过 norm** 的 raw hidden（**待验证**）。**⇒ 结论：形态应为 D→L** —— torch 侧天然支持「出/吃 hidden」，而 **llama.cpp Python 绑定（开箱）只暴露最终输出**（无「取第 k 层 hidden」API），故 **llama 难以当上游**。⚠️ **范围修正（2026-09-19 复查）**：① **pooling 假设已排除** —— 对照实测 `UNSPECIFIED(-1)` 与 `NONE(0)` **逐位相同**（cos=0.470030）、`MEAN(1)` 才池化（全 0）⇒ 不是「默认池化」污染；② **历史存在反例** —— D29 载「llama.cpp 完整模型 **layer 3 hidden** 与 PyTorch hidden **cosine 1.0**、max|Δ|≈2.44e-4」⇒ 曾取到过干净中间层 hidden（来源票面未载；`CORE-HIDDEN-CALLBACK-01` 经查是**调度器回调**，D26，与取 hidden 无关）；③ **本地有 `llama.cpp` 源码副本**且**已知存在本地补丁**（显式 pos 补丁必须保留）⇒ **改 C++ 源码的路是通的**。⇒ 准确说法是「**当前绑定开箱只暴露最终输出**」，**不是「任何路径都取不到」**；自制路径的真实成本是**维护一份 fork**（含量化 kernel 语义一致性），而非不可能。（L→D 的 0.814× 若将来能解决取 hidden 问题值得重测。）报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-swap-ld-rootcause-2026-09-19.json`。**★ 澄清：llama.cpp 在中间层 hidden 上「读写不对称」（2026-09-19 用户追问）** —— 文档长期记「llama.cpp 无公开中间层 hidden **读写** API（`#27112`）」，但 **D→L 却通了**，看似矛盾。**真实情况是单向可用**：**写（注入）公开** —— `llama_batch.embd` 是公开字段、`llama_decode` 直接接受 ⇒ **下游**（llama 当第二段）可用 ⇒ **这正是 D→L 成立的原因**；**读（取出中间层）没有** —— 只暴露最终输出（`get_embeddings` 实测只与「跑完全部层」相似 ≈0.92）⇒ **上游**（llama 当第一段）不可用 ⇒ **L→D 卡在这**。**⇒ 引用时请写明「上游能力缺失、下游能力具备」，不要再用「无公开中间层读写 API」这种把两方向混起来的表述。** **能否自制**：技术可行（GGUF/量化格式公开），但**要重实现前向** ⇒ **失去 llama.cpp 的 kernel 优势**（1.064 vs torch 1.665 ms/层）⇒ 最多落到 torch 级，**那不如直接用 torch 当上游**（D→L 本就够）。**正解**：让 llama 参与算力应走**张量级分片/RPC**（`-ts`，官方支持、不需要中间层能力）—— 层接力与 RPC 是**互补而非竞争**；而层流水线（单机装不下）与「只能用 torch 跑的层」**只有层切分能做**。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-hidden-api-asymmetry-2026-09-19.json`。**★ D29 优化现状汇总与进一步空间（2026-09-19）** —— **① 已完成（均有实测）**：P0 切点扫描（2026-09-18，含 hybrid 整倍约束与「孤立测量偏低 5.6×」方法学警告）、P4 加载开销（**3.59 s**，对照不用 meta 的 27.36 s = **7.63×**）、P6 上游开关矩阵（**f16+sdpa 1.665 ms/层为最佳**；量化因 dequant 开销**劣于** f16）、端到端编译层循环（qwen3-5-2b hybrid 24 层 **51.259 → 19.002 ms/step = 2.697×**，逐 token 一致）、**D→L 两端主仓接线**（`forward_layers_from_hidden` / `forward_layers_to_hidden`，本日完成并入库）、**交换上下游实验（L→D 否定结论，本日完成）**。**② 现状差距（两套口径，不可混用）**：旧基线 21.3 s/64 步（≈333 ms/步，上游**手动 4 层**）⇒ ≈**25×**（对 llama 原生 ≈13.5 ms/步）；**新端到端 ≈28.1 ms/step**（D→L，上游走**主仓** + 编译层循环，gen=64，**6/6 逐 token 一致**）⇒ 差距缩至 **≈2×** 量级（**口径不同，定稿需单变量重测**）。**③ 为什么「torch 追平 llama」至今没实现（根因判断）**：llama.cpp 的吞吐主力是**量化 kernel 直接对 4bit 数据做 matmul**（`ggml-cuda/mmq`，Q4_K_M **74.4 t/s** vs torch **17.29 t/s**）；而**主仓 torch 路径的量化反而更慢**（dequant 开销）⇒ **在「现成 torch + bitsandbytes」框架内不可能追平** —— 这不是调参问题，是**计算路径不同**。**④ 进一步优化空间（按预计收益排序，均未开工）**：**(a) 换量化后端**（最有希望）：主仓现用 bitsandbytes，其 kernel 面向大 matmul、dequant 开销盖过带宽收益 ⇒ 若换到**自带融合 CUDA kernel 的量化格式**（GPTQ/AWQ 类，dequant 与 matmul 融合）才有机会接近 llama 的量化吞吐。**(b) 自研融合算子**（triton 已装）：把 dequant + matmul 融进单个 triton kernel，等价于自实现一份 `mmq`；工程量与正确性风险高，宜先做单层基准再用端到端验证。**(c) 切点后移（P0 变体）**：既然 llama 侧量化吞吐不可追平，**让 llama 承担更多层**、torch 只跑必须用 torch 的层（hybrid/自定义算子），比「让 torch 追平」现实得多 —— 这与层切分能力位（`FORWARD_LAYERS`）天然兼容。**(d) P1 下游 GPU offload**：下游当前以 CPU 为主；需先核实既有「部分证伪」的范围再定。**(e) P2 批量交叠**：已实测有收益，与多请求流式交叠合流后可能放大。**⑤ 纪律**：以上各项**均需实测才能改口径**；跨报告数字不构成对照。**★ (a) 换量化后端首轮微基准（2026-09-19，用户裁定走 A 路线：正面解决、不绕开）**：同块真实层权重（qwen2.5-0.5b 的 q/o/gate/down，**batch=1 decode 形态 M=1**，reps=50）—— **bnb nf4 平均 0.317×（慢 3.2×）**、bnb fp4 **0.372×**、torchao int4（D）**缺 `mslk`**、torchao a8w4（E）**0.039× 但未加 `torch.compile` ⇒ 该数字无效**。**⇒ 首次拿到「主仓量化反而更慢」的微观证据**（主仓 `QUANT_TYPE="int4"` 即走 bnb）—— 印证 P6「量化因 dequant 开销劣于 f16」，且**在单层 matmul 层面成立**。**障碍**：torchao 的融合 int4（tinygemm）**依赖 `mslk`，Windows 无 wheel ⇒ 本机不可用**（与「Windows 是 compile 死开关」同源，可转 WSL2）。**下一步**：E 加 `torch.compile` 重测（关键判据）、D 换 `PLAIN` packing 对比、**端到端需另测**（微基准只覆盖 matmul）。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-quant-backends-2026-09-19.json`。**★ (a) 量化后端 × batch 扫描 ⇒ 「批量交叠拿量化红利」证伪（2026-09-19）**：针对用户判断「torch 长于并发，但异构网络下并行张量难做 ⇒ 只能批量交叠」，测其**前置假设** —— M 增大后 matmul 转 compute 受限，量化是否反超 f16？**结果：无交叉点**。`q_proj` 的 bnb/f16 比值 **M=1: 0.17× → M=8: 0.12× → M=32: 0.13× → M=128: 0.11×** ⇒ **一直慢 6~9×**。**⇒ 批量交叠的收益只能来自「重叠 I/O 与计算」，不能指望量化提速。** **副产品**：① **E 加 `torch.compile` 后**（上轮无效项重测）q 0.14× / gate 0.54× / down 0.64× ⇒ 仍慢，但**比未编译的 0.039× 好 4~16×**（编译确有用）；② **D 换 `PLAIN` 仍报 `Requires mslk`** ⇒ **torchao int4 在 Windows 完全不可用**。**边界**：gate/down 的 M>1 数据因我方 `Linear4bit` 转置用法 bug 缺失（结论方向不变）；**未给 std**（噪声可见：M=1 两次 0.0176/0.0118）；仅 matmul 微基准。**剩余希望**：**WSL2 + mslk + tinygemm**（a8w4+compile 已把差距压到 1.6~7×，若 tinygemm 可用理论上还有反超空间）—— **若此路亦否定，则 (a) 整体证伪**。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-quant-batch-2026-09-19.json`。**★★ (a) 量化 × batch 扫描（修正 + std + 大 M）⇒ 交叉点趋势确认（2026-09-19）**：修正上轮两处缺陷（`Linear4bit` **不应转置** ⇒ 四权重曲线补齐；**补 std**）。**核心结果**：比值随 M **单调上升，无例外** —— `gate_proj`/`down_proj`（4864×896）**M=128:0.52× → 256:0.70× → 512:0.79× → 1024:0.88×**；`q/o_proj`（896²）到 **0.58~0.70×**。⇒ **「batch 增大后量化反超 f16」方向确认**（首轮仅测到 M=128 时判的「无交叉点」**按本轮修正**）；**外推交叉点 M ≈ 2000~4000**。**且大矩阵爬升更快**（0.88 vs 0.70）⇒ **compute-bound 更彻底、dequant 摊薄更充分；而真实 LLM 计算主力正是大矩阵（FFN）⇒ 端到端比值会比 q_proj 乐观**。**⇒ 支持用户判断**：「torch 长于并发，但异构网络难做并行张量 ⇒ 只能批量交叠」——**torch 的量化优势只在并发（大 M）下出现**。**权重显存 4×**（0.383 vs 1.531 MiB；0.5B 下无压力，**要到 f16 装不下/4bit 装得下的规模才体现**）。**边界**：仅 matmul 微基准（端到端须另测）、交叉点为**趋势外推未实点验证**。**下一步**：实点验证 M=2048/4096、**端到端批量交叠（P2）**、大模型显存优势、WSL2+mslk。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-quant-batch2-2026-09-19.json`。**★★★ (a) 交叉点实点验证 ⇒ 不交叉、渐近收敛（2026-09-19）**：M=1024/2048/4096 实测，`gate_proj` **0.85× → 0.92× → 0.95×**、`down_proj` **0.87/0.92/0.93**、`q_proj` 0.68/0.85/0.90、`o_proj` 0.59/0.77/0.85。**⇒ 不交叉**（最大仅 0.95），bnb **渐近下限 ≈0.93~0.95** ⇒ **大 batch 下永远慢 5~7%**；**且比值收敛**（M 翻倍只涨 0.03）⇒ **再增大 M 也不会翻转** ⇒ **本轮否定上一轮「外推交叉点 M≈2000~4000」的判断**。**⇒ 量化的价值不在速度、在容量**：bnb 权重 **4× 小**（0.383 vs 1.531 MiB）；0.5B 下双方都装得下 ⇒ 量化纯亏 5~7%，**但模型大到 f16 装不下、4bit 装得下时，量化从「更慢」变为「唯一可行」** —— **这才是 llama 的真实优势，且只在显存受限场景出现**。**对 P2 的结论**：批量交叠**不要指望量化提速**，收益只能来自**重叠 I/O 与计算**。**对用户判断的修正**：「torch 长于并发」趋势成立（比值随 M 单调升），但**上限 0.93~0.95 不反超** ⇒ 更准确：**并发能把量化劣势从 6~9× 压到 5~7%，但压不平**。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-quant-cross-2026-09-19.json`。**★ 2（端到端批量交叠）现状核查（2026-09-19，执行前定位缺口）**：**P2 并非从零开始** —— 已有 ①**同机两序列软件流水线交叠**（`overlap_pipeline.py`，serial 101.11 → **threaded 78.31 ms/token = 1.291×**，吞吐 154.5→199.5 tok/s，**两条序列与单序列基线 token 完全一致**，达天花板 72%）；②端到端编译收益（`bench_p2_e2e_compile.py`，hybrid 1.269×，另测可达 2.5×）；③「孤立 7.6 vs 端到端 42.7 ms」5.6× 差异定位（`idle_wakeup_and_overlap.py`，假说=GPU 空闲唤醒）；④「全 llama+CUDA」公平对照（`-ngl 24` = 26.6 ms/token，快 4.85×）。**⇒ 缺口只有三个**：(a) **真 batch 未做**（现为两条独立序列交错，**不是同一次 forward 的 M>1**）—— 结合 1 的结论（量化在大 M 下仍慢 5~7% 且收敛），**真 batch 是唯一能同时吃「量化摊薄 + 并行」的形态**；(b) **跨机对照未测**；(c) **交叠未进主仓**（`config.py:402 PIPELINE_MAX_CONCURRENT=1`「仅支持 1，串行执行」）。**⇒ 2 的目标不是更高单步吞吐，而是「真 batch 形态下能叠加多少重叠收益」**；**不再重复「两序列交错」已验证形态**。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-p2-status-2026-09-19.json`。**★★ 2 第一半：上游真 batch（M>1）近线性扩展（2026-09-19）**：主仓 `load_layer_range` + `forward_layers`（**前 12 层**，qwen2.5-0.5b，同 prompt/同 gen/同切点，**只变 M**，rounds=3）—— **每序列 ms/token：M=1 10.558 → M=2 5.015 → M=4 2.605 → M=8 1.293** ⇒ **8.2× 摊薄，吞吐随 M 近线性增长**；**单步耗时几乎不变**（10.03~10.56 ms）⇒ **直接印证用户判断「torch 长于并发」，且是近完美线性而非打折**——**这是 P2 批量交叠的核心依据**。**意外线索（新优化点）**：M=1 与 M=8 单步耗时几乎相同 ⇒ 那 ~10 ms **绝大部分是固定开销**（kernel 启动/同步/Python 逐层循环），**与 batch 无关** ⇒ 优化方向是**压固定开销**而非提高算力利用率；且该值本身偏慢（12 层 0.5B），值得单独剖析。**口径更正**：脚本输出的「吞吐提升 1.02×」是取数错误（拿单 token 耗时倒数比）⇒ **正确口径为「每序列 ms/token」= 8.2×**。**边界**：**只测上游**（下游多序列 `llama_batch` 未接）⇒ **非端到端**；M 条同 prompt 同步推进（未做逐 token 数值判据）。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-real-batch-2026-09-19.json`。**★★ 2 第二半：真 batch 端到端（2026-09-19）**：**上游 `forward_layers([M,L])` + 下游新增多序列入口**（`llama_engine.forward_layers_from_hidden` 加 `seq_ids`/`positions`/`all_logits`，**向后兼容**，`tests/test_llama_relay_entry.py` 8 passed）。**性能**：M=1 41.261 ms/token；**M=2 每序列 27.587（交叠 1.364×）**；**M=4 每序列 14.605（交叠 2.446×）** ⇒ **收益显著**。**但未过「逐 token 一致」判据 ⇒ 性能数字暂不可采纳**：batched `matches_M1=False`、serial `True`。**根因已定位到上游**：**① 下游多序列 vs 单序列 logits `max|diff|=0`**（✅ 新增多序列实现**逐位正确**）；**② 上游 M=2 内部两条 `max|diff|=0`**（**非错位/非 mask 错**）；**③ 上游 M=1 vs M=2 hidden `max|diff|=0.0625`**（相对 ~20%）；**④ sdpa 两次结果相同**（确定性）。**机制**：上游用 **f16**，**batch 形状下的归约顺序/kernel 选择不同**（`is_causal` 的 SDPA 在 batch=1 与 >1 可走不同内核）⇒ **f16+batch 的固有数值行为，非实现 bug**。**副产**：定位并解决 llama-cpp-python 陷阱 —— `Llama.__init__` **只在 `embedding=True` 时**才设 `n_seq_max`（:341-346）⇒ 非 embedding 恒为 1 ⇒ `seq_id>0` 必 `rc=-1`；经 `llama_context_default_params`（:253）在**建 context 前**替换即可（已验证）。**下一步**：上游改 **f32** 验证假设（若一致即确认 f16 是唯一原因）⇒ 该判断直接决定「批量交叠能否用于生产」。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-real-batch-e2e-2026-09-19.json`。**★★ 执行 B 的 profile 方案 ⇒ 归因「上游每步 ~10 ms」（2026-09-19）**：主仓 `forward_layers`（qwen2.5-0.5b / M=1 / gen=40 / decode），**零侵入**（cProfile + torch.profiler + 变层数）。**① 修正此前假设**：变层数实测 layers=2→**2.390** / 4→4.272 / 8→8.223 / 12→**11.331 ms/step**，线性拟合 **每层 0.902 ms / 层外固定仅 0.691 ms（6.1%）** ⇒ **开销在「每层」而非「层外」**（此前据 M=1≈M=8 推断的「层外固定开销」**表述不准确**）。**并不矛盾**：每层 0.902 ms 主要不是算力（0.5B 单层算力按微基准约 0.1 ms）⇒ **约 9× 是每层 Python/launch 开销，且与 batch 无关**（batch 增则算力增、但 launch 次数不变）⇒ 这才是 M=1≈M=8 的原因。**② cProfile 构成**（40 步 0.515 s）：**真算力（sdpa 0.097 + linear 0.064）= 31%**，其余 **69% 为辅助**：**`torch.cat` 2969 次（0.035，≈12×41×2 = KV 拼接）**、**`Tensor.to` 2173 次（0.027）**、**RMSNorm 的 pow/mean/rsqrt 各 984 次（合计 0.032）**、`torch.tensor` 1025 次（0.010）、`module.__getattr__` 15907 次（0.008）。**③ 优化候选（按量级）**：rank1 **KV 用预分配+索引写替代 `torch.cat`**（M=1 与 M>1 同时受益，与子 agent #7/#8/#19 吻合）；rank2 审计冗余 `Tensor.to`；rank3 换 `F.rms_norm` 融合；rank4 复用 buffer 消 `torch.tensor`；rank5 Python 调度（`__getattr__`/`_call_impl`）**唯一根治是 `torch.compile`，但被 `COMPILE_MIN_PARAMS=1.5e9` 规模门挡住**。⚠️ **口径**：P6 的「1.665 ms/层」是 **2B**，本次 **0.5B 为 0.902 ms/层**，**不可直接比较**。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-profile-2026-09-19.json`。**★ 待办 v3：优化候选清单 + `n_seq_max` 进主仓（2026-09-19，用户要求记录以免遗忘）** —— 文件 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-TODO-v3-2026-09-19.json`（取代 v2）。**优化候选（源出 profile 归因）**：**rank1 `OPT-KV-PREALLOC`** —— KV 用**预分配+索引写**替代每步 `torch.cat`（**2969 次**、0.035 s，**最大单一非算力项**；**M=1 与 M>1 同时受益**）；rank2 `OPT-TENSOR-TO`（`Tensor.to` 2173 次）；rank3 `OPT-RMSNORM-FUSE`（pow/mean/rsqrt → `F.rms_norm`）；rank4 `OPT-BUFFER-REUSE`（`torch.tensor` 1025 次）。⚠️ **rank5 已撤回**：上一轮建议「降 `COMPILE_MIN_PARAMS` 门让 0.5B 也能 compile」**是错的** —— 实测 **0.5B/12 层开编译层循环反而慢约 6×**（**84.2 vs 13.9 ms/step**）⇒ **小模型 compile 负收益**，**规模门 1.5e9 的存在是正确的、不应下调**。**3（`OPT-NSEQMAX-MAINREPO`）**：把多序列 `n_seq_max` 支持正式进主仓 —— `Llama.__init__` **只在 `embedding=True` 时**才设 `n_seq_max`（:341-346）⇒ 非 embedding 恒为 1 ⇒ `seq_id>0` 必 `rc=-1`；建议给 `load_model` 加 `n_seq_max=None` 参数（None 时行为不变），在建 `Llama` 前经**模块级 `llama_context_default_params()`（:253）**注入并在 `finally` 恢复，**需配单测**；注意与 `patched_llama_loader`（`:347`）的嵌套顺序。**★★★ 2 完成：f16 假设证实 + f32 反而更快（2026-09-19）** —— **根因修正**：上轮 monkey-patch 失效原因是 `_select_layer_runtime` 是**模块级函数**（`model_module.py:1870` 直接调用）**而非 `ModelManager` 方法** ⇒ `hasattr` 为 False、**静默未生效**；本轮 patch `model_module._select_layer_runtime` 生效。**假设检验**：**f16** dtype=float16，M=1 vs M=2 **max|diff|=0.0625**、`allclose(1e-3)=False`；**f32** dtype=float32，**max|diff|=1.52588e-05**、**`allclose(1e-3)=True`** ⇒ **差 4096×（=2^12，正是 f16 舍入精度）⇒ 假设成立：上游 batch 漂移完全由 f16 造成**。**速度（意外）**：**f32 M=1 9.971 ms vs f16 10.981 ms（f32 快约 9%）**；M=2 每序列 4.764 vs 5.920 ⇒ **f32 不但更准还更快，无需取舍**。**原因与 cProfile 一致**：该路径 **CPU/launch-bound 而非带宽受限**（真算力仅 31%、M=1≈M=8）⇒ **f16 省的带宽无价值，反而多付 `Tensor.to` 开销（2173 次）**。**含义**：① **「真 batch 逐 token 一致」可用 f32 达成且不牺牲速度** ⇒ **应改用 f32 重跑真 batch 端到端**（此前因 f16 判「数字暂不可采纳」）；② **上游默认 dtype 值得重新评估**（当前 `_select_layer_runtime()` = cuda:0 + float16）：小模型 + decode 下 f32 更快更准，**但不可武断改默认 —— 需在更大模型/prefill/批量下复测**（大模型可能转回带宽受限）；③ 与量化扫描同指向：**该路径不是带宽瓶颈**。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-f32-2026-09-19.json`。**★★★ A 完成 + FP16 漂移机制证实（2026-09-19）** —— **① A（f32 真 batch 端到端）判据通过**：M=1 **27.495 ms/token**；**M=2 每序列 18.277（交叠 1.518×）**、**M=4 每序列 10.375（交叠 2.654×）**，且 **`matches_M1=True`** ⇒ **真 batch 成立、性能数字首次可采纳**（f16 版为 `False`）。**且 f32 反而更快**：M=1 由 f16 的 41.261 → **27.495 ms（快 33%）**。**② FP16 漂移机制证实（kernel 都换了）**：`torch.profiler` 取到 **M=1 走 `cuBLAS GEMV`**（`...gemvx...cublasGemvParams...`）、**M=2 走 **CUTLASS WMMA GEMM**（`...cutlass_80_wmma_tensorop_f16_s161616gemm...`）⇒ **归约顺序不同**。**单算子实测**（`nn.Linear(4864,896)`，M=1 vs M=2）：**f16 diff/eps = 1**（正好 1 ULP）、f32 = 6、bf16 = 0.125 ⇒ **单次差异仅约 1 ULP，但 f16 的 ULP 是 f32 的 8192×** ⇒ **同样「1 ULP 级」差异在 f16 下绝对值大 8192 倍**，经 12 层累积放大到端到端的 0.0625。⚠️ **顺带纠正我前面的一个错数**：`f32` 报告里写「f16/f32 eps 比 = **4096（2^12）**」**是错的**，`torch.finfo` 实测为 **8192（2^13）**；端到端的 4096 是累积结果，单算子的到比值是 1365。**含义**：本路径 **f32 更准且更快** ⇒ 真 batch 应默认 f32；**但不可据此改全局默认**（结论限定「小模型 + decode + batch」，大模型/prefill 可能转回带宽受限）。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-A-f32-batch-2026-09-19.json`。**★★ 待办 3 完成：多序列 `n_seq_max` 工程化收口（2026-09-19）** —— `LlamaCppEngine.load_model` 新增 `n_seq_max: int | None = None`（**None 时行为与旧版完全一致**）；新增模块级 helper **`_new_llama_with_seq_max`**：非 None 时在建 context 前临时替换模块级 `llama_context_default_params()`、把 `n_seq_max` 抬到 `max(指定, 默认)`，**`finally` 严格恢复**；**三处 `Llama(**load_kwargs)`（`:352` RPC / `:373` align_numerics / `:377` CPU）全部改走该 helper**。**起因**：`Llama.__init__` **只在 `embedding=True` 时**设 `n_seq_max`（:341-346）⇒ 非 embedding 恒为 1 ⇒ `seq_id>0` 必 `rc=-1`。**单测 3 项**（`TestMultiSequence`）：默认路径**零副作用**、构造后**必须恢复全局函数**、多序列与单序列 **`argmax` 相同** ⇒ 该文件 **11 passed**、`-k llama` **98 passed / 1 skipped**。**⚠️ 测试中的重要发现**：写「多序列 vs 单序列」时**逐位相等与 `allclose(1e-2)` 均失败**（logits 尾部差异 >1%）⇒ **这是 llama.cpp 与 torch 同源的现象**：`batch=1`/`batch>1` 走**不同 kernel**，f16 下产生舍入差异（与上游 torch 的 f16 漂移 0.0625 同类）⇒ **判据必须按实际用途选：下游只用 `argmax`，故验收判据是「选出的 token 一致」；「真 batch 逐位一致」在 f16 下本质不可得**（与 A/f32 结论一致）。**回归**：串行全量 **2817 passed / 11 skipped / 1 failed**，唯一失败为 `test_external_provider::test_chat_falls_back_to_local_on_backend_down`，**复跑判定 flaky 且与本改动无关**（该文件复跑 39 passed、该用例单跑 3 次全过）。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-todo3-done-2026-09-19.json`。**★★ 复核 `OPT-KV-PREALLOC` 性价比 + 核实两前提（2026-09-19）** —— **① 整模加载核实（用户记忆正确）**：上游 `model_module.py:1806` `from accelerate import init_empty_weights`、`:1880` `with init_empty_weights():`、`:1885/:1892` `from_config(...)`、`:1899` 读 `model.safetensors.index.json` ⇒ **先建空壳再按 key 物化，不加载整模**；下游用**裁层 GGUF** ⇒ **上下游都不加载整模**，「层流水线摆脱整模加载」成立。**② 安卓门禁现状**：`scheduler.py:3754-3760` **仍在阻止**（`node_type == "android"` ⇒ `layer_assignment_node_unsupported`）；**注释理由写「无 PyTorch 推理能力」但代码阻止的是「层前向传播」整体、不区分 backend** ⇒ **实现过宽**（Android 上不了 torch 但能上 GGUF 引擎）——**用户判断被代码证实**；**本轮只核实未改动**。**③ `OPT-KV-PREALLOC` 复核 ⇒ 降级**：`torch.cat` tottime 0.035/0.515 = **仅 6.8%** ⇒ **收益上限约 7%**；而实现要改 `DynamicCache`（被 A3/B9/B14 反复修过：跨版本兼容 `:215-231`、本地索引 `:3493-3503`、tuple↔cache 往返 `:3547/:3800`）⇒ **风险远高于收益，不值得按原设想做**；若动 KV 应评估 **StaticCache 整体替换**。**④ 引用既有 B6 数据**（`out/b6-static-cache.json`，0.5B/12 层/gen=64/f16/sdpa）：**B eager+StaticCache(整段) 16.097 ms/step**、**C compile+DynamicCache 8.198**、**D compile+StaticCache 6.18（0.515 ms/层，argmax 全一致）**；⚠️ **A（现状）报错「cache 层数不匹配」⇒ 该文件无「D vs 现状」可比数字**；且 **B/D 走「整段 `model.forward`」，层流水线必须用 `forward_layers`（只跑层段、出 raw hidden）⇒ B6 的 D 不能直接套用**（与本次 profile 的 11.331 比值 1.83× **口径不同、不构成对照**）。**注**：①「0.5B compile 特慢」与 B6 里 compile 每次调用更快**不矛盾** —— B6 的 `prefill_ms=14806~15657`（**首次编译 15 s 级**）在短 gen 下会使总墙钟倒挂 ⇒ 取决于 gen 长度与摊销。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-kv-reassess-2026-09-19.json`。**★★★ 待办 C 完成：Android 参与层流水线的门禁「按平台 → 按能力」（2026-09-19）** —— **根因**：`koakuma_engine.py:53` 的 `_BACKEND_CAPABILITIES` 里 **`Capability.FORWARD_LAYERS` 此前只挂在 `PYTORCH` 上，`LLAMA_CPP` 没有** ⇒ 全系统据此认为「只有 PyTorch 能做层前向」，而主仓现已具备 llama.cpp 的层前向入口 ⇒ **能力位缺失**。**改动**：① `koakuma_engine.py` 给 `LLAMA_CPP` **补上 `FORWARD_LAYERS`**（不含 `LOAD_LAYER_RANGE`/`ENSURE_FULL_MODEL`，那两个是 PyTorch 侧语义，llama.cpp 用「裁层 GGUF」）；② `scheduler.py` 新增 `_node_supports_forward_layers`，把 `validate_layer_override` 的「`node_type == "android"` 一律拒绝」改为**按能力**；③ `api_server.py` 新增 `_client_supports_forward_layers`，把 `first_connect_bootstrap` 的 `pipeline_worker = node_type == "pc"` 与 android 段硬编码 `False` 一并改为**按能力**。**判定顺序（保守、向后兼容）**：① 自报 `capabilities` 含 `FORWARD_LAYERS`（或 `forward_layers=True`）⇒ 允许；② 自报 `backend_id`/`engine` ⇒ 按公开 `backend_capabilities(...)`；③ **都未自报 ⇒ 退回旧行为（仅 `pc`）** ⇒ **未声明能力的 Android 仍被拒绝**。**测试**：新增 `tests/test_android_layer_pipeline_gate.py`（**19 passed**，含「未自报的 android 仍拒绝」「自报 llama_cpp 放行」「畸形输入不崩」「大小写敏感不漂移」）；更新 `test_koakuma_engine` 的旧断言（原 `not llama.supports(FORWARD_LAYERS)` 反映旧认知）。**回归**：**串行全量 2837 passed / 11 skipped / 0 failed**（过程中 `test_island_engine::test_backend_down_raises_clean_transport_error` 曾失败一次，**复跑该文件 20 passed ⇒ flaky、与本改动无关**）。**⇒ 回答用户提问**：**此前未放开**（两处均按平台一刀切），**本轮已放开**，方式是**按能力** —— Android 自报具备层前向能力（如跑 llama.cpp/GGUF）即可参与层流水线，**未自报者行为不变**；⚠️ **客户端需实际上报 `capabilities`（协议字段已存在）**。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-android-gate-2026-09-19.json`。**★★★ D 完成：compile 摊销判据（2026-09-19）** —— 起因是两处表面矛盾：旧记载称「0.5B/12 层开 `USE_MONOLITHIC_FORWARD` **反而慢约 6×**」（`model_module.py:2760`），而 B6 里 compile **快约 2×**（然 B6 是**整段 `model.forward`**）。**关键区分**：`forward_layers()` 只用 **`_compiled_layer_loop`**（`:3511`），**整段编译在层流水线里用不到**。**实测**（主仓 `forward_layers`/12 层/f16，时间线切成 build/1st/2nd/3rd/稳态）：**A 不编译** 1st=297/2nd=59/3rd=9 ms、**稳态 11.204±2.02 ms**；**B 编译层循环** 1st=2052/2nd=1199/3rd=3565 ms、**稳态 4.615±1.11 ms** ⇒ **① 「慢 6×」是误读**：稳态**快 2.428×**（每步省 **6.589 ms**），旧说法把**约 6.45 s 首次编译**摊进短 gen 的每步；**② 真正代价是编译 ≈6451 ms**；**③ 盈亏平衡 ≈ 979 步**（>约 1000 步才回本，典型对话远短于此 ⇒ **默认不开正确**）；**④ 规模门结论对、依据错** —— 不是「小模型编译更慢」，而是「**小模型每步省得少（6.6 ms），摊不平 6.45 s 编译**」⇒ 门保留，但**更贴切的判据是「预期生成长度」**（≈1000 步以上才值得）。**已修正 `model_module.py:2760` 那句依据错误的注释。**报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-amortization-2026-09-19.json`。**★ 待办：harness `rag_baseline.py` 单端化（2026-09-19，RAG 裁撤遗留）** —— **`OPT`/`HARNESS-RAG-BASELINE-SINGLESIDE`**：`harness_workbench/tools/rag_baseline.py`（646 行，独立子模块）原为「**主仓 `src/rag_store` vs harness `rag.store`**」的**双端对照**工具；主仓 RAG 已裁撤（`a07e046`）⇒ **对照维度失效**，应改为只依赖 harness 自带 RAG。**证据**：`:55` 硬检查 `src/rag_store.py`、`:47-60` `_resolve_main_project_root`、`:63-82` `_main_project_importable`、**`:388/:456/:539` 三处** `from src.rag_store import ... as MainRagStore`、`:623` CLI `--main-project-root`。**本轮两次尝试均失败并已回滚**（① 整体替换致块变空 → `IndentationError`；② 三处对照点**结构不同** —— 第 2 处 `main_store` 构造在 `for strategy` 循环体内）⇒ **需重新设计「单端 baseline」语义**（报告 schema 本为**对照**语义、三个 `run_rag_*` 的 `main_*` 计算需整体重写）⇒ **建议单独立票**。**影响低**（离线基准，不在运行时链路）。**★ BUG（用户报障）qwen-1_8b 加载崩溃：`normal_kernel_cuda not implemented for 'Byte'` —— 定位完成 + 修复已验证未实施（2026-09-19）** —— **复现**：`python locate_byte_normal.py --quant int4 --engine pytorch` ⇒ 加载在 **`Loading weights 1%|1/195`** 处崩溃。**完整调用栈（实证）**：transformers 5.17 `modeling_utils.py:2379` `self._init_weights(module)` → `:2401-2402` `smart_apply` → **remote code** `modeling_qwen.py:666` `_init_weights` → `p.data.normal_(...)`（`p.data` 是 **int4 量化后的 `uint8`**，shape `[2097152, 1]`）→ **CUDA 无 uint8 的 `normal` 内核** ⇒ `NotImplementedError` ⇒ HTTP 500。**为何现有守卫没救回来**：`_verify_and_repair_loaded_weights`（A7/B7）在 `:2542` **加载完成后**才跑，实测 `guard_calls: []`（**根本没被调用**）⇒ 它治的是「权重被**静默覆盖**」，本次是「覆盖动作**本身崩溃**」⇒ **同一 transformers 5.x 缺陷的另一种表现**。**机制（源码）**：`modeling_utils.py:2361` 「已标记 `_is_hf_initialized` ⇒ **直接 return**」、`:2379` 「**仅当未标记**才调 `_init_weights`」，而 transformers **自己**在加载后修复 missing keys 时才打该标记（`:4766-4769`）⇒ **加载中 vs 加载后的时间差**就是漏洞。**修复（✅ 已验证）**：在 `initialize_weights` 前给**全部模块及直接参数**预打 `_is_hf_initialized=True` ⇒ 实测 **`uint8` 的 `normal_` 调用 1 → 0、加载成功（≈8 s）**。**落点**：`_load_pytorch()` 中 `from_pretrained` 之前，**作用域收窄到「量化 + TRUST_REMOTE_CODE」**，`try/finally` 恢复；**必须配回归测试**（当前**无**该用例，故缺陷一直未被测出）。状态：**已验证未实施，等裁决**。报告 `local_docs/BUG-QWEN18B-BYTE-NORMAL-2026-09-19.json`。**★ Android-as-L-segment（2026-09-19 用户裁决，待开工）**：澄清「排除的是 **PyTorch**、不排除 **llama**」（`scheduler.py:3754` 注释原文即「Android 无 PyTorch 推理能力」，但 `:3756` 的实现是「阻止被分配层」⇒ **实现过宽**，需按 backend 细化才能放行 llama 层段）。**方向**：让 Android 以 **llama.cpp 层段**（L 段）参与**跨框架层接力**。**待实测的核心问题**：该路径与 **RPC（`-ts` 分片）** 谁更优 —— 两者都要把权重放到节点上（RPC 按 tensor 比例切分、接力按层切分），差别在**切分粒度、每 token 传什么（两者都是 hidden）与协议/校验成本**；用户 2026-09-19 判断「接力或许因不必在内网传整份权重而更好」，**但结论需实测**。**判据建议**：同设备对、同模型，测「首次装载成本 + 每 token 成本 + 数值一致性」三项，并注明 Android 段可承载的层数上界。**准入不变**：`admit_relay_xframe` 仍 fail-closed。**产物要求**：每项落 `local_docs/CORE-RELAY-XFRAME-02-<项>-<日期>.json` 报告与脚本；**不得因优化进展放宽 `admit_relay_xframe` 的生产门**。**★ P0 实测结论（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-sweep-2026-09-18.json`）**：扫描 N∈{0,4,8,12,16,20}（**N=0 为「不接力」对照**）—— 全部切点的 64-token 贪心序列**逐 token 一致**（正确性不随切点变化）；**总时间对切点几乎不敏感**（N∈{4,8,12,16} 落在 214–225 ms/步，±2.6%）；**不接力反而最快（186.8 ms/步，比默认 N=4 快 12.9%）**，即本机同机单序列下接力开销约 **+15%**（相对纯 llama.cpp CPU 口径）——远小于「对比原生 GPU 慢约 25×」的印象（那 25× 主要是 CPU/GPU 之差；参照谱见 §F/§H）；上游 torch/CUDA 每层成本（8.8–10.7 ms）**不低于**下游 llama.cpp/CPU（7.7–8.6 ms），故「把层搬到 torch GPU」在本机不产生速度优势。**结论：现有默认 N=4 在「必须接力」前提下已是最优点；P0 未发现中间谷底**。**工程约束**：切点必须是 `full_attention_interval`（Qwen3.5 = 4）的整数倍，否则裁层 GGUF 层类型错位（N=2 实测报 `missing tensor 'blk.1.ssm_conv1d.weight'`，无法加载）。**方法学警告**：孤立测量不可信 —— `upstream_layer_cost.py` 报 decode(N=4)≈7.6 ms，而 relay 端到端（42.7）、逐段诊断（42.9）、形状敏感度（34.4/47.4）三种测量均为 34–48 ms（偏低约 5.6×，成因未定，已验证**不是**「KV 形状是否固定」）；**★ P0 修正（v2，同日）—— 上一段 v1 结论作废**：v1 的上游**全部跑在 CPU** 上（`relay_sameproc_4L.py` 的 `from_pretrained` 之后没有 `.to(device)`，`dev = tmodel.device` 于是是 cpu），而孤立脚本显式 `.to("cuda")` —— 同脚本同口径实测同 4 层：**cpu 34.9 ms / cuda 8.3 ms**，**5.6× 由设备解释**（已否证 KV 形状假说与空闲降频假说：`shape_sensitivity` fixed 47.4 > growing 34.4；`idle_wakeup_and_overlap` idle 8.56 vs continuous 7.53 = 1.14×，SM 时钟全程 780/3105 不变）。给 relay 加 `--upstream-device cuda`（配 f16 + `--upstream-partial` 只加载前 N 层，显存约 N/24×4.3 GB）后重扫：**N=8 → 169.0、N=12 → 155.0、N=16 → 146.4、N=20 → 129.1 ms/步**（对应 CPU 上游为 223.8 / 224.6 / 221.9 / 249.7，提升 1.32× / 1.45× / 1.52× / **1.93×**）；全部切点 64-token 序列仍**逐 token 一致**。**上游 GPU ≈ 2.5–4.3 ms/层 vs 下游 CPU llama.cpp ≈ 5.8–8.6 ms/层** ⇒ **应把层尽量推给 GPU 上游**，而下游多为无 CUDA 的边缘设备，恰好支持该方向。**修正后最优（已测）N=20 = 129.1 ms/步，比「不接力」的 186.8 快 1.45×** —— 接力首次显示明确收益；v1 的「不接力最快 / 切点无收益」**仅在 CPU 上游条件下成立**，不可外推。报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-p0-corrected-2026-09-18.json`（取代 v1）。**★ P2 与「全 llama + CUDA」公平对照（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-p2-2026-09-18.json`）**：**回答「集群中有 CUDA 节点时，全 llama 是否不如接力」—— 不是**。同机同模型实测：**llama.cpp + CUDA（build-cuda，`-ngl 24` 全部层上 GPU，f16，t=8）= 26.6 ms/token（37.54 t/s）**，而接力最优（N=20）= 129.1 ms/token ⇒ **llama.cpp 用上 CUDA 快 4.85×**；`-ngl` 曲线单调（ms/token = 91.1 / 71.0 / 60.5 / 52.9 / 44.2 / 34.7 / 26.6，对应 `-ngl` 0/4/8/12/16/20/24）。⚠️ 参照物必须**同构建**：build-cuda 的 llama-bench 即使 `-ngl 0` 也比 build-cpu 快约 2×（91.1 vs 194.2 ms/token）。**P2 交叠实测**：两条独立序列（同 prompt）用软件流水线交错（上游 GPU torch 与下游 CPU llama.cpp 各持一把锁，torch 的 CUDA 调用与 ctypes 的 llama.cpp 调用都释放 GIL ⇒ 可真并行）：serial 101.11 → **threaded 78.31 ms/token（加速 1.291×）**，吞吐 154.5 → 199.5 tok/s，**两条序列 token 与单序列基线完全一致**（数值未被破坏）；理论天花板约 1.79×（取上游 72.3 / 下游 56.8 之较大者），实测达到约 72%。**定位结论（重要）**：P0 的 1.45× 只是「CPU llama.cpp → GPU torch」的局部收益，更好的做法是「CPU llama.cpp → **GPU llama.cpp**」（`-ngl`）—— ⇒ **跨框架接力不是更快的推理路径**，而是「**只能用 torch 跑的层**」（hybrid/自定义算子）与「**容量合并 / 层流水线**（单机装不下）」的机制，外加实验平台；**集群里有 CUDA 节点时，最佳实践是把它作为 llama.cpp 的 CUDA worker（RPC/分片），而不是接力上游**。P2 交叠只在「不得不接力」的场景内把损失补回一部分（78.3 ms/token 仍慢于 26.6 约 3×）。⚠️ 本机**同机**数据；**跨机（GPU 节点 + 无 CUDA 边缘节点）的 RPC vs 接力对照仍未测**。**★ 预测环节（2026-09-18，汇总报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-切点-2026-09-18.json`）**：用两侧单位层耗时做线性拟合即可**预测**最优切点并**命中实测** —— GPU 上游 2.484 ms/层 < 下游 5.691 ms/层 ⇒ T(N) 单调递减、最优落在**允许的最大 N**（预测 N=20 = 实测 N=20，逐点误差 ≤2.02%）；CPU 上游 8.494 ms/层 > 下游 6.257 ms/层 ⇒ 单调递增、最优落在**最小 N**（即不接力；预测 N=4 = 实测 N=4，误差 ≤6.36%）。**方法论**：换设备/换模型时**不必全扫**，各测一次单位层成本、比较两者大小即可定位最优边界切点；也说明「中间切点最优」的谷底在该成本模型下**不存在**。**★ P6 torch 上游开关矩阵（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-torch-modes-2026-09-18.json`）**：核实「实验路径 ≠ 主仓引擎」的影响 —— 实验的 `manual_forward_n` 强制 `attn=eager` 且默认 f32，**绕过了主仓会用的 sdpa / 量化 / compile**。同模型（Qwen3.5-2B，gen=64，CUDA）矩阵（ms/层）：**f16-sdpa 1.665（最佳）** / f32-sdpa 1.810 / f16-eager 1.918 / f32-eager 1.948（= 实验路径现状）/ **nf4-sdpa 3.210（慢 65%）** / **int8-sdpa 8.0–9.8（慢 4–6×）**；`flash_attention_2` 未安装；⚠️ **本轮更正（2026-09-19，见汇总「已推翻结论」）**：此处原写「`torch.compile` 在 Windows 因 triton 无构建而不可用（TritonMissing）⇒ `USE_COMPILE` 在 Windows 是死开关」——**该结论已不成立**：装社区构建 `triton-windows-3.8.0.post28`（`requirements-compile.txt` 已声明）后 **Windows 原生 compile 可用**，本仓已在 CPU（`.venv-test`）与 CUDA（主运行时）两侧实测编译成功。⇒ **compile 收益应计入 Windows 方案**。对照 **llama.cpp CUDA = 1.108 ms/层** ⇒ **最佳 torch 配置仅差 1.5×**（不是 5×）。**可执行建议**：① 单序列/交互式默认用 **fp16**（`QUANT_TYPE="int4"`（NF4）在单序列是**负优化**，量化只在显存受限时值得：峰值 1.68 vs 3.54 GB）；② **显式设置 `attn_implementation="sdpa"`**（当前 `model_module.py` 全仓无此参数，依赖库默认）；③ compile 在 Windows 无效，不应计入 Windows 方案。**结论：torch「接近 llama」已达成（1.5×），差距主因是配置而非框架上限**。**★ P6-b 量化 vs 并发判决（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-quant-batch-2026-09-18.json`）**：假说「量化劣势来自 batch=1 时 bnb kernel 利用率低」**被否证** —— NF4/f16 在 batch 1/2/4/8 = **1.71 / 1.90 / 1.99 / 1.65×**，全程更慢且**比值不随 batch 改善**。真因是 **kernel 质量**（bitsandbytes NF4 matmul 慢于原生 f16 cuBLAS ~1.8×），而它省的带宽在本场景**无价值**（f32→f16 只快 8.2% ⇒ 2B/8GB 单序列非带宽瓶颈）⇒ **量化是容量旋钮，不是速度旋钮**（与 `--override-tensor` 结论同构）。副产品：f16 batch 1→8 每 token 36.03→5.53 ms（**6.5× 扩展**）⇒ D 档并发扩展性可用。**★ 下游 llama 优化位（代码核实）**：主仓 `llama_engine.load_model()` 只显式设 `n_ctx`/`n_threads`/`chat_format`（RPC 时加 `tensor_split` + `n_gpu_layers` 默认 `-1`），而 **`flash_attn` / `type_k` / `type_v` / `n_batch` / `n_ubatch` / `use_mmap` 全部未设**（走 llama.cpp 默认）—— `load_model(**kwargs)` 是支持透传的（docstring `llama_engine.py:230-242` 明列）⇒ **三个现成优化位（flash_attn、KV 量化 type_k/type_v、prefill 分块 n_batch/n_ubatch）**，注入点 `model_host.py:271`（经 ModelManager）与 `:329`（直接调 `LlamaCppEngine`）。**★ triton / torch.compile 结论**：**不需要编译 triton** —— Linux 有官方 wheel（WSL2 内实测 `pip download triton` 拿到 `triton-3.8.0-cp310-cp310-manylinux_2_28_x86_64.whl`，247.8 MB）；WSL2 = Ubuntu 22.04.5 / python 3.10.12、GPU 直通正常（RTX 4060 Laptop, 610.47, 8188 MiB）但默认环境未装 torch/triton；Windows 原生下 `torch.compile` 报 `TritonMissing` ⇒ **主仓 `USE_COMPILE` 在 Windows 是死开关**。**★ P7 下游优化位实测（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-downstream-opts-2026-09-18.json`）**：用 `llama-bench`（build-cuda，同构建同口径，**同次运行内**比较）在 f16 整模上扫 `-fa` / `-ctk,-ctv` / `-b,-ub` / `-ngl`（`-t 8 -p 5 -n 64`）：基线 CPU `-ngl 0` **12.37 t/s（80.8 ms/token）**；`-fa 1` **1.001×（无收益）**；`-ctk/-ctv q8_0` **0.917×（慢 8%）**；`-b 1024 -ub 256` **0.745×（慢 26%，decode 受损）**；组合 0.904×；**`-ngl 24` = 38.96 t/s = 3.15×（唯一大杠杆）**。⇒ **实验下游（= 主仓默认参数）已在 CPU 优化极限附近**（用户判断成立）；flash_attn / KV 量化的价值在**长上下文与显存**而非短序列 decode（本轮 prompt 仅 5 token，未覆盖该场景）；**KV 量化再次印证「容量旋钮」**（与 NF4 权重、`--override-tensor` 三者同构）。**与主仓对齐**：主仓下游与实验下游**唯一实质差异是 `n_gpu_layers`**（主仓默认 `-1` 全 offload / 实验 `0` 纯 CPU），其余参数主仓都走 llama.cpp 默认 ⇒ 要 CPU 对照时显式传 0。**上游 torch 的追赶目标据此定义为**：同 CUDA = **1.071 ms/层**、同 CPU = **3.367 ms/层**。⚠️ 测量漂移：同命令跨运行约 1.13×（10.98 vs 12.37 t/s），故只做同次运行内比较。**★ P7 修正（v2，2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-downstream-opts-v2-2026-09-18.json`）**：v1 用 `-r 1`，其跨运行漂移 12–17% **大于**被测量效应（5–13%），曾据此误判「`-fa` 对 prefill +6.2%」。改用 **`-r 3` + std 判据**后：decode 侧 `-fa` = **−5.3%(CPU) / −1.7%(GPU)**、KV `q8_0` = **−5.4%(CPU) / −1.1%(GPU)**，**均为轻微负收益**；prefill 侧 CPU 三个变体无差异、GPU 的 +2.6% 落在 std（±19.25）内 ⇒ **不显著**。**唯一稳健的大杠杆 = GPU offload 3.267×**（11.99 → 39.17 t/s），而主仓默认 `n_gpu_layers=-1` 正是此项 ⇒ **实验下游（= 主仓默认参数）已接近优化极限**（用户判断成立）。**方法论（重要）**：llama-bench **必须 `-r ≥3` 并报 std**，否则噪声会伪造结论；不同构建不可比（build-cuda 即使 `-ngl 0` 也比 build-cpu 快约 2×）。**上游 torch 追赶目标（最终定义）**：同 CUDA **1.064 ms/层**、同 CPU **3.475 ms/层**；torch 最佳（f16+sdpa）1.665 ms/层 ⇒ 对 CUDA llama **慢 1.56×**、对 CPU llama **快 2.09×** ⇒ 「接近 llama」已达成。**★ P8 主仓引擎本体实测（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-mainrepo-engine-2026-09-18.json`）**：按用户裁定「实验必须用主仓引擎」，首次跑通 **`ModelManager.load_layer_range` + `forward_layers`**：**Qwen2.5-0.5B 前 12 层 = 13.331 ms/step = 1.1109 ms/层（cuda:0, float16）** ⇒ 对照 **llama.cpp CUDA 1.064 ms/层仅差 4.4%**、llama.cpp CPU 3.475 ms/层为 0.32×。加载模式确认为 **`safetensors_key_filtered`**（按 key 过滤物化，146 张量 / 0.63 GB，**非整模后裁剪**）⇒「摆脱整模加载」在主仓 PyTorch 侧**已经成立**（llama.cpp 侧则必须离线裁层）。**三个 API 细节（本次排障所得，值得留存）**：① `load_layer_range` 的 `model_path` 是**第 5 个参数**（关键字）；② 不传它会回退 `config.MODEL_PATH = models/qwen-1_8b-chat`（**该老模型带自定义代码** ⇒ `TRUST_REMOTE_CODE=True` **并非冗余**，我先前的判断已更正）；③ 前向**必须**用 `forward_layers()`（手写逐层会漏 `position_embeddings` → TypeError）。**环境绕行**（按裁定不改产品代码）：本进程内覆盖 `TRUST_REMOTE_CODE=False`（Qwen2.5 是 transformers 原生架构，config 无 auto_map、目录无 .py）。⚠️ 口径：本轮为 0.5B / 12 层，**不与 Qwen3.5-2B 的实验读数直接横向比较**；llama 对照取自 f16 整模 tg64。**★ P9 主仓引擎 compile 档（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-2026-09-18.json`）**：装 `triton-windows` 后 `torch.compile` **从「Windows 死开关」变为 3.2× 加速** —— 主仓层拆分前向 **1.0443 → 0.3258 ms/层**（3.910 ms/step），对照 llama.cpp CUDA 1.064 ms/层 ⇒ **compiled 快 3.27×**。**但两个必须记住的代价**：① **`mode="default"` 不是 bit-exact**（compiled vs eager 的 hidden `max|diff| = 7.8125e-02`、mean 6.7e-3，fp16）⇒ 用于接力前**必须先过「逐 token argmax 一致」判据**；② **`mode="reduce-overhead"` 在解码路径直接失败**（`RuntimeError: accessing tensor output of CUDAGraphs`，cudagraphs 与「KV cache 每步换 tensor」不兼容）—— 而主仓 `_apply_compile()` 用的**正是该模式** ⇒ **建议改为 `mode="default"` 或按有无 KV 复用分场景**。另：`load_layer_range()` 路径**没有 compile 应用点**（`_apply_compile` 仅在 `_load_pytorch()` 的 `if USE_COMPILE:` 分支调用一次，`model_module.py:1989-1998`）⇒ 层流水线节点若要 compile 需补应用点。⚠️ 待测：compiled 与 eager 的 **argmax 序列**是否一致；compile 在 Qwen3.5-2B 上仍会崩（疑与规模/显存或 hybrid 有关，未定位）。**★ P9-b compile 缺陷修复与验证（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-fix-2026-09-18.json`）**：把 P9 发现的两个缺陷**修进产品代码**（`src/model_module.py` **三处**，最小侵入）：① `_apply_compile()` 的 `mode` 由 `reduce-overhead` 改 **`default`**（前者 CUDA Graphs 与 KV 解码不兼容）；② **不再** `self.model = torch.compile(self.model)`（会破坏 `self.model.model` 结构访问，而 `forward_layers` / `_count_transformer_layers` 都依赖它），改为编译内部 transformer 并另存 `self._compiled_transformer`；③ 抽出 `_maybe_apply_compile()` 并在 `load_layer_range()` 末尾调用 —— **补齐分层路径的 compile 应用点**。**验证**（`verify_compile_fix.py`，Qwen2.5-0.5B/12 层/24 步）：`compile_point_exists=True`、`structure_intact=True`、`decode_runs=True`、**`argmax_identical=True`（first_divergence=None）**、本轮加速 **1.904×**（29.594 → 15.546 ms/step）。⇒ ★★ **尽管 hidden 数值存在 max\|diff\|≈7.8e-2 的差异，24 步 argmax 序列仍与 eager 完全一致** ⇒ **`mode=default` 的加速在「逐 token 一致」这一接力判据下可用**。回归：`-k "model_module or layer_range or compile"` **116 passed / 2 skipped**（全量见同轮执行）。⚠️ 待验：argmax 一致性只测了 **24 步**，长序列（64/141 步）未验；`USE_COMPILE` **默认仍为 False**（本轮只修能力与正确性，未改默认行为）。**★ P10 USE_COMPILE 默认开启 + 上下游速率对照 + 下游整模/裁层核查（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-default-relay-audit-2026-09-18.json`）**：按用户裁定把 **`config.USE_COMPILE` 默认由 False 改 True**（动机：装 triton-windows 后 Windows 下才真正可用、mode 已修为 `default`、triton 缺失时自动告警回退不影响启动）；相关测试 **252 passed / 2 skipped**（`.venv-test` 无 triton 仍不崩 ⇒ graceful 生效）。**上下游速率对照**（ms/层，口径不同仅同量级比较）：上游主仓 **eager 1.0443** / **compile 0.3258**；下游 llama.cpp **CUDA 1.064** / **CPU 3.475** ⇒ **上游 eager ≈ 下游 CUDA；上游 compile 后比下游 CUDA 快约 3.3×** ⇒ **启用 compile 后上游不再是瓶颈，优化重心回到下游**。**下游整模/裁层核查（回答用户质疑「下游是不是还在加载整模」）**：① 接力实验用的是**离线裁层 GGUF**（`cut-k20.gguf` **1.50 GB** vs 整模 **3.63 GB** = 41%，真·层裁切）；② 但**主仓 runtime 只能加载完整 GGUF**（`load_model` 无层范围参数、`ensure_full_model` 明示「不存在层裁剪」）；③ 主仓 `relay_contract.py` **有**裁层合同（`TrimPlan.trim_layers` / `kept_block_count` / `CUT_LAYER_MIN` / `cut_layer`，由 `pipeline_node_contract.py:473-477` 校验），但 `src/`、`scripts/` 下 **`*cut*` 零命中** ⇒ **「有合同、有校验、没有生成器」的功能缺口**。**建议**：把实验区 `cut_layers.py` 收敛到 `relay_contract.TrimPlan` 语义并正规化进主仓（产物带 block_count/MTP 布局的 manifest）。⚠️ 未验：「主仓上游 + llama.cpp 裁层下游」的**真接力**（主仓不支持 Qwen3.5 层拆分；Qwen2.5-0.5B 无对应 GGUF/裁层工件）；compile 对长序列（64/141 步）的 argmax 一致性。**★ P11 真接力：主仓引擎上游 + llama.cpp 裁层下游（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-real-relay-v2-2026-09-18.json`（v2 修正版）+ v1 同日文件）**：为回答「主仓接回接力 / 上游快还是下游快 / 下游是否整模」，先补齐**工件**——用 `llama.cpp/convert_hf_to_gguf.py` 把 Qwen2.5-0.5B 转 f16 GGUF（**948 MB**），再用新写的通用裁层脚本 `cut_layers_generic.py --k 12` 生成**裁层工件 607 MB**（整模的 **64%**；`qwen2.block_count 24→12`、保留 146 / 丢弃 144 张量）。随后 `relay_mainrepo_upstream.py`：上游走**主仓** `load_layer_range(0,12,...,model_path=...)` + `forward_layers()`，下游走**裁层 GGUF**（`llama_batch.embd` 注入）。**结果**：① **接力 token 与「纯 llama.cpp 整模」逐 token 一致**（`tokens_match_baseline=True`）⇒ 主仓 `forward_layers()` 可作跨框架上游执行器；② **上游 3.1583 ms/层 > 下游 1.4872 ms/层**（下游更快 2.1×；注意上游 CUDA / 下游 CPU 属**异构**对照）；③ **下游确实是裁层**而非整模（607/948 MB），但**主仓 runtime 本身只能加载整模**⇒ 裁层必须离线生成。**★ v2 修正（撤回一条错误结论）**：v1 写的「`forward_layers()` 手动逐层比整段前向慢约 3×」**撤回** —— 那是**跨报告比较**；**同进程对照**实测 **A(`forward_layers`) 1.1109 vs B(整段 `inner(...)`) 0.8427 ms/层 ⇒ 只慢 1.318×**，且 A 与 P8 的 1.0443 基本一致 ⇒ **`forward_layers` 的实现开销不是主要优化空间**（v1 的 action ① 降级）；另两条路径 hidden `max\|diff\|≈44` ⇒ **语义不等价**（A 会补丁 `self_attn.layer_idx` 为连续本地索引、B 用原始 global index），**性能可比、数值不可比**。**⚠️ 未定位**：真接力的上游比同进程对照慢 **2.8×**（1.1109 → 3.1583），候选原因：up_times 计入了每步 `.float().cpu().numpy()` 同步、三模型共存（torch 分段 + 954 MB 整模 + 607 MB 裁层）的内存压力、下游 CPU decode 期间 GPU 空闲、带宽争用；下一步在接力脚本内插桩「纯 forward / +张量转换 / +下游等待」三段。**方法学（本会话第二次踩）**：跨报告/跨脚本的数字不构成对照 ⇒ 任何对比必须在**同进程、同条件**下复测后才可写进结论。**★ P12 上游 2.8× 偏差定位（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-upstream-profile-2026-09-18.json`）**：P11 的未定位项已查清。方法：**同进程四变体**（v1 的两个坑已修：**每个会 decode 的变体用独立 llama context**、**先 warmup 吸收 compile 首次编译开销**）：A forward-only **12.089** / B +convert **12.277** / C +downstream（真接力）**30.003** / D +whole-model（多驻留 948 MB 整模）**30.798** ms/step。**增量**：张量转换 **+0.188**（可忽略）、**下游 decode 期间干扰 +17.726（主因）**、内存压力 **+0.795**（可忽略）；**C/A = 2.482×** ⇒ 与 P11 观察的 2.8× 同量级。**机制推测**：llama.cpp 的 CPU 解码以 8 线程满负荷读写内存，与上游 torch 每步所需的 H2D/分配/同步争用**内存与总线带宽**（上游在 GPU、下游在 CPU，但**同机同内存**）。**与 P2 互为印证**：P2 实测交叠仅 **1.291×**（理论上限约 1.79×），差额同源 ⇒ **同机交叠达不到上限**。**结论/部署建议**：① 要让交叠接近理论上限，**上游与下游应分处不同机器**（即 QLH 的跨机目标）——同机实验会**低估**交叠收益；② **上游 forward 本身干净**（A/B 折算 1.007/1.023 ms/层，与 P8 的 1.0443、P9 对照的 1.1109 一致）⇒ 主仓 `forward_layers()` 的实现开销**无异常**，v1 的「手动逐层慢 3×」确属跨报告比较之误。⚠️ 机制为**推测**（未用性能计数器验证）；仅一组配置（0.5B / 12+12 层 / gen=32 / CPU 8 线程），`--threads` 与层数的敏感性未扫。**★ 待完成清单（2026-09-18 登记，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-TODO-2026-09-18.json`）** —— 按用户指示「等并行线完事再继续改」，分三组登记：**A. 需改 `src/`（阻塞于并行线的后端/TUI 改动落地）**：**A1 裁层生成器正规化**（把实验区 `cut_layers_generic.py` 收敛到 `relay_contract.TrimPlan` 语义 + manifest，补 P10 查出的「有合同、有校验、没有生成器」缺口；判据=产出物 block_count/层几何正确且能跑通接力）；**A2 架构泛化（仅低风险部分）**（safetensors key 前缀自动探测、架构分派放宽、包装器路径探测；判据=Qwen2.5 行为不变 + Qwen3.5 可 load_layer_range 且数值一致）；**A3 hybrid per-layer mask 支持**（移植 `manual_forward_n`；用户要求**另开票**）；**A4 `forward_layers` 与 compile 的兼容**（收益优先级中低：同进程整段只快 1.318×）；**A5 把 triton-windows 写进 requirements**（平台条件；否则 Windows 上 compile 只能告警回退）。**B. 纯实验区可做（不被阻塞）**：**B1** compile 的长序列 argmax 复验（24→64/141 步）；**B2** 定位 compile 在 Qwen3.5-2B 上崩溃（分离显存规模 vs hybrid）；**B3** 用性能计数器验证 P12 的争用机制（**★ 延后，2026-09-19 用户裁定**：该票需**锁 GPU 时钟**做低噪声对照，而 `nvidia-smi -lgc` 在本机**需管理员权限**（当前无）⇒ **受环境阻挡**；结论暂由 P12 的「同机上下游内存/总线争用」**替代证据**覆盖；恢复条件 = 取得管理员权限可锁频，或改用外部时钟基准）；**B4** 扫 `--threads`×切点对「下游干扰」的敏感性；**B5** 跨机对照（GPU 节点作 CUDA worker vs 作接力上游）——**需 Surface 从节点在线**；**B6** 评估是否值得为 compile 改 `forward_layers`。**C. 环境**：**C1** 修 `.venv-xframe` 的 `transformers_stream_generator` 兼容（当前靠脚本内覆盖 `TRUST_REMOTE_CODE=False` 绕过）。**D. 文档**：**D1** 是否把「同机上下游争用 / 部署应分机」补进 README。**建议顺序**：A1 →（B1/B2 并行）→ A2 → B4/B3 → A3 → B5 → A4/A5/B6/D1。**★ B1/B2 已完成（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b1b2-compile-longseq-2026-09-18.json`）**：**B1（compile 长序列）**：argmax 一致性**成立到 141 步**（24 / 64 / 141 步全部 `argmax_identical=True`、`first_divergence_index=None`）⇒ compile 的**数值**在接力判据下没问题；**但性能随序列长度劣化** —— gen=24 **1.904×** → 64 **1.372×** → **141 → 0.672×（反而慢 1.49×）**。**B2（Qwen3.5-2B 的 compile）**：采用「阶段化打印 + 末尾 `os._exit(0)` 跳过解释器清理」后**全部阶段通过**（`compiled_forward_1` 12.32s 含编译、`_2` 仅 0.19s、peak 3.76 GB）⇒ **此前观察到的"崩溃"实为解释器退出期清理崩溃**（`_PyModule_ClearDict` 堆栈与之相符），**不影响推理结果**，但会让**进程退出码非零**（脚本/CI 会误判）。**真问题**：`torch._dynamo hit config.recompile_limit (8)`，最后一条重编译原因落在 hybrid KV 路径的 `cache_params.layers[9].device is None`（`cache_utils.py:1026 lazy_initialization`）—— **每次调用都可能变化的守卫**把重编译预算打满 ⇒ 触顶后放弃编译、退化为逐段重编译，**同时解释了 B1 的长序列劣化**。**嫌疑分离结论**：**不是显存规模**（3.76 GB、编译与执行均成功）、**也不是「hybrid 不能 compile」**（能）⇒ 而是 **hybrid KV 守卫不稳定**（属 **transformers 上游问题**）。**可执行建议**：① `USE_COMPILE=True` 默认**需加保护**（按 `max_seq_len`/`n_ctx` 阈值或「是否含 hybrid 层」决定）；② 含 hybrid + compile 时注意**退出码**（清理期可能崩）；③ 可考虑向上游上报或「预热固定形状」规避。**附（有价值）**：B2-B（只 compile 前 4 层）因 `Qwen3_5DecoderLayer.forward() missing 1 required positional argument: 'position_embeddings'` 失败 —— **这是「手写逐层前向必错」的第三例**（Qwen2 与 Qwen3.5 各踩过），故未继续；B2-A 已足以证明「能 compile」。**★ B1/B2 落地：compile 保护实现 + Windows GBK 发现（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-guard-2026-09-18.json`；另备 issue 材料 `local_docs/issue-draft-transformers-hybrid-kv-recompile-2026-09-18.md` 与**独立最小复现** `local_docs/repro_transformers_hybrid_kv_recompile.py`（不依赖 QLH，供上游复现）——按用户裁定**待审后提交**）**：按裁定「`USE_COMPILE` 保持 True + 加长序列/hybrid 自动回退」，在 `src/config.py` 新增 **`COMPILE_MAX_SEQ_LEN`（默认 `None` = 不做长度保护）**（v1 曾设为 128，但 `MAX_SEQ_LEN` 默认 4096 ⇒ 会让 compile 永远空转，故 v2 改回 None），在 `src/model_module.py` 的 `_maybe_apply_compile()` 加两道门（长度阈值仅当常量非 None 时生效；**hybrid 架构一律跳过**，该判定有 `recompile_limit` 触顶 + 退出期崩溃的硬证据），并新增 `_is_hybrid_architecture()`。**验证**（`PYTHONUTF8=1`，Qwen2.5-0.5B 非 hybrid + fp16 + limit=None）：日志 `✅ torch.compile 已启用`、`compiled_transformer_set=True`；hybrid 判定纯逻辑单测 `['linear_attention','full_attention']→True`、`['full_attention']→False`；相关测试 **181 passed / 2 skipped**。**★ 过程中暴露并更正的错误（重要）**：第一次验证**未设 `PYTHONUTF8`**，看到 `compiled=False` 后我误判为「长度保护生效」；完整打印日志才发现真因是 **`torch.compile` 自身抛 `UnicodeDecodeError`**（`'gbk' codec can't decode byte 0x94 in position 618: illegal multibyte sequence`）⇒ Windows 非 UTF-8 模式下 torch/inductor/triton 内部按 GBK 解码失败、**静默回退 eager**。**已给该错误补上明确提示**（`_apply_compile` 识别 GBK 并指引 `PYTHONUTF8=1`）。⚠️ 本会话**第三次**「结论回头更正」（P0 的 CPU/GPU 口径 → v1 的「手动逐层慢 3×」→ 本次 GBK）⇒ 已形成规定：**跨报告数字不构成对照，且必须完整看日志再下结论**。**待决**：是否在 requirements/启动脚本层面统一建议 `PYTHONUTF8=1`（主仓无法强制用户启动模式）。**★★ 第四次更正 + v4 策略（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-corrected-2026-09-18.json`；上游调研纪要 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-upstream-compile-research-2026-09-18.json`）**：**B1 的「长序列 compile 劣化（141 步 0.672×）」撤销** —— 原因是 B1 脚本的 compiled 档取 `mgr.model.model`（**未被替换的原始模块**），与主仓「编译版本另存 `_compiled_transformer`」的实现不符 ⇒ 等于两条 eager 相比。**正确测量**（直接调用 `torch.compile(inner)` 返回值）：**非 hybrid**（Qwen2.5-0.5B，gen=141）：eager 12.958 → compile(limit=8) **7.240 ms/step（快 1.79×）** → **compile(limit=64) 5.042 ms/step（快 2.57×，调大 limit 再 +44%）**；**hybrid**（Qwen3.5-2B，gen=64）：compile **恒定慢 1.5×**（vs eager 0.638×/0.66×，head20≈tail20、drift 0.9–1.0）且**调大 limit 无效**。**上游调研结论（决定性）**：transformers 官方文档把 **DynamicCache 标为不支持 torch.compile**（Static Cache 才是 Yes）；代码里 hybrid 层 cache 显式 **`is_compileable = False`**（`cache_utils.py:1108-1110`，注释「The dynamic Attention part makes it non-compilable」）；`Cache.is_compileable = all(...)` 配合 `generate` 的 `using_compilable_cache` ⇒ **官方路径在「hybrid + DynamicCache」下本就不会启用编译** ⇒ **我们的「hybrid 跳过」是遵循官方设计，而非绕过上游 bug**；且 **5.17.0 已是 PyPI 最新、main 上无相关修复 ⇒ 升级无用**；transformers 自己在 chunked prefill 用 `cache_size_limit = 64`（`generation/utils.py:4113-4115`）。**v4 落地**：① **移除** `COMPILE_MAX_SEQ_LEN`（长度保护，基于错误结论）；② 新增 **`COMPILE_RECOMPILE_LIMIT = 64`**，在 `_apply_compile()` 里设置 `recompile_limit`/`cache_size_limit`；③ **保留 hybrid 自动跳过**。**验证**：`compiled? True`、`recompile_limit now = 64`、相关测试 **181 passed / 2 skipped**、`py_compile OK`。**issue 草稿处置**：`local_docs/issue-draft-transformers-hybrid-kv-recompile-2026-09-18.md` 应**改性质或撤销** —— 上游未查到同因 issue，官方已声明 DynamicCache 不可编译 ⇒ 准确定位是「**我方用法越界**（对 DynamicCache 手动 compile）」。**新增待办**：⚠️ `torch._dynamo.config` 在 torch ≥2.12 是 **thread-local**（pytorch #186537）⇒ 主线程设置未必对 autograd/backward 线程生效（本仓影响未验证）；要在主仓**合法**启用 compile 需改走 **static cache + generate 自动编译**（A 类改造，等并行线），而 `forward_layers()` 手动逐层 + DynamicCache 现状下**吃不到整段 compile 收益**。**★★ 第五次更正（同日，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-correction-5-2026-09-18.json`）**：上面把 hybrid 的 `vs_eager 0.638` 读成了「慢 1.5×」——**方向读反了**（该比值 = compile/eager，**小于 1 即 compile 更快**）。**正确读数**：hybrid（Qwen3.5-2B，gen=64）eager **40.808** → compile(limit=8) **26.046 ms/step ⇒ 快 1.57×**（limit=64 为 0.66×、快 1.52×）；drift 0.996/0.899 ⇒ **不随长度劣化**、调大 limit **无额外收益但两档都优于 eager**。⇒ **修正后的总结论：两种架构上 compile 都有效**（非 hybrid 1.79–2.57×、hybrid 1.57×），**「长序列劣化」两处都不存在**；官方 `is_compileable = False` 只影响 `generate()` 的**自动**编译决策，**不禁止手动编译**。⇒ ⇒ **「hybrid 跳过 compile」这条保护会放弃约 1.57× 收益**，其原依据已被推翻；唯一仍成立的 hybrid 风险是**解释器退出期崩溃**（不影响结果但退出码非零）。**是否保留 hybrid 跳过属待用户裁定**（保留＝稳妥但放弃 1.57×；移除＝拿回收益但需处理退出码）。**方法学教训（本会话第五次更正，其中③④⑤连续三次都在 compile 一件事上）**：根因全是**读数/取数错误**（③ 未设 PYTHONUTF8；④ 取错模块；⑤ 比值方向读反）⇒ 新增纪律：**凡涉及「快/慢」的断言，必须先写明「谁的比值」并复算一遍**。**★ v5 实施 + 第六次更正（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-compile-v5-2026-09-18.json`）**：按用户裁定「移除 hybrid 跳过 + 配套处理退出码」，已落地两处：① `_maybe_apply_compile()` 的 **hybrid 跳过改为放行**（提示后继续启用 compile）；② `_apply_compile()` **登记 `atexit`** 回调，在解释器清理**之前**释放 `_compiled_transformer` / `model` / CUDA 缓存。**净效果：hybrid 现在会启用 compile ⇒ 拿回实测的 1.57×**（Qwen3.5-2B，gen=64：26.046 vs eager 40.808 ms/step）；非 hybrid 不变（1.79×，`limit=64` 时 2.57×）。**验证**：`py_compile` OK；相关测试 **181 passed / 2 skipped**；**退出码对照实验**（`check_exit_code.py`，hybrid + compile、8 步、**不使用 `os._exit`**）**两组 rc 均为 0 ⇒ 退出期崩溃未复现**。**★ 第六次更正**：此前把「hybrid + compile 会在解释器退出期崩溃、退出码非零」当作既成事实（并据此加过 hybrid 跳过），但本次对照**未能复现**，且原证据来自另一脚本、那次工具层报的失败还可能混入管道判定 ⇒ **准确表述应为「该风险未证实、未复现」**；`atexit` 释放属**预防性**措施，**不是已验证的修复**。⚠️ 本会话**六次更正**，其中 ③④⑤⑥ **四次集中在 compile 这一件事**（根因全是读数/取数错误）⇒ 纪律再补一条：**对「风险/崩溃」类断言同样必须有对照实验，不能以单次现象定案**。**★ issue 材料处置 + 待办清单 v2（2026-09-18）**：① **issue 草稿已撤销** —— 按用户裁定**移至 `_to_delete/`**（`issue-draft-transformers-hybrid-kv-recompile-2026-09-18.md` 7.1 KB、`repro_transformers_hybrid_kv_recompile.py` 4.2 KB；该目录已 gitignore（`.gitignore:97`），可恢复；**原位置已清空并有核验证据**）。撤销理由：上游调研确认官方已声明 **DynamicCache 不支持 torch.compile**，且 hybrid 层 cache 显式 `is_compileable=False`（仅影响 `generate()` 的自动编译）⇒ 本问题性质是「**我方用法越界**」而非上游缺陷；上游亦未查到同因 issue。② **待办清单升级为 v2**（`local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-TODO-v2-2026-09-18.json`，取代 v1）：分「已完成 / A 类需改 src / B 类纯实验区 / C 环境 / D 文档 / 已撤销与已关闭」六节 + **推荐顺序**。**A 类**：A1 裁层生成器正规化、A2 架构泛化（低风险）、A3 hybrid mask（另开票）、**A4 ★static cache 改造（取代原「forward_layers 与 compile」——因 `forward_layers` 是手动逐层 + DynamicCache，**现状下吃不到整段 compile 的 1.57–2.57× 收益**，正路是改走 `cache_implementation="static"` + `generate` 自动编译）**、A5 triton-windows 进 requirements + 统一建议 `PYTHONUTF8=1`、A6 验证 dynamo config 的 **thread-local** 语义（pytorch #186537）。**B 类**：B3 争用计数器验证、B4 `--threads`×切点敏感性、B5 跨机对照（需 Surface 在线）、**B6 实验区先行验证 static cache + compile 的收益上限（A4 的前置）**、**B7 更重负载下重跑退出码实验（判定 atexit 是否真有必要）**。**C1** 修 `.venv-xframe` 的 transformers 兼容；**D1** README 是否补「同机争用/部署分机」与「compile 适用边界」。**推荐顺序**：B6 → A4；A1；A2 → A3；B4/B3；其余视并行线节奏；**B5 等 Surface 在线**。**★ B6 完成（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b6-static-cache-2026-09-18.json`）**：在**主仓 12 层分段**（Qwen2.5-0.5B，f16+sdpa，gen=64）上对照四条路线，**三方 argmax 逐 token 一致** —— **A** eager+DynamicCache（**主仓 `forward_layers` 现状**）**12.787 ms/step（1.0656 ms/层）**；**B** eager+StaticCache（整段）**16.097**；**C** compile+DynamicCache（整段）**8.198**；**D** **compile+StaticCache（整段）6.180 ms/step（0.5150 ms/层，161.8 tok/s）**。**runtime 直接确认官方立场**：`DynamicCache().is_compileable = False`、`StaticCache(...).is_compileable = True`。**结论**：① **A4 值得开工**（D 相对现状 **2.07×**）；② **收益可分解**：整段+compile 值 **1.56×**，再换 static cache 到 **2.07×**；③ **硬约束：static cache 必须与 compile 成对**（B 单独用是 **0.79× 负优化**），不能只换 cache；④ 现状 A 的 **1.0656 ms/层 ≈ llama.cpp CUDA 1.064 ms/层**（与 P8 结论一致），D 的 0.5150 已**反超 llama 约 2.07×**；⑤ **代价**：显存 **0.6 → 2.4 GB（4×）**、prefill 反而变慢，且 compile 档的 `prefill_ms` **含 `torch.compile` 首次编译开销**（14.8–15.7 s）**口径不干净**。**顺带发现**：`forward_layers` **不接受空 `DynamicCache`**（报「Qwen2 KV cache 层数不匹配: cache=0, local=12」）⇒ 必须传 `past_key_values=None`。**B6 遗留（已进 TODO v2）**：clean prefill 复测（去掉编译开销）、长序列（512/1024）下的收益与显存、**hybrid（Qwen3.5）在 static cache 下的可行性**（其 cache 结构与 Qwen2 不同，未验证）。**★ B8 完成（2026-09-18，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b8-2026-09-18.json`）**：三项遗留全部有答案 —— **① clean prefill**（先以全新 cache 预热触发编译、再新建 cache 正式计时）：B6 的 14.8–15.7 s 确为编译开销；clean 值 **A 27.7/48.2/117.6 ms、C 10.4/29.8/57.8、D 19.9/37.8/63.8** ⇒ 整段前向大幅改善 prefill，而 **static cache 反而略慢**（预分配开销）。**② 长序列**（prefill 128/512/1024，gen=32）：decode **D/A = 3.364× → 2.412× → 2.047×**（收益随序列变长缩水，因 A 手动逐层的固定开销被摊薄：21.9 → 15.9 ms/step）；**static 相对 Dynamic 的优势同样随序列变化** —— 128 时 C 更快（6.061 vs 6.514）、512 持平、1024 时 D 更快（7.767 vs 8.095）。**③ hybrid（Qwen3.5）**：探针表明 **StaticCache 构造成功且 `is_compileable=True`**（full 层 → `StaticLayer`，linear 层两种 cache 都用 `LinearAttentionLayer`；18 linear + 6 full 与 `full_attention_interval=4` 一致）；实跑**跑通**（**A 36.359 / B 25.992 / C 25.981 ms/step**，**argmax 全一致**）⇒ **但 B ≈ C（差 0.04%）⇒ hybrid 上 static cache 无收益**（其 cache 主体是 linear 层递归状态，与 cache 类型无关），且 **C 的 prefill 更慢（59.18 vs 38.47 ms）** ⇒ **hybrid 应保持 DynamicCache**。**★★ B8 更正 B6 两处**：**更正 1（显存结论不可信）**：B6 报 "D peak 2.4 GB（约 4×）" 实为**首次编译峰值**（inductor autotune 临时张量）；B8 clean 稳态为 **A 0.61–0.76 GB / D 0.61–0.65 GB ⇒ D 不高于 A，甚至更低**。**更正 2（表述）**：「static cache 必须与 compile 成对」应表述为「**若用 static cache 就必须配 compile（否则 0.79×）；但完全可以不用 static cache**」——C（compile + DynamicCache，整段）本身即 2.0–3.4× 的强路线。**★ A4 建议据此收敛**：**第一步（必做、主收益）= 整段前向 + `torch.compile`**（非 hybrid 2.0–3.4×、hybrid 1.40×，**显存不变**）；**第二步（可选）= static cache**，仅对**短序列（<128）有明显增益（B6 口径 D/C 1.33×）**、长序列仅约 4%、**hybrid 无益** ⇒ 可按部署画像跳过。**兼容性坑（新增，两处同源）**：**hybrid 不能传空 `DynamicCache()`**（报 `IndexError: list index out of range`，须 `DynamicCache(config=cfg)`）；`forward_layers` 不能传空 `DynamicCache`（须传 `None`）。**★ cache 机制归因（2026-09-18，回应用户三问「static 为什么快 / 是否只是被 compile 掩盖 / hybrid 为何负收益」；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-cache-mechanism-2026-09-18.json`）**：补测此前**从未测过**的「eager+整段+Static」格，并把每档**重复 5 次给 std**，单变量分离后全表（ms/step，mean±std）：**A**(eager/Dyn/逐层) 12.241±1.069 / 12.077±0.854 / 12.361±1.355 / 13.452±0.980；**B0**(eager/Dyn/整段) 11.812±0.345 / 11.649±0.818 / 12.179±0.800 / 11.579±0.873；**B1**(eager/Static/整段) 11.190±0.619 / 9.832±0.882 / 11.270±0.618 / 11.940±0.812；**C**(compile/Dyn/整段) **4.720±0.103 / 4.973±0.078 / 5.891±0.105 / 6.855±0.085**；**D**(compile/Static/整段) 4.783±0.098 / 6.282±**1.402** / 7.865±**2.056** / 6.888±0.210。**归因矩阵**：`整段 vs 逐层` 1.015–1.162×；**`Static vs Dynamic (eager)` 1.06–1.19×（均值约 1.07 ⇒ 微弱正收益）**；**`Static vs Dynamic (compile)` 0.75–1.0×（反而更慢）**；`compile 是否放大 static 效果` 0.67–1.03（<1）。**机制级证据（microbench）**：KV 每 token 每层 512 B（12 层 6144 B），**单层单次 `cat` ≈ 0.016 ms ⇒ 全 12 层 ≈ 0.2 ms/step，仅占 decode 单步的 3–4%** ⇒ **static cache 能省的东西本就微不足道**。**答三问**：**Q1** 它省的只是 DynamicCache 每步 `cat`（拷贝 + 新分配 + 形状变化），实测收益约 7% ⇒ 谈不上「加速」；**Q2** 方向对、但机制要改写 —— **不是「compile 掩盖拖累」，而是 compile 与 static 互斥**（compile 已优化掉 dynamic 的 cat，而 static 的 `index_copy_` / 固定形状 mask 在 inductor 下并不更便宜 ⇒ 转负）；**Q3** **hybrid 的 KV 只存在于 6/24 层**（18 层 linear 是递归状态、**大小与序列长度无关**）⇒ dynamic 的 cat 开销本就只有约 1/4，static 更无从省起；且仍需为全部 24 层预分配 ⇒ prefill 实测慢 54% ⇒ **净负**。**★★ 再更正 B6 两处**：① **「D 比 C 快 1.33×，D 最优」——推翻**：B6 每档仅测 1 次；本轮 5 次重复显示 **D 有偶发异常值**（128: [5.06, 5.149, **8.066**, **7.5**, 5.636]；512: [5.877, 7.386, **10.364**, **9.65**, 6.049]），而 **C 极稳（±0.08）**，D 最好一次也只与 C 打平 ⇒ 1.33× 是**噪声假象**；② **「B 16.097 比 A 12.787 慢 21%」口径不干净、不可用**：B6 的 A 是手动逐层、B 是整段（两变量同变）；单变量分离后「整段」效应仅 1.015–1.162×、「static」效应约 1.07×，相乘也到不了 21% ⇒ **eager 下 static 实为微弱正收益（约 7%）**。**★ A4 第三次修订（范围进一步缩小、更安全）**：**只做「整段前向 + `torch.compile`」，cache 保持 DynamicCache** ⇒ 实测 **2.50× / 2.34× / 2.07× / 1.69×**（prefill 16/128/512/1024）且 **C 的 std 仅 0.078–0.105 ms**；**明确不上 static cache**（compile 下负收益且不稳定）。**方法学（本轮定案的关键）**：补上**缺失的单变量格** + **重复 5 次给 std**（这正是 B6 出错的原因）⇒ 此后凡「A 比 B 快多少」类结论，默认要求「**单变量分离 + 重复测量 + 给出 std**」。**★ TODO-A4 开工（2026-09-18，第一版；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a4-monolithic-semantics-2026-09-18.json`）**：按用户裁定做成**可开关最小版本** —— `config.USE_MONOLITHIC_FORWARD` **默认 False**（既有路径含分布式层流水线**行为完全不变**）；`forward_layers` 的 qwen2 分支增加分派，新增 `_should_use_monolithic_forward()` 与 `_forward_monolithic_qwen2()`，对外契约与逐层版完全一致（`past_key_values` 为 tuple of (k,v) ⇄ DynamicCache，保留 `layer_idx` 本地索引补丁）。**实测三档**（主仓 12 层分段，Qwen2.5-0.5B，f16+sdpa，prefill 64，gen 32，5 次重复）：**A** 逐层+eager **11.184±0.963** ms/step；**B** 整段+eager **10.303±1.309**（1.086×）；**C** 整段+compile **4.811±0.104**（**端到端 2.325×**）。**⚠️ 判据抓到一个真问题**：**B vs A 的逐 token argmax 不一致**（首个差异在 decode 第 1 步）⇒ **整段实现在「分段语义」下不正确**；而 **C vs B 完全一致** ⇒ compile 本身不改数值，差异 **100% 来自语义**。**根因**：`Qwen2Model.forward()` 的返回值**经过 `self.norm`**（这是"完整模型"的语义），而 `forward_layers` 在 `has_lm_head=False` 时返回的是**未过 norm 的 raw hidden** —— 分布式首/中间节点必须如此（norm 只在末节点做）⇒ 整段路径**多做了一次 norm**。**★ 这同时限定了 B6/B8 的适用范围（重要）**：B6/B8 的 C/D 档用的是**整模** `AutoModelForCausalLM`（自带 norm + lm_head，语义天然正确）⇒「compile 相对 eager 2.5×/1.7×」**对整模成立**，但**分段场景不能直接照搬**。**正确修法（下一步）**：把 compile 施加在**层循环**上 —— 编译 `hidden → for layer in layers: layer(...) → hidden`（**不含**末端 norm/lm_head），既与 `forward_layers` 语义一致、也吃到 compile 核心收益；需改 `_apply_compile()` 而非编译 `Qwen2Model`，且**编译函数必须缓存**以免重复编译。**代码内已加警示**（`config.py`：开启前必须先修 norm 语义）；相关测试 **249 passed / 2 skipped**。**★ TODO-A4 完成（层循环版，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a4-layer-loop-2026-09-18.json`）**：**改用「编译层循环」而非编译整模** —— 新增模块级 `_LayerLoop(nn.Module)`（把 `for layer in layers: ...` 包成可编译模块），**前置**（embedding / cache / causal mask / RoPE）与**后置**（norm / lm_head）仍由 `forward_layers()` 负责 ⇒ **语义与逐层版天然一致**；`_apply_compile()` **额外**编译 `self._compiled_layer_loop`（整模 `_compiled_transformer` 保留不动，兼容既有用法），并在**编译之前**把 `layer.self_attn.layer_idx` **永久**本地化（`torch.compile` 会把该属性纳入 guard，原「每次调用临时改再恢复」会让编译版反复重编译）；**开关关时完全走原路径**（含临时补丁 + finally 恢复）⇒ **默认行为零变化**。**实测**（12 层分段，Qwen2.5-0.5B，f16+sdpa，prefill 64，gen 32，**5 次重复**）：**A 逐层（开关关）10.269±1.155** vs **C 层循环+compile（开关开）6.133±0.167 ms/step** ⇒ **加速 1.674×**，且 **逐 token argmax 与逐层版完全一致（首个差异 None）** ✓✓ —— 这正是第一版栽掉的地方。**第一版（整段 `Qwen2Model`）的 2.325× 建立在错误语义上**（多算一次 norm），**1.674× 才是语义正确下的真实收益**；且层循环**共用** ⇒ **首/中间/末节点角色都能受益**（第一版仅首节点）。**口径提醒**：B6/B8 的「compile 整模 2.34×」是**整模**口径（含 embed/norm/lm_head 全链路），本项是**分段**口径（只编译该段的层循环）⇒ 数字略低且**不可直接比较**。**验证**：`py_compile OK`；`pytest -k "model_module or layer_range or compile or config or relay"` **249 passed / 2 skipped**；**全量测试 2756 passed / 1 failed / 13 skipped**，唯一失败为 `tests/test_scheduler.py::TestComputeLayerAssignment::test_disconnect_reshard_uses_only_connected_survivors`，**单进程复跑 3 次全过** ⇒ **并发 flaky**，与本次改动无关。**未验证（已登记）**：hybrid 分段（主仓尚不支持 Qwen3.5 层拆分，见 TODO-A2）、更大模型与长序列（prefill 512/1024）、`layer_idx` 永久本地化的单独用例。**★ B10 完成（A4 扩展验证，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b10-2026-09-18.json`）**：① **长序列**（Qwen2.5-0.5B，12 层分段，f16+sdpa，**gen=141**，3 次重复）：prefill **64/256/512/1024** 的加速比 = **2.082× / 1.613× / 1.928× / 1.994×**，peak 0.60→0.76 GB ⇒ **长序列上收益不衰减**（512/1024 仍约 1.93–1.99×）；② **更大模型**（`models/distilqwen25-ds3-0324-7b`，**model_type=qwen2**，28 层 / hidden 3584，取 **8 层分段**，gen=64）：A 21.0–21.9 vs C 19.1–19.4 ms/step ⇒ **加速仅 1.099× / 1.105× / 1.129×**，peak 4.52–4.63 GB ⇒ ⇒ **模型越大，compile 的相对收益越小**（大模型是计算密集，固定开销占比低）——这与「compile 主要省的是 kernel launch/框架开销」的机制解释一致；③ **`layer_idx` 副作用检查通过**：两个模型均满足「**开关关时恢复原值**、**开关开时为本地索引 0..N-1**」⇒ **无副作用**。**⚠️ 一处必须记录的不一致**：0.5B 的 **prefill=256** 档 argmax 在**位置 100** 分叉（其余三档全一致、7B 三档全一致），且该档 C 的 std 偏大（±1.134）⇒ 判为 **`torch.compile` 非 bit-exact 的数值噪声累积**（分叉点很靠后；若是语义错误会从第 1 步就分叉，如被废弃的第一版整段实现）⇒ **结论：开关打开即引入 compile 的数值差异，凡有「逐 token 一致」验收的场景（如跨框架接力）不得开启**，与 v4/v5 的数值提示一致。**★ TODO-A1 完成（裁层生成器正规化，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a1-cut-layers-2026-09-18.json`）**：新增 **`scripts/cut_layers.py`**（主仓首个裁层生成器 —— 此前只有 `src/relay_contract.py` 的合同与校验、`src/pipeline_node_contract.py` 的节点校验，**没有生成器**，只能靠实验区脚本临时承担）+ **`tests/test_cut_layers.py`**（自包含：现场合成 4 层迷你 GGUF；`gguf` 缺失时 `importorskip` 整体跳过）。**语义与 `RelayTrimPlan` 完全对齐**：保留 `blk.K..blk.(N-1)` 并重命名为 `blk.0..`、`<arch>.block_count` 改写为 `N-K`、非 blk 张量（token_embd/output_norm/output）原样保留、其余 KV 全部复制；提供 `--dry-run`（只列影响、不写文件）、`--manifest`（含 `trim_layers`/`kept_block_count`/`artifact_sha256`/`contract.trim_plan_valid`/`local_to_source_layer_0`）、`--verify-manifest`（对照校验既有产物）。**防护**：`0 < K < n_layer`；**hybrid 架构下 K 必须是 `<arch>.full_attention_interval` 的整数倍**（否则层类型错位、llama.cpp 无法加载）；缺 `--k` / 源文件不存在 / 非法 K 均 `rc=2`。**★ 关键验证**：用新脚本对 `out/qwen25-05b-f16.gguf` 做 K=12 裁层，产物与实验区既有 `-cut-k12.gguf` **sha256 逐字节相同**（`B754A8FD7D483693`，两者均 606.7 MB）⇒ **正规化版与实验版行为完全等价**；manifest 的合同字段 `trim_plan_valid=True`、`kept_block_count=12`、`local_to_source_layer_0=12`、`last_handoff_layer=22` 均正确。**测试**：`pytest tests/test_cut_layers.py` **10 passed**（无 gguf 环境 → 1 skipped）；`relay or contract or pipeline or cut_layers` 合计 **513 passed / 1 skipped**（无回归）。**依赖发现（并入 TODO-A5）**：`gguf` 此前**不在任何主仓环境**（`.venv-test` 与系统 python 均无，仅实验区 `.venv-xframe` 有）⇒ 已给 `.venv-test` 安装 `gguf==0.19.0`（纯 Python、无编译依赖）让测试真正执行，并**建议写入主仓依赖声明**。**★ TODO-A2（部分）完成 —— 层前缀自动探测（2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a2-prefix-detect-2026-09-18.json`）**：按用户裁定**只做 key 前缀自动探测**（低风险、纯加载侧）。`_load_qwen2_layer_range` 此前硬编码 `model.layers.{i}.` / `model.norm.` / `model.embed_tokens.`（docstring 亦声明「key layout 由 Qwen2 共享」），而**实测 `models/qwen3-5-2b` 的 key 为 `model.language_model.layers.*`**（另有 `model.visual.*` 视觉塔）⇒ 硬编码会**静默匹配不到任何张量**。新增模块级 `_iter_safetensors_keys`（优先 index.json 的 weight_map，否则逐个 shard `safe_open(...).keys()`，只读 header、不物化张量）+ `_detect_qwen_root_prefix`（正则 `^(?P<root>.*\.)layers\.\d+\.` 统计各 root 出现次数、取最多者；异常或无匹配一律回退 `"model."`）；`_load_qwen2_layer_range` 改用探测到的 root 拼 `layers.` / `norm.` / `embed_tokens.`，`layer_prefix` 同步。**实测**：`qwen2.5-0.5b-instruct → model.`（**与默认一致 ⇒ 行为逐字节不变**）、**`qwen3-5-2b → model.language_model.`（正确，且未误选 `model.visual.`）**、`distilqwen25-ds3-0324-7b → model.`；主仓 `load_layer_range(0, 12)` 加载 Qwen2.5-0.5B **成功**（12 层 / total=24 / fp16）。**测试**：新增 `tests/test_layer_prefix_detection.py`（7 用例）**7 passed**；`pytest -k "model_module or layer_range"` **114 passed / 2 skipped**。**范围说明（重要）**：**分派放宽**（让 `qwen3`/`qwen3_5` 等进入加载器）与**包装器路径探测**（`model.model.language_model.layers`）**未做** —— 它们直接牵动 forward 契约（`forward_layers` 仍只支持 `model_type == "qwen2"`，且 hybrid 的 per-layer mask 需 A3 才能正确前向）⇒ **与 A3 合并一次完成**，以免产生「能加载、不能前向」的中间态；本项是 **A3 的必需前置**（没有正确前缀，A3 连权重都加载不出来）。**★★ TODO-A3 完成（2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a3-qwen35-2026-09-18.json`）**：**主仓现在能加载并前向 Qwen3.5（hybrid）的分段，且数值与 transformers 整模一致**。**六个关键契约差异**（都是读 transformers 源码 + 实测探明）：① **每层 mask 不同** —— full_attention 用 `create_causal_mask`、linear_attention 用 `create_recurrent_attention_mask`，按 `config.layer_types[i]` 查表；② **`Qwen3_5TextModel.forward` 会 apply `self.norm`** ⇒ 与 Qwen2Model 一样**不能整段调**，必须手动逐层（A4 的教训同样适用）；③ **key 与属性路径差一层** —— safetensors 是 `model.language_model.layers.*`，而 `Qwen3_5ForCausalLM` 的文本塔直接挂在 `model.`（`model.layers.*`）⇒ 需 key 归一化；④ **`layer_idx` 持有者名随架构变** —— Qwen2 是 `self_attn`，Qwen3.5 的 linear 层是 `linear_attn`（`Qwen3_5GatedDeltaNet`）⇒ 补丁按属性探测；⑤ **MRoPE 需 3 维 position_ids** —— rotary 用 `(3, bs, seq)`、层用 `(bs, seq)`，transformers 的做法是展开成 `(4, bs, seq)` 再取第 0 维；⑥ **空 `DynamicCache()` 对 hybrid 会越界** ⇒ 需 `DynamicCache(config=cfg)`（旧版 transformers 无该参数 ⇒ 用 `_new_dynamic_cache` 跨版本兼容）；另有 **多模态 config 嵌套**（`num_hidden_layers` 在 `text_config` 下）。**实现**：新增 `_QWEN_LAYER_RANGE_TYPES` / `_locate_text_transformer` / `_layer_idx_holders` / `_new_dynamic_cache`，`load_layer_range` 与 `forward_layers` 分派放宽，hybrid 时按 `layer_types` 建 mask 映射并逐层取用、MRoPE 展开、key 归一化（`model_key_of`）。**验证**：主仓 `load_layer_range(0, 24)`（全 24 层，f16，约 4 GB）+ `forward_layers` vs transformers 整模 —— **argmax 一致（109705）**，logits `max|diff| = 0.0043`、`mean|diff| = 0.00036`（f16 累积误差量级）⇒ **语义正确**。**回归**：`-k "model_module or layer_range or prefix or relay"` **192 passed / 2 skipped**；**全量测试 2768 passed / 19 skipped / 0 failed**。**未做**：视觉塔语义（主仓只做文本层流水线）；Qwen3.5 的 linear 层 cache 在**跨节点**层流水线传递时的格式未验证（可作后续项）。**★ TODO-A5 完成（依赖与环境整理 + transformers 5.x 升级，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a5-deps-tf5-2026-09-18.json`）**：① **依赖按用户裁定「拆两份」** —— `gguf>=0.10` 进主 `requirements.txt`（`scripts/cut_layers.py` 是主仓工具，属硬依赖）；**新增 `requirements-compile.txt`** 放 `triton-windows>=3.8,<3.9 ; sys_platform == "win32"`（**可选加速**，Linux 不需要；`pip install --dry-run` 验证可装 `3.8.0.post28`，正是实测可用的版本）；**`PYTHONUTF8=1` 早已就绪**（`qlh.bat`/`koakuma.bat`/`bjtu.bat`/`start_backend.bat`/`start_tui.bat` 第 3 行）无需改动；`requirements-lock/*.txt` 是 `setup_envs.py --snapshot` 的 pip freeze 产物（头注「勿手改」）⇒ **未手改**。② **`.venv-test` 从 transformers 4.47.1 升到 5.17.0**（连带 `huggingface_hub` 0.36.2→1.32.0、`tokenizers` 0.21.4→0.23.2），备份 `out/pip-freeze-before-tf5.txt` 可回滚。**升级过程中揪出并修掉 4 类问题**：**(a)** **A3 的 hybrid 判定过宽** —— transformers 5.x 给 `Qwen2Config` 也加了 `layer_types`（实测 24/24 全 `full_attention`），只判断「有无」会把纯 full-attention 模型误判为 hybrid ⇒ 走进 MRoPE/逐层 mask 分支致 RoPE 维度错乱（影响 5 个用例）；修法：新增共享判定 `_is_hybrid_layer_types`（仅当存在既非 `full_attention` 也非 `sliding_attention` 的层才算 hybrid）。**(b)** `DynamicCache` 构造在假 config（`SimpleNamespace`）上抛 `AttributeError`（5.x 会调 `config.get_text_config()`）⇒ `_new_dynamic_cache` 与 `gemma4_pipeline_adapter._cache` 均兜住 `(TypeError, AttributeError)`（影响 2 个用例）。**(c)** **5.x 加载 remote code 时 `transformers_stream_generator` 导入失败** —— 5.x 的 `dynamic_module_utils.check_imports` 在加载**任何** remote code 时会 import 其声明的依赖，**非 "No module named" 的 ImportError 直接抛** ⇒ 连模型都加载不了；**新增 `src/transformers5_compat.py`** 把缺失的 5 个符号补回（transformers 顶层 4 个 + `transformers.generation.utils` 1 个，占位类 **fail-loud**），在 `model_module` 导入 transformers 后安装。**(d)** **既存的测试卫生缺陷**（**与版本无关，5.x 只是让它暴露**）：`test_island_engine` 把 `sys.modules["transformers"]` 换成桩且**不恢复**（随后在假 transformers 下 import `model_module` ⇒ 桩类被绑定进该模块）⇒ 同会话后跑的用例看到「假 transformers」；修法：① 该处 `finally` 复原注入的桩（含 `sys.modules["transformers"]` / `["model_module"]` / `torch.nn`）；② **新增 `tests/conftest.py`** 加 `pytest_runtest_setup` 兜底 —— 每个用例前确保 shim 符号在位（⚠️ 判定必须基于「**shim 符号是否存在**」而非「模块是否存在/是不是桩」，实测被污染时 transformers 仍是真模块、只是少了符号）。**验证**：单进程全量 **2768 passed / 18 skipped / 1 failed**，唯一失败为早已确认的**并发 flaky**（`test_scheduler` 的 `test_disconnect_reshard_uses_only_connected_survivors`，复跑 3 次全过）；**生产路径 shim 生效**（`import model_module` 后 `hasattr(transformers,'DisjunctiveConstraint')` → True、`transformers_stream_generator` 可导入 ⇒ **Qwen-1.8B 的 remote code 依赖检查能通过 ⇒ 无需退役**，但用户给的「处理不了就退役」仍作备选保留）。**未做**：主运行时仍锁 4.47.1（新增 TODO-A7 评估）；Qwen-1.8B 的 5.x **运行期**实测（新增 TODO-B15，本次只验证了导入关）。**★ TODO-B14 完成（编译层循环支持 hybrid 的 per-layer mask，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b14-hybrid-layer-loop-2026-09-18.json`）**：**hybrid（Qwen3.5）现在能走编译层循环**，把 A4 的收益带给了它。**做法**：`_LayerLoop` 新增 `mask_index_per_layer`（每层的 mask 索引），`forward` 里 hybrid 时从 `attention_mask` **元组**按索引取本层 mask —— **用「元组 + 静态索引」而非 dict**（字典的字符串 key 对 `torch.compile` 的 guard 不友好）；新增共享辅助 **`_hybrid_mask_index_per_layer()`** 给出（去重层型顺序, 每层索引），让 `_apply_compile`（编译进层循环）与 `forward_layers`（构造元组）**同源调用** ⇒ 避免两边顺序错配导致 **mask 张冠李戴**；并去掉「hybrid 不走编译层循环」的跳过。**⚠️ 一处关键修正**：走编译层循环时 `position_ids` 必须传 **`layer_position_ids`**（`(bs, seq)`）而不是 MRoPE 的 `(3, bs, seq)` —— 此前 hybrid 不走它，所以这个错配**一直没暴露**。**实测**（Qwen3.5 24 层 / prefill 32 / gen 32 / 3 次重复）：逐层 **41.058±2.542** → 编译层循环 **32.349±1.245 ms/step** ⇒ **加速 1.269×**，且 **逐 token argmax 一致**（首个差异 None）。**★ 顺带修掉一个 A3 遗留 bug**：**hybrid 的 decode 阶段 KV cache 层数不匹配**（`cache=6, local=24`）—— linear_attention 层**不写 KV**（用 recurrent state）⇒ 收集侧**跳过** None 导致 tuple 只有 6 项、**下标错位**；且 `DynamicCache(config=...)` 是按 **config** 建槽的，而分段加载时 config 仍是**完整模型层数**（如 4 层模型取 2 层）⇒ 多出的槽也是 None。修法：**收集侧保留 None 占位**且**只取本段实际层数**（`cache.layers[:len(transformer.layers)]`）、**写侧跳过 None**。**A3 只验证了 prefill 所以没暴露，是 B14 的 decode 验证抓到的**（教训：分段前向的验证必须覆盖 decode）。**验证**：`-k "model_module or layer_range or prefix or relay or compile"` **195 passed**；**全量 2768 passed / 18 skipped / 1 failed**（唯一失败为已知并发 flaky）。**口径提醒**：A4 的 1.674× 是非 hybrid（Qwen2.5 12 层 / prefill 64）口径，B14 的 1.269× 是 hybrid（Qwen3.5 24 层 / prefill 32）口径 ⇒ **不可直接比较**；hybrid 的 18 层是 linear_attention（GatedDeltaNet），可融合点比纯 attention+MLP 少，故收益偏低属合理。**★ TODO-B13 完成（Qwen3.5 的 linear 层 cache 在跨节点层流水线传递时的格式验证，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b13-hybrid-pipeline-2026-09-18.json`）**：**结论是「段间不需要传任何 cache」** —— 契约本就如此（`src/model_module.py:3039` 的 `past_key_values` 索引对应**本节点本地层**；`src/scheduler.py:1090` 的 `_kv_cache` 注释为「本节点层范围的 KV」）。hybrid 的 `linear_attention` 层**不写 KV**、用 conv/recurrent state，**只依赖本层的输入序列** ⇒ 只要 **prefill 阶段上游把完整序列的 hidden 交给下游**，下游就能从 token 0 正确累积递归状态 ⇒ **hybrid 不引入额外的跨节点状态传输需求**（与纯 full-attention 模型的契约相同）。**实测**（同进程，Qwen3.5-2B，split=12，prefill=16，gen=32，repeats=3，CUDA f16，compile 关闭）：上游 0-11 **25.251 ± 0.232** ms/步、下游 12-23 **23.960 ± 0.602** ms/步、合计 **49.211 ms/步**；**逐 token argmax 与单段 0-24 完全一致**（33 个 token，首个差异 = None）。⚠️ 对照口径：单段 vs 两段是「同进程、同脚本、同输入条件」（单段口径 A3 已证与 transformers 整模 argmax 一致）；**耗时只在两段侧测量**（8GB 卡放不下单段 4GB + 两段 4GB 同时驻留 ⇒ 单段跑完先完全释放），参照侧仅用于 token 对照。**★ 副产品（主仓真缺陷，未修，已登记 TODO-B16）**：**tied 模型（Qwen3.5）无法做「末节点」** —— `load_layer_range(..., has_lm_head=True)` 抛 `RuntimeError: qwen2 分层权重不完整: lm_head.weight`（`src/model_module.py:1698`），根因是 **Qwen3.5 用 tied embeddings，safetensors 里没有 `lm_head.weight`**；且 `has_embedding=False` 时本地也没有 `embed_tokens`（被置 None）⇒ 末节点连可借用的 tied 权重都没有。影响面：**跨框架接力不受影响**（下游是 llama.cpp，不走该校验），受限的是**纯 PyTorch 层流水线做末节点**；修法是 tied 模型跳过 `lm_head.weight` 完整性校验、并把末节点 lm_head 解析为共享的 `embed_tokens`。**★ 项目报告与数据汇总（2026-09-18，入库 `docs/`）**：新增 `docs/跨框架层接力-项目报告.md`（三代架构演进与各自否决依据、为什么纯工程不可行、转向半科研半工程的定位与纪律、24 条困难与坑、与「不接力」的诚实边界）与 `docs/跨框架层接力-实验数据汇总.md`（按主题分组的数字 + 来源 + 测试条件 + 「不可直接比较」标注 + 12 条口径冲突）；两份均声明**持续更新**、生产门仍 fail-closed。**Android 节点角色核查结论（2026-09-18）**：Android 是 **llama.cpp RPC「借算力」后端**（`android/app/src/main/cpp/CMakeLists.txt:49-50` `GGML_RPC ON`；`qlh_llama_jni.cpp:841-903` `ggml_backend_rpc_start_server`），**不是层接力节点**；主仓在层分配与层拆分两处**硬性排除** Android（`src/scheduler.py:3754`「阻止 Android 节点被分配层（Android 无 PyTorch 推理能力）」、`:2620`「仅 PC 节点参与层拆分」）；其唯一计算形态是整请求级 `full_inference`（`android_full_worker`，加载整模）。⚠️ 术语更正：llama.cpp RPC 里**加载整模的是 host 侧**，Android backend 只提供设备/内存（张量由 host 按 split 推送）⇒「模型分片」在 Android 侧指的是 llama.cpp 的 `--rpc` + `tensor_split`，**不是**主仓那套「层拆分 + hidden 传输」。票 `CORE-LLAMA-ANDROID-01`：**P0 已完成（2026-09-16）；P1 开发门已补齐，设备门待 ARM64 环境**，**真机硬门仍未完成**（开发机不是 Android 真机）⇒ 名义活跃、实质被设备阻塞。**★ TODO-B12 完成（README 补充 compile 开关与「非逐位一致」注意事项 + 新增实验数据图集，2026-09-18）**：在 `README.md` 与英文版 `docs/README.en.md` 的「与全 llama+CUDA 的公平对照」之后新增一节 **「### torch.compile 与「层循环」开关（`USE_COMPILE` / `USE_MONOLITHIC_FORWARD`）」**：① 两开关默认值与语义（`USE_COMPILE=True`；`USE_MONOLITHIC_FORWARD=False`）；② **为什么只编译「层循环」而非整模**（`Qwen2Model.forward()` 的返回值会过 `self.norm`，而分段前向在 `has_lm_head=False` 时必须返回**未过 norm** 的 raw hidden；第一版整段编译得 2.325× 但**多算一次 norm**、argmax 从 decode 第 1 步分叉 ⇒ 该数字已废弃）；③ **实测收益表**（A4 非 hybrid **1.674×**、B14 hybrid **1.269×**，均逐 token argmax 一致）+ 口径不可比声明；④ **三条边界**：compile 与 eager **非逐位一致**（f16 1 ULP）但**不得表述为「compile 更差」**（上游对 float64 基线时 rtol 反而更好）、**有「逐 token 一致」验收判据的场景不得开启**、Windows 需 `PYTHONUTF8=1` + `triton-windows`（缺一不崩，只是没收益）。**中英双语一致性检查 `scripts/check_readme_l10n.py` 通过**。**同时新增实验数据图集**：新脚本 `build/cross-framework-layer-poc/make_figures.py` **直接从** `local_docs/CORE-RELAY-XFRAME-02-*.json` 生成 **7 张图**到 `docs/figures/cross-frame-relay/`（P0 切点扫描 / `-ngl` 曲线 / A4+B14 compile 收益 / 权重占比 / 优化总账 / 起点基线 / 同机争用），已纳入 `docs/跨框架层接力-项目报告.md` §6.4 与数据汇总；图注统一带**来源与测试条件**，字体缺失符号（⚠️ / ⇒）已替换为 ASCII 以避免豆腐块。**★ RPC「借算力」机理查证（2026-09-18，只读子 agent 读上游 `ggml/src/ggml-rpc/ggml-rpc.cpp` 1974 行，pin `47e1de77a`）**：**host（主节点）是唯一模型所有者 + 调度者 + 内存需求方；server（从节点）是被远程安装的 ggml 设备 + 内存提供方 + 计算执行方**。① **权重**：**host 加载完整 GGUF**，再按 `--tensor-split` 把被选中的权重张量**推给**从节点（`RPC_CMD_SET_TENSOR`，字节传输；>10 MiB 时先发 hash，命中 server 缓存则**一字节都不传**）⇒ **加载阶段内网传的是权重张量**；② **运行中传的是「指令」为主**：`GRAPH_COMPUTE`（图描述，无张量数据）/ `GRAPH_RECOMPUTE`（只带 device）；张量字节只在 ① 用户 I/O（`SET/GET_TENSOR`）② **split 边界 activation** 出现，而 **KV cache 常驻远端、不回传**；③ **交互模式** =「host 告诉从节点算什么（图描述）+ 必要时贴数据；从节点算完**不返回值**，需要时 host 再 `GET_TENSOR` 拉回」（同步靠 TCP 顺序 + server 单连接串行，`synchronize` 是 no-op）；④ **容量语义**：权重是**加载期一次性、按比例静态归属**，无运行期迁移/按需/重分片，每节点必须**各自装下自己那份**（远端 `alloc_buffer` 失败即 host 抛异常）⇒ **能「摊到多台」，但不能「内存池化」**；⑤ **与层接力的本质差别**：RPC = 一个 host 独占模型、把远端当设备（无层段合同、无 hidden 传输语义、无容错，上游自述 PoC/fragile/insecure）；层接力 = 每节点各自持有一段层的权重 + 自己的 KV，每步**显式把 hidden_states 当消息**传递。**用户对 RPC 持保留态度、将来可能把 Android 纳入层接力；「layer 接力 vs RPC 哪个好」留待将来开发后实测。**★ TODO-B16 完成（tied embeddings 模型的「末节点」支持，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b16-tied-lastnode-2026-09-18.json`）**：**问题**——tied 模型（Qwen3.5）的 safetensors 里**没有** `lm_head.weight`（与 `embed_tokens` 共用），而 `_load_qwen2_layer_range` 的完整性校验集由 `selected_prefixes` 派生、`has_lm_head=True` 会把 `lm_head.` 放进去 ⇒ 必然抛 `qwen2 分层权重不完整: lm_head.weight`（**由 B13 的实验发现**）。**修法**（`src/model_module.py`，最小侵入三处）：① 新增 **`_is_tied_word_embeddings(config, model_path)`** —— `config.tie_word_embeddings` 显式即定论，字段缺失时扫 safetensors key（无 `lm_head.weight` 且有 `embed_tokens.weight` ⇒ tied），探测失败一律返回 False ⇒ **对既有非 tied 模型行为完全不变**；② tied 且 `has_lm_head=True` 时**加载 `embed_tokens`** 且**不**把 `lm_head.` 放进 `selected_prefixes`（⇒ 不入校验集 ⇒ 不误报 missing）；③ 加载后**重绑** `lm_head.weight = <embed_tokens 的同一个 Parameter 对象>` —— 因为 `set_module_tensor_to_device` 会**替换** Parameter 对象而**破坏 tied 共享**（`lm_head.weight` 会留在 meta），重绑同一对象 ⇒ **共享、零额外内存**。**验证**（Qwen3.5，prefill 16，CUDA f16）：`has_lm_head=True` **加载成功**、`lm_head.weight` **正确物化**（非 meta，`248320×2048` f16 cuda）、`forward_layers()` **直接返回 logits**，且 **argmax 109705 与「`has_lm_head=False` + 脚本补 norm/tied-embed」口径完全一致**；**另一个 tied 模型 `qwen2.5-0.5b-instruct` 同样可做末节点**（其 `tie_word_embeddings` 也是 True —— 此前 P8/P11 一直用 `has_lm_head=False` 所以没踩到）。**测试**：新增 `tests/test_tied_word_embeddings.py`（**7 passed**，纯逻辑/离线，覆盖显式 True/False、字段缺失时的 key 推断、多模态前缀、探测失败静默、无 key 目录）；既有 `model_module/layer_range/tied/prefix` 回归 **135 passed**；**全量 2775 passed / 18 skipped / 1 failed**（唯一失败为已知并发 flaky）。**注意**：tied 末节点会**多加载一份 `embed_tokens`**（Qwen3.5 ≈ 1 GB f16），这是接力的必要条件而非缺陷。**★ TODO-B11 完成（prefill=256 异常档复核，2026-09-18；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b11-2026-09-18.json`）**：B10 的「**只有** prefill=256 分叉 + 该档 std 偏大 ±1.134」经**三路独立测量**复核后**两项均作废**。**方法**：① `isolate`（单档、独立进程、dynamo 全新）；② `inorder`（独立进程内复刻 B10 的 64→256→512→1024 顺序）；③ **直接重跑 B10 原脚本** `bench_a4_extended.py`；以 token 序列 sha256 跨进程判等。**结果（first_diff 位置）**：B10 原记录 `256(100)`；本次复现 `256(100) + 512(105)`；B11 inorder `256(100) + 512(105)`；B11 isolate256 `分叉(100)`、isolate512 **一致**。**结论**：① **「仅 256 分叉」不复现** ⇒ 分叉不是 256 这一形状的属性（**不按「特定形状风险」登记**）；② **分叉位置确定性可复现**（256→100 在**四处**独立测量一致、512→105 在两处一致；档内每次 rollout 的 hash 完全相同）⇒ 是**输入相关的确定性数值差**，非随机噪声；③ **同一档位的 compile 结果还依赖编译上下文** —— 512 档 isolate（C hash == A hash）与 inorder（分叉 105）**不同**，而 256 档两者完全相同 ⇒ **无法用「形状白名单」规避**；④ **B10 的「256 档 C_std ±1.134」未复现**（复现 **0.015**，小约 75×），且本次复现里**分叉档 std 最小、未分叉的 1024 档反而更大**（0.154）⇒ **std 与分叉无因果**，属当次环境干扰；⑤ B10 的「256 档加速最低 1.613×」也未复现（本次 1.731×，与 64 档 1.739× 持平）。**对 B10 的更正**：`anomaly.what`（「只有 256 分叉」「std 偏大」）作废；`verdicts[512].argmax_match=true` 作废（本次 false）；`anomaly.reading` 的「compile 数值噪声累积」**部分成立**（分叉确属 compile 与 eager 的数值差），其 `not_investigated`（std 是否由首次编译残留导致）**否证**（尖峰数全 0，warmup 已覆盖形状）。**保留并加强**：编译与 eager 数值不等价是**普遍的**，分叉位置**输入相关且确定性可复现** ⇒ **任何要求「逐 token 一致」的场景一律不得开启该开关**。**时间线旁证（未做因果对照）**：B10 测于 09-18 **16:39**，当时 `_LayerLoop` 为 A4 版（commit `aede823`，**16:35**）；现行已被 B14 改写（commit `197e06a`，**19:51**，新增 `mask_index_per_layer`）⇒ 编译图不同，分叉档位集可从 {256} 变为 {256,512}。**未做**：未在 A4 版代码上重跑（因果未证）；未扫更多 prefill/prompt（分布未知）；未定位数值差的来源算子。**★ TODO-B11 续 · 分叉成因探针（2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b11-fork-cause-2026-09-19.json`）**：在 256 档（跨进程 hash 稳定）上做逐 token 探针，**给出分叉的完整机制** —— ① **数值差从第 1 步就存在**（compile 与 eager 的末位 hidden `max|diff| = 0.011719`），随序列**逐步放大**到分叉附近 **2.639648（225.245×）**；② **翻转发生在 argmax 的「平局」处** —— 分叉步附近的 top1−top2 logit 间隔 A = `[0.05273, 0.15039, 0.00293, 0.00488, 0.00977, 0.04395]`、C = `[0.05273, 0.14941, **0.0**, 0.15527, 0.05566, 0.04004]`（**C 在分叉步 gap = 0**，完全平局）⇒ **当累积差超过该步的 logit 间隔时 argmax 必然翻转**，因此**分叉位置由「输入本身的 gap 分布」决定**（这同时解释了「位置确定性可复现」与「前期几十步不翻转」）；③ **`torch._dynamo.reset()` 后重新编译，C2 的 token hash 与 C 完全相同、first_diff 仍为 100** ⇒ **否定「编译上下文是 256 档分叉原因」**。**可操作推论**：分叉位置**可复现但不可预测**（除非先算 gap）；**凡要求「逐 token 一致」的场景一律不得开启该开关**（现已有机制层面解释）；若确需开启，**可监控每步 top1−top2 间隔** —— 当该步 gap 小于数值差量级时，该步 argmax 不可信。**未做**：P3（`TORCHINDUCTOR_MAX_AUTOTUNE=0`）⇒ **512 档的「跨进程结果不同」成因仍未定位**（256 档不受 dynamo 重置影响，512 档受影响）；未测 fp32 上游（可作第 3 个机制佐证）。**★ TODO-B5 完成（跨机对照，2026-09-19；报告 `local_docs/CORE-RELAY-XFRAME-02-b5-2026-09-19.json`）**：**动机**——P2/P12 全是同机数据（上游 GPU torch 与下游 CPU llama.cpp 同机争用；P12：上游 +17.726 ms/step、C/A=2.482×）。**装置**——上游 = **主仓引擎**（按用户裁定）`load_layer_range(0, 4)` + `forward_layers()`（CUDA f16；4 层 = Qwen3.5 `full_attention_interval` 的最小整倍数）；下游 = `llama-relay-gen-dl.exe` + `qwen35-2b-f16-cut.gguf`（保留 L4-23，20 层）；传输 = loopback TCP + **`ssh -L`**（`RelayTcpClient` 强制 loopback-only）；对端 = **Surface**（tablet-2tlucnu8，Tailscale 100.100.52.106，16 GB，**无 CUDA**）；prompt `The capital of France is`，gen=32。**单序列（ms/step）**：**threads=4：跨机 173.038（3 次 197.663/161.168/160.282）vs 同机 209.818（210.579/205.923/212.953）⇒ 跨机快 1.213×**；**threads=2：跨机 221.664（192.255/221.329/251.407）vs 同机 196.420（189.366/201.145/198.749）⇒ 同机快 1.129×**；**各自最优：跨机 173.038 vs 同机 196.420 ⇒ 跨机快 1.135×**。⚠️ **两个方向都出现 ⇒ 主变量是「下游 `--threads` × 空闲核数」，不是「同机 vs 跨机」**（Surface 少核平板需要更多线程：2→4 使跨机快 28%；本机多核 4 线程反而慢 6.8%，符合同机争用）。**交叠**：**同机 serial 196.420 → threaded 146.501（1.341×，有效）**；**跨机 serial 221.664 → threaded 219.402（1.01×，无效）** —— 收益来自「两条序列的下游能否各自占住**独立核**」（本机 `--threads 2` 仍有富余核故真并行；Surface 的 2+2 线程近占满故互抢）；**且本配置上限本就 ≈1.01**（切点 N=4 ⇒ 上游仅 ~2 ms/步、下游 ~200 ms/步 ⇒ 上游只占 ~1%，交叠上限 = (上+下)/max(上,下) ≈ 1.01，与实测吻合；对照 P2 的 1.291× 是因为它上下游耗时**相当**，72.3 vs 56.8 ms）。**数值**：跨机/同机/各 threads 档的 32-token 贪心序列**全部相同**（`tokens_match_first_run` 全真）⇒ 隧道只搬运字节、无副作用。**结论/部署含义**：边缘集群「上游 GPU 节点 + 下游弱 CPU 节点」时，**先把层推给 GPU 侧（提高切点，P0 结论），再看是否需要多序列并发；不要指望靠交叠兜住一个过慢的下游**。**⚠️ 会话中途更正**：曾据 threads=4 单组数据写成「跨机比同机快 1.213× ⇒ 印证同机争用」，**该结论不完整**（threads=2 时方向相反），已更正。**未做**：① 切点未扫（只有 N=4）；② Surface 核数未确认、threads>4 未测；③ **原定义的「GPU 作 llama.cpp CUDA worker」路径本机不可测**（只有一台 CUDA 机器，对端无 CUDA 且只有裁层权重 ⇒ 需第三台设备）；④ 跨机 RPC 未排入；⑤ 正确性基线（整模 llama.cpp 贪心）未取 —— 本次上游是主仓 f16+默认 sdpa，与 XFRAME-01 的 f32+eager 口径不同，故 token 与旧报告不同（**非回归**）。⚠️ 两侧 runner 二进制不完全同版本（6569906 vs 6569394 B），但 tokens 完全一致。**★ TODO-B4 完成（`--threads` × 上游层数对「下游干扰」的敏感性，2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b4-2026-09-19.json`）**：**装置** —— 上游 = 主仓引擎 `load_layer_range(0, L)` + `forward_layers()`（f16, CUDA）；下游 = llama.cpp 裁层（`cut_layers_generic.py --k L`，与上游互补；本票新生成 0.5B 的 k=4/8/16 工件）；网格 **L ∈ {4,8,12,16} × T ∈ {2,4,8} = 12 组**，每组**独立子进程**，gen=32、每变体重复 2 次。**关键实验设计修正（v3）**：v2 的 warmup **只覆盖 prefill**，而 compile 按**形状**编译 ⇒ **decode 的首次编译落在第一个 decode 变体上**（v2 smoke 实测 A=10.600 反慢于 B=3.692，`B−A = −6.908`）⇒ v3 把 prefill **与一次单 token decode** 都 warmup。**结果矩阵 `C−B`（ms/step）**：L=4 → 1.163 / 2.242 / 3.300；L=8 → 2.652 / 1.567 / 6.235；L=12 → 2.250 / 2.976 / **8.435**；L=16 → 2.167 / 2.953 / **9.438**（T=2/4/8）⇒ **方向上「线程越多干扰越大」**。**但跨运行不可复现**：同一配置 L=12/T=8 反复独立测得 **C−B 跨度 −1.036 ~ +8.435（9 倍）**、**A 本身跨度 8.854 ~ 16.579（1.9 倍）** ⇒ **单点测量无意义**。**根因（决定性，nvidia-smi 证据）**：**GPU SM 时钟在 210–780 MHz 之间大幅波动**（`clocks.max.sm = 3105`，即只跑低频档），且**任务期间利用率仅 40–60% 时就开始降档**、`throttle_reasons = 0x1`（GpuIdle）；跨运行的「时钟稳态」实测**差 51%**。**机制判定实验**：加入 **A2 = forward + 每步 `time.sleep(15ms)`**（**不碰 CPU/内存**，只让 GPU 空转等）⇒ 两次实验都**不支持任一机制**（一次 A=14.358 反而最慢、A2=13.084、C=13.232 ⇒ C−A = −1.126；另一次 C−A = +8.9）⇒ **「下游干扰」在本机被时钟噪声淹没，既不能证实也不能否证 P12 的争用假说**。**P12 的 +17.726 / C/A=2.482 未复现**（v2 复跑得 −0.062、v3 得 +2.064；6 次独立运行全落在 −1.04 ~ +8.44）。⚠️ `nvidia-smi -lgc` **锁频失败**（`does not have permission`，当前非管理员）⇒ 需提权重做。**连带影响**：B5 的跨机结论（1.213×，21%）与其自身噪声（±11%）同量级 ⇒ 应视为**方向性提示**。**accept 判定**：表格已给出，但**结论是「该表格在当前测量条件下不可靠」**⇒ 本票实际产出是**定位测量学障碍并给出解法**（锁频 / 提高利用率 / 换更大模型），而非可用的干扰数字。**★ 512 档上下文探索完成（B11 遗留，2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b11-512context-2026-09-19.json`）**：**问题** —— `isolate(512)` 时 compile 与 eager **一致**，而 `inorder(512)`（同进程先跑 64/256）**分叉于 105** ⇒ 同一档位同一输入的 compile 结果因上下文而异。**结果（512 档 A/C hash）**：isolate ⇒ **一致**（C=`78889c287009097a`=A）；`64→512`、`256→512`、`64,256→512` ⇒ **分叉(105)** 且 C hash **完全相同**（`a9aa73798cf0e1d2`）；**`64,256→512` + 每档后 `torch._dynamo.reset()` ⇒ 恢复一致**（C=`78889c287009097a`=A）。**结论**：① **根因是 `torch._dynamo` 的编译历史** —— 所有条件「C 期间重编译增量」**全为空 `{}`**（排除 recompile_limit），而 `reset` 能完全消除 ⇒ 指向 dynamo 的编译缓存/守卫状态；② **任一前档位都足以触发**，且产出**同一个**另一套结果（与前档位具体值无关）；③ **单向性** —— `512→64` 时 64 仍一致（小形状不受大形状历史影响），反之成立；④ **256 档是另一套机制**（reset 前后 hash 都是 `702c5563255bbc7e`，由「数值差 × gap 平局」解释）；⑤ 512 隔离时的「一致」是**偶然**（数值差确定的非零，只是没超过任何一步的 gap）。**对「逐 token 一致」验收（进一步加强）**：compile 结果**不仅依赖输入，还依赖同进程编译过哪些形状** ⇒ 要么不开 compile，要么**在每个测量单元之间 `torch._dynamo.reset()`**（代价是重编译）。**未做**：`TORCHINDUCTOR_MAX_AUTOTUNE=0` 未测；未测 512 隔离时的 hidden `max|diff|`；未扫更多 prefill 组合；未定位到 dynamo 的具体缓存层。**★ TODO-B15 完成（Qwen-1.8B 在 transformers 5.x 下的运行期实测，2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b15-2026-09-19.json`）**：**装置** —— `models/qwen-1_8b-chat`（**带 remote code**，1.84B 参数）；基线 = 系统 python **4.47.1**（= 主运行时）；目标 = `.venv-test` **5.17.0 / torch cpu**；逐步执行 `config → tokenizer → model → generate → decode`。**结果**：① **能加载** —— A5 的 shim 补 5 个符号后 `S1..S5` 全过（`QWenConfig`/`QWenTokenizer`/`QWenLMHeadModel`），且 `output_loading_info=True` 显示 **`missing_keys=0 / unexpected_keys=0 / mismatched_keys=0`**（**权重映射无错配**，本票据此排除「参数未加载」假说）；② **`generate` 有两处独立硬阻塞** —— `modeling_qwen.py:800` 期待 legacy tuple 而 5.x 默认给 `DynamicCache`（`TypeError: 'DynamicCache' object is not subscriptable`；**`use_cache=False` 可绕过**）、`modeling_qwen.py:819` + `:896` 依赖 5.x **已移除**的 `PreTrainedModel.get_head_mask`（补丁**必须按 4.x 语义返回 `[None] * num_hidden_layers`** —— 先返回 `None` 会以 `'NoneType' object is not subscriptable` 再次失败，本票实际踩过这一步）；③ ⚠️ **两处都修好后「能跑通」但输出错误且非确定** —— 同配置**四次运行 logits top-5 全不同**（`[99730,34187,..]` / `[41932,86341,..]` / `[107267,111632,..]` / `[107267,111632,..,81892]`），而 4.47.1 **三次完全相同**（`[9909,106004,7,2130,99245]` ⇒ `'（ ）\nA. 巴黎'`，prompt「法国的首都是」⇒ 合理）；④ **非确定起点精确定位在「第 0 层内」** —— dump `output_hidden_states`：`hidden_states[0]`（embedding）在 4.47.1 与 5.17.0 的**全部 7 次运行中逐位相同**（`absmax=0.08496`、前 8 维一致），而 `hidden_states[1]`（第 0 层输出）4.47.1 = `9.8125`（2 次相同）、5.17.0 = `5.85 / 6.33 / 6.00 / 6.01 / 6.22`（**5 次全不同**）；⑤ **已系统性排除**（均有实测证据）：**CPU/设备**（4.47.1 + `CUDA_VISIBLE_DEVICES=-1` 与 GPU 结果**完全相同**）、**线程调度**（`OMP_NUM_THREADS=1` + `MKL_NUM_THREADS=1` 下 5.17.0 **仍非确定**）、**精度标志**（两侧 `is_fp32` 全为 `False`）、**参数缺失**（`missing_keys=0`）、**`RotaryEmbedding`**（`inv_freq` 由 `torch.arange` 确定生成）。**结论**：**5.x 下 Qwen-1.8B 能加载但不能正确推理**，且**前向非确定**（不可复现）⇒ **不建议升级主运行时**。**建议**：① 维持 4.47.1；② 或**退役 / 改走 llama.cpp GGUF-only**（仓库已有 `models/Qwen-1_8B-Chat.Q4_K_M.gguf`）。**★ 对 A7 的影响（已回填票内 `note_b15_2026_09_19`）**：A7（主运行时 transformers 升级评估）**强烈偏向「不升」**。**未做**：未定位第 0 层内的**具体非确定算子**（下一步应在 `QWenAttention.forward` / `QWenBlock.forward` 内插桩逐算子对拍）；未测 **5.x + CUDA**（`.venv-test` 是 CPU torch）⇒ 结论**限于 CPU 环境**；未测 `num_beams>1` / 采样路径。。**★ TODO-B9 完成（把「空 cache」兼容性坑修进主仓，2026-09-19）**：**问题** —— 调用方常传一个**尚未写入任何内容**的 cache 对象（`DynamicCache()` / `DynamicCache(config=cfg)`）而不是 `None`，导致 ① 报 `Qwen2 KV cache 层数不匹配: cache=0, local=N`（**报错不指向根因**）、② hybrid 上直接 `IndexError`（A3 的旧缺口）。**修法** —— 新增模块级 `_normalize_past_key_values()`，在 `forward_layers` **入口**统一把「空的 cache」视同 `None`（由本方自建），**所有架构路径一次覆盖**。**关键判据（本票踩到的坑）**：**不能只看层容器是否为空** —— 5.x 的 `DynamicCache(config=...)` 会**按 config 预先建出**每层的 `DynamicLayer` 占位对象（实测 `layers=[DynamicLayer×4]`、`len(cache)==4`），但 `get_seq_length()` 仍是 0；按 `all(x is None)` 判断会漏掉，随后 `enumerate` 这些占位对象会以 `k, v = item ⇒ ValueError: too many values to unpack` 失败 ⇒ **正确判据是 `get_seq_length() == 0`**（层容器/纯 tuple 判据仅作无该 API 时的回退）。**只处理完全空**：部分写入的 cache 原样返回。**测试** —— 新增 `tests/test_model_module.py::TestEmptyCacheNormalization`（**11 项**）：纯逻辑覆盖 `None`/`()`/`[]`/全 None tuple/真实空 `DynamicCache()`/真实 `DynamicCache(config=...)`/非空 cache 必须保留；集成覆盖传空 cache 不再报错且**结果与传 `None` 逐位相同**；并加**回归保护**（**非空但层数不对**的 cache 仍必须报错，原 `test_qwen2_decode_rejects_cache_layer_count_mismatch` 保持通过）。**回归** —— 全量 **2787 passed / 18 skipped / 0 failed**。**★ Qwen-1.8B 在 transformers 5.x 下「前向非确定」的根因定位（B15 遗留，2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-qwen18b-nondeterminism-2026-09-19.json`）**：**性质** —— **跨进程**非确定、**进程内**确定；`torch.use_deterministic_algorithms(True)` **不抛异常**且 `warn_only=True` 下 **0 条告警** ⇒ **不是「算子被标记为非确定」，而是「读未初始化内存 / 低精度通路」**（确定性 API 检测不到这类问题）。**逐层收窄（均跨进程比较）** —— `wte(embed)`/`hidden_states[0]` 在 **8 个进程中逐位相同**；`h0.ln_1(RMSNorm)` ✓、`h0.attn.c_attn(QKV)` ✓ 一致；**`h0.attn.forward` 输出首次不同** ⇒ 源头夹在 `c_attn` 与 `attn` 输出之间。**已排除**：① **RoPE** —— 运行时包装 `apply_rotary_pos_emb`，第 0 层的两次调用（q/k）其 `t_in` 与 `out` 在进程间**完全相同**（第 1 层的差异是第 0 层输出已不同的**传导**）；② **SDPA** —— 强制 `sdpa_kernel(SDPBackend.MATH)` 数值虽变（top5 `[107267,…]` → `[101437,…]`）但**跨进程仍 3/3 不同**，且把 remote code 的模块级 `SUPPORT_TORCH2` 运行时置 False（`:521` 据此选 SDPA、`:533` 是显式 `_attn`）**完全绕开 SDPA 后仍非确定**。⇒ **剩余嫌疑收窄到 `RoPE 之后 → c_proj 之前`**（`causal_mask` `:503-505`、`masked_fill` `:523-527`、**f16 softmax**（`softmax_in_fp32` 默认 False）、`_merge_heads` `:536-540`），**未继续细分**。**⚠️ 对 B15 的两处更正**：①「已排除 CPU/设备」**不严谨** —— 那次用系统 python 的 **cu126 build** + `CUDA_VISIBLE_DEVICES=-1`，**并非 cpu build**，而 5.17.0 侧是 `2.13.0+cpu`，两环境**同时**变了 transformers 版本与 **torch build**；②「5.x 加载成 fp16」**不准确** —— safetensors 实测 **BF16**，而两侧 CPU 场景最终 dtype 都是 f16 却**一确定一非确定** ⇒ **dtype 不是根因**。**B15 核心结论不变**：5.x 下 Qwen-1.8B **不能正确推理** ⇒ **主运行时不建议升级**。**未决** —— 「同 torch build 只换 transformers」的单变量对照未做成：在 `.venv-test`（torch cpu）里装 `transformers==4.47.1` 会**连环依赖冲突**（先 `tokenizers>=0.21,<0.22`，补装后**再** `huggingface-hub>=0.24,<1.0`）⇒ 正是 A7 已记的障碍；需**新建独立 venv**。另未把剩余嫌疑细分到单算子；未测 **5.x + CUDA**（结论**限于 CPU**）；未测 fp32 权重。。**★ TODO-A7 完成（主运行时 transformers 升级评估 4.47.1 → 5.x ⇒ 结论：**不升**，2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a7-2026-09-19.json`）**：**四项评估** —— **① remote code（❌ 硬阻塞）**：Qwen-1.8B（主运行时现役模型）在 5.17.0 的 `from_pretrained(trust_remote_code=True)` 下被**静默漏装全部 24 层的 `transformer.h.{0..23}.attn.c_proj.weight`** —— 逐键比对（**bf16** 加载以避免 dtype 转换造成假不一致）得 **`ok=171 / bad=24`**（bad 后缀 **100% 是 `c_proj.weight`**、`absent=0`），而 **`missing_keys` 仍报 0** ⇒ 那些张量留在**未初始化内存**上 ⇒ ① 注意力输出被随机投影 ⇒ **输出错误**；② **跨进程非确定**（garbage 因重启而异；进程内确定）；③ `use_deterministic_algorithms(True)` **检测不到**。**修复已实测**：加载后从 safetensors **手动重载全部 `state_dict` 键** ⇒ 3 个独立进程 logits **全部相同**且 `top5=[9909, 106004, 7, 2130, 99245]` = **4.47.1 的正确答案**（只重载 `c_proj.weight` 时该层与 `h1` 已一致、端到端仍非确定 ⇒ 说明受影响的就是这 24 个张量）。**② huggingface_hub 1.x（✅ 非阻塞，需 3 处字面修改）**：主仓仅 4 类调用点；**`huggingface-cli` 在 1.x 被废**（`entry_points.txt` 指向 `deprecated_cli.py`，后者只告警并 `sys.exit(1)`）⇒ 影响 `src/model_downloader.py:417/:473` 的 CLI 下载（**已有 try/except** ⇒ 表现为「下载失败」而非崩溃）；**生产 job 路径 `src/model_download_jobs.py:636` 的 `snapshot_download` 不受影响**（`local_dir_use_symlinks` 被 pop+warning，不 TypeError）；`hf-transfer` extra 在 1.x **不存在**（静默失效）；代理走 `os.environ` **不受影响**（1.x 只移除 per-request `proxies`）。**修法**：`huggingface-cli`→`hf`、删 `[hf_transfer]` extra、删 `local_dir_use_symlinks` kwarg。**③ 运行期 Qwen（❌）**：见 ①。**④ 同步 requirements-lock**：不升 ⇒ 无需同步。**若将来要升的前提**：① 先退役/替换 Qwen-1.8B 或等上游修复；② 若保留则**必须**做「`model.state_dict()` vs safetensors **逐键比对**」（`probe_qwen18b_which_keys_bad.py` 即现成工具）；③ 完成 hub 1.x 三处修改；④ **逐模型**回归（该缺陷**静默**，只有逐键比对可见）。**上游 issue 材料**见报告 `upstream_issue` 段。**★ Qwen-1.8B 非确定性根因收紧到张量级（2026-09-19）**：**排除链（全部多进程实测）** —— SDPA（强制 `SDPBackend.MATH` 与**完全绕开 SDPA**（把 remote code 模块级 `SUPPORT_TORCH2` 置 False）**都仍非确定**）、RoPE（运行时包装 `apply_rotary_pos_emb`，第 0 层两次调用的**输入与输出都跨进程一致**）、bmm / f16 softmax / `nn.Linear`（**固定输入下跨进程确定**；含 c_proj / c_attn / mlp 各形状 × 5 进程）、多线程（单线程仍非确定）、dtype（bf16 与 f16 都非确定）、`use_deterministic_algorithms`（不报错）。**关键转折**：逐算子插桩显示 `c_attn`(QKV) 输出跨进程一致而 **`c_proj` 输出首次分歧**；进一步 dump **权重**发现 **`transformer.h.0.attn.c_proj.weight` 的内容跨进程不同**（**其余权重一致**）⇒ 逐键比对 safetensors 确认 **24 层全部未加载** ⇒ **手动重载后输出完全确定且等于 4.47.1 的正确答案**。⇒ **性质修正**：**不是「算子/数值精度」问题，而是「加载漏装」**（未初始化内存）；**B15 报告已追加 `correction_by_rootcause_a7`**。**次要发现（与主因无关，但为真实代码陷阱）**：`masked_bias` / `logn_tensor` 是 `persistent=False` 的 buffer，`low_cpu_mem_usage=True` 时**也会留有未初始化内容**（关掉即一致）；`logn_tensor` 形状 `[1,32767,1,1]` 而 `seq_length=8192`，本场景不使用。**未做**：未定位 5.x 漏装该权重的**代码原因**（key 匹配 / `_init_weights` / `_keys_to_ignore` 正则误伤）；未测 **5.x + CUDA**；未验证其他 remote-code 模型是否有同类漏装；未做「同 torch build 只换 transformers」的单变量对照（受阻于依赖连环冲突，**但根因已由「手动重载后与 4.47.1 逐位一致」直接证实**）。。**★ TODO-A6 完成（`torch._dynamo.config` 的 thread-local 语义，2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-a6-and-leak-2026-09-19.json`）**：**源码证据** —— `torch/utils/_config_module.py:53`「User overrides are thread-local」、`:394-395`「Per-thread cache state, backed by ContextVar … config values are per-thread via ContextVar」、`:812`「patched settings are thread-local」。**实测（torch 2.13.0）** —— 隔离实验：**主线程设 `recompile_limit=64` ⇒ 新线程读回默认 8**（`config.patch` 亦不跨线程）⇒ **thread-local 确认**；主仓集成（在**有 triton** 的 `.venv-xframe` 上）：主线程加载后读到 **64**（`_apply_compile()` 生效；代码核实它确实设 `recompile_limit`/`cache_size_limit`），但 **worker 线程读到 8** ⇒ **设置对推理线程失效**。**根因**：`torch._dynamo.config` 是 thread-local，而主仓的模型加载/编译与推理**不保证同线程**（`_serialized_model_access` `src/model_module.py:495` **只加 RLock、不换线程**；而 `src/api_server.py` 大量用 starlette `run_in_threadpool`）。**修法**（accept 要求的「在使用线程内设置」）—— 新增模块级 `_ensure_compile_limits_in_current_thread()`（`threading.local()` 做**每线程幂等**标记；仅在当前值**小于**配置值时写，不覆盖用户调大的值；`USE_COMPILE=False` 时 no-op；异常吞掉、不影响前向），并在 `_serialized_model_access`/`_serialized_model_stream` 的 wrapper 里调用 ⇒ **覆盖所有模型访问入口**（含被装饰的 `forward_layers`，`:3173`）。**验证**：修复后 worker **进入 `forward_layers` 后读到 64**（裸读仍 8，属预期的 thread-local 语义）；新增 `tests/test_a6_dynamo_threadlocal.py`（**6 项**，含「新线程裸读得不到 / 补设后得到」这一关键判据，以及幂等、不覆盖更大值、装饰器确实调用 helper）；**串行全量 2800 passed / 11 skipped / 0 failed**（xdist 下的 2 个失败经复跑确认为**并发 flaky**，单跑通过、两次全量失败的用例还不同）。**意义**：默认 `recompile_limit=8` 远小于配置的 64 ⇒ 长序列/多形状下**更早触顶**并放弃编译，与 B1/B2 观测的「长序列 compile 劣化」一致，**本修复应能改善该表现**（未重测）。**★ 「24 层 attn.c_proj 被覆盖」根因闭环（2026-09-19）**：⚠️ **更正 A7 报告的表述** —— 不是「静默漏装（未初始化内存）」，而是**装载成功后被 remote code 的 `_init_weights` 用随机数覆盖**。**因果链**：① 24 个 `attn.c_proj.weight` **装载成功**（故 `missing_keys=0`）；② `get_init_context` 用 `torch.device("meta")` 造模型 ⇒ 模块级 `_is_hf_initialized` 标志**全部缺失**；③ 收尾 `_finalize_model_loading` **无条件**跑 `initialize_weights()`（后序遍历）；④ `_move_missing_keys_from_meta_to_device` 把**非持久 buffer** 换成 `torch.empty_like`（新对象、无标志）—— `QWenAttention` 恰持有 `masked_bias`/`logn_tensor`；⑤ ⇒ `_initialize_weights` 的检查里 `parameters(recurse=False)` 为空（`all([])==True`）而 `buffers(recurse=False)` 已无标志（`all(...)==False`）⇒ **早退失败** ⇒ 走到 `_init_weights`；⑥ `modeling_qwen.py:664-672` 的 `if name == "c_proj.weight": p.data.normal_(0, initializer_range/√(2·n_layers))`（`named_parameters()` 是**递归**的）⇒ `QWenAttention` 恰好**直接持有** `c_proj` ⇒ 递归名正是 `c_proj.weight` ⇒ **命中、被覆盖**；⑦ **`QWenMLP` 也有 `self.c_proj`**，但直接 param/buffer 为空 ⇒ 在⑤处**早退** ⇒ **不被覆盖** —— 这就是「**只有 attn 的坏**」的判别点。**决定性验证（seed 判别实验 `probe_leak_by_seed.py`）**：加载前 `torch.manual_seed(0)` ⇒ **3 个进程的 `attn.c_proj.weight` 完全相同**（sha `7437bb3145c7c9d3`）且 **`std = 0.00288618` ≈ 理论 `0.00288675`**（`= initializer_range/√(2·n_layers) = 0.02/√48`，**数学指纹**）；**无 seed ⇒ 各进程不同**；而 `mlp.c_proj.weight` 跨进程一致（= ckpt 原值，**未被覆盖**）⇒ 与⑦的预测一致。**4.47.1 为何正常**：它有 `set_initialized_submodules()`（用**递归 `state_dict()`** 判定，而 `state_dict()` **不含** `persistent=False` 的 buffer ⇒ `QWenAttention` 被正确标记为已初始化 ⇒ 早退）；**5.17.0 删除了该函数**，并把初始化从「装载**之前**」搬到「装载**之后**」，新增的⑤那段「非递归 parameters/buffers」检查（注释自称是给 remote code 兜底）**正是引入误判的地方**。**上游 issue 材料**已更新（标题 / 最小复现 / 根因 / 建议修法：检查范围应与写入一致，或恢复 `set_initialized_submodules` 语义）。**结论不变**：5.x 下该模型不可直接使用（除非做逐键比对/重载），**主运行时不建议升级**。。**★ TODO-B7 完成（更重的编译负载下重跑退出码实验，2026-09-19；报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-b7-and-guard-2026-09-19.json`）**：**负载相对原实验加重三项** —— ① `--gen 64`（原为 8）；② `--eager-segment --eager-gen 16`（**混入未编译的 eager 前向** ⇒ 编译图与 eager 共存）；③ `--compile-then-drop`（跑完**主动丢弃编译对象并 gc**，放大「退出期清理编译对象」的压力）。模型 `qwen3-5-2b`（hybrid，即当初报崩溃的架构），环境 `.venv-xframe`（transformers 5.17.0 / torch 2.13.0+cu126 / triton 3.8.0），两组各 **8 轮独立子进程**。**结果**：`atexit` 组 rc 分布 **`{0: 8}`**（非零 0/8）；`no_atexit` 组 rc 分布 **`{0: 8}`**（非零 0/8）⇒ **两组仍全为 0，崩溃未复现** ⇒ **atexit 的防护无法被证实**。**accept 达成**：按票面要求，`_apply_compile()` 里的 atexit 释放回调应标注为「**预防性、未证实必要**」—— 保留（成本极低、无副作用），但**文档中不得声称它解决了一个可复现的崩溃**。**★ 「以补丁形式兼容 Qwen-1.8B」完成 ⇒ 决策：保留 + 补丁，不退役（2026-09-19）**：**两个新补丁** —— **① `src/transformers5_compat.py` 补回 `PreTrainedModel.get_head_mask`**（transformers 5.x 移除，而 `models/qwen-1_8b-chat/modeling_qwen.py:819` 前向里调用、`:896` 用 `head_mask[i]` ⇒ 不补则**加载能过、前向必崩**；按 4.x 语义返回 **`[None] * num_hidden_layers`** —— ⚠️ 返回 `None` 会以 `'NoneType' object is not subscriptable` 再失败，B15 实际踩过；非 `None` 时 **fail-loud**）；**② `src/model_module.py` 新增「权重守卫」`_verify_and_repair_loaded_weights()`** —— 逐键把 `model.state_dict()` 与 safetensors 原值比对（`torch.equal`），**不符的就地重载**；**只在 `TRUST_REMOTE_CODE` 且 transformers ≥5 时执行**（4.x 无此缺陷 ⇒ **零开销**）；**只修不抛**（异常只记日志）+ **绝不静默**（发现不符必然 `logger.warning`）。**为什么需要**：transformers ≥5 把初始化从「装载**之前**」搬到「装载**之后**」，且 `_initialize_weights` 的「未初始化」检查只看 `recurse=False` 的 params/buffers，而 remote code 的 `_init_weights` 用**递归** `named_parameters()` ⇒ 当模块**只直接持有 non-persistent buffer**（`_move_missing_keys_from_meta_to_device` 会把它们换成无标志的新张量）时会被**误判为未初始化**；若该 `_init_weights` 又按**相对名**匹配（Qwen-1.8B 的 `if name == "c_proj.weight"`）就**覆盖已正确装载的权重**，且 `missing_keys` 仍报 0（**静默**）。**验证（走主仓路径 `ModelManager.load_model`）**：transformers **5.17.0** 下守卫报告 **`repaired = 24`**（正是那 24 层 `attn.c_proj.weight`，`checked = 195`）并就地重载；修复后 **3 次前向 logits 完全相同**，且 top5 = **`[9909, 106004, 7, 2130, 99245]`** = **4.47.1 的正确答案** ⇒ **5.x 下该模型恢复正确**。**开销**：加载变慢（多读一遍 ~3.5 GB 逐键比对；实测总加载 17.0s），但**4.47.1 主运行时零影响**。**测试**：新增 `tests/test_transformers5_weight_guard.py`（**7 项**）；**串行全量 2808 passed / 10 skipped / 0 failed**。**不退役的理由**：Qwen-1.8B 是主仓**唯一的「内置默认模型」**（`DEFAULT_MODEL_ID` `src/model_config.py:146`、`ACTIVE_MODEL_ID` `src/config.py:317`、`TOTAL_MODEL_LAYERS=24` `:331`、离线包**必需项** `scripts/build_offline_bundle.py:99-110`、下载预设 `src/model_download_jobs.py:69-86`、一大批测试/fixture/Android 测试）⇒ **退役代价远大于收益**；且补丁**插入点唯一**（`_load_pytorch` 的 `from_pretrained` 之后）、**层流水线路径天然免疫**（走 `init_empty_weights` + `set_module_tensor_to_device`，不经 `from_pretrained` 的收尾初始化）、**风险面小**（另一个 remote-code 模型 `minicpm4-0.5b` 的 `_init_weights` **无按相对名匹配单 Linear 的规则**，不同构；其余 `models/` 下模型均不带 remote code）。**caveat**：这是**兜上游 bug** ⇒ 必须配 fail-loud 日志（已做）并跟上游 issue（材料已备）。**未做**：未在 **5.x + CUDA**（`device_map="auto"` 分支）验证该守卫；未实测 `minicpm4-0.5b` 在 5.17.0 下的行为；未重测 B1/B2 的长序列 compile 表现（A6 修复的收益）。。**★★ 主运行时升级完成：transformers 4.47.1 → 5.17.0（2026-09-19，用户裁定）** —— **范围**（用户选 A）：主运行时（系统 Python）+ **全部 PyTorch 侧车**一起统一到 5.17.0（`.venv-qwen3-sidecar` 4.57.6→5.17.0、`.venv-gemma4-pipeline` 5.10.1→5.17.0、`.venv-test` 已是 5.17.0），并同步 `huggingface_hub` 0.36.2→1.32.0、`tokenizers` 0.21.4→0.23.2。**所有 pip 变更均先 `--dry-run`**（分别只动 4 / 10 / 2 个包，无冲突）。**改动的声明共 7 处**：`requirements.txt`、`requirements/requirements-{qwen3-sidecar,gemma4-pipeline-sidecar}.txt`、`packaging/packaging/requirements-{cpu,runtime-cpu,runtime-cuda}.txt`（`packaging/` 是独立仓库，另行提交），并新增 `huggingface_hub` 显式约束。**hub 1.x 的 4 类破坏性变更已全部处理**：① CLI 改名 —— 新增 `_hf_cli_name()` 版本自适应（优先 `hf`、回退 `huggingface-cli`，同时兼容 0.x/1.x）；② 删 `huggingface_hub[hf_transfer]` extra（1.x 已移除该 extra、`hf-xet` 进基础依赖）；③ 删 `snapshot_download(local_dir_use_symlinks=...)` kwarg（1.x 已移除）；④ 更新提示字符串。**升级暴露并修掉的 5.x 不兼容共 6 处**：① **`_verify_and_repair_loaded_weights` 的跨设备 bug**（`.venv-test` 是 CPU 未暴露；CUDA 上 `state_dict()` 在 GPU 而 safetensors 读到 CPU ⇒ `torch.equal` 恒 False ⇒ 曾误报 **195/195**）⇒ 改为**显式搬到 target 设备**再比较/写入，CUDA 上复测恰 **24/195**；② **`get_head_mask`**（5.x 移除，Qwen remote code 前向调用）—— 补进 `transformers5_compat`；③ **多模态 image_processor 类名白名单**（4.x `Qwen2VLImageProcessorFast` → 5.x `Qwen2VLImageProcessor`；tokenizer 同理去掉 `Fast`）⇒ 改为**允许集合**；④ **`Qwen3VLVisionModel.forward` 返回形状与语义变化**（4.x 2 元 tuple（merger **后**）；5.x `BaseModelOutputWithDeepstackFeatures` —— `last_hidden_state` 是 merger **前**（1024）、`pooler_output` 才是 merger **后**，且其 `__iter__` 迭代全部非空字段 ⇒ 直接解包报 `too many values to unpack`）⇒ 改为**属性优先、回退按位置**且取 merger 之后的那个；⑤ **`create_causal_mask` 形参改名** `input_embeds`→**`inputs_embeds`**（且 `cache_position` 不在签名里）⇒ `qwen3_pipeline_adapter` 改为**探测签名**（与 `model_module` 同法）；⑥ **`Cache.to_legacy_cache` / `DynamicCache.from_legacy_cache` 双双被移除** ⇒ 改为**迭代取前两元**与 `DynamicCache(ddp_cache_data)` 构造。另加 `QLH_MM_DEBUG=1` 调试开关（默认关闭，仍是 fail-closed 只报类名），否则 worker 只回类名无法定位。**验证**：**逐模型真机复测** —— Qwen-1.8B 在**真实主运行时（5.17.0 + CUDA）**上守卫报告 `repaired = 24`（恰那 24 层 `attn.c_proj.weight`），3 次前向 logits 相同、top5 = `[9909, 106004, 7, 2130, 99245]` = **4.47.1 的正确答案**；**串行全量 2807 passed / 11 skipped / 0 failed**。**★ 虚拟环境收敛（保守，用户选 A）**：① `.venv-xframe` 的内容已**全部并入 `.venv-test`**（`triton-windows 3.8.0.post28` + `flash-linear-attention 0.5.2`）⇒ 验证 `.venv-test` 下 `_apply_compile()` **不再回退**（`recompile_limit` 8→64，子线程补设后 64）；**`.venv-xframe`（296.9 MB / 8121 文件）已 dry-run 后归档到 `build/_to_delete/`（移动而非删除，可回退）**；② 修 `scripts/setup_gemma4_pipeline_env.py` 的**错误 requirements 路径**（指向不存在的 `packaging/...`）并同步其 transformers 常量；③ `gemma4_pipeline_contract.TRANSFORMERS_VERSION` 与 `gemma4_pipeline_runtime_worker` 的**硬校验值**同步到 5.17.0（不匹配会**直接拒绝 sidecar 启动**）；④ `.venv-edge` 补装 `textual 8.2.8`（`requirements-edge.txt` 已声明但未装），并复验 L 档边界 —— **torch/transformers 仍正确地不存在**；⑤ 重建全部 5 个 lock 快照。**注**：重建时发现**旧 `test.lock.txt` 本身就是错的**（含 `spacy`/`anthropic`/`ast-grep-cli` 等**系统 Python 的包**与 `torch+cu126`，且 `--snapshot` 的 `base_python` 决定 `main` 的快照来源 ⇒ 必须用系统 Python 触发）⇒ 已修正。**L 档 llama.cpp 硬边界按裁决保持独立不合并**（`.venv-edge` / `.venv-gemma4-native` / `.venv-llama-rpc`）。**★ TODO-B3 延后**（见下段）。 |

| D30 | `CORE-TUI-E2E-01`：TUI 端到端 flow 测试（F1：假后端 / 零模型；**F2：真后端 + 真模型**）+ 答辩演示复用 | `已落地（2026-09-24，F1 + F2 档）` | 剧本单一来源 `fixtures/tui_e2e_flow.json`（`actions`/`expect`/`caption` 分离，10 步）；引擎 `tests/tui_e2e_flow.py`（`--mode assert\|demo`，退出码 `0`/`1`/`2`/`3`，demo 有 DEGRADED **绝不返回 0**）；pytest 入口 `tests/test_tui_e2e_flow.py`（**14 例**：2 条 `pilot.click` 探针 + 10 步逐条 + 整条序列 + demo 退出码硬约束）已纳入 `scripts/startup_matrix.py` 的 `TUI_TESTS`（只加文件名、未改判据）。**探针实测**（消除计划 §1.3 的「未实测」）：`Pilot` 鼠标注入稳定可切屏、全 10 屏可达、命中返回 `True` / 落点在屏幕内但不在目标 widget 上返回 `False` / 落点超出屏幕抛 `OutOfBounds` / selector 不存在抛 `NoMatches`，连跑 8 次全绿。**边界（不得越界引用）**：F1 仅 headless + 假后端 + 零模型；**F2** 虽真模型但**单机、无在线从节点**（状态行如实显示 `pipeline_nodes_not_ready` 回退）⇒ **两者均不得**宣称双机物理分布式或「分布式推理已验收」。**F2 档**：`tests/test_tui_e2e_flow_real.py`（2 例，`real_model`+`slow`）默认 **2 skipped**，带 `QLH_RUN_REAL_MODEL_SMOKE=1` 实测 **2 passed**（真起后端 + 真加载 GGUF + TUI 聊天，真回答逐字为「在标准数学体系中，1+1 等于 2。」）；独占端口 8231，落进 `run_test_channels.py:314-331` 的 `smoke` 通道（`-m real_model -n 0` 串行）。**与计划的差异**：计划原写 `scripts/tui_e2e_flow.py` 作引擎，但 `scripts/*` 被 `.gitignore` 整体忽略（白名单逐条列举）⇒ 经用户裁定 `dec-fc91fb5c966c7786` 把引擎改放 `tests/`（可入库），`scripts/` 只留转发壳（需补白名单行 `!scripts/tui_e2e_flow.py` 才入库，本项未代改 `.gitignore`） |


- **2026-09-24**：**`CORE-TUI-E2E-01` 落地（F1 档）** —— TUI 端到端 flow 测试与答辩演示复用。
  **新增**：`fixtures/tui_e2e_flow.json`（剧本单一来源）、`tests/tui_e2e_flow.py`（step 引擎 + `--mode assert|demo`）、
  `tests/test_tui_e2e_flow.py`（14 例：**2 条 `pilot.click` 探针** + 10 步逐条 + 整条序列 + demo 退出码硬约束）；
  **改**：`scripts/startup_matrix.py` 的 `TUI_TESTS` 追加一行（只加文件名、未改判据）。
  **关键证据**：计划 §1.3 记「仓库现有测试从未用过鼠标注入」标为**未实测** —— 本轮探针把它测实：
  `pilot.click(selector)` 命中返回 `True`、落点在屏幕内但不在目标 widget 上返回 `False`、落点超出可见屏幕
  抛 `OutOfBounds`、selector 不存在抛 `NoMatches`；全 10 屏可由点击到达；连跑 8 次全绿。
  **验证**：`pytest tests/test_tui_e2e_flow.py` **14 passed**；`--mode assert` exit 0；`--mode demo --pace 0.15`
  exit 0 且生成 10 张 SVG（`build/tui-e2e/frames/`，不入库）。
  **边界**：仅 headless、假后端、零模型 ⇒ 不宣称真模型已加载 / 双机物理分布式；F2 后置。
  **落点差异**：引擎原计划放 `scripts/tui_e2e_flow.py`，因 `scripts/*` 被 `.gitignore` 整体忽略（白名单逐条）
  改放 `tests/`（用户裁定 `dec-fc91fb5c966c7786`），`scripts/` 只留转发壳（需用户/并行组补白名单行）。
- **2026-09-19**：**默认模型改为按设备画像选择**（用户裁定：**边缘设备 <1B，PC ~2B**，后者贴合原 1.8B 体量）。
  **改动**：新增 `model_config.DEFAULT_MODEL_BY_TIER` + `get_default_model_id(tier)` / `get_profile_default_model_id()` / `get_profile_default_model_paths()`；`config.get_active_model_paths()`。消费点全部改为按画像取值（`model_module` 构造/`load_model`/`ensure_full_model`、`api_server./api/models/load`、`api_server` 与 `engine_host` 的 `_auto_load_default_model`）。
  **分档**：`workstation`/`laptop` ⇒ `qwen3-5-2b`；`ultrabook`/`edge` ⇒ `qwen3-0.6b`；`mobile` ⇒ `qwen2.5-0.5b`；未知/缺失 ⇒ 兜底 `DEFAULT_MODEL_ID`。
  **分档依据（硬约束）**：2B int4 实测 **VRAM 峰值 2.24 GB** ⇒ `ULTRABOOK`（≤2 GB **共享**显存）**放不下**，必须退到 <1B。
  **PC 档选型（`qwen3-5-2b` vs `qwenseek-2b`，单变量对照：同 prompt / 同贪心 / 同 int4 / 3 轮重复）**：

  | 指标 | qwen3-5-2b | qwenseek-2b |
  |---|---|---|
  | 加载 | **9.36 s** | 12.38 s |
  | 稳态吞吐 | 18.19 ± 1.49 | 17.57 ± 1.93 tok/s |
  | 轮内波动 | **±0.41 ~ 0.72** | ±1.74 ~ 2.39 |
  | VRAM 峰值 | 2.24 GB | 2.24 GB |
  | `.qlh-model-asset.json` | **有** | 无 |

  ① **速度差仅 3.5%，落在 std 内 ⇒ 无显著差异**（不构成选型依据）；② `qwen3-5-2b` 加载快 24% 且**轮内波动小 2~3×**；③ 质量各有取向（`qwen3-5-2b` 输出更完整：改写题给多方案 + Markdown、复杂度题带公式；`qwenseek-2b` 更简洁：改写题只给「这个方案尚可。」）；④ `qwenseek-2b` **缺完整性元数据**（来源/SHA 未受管）。⇒ 取 `qwen3-5-2b`，**换用 `qwenseek-2b` 只需改一行**（对照数据已留档）。

  **顺带修掉两个由此暴露的真实 bug**：① `config.MODEL_NAME`/`MODEL_PATH`/`GGUF_MODEL_PATH` 原**指向已退役的 `qwen-1_8b`** ⇒ 「无人值守自动加载」实际加载的就是 1.8B（**这才是该症状的真正入口**）；② `model_module._load_llama_cpp` 的 fallback 用**模块级静态常量** `GGUF_MODEL_PATH` ⇒ **切到任意 GGUF 模型都会加载成兜底那个**（实测：切 `qwen3-5-2b` 实际加载 `qwen3-0.6b-q8_0.gguf`，连返回体的 `model` 字段都是错的）——现改为优先按活跃模型在注册表里的 `gguf_path` 解析。
  **端到端验证**（本机 `tier=laptop`）：画像默认 `qwen3-5-2b` ⇒ `ModelManager._active_model_id` 即为该档 ⇒ 加载 `qwen35-2b-Q4_K_M.gguf`（1.81 s）⇒ 输出「我是 Qwen3.5，由阿里巴巴集团旗下的通义实验室自主研发的超大规模语言模型。」且返回体 `model` 字段正确。
  **新增测试**：`test_default_model_is_chosen_by_device_tier` / `test_default_model_falls_back_for_unknown_tier` / `test_default_model_id_accepts_enum_and_str` / `test_profile_default_paths_resolve_to_existing_files`。

- **2026-09-21（晚场）**：D29 / `CORE-RELAY-XFRAME-02` **P0 完成 + 切点缺口补齐**：
  ① **qwen3.5-2b 正确性切点重跑**（与容量扫描同口径的 raw 下游，gen=32）：`K=8/12/16/20` **全部 32/32**（工件 `build/cross-framework-layer-poc/out/cap/k{8,12,16,20}-regress.json`，K=12 为 `k12-qwen35-regress.json`）⇒ 只结清正确性回归中的「未重跑（旧 FAIL）」三行；性能切点闭环仍需按多轮 raw gate 重新生成分析报告。
  ② **双主仓引擎端到端矩阵 27/27 逐 token 一致**：上游 `ModelManager.load_layer_range` + `forward_layers`，下游 **`LlamaCppEngine.forward_layers_from_hidden`**（驱动 `build/cross-framework-layer-poc/relay_mainrepo_both.py` + `p0_matrix.ps1` / `p0_quant.ps1`，数据 `out/p0/*.json`）；覆盖 qwen2.5-0.5B `K=4/8/12/16/20`、qwen3.5-2B `K=8/12/16/20`、`prefill 32/128/512`、`decode 32/64/256`、`B=2/4`（下游 `seq_ids` + `positions` 显式绑定）、混合精度（上游 fp16/f32/NF4 × 下游 Q4_K_M，与**同精度**整模对照）；
  ③ **新增引用规则**：`--upstream-quant int4` **静默回退 fp16**（`load_layer_range` 按 key 手工物化、不走 `BitsAndBytesConfig`）⇒ 真 NF4 需显式把 `nn.Linear` 替换为 `Linear4bit`（qwen2.5 84 层 / qwen3.5 93 层）；上游 compile 被 `COMPILE_MIN_PARAMS`（1.5B）规模门禁用 ⇒ 本矩阵是 **eager 上游**口径、不含 compile 收益；混合精度档位的 `capacity_gain_x < 1`（实测 0.2722–1.2359×）⇒ 容量收益只在**同精度**口径下解释；下游是 CPU-only 绑定 ⇒ **prefill 是主要开销**（K=12：32 token ≈ 1.5 s、512 token ≈ 5.7 s，含首次 ubatch 图开销）；
  ④ **仍未覆盖**：3 段及以上外推、跨机 SSH/RPC、弱网/长时 decode、下游 GPU offload、上游 compile 档；混合精度只做 K=12 且判据为 32 步贪心单 prompt，**不构成质量结论**。
  ⑤ **P1 统一实验驱动落地**：`scripts/relay_experiment.py`（只读驱动，四条链路）+ `schemas/relay-experiment-record.schema.json`（`kind` 三类互斥、kind 与 path/engines 一致性由 schema 强制）+ `src/relay_experiment_record.py` + `tests/test_relay_experiment_record.py`（25 项守卫）；四条链路各出一条记录（`build/relay-records/*.json`），并**否证「L→L 可直接用 pip 的 embeddings 通道当上游」**（该通道 = `output_norm(H)`，`rel_err=0.0018`/`cos=0.999998` ⇒ 驱动默认 fail-loud）。
  ⑥ **P2 切点求解闭环**：`src/relay_cut_objective.py`（n 段求解：容量/延迟/风险三输出 + 合法切点硬约束 + 从实测拟合段画像）+ `scripts/relay_cut_plan.py`（多轮原始实测分析→拟合→预测→对比报告）+ `tests/test_relay_cut_objective.py`（17 项）。历史 qwen2.5-0.5b / qwen3.5-2b 结果仍可作为正确性与拟合参考，但属于旧输入口径；当前门禁要求来源为 raw 多轮实测、实验身份与 warmup 可追溯，**两模型均需按新 gate 重跑后才能称为闭环通过**。现阶段只能说历史数据提示 hybrid 每层成本不均，单一线性画像只是近似（应按层类型分组或直接用实测离散候选）。
  ⑦ **P2 续：多段拓扑打通（路线 C，用户裁定）**：补丁版 keep-head 通道 —— 用含补丁的 `android/app/src/main/cpp/llama.cpp`（`cb5f7b7a8`）CMake 构建 `libllama` → C shim（`scripts/model_tools/keep_head_shim/`，与被调 DLL 同一份 `include/llama.h`，规避 `llama_context_params` 按值传参的布局错位）→ `src/llama_keep_head.py`（`forward_tokens_to_hidden` / `forward_hidden_to_hidden`）→ 驱动两条新链路 `l2l_keep_head` / `d2l2l_keep_head`（中间段接口参与链路签名，避免与两段 D→L 混表）。**语义**：`nextn`（head 工件）与 `layer_inp`（整模 + cut_layer）两通道**逐位相同**，与 PyTorch 上游 `rel_err=0.00214`（旧 embeddings 通道 11.69 ⇒ 改善约 5000×）。**实测**：L→L **32/32**；三段（torch 0..7 → llama blk.8..15 → llama blk.16..23）**32/32**。**限制**：单序列、需 MinGW 运行时（`QLH_KEEP_HEAD_DLL_DIRS`）、串行 hop 延迟随段数线性增长。
- **2026-09-19**：修复**用户实报的四个问题**（提交 `c809469`（非 torch 引擎属性转发）、`0c3d937`（本节全部内容））：
  ① **脏 token 根因 = 上一轮引入的 `_premark_hf_initialized` 作用域过宽**（**单变量实证**）——判据原为全局 `TRUST_REMOTE_CODE`（主仓默认 `True`），它只是「用户授权开关」，**不代表该模型真的需要 remote code** ⇒ 把**原生模型**一并纳入作用域 ⇒ 跳过整个 `from_pretrained` 窗口的权重初始化 ⇒ buffer（如 RoPE `inv_freq`）不被初始化。实测 `qwen3-0.6b` + int4（主仓引擎，**仅切换这一个变量**）：允许 premark ⇒ 输出 `<think>` 重复 12 次 + `رو`/`ismatic` 乱码；**禁用 ⇒ 输出通顺**。⚠️ **权重审计（NaN/Inf/零值）两侧全为 0** ⇒ 这类「值不合理但非 NaN」的损坏**逃过审计**，必须靠**输出对照**才能发现。修法：新增 `_model_declares_remote_code()`（读该模型 `config.json` 的 `auto_map`），`_needs_premark` 改用它。**实测各模型 `auto_map`**：**只有** `qwen-1_8b-chat` 与 `minicpm4-0.5b` 声明；其余（`qwen3-0.6b`/`qwen3-4b`/`qwen3-5-2b`/`qwenseek-2b`/`qwen3-5-9b`/`qwen3-vl-4b`/`deepseek-r1-*`/`distilqwen*`/`gemma4-*`/`qwen2.5-*`）**全部是原生架构**。回归验证：`qwen-1_8b` 加载仍成功（`uint8 normal_` 修复保持），`qwen3-0.6b` 输出恢复；**新注册的 `qwen3-5-2b` 端到端输出通顺**。
  ② **补注册 7 个「磁盘有、内置清单没有」的模型**（用户报「有些模型不在列表，比如有些 2B 模型」）：`qwen3-4b` / `qwen3-5-2b` / `qwen3-5-9b` / `qwen3-vl-4b-instruct` / `qwenseek-2b` / `gemma4-12b-safetensors` / `qwen35-9b-dsv4flash`；路径与量化名**按磁盘实际资产**（`models/*/.qlh-model-asset.json` 与 GGUF header）逐一核对；多模态两个按 `gemma4-native` 定位。**内置清单 13 ⇒ 19**。
  ③ **`qwen-1_8b` 从内置列表移除**（用户裁定）：此前只标「已退役」**但仍排在第一位**，导致 UI 误选/误加载成 1.8B。`DEFAULT_MODEL_ID` 保持 `qwen3-0.6b`；`config.ACTIVE_MODEL_ID` 同步为 `qwen3-0.6b`。**模型文件与 GGUF 路径保留**，只是不再作为可选项暴露。
  ④ **模型表刷新丢失光标**（TUI）+ **前端静默回退**：`fill_models` 每轮后台刷新 `clear()` 后重建 ⇒ **光标跳回第 0 行**；用户按**视觉记忆**选行再按 `L`，`action_load_model` 读的却是**光标行** ⇒ 加载**列表第一项** ⇒ 报障「选择了其他模型却加载成 1.8B」。修法：按 **row key** 记住并恢复（不依赖行序）+ 回归测试 `test_models_table_cursor_is_preserved_across_refresh`；前端 `ModelsPage` 新增 `explicitSelection`，`handleLoad` **拒绝**用展示兜底值加载（子模块提交 `223fcdb`）。
  **测试**：串行全量 **2923 passed / 19 skipped / 1 failed**；唯一失败 `test_qwen3_network_sidecar_chain::test_real_sidecar_two_node_chain_prefill_decode` **与该改动无关**（它用 `models/qwen3-4b` **直接路径**、不读注册表），单跑 **6 skipped**（真机前置不满足）⇒ 属并发竞态。
  **下载预设**：`model_download_jobs.PRESETS` 里的 `qwen-1_8b-gguf-q4` **已一并移除**（用户裁定；已按 dry-run 流程先列清单再执行）⇒ 预设数 8 → 7，相关断言同步（`test_list_presets_has_expected_fields`、`test_preset_without_ms_source_blocks_modelscope` 改用默认模型）。
  **前端**：React 改动经**人工审查**（用户裁定不装 `node_modules` 跑 `tsc`），仅为 `selectedModel` → `explicitSelection` 的逻辑替换，无新 import / 类型变动。

- **2026-09-18**：D29 / `CORE-RELAY-XFRAME-02` **P2 交叠 + 「全 llama + CUDA」公平对照完成**（报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-p2-2026-09-18.json`）：① **对照结论**——同机同模型 `llama.cpp + CUDA`（build-cuda，`-ngl 24`）= **26.6 ms/token**，接力最优（N=20）= 129.1 ⇒ **llama.cpp 用上 CUDA 快 4.85×**，`-ngl` 曲线单调（91.1/71.0/60.5/52.9/44.2/34.7/26.6 对应 ngl 0/4/8/12/16/20/24）；⚠️ 参照物必须同构建（build-cuda 即使 `-ngl 0` 也比 build-cpu 快约 2×：91.1 vs 194.2）。② **P2 交叠**——两条独立序列软件流水线（上游 GPU torch 与下游 CPU llama.cpp 各持锁，两侧调用均释放 GIL）：serial 101.11 → **threaded 78.31 ms/token（1.291×）**，吞吐 154.5 → 199.5 tok/s，**两条序列 token 与基线完全一致**；天花板约 1.79×，达 72%。③ **定位修正（重要）**：P0 的 1.45× 只是「CPU llama → GPU torch」的局部收益，更优做法是「CPU llama → **GPU llama**」（`-ngl`）⇒ **接力不是更快的推理路径**，而是「只能用 torch 跑的层」与「容量合并/层流水线」的机制 + 实验平台；**集群有 CUDA 节点时应作为 llama.cpp 的 CUDA worker（RPC/分片），而不是接力上游**。脚本 `overlap_pipeline.py`、`summarize_p2.py`；**跨机对照仍未测**；生产准入仍 fail-closed。

- **2026-09-18**：D29 / `CORE-RELAY-XFRAME-02` **P0 实测完成**（切点重搜，报告 `local_docs/evidence/relay-xframe/CORE-RELAY-XFRAME-02-sweep-2026-09-18.json`）：扫描上游层数 N∈{0,4,8,12,16,20}（N=0 为「不接力」对照，下游为对应 f16 裁层 GGUF，CPU 8 线程），全部切点的 64-token 贪心序列**逐 token 一致** ⇒ 正确性不随切点变化；**总时间对切点几乎不敏感**（N∈{4,8,12,16} 为 214–225 ms/步，±2.6%，无中间谷底）；**不接力最快（186.8 ms/步，比默认 N=4 快 12.9%）**，即本机同机单序列下接力开销约 **+15%**（相对纯 llama.cpp CPU 口径），远小于「对比原生 GPU 慢约 25×」的印象（那主要是 CPU/GPU 之差；口径见 §F/§H）；上游每层（torch/CUDA 8.8–10.7 ms）**不低于**下游每层（llama.cpp/CPU 7.7–8.6 ms）。**工程约束**：切点必须是 `full_attention_interval`（Qwen3.5=4）的整数倍，否则裁层 GGUF 层类型错位、无法加载（N=2 实测）。**方法学警告**：脱离端到端的孤立测量偏低约 5.6×（7.6 vs 实测 42.7 ms），成因未定，已验证不是「KV 形状是否固定」；切点结论以端到端口径为准。**生产准入仍 fail-closed**；P1–P5 待开工。

- **2026-09-18**：立项 D29 / `CORE-RELAY-XFRAME-02`（**未做新实验**）：把《同进程双后端接力实现与性能》§12–§14 已实测的结论固化为优化票——**P0 切点重搜**（当前固定 4 层不合理：`L0-3` 仅占权重 10.1%、`L4-23` 占 50.3%，应按两侧单位层耗时求最优切点）、**P1 下游 GPU offload**（`--gpu-layers` 已实测 2.18×，同进程叠加阻塞于未构建的 CUDA 版 `llama-cpp-python`）、**P2 批量交叠 / 批量验证**（把单序列延迟转为吞吐）、**P3 混合精度与量化误差累积测量**（业界无公开数据）、**P4 加载 `meta` 化**（层流水线 42 s 墙钟中约 29 s 是 737M 参数随机初始化）、**P5 上游算子**（fla/triton 仅 WSL2 可用）；同时登记**明确不做**的四项（减少同步次数、复用 `llama_batch`、朴素截断上游层、把 relay 下沉 C++ 以减少 Python per-step 参与）。基线：累进 182 s → **21.3 s（8.5×）**，对照 llama.cpp 原生整模 GPU 约 **13.5 ms/步** 仍慢约 **≈25×**（`333 ÷ 13.5`；⚠️ 参照不唯一：`-ngl 24` 的 `26.6 ms/token` 口径下为 12.5×，见 §F/§H）；五段剖析 **99.3% 为两侧实际计算**、通信与同步 0.34%。上游 #28963 修复与本仓库独立验证、上游手动 4 层 **10.7×**（bit-exact）、层流水线 **1.47 GB f16**（3.1× 小，端到端 64/64）归 D22 与本票基线；**生产准入仍 fail-closed**。

- **2026-09-17**：新增 D24 / `CORE-KOAKUMA-ENGINE-01`：Koakuma Protocol、backend 注册表、节点画像选择、公共能力集与兼容读取边界已落地；CUDA 画像默认 PyTorch、无 CUDA 默认 llama.cpp、显式 llama.cpp 可退化。`scheduler.py`、`api_server.py`、`inference_service/engine_host.py` 的 `_engine_type` 命中均为 0，层拆分通过 `FORWARD_LAYERS` 能力探测；定向回归 `592 passed, 1 skipped`，动态配置修复后复核 `309 passed, 1 skipped`。

- **2026-09-17**：新增 D25 / `CORE-NO-TORCH-GUARD-01`：默认 GGUF 运行路径不再因设备探测加载 torch，分页 KV 缓存延迟到实际 D-tier 使用处解析 torch；无 torch 隔离启动、假 GGUF 推理和 TUI 入口守卫通过，显式 PyTorch/auto 设备探测回归保持兼容。定向回归 `7 + 22 + 5 passed`。下一张低耦合票为 `CORE-HIDDEN-CALLBACK-01`。

- **2026-09-17**：新增 D26 / `CORE-HIDDEN-CALLBACK-01`：移除字符串式 `attach/get_attachment` 注入和模块级 `_ATTACHED_ATTRS`，以不可变 `SchedulerCallbackSet` 显式提供调度依赖；API、进程内 host 与远程 `InferenceClient` 的装配顺序不再影响行为，scheduler 不再调用 host 私有回调。定向回归 `53 + 7 passed`；下一张低耦合票为 D27 / `CORE-DATAPLANE-NO-WRITEBACK-01`。

- **2026-09-17**：完成 D27 / `CORE-DATAPLANE-NO-WRITEBACK-01`：数据面删除 `callback_owner._tcp_server = self`，三类 TCP 生命周期事件改为启动前显式注册；调度器通过线程局部 source context 隔离并发 server，不再依赖 I/O 线程修复全局绑定。TCP 回调仍在接收/心跳 I/O 线程同步执行，`TCPServer` 调用控制面前不持 `_clients_lock`，调度锁由业务回调自身管理。低耦合 10A-10E 全部完成，下一票为 10F / `CORE-LAYER-CONTRACT-01`。

- **2026-09-17**：完成 D28 / `CORE-LAYER-CONTRACT-01`：独立节点合同将既有容量、manifest、Relay 和 RPC lease 的层段语义收敛为不可变 `PipelineLayout`，并在调度器容量准入边界 fail-closed；新增只读聚合资源 API/TUI 投影。`is_distributed` 只由跨机节点决定，因此同机多段/跨引擎接力不会被误报为分布式。回归 `合同 39 + API/TUI 115 + Scheduler 286 passed`。

- **2026-09-17**：完成 D29 / `CORE-RESHARD-01` 开发门：断线后以在线 PC 幸存节点重解完整层段布局；容量、模型身份、工件范围与首尾边界组件必须满足才可进入两阶段恢复加载。新布局仅在匹配的 `plan_id`/合同摘要全部 ready 后通过 lease epoch 原子发布，旧 epoch 结果拒绝；GGUF 远端同步计划只能在实际同步成功后导出工件可用性。新增只读 API/TUI 状态。回归 `Scheduler 287 + core/API/TUI/RPC 164 passed`；真实 PC/Android 故障注入、同步和性能验收后置，下一票为 `CORE-LLAMA-ANDROID-01`。

- **2026-09-17**：登记 `CORE-LLAMA-ANDROID-01` 的 **P0 已完成（2026-09-16）**（票表原记为「规划」，此处对齐事实）：① 交叉编译 `arm64-v8a-16k`，16 KB page 对齐已固化进 `app/build.gradle.kts`；② **JNI 打通** —— `libqlh_llama_jni.so` 导出 4 个 `nativeRpc*`，在 App 进程内独立线程跑 `ggml_backend_rpc_start_server`（**不 fork 子进程**），并自动排除 RPC 设备避免自环；上游无优雅停止 API，`stop_supported=false` 如实上报；③ 引擎/Stage 硬编码改为能力探测（对齐 `CORE-KOAKUMA-ENGINE-ABC-01` 判据），JVM 单测 17 passed；④ 嵌套 submodule 规范化为 `47e1de77`（gitdir 644 M → 431 M）。证据：`local_docs/evidence/llama-android/CORE-LLAMA-ANDROID-01-p0-交叉编译-2026-09-16.md` 与 `CORE-LLAMA-ANDROID-01-p0-后续与submodule修复-2026-09-16.md`（**本地产物，不入库**）。**P1 待设备**：真机/emulator 运行 RPC worker、能耗/热/断线与安全实测、`System.loadLibrary` 运行时路径与 APK 打包验证。**工程债核实更正（2026-09-17）**：`qlh-android` 的 `984bc95` **已在远端**（`git ls-remote origin main` = `984bc958…` = 本地 HEAD，P0 文档记的"未推送"已过期）；**仍待解决**的是嵌套 submodule 的跨机 fetch —— 主仓 `.git/config` 的 `http.proxy` 不会传给嵌套 submodule 自己的 fetch，无代理直连时 `android/app/src/main/cpp/llama.cpp` 仍会报 `fatal: Unable to find current revision in submodule path`，处置见 `scripts/init-submodules.sh|.bat`。

- **2026-09-16**：新增 D23 / `CORE-TRANSPORT-PORT-01` 低耦合门：`src/transport_port.py` 提供服务器/客户端、协议枚举、张量编解码、集群配置和本机网络信息的适配入口；`src/scheduler.py` 不再直接导入 `tcp_comm`、构造 `TCPServer` 或调用旧张量编解码器；静态边界断言与动态替身回归已补齐。票 2-5 尚未完成。

- **2026-09-16**：D20 增加远端 GGUF 资产同步合同：`llama_pc_rpc.py --sync-remote-model` 只读核验并生成计划，`--apply-remote-model-sync` 才执行带 SHA256/大小校验的 `.part` 上传与原子替换；旧远端模型只移动至 `_to_delete`，不执行删除。代码/命令契约专项 `9 passed`，真实大文件传输、长时及更大模型容量收益仍未验收。

- **2026-08-23（第二批）**：本机验收登记扩展——D 表新增 D11-D16：①模型一键下载与搜索（P0a job/P0b 页面/P1a 搜索，service 8+6/搜索测试/前端 build 通过，真实下载与冒烟后置）；②接口契约对齐审计工具（FE-AUD-01/02，`api_gap_scan` canonical/legacy/`--ownership`，专项 15 passed）；③Android presence 租约 + Full Worker 客户端全链路；④Android 控制面 `AND-CTRL-01..05` 与前置契约②③④（control 38/326、gateway 9/121、Android 42 全绿）；⑤docagent 检索/语义门、实验质量生产门与离线资产整合包；⑥`CY-PKG-01` 打包基线切换 cybergothic。**澄清登记：特定时段具备双机与 IPv6 网络**——A4/B3 相关的 E1（双机稳定性）、E2（IPv4/IPv6-only/双栈矩阵）、E6（双机 SD/异构链）改为**按双机/IPv6 可用时段排期验收**，不再是无限期后置；B3 已同步 `NET-DUALSTACK-PREF-01` 回退顺序收口。
- **2026-09-15**：主线 v5 重排新增 D17-D19：先验收裁切/低耦合与无 torch 入口，再验收 llama.cpp/GGUF 单机，最后验收同机 host + `ggml-rpc-server` 双进程部分驻留；三门未完成前，旧 PyTorch 双机证据、任务图仿真和 Silica Android 可行性证据均不得替代模型分片验收。
- **2026-09-15**：D19 / `CORE-LLAMA-SIM-01` 完成同机真实探针：新增 `scripts/llama_rpc_sim.py`，完成 DS3-7B 受控容量门、输出、断开检测和 CPU 回退；不将受控 budget 或本机 loopback 证据扩写为物理 OOM、跨机或生产 RPC 结论。
- **2026-09-16**：D20 / `CORE-LLAMA-PC-RPC-01` 增量完成自动分层与治理门：`scripts/llama_pc_rpc.py` 默认读取远端硬件画像，复用 `DeviceProfiler` 画像字段/评分语义；Surface 4 核、1.498GHz、8.36GiB 可用内存被限制为 `1/25` 层。新增 RPC lease TTL/renew、epoch fencing 与断线重分配，真实报告验证远端提交、旧 lease 拒绝、本地 fallback 提交、部分驻留与模型身份一致。最终分布式请求 657ms 慢于 CPU 回退 313ms，性能收益不达标，直连长时及更大模型容量仍后置。
- **2026-09-16**：D21 / `CORE-RELAY-01` 收口：合同、画像驱动切层规划器、L→L 验收探针和真实单机回退验证完成；补强裁层 artifact 与逻辑模型身份分离、MTP/block_count 布局校验和短输出拒绝。定向回归 `43 passed`。D→L、跨进程/网络、长序列和采样未验收，下一票为 `CORE-RELAY-XFRAME-01`。
- **2026-09-16**：D22 / `CORE-RELAY-XFRAME-01` 增量：新增 `llama_relay_xframe_probe.py`，以逐位置 argmax 判定 raw float32 logits，并记录 SHA、bitwise、cosine/top-k 诊断与 fail-closed 生产门。D→L 同机文件交接仅 5-token f16 `5/5` 通过；141-token f16 `132/141`、全 f32 GGUF `128/141` 均失败，完整 f32 llama.cpp 与 PyTorch 末位仍 argmax 相同。故 D22 继续进行中，不能宣称跨框架/长序列/网络已通过。
- **2026-09-16**：D22 首轮**控制组根因收敛 + 假设补丁编译验证**：① 控制组一——llama.cpp 完整模型 layer 3 hidden 与 PyTorch hidden 平均 cosine `1.0`、最大绝对误差 ≈`2.44e-4` → 跨框架 hidden 语义一致；② 控制组二——把 llama.cpp **自己**导出的 hidden 注入裁层模型，141 token 仍只有 `130/141` → 与 PyTorch 侧无关；③ 控制组三——**完整模型**直接改为接收 token embedding，141 token 仍 `132/141`、首分歧提前到 `5` → 问题在 **llama.cpp 长批 `embd` 输入图/续算路径**。源码依据 `build_inp_embd()` 在 embedding 模式下仍构造并调度未被写入的 token 分支。已在实验 fork（`build/cross-framework-layer-poc/llama.cpp`，gitignored）做「只构造被选中分支」的最小假设补丁并**编译通过**（`llama-graph.cpp` → `libllama.a`，产物 `llama-hs-extract-batch.exe` / `llama-embd-inject-test.exe` / `llama-relay-check.exe`）。**下一步**：重跑完整模型 token/embedding 141-token 控制组验证补丁；未通过前 D22 生产门保持关闭。**主仓运行时不依赖该补丁**。
- **2026-08-23**：`NET-DUALSTACK-PREF-01` 本机开发门完成：显式 `/api/cluster/connect` 成功后将 canonical endpoint 持久化为用户首选，启动时覆盖陈旧 `.env`，自动回退顺序固定为“首选 → bootstrap 原地址 → Tailnet”。同 cluster bootstrap 保留偏好、跨 cluster 清除偏好；`test_scheduler.py` `284 passed`，API/bootstrap/双栈专项 `36 passed`。B3 仍只完成短任务 IPv6 实跑，IPv4-only/IPv6-only/双栈安装包与升级矩阵按双机/IPv6 时段排期（E2）。
- **2026-08-21**：本机开发门完成批次登记——①投机解码三票 `SPC-CS/MEDS/REV`（置信度调度 GammaPolicy、Medusa 候选树、逆向回退）专项定向 `107 passed`；②`TG-OPT-G5.3` 影子级动态故障注入闭环（白名单扰动/同 seed digest 重放/影子只读，真实双 Worker 仍 Blocked）；③RAG 后置可准备（provider 长时 soak harness + sqlite-vec ANN 决策）；④**打包瘦身改造**：PyTorch 系移出安装包（`packaging/qlh-slim.spec` + `runtime_guard` 外部运行时 pip 引导，CPU 走 PyTorch CPU index、CUDA 官方源），单元/静态验证 `64 passed`，真 PyInstaller 构建待打包环境；`test_task_graph_gate_off` 测试自包含修复。D 表新增 D7-D10。B3 更新为 2026-08-21 双机 IPv6 实跑通过（长时/多轮仍后置）。本机全量快照 3032 passed / 19 skipped（`-n 4`）。
- **2026-08-18**：B3 增加双机连接前只读预检；不扫描网段、不写节点配置、不输出原始 Tailnet peer/地址。主节点本次实跑显示双栈 socket 与 Qwen CUDA sidecar 通过，但 Tailscale 当前 `NoState`/本机离线；启用 Tailscale 后再与已就绪从节点执行明确 IPv6 端点验收。
- **2026-08-13**：`QW3-S0` 文档与官方工件调研完成。Qwen3-4B、Qwen3-VL-4B 与 Qwen3.5 均尚未下载或注册；`QW3-D1` 只允许按受控 resolve/代理/Range/SHA/manifest 流程逐项登记，后续运行时、模型 smoke、EX-N3 三轮标定和正确率门恢复分别保留独立阶段门。
- **2026-08-13**：文档创建。B6 标记已验收（外部验证通过）；A1 改为本机条件性（关闭无关软件/codex 腾出 RAM 即可），不再属外部设备阻塞。
- **2026-08-13**：C3 质量总 gate 实跑 2/3 侧完成（LLM 格式率 4/11 ≥ 0.30 + SD 自动门 passed，均新鲜重跑）；Gemma 判题侧待 RAM ≥ ~8GB 补跑（codex 占用期间不足）。
- **2026-08-13**：**C3 收口**——Gemma 判题侧补跑完成（topic 0.9 / coverage 0.55 / 0 失败，RAM 释放后），三侧全绿，C3 `已验收`。
- **2026-08-14**：**开发组工具链变更**——GROK 组由 VSCode Codex 插件迁移至 **grok CLI（TUI，原生编辑落盘正常）**；VSCode 通道弃用原因：`config.toml` 的 `apply_edit`/`workspace_edit` 为 0.147.0-alpha 无效 feature（二进制无此符号），且 sub2api 中转（`wire_api=responses`）过滤编辑工具定义 → 模型仅可见 `shell_command`/`update_plan`，编辑永不落盘（会话取证：71 次只读 shell + 1 次写 `/tmp` 未应用 + 6× Missing SUB2API_API_KEY + 2× 429 断流）。
- **2026-08-14**：**GROK 组两次"PT-PIPE-QW3.4 completed"声称核验为不实**——声称的 `_simulate_prepare_commit`/`_build_failure_matrix` 在仓库（代码/提交/工作区）中均不存在，相关符号均来自既有提交（`4b739c0`/`fc3ef61` 等）；**此后 GROK 组声称一律以 git 证据（commit/diff）为准**。
- **2026-08-14**：**QW3.4 实际状态：未完成**。现有 Scheduler 两阶段事务与层加载守卫经安全审查评级 warn：2 HIGH（commit 可覆盖 abort 的半提交竞态；ready 后被 error ACK 覆盖仍判 fail-open）+ 3 MEDIUM（事务无超时、容量自报无上限、错误细节泄漏）。退出条件（结构化拒绝、无整模回退）未满足，待开发组修复后补失败矩阵测试并登记开发记录（PyTorch 峰值方案 8.15/8.16）。
- **2026-09-16（v7 口径同步）**：**引擎层命名与节点级异构口径入档**——引擎层统一命名 **Koakuma（混合推理引擎）**，内部承载 llama.cpp/GGUF 与 PyTorch/Safetensors 两套 backend；**有 CUDA 的节点保留 torch 能力并默认开启（显式开关，可关闭并退化）**，**无 CUDA 节点（含边缘）去 torch 化为硬约束**。本清单中原「无 torch Edge 入口」（D17）与「边缘/PC 默认禁止 torch」等表述**仅适用于无 CUDA 节点与边缘交付**，不适用于有 CUDA 节点的 Koakuma torch backend。
