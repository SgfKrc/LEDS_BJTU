# QLH（Kllama）TUI 端到端 Flow 测试 + 答辩演示复用计划

> **状态**：**F1 档已落地（2026-09-24）**，详见 §七「落地现状」；F2（真后端 + 真模型）与
> Web/Android 前端 E2E 仍未落地（属支线）。
>
> **更新日期**：2026-09-24
>
> **适用范围**：`src/tui_textual.py`（当前唯一交互外壳）的端到端 flow 测试脚本与其「演示模式」复用；
> 不含 Web/Android 前端 E2E（属支线）。
>
> **判据纪律声明**：本计划不改动任何数值一致性判据 —— 分布式链路的正确性仍**只认 per-token argmax**
> （`scripts/relay_experiment.py:18-20` 引用 `src/relay_contract.RELAY_ACCEPTANCE`），不用 cosine/时长替代。
>
> **来源**：本稿由只读子 agent 侦察（逐条 file:line 核实）+ 主 agent 整理而成；文中标「**未实测**」的
> 条目表示仅有代码层推导、本机尚无执行证据。

---

## 一、现状核实（逐条带证据）

### 1.1 TUI 技术栈与实现

| 事实 | 证据 |
|---|---|
| 交互外壳是 **Textual**（非 curses / rich / 自研 ANSI） | `src/tui_textual.py:49-54`；`:126` |
| 主应用类 | `src/tui_textual.py:3275-3285`（`class KoakumaApp(App)`） |
| 屏幕结构 | Splash `:269`、ConfirmScreen `:343`、ActionFormScreen `:386`、ChatPane `:449`、MainScreen `:1275` |
| 十屏清单（9 功能 + 1 调试） | `src/tui_textual.py:153-164`（`PAGES`：`chat/status/models/cluster/nodes/queue/logs/device/settings/api`） |
| 依赖 | `requirements-tui.txt:8`（`textual>=8,<9`）；本地实装 `textual 8.2.8` |
| 自绘 ANSI TUI **已归档**、不再回退 | `qlh.py:164-175`；`src/tui_commands.py:3-12` |

### 1.2 启动入口与命令

| 事实 | 证据 |
|---|---|
| 统一入口 | `python qlh.py`（`kllama.sh:22` / `start_tui.sh:50` 都 exec 它） |
| 无参即交互外壳 + 自动起本机后端 | `qlh.py:178-181`、`:76-78` |
| 后端在**当前进程内**启动（daemon 线程 uvicorn），随 TUI 退出而停 | `qlh.py:145-151`（`BackendSupervisor`）；`src/tui_backend.py:21-31, 66-128, 153-159` |
| 启动流程在 Splash 内完成 | `src/tui_textual.py:3303-3321`；`src/tui_backend.py:43-46, 68-124` |
| **不需要模型即可启动**：后端不可达时外壳照常可用 | `tests/test_tui_textual.py:114-135`（断言状态屏出现「后端不可达」） |
| 只读单命令模式（不起后端） | `qlh.py:26, 218-219`；`start_tui.sh:38-42` |

### 1.3 能否程序化注入鼠标事件 —— **可以，且是原生能力**

| 事实 | 证据 |
|---|---|
| `App.run_test()` 是 **headless** driver | `textual/app.py:2133-2160`（`headless: bool = True`）、`:3334-3335`、`:1188-1193` |
| `Pilot` 原生提供鼠标注入：`click / double_click / triple_click / hover / mouse_down / mouse_up` | `textual/pilot.py:192-249`、`:251`、`:300`、`:349`、`:100`、`:147` |
| `click` 支持 **selector + 相对 offset**（不必用绝对屏幕坐标），且返回 `False` 表示落点不在目标 widget 上 ⇒ **本身即断言** | `textual/pilot.py:216-236` |
| 侧栏 ListView 的点击语义：点 `ListItem` → 高亮 + `Selected` 消息 | `textual/widgets/_list_view.py:388-392` |
| 本仓库 TUI 正是监听这类消息切屏 | `src/tui_textual.py:1425-1433`（`switch_page(item.id.removeprefix("nav-"))`） |
| ⚠️ **仓库现有测试从未用过鼠标注入**（`tests/` 搜 `pilot.click｜hover｜mouse_down` → 无匹配） | **未实测**：`pilot.click("#nav ListItem#nav-cluster")` 能否稳定切屏，仅有代码层推导 |
| 真终端下 Textual 会开鼠标上报（SGR 1006） | `textual/drivers/windows_driver.py:56-75`；`drivers/linux_driver.py:134-142` |

### 1.4 既有测试基础设施

| 事实 | 证据 |
|---|---|
| pytest 全局配置（`-n 4`、`--timeout=300`、`--strict-markers`） | `pytest.ini:1-14`；worker 数理由 `pytest.ini:15-20` |
| marker 注册（`external/real_model/slow/...`） | `pytest.ini:24-30` |
| conftest：`src/` 上 `sys.path`、`--qlh-order-seed`、transformers 5.x shim | `tests/conftest.py:24, 27-34, 47-52, 55-86` |
| **TUI headless E2E 已有成熟骨架** | `tests/test_tui_textual.py:46`（`importorskip`）、`:61`（`async with app.run_test() as pilot`）、`:82-99`（十项 nav id / 黄金比例 0.382 断言）、`:102-109`（`pilot.press("]")` 切屏） |
| 假后端（`RecordingApi`）驱动写操作，无需真后端/模型 | `tests/test_tui_write_ops.py:33-77, 158-180, 215-219` |
| 轮询等待与就绪判定的既有约定 | `tests/test_tui_write_ops.py:187-200`（`_wait_for`）、`:203-212`（`_enter_main`；`ContentSwitcher` **只挂载当前屏**） |
| 确认框闸门测试（`y`/`n`） | `tests/test_tui_write_ops.py:222-238`；`src/tui_textual.py:343-384` |
| **启动回归矩阵已有 `tui` profile 门禁** | `scripts/startup_matrix.py:17, 35-38, 155-175`；用法 `docs/测试通道运行说明.md:96-101` |
| 「不许用 rerun 把失败变绿」的既有纪律 | `docs/测试通道运行说明.md:69-78` |
| 文档门禁已存在且有测试覆盖 | `scripts/run_doc_checks.py:37-40, 65-83`；`.github/workflows/checks.yml:47-48` |

### 1.5 「缺工件 / 缺模型 → 条件跳过」的既有写法（**照抄**）

| 写法 | 证据 |
|---|---|
| 整模块 `importorskip`（缺 UI 依赖就跳过） | `tests/test_tui_textual.py:46`；`tests/test_tui_write_ops.py:151` |
| 缺工件在用例/类级 skip | `tests/test_llama_keep_head.py:46-56, 168, 373` |
| 缺 GGUF 工件 → 条件跳过 | `tests/test_llama_relay_entry.py:8, 28, 33, 58` |
| 缺模型目录/隔离 venv → `pytestmark` skipif + RAM 资源门 | `tests/test_qwen3_network_sidecar_chain.py:10-13, 55-61, 372, 409` |
| 真模型必须显式开关才跑 | `tests/test_real_model_smoke.py:27, 99` |

### 1.6 演示材料现状（**较贫瘠**）

| 事实 | 证据 |
|---|---|
| `docs/figures/` 只有跨框架接力图表，**没有 TUI 截图/GIF** | `docs/figures/cross-frame-relay/`；`README.md:246` 是唯一图片引用 |
| 根目录 `qlh.jpg` 只被**历史文档**引用 | `docs/archive/distributed/总体下一步计划.md:92`、`docs/已知问题记录.md:580` |
| 旧走查脚本已**不可运行**（引用不存在的 `src/tui_chat.py`），且不在白名单 | `scripts/tui_chat_walkthrough.py:28` |
| 现行演示入口 = README/速览里的**手工命令** | `docs/项目速览-QLH-at-a-Glance.md:105-120` |

### 1.7 仓库纪律（必须遵守）

| 纪律 | 证据 |
|---|---|
| `scripts/*` 整体忽略 + **逐条白名单**才能入库 | `.gitignore:169-222`（**该文件属并行组在制品，本计划只给建议行、不改它**） |
| 文档必须声明状态 / 更新日期 / 适用范围 | `docs/文档状态与清理清单.md:22-45` |
| 文档门禁 = `run_doc_checks.py`（死链 + README 双语结构） | `scripts/run_doc_checks.py:37-40` |
| 票号体系：`CORE-*` 具名票 + `D<n>` 登记行 | `docs/验收清单与资源限制登记.md:53-85` |

### 1.8 可复用的「本机多段」做法（演示降级用）

| 做法 | 证据 |
|---|---|
| **同机多段不会被误报为分布式**（仓库显式口径） | `docs/验收清单与资源限制登记.md:143` |
| 本机多进程多段 relay 服务（**强制 loopback 绑定**） | `scripts/relay_mid_service.py:2-26` |
| 三段链路驱动参数（`--mid-endpoint` 可重复 + `--mid-stop` + `_MiddleChain`） | `scripts/relay_experiment.py:342-353, 1193-1199` |
| 纯 llama（L→L）三拓扑探针，含**全本地对照拓扑 C** | `scripts/relay_l2l_net_probe.py:14-29`；`tests/test_relay_l2l_net_probe.py:25-96` |
| 健康检查入口 | `docs/项目速览-QLH-at-a-Glance.md:117` |
| llama.cpp 同机双进程分片（2 节点形态） | `docs/验收清单与资源限制登记.md:75, 155`；`scripts/llama_rpc_sim.py` |

### 1.9 TUI 内可断言/可演示的「分布式 + 推理状态」钩子

| 钩子 | 证据 |
|---|---|
| 分布式屏开关（T）= `PUT /cluster/config/distributed-inference {enabled}` | `src/tui_textual.py:2212-2230`；断言样例 `tests/test_tui_write_ops.py:322-330` |
| 节点屏入群：`POST /cluster/join/request|grant|consume` | `src/tui_textual.py:2293-2370`；断言样例 `tests/test_tui_write_ops.py:332-362` |
| 侧栏每次操作后的可见摘要 | `src/tui_textual.py:1330-1331, 1517-1523` |
| 聊天路由偏好 `/route` → `app.routing_preference` | `src/tui_textual.py:508-517`；`docs/TUI使用指南.md:35` |
| 推理状态：`app.generation_id` 生成中置位 / 结束清空 | `src/tui_textual.py:1231-1232, 1264` |
| 推理结果文本与 metrics 状态行 | `src/tui_textual.py:1221-1269, 1253-1256, 459, 490` |
| 字幕/toast 原语 | `textual/app.py:4621`（`notify`）、`:3907`（`bell`） |
| 帧导出 | `textual/app.py:1855`（`export_screenshot`）、`:1889`（`save_screenshot`） |
| 退出 | `q`/`ctrl+c` → `src/tui_textual.py:1301-1305, 3271-3272`；`textual/app.py:976-990` |

---

## 二、方案选型

### 候选 A（**推荐**）：Textual `App.run_test()` + `Pilot` 原生驱动（headless）

- 机制：`run_test(size=(W,H))` 起 headless driver；`pilot.click(selector, offset)` / `pilot.press(...)` / `pilot.pause(delay)` 注入鼠标与键盘；断言直接读 widget 树与控件文本。
- **稳定性 高**：headless 无终端/字体/DPI 变量；`pilot.click` 自带落点校验。
- **可移植性 高**：Windows/Linux/macOS/CI 同一套代码；只用既有 `textual` + pytest。
- **可演示性 中高**：可逐帧 `save_screenshot()` 产证据；但**不能**直接当"真终端播放器"（真终端需另起进程）。
- **维护成本 低**：仓库已有同构用例可抄（`tests/test_tui_textual.py:53-111`、`tests/test_tui_write_ops.py:187-219`）。
- **局限**：headless **不覆盖**真终端渲染与鼠标上报链路 ⇒ 不得把 headless 通过当作"真终端可用"的证据。
- **未实测**：本仓库 `tests/` 零 `pilot.click` 先例 ⇒ 落地第一步先写 1 个探针用例。

### 候选 B：pty/伪终端 + ANSI 鼠标序列（SGR 1006）

- 机制：真终端里 Textual 会开 `1000h/1003h/1015h/1006h`，理论上可向 master 端写 SGR 序列模拟点击。
- **稳定性 低**：需要真 TTY + 已知窗口像素/cell 尺寸 + 坐标映射；Windows 走 Win32/ConPTY，行为需实测；断言只能解析 ANSI 屏（正则剥绘图字符），很脆。
- **可移植性 低**（`pty` 是 POSIX；Windows 要 ConPTY）；**维护成本 高**。
- 结论：**不作主路径**；可作为**可选的真终端冒烟档**（默认关闭、不进 CI），用来回答"headless 通过 ≠ 真终端能点"。**未实测**。

### 候选 C：外部 GUI 自动化（pyautogui / pywinauto / SendInput）

- **不选**：① 仅 Windows，焦点/DPI/遮挡导致 flaky；② 无 headless 断言能力，只能截图比对；③ 仓库零依赖零用法（全仓 grep 无命中）；④ 把"演示"与"测试"绑死在演示机桌面上，无法进 CI。

### 推荐结论

**A 为主**（门禁 + CI 断言 + 演示驱动），**B 作为可选真终端冒烟档（默认不跑）**，**C 不做**。
理由：仓库已用 A 写了 12 个 TUI 测试文件与 `startup_matrix --profile tui` 门禁，与既有纪律零冲突；B/C 都无法同时承担「可重复、可门禁、跨平台」。

---

## 三、测试 flow 设计

### 3.1 控件定位：语义优先，坐标仅作相对偏移

1. **首选 widget 语义选择器 / id**，沿用既有 id 契约：`#nav`（`src/tui_textual.py:1361-1364`）、`#content`（`:1365`）、`#topbar`（`:1358`）、各屏 pane/table（`:1366-1420`）、聊天 `#chat-input`/`#chat-status`/`#chat-log`（`:457-460`）。
2. **页面切换两把钥匙**：
   - 键盘（稳定、已有先例）：`pilot.press("]")` / `"["`（`src/tui_textual.py:1301-1302`；用例 `tests/test_tui_textual.py:102-109`）；
   - **鼠标（本计划的要求）**：`pilot.click("#nav ListItem#nav-cluster")`，或对 `#nav` 用 `offset=` 相对定位；
     落点校验：`ok = await pilot.click(...)`，`ok is False` 表示落点不在目标 widget 上 ⇒ 直接断言失败。
3. **消息链路**（鼠标点击→切屏的因果）：`ListItem` 被点 → `_on_list_item__child_clicked` → `Selected`（`textual/widgets/_list_view.py:388-392`）→ TUI 的 `on_list_view_selected` → `switch_page(...)`（`src/tui_textual.py:1425-1433`）。
4. ⚠️ 落地第一步先交 **1 个探针用例**（点 `#nav` 切屏 + `assert ok is True`），确认稳定后再铺剧本（**未实测**）。

### 3.2 断言层次：语义断言为主，字符画快照为辅

**L1 语义断言（门禁主体）** —— 只读控件/属性/消息，不读像素：

| 断言对象 | 读取方式 | 证据 |
|---|---|---|
| 侧栏高亮项 | `nav.index` | `src/tui_textual.py:1361-1364`；`tests/test_tui_textual.py:106` |
| 侧栏点击/选中消息 | 点 `ListItem` → `Selected` → `switch_page` | `_list_view.py:388-392`；`src/tui_textual.py:1425-1433` |
| 当前屏 id | `#content` 的 `ContentSwitcher.current == "page-<key>"` | `src/tui_textual.py:1365`；`tests/test_tui_textual.py:102-109` |
| 屏幕类型 | `isinstance(app.screen, SplashScreen/MainScreen/ConfirmScreen)` | `src/tui_textual.py:269, 343, 1275` |
| 控件存在性 | `app.screen.query_one("#topbar"/…)` | `tests/test_tui_textual.py:87-90` |
| 文本内容（语义） | `str(widget.render())` on `Static` | `tests/test_tui_textual.py:67, 91-92` |
| 后端不可达的降级语义 | `#status-pane` 含「后端不可达」 | `tests/test_tui_textual.py:114-135` |
| 推理进行中/结束 | `app.generation_id`（非空=生成中） | `src/tui_textual.py:1231-1232, 1264` |
| 推理结果 + metrics | `#chat-status`（经 `format_metrics`） | `src/tui_textual.py:459, 490, 1253-1256` |
| 请求级路由偏好 | `app.routing_preference` | `src/tui_textual.py:508-517` |
| 侧栏操作摘要 | `screen.op_status` + `#nav-summary` | `src/tui_textual.py:1330-1331, 1364` |
| **写操作真发出（最强证据）** | 假后端 `api.calls` 的 `method/path/body` | `tests/test_tui_write_ops.py:33-77, 267, 286, 329, 351` |
| **确认闸门先于请求** | 确认前 `api.calls` 为空；`n` 后仍为空 | `tests/test_tui_write_ops.py:222-238` |
| 退出 | `app._exit is True` / `app.return_code` | `textual/app.py:976-990, 1283-1285` |
| 版式（结构级） | 黄金比例 `0.382±0.03` | `tests/test_tui_textual.py:96-99` |

**L2 字符画快照（辅助，不做相等门禁）**：`app.export_screenshot()`（`textual/app.py:1855`）/ `app.save_screenshot(path)`（`:1889`）。**为何不做门禁**：SVG 含绝对坐标、主题色、与终端宽度相关布局 ⇒ 换 textual 小版本或换 `run_test(size=...)` 就整体漂移，且失败无法指出"哪个语义坏了"。仅用于：① 答辩留证；② 版式异常时人工定位。落盘目录用 `build/`（不入库）。

### 3.3 模型依赖隔离与退出码三态

**跳过写法（照抄既有约定）**：

```python
# ① 缺 UI 依赖 ⇒ 整模块跳过（不是失败）
pytest.importorskip("textual")                    # tests/test_tui_textual.py:46

# ② 缺模型/工件 ⇒ 模块级 skipif + 写明缺什么（真机工件门）
pytestmark = [
    pytest.mark.real_model,
    pytest.mark.skipif(not MODEL_PATH.is_dir(),
        reason="需要真实模型工件（真机工件门）"),
]                                                 # tests/test_qwen3_network_sidecar_chain.py:55-61

# ③ 缺工件 ⇒ 函数/夹具级 skip，注明相对路径与补齐方式
pytest.skip(f"需要 keep-head shim（{SHIM.relative_to(ROOT)}）；…")   # tests/test_llama_keep_head.py:46-56

# ④ 资源门（RAM/显存不足）同样跳过
pytest.skip("可用 RAM < 3GB（资源门）")            # tests/test_qwen3_network_sidecar_chain.py:372
```

**一律 `skip`，不用 `xfail`、不静默 `pass`**（`tests/test_qwen3_network_sidecar_chain.py:10-13`：「真机工件门，不虚构证据」）。

**两档划分**：

| 档 | 内容 | 依赖 | 门禁地位 |
|---|---|---|---|
| **F1 假后端（零模型）** | `RecordingApi`（`tests/test_tui_write_ops.py:33-77, 158-180, 215-219`）或 `qlh chat --fixture` 离线 SSE 回放（`qlh.py:206-209`） | 仅 `textual` + pytest | **必跑**，纳入 `unit` 通道 + `startup_matrix --profile tui` |
| **F2 真后端 + 真模型** | 真 `api_server` + 真权重 | 模型工件 + 隔离 venv + 显式开关 | **默认不跑**：需 `QLH_RUN_REAL_MODEL_SMOKE=1`（`tests/test_real_model_smoke.py:27, 99`），走 `smoke` 通道且**串行** |

**脚本退出码三态**（与 pytest 的 skip 语义对齐）：`0` = 全通过；`1` = 有断言失败；`2` = **环境缺失（等价 skip）**——缺 `textual`／缺模型工件／缺隔离 venv，**不判为失败**。门禁侧仍以 `skipped` 呈现，**不得**把 skip 改写成 `passed`。

### 3.4 超时与重试

- **复用既有约定，不另发明**：`_wait_for(pilot, predicate, timeout=6.0)`（`tests/test_tui_write_ops.py:187-200`）——轮询期间 `NoMatches` 视为"未就绪"；就绪判定**不得用屏内控件**，因为 `ContentSwitcher` **只挂载当前屏**（`:203-212`）。
- 节奏用 `pilot.pause(delay)`（`textual/pilot.py:535`）：启动屏逐字 0.45s、数据表填充 2.0s、确认后 0.1–0.3s（既有用例值）；能等条件的用 `_wait_for`，不要固定 sleep。
- 单用例预算受 `pytest.ini:14` 的 `--timeout=300` 约束；单步 `_wait_for` 上限取既有 6.0s；真模型档另立 `real_model`/`slow` 并串行，避免与默认 `-n 4` 争内存（`pytest.ini:15-20` 记录过 16GB 机器被击穿的事件）。
- **不引入 rerun/flaky 把失败变绿**：仓库明文「重复执行用于观察 flaky，不是失败重试」「不使用 rerun 插件把失败隐藏成绿色」（`docs/测试通道运行说明.md:69-78`）。要暴露顺序相关抖动，用 `--repeat` + `--qlh-order-seed`（`tests/conftest.py:27-34`）。

---

## 四、答辩演示复用

### 4.1 同一脚本两种模式：`--mode assert` / `--mode demo`

同一可执行文件、同一份 step 剧本、同一执行器，只切换"节奏 + 输出 + 失败策略"：

| 维度 | `--mode assert`（门禁） | `--mode demo`（答辩） |
|---|---|---|
| 节奏 | 取最小等待（同 §3.4） | 每步前后 `await pilot.pause(pace)`，`--pace` 默认 1.2–1.5s，现场可调 |
| 字幕 | 无 | 每步 `app.notify(caption)`（`textual/app.py:4621`）/ `bell()`（`:3907`）；或写 `#topbar`、`#chat-status`（**不得改其语义**） |
| 留证 | 只出 JSON 结果 | 每步 `save_screenshot("build/tui-e2e/frames/{step_id}.svg")`（`app.py:1889`） |
| 失败策略 | 立即失败，停在首个不满足的 `expect` | **降级而不中断**：标该步 `DEGRADED`、打印原因、继续播（现场不黑屏） |
| 退出码 | `0` 全通过 / `1` 有失败 / `2` 环境缺失 | `0` 全 PASS / **`3` 有 DEGRADED** / `1` 有 FAIL / `2` 环境缺失 |

**硬约束**：`--mode demo` 只要出现 DEGRADED/FAIL **绝不允许返回 0**（避免"演示变绿"被误读为验收通过）；演示模式**不进门禁、不进 CI**。

### 4.2 演示分布式推理：需要几个节点 / 无多节点时怎么降级

| 档 | 节点数 | 形态 | 依据 |
|---|---|---|---|
| **D-a 本机 loopback 多段** | 1 台机、多进程 | 本机起 N 个 `relay_mid_service.py --role middle\|tail --listen 127.0.0.1:<port>`，驱动侧 `--mid-endpoint`（可重复）+ `--mid-stop`；或用 `relay_l2l_net_probe.py` 的**全本地对照拓扑 C** | `scripts/relay_mid_service.py:2-26`（**强制 loopback 绑定**，跨机才用 `ssh -L`）；`scripts/relay_experiment.py:342-353, 1193-1199`；`scripts/relay_l2l_net_probe.py:14-29` |
| D-b 同机双进程分片（2 节点语义） | 1 台机、host + 1 个 `ggml-rpc-server` | 部分驻留 / 容量合并 / 断开回退 | `docs/验收清单与资源限制登记.md:75, 155` |
| D-c 真跨机 | ≥2 台（同 Tailscale/局域网） | TUI 集群/节点命令让节点上线 + 层段接力 | `docs/项目速览-QLH-at-a-Glance.md:66, 120` |

**TUI 侧的演示动作不依赖第二台机器**（F1 假后端即可）：分布式屏 `T` → `PUT /cluster/config/distributed-inference`（`src/tui_textual.py:2212-2230`；样例 `tests/test_tui_write_ops.py:322-330`）；节点屏 → `POST /cluster/join/*`（`:2293-2370`；样例 `:332-362`）；聊天页 `/route distributed_preferred` → `app.routing_preference`（`:508-517`）+ `#chat-status` 的 route/metrics（`:1253-1256`）。

**降级链路（本机多段）**：

```bash
python scripts/relay_mid_service.py --role middle --listen 127.0.0.1:50161 \
    --keep-head-shim build/keephead/build-cpu/bin/qlh_keep_head.dll \
    --model build/cross-framework-layer-poc/out/qwen25-05b-f16-mid8-12.gguf \
    --ready-file build/relay-records/mid.ready
python scripts/relay_mid_service.py --role tail --listen 127.0.0.1:50183 \
    --model build/cross-framework-layer-poc/out/qwen25-05b-f16-cut-k12.gguf --ready-file build/relay-records/tail.ready
python scripts/relay_l2l_net_probe.py --head-model <head8.gguf> \
    --middle-endpoint 127.0.0.1:50161 --tail-endpoint 127.0.0.1:50183 \
    --whole-model <同精度整模.gguf> --prompt <prompt.txt> --prefill 32 --gen 32 \
    --json-out build/relay-records/l2l-net-3seg-loopback.json
python scripts/relay_health.py --check tail=tcp:127.0.0.1:50183 --json   # 0=全健康 / 1=有异常
```

> ⚠️ **单序列长 decode 用 `--n-seq-max 1`**（或把 `--n-ctx` 放大到 `n_seq_max` 倍）：llama.cpp 把
> `n_ctx` 按 `n_seq_max` **均分**，否则会在中途报 `llama_decode rc=1`。详见
> `docs/跨框架接力-当前有效基线与后续优化计划-2026-09-21.md` 的 P5 段。

**绝不允许把 loopback 多段说成物理分布式**：仓库判据明确「`is_distributed` **只由跨机节点决定**，因此同机多段/跨引擎接力**不会被误报为分布式**」（`docs/验收清单与资源限制登记.md:143`）。演示输出必须打标签 `topology=loopback_multiseg`、`physical_dual_host=false`、`real_model_loaded=<true|false>`。

### 4.3 两种模式共用同一份 step 剧本

- **单一来源**：`fixtures/tui_e2e_flow.json`（`fixtures/` 已入库）。`caption`（只给 demo 用）与 `expect`（只给 assert 用）**字段分离**，避免"演示文案变了导致断言口径漂移"：

```json
{
  "id": "cluster-toggle",
  "caption": "打开分布式推理开关（PUT /cluster/config/distributed-inference）",
  "actions": [{"click": "#nav ListItem#nav-cluster"}, {"press": "t"}, {"press": "y"}],
  "expect": {
    "screen": "MainScreen",
    "calls": [{"method": "PUT", "path": "/cluster/config/distributed-inference"}]
  },
  "timeout_s": 6.0
}
```

- `actions` 词表（`click` / `press` / `hover` / `wait`）与控件定位一致 ⇒ **同一动作被两个模式解释**：assert 逐条硬校验 `expect`；demo 播同一动作 + 打 `caption` + 存帧。
- `expect` 只允许 §3.2 的 L1 语义断言（`screen` / `current` / `text` / `nav_index` / `calls` / `generation_id`），**不允许**放 SVG/像素断言。
- 既有先例支持"一处定义、多处复用"：`scripts/run_doc_checks.py:2-8` 明说收敛清单的动机就是"一处定义、两处复用，否则迟早漂移"；命令表 `src/tui_shared.py:266-323` 亦同哲学。

---

## 五、落地清单

### 5.1 新增文件（建议）

| 文件 | 作用 |
|---|---|
| `scripts/tui_e2e_flow.py` | 单一执行器：`--mode assert\|demo [--pace S] [--frames DIR] [--scenario PATH] [--json-out PATH]` |
| `fixtures/tui_e2e_flow.json` | step 剧本单一来源（`actions` / `expect` / `caption` 分离） |
| `tests/test_tui_e2e_flow.py` | pytest 入口：只调 `--mode assert` 的同一份 step 列表，逐条转用例；F2 档按 §3.3 skip |

### 5.2 需要改的既有文件（**最小**）

| 文件 | 改什么 |
|---|---|
| `.gitignore` | 追加白名单行（**建议，由用户/并行组执行**）：`!scripts/tui_e2e_flow.py` —— 该文件属并行组在制品，本计划**不代改** |
| `scripts/startup_matrix.py` | 往 `TUI_TESTS`（`:35-38`）追加 `"tests/test_tui_e2e_flow.py"`；**只加文件名、不改判据** |

### 5.3 票号建议

- 具名票 **`CORE-TUI-E2E-01`**（端到端 flow 测试，F1 档入门禁）；演示复用部分若需独立跟踪，可在 `docs/验收清单与资源限制登记.md` 的 D 节追加登记行（沿用 `D<n>` 形态）。

### 5.4 验收判据（可直接执行）

```powershell
# 1) 断言模式（进门禁的那档）
.venv-test\Scripts\python.exe scripts/tui_e2e_flow.py --mode assert --json-out build/tui-e2e/latest.json
#    期望 exit 0；全失败 exit 1；环境缺失 exit 2

# 2) 演示模式（人工/答辩）
.venv-test\Scripts\python.exe scripts/tui_e2e_flow.py --mode demo --pace 1.5 --frames build/tui-e2e/frames
#    期望 exit 0（全 PASS）/ 3（有 DEGRADED）/ 1（有 FAIL）；生成 build/tui-e2e/frames/*.svg

# 3) 纳入既有 TUI 启动档门禁
.venv-test\Scripts\python.exe scripts/startup_matrix.py --profile tui        # 期望 exit 0

# 4) 单元通道无回归
.venv-test\Scripts\python.exe scripts/run_test_channels.py --channel unit    # 期望 exit 0

# 5) 文档门禁
python scripts/run_doc_checks.py                                            # 期望末行 [verdict] 文档检查全部通过

# 6) 演示降级链路（本机多段，需裁层 GGUF；缺工件应跳过/exit 2）
python scripts/relay_health.py --check tail=tcp:127.0.0.1:50183 --json      # 0=全健康 / 1=有异常
```

---

## 六、风险与不做的事

### 6.1 已知风险（含规避）

1. **headless ≠ 真终端**：`run_test()` 走 HeadlessDriver，不覆盖真终端鼠标上报与 conhost/字体差异。规避：文档明确"headless 通过 ≠ 真终端可用"；可选 B 档作真终端冒烟（默认关、不进 CI）。
2. **`pilot.click` 在本仓库零先例**（**未实测**）。规避：落地第一步先交 1 个探针用例，确认稳定后再铺全量剧本。
3. **页面挂载竞态**：`ContentSwitcher` 只挂载当前屏，直接 `query_one` 未显示屏控件会抛 `NoMatches`。规避：只用 `_wait_for`，就绪判定不依赖已挂载控件。
4. **textual 版本漂移**：`requirements-tui.txt:8` 是 `textual>=8,<9`，SVG/选择器语义可能在小版本内变化。规避：断言只用公共 API 与 id 契约；SVG 不做相等门禁。
5. **演示现场时长不可控**：真后端冷启动有 30s 上限（`src/tui_backend.py:30, 113-128`）。规避：答辩默认用 F1 假后端档（零等待），真后端档单独预热；剧本支持 `--pace` 与跳过某步。
6. **被误读为"分布式已验收"（最大风险）**：规避：字幕/报告强制打印 `topology`、`physical_dual_host=false`、`real_model_loaded=`，并引用 `docs/验收清单与资源限制登记.md:143` 的口径。

### 6.2 明确不做的事

1. **不放宽任何既有验收门禁**；数值一致性**只认 per-token argmax**，不用 cosine/top-k/时长替代。
2. **不为演示改动生产代码路径**：不改 `src/tui_textual.py` 的行为、不加"演示专用分支"、不在生产入口埋 demo 开关；演示只通过公共 API 驱动（`Pilot` + `App.notify` / `save_screenshot`）。
3. **不引入外部 GUI/浏览器自动化依赖**（pyautogui/pywinauto/Selenium/Playwright 用于 TUI）；仓库当前零命中。
4. **不做 pty + SGR 的主路径实现**；B 档若做，必须默认关闭且不进 CI。
5. **不复活自绘 ANSI TUI**，不重建已消失的 `src/tui_chat.py` / `tui_admin.py`，不修 `scripts/tui_chat_walkthrough.py`（不可运行的遗留物；如要清理，按 `docs/文档状态与清理清单.md:37` 的"先归档后删"单独开票）。
6. **不把演示模式接进 CI/门禁**：演示模式退出码非 0 语义与 CI 的"绿色即通过"冲突。
7. **不用 rerun/flaky 插件掩盖失败**；不写"失败自动重试 N 次"的自旋。
8. **不产出任何夸大的演示结论**：不宣称真模型已加载、不宣称双机物理分布式、不把 loopback 多段/受控预算写成物理 OOM 或跨机结论。
9. **不真改 `.gitignore`**：只给建议行（该文件属并行组在制品）。
10. **不碰并行组在制品**：`src/torch_*.py`、`scripts/torch_*.py`、`tests/test_torch_*.py`、`src/journal_replication*.py`、`tests/test_journal_replication.py`、`docs/主节点动态选举*`、`.gitignore`。
11. **不新增第三方依赖**：F1 档只用 `textual`（既有）+ 标准库 + pytest（既有）。

---

## 七、落地现状（2026-09-24）

| 计划项（§5） | 状态 | 实际落点 |
|---|---|---|
| 剧本单一来源 | ✅ | `fixtures/tui_e2e_flow.json`（`actions` / `expect` / `caption` 分离；`nav_ids` 同源） |
| 单一执行器 `--mode assert\|demo` | ✅ | **引擎在 `tests/tui_e2e_flow.py`**（不是 `scripts/`，见 §7.1） |
| pytest 入口（逐条转用例） | ✅ | `tests/test_tui_e2e_flow.py`（**14 例**） |
| 纳入 `tui` 启动档 | ✅ | `scripts/startup_matrix.py` 的 `TUI_TESTS` 追加一行（只加文件名、未改判据） |
| `.gitignore` 白名单行 | ⏸ **未做**（§5.2 即如此约定） | 需 `!scripts/tui_e2e_flow.py`；由用户/并行组执行 |

### 7.1 与计划的两点差异

1. **引擎落点**：§5.1 原写 `scripts/tui_e2e_flow.py` 作单一执行器，但 `scripts/*` 被 `.gitignore` 整体忽略
   + **逐条白名单**（`.gitignore:169-222`，该文件属并行组在制品）⇒ 放那里在补白名单行**之前不会入库**，
   等于核心逻辑没交付。用户 2026-09-24 裁定（`dec-fc91fb5c966c7786`）：**引擎放 `tests/tui_e2e_flow.py`**
   （可入库），`scripts/tui_e2e_flow.py` **只做转发壳**；两者**命令行等价**。
2. **探针先于剧本**：§6.1 风险 2 要求「先交 1 个探针用例，确认稳定后再铺全量剧本」—— 已照做，结果见 §7.2。

### 7.2 探针实测结论（消除 §1.3 的「未实测」）

| 探明的行为 | 实测结果 |
|---|---|
| `pilot.click("#nav ListItem#nav-<id>")` 切屏 | ✅ 稳定；`ContentSwitcher.current` 与 `nav.index` 同步 |
| 全 10 屏由鼠标点击到达 | ✅ 全部可用（非「只对某项生效」） |
| `click` 命中目标时的返回值 | `True`（`bool`） |
| 落点在**屏幕内**但**不在目标 widget 上** | 返回 `False`（不抛错）⇒ 返回值本身即断言 |
| 落点**超出可见屏幕区域** | 抛 `textual.pilot.OutOfBounds`（**不是** `False`） |
| selector **不存在** | 抛 `textual.css.query.NoMatches`（不静默成功） |
| 连跑 | **8/8 全绿**（10.0–10.9 s 量级）⇒ 非偶发 |

### 7.3 复现命令与实测结果

```powershell
.venv-test\Scripts\python.exe tests\tui_e2e_flow.py --mode assert --json-out build/tui-e2e/latest.json
#   ⇒ exit 0；10 步全 PASS
.venv-test\Scripts\python.exe tests\tui_e2e_flow.py --mode demo --pace 0.15 --frames build\tui-e2e\frames
#   ⇒ exit 0；生成 10 张 SVG（build/ 下，不入库）
.venv-test\Scripts\python.exe -m pytest tests\test_tui_e2e_flow.py -q
#   ⇒ 14 passed
```

⚠️ **边界**：以上全部是 **headless + 假后端 + 零模型**。**不得**据此宣称真模型已加载、双机物理分布式，
或「分布式推理已验收」（§6.1 风险 6）。**F2 档（真后端 + 真模型）仍后置**；D-a 档降级演示链路
（本机 loopback 多段）需裁层 GGUF 工件，缺工件时应按 §3.3 跳过。
