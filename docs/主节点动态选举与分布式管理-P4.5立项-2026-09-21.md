# P4.5 立项：主节点动态选举与配套分布式管理（2026-09-21）

> 状态（2026-09-23）：**`HA-CONTRACT-01`、`HA-STATE-CATALOG-01`、`HA-QUORUM-01`、`HA-FENCE-01`、`HA-PARTITION-01`、`HA-ROLE-AUTO-01`、`HA-HANDOFF-01`、`HA-RECOVERY-01`、`HA-SCORE-OBS-01`、`HA-CROSSHOST-01`（本机/物理部分证据）和 `HA-TUI-01` 已有代码与测试产物**。quorum/fencing 已实现并保持 fail-closed；`NODE_ROLE=auto` 仍未默认启用，生产 HA 可用性仍受物理弱网、长时、持久化日志和 Android APK/JNI 证据限制。
> 触发：P4 收口后发现当前实现的一个结构性不合理 —— 见 §1。

## 1. 问题：主节点身份是"部署时钉死"的，而且理由已经消失

**当前实现（源码证据）**：

| 位置 | 事实 |
| --- | --- |
| `src/config.py:450` | `NODE_ROLE` 默认值 = `"master" if getattr(sys, "frozen", False) else "client"` —— 角色来自**环境变量/打包形态** |
| `src/config.py:452` | `_normalize_node_role(_initial_role)` —— 角色是一次性解析的常量 |
| `src/config.py:485` | `CLIENT_MASTER_HOST` / `CLIENT_MASTER_PORT` —— 从节点连谁，是**静态配置** |
| `src/bootstrap.py:76` | `select_advertised_master_host()` —— 只决定"广播哪个 host 当入口"，**不是选举** |
| `src/api_server.py:7470` | 已有 `presence_generation` / `presence_lease_id` —— 存在**租约与代次**的概念，但用于 Android presence，**没有用于主节点身份** |

**为什么这是结构性问题**：

1. **主节点的职责已经与 GPU 算力解绑**。P0–P4 之后，主节点承担的是控制面：设备画像、容量计划、
   层段合同、准入、租约与 epoch fencing、任务图调度。它**不需要 CUDA**，也不是吞吐瓶颈所在。
   ⇒ 但"谁是主节点"仍然由**部署时的一个环境变量**决定 —— 于是整个集群的管理角色被钉在
   一台**与它的实际能力无关**的机器上（可能是最弱的那个）。
2. **边缘集群的算力排序本来就会变**：设备上/下线（y700 会休眠、Surface 会合盖）、负载波动、
   热降频、网络路径变化。静态身份无法跟随，只能靠人工改配置重启。
3. **代价不对称**：主节点选错（选到弱机）会持续拖慢整个集群的调度与准入；而"谁是主节点"
   恰恰是**最容易按能力自动决定**的一件事。

用户裁定（2026-09-21）：
> 主节点身份就不该是永久的，而是自行选择 CPU 算力最强的那个；还要实现配套的分布式管理机制。

## 2. 目标

| # | 目标 | 可验证形态 |
| --- | --- | --- |
| G1 | **受约束自选**：只有拿到集群授权的候选才能按管理能力排序成为主节点 | 有效多数派时产生唯一可写主；无多数派时所有候选降为只读/不可提交 |
| G2 | **可迁移**：更强的候选上线、或现任主节点降级/掉线时，主节点身份可以**交接** | 交接完成后旧主不再是主，且新 term/epoch 由授权集签发并严格递增 |
| G3 | **不脑裂**：任何时刻**至多一个**持有有效 quorum certificate 的主节点可以提交状态 | 旧主失去有效证书后写入被拒（fail-closed）；本地看到更大 epoch 不能单独取得写权 |
| G4 | **不改数值面**：主节点迁移**不得**改变任何推理结果的判据 | 迁移前后同一 prompt 的 per-token argmax 一致（沿用现有判据） |

G4 是边界：P4.5 是**控制面**重构，必须证明它对数值面是透明的（这也是"系统软件内核 / 用户软件外壳"
分层里，控制面可以演进的那部分 —— 见 README「这是什么软件」一节）。

## 3. 设计

### 3.1 能力评分（候选排序）

主节点候选按**管理能力**打分，而不是按推理能力。建议维度（都可本地采集，无需特权）：

| 维度 | 采集方式 | 权重意图 |
| --- | --- | --- |
| CPU 逻辑核数 | `os.cpu_count()` | 主要项（用户明确要求"CPU 算力最强"） |
| CPU 基准分 | 启动时跑一次固定微基准（矩阵乘/哈希，~100 ms，结果入缓存） | 区分同核数不同代次 |
| 可用内存 | `psutil.virtual_memory().available` | 控制面内存需求与容忍度 |
| 当前负载 | 采样 1/5/15 分钟负载或进程 CPU | 避免选到已被推理占满的机器 |
| 稳定性 | 在线时长 + 近期心跳丢失次数 | **平台中性**：按可测的在线/抖动表现排序；"平台是 Android 所以降权"**不得**作为内建规则（见 §3.5） |
| 网络位置 | 与多数节点的 RTT 中位数（tailnet 直连/中继） | 减少控制面延迟 |
| 角色约束 | 是否被运维显式钉为 `client`（opt-out） | 尊重人工意愿 |

**输出**：每个节点一个 `master_score`（浮点）+ `master_score_version`（评分算法版本，
避免不同版本节点互相比较）。评分**不参与数值面**，只影响排序。

**关键取舍**：评分是**启发式**，因此选举必须能容忍"选错"。为此 G2（可迁移）比"选得绝对准"更重要 ——
先保证能换，再逐步调准。

### 3.2 选举与续租（谁持有 quorum certificate，谁是主）

原设计的“广播 + 先到先得 + `max(seen_epoch)+1`”不能证明 G3：网络分区时两侧都能看到租约过期、各自生成 epoch 并继续写入。新的安全基线如下：

1. 集群维护带版本的 `voter_set`。每个投票节点使用已有 Ed25519 节点身份签名；节点能力/score 只决定候选顺序，不产生投票权。
2. 选举、续租和 term 分配必须取得上一版 `voter_set` 的严格多数签名，形成 `QuorumCertificate{cluster_id, voter_set_epoch, term, leader_id, lease_id, expires_at, signatures}`。没有多数派时不得产生可写主。
3. 一个 voter 在同一 `voter_set_epoch + term` 最多签一个候选；term 由持久化的投票/任期记录推进，不能由候选单独用 `max(seen_epoch)+1` 生成。
4. 状态提交（容量计划、层段合同、任务图控制状态、模型/分片租约）必须携带证书摘要和 `(term, lease_id)`。接收方只有在证书可验证、租约未过期、term 不低于本地已提交 term 时才接受；只有本身仍能维持多数派的主才允许继续写。
5. `NodePresence` 仅用于发现和候选观测；它不能授予主权。广播/发现也不能替代已认证的 TCP/控制面请求。

这不是引入完整 Raft 数据复制，而是先实现最小的多数派租约/投票合同。单节点或两节点没有外部 witness 时，自动故障接管必须保持关闭；可以继续使用人工 TOTP 转让，但转让结果仍必须落入同一 term/证书校验路径。

### 3.3 交接（handoff）

主节点迁移不是"重新开始"，要交接四类状态：

| 状态 | 交接方式 | 失败时的行为 |
| --- | --- | --- |
| 任务图（`TaskGraphCoordinator` 的在途任务与预约） | 先只转移已落盘、可重放的 journal 状态；当前在途任务不得假定可从 `snapshot()` 恢复 | 旧主失联时任务按 lease 超时/幂等重试处理；在途任务不得声称“已迁移” |
| 容量计划 / 层段分配（`pipeline_capacity` / `pipeline_reshard`） | 传 manifest（已是可序列化契约） | 新主重新求解，不猜测旧主状态 |
| 租约与预约（worker 预约、模型加载预约） | 按 `term/lease_id` 转移；旧 term 的预约在新 term 下**默认失效** | 默认失效而非默认继承（避免幽灵预约占用算力） |
| presence / 心跳表 | 新主从自己的视角重建，旧主数据仅作参考 | 允许短期不一致，以心跳为准 |

**交接的原子性**：term/证书签发、manifest 摘要和新主状态提交必须由同一控制面事件记录；不能把两个节点各自写入本地 SQLite 描述成跨节点原子事务。未完成证书提交时维持只读或回退到人工接管。

### 3.4 与现有组件的关系

| 组件 | 复用 / 新增 |
| --- | --- |
| `presence_generation` / `presence_lease_id`（`api_server.py:7470`） | **不直接复用字段**：它属于 Android presence 生命周期；只复用 TTL/过期校验的编码经验，新增独立 control-lease schema |
| epoch fencing（`test_task_graph_fencing` 覆盖的机制） | **复用判据，不复用为选举权威**：它必须增加 quorum certificate/term 验证，单个节点的已见 epoch 不足以裁决 |
| `bootstrap.discover_master_via_tailnet` / `select_advertised_master_host` | **改造**：从"发现一个静态入口"改为"发现当前租约持有者" |
| `src/config.py:450` 的 `NODE_ROLE` | **先保兼容、后引入模式**：当前 `master`/`client` 仍是静态兼容模式；`auto` 只能在安全契约票完成后启用。显式 `master` 只能作为单机/引导/人工维护模式，不能绕过 fencing |
| 设备画像（`device_profiler.py`） | **新增维度**：CPU 基准分与稳定性（现有画像偏推理能力，需补管理能力） |
| TUI 节点页 | **新增展示**：当前主节点、score、epoch、上次交接原因 |
| `presence_heartbeat`（Android 侧已实现） | 只扩展观测字段；**平台不作为投票资格的判据**（见 §3.5），是否参与投票由能力合同与显式配置决定 |

### 3.5 集群构成中性：不假设有 PC，也不预设平台排除

**两条约束（2026-09-21 用户裁定）**：

1. **不假设集群里有 PC 节点**。边缘集群的实际构成**未知**——无法确认现场是 Linux 开发板更多
   还是 Android 设备更多，也完全可能**一个 PC 都没有**。因此"主节点/上游需要一台 PC"这类
   隐含前提不得出现在设计里。
2. **不预设"Android 不能当主节点"**。此前草案里"Android 默认不自动取得 voter 身份"是一个
   **未经验证的假设**（依据只是"移动端会休眠"），**降级为待定项**，并且必须做成
   **可更换、可裁撤**的策略：

| 项 | 调整后的做法 |
| --- | --- |
| voter 资格 | **只看能力合同 + 显式配置**（证书、term、可用性、score 下限），**不按平台预判**；Android 与 Linux 板走同一条资格路径 |
| "移动端降权" | 从**内建规则**降为**可配置策略**（默认仅按可测的在线/抖动表现排序）；策略本身有版本号，可整包更换或删除 |
| 决策依据 | 新增一张**设备构成调查票**（现场 Linux 板 vs Android 的比例、休眠模式、供电与常驻条件），作为"是否值得引入平台相关策略"的**输入**，而不是先写死结论 |
| 可裁撤性 | 任何平台相关策略都必须能**在不改 schema、不改协议**的前提下关闭；关闭后集群行为退化为"纯能力排序" |
| 休眠抖动 | 用**机制**解决而不是用平台标签解决：任期最短存活时间 + 续租 TTL + 交接确认（§3.2/§3.3），这对任何会休眠的设备都成立 |

**退化路径：没有 PC 时，层接力退化为纯 llama 接力**。D 档（PyTorch 上游）是**可选的加速/对照
手段，不是接力的前提**；当集群里没有任何适合跑 torch 的节点时（纯边缘集群），接力应退化为
**L→L**：所有段都由 llama.cpp 承载（head GGUF + 中间段 keep-head + cut GGUF 下游），并且
**具备 GPU 的 Android 平板也按同一路径参与**（GPU 后端由 llama.cpp 侧提供）。

- 仓内已有这条路径的 2 段形态：`l2l_llama` / `l2l_keep_head`（两侧都是 llama.cpp，中间用
  keep-head 通道吐 hidden）；多段形态见 §P2 续「多段拓扑」；
- **待验证项**：L→L 多段在"纯边缘集群"（无 PC、含 Android GPU 设备）上的数值与性能证据；
  Android 侧 GPU 后端的可用性与开关（属 D/E 层的既有工作，见 android 验证文档）；
- **判据不降级**：退化路径仍要求 per-token argmax 与同精度整模一致（不得因为是"无 PC 场景"
  就放宽判据）。

**已取得的证据（2026-09-21）**：探针 `scripts/relay_l2l_net_probe.py`（**用主仓组件**：
`llama_keep_head.KeepHeadUpstream` 作上游、`relay_transport.RelayTcpClient.request_hidden/.request_token`
作远端段、`llama_cpp` 整模作对照；判据 per-token argmax）：

| 拓扑 | 链路 | 结果 |
| --- | --- | --- |
| C 对照（全本机） | head12(本机 shim) → tail(本机) | ✅ **32/32** |
| 三段跨机（中段在真机） | head8(本机 shim) → **mid8-16(y700)** → cut-k16(本机 tail) | ✅ **32/32** |

⇒ **"纯 llama、无 torch 参与"的两段与三段链路都成立**，且**真 Android 设备（ARM64）可以承担中间段**。

**尚未取得的证据（下一步的明确实现项）**：要做到"**本机完全不参与**"（head 与 tail 也落在设备上），
需要两件**服务端能力**：

1. ✅ **末段走 shim（本轮已实现并验证）**：`relay_mid_service.py --role tail` 加 `--keep-head-shim`
   即改用新增的 `ShimTailRunner`（吃 hidden → 吐 token）。为此给 shim 增加了
   `qlh_kh_forward_embd_token`（与 `qlh_kh_forward_embd_seq` **共用同一条 decode 路径**，只把输出
   换成末位 argmax ⇒ 数值口径天然对齐），绑定侧按**可选符号**处理（旧 shim 仍可用；缺这一个入口时
   给明确错误而不是静默降级）。**验证**：同一 head 工件 + 同一 prompt，`pip tail` 与 `shim tail`
   各自与整模对比均 **32/32**（记录 `build/relay-records/l2l-net-tail-{pip,shim}.json`）
   ⇒ 两者等价，**设备侧无需 pip `llama_cpp`**。
2. ✅ **设备端 `--role head`（本轮已实现并验证）**：吃 **token** 吐 **hidden** —— 即上游段也跑在
   设备上。为此做了**协议扩展**：新增 `RelayFrameKind.TOKENS = 6`（客户端送 token id 列表，
   远端回 `HIDDEN`）+ `encode_tokens` / `decode_tokens` / `RelayTcpClient.request_hidden_from_tokens`
   + `serve_relay_middle_connection` 的 `TOKENS` 分支（旧 runner 收到该帧一律
   `token_frame_unsupported` **fail-loud**，绝不猜测帧语义），错误码白名单同步 +2；
   服务端加 `--role head` + `HeadRunner`（复用 keep-head 的 token 入口，设备侧无需 pip `llama_cpp`）。

**完全无本机的链路（2026-09-22 真机验证）**：

| 拓扑 | 链路 | 本机角色 | 结果 |
| --- | --- | --- | --- |
| **单设备两段** | **y700 head12（blk.0..11）→ y700 cut-k12 tail（blk.12..23 + head）** | **只发命令 + 整模对照**（不参与计算） | ✅ **32/32** |

⇒ 这条链路**全程 llama.cpp + shim**（无 torch、无 pip `llama_cpp`、无本机模型计算），
即"无 PC 边缘集群"下退化路径的**完整形态**已跑通（记录
`build/relay-records/l2l-net-device-only-2seg.json`）。

**仍可加强（下一步）**：**跨设备**形态（如 Surface 作 head 段 + y700 作 tail 段）。路径已通：
head 段只需 `forward_tokens_to_hidden`（**旧 shim 就有**），因此 Surface 侧无需升级 shim，
只需新绑定与新服务脚本 + 一份 head 工件。

**关于"不能在设备上装 llama-cpp-python"（上一轮表述已修正）**：

准确说法是**不是不能编，而是成本与风险明显更高**。实测依据（本机 pip 绑定）：

| 补丁符号 | `llama_cpp.llama_cpp` |
| --- | --- |
| `llama_get_embeddings_nextn_ith` | **MISSING** |
| `llama_get_embeddings_layer_inp` | **MISSING** |
| `llama_set_embeddings_layer_inp` | **MISSING** |
| `llama_set_embeddings_nextn` | **MISSING** |
| `llama_get_model` / `llama_vocab_n_tokens` / `llama_get_logits_ith` | 都在 |

⇒ 用 pip 版做 **head/middle** 需要三件事：① 把它 vendor 的 llama.cpp 换成我们的补丁版（**可行**）；
② **给 `llama_cpp/llama_cpp.py` 补符号声明** —— 它是**逐函数 ctypes 声明**，不认识我们新增的符号，
**这才是真正的卡点**；③ 长期跟进上游维护这个 fork。此外 pip 版的构建选项（`GGML_*`、后端、OpenMP）
由它的 CMake 默认值决定、**不受我们控制**（而 shim 用我们自己的构建脚本），且编译要发生在**设备侧**
（Termux 现场装 clang/cmake/ninja，Surface 需 MSVC/MinGW，每个 Python 版本各编一次）。
相反，**tail 段不需要补丁**（只用 embd 注入 + logits 等标准 API）⇒ 若某设备**只做 tail**，
用 pip 包是可接受的**过渡手段**，但记录必须写清该段用的是**外部 pip 版本**。

**当前诚实结论**：退化路径的**数值正确性**已证（纯 llama 两段/三段 32/32，含真机中段；tail 的
shim 实现与 pip 实现等价）；"**位置上也完全不依赖 PC**"尚待第 2 项（设备端 head 角色 + 协议扩展）
落地。在此之前，本机在这条链路里的角色是"跑 head 段的普通 CPU 节点"（llama.cpp、无 torch、
无 GPU），**不等价于"集群必须有 PC"**。

## 4. 分阶段

| 阶段 | 内容 | 产出 |
| --- | --- | --- |
| **P4.5.0 安全契约与兼容边界** | 固化 cluster/term/voter/lease/certificate schema、错误码、只读降级和静态模式兼容；**不改变角色行为** | 契约测试先通过；无证书不能提交；旧模式不绕过 fencing |
| **P4.5.1 观测与评分** | 只采集并展示候选 score、版本、稳定性、voter 状态和“可否取得多数派”，**不改变主节点行为** | TUI/API/日志可解释；score 不再冒充选举裁决 |
| **P4.5.2 最小多数派租约** | 实现 voter ledger、term 分配、投票一次性约束、lease renew、certificate 校验；无多数派只读 | 双分区模拟中至多一侧取得证书，另一侧写入 fail-closed |
| **P4.5.3 自动接管** | `NODE_ROLE=auto` 仅在多数派证书路径启用；单/双节点继续人工 TOTP 或只读 | 主节点掉线后新主可写；无 witness 的两节点不自动接管 |
| **P4.5.4 计划内交接** | 复用现有 `ROLE_TRANSFER` 做控制面交接，但先提交新 term/certificate，再切换旧主写权 | 旧主被 fencing；交接事件可审计；不宣称在途任务已迁移 |
| **P4.5.5 恢复与任务语义** | 基于本地 journal 的可重放状态恢复；明确 in-flight 任务继续、取消重试或 `handoff_timeout`，不伪造快照迁移 | 崩溃/断线后任务无静默丢失；恢复状态可追溯 |

**顺序理由**：先修正安全不变量和兼容边界，再做观测/评分；之后才允许多数派租约和自动接管，最后处理计划交接与任务恢复。任何“先自选、后仲裁”的实现顺序都不再接受。

## 5. 验收判据（fail-closed）

1. **唯一性**：任意时刻，只有持有当前 term 有效 quorum certificate 的一侧可以提交状态；无多数派时全部写请求拒绝或进入只读，
   并且**报错要进错误码白名单**（不得泄露内部异常）。
2. **迁移可观测**：每次主节点变化都有 `(old_term, new_term, reason)` 记录；无变化时不得产生噪声。
3. **任务不丢**：交接期间在途任务必须落到"完成 / 显式转移 / `handoff_timeout` 上报"三者之一，
   不得静默消失。
4. **数值透明（G4）**：迁移前后跑同一 prompt，per-token argmax 一致（沿用 P3/P4 的判据与记录 schema）。
5. **候选排序可解释**：在同一 `master_score_version` 和相同资格集合内，主节点应按 score/稳定性/节点 ID 的确定性规则产生；score 低不构成安全失败，失去证书才构成安全失败。

## 6. 风险与对策

| 风险 | 对策 |
| --- | --- |
| 移动端（Android）休眠被误判为掉线 → 频繁换主 | **不靠平台标签，靠机制**：任期最短存活时间 + 续租 TTL + 交接确认（§3.2/§3.3）—— 对任何会休眠的设备都成立；"Android 降权/排除"是**待定假设**，且必须可整包裁撤（§3.5） |
| 低算力节点因心跳快而抢到主节点 | 主节点资格设**下限**（如 score ≥ 阈值），不足则从下一名顺延 |
| 交接期状态不一致导致幽灵预约占住算力 | **旧 epoch 的预约默认失效**（§3.3），宁可让用户重买一次算力 |
| 跨机推理正在进行时换主 | 只影响控制面：在途的层段接力按已签发的层段合同继续跑完；合同变更走下一轮 |
| 评分算法版本不一致导致排序无意义 | 记录 `master_score_version`；版本不一致时**按 node_id 字典序**保守裁决 |

## 7. 边界（不做的事）

- **不做**推理层的重排或重分片（那是 P2/pipeline_reshard 的职责）——P4.5 只换"谁在管"；
- **不做**完整 Raft/Paxos 数据复制栈；但**必须做最小多数派租约/投票证书**。仅靠租约 + epoch 无法满足“不脑裂”，
  无多数派时宁可只读，不以可用性换写安全；
- **不做**自动跨节点权重分发（与既有裁决一致：不内网传权重）。

## 8. 与文档/计划的关系

- 本文是 **P4.5 票面**；进度与结论在实现后回填到
  `docs/跨框架接力-当前有效基线与后续优化计划-2026-09-21.md`（新增 §P4.5 节）；
- 与 P5（长时、跨机与故障证据）的关系：P5 的"故障证据"**应当覆盖** P4.5 的换主过程，
  两者共用同一批设备（本机 / Surface / y700）。

## 9. 设计审计（2026-09-22）

### 9.1 结论

原设计**不能直接进入自动选举开发**。能力评分、租约和 epoch fencing 的方向可保留，但“广播 + 先到先得 + 本地 `max(seen_epoch)+1`”无法在网络分区时保证 G3。若按原顺序先做 `P4.5.1 自选`，会得到两个都能写入控制面的候选，而不是一个安全的主节点。

### 9.2 问题清单

| 严重度 | 问题 | 证据/影响 | 修订 |
| --- | --- | --- | --- |
| **P0** | 没有外部裁决者却声称“至多一个可写主” | 两侧都能观察到 lease 过期并自行续租；TCP 广播不是共识，`lease_id` 也不能让另一侧立即知道撤销 | 引入固定 voter set + 严格多数 `QuorumCertificate`；无多数派只读 |
| **P0** | `max(seen_epoch)+1` 不是全局 term 分配 | 并发竞选可生成相同 epoch；分区后新 epoch 可能只在一侧存在，接收方的“已见最大值”不是共享裁决 | term/投票记录持久化；每个 voter 每个 term 只签一个候选 |
| **P0** | “epoch + manifest 原子提交”在两个本地 SQLite 间并不原子 | `TaskJournal` 是本地 SQLite，且有实例锁；`TaskGraphCoordinator.snapshot()` 是观测投影，不是可恢复的运行时迁移协议 | 先做控制面事件/证书提交；任务迁移改为 journal 重放与显式重试，未实现前不宣称 handoff 完成 |
| **P1** | 审计前的决策文档与本立项冲突 | `docs/QLH异构分布式形态决策-2026-09-14.md` 原先写“人工 TOTP 快转让、不做 quorum”；本立项却要求自动接管 | 已同步修订为：单/双节点无 witness 只能人工转让或只读；自动接管必须走多数派票 |
| **P1** | `NODE_ROLE=auto` 尚未存在 | `src/config.py:_normalize_node_role()` 当前只归一为 `master/client`；`scheduler.init_nodes()` 仍按静态角色创建本地主节点或客户端 | 先实现兼容状态机，再启用 auto；显式 master 不能绕过 fencing |
| **P1** | Android presence 租约与控制面主租约边界混淆 | `presence_generation/presence_lease_id` 服务 Android presence 注册/心跳，不包含 voter、term 或控制面写权限 | 新增独立 control-lease schema，只复用 TTL/过期处理经验 |
| **P1** | score 不是选举裁决，且当前画像没有稳定的管理 score 合同 | 现有 `score_total`/scheduler weight 主要服务推理资源分配；CPU 基准、版本、签名、滞后和最低资格尚未固定 | score 只做候选排序；先完成版本化观测和资格快照，再参与选举 |
| **P2** | G4 数值透明判据过宽 | 主节点迁移可能改变 KV、采样状态或模型工件；只比较 prompt 的 argmax 不能覆盖控制面之外的状态变化 | 固定模型摘要、引擎、采样矩阵和输入；控制面先证明不改请求/工件，数值复验归入既有 P3/P4/P5 门 |

### 9.3 不变量修订

1. **安全优先于可用**：没有 quorum certificate 就没有自动可写主；网络分区时允许停写，不允许双写。
2. **score 不授予权力**：score 只能在同一资格集合内排序候选，不能覆盖 voter 身份、term 或证书。
3. **发现不等于裁决**：tailnet/heartbeat 只提供候选观测；所有主权、续租和状态写入必须走认证控制面。
4. **静态模式仍需 fencing**：`master` 只表示引导/单机/人工维护意愿，不得绕过 term、lease 和写入校验。
5. **任务迁移后置**：在途任务没有可恢复协议前，只能继续等待、按租约取消重试，或显式报告 `handoff_timeout`。

## 10. 修订后的开发票排期

票面按“先安全契约、再可写能力、最后迁移体验”排列。未通过前置票时，不得提前开启后续自动行为。

| 顺序 | 票号 | 优先级 | 内容 | 依赖 | 主要验收 |
| ---: | --- | --- | --- | --- | --- |
| 1 | `HA-CONTRACT-01` | **P0** | 固化 `cluster_id`、`voter_set_epoch`、`term`、`leader_id`、`lease_id`、证书、状态机、错误码与只读降级；定义旧 `master/client` 兼容边界 | 无 | schema 可版本化；旧模式可启动；无证书的控制写入统一拒绝；契约测试覆盖旧/新字段 |
| 2 | `HA-STATE-CATALOG-01` | **P0** | 盘点本地 SQLite/内存状态：必须复制、可重建、仅审计、禁止迁移；明确 task journal 与节点注册表的权威边界 | `HA-CONTRACT-01` | ✅ `src/cluster_state_catalog.py` + schema 形成白名单 manifest；每项有恢复策略；不把本地 snapshot 误称为跨节点复制 |
| 3 | `HA-SCORE-OBS-01` | P1 | ✅ `src/cluster_score.py` + `/api/cluster/management-score`：版本化管理能力 score、最低资格、滞后/稳定性、HMAC 完整性快照和只读 TUI 观测 | `HA-CONTRACT-01` | score 变更可解释、可回放；同分按 node ID 稳定排序；不改变当前主节点行为 |
| 4 | `HA-QUORUM-01` | **P0** | ✅ `src/cluster_quorum.py`：voter ledger、持久化 term、每 voter 每 term 单票、严格多数 certificate、lease renew；支持无 witness 的单/双节点只读策略 | `HA-CONTRACT-01`, `HA-STATE-CATALOG-01` | 分区模拟中最多一侧取得有效证书；term 不回退、不重复签票；无多数派不可写 |
| 5 | `HA-FENCE-01` | **P0** | ✅ 将证书/term 校验接入 API 写端点、scheduler 控制写入、TCP 控制消息和模型/层段租约；统一错误码与审计事件 | `HA-QUORUM-01` | 旧主、旧 lease、伪造证书、低 term、过期证书全部 fail-closed；读接口仍可用 |
| 6 | `HA-PARTITION-01` | **P0** | Barrier/可控网络分区测试工具，覆盖双主竞选、续租竞态、重复 term、恢复合并、旧包迟到 | `HA-QUORUM-01`, `HA-FENCE-01` | 并行/串行重复通过；任何测试均不能出现两张可写证书；失败保留证据 |
| 7 | `HA-ROLE-AUTO-01` | P1 | 增加 `auto` 状态机和启动/掉线/恢复路径；静态 `master/client` 兼容；auto 只消费有效 certificate | `HA-FENCE-01`, `HA-PARTITION-01` | 有多数派才自动接管；无多数派进入只读；恢复旧主不得抢写权 |
| 8 | `HA-HANDOFF-01` | P1 | 计划内 `ROLE_TRANSFER` 改为 term/certificate 先行的控制面交接；记录 old/new term、原因、操作者和结果 | `HA-ROLE-AUTO-01`, `HA-STATE-CATALOG-01` | 旧主先失写权再完成新主切换；交接可重放、可审计；失败不释放未确认资源 |
| 9 | `HA-RECOVERY-01` | P1 | 基于 journal 的崩溃恢复和任务语义：继续、取消重试、显式 `handoff_timeout`；不实现虚假的在途 snapshot 迁移 | `HA-HANDOFF-01` | 进程崩溃/网络断开后无静默丢任务；重复结果不重复提交；恢复状态与证据一致 |
| 10 | `HA-TUI-01` | P2 | ✅ 节点页展示当前主、term、证书/多数派状态、score 版本、最近交接原因与只读原因 | `HA-SCORE-OBS-01`, `HA-FENCE-01` | 后端不可用、无多数派、旧主和正常状态均有明确显示；不提供绕过安全校验的按钮 |
| 11 | `HA-CROSSHOST-01` | P1 | 本机双进程、PC/Surface/y700 跨机的故障、重连、长时和弱网实测 | `HA-PARTITION-01`, `HA-ROLE-AUTO-01`, `HA-RECOVERY-01` | 记录 RTO/RPO、term 变化、写拒绝、任务结果和资源状态；未通过不宣称生产可用 |
| 12 | `HA-ANDROID-01` | P2 | **设备构成调查 + 平台中性资格**（§3.5）：调查现场 Linux 板 vs Android 的比例与常驻条件；voter 资格只看能力合同与显式配置，**平台不参与判据** | `HA-CROSSHOST-01` | 调查结论成为决策输入而非结论；关闭平台策略后退化为纯能力排序；休眠不造成双主 |

### 10.1 开发顺序与并行关系

主线严格为：

`HA-CONTRACT-01 → HA-STATE-CATALOG-01 → HA-QUORUM-01 → HA-FENCE-01 → HA-PARTITION-01 → HA-ROLE-AUTO-01 → HA-HANDOFF-01 → HA-RECOVERY-01 → HA-CROSSHOST-01`

`HA-SCORE-OBS-01` 已在契约票完成后补齐并与状态盘点并行；`HA-TUI-01` 已在后端字段和安全状态稳定后完成；
`HA-ANDROID-01` 的内容已按 §3.5 调整：**从"把 Android 排除出 voter"改为"平台中性 + 设备构成
调查"**（voter 资格只看能力合同与显式配置）。它仍不得早于跨机故障证据，但**不要求证据里含 PC**
——纯边缘集群（无 PC、含 Android/Linux 板）同样必须能验证。`HA-CONTRACT-01`、
`HA-STATE-CATALOG-01`、`HA-QUORUM-01`、`HA-FENCE-01`、`HA-PARTITION-01`、
`HA-ROLE-AUTO-01`、`HA-HANDOFF-01`、`HA-RECOVERY-01`、`HA-SCORE-OBS-01` 和
`HA-TUI-01` 已完成相应代码/测试收口；下一票为 **`HA-CROSSHOST-01` 剩余物理验收**，
此前不开放 `NODE_ROLE=auto` 和自动故障接管。

### 10.2 票面完成定义

- 每票必须有代码、测试、错误码/日志证据和简短文档回填；不能只改 TUI 文案宣称完成。
- 任何“分区后两侧都能继续写”的结果都是 P0 阻断，不得用重试或最终一致性解释。
- 只有 `HA-PARTITION-01` 通过后，P4.5 才能从“设计审计通过”进入“自动角色开发中”；只有
  `HA-CROSSHOST-01` 通过后，才可讨论默认开启。

## 11. 审计后的当前状态

- 本文的原始自动选举设计已降级为**不可直接实现的草案**；第 3、4、5、7 节已按上述安全基线修订。
- `HA-CONTRACT-01` 已落地：`src/cluster_control_contract.py` 冻结 `cluster_id`、`voter_set_epoch`、`term`、
  `leader_id`、`lease_id`、Ed25519 quorum certificate、稳定错误码、只读快照和控制写入 gate；
  `schemas/cluster-control-v1.schema.json` 与契约测试同步覆盖旧角色可启动、缺证书拒写、签名/多数派/过期/冲突校验。
- `HA-STATE-CATALOG-01` 已落地：`src/cluster_state_catalog.py` 与
  `schemas/cluster-state-catalog-v1.schema.json` 固化 19 项状态白名单，覆盖必须复制、可重建、仅审计、禁止迁移四类，
  每项声明 authority、当前边界、恢复策略和交接策略；`scripts/cluster_state_catalog.py` 只输出元数据，
  不读取用户数据库、密钥、会话、模型权重或运行时句柄。Task journal 的 event 是可恢复权威，snapshot 只是可重建投影；
  live lease、KV、私钥、session 与用户对话明确禁止跨节点搬运。
- `HA-QUORUM-01` 已落地：`src/cluster_quorum.py` 为每个 voter 使用独立 SQLite ledger，
  原子持久化 promised/active term、leader 绑定、每 term 单票和 lease；`QuorumCollector` 只在已配置 voter 集合
  的严格多数签名齐备时返回证书，单/双节点无第三 witness 时返回 `quorum_requires_witness` 只读结果。
  续租保持 term/lease_id 不变，仅延长到新的多数签名证书；运行时接入由 `HA-FENCE-01` 等后续票完成。
- `HA-SCORE-OBS-01` 与 `HA-TUI-01` 已分别提供版本化、可解释、可回放的只读 score 快照和节点页投影；score 不参与
  quorum、证书签发或推理数值路径。
- 当前仍不默认启用 `NODE_ROLE=auto`，也不宣称生产 HA 已可用；物理跨机剩余验收和 Android APK/JNI 路径仍是门槛。
- 之前的人工 TOTP 转让、静态主节点和现有 `ROLE_TRANSFER` 仍是兼容/过渡能力；它们必须逐步接入统一
  term/fencing 合同，不能成为自动选举的安全依据。
## 12. HA-FENCE-01 implementation update (2026-09-22)

Status: complete for the runtime fencing scope. The quorum protocol is now connected to:

- `src/cluster_fence.py`: explicit opt-in gate, certificate/header parsing, stable rejection codes, permit context, and audit events.
- API control writes: queue/config/layer/model-runtime/role-transfer/spare-master/node deregistration/identity reset. `GET /api/cluster/control-plane` remains read-only; `POST /api/cluster/control-plane/certificate` installs an already quorum-signed certificate.
- `scheduler.py`: control-write admission plus topology reshard lease binding.
- `tcp_comm.py`: fenced control messages are validated in both directions; inference/data-plane frames and health/read paths remain compatible.
- `llama_rpc_contract.py`: shard lease assign/renew/commit can bind to the current certificate term and digest.

Compatibility rule: fencing is enabled only when `QLH_CONTROL_FENCE_REQUIRED=true` or an explicit `QLH_CONTROL_VOTER_SET` is configured. Once enabled, missing, stale, expired, forged, non-current, or low-term certificates fail closed. This does not enable `NODE_ROLE=auto` or perform automatic election.

Evidence: `.venv-test\\Scripts\\python.exe -m pytest -q` on the affected suite: `429 passed`; fencing/quorum/transport/reshard regression subset: `129 passed`; `compileall` and `git diff --check` passed.

Next ticket: `HA-ROLE-AUTO-01`; physical cross-host network validation remains reserved for `HA-CROSSHOST-01`.

## 13. HA-PARTITION-01 implementation update (2026-09-22)

Status: complete for the deterministic partition safety gate.

- `src/cluster_partition.py` drives the real SQLite voter ledgers and `QuorumCollector` behind a controlled majority/minority partition. It does not change `NODE_ROLE` and does not perform runtime automatic election.
- The harness covers serial and barrier-synchronized double-primary races, renewal races, duplicate-term attempts, partition healing with term advancement, stale certificate rejection, and delayed old transport frames after reconnect.
- Failed scenarios can be written as atomic, replayable JSON evidence with seed, side membership, barrier events, outcomes, and stable rejection codes. The evidence excludes private keys and model/user payloads.
- `tests/test_ha_partition.py`: `6 passed`; this is a deterministic protocol/test-kit gate, not a claim of physical cross-host or production network availability.

The next mainline ticket is `HA-ROLE-AUTO-01`. `NODE_ROLE=auto` remains disabled until this state-machine work and its recovery gates are implemented.

## 14. HA-ROLE-AUTO-01 implementation update (2026-09-22)

Status: complete for the explicit controller and scheduler adapter scope.

- `src/cluster_auto_role.py` adds a fail-closed `AutoRoleController` with `starting`, `leader`, `follower`, `read_only`, and `stopped` states. It can only consume a certificate issued by the existing quorum collector; it cannot self-sign or manufacture write authority.
- No majority, missing collector, invalid certificate, expired certificate, stale term, or control disconnect enters `read_only`. A node that consumes a valid certificate for another leader remains `follower` and cannot write.
- After a disconnect, replaying the previous unexpired certificate is rejected. Reconnect requires a fresh quorum acquisition, which advances the term before the former leader may regain write authority.
- `Scheduler.set_auto_role_controller()` and the related read-only status/transition methods expose the adapter to runtime composition and TUI/API status. The existing static `master`/`client` startup branches remain unchanged.
- `NODE_ROLE=auto` is still not enabled by default. This ticket provides an explicit opt-in runtime adapter; default activation remains blocked until handoff, recovery, and physical cross-host gates are complete.
- `tests/test_ha_role_auto.py`: `7 passed`; HA contract/quorum/fence/partition regression subset: `30 passed`; Scheduler role/control subset: `10 passed`; compileall and `git diff --check` passed.

Next ticket: `HA-HANDOFF-01`. `HA-CROSSHOST-01` remains the gate for physical cross-host activation.

## 15. HA-HANDOFF-01 implementation update (2026-09-22)

Status: complete for the explicit certificate-first handoff contract.

- `src/cluster_handoff.py` adds `HandoffCoordinator` with `prepared`, `awaiting_quorum`, `committed`, `aborted`, and `failed` states. A handoff records old/new leader, old/new term, certificate digests, manifest digest, reason, operator, result, and bounded audit events.
- Preparation requires the current certificate to name the old leader and pass the existing control-write gate. The manifest is canonicalized and hashed; task snapshots, journal contents, model paths/weights, sessions, live worker leases, and runtime handles are not copied into the contract.
- Commit fences the old role controller before requesting the new leader's certificate. No majority leaves the handoff in `awaiting_quorum`; a new certificate is installed only after strict quorum issuance and target-role acceptance. The old unexpired certificate cannot be replayed as the new handoff certificate.
- Handoff preparation is idempotent for the same target/manifest/reason/operator tuple. A changed current certificate, conflicting active handoff, invalid target/manifest, target rejection, or stale terminal operation fails closed. A fenced but quorum-unavailable handoff cannot be locally aborted; recovery remains `HA-RECOVERY-01` work.
- `Scheduler.set_handoff_coordinator()` and `prepare_leader_handoff`/`commit_leader_handoff`/`abort_leader_handoff` expose this path only when explicitly attached. The legacy `ROLE_TRANSFER` API remains compatibility code and is not counted as certificate-first HA handoff.
- `tests/test_ha_handoff.py`: `7 passed`; compileall and `git diff --check` passed. Physical TCP/cross-host execution remains reserved for `HA-CROSSHOST-01`.

Next ticket: `HA-RECOVERY-01`.

## 16. HA-RECOVERY-01 implementation update (2026-09-22)

Status: complete for local journal recovery and explicit fenced-handoff recovery.

- `src/cluster_recovery.py` defines a metadata-only, fail-closed recovery policy. A journal projection is classified as `continue`, `retry`, `cancel`, `handoff_timeout`, or `fail`; terminal projections are ignored. The policy never copies prompts, model outputs, paths, live leases, KV state, or runtime handles.
- `TaskGraphCoordinator.recover_persisted_workflows()` now consumes the policy and records one deterministic recovery event per workflow. A pure and explicitly `retry_safe` stage becomes `ready` with `recovery_action=retry`, exposed as `recovery_retry_pending`; no Provider submission occurs during restart recovery. A caller must re-attach execution context before a retry, so a restart cannot double-submit a result.
- A persisted cancel request becomes a terminal `cancelled` workflow. `result_ready` and non-retry-safe in-flight work remain fail-closed with their existing restart reason codes. An explicit handoff marker becomes terminal `handoff_timeout`, rather than pretending an in-flight snapshot was migrated.
- `HandoffCoordinator.recover_pending()` and `Scheduler.recover_leader_handoff()` bound an orphaned fenced handoff. Before timeout it remains `awaiting_quorum`; after timeout it emits `handoff_timeout` and remains unable to restore the old write permit. A fresh certificate-first handoff is required.
- Evidence is append-only, idempotent, and metadata-only. `tests/test_ha_recovery.py`: `9 passed`; handoff/journal recovery regression: `33 passed`; compile and diff checks pass. Physical cross-host restart, durable cross-node journal replication, and automatic role activation remain reserved for `HA-CROSSHOST-01`.

Next ticket: `HA-CROSSHOST-01`.

## 17. HA-CROSSHOST-01 implementation update (2026-09-23)

Status: local dual-process development gate complete; physical cross-host validation remains open.

- `src/cluster_crosshost.py` adds a Windows-compatible `spawn` process-boundary harness. Two independent worker processes validate the production `TransportEnvelope` contract, one receiver is stopped and restarted, an old-generation frame is rejected with stable code `generation_stale`, and a new-generation control frame is accepted.
- `scripts/ha_crosshost_smoke.py` runs the gate without model weights. `--evidence <path>` writes an atomic metadata-only report; payload bodies, host addresses, certificates, and model/runtime handles are not recorded.
- `tests/test_ha_crosshost.py` covers restart/reconnect, RTO/RPO fields, term/write/task metadata attachment, and atomic evidence output. The harness reports `physical_nodes=false` and `transport=multiprocessing_pipe`; it is not a substitute for PC/Surface/y700 TCP/TLS/weak-network execution.
- The existing quorum, fence, handoff, and recovery tests remain the source of truth for term changes, stale control writes, and retry-safe task recovery. Cross-process evidence can attach their metadata through `CrossHostEvidence.with_control_metadata()` without duplicating election logic.

Physical acceptance is still blocked until the same evidence schema is collected over the configured PC/Surface/y700 route, including reconnect, weak-network, long-running, and durable-journal checks. Until then P4.5 must not claim production HA availability or enable automatic role takeover by default.

Next ticket: `HA-ANDROID-01` (after physical cross-host evidence is collected or explicitly waived).

## 18. HA-CROSSHOST-01 / HA-ANDROID-01 physical follow-up (2026-09-23)

The bounded physical control-plane smoke has now run over the configured Surface
route. `scripts/ha_crosshost_physical_smoke.py` used SSH reverse TCP to the
Surface worker and recorded the following metadata-only evidence:

- Surface accepted generation 1, the worker was stopped to simulate a crash,
  the replacement rejected the old frame with `generation_stale`, and accepted
  a generation 2 frame after reconnect. The latest measured RTO was 695 ms
  (the previous run measured 580 ms; this is a smoke observation, not a SLO).
- The y700 probe reports `TB321FU`, Android SDK 35, `arm64-v8a`, 8 logical
  CPUs, and an existing model root at
  `/data/data/com.termux/files/home/storage/shared/Download/QLH/models` with
  six active GGUF files. The `.fmtrashed-*` file is not counted.
- Evidence file: `local_docs/evidence/ha-crosshost/HA-CROSSHOST-01-physical-2026-09-23.json`.
  `production_availability_claim` remains false. This run covers SSH/reverse
  TCP reconnect and generation fencing only; weak-network, long-running,
  quorum certificate exchange, and durable cross-node journal replication are
  still open.

The y700 ARM64 layer probe was also run against
`qwen25-05b-f16.gguf` and `qwen25-05b-f16-cut-k12.gguf`. Five consecutive
generation steps returned `relay(cut+embd) == baseline(full)` and exited with
code 0. This validates the Termux/llama.cpp layer-forward path on the physical
device. It does not validate the APK/JNI `layer_forward` path: the existing APK
install-and-launch result only proves ABI/control-plane startup, so that gap
remains explicitly tracked.

`HA-ANDROID-01` now has a platform-neutral qualification contract in
`src/cluster_device_qualification.py`. It produces a deterministic composition
snapshot and requires explicit `device_info.cluster_voter`, a declared
`cluster_control`/`control_plane`/`ha_voter` capability, fresh heartbeat, and an
online/busy state. Platform is reported but never grants or denies voter
eligibility; `forward_layers` is reported independently. The module does not
modify roles, voter ledgers, or automatic election. Regression coverage is in
`tests/test_cluster_device_qualification.py` (5 tests), alongside the physical
cross-host smoke tests (8 tests total in the targeted run).

This closes the scoped platform-neutral/device-composition work for
`HA-ANDROID-01`. The next P4.5 development ticket is `HA-TUI-01`; automatic
role activation and production HA availability remain disabled until the open
physical and APK/JNI evidence gaps are closed.

## 19. HA-TUI-01 implementation update (2026-09-23)

Status: complete for read-only HA observability on the nodes page.

- `src/tui_shared.py` now registers `/cluster/control-plane` alongside the
  existing transfer-log endpoint.
- The nodes page renders a read-only HA control block using the control-plane,
  role, transfer-log, and node snapshot payloads. It shows the current leader,
  term/voter epoch, truncated certificate digest, majority state, score version,
  latest handoff reason, and read-only reason.
- Disabled fencing, unavailable control-plane state, missing quorum, stale
  backend fields, and failed requests are shown as disabled, unavailable, or
  unknown. No role takeover, certificate installation, or validation bypass
  control was added to the TUI.
- `tests/test_tui_textual.py` covers missing-quorum fail-closed rendering and
  score-version projection; `tests/test_tui_shared.py` covers the new paths.

The TUI observability ticket is closed. Physical weak-network/long-running/
durable-journal evidence and Android APK/JNI `layer_forward` evidence remain
open; automatic role activation and production HA availability remain disabled.

## 20. HA-SCORE-OBS-01 correction (2026-09-23)

Status: complete as a read-only management-capability observation artifact.

- `src/cluster_score.py` implements the versioned `management-v1` algorithm.
  It scores management capacity, memory headroom, load headroom, heartbeat
  stability, and network quality. Each node includes the component breakdown,
  minimum qualification, voter qualification, heartbeat age, and reasons for
  missing or stale inputs.
- `GET /api/cluster/management-score` returns a replayable snapshot with
  `schema_version`, `algorithm_version`, UTC observation time, stable
  `node_id` tie-breaking, and a canonical `snapshot_digest`. When the cluster
  secret is available it also returns an HMAC-SHA256 integrity signature; this
  signature is not a quorum certificate and cannot grant write authority.
- The TUI consumes this endpoint and displays its algorithm version. Missing
  score data is shown as `not reported`; it is never synthesized from an
  inference-placement weight or an unversioned node field.
- `tests/test_cluster_score.py`, `tests/test_tui_textual.py`, and
  `tests/test_tui_shared.py` cover replay, explanation, stable ties, fail-closed
  qualification, signature state, and endpoint registration.

The missing dependency behind `HA-TUI-01` is now an actual code/API/test
artifact. The next ticket is `HA-CROSSHOST-01` remaining physical acceptance:
weak-network, long-running, durable-journal, and Android APK/JNI evidence.
