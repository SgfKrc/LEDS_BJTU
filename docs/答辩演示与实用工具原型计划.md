# 答辩演示与实用工具原型计划

> 状态：`进行中`（2026-08-14 立项；GROK 组 2026-08-16 退出，承接移交主开发组；**P5-P8 已完成（2026-08-16）**；`DEF-P1` 已于 2026-09-09 开始实施，P2-P4 排队；**双机真机验收已于 2026-08-20/21 完成（QW1.8B 分层数据面 + TaskGraph 恢复 + B3 IPv6）**，剩余长时/断电恢复、SD 跨机与生产路由类验收后置）
>
> 更新日期：2026-09-09
>
> 适用范围：大创收尾阶段（答辩目标 **2027-05**，项目截止 **2027-04**）的实用小工具/演示原型；与总体计划的剩余真机验收（A4 长时/断电恢复、SD-N4 跨机、生产路由）并行，不引入架构级新工作。所有原型须可落盘、可验证，声称一律附 git 证据。
>
> 总计划入口：[总体下一步计划](总体下一步计划.md)
>
> 当前开发票入口：[审计收口与答辩演示开发票计划](开发票计划-审计收口与答辩演示-2026-09-09.md)

## 1. 背景与策略

项目（QLH 分布式推理集群）体量已远超一般大创，核心风险**不是功能数量**，而是：

1. **真机闭环**——**短程双机已验**（2026-08-20 分层数据面 / 2026-08-21 任务链恢复 + IPv6），答辩核心卖点证据在库；剩余长时多轮（T-RACE-6-C）、断电恢复、SD 跨机与生产路由验收继续推进；
2. **演示稳定性**——现场演示不翻车；
3. **性能数据**——单机 vs 双机对比，用数字说话；
4. **结题材料**——文档收口 + 答辩 PPT + 正式 release。

本计划只承接"演示 + 数据 + 易用性"侧的实用原型，**明确不做**：QW3.5 运行时接入（QW35-R1）、Gemma 4 真实三轮标定、音频/8K 上下文等架构级新工作（登记为"未来工作"，答辩中体现规划能力）。

## 2. 原型清单

| # | 原型 | 目标 | 验收标准 | 优先级 |
|---|------|------|---------|--------|
| P1 | **一键演示模式** | `demo.sh`/`demo.bat` 启动全栈（后端+前端+worker）并跑通预置脚本（对话/图像各一），演示不翻车 | 干净环境一键启动 ≤2 分钟；预置脚本输出固定成功标记；失败有中文提示 | **P0** |
| P2 | **失败注入演示** | 断网/杀 worker → 自动重派恢复的一键演示 | 演示脚本：kill worker → 观察重派日志 → 任务完成；重复 3 次稳定 | **P0** |
| P3 | **分布式 benchmark 工具** | 单机 vs 双机延迟/吞吐/显存对比，输出图表 | 同负载下输出 JSON+PNG 图表；数据可复现（seed 固定） | **P0** |
| P4 | **集群拓扑可视化** | Web 前端绘制节点/层分配图（基于 readiness 快照） | 真实集群渲染节点+层段分配；无绝对路径/密钥泄漏 | P1 |
| P5 | **判题口径改进** | ✅ 原型完成（2026-08-16，主开发组）：`loose_contains` 宽松匹配 + 512 token 预算，v2 rubric 三轮重标 | **Qwen3-4B correctness 1/4×3 vs Qwen 1.8B 0/4×3——可区分达成**（v1 同分 0/0.364）；v1 语义不回退、EX-N3 回归通过；format 判据保持 v1/192 口径（v2 长输出破坏格式检查） | P1 |
| P6 | 模型加载预热/缓存 | 常用模型常驻/加载队列优化 | 二次加载耗时下降可量化 | P2 |
| P7 | 跨节点日志聚合 | 主节点聚合各 worker 日志的小工具 | `qlh log` 汇总各节点最近 N 行；无凭据泄漏 | P2 |
| P8 | 模型导入向导 | ✅ 完成（2026-08-16）：`model_tools import-model` 四步向导 + **真实网络下载验证通过**（7897 代理，HF `sshleifer/tiny-gpt2` 全流程 resolve→下载→校验→manifest→登记，artifact/manifest SHA 落库；登记后已清理）。**ModelScope 路径真实验证通过（2026-08-16）**：`Qwen/Qwen2.5-0.5B` 988MB 经 7897 代理全流程下载→校验→登记（修复 modelscope 子进程 GBK 编码崩溃）；两源（HF/ModelScope）均真实可用 | 引导式完成 resolve→下载→校验→登记；`--proxy`/`QLH_HTTP(S)_PROXY` 代理链路真实可用 | P2 |

## 3. 顺序与依赖

- **第一批（P0）**：P1 → P2 → P3。当前 `DEF-P1` 已开始，先完成本机 fixture/live 启动和脱敏报告，再推进失败注入与 benchmark。
- **第二批（P1，2026-02~03）**：P4 → P5。
- **第三批（P2，有余力）**：P6/P7/P8 任意。
- 依赖：P3 双机数据源**已就绪**（2026-08-20/21 双机实跑证据，见[项目进展与下一步计划](项目进展与下一步计划.md)），可做实测；P1/P2 不依赖外部设备，本机即可原型。

## 4. 协作约定（GROK 组原型）

1. 每个原型独立提交，声称完成必须附 `git status`/`git diff`/commit hash 证据；
2. 原型放 `scripts/` 或 `build/`（临时验证物）不入主流程，验收通过后由主开发组收编；
3. 涉及网络/密钥/路径的脚本须遵循项目脱敏纪律（不落凭据、不落绝对路径到报告）；
4. 原型验收由本机实测（跑通验收标准）而非文本声称。

## 5. 时间线（答辩 2027-05，项目截止 2027-04）

当前 2026-08，距离截止约 8 个月，节奏从容：

```
2026-08~2026-12  ✅ 双机真机验收已完成（分阶段落袋）：2026-08-20 QW1.8B 分层数据面
                 （RTX 4060 Layer 0-21 + Surface Layer 21-24，3 次 distributed_required 全过）；
                 2026-08-21 TaskGraph 重启/杀进程恢复 + B3 Tailnet IPv6 实跑 → 剩余
                 长时多轮（T-RACE-6-C）、断电恢复、SD 跨机与生产路由验收
                 + 开发收尾（QW3.4 等）+ P0 原型（P1 演示 / P2 失败注入 / P3 benchmark）
2027-01~2027-02  P1 原型（P4 拓扑可视化 / P5 判题口径）+ 性能数据采集定稿
2027-02~2027-03  结题材料：文档收口 + 答辩 PPT + 演示彩排 + 正式 release
2027-03~2027-04  缓冲与整体打磨（预留 ≥1 个月）
2027-04          项目截止
2027-05          答辩
```

> 提示：双机核心证据已实跑（分层 + 故障注入恢复 + IPv6），答辩展示页应把 2026-08-20/21 的证据链（日志/任务 ID/截图）做成核心页；当前重心转向**长时/断电恢复与生产路由验收**与演示稳定性，不要再等"第二台 CUDA PC"作为前置条件。

## 6. 不做清单（防扩张）

- ❌ QW3.5 运行时接入（QW35-R1）、Gemma 4 真实三轮标定、音频/8K/16K 上下文、新多模态——全部登记"未来工作"。
- ❌ 任何需要第二台 CUDA PC / Linux 真机 / 大显存之外的架构改动。
