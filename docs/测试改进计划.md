# 测试改进计划

> 状态：持续维护（2026-08-11 建立；2026-08-12 完成 P0/P1、P2-A、P2-B 整改）
>
> 更新日期：2026-08-12
> 适用范围：QLH 测试套件的薄弱点、遗留问题与质量复盘清单，供测试维护者按优先级处理；不改变各专项的验收口径
>
> 上游事实入口：[测试与评判标准](测试与评判标准.md)；总计划：[总体下一步计划](总体下一步计划.md)

## 1. 当前基线（2026-08-12，P2-B 整改后）

- Python 全量：**1987 passed / 8 skipped / 0 failed**（Windows/Python 3.12；含真实 TCP、uvicorn 与 SD 跨进程 HTTP 回归）
- control-svc `317/317`、gateway `113/113`、前端单元 `38/38`、浏览器 mock `5/5`、Android JVM 双 flavor 各 `16/16`
- 8 个 skipped 全部有明确原因（symlink 平台限制 4、SMTP/真实模型环境门 2、仿真入口 1、transformers 兼容 1），见[测试与评判标准 §4.2](测试与评判标准.md)
- 代码量：功能 ~95,000 行 / 测试 ~63,000 行，**测试:功能 ≈ 0.66**——**但代码量覆盖 ≠ 质量覆盖**，§6 质量复盘是本文档的核心新增

## 2. 已完成处置（首轮整改，2026-08-11）

| 项 | 结果 |
|---|---|
| 13 个 requires_db 用例（原 §2 决策） | ✅ 改写为 local_store SQLite 测试（TestLocalSettings 15 用例），跳过 26→13 |
| Android 零测试 | ✅ JVM 16 用例（NetworkAddress + MainViewModelLogic 纯函数），双 flavor 通过 |
| 前端占比 0.10 | ✅ apiClientErrors 8 用例（错误路径/fail-closed），单测 30→38 |
| diffusers 侧车跳过 5 个 | ✅ fake 命名空间 fixture，默认环境与 CUDA venv 均 30/30 |
| WSL bash 2 fail | ✅ MSYS/WSL 双语义路径映射，全量 0 failed |
| 环境门与跳过项 | ✅ 测试与评判标准 §4.2 文档化（8 项逐条） |
| db.py 测试引用 | ✅ 清理 −480 行；随后 PG 生产代码（src/db.py、config-dao、postgres-projector、setup_test_db、verify_fixes）整体移除 |
| P0/P1 测试质量整改 | ✅ Q1-Q8 已完成；Q2 的 Review 端点授权契约已落地并覆盖回归 |

## 3. 剩余跳过与分布

见[测试与评判标准 §4.2](测试与评判标准.md)：8 项（symlink 4、SMTP/真实模型 2、仿真入口 1、transformers 1）。

## 4. 质量复盘（2026-08-11 深度审计，核心章节）

> 审计方法：4 个只读子代理并行（断言质量 / 覆盖率盲区 / 可靠性隔离 / 非 Python 层），覆盖全部 130+ Python 测试文件与 control/gateway/frontend/android 全部测试。下表按当前实现持续维护，已完成项保留处置事实，未完成项继续作为后续票据入口。

### 4.1 总体评估

**整体评级：优秀**（高于一般仓库水平）——~1900 用例绝大多数为行为断言 + 结构化错误码断言，大量使用真实组件（真 socket、真 SQLite、真 HTTP 桩、Monte-Carlo 统计等价）；未发现无断言测试、测死代码、系统性形式主义。问题集中在**可维护性风险**（文案/命名/私有成员断言）与**特定模块的覆盖空洞**（HA 状态机、安全拒绝分支）。

### 4.2 问题清单（按优先级）

| # | 优先级 | 问题 | 位置 | 说明与建议 |
|---|---|---|---|---|
| Q1 | **P0** | **[已完成] scheduler HA 主备切换/晋升状态机回归** | `tests/test_scheduler.py` | 5 项独立临时 SQLite 回归覆盖跨实例恢复、日志边界与拒绝、备用主节点清除、晋升/ACK、启动退出暂代通知；与既有身份测试合计 7 passed |
| Q2 | **P0** | **[已完成] api_server 安全鉴权失败分支** | `tests/test_api_security_boundaries.py` | 12 项覆盖 shutdown token 三态、日志下载远程 403、模型路径穿越/受信来源、reset/connect/transfer 拒绝映射、review create/vote 角色门，以及 §4.2.1 四个副作用端点的 client 403 和 master 正常路径 |
| Q3 | **P0** | **[已完成] cold-start 死补丁** | `tests/test_api_cold_start.py` | 删除无关 `model_downloader` 补丁，直接替换 `_get_all_model_configs()`，用例不再依赖开发机模型文件状态 |
| Q4 | **P1** | **[已完成] 端口 TOCTOU 竞态** | `tests/network_test_support.py`、5 个网络测试文件、`src/tcp_comm.py` | 不可达端口在用例期间保持 bind 且不 listen；TCP/uvicorn 服务直接绑定 `0`；修复 TCPServer 吞掉 `port=0` 以及双栈分别分配端口的问题；SD 子进程通过就绪文件回报内核分配的 HTTP 端口。真实网络组 271 passed |
| Q5 | **P1** | **[已完成] `load_model` mock 过度** | `tests/test_api_models.py` | 测试使用真实临时 GGUF 与注册表数据，真实执行校验、文件探测、路径解析和 effective engine，只隔离模型加载副作用 |
| Q6 | **P1** | **[已完成] llama.cpp 量化弱断言** | `tests/test_api_models.py` | 改为直接断言 llama.cpp 保留 `Q4_K_M`，PyTorch 对同值精确抛 400 |
| Q7 | **P1** | **[已完成] model_runtime_sidecar 零引用** | `tests/test_model_runtime_sidecar.py` | 新增 10 项请求/哈希/大小/路径测试；同时修复 safetensors 清单的 `..`、绝对路径和符号链接目录逃逸 |
| Q8 | **P1** | **[已完成] 邮件测试导入污染环境** | `tests/test_email_notifier.py` | 删除模块导入时对 QLH_SMTP_*/QLH_IMAP_* 的写入；真实 SMTP 测试仅由用户显式环境门开启 |
| Q9 | **P2** | **中文错误消息文本断言 ~15+ 处**（文案一改即挂，非 API 契约） | `test_api_models.py:601`、`test_scheduler.py:606`、`test_task_graph.py:353`、`test_task_graph_api.py:593/911`、`test_inference_service_protocol.py:938`、`test_updater.py:224`、`test_launcher_bootstrap.py:71`、`test_signing.py`（~10 处）、`test_gemma4_native_probe.py:144` | 迁移到结构化错误码（仓库已有 `reason_code`/`code` 惯例）；UI 文案断言（tui 层）可豁免 |
| Q10 | **P2** | **[已完成] diffusion/data_plane 上传方向安全矩阵** | `tests/test_diffusion_data_plane.py` | 新增上传 grant 缺失/attempt 错配/upload_id 错配、invalid offset、空块、内容类型、提交体的 401/400/413/415 回归，以及 `_http_error` 的 403/404/409/422 映射；并修复 `Content-Length: 0` 被误报 413 的实现问题 |
| Q11 | **P2** | **私有成员/内部状态直测 ~12 处**（改名即挂） | `test_scheduler.py:1188/1190/1203/517-521`、`test_launcher.py:139-140`、`test_api_models.py:1076`、`test_inference_service_protocol.py:429/710/988/1085`、`test_model_host_protocol.py:43-44` | 优先断言公开快照/返回值；部分状态机白盒（paged_kv_cache、tcp_comm）可辩护 |
| Q12 | **P2** | **[已完成] SQLite 初始化集合跨用例污染** | `tests/test_local_store.py`、`tests/test_scheduler.py` | 所有直接 `clear()` 改为 `monkeypatch.setattr(local_store, "_initialized_paths", set())`；fixture teardown 不再修改共享全局集合 |
| Q13 | **P2** | **[已完成] scheduler 主节点发现/自动连接/抢占决策回归** | `tests/test_scheduler.py`、`src/scheduler.py` | 覆盖本地配置优先、Tailnet 回退/异常 fail-closed、自动切换/加入/连接、备用地址重试及抢占真值表；启动期 2/5 秒等待改为 `Event` 可取消，`stop()` 后不再发起连接；同时修复 `Scheduler.start(port=0)` 回退默认端口 |
| Q14 | **P2** | **[已完成] engine_host 关键函数真实执行** | `tests/test_inference_service_protocol.py`、`src/inference_service/engine_host.py` | 真实执行模型配置驱动的 KV cache、临时 GGUF 自动选择、远端 worker 特性/健康/模型/负载门及 reservation 判断；修复自动加载“先初始化 KV、后 reset 清空”的顺序错误，并更正 provider ID 函数返回类型 |
| Q15 | **P2** | **[已完成] tcp_comm 安全函数函数级回归** | `tests/test_tcp_comm.py` | 新增 HMAC 缺字段/无密钥/过期/不匹配/成功、保留 master 身份和 master 角色注册拒绝 ACK、broadcast 单连接失败继续发送；共新增 6 项参数化/函数测试 |
| Q16 | **P2** | **[已完成] 固定 sleep 时间敏感点** | `tests/test_scheduler.py`、`tests/test_local_store.py`、`tests/helpers/diffusion_worker_process.py` | 队列/流水线等待改为 Event 或 Barrier；会话排序使用确定性时钟；SD 跨进程结果传输由“已消费”就绪文件放行，启动与注册阶段保留带 deadline 的真实就绪轮询 |
| Q17 | **P3** | 宽区间模糊断言（设备评分 20% 内回归测不出） | `test_scheduler.py:105-164` | 断言相对序 + 关键比例，区间仅 sanity |
| Q18 | **P3** | 极弱断言（`assert text` 非空即过） | `test_launcher.py:280` | 断言具体文本片段 |
| Q19 | **P3** | 冗余双补丁（`_store_dir` 与 `_get_store_dir` 同 patch） | `tests/test_local_store.py:35-36` | patch 一个即可 |
| Q20 | **P3** | control/gateway 个别 `.e2e-spec.ts` 实为纯函数单测（命名不副实） | control/test/network-address 等、gateway auth-policy/auth-proxy/forward-client | 改名 `.spec.ts`；auth-policy 可补一条带真实 control client 的链路测试 |
| Q21 | **P3** | androidTest 过浅（仅 SettingsScreen 主题 2 例，无状态选中态/聊天/会话/DataStore） | `android/app/src/androidTest/` | 真机窗口时补主交互路径；JVM 层优先补 ViewModel 状态机 |
| Q22 | **P3** | frontend `settings.test.mjs` 最薄（3 断言）；Playwright fixture 与后端契约无自动对齐 | `frontend/tests/` | 后续考虑 schema 生成 fixture |

### 4.3 已排除的怀疑项（说明审计深度）

- 无断言的测试文件：**未发现**（40+ 文件抽查）
- 测生产死代码：**未发现**（speculative/task_graph/external_provider 均有真实引用）
- 死补丁：全仓核对 ~70 个补丁目标，仅 Q3 一处
- 系统性形式主义（只断言 200/只断言 called/测试-实现同步复制）：**未发现**
- 凭据脱敏与数据作用域门：**总体良好**（external_provider/island_engine/task_journal/network_path/data_plane 均有测试），上传方向 Q10 已补齐
- `test_database_fallback.py` 退场契约、`simulation/` 进程隔离：设计正确

### 4.2.1 Review 端点授权契约（2026-08-12 决策，测试实现依据）

**背景**：项目当前**无请求级节点认证**（HTTP 层无节点 token/签名；节点身份靠 TCP 注册 + cluster secret；MF-AUTH 的 Bearer 是用户登录，不适用于节点）。既有 create/vote 校验（`scheduler._effective_role()`、`can_node_vote(node_id)`）是**本机角色**校验——约束"运行 API 的这台机器"的角色，即部署位置约束，而非调用者身份校验。以下契约沿用该模式。

| # | 端点 | 授权策略 | 非授权响应 |
|---|---|---|---|
| 1 | `POST /api/cluster/review/create` | 仅 master 机器（已有） | 403 |
| 2 | `POST /api/cluster/review/vote` | 仅具投票资格节点机器（已有：PC+CUDA）+ 值校验 + 工单存在 | 403 / 400 / 404 |
| 3 | `GET /api/cluster/review/tickets` | **开放**（网络信任域内） | — |
| 4 | `GET /api/cluster/review/tickets/{id}` | **开放** + 工单不存在 | 404 |
| 5 | `GET /api/cluster/review/can-vote` | 开放自查询（现状合理，无敏感信息） | — |
| 6 | `POST /api/cluster/review/expire-check` | **仅 master**（新增） | 403 |
| 7 | `DELETE /api/cluster/review/tickets/{id}` | **仅 master**（新增） | 403 |
| 8 | `DELETE /api/cluster/review/tickets` | **仅 master**（新增） | 403 |
| 9 | `POST /api/cluster/review/mail-poll` | **仅 master**（新增；真实 IMAP 拉取 + SMTP 副作用） | 403 |

**策略理由**：
- **读开放（3/4/5）**：已注册节点需要读待审查工单才能投票决策，列表/详情是投票流程的必要输入；工单内容（target_node_id、votes）是集群管理信息，在 Tailscale/受信 CIDR 网络信任域内透明可接受。无节点身份机制下，"开放"的边界就是网络信任域（HTTP 不暴露公网）。
- **写收紧为仅 master（6/7/8/9）**：expire-check（状态变更 + SMTP 通知）、delete（管理操作）、mail-poll（真实 IMAP + 可能触发结果通知）均为副作用/破坏性操作，与 create 的"仅 master"一致；从节点不应能删除工单或触发轮询。
- **vote 保持现状**：资格门（PC + CUDA）已是语义正确的授权。

**实现模式**（与 create 同模式）：
```python
if scheduler._effective_role() != "master":
    raise HTTPException(status_code=403, detail="仅主节点可执行此操作")
```

**验收测试矩阵**（已纳入 `tests/test_api_security_boundaries.py`）：
- 非 master 角色（monkeypatch `scheduler._effective_role` 返回 `"client"`）调用 6/7/8/9 → **403**；
- master 角色调用 6/7/8/9 → 正常执行（expire-check 返回计数、delete 返回 deleted、mail-poll 返回轮询结果）；
- 读端点 3/4/5 不因角色变化而 403（保持开放）；vote 的资格门 403 已覆盖。

**演进边界**：将来若引入请求级节点认证（MF-AUTH 扩展为节点身份），本契约升级为"调用者身份"校验；当前按部署位置约束，测试注明本机角色语义。

### 4.4 修复顺序建议

1. **已完成**：Q1-Q8；Q2 契约（§4.2.1）已实现并回归
2. **已完成 P2-A**：Q10 diffusion upload 安全矩阵、Q12 SQLite fixture 状态隔离、Q15 tcp_comm 鉴权/保留字/广播容错
3. **已完成 P2-B**：Q13 scheduler 自动发现/切换与抢占决策、Q14 EngineHost 真实执行、Q16 Event/就绪信号替换；并修复自动加载 KV 初始化顺序
4. **下一票 P2-C**：Q11 将可替换的私有状态断言迁移至公开快照/返回值；保留 paged_kv_cache、TCP 状态机等合理白盒边界
5. **后续**：Q9 错误码迁移作为独立兼容票；Q17-Q22 按价值择机

## 5. 变更记录

| 日期 | 事件 |
|---|---|
| 2026-08-11 | 建立本文档；登记 13 个 requires_db 处置决策与 8 项薄弱点 |
| 2026-08-11 | 首轮整改完成（§2 全部 8 项：requires_db 改写、Android/前端补测、diffusers fake、WSL 双语义、环境门文档化、db.py 引用清理）；全量 1930 passed / 8 skipped / 0 failed |
| 2026-08-11 | **深度质量复盘**：4 子代理审计（断言/覆盖率/可靠性/非 Python 层），登记 Q1-Q22；增量复核确认 PG 移除后全部发现仍成立；文档重写为持续维护版 |
| 2026-08-12 | 完成 Q1、Q3-Q8；Q2 完成当前安全拒绝策略的 10 项回归并登记授权策略待决。新增 sidecar 目录逃逸防护与无竞态网络 helper；Python 全量 `1957 passed / 8 skipped / 0 failed` |
| 2026-08-12 | **Q2 Review 端点授权契约决策（§4.2.1）**：读端点（tickets 列表/详情/can-vote）保持网络信任域内开放；写端点（expire-check/删除×2/mail-poll）新增"仅 master"403；背景为无请求级节点认证、按本机角色校验；验收矩阵与演进边界随文登记。测试按契约补齐 |
| 2026-08-12 | 完成 P2-A：Q10/Q12/Q15；修复空上传 400、TCPServer `port=0` 和双栈同端口问题；Python 全量 `1969 passed / 8 skipped / 0 failed` |
| 2026-08-12 | 完成 Q2 契约落地与 P2-B：Review 四个副作用端点新增 master 角色门；scheduler 启动发现可取消并补决策表；EngineHost 真实执行 KV/GGUF/worker 门并修复 KV 初始化顺序；跨线程/进程固定等待改为 Event、Barrier 或就绪文件；Python 全量 `1987 passed / 8 skipped / 0 failed` |
