# 测试改进计划

> 状态：规划（待执行，2026-08-11 建立）
>
> 更新日期：2026-08-11
> 适用范围：QLH 测试套件的薄弱点与遗留问题清单，供测试维护者按优先级处理；不改变各专项的验收口径
>
> 上游事实入口：[测试与评判标准](测试与评判标准.md)；总计划：[总体下一步计划](总体下一步计划.md)

## 1. 当前基线（2026-08-11）

- Python 全量：`1908 passed / 26 skipped / 2 failed`（2 failed 为 Windows WSL 无法访问 `/g/...` 的 Linux env-register 环境门，非产品缺陷）
- control-svc `317/317`、gateway `113/113`、前端单元 `30/30`、浏览器 mock `5/5`
- 代码量：功能 99,504 行 / 测试 64,582 行，**测试:功能 ≈ 0.65**（分层见 §3）

## 2. 处置决策：test_db_config.py 的 13 个 requires_db 用例

**决策：改写成 local_store（SQLite）测试，不清理。**

- 现状：`TestClusterConfig`（5）、`TestUserSettings`（4）、`TestDedicatedKeys`（4）标记 `requires_db`/`external`，因 PG 退场（M1.3）`_db_connected()` 恒 False，13 项在本地与 CI 均恒跳过；
- 功能仍真实存在：api_server 的 settings 端点已改走 `local_store`（`get_local_setting/set_local_setting`、`get_local_user_settings/set_local_user_settings`、`get_local_save_history/set_local_save_history`、distributed_inference 开关），这些功能**没有完整的回归保护**；
- `test_local_store.py` 目前仅 1 个 `test_user_settings_round_trip` 覆盖设置读写，**缺失**：通用键值存储（默认值/覆盖/删除）、`save_history` 默认值与写回、`distributed_inference` 默认值与切换；
- 先例：`test_review.py` 的 7 个 DB 用例已改写为 local_store 隔离测试（2026-08-11，monkeypatch `_sqlite_path` 到 tmp_path + 屏蔽邮件通知），跳过数 33→26，改写后 1.42s 全绿。

**实施建议**：
1. 删除 `tests/test_db_config.py` 中 13 个 `requires_db` 用例及 `DB_AVAILABLE`/`requires_db` 辅助（保留 18 个离线用例：配置解析/凭据安全/线程安全）；
2. 在 `tests/test_local_store.py` 增加：
   - `TestLocalSettingStore`：`set_local_setting`/`get_local_setting` 默认值、覆盖、持久化跨连接；
   - 补充 `TestUserSettings`：设置覆盖、跨连接持久化（现有 round-trip 扩展）；
   - 补充开关：`save_history` 默认 true、`set_local_save_history` 写回、`distributed_inference` 默认与切换（对应旧 `TestDedicatedKeys` 语义）；
3. 隔离方式与 `test_local_store.py` 现有 `temp_store_dir` fixture 一致（monkeypatch `_sqlite_path`/`_store_dir`/`_get_store_dir`/`_legacy_store_dir`）。

**验收**：删除 13 个跳过；新增用例全绿；全量 skipped 26 → 13（剩余：diffusers 5、symlink 4、SMTP/真实模型 2、设计性 1、transformers 兼容 1）。

## 3. 测试分布与薄弱点

### 3.1 代码量占比（2026-08-11 实测）

| 层 | 功能 | 测试 | 比值 |
|---|---:|---:|---:|
| Python 后端（src/ vs tests/） | 65,473 | 51,805 | 0.79 |
| control-svc | 14,871 | 8,463 | 0.57 |
| gateway | 1,554 | 2,016 | 1.30（测试多于功能，e2e 为主） |
| frontend | 17,152 | 1,732 | 0.10 |
| Android | 9,403（含 res） | ~0 | ~0 |

### 3.2 薄弱点清单

| # | 问题 | 位置 | 优先级 | 建议 |
|---|---|---|---|---|
| 1 | `test_db_config.py` 13 个 requires_db 恒跳过（PG 退场残留） | `tests/test_db_config.py` | P0 | 改写为 local_store 测试（§2），不清理 |
| 2 | Android 测试几乎为零：UI 重构（Material 3/三屏）后无单元/仪器测试，L0-2 只靠 assemble + 目视 | `android/app/src/test/`、`androidTest/` | P1 | 至少补 ViewModel 纯逻辑单测（会话/设置状态）；仪器测试可缓 |
| 3 | 前端测试占比 0.10：单测仅 5 个 .test.mjs，Playwright 仅 5 个 mock 用例；`diffusion-real.spec.js` 真机门不属常规回归 | `frontend/tests/` | P1 | 新前端功能（认证 UI、模型工作区）优先补 node --test 单测；Playwright mock 覆盖核心流程 |
| 4 | 5 个 `test_sd15_engine.py` 用例在默认 python 环境因缺 `diffusers` 跳过（侧车环境选择） | `tests/test_sd15_engine.py` | P2 | 在 `.venv-packaging-cuda` 下纳入 CI/回归通道，或文档标注必跑环境；不得在无 diffusers 环境宣称 SD 引擎已测 |
| 5 | 4 个 symlink 用例跳过（Windows 未开开发者模式/无管理员权限） | `test_data_retention.py`、`test_model_tools.py` | P2 | 可选：开发者模式开启后本地补跑；非阻塞 |
| 6 | **WSL/Linux 相关 2 fail 根因（当前全量唯一失败）**：`tests/test_env_registration.py` 的两个 Linux 脚本执行用例在**安装了 WSL 的机器**上失败。`shutil.which("bash")` 可能解析到 `C:\Windows\System32\bash.exe`（WSL 启动器，`uname -s`=`Linux`），而 `_shell_path()` 的 `cygpath`/`/g/...` 转换只对 MSYS/Git Bash（`MINGW64_NT`）有效——WSL 挂载点是 `/mnt/g/`，执行 `/g/...` 报 `No such file or directory`。已实测复现：Git Bash 3/3 通过，WSL bash 复现失败 | `tests/test_env_registration.py` | P1 | 修复：bash fixture 先跑 `bash -c "uname -s"` 判定解释器——非 MSYS（`Linux`）时 `pytest.skip` 并注明"Linux 包脚本测试需要 MSYS 路径语义"；或在 `_shell_path` 对 WSL bash 输出 `/mnt/g/` 映射。修复后全量应 0 failed |
| 7 | SMTP 真邮件 1 个、真实模型冒烟 1 个为环境门（需 `QLH_RUN_SMTP_TESTS=1` / `QLH_RUN_REAL_MODEL_SMOKE=1`） | `test_email_notifier.py`、`test_real_model_smoke.py` | P2 | 保持环境门语义，文档注明触发条件 |
| 8 | 已完成项（记录）：`test_review.py` 7 个 DB 用例改写为 local_store（2026-08-11，全量 1836→1874 passed、跳过 33→26）；随后 G4.1/NW1.4 增量至 1908 passed | — | — | 后续同类遗留（旧 db.py 引用）按 §2 模式处理 |
| 9 | 部分旧测试仍引用已退场 `db` 模块（如 `test_db_config.py` 离线段、`test_distributed_inference.py` 的 db 注入） | `tests/` | P3 | 梳理 db.py 引用：生产代码零引用，测试引用逐项改为 local_store 或移除 |

## 4. 处理顺序建议

1. **P0（§2）**：13 个 requires_db 改写为 local_store 测试——半天工作量，收益是跳过数下降 + 设置/开关回归补全；
2. **P1-Android**：补 ViewModel 单测（可在本机 JVM 跑，无需模拟器）；
3. **P1-前端**：为新功能补 node --test 单测；
4. **P2**：SD 引擎用例的 CUDA venv 回归通道；symlink/环境门文档化；
5. **P3**：db.py 测试引用清理（配合 `src/db.py` 后续退役）。

## 5. 变更记录

| 日期 | 事件 |
|---|---|
| 2026-08-11 | 建立本文档；登记 13 个 requires_db 用例处置决策（改写）与 8 项薄弱点 |
