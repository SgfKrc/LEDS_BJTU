# 测试修复票排期（2026-08-17）

> 状态：现行（排期计划；票按顺序执行）
>
> 更新日期：2026-08-17
> 依据：[测试审查报告](测试审查报告.md)（新分类法：修复驱动/生命周期覆盖/价值密度×维护成本）+ 安卓深度审查 §5
>
> 目标：把审查发现拆成可执行、可验收的测试票，明确优先级、工作量与依赖

## 1. 票总览

| 票 | 内容 | 优先级 | 预估 | 依赖 |
|---|---|---|---|---|
| T1 | 补 `6baab87` 推理加载路径回归测试（use_compile 传参/model_loaded 状态/remote-model 缓存超时） | **P0** | 0.5-1 天 | 无 |
| T2 | 补 `9a71ccb` gateway request_id fallback 用例 | **P0** | 0.5 天 | 无 |
| T3 | `scripts/model_tools/` 建测试（gguf_convert 补丁持久化/import_model 子进程 GBK） | P1 | 1 天 | 无 |
| T4 | **安卓 instrumented 12 用例真正跑通**（connectedFullDebugAndroidTest）+ 修阻塞 | **P0-安卓** | 1-2 天 | 模拟器/真机（surface7） |
| T5 | 安卓 instrumented 接入 CI（GitHub Actions emulator + connectedCheck） | P1-安卓 | 1 天 | T4 |
| T6 | 安卓高风险盲区补测：LocalInferenceEngine+JNI 边界（stub/错误路径） | P1-安卓 | 1-2 天 | T4（需 instrumented） |
| T7 | 安卓盲区补测：Room DAO 迁移/CRUD、SettingsDataStore 持久化 | P1-安卓 | 1 天 | T4 |
| T8 | TaskWorkerProtocol validate 分支参数化补齐（ID 正则/capabilities/协商矛盾/digest mismatch/超限） | P1 | 1 天 | 无 |
| T9 | DiffusionUiState 状态矩阵补齐 + `failDiffusion` 残留 imageBytes 语义决策（修正或断言） | P1 | 0.5 天 | 无 |
| T10 | ApiClient 契约补：uploadDiffusionBlob 前置校验（>16MiB）、poll 超时、download 32MiB 限制 | P1 | 0.5 天 | 无 |
| T11 | ModelDownloadContract 边界（非 200/206、Content-Range 非法、区间边界） | P2 | 0.5 天 | 无 |
| T12 | MainViewModelLogic 同构复制治理 + validateChatImageSubmission 分支补齐 | P2 | 0.5-1 天 | 无 |
| T13 | task_graph 系重复 helper 收敛（`_single_stage`/`_make_tiny_model` → tests/helpers/） | P2 | 0.5 天 | 无 |
| T14 | ApiClient body 子串断言升级为完整解析比较 | P2 | 0.5 天 | 无 |
| T15 | `test_scheduler.py`（6521 行）按类域拆分 | P3（可选） | 1 天 | 无 |
| T16 | 88 个"1 次修改"测试文件 2 个发布周期后复评 | P3（定期） | 0.5 天/次 | 时间 |

## 2. 排期建议（依赖顺序）

### 第一批（P0，本周内，无依赖）
```
T1 → T2 → T3（主开发，纯本机）
T4（安卓，需设备/模拟器——与 surface7 修复同步安排）
```
验收：三个裸修复有回归测试；安卓 instrumented 12 例真实跑通（有报告输出）。

### 第二批（P1，下周，多数无依赖可并行）
```
T8、T9、T10（本机，可并行）
T6、T7（安卓，依赖 T4 的设备通道）
T5（CI，依赖 T4）
```
验收：TaskWorkerProtocol validate 分支覆盖 ≥2/3；DiffusionUiState 全矩阵 + 残留语义有明确决策；JNI/Room/DataStore 有首批测试。

### 第三批（P2-P3，择机）
```
T11 → T12 → T13 → T14（本机）
T15（可选，体积管理）
T16（定期复评）
```

## 3. 分配建议

| 归属 | 票 |
|---|---|
| 主开发组 | T1、T2、T3、T8、T9、T10、T11、T12、T13、T14、T15 |
| GPT 组（安卓） | T4、T5、T6、T7（与 A1/B Worker 后续开发同仓） |
| 定期 | T16 |

## 4. 验收口径

- 每票 = 一个 commit（或一组），**先红后绿**（新增测试在修复前应失败/在修复后通过——T1-T3/T9 尤其要求）
- 全量回归：Python 2791 passed / 18 skipped 基线不回退；安卓 JVM 78 例 BUILD SUCCESSFUL
- T4 特例：instrumented 报告目录非空（`android/app/build/outputs/androidTest-results/connected/`）

## 5. 变更记录

| 日期 | 内容 |
|---|---|
| 2026-08-17 | 从测试审查报告拆票 16 张（P0 三张 + 安卓 P0 一张 + P1 七张 + P2-P3 五张）；三批排期与分配建议 |
