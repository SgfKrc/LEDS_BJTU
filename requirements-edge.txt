# QLH Edge L runtime: the small GGUF/llama.cpp profile.
# Keep this file independent from requirements.txt; torch/Transformers are
# intentionally absent from the edge installation.
llama-cpp-python==0.3.35
fastapi==0.141.1
uvicorn[standard]==0.53.0
psutil==7.2.2
httpx==0.28.1
numpy==2.5.3
# TUI 依赖（2026-09-17 裁决：Edge 同装）。Textual 约几 MB，Edge 的
# 「无 torch + venv ≤300MB」边界仍然成立；协议层 src/tui_api.py 仍是纯标准库。
textual>=8,<9
