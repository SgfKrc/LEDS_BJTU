# cmd.exe 对 LF-only 批处理的 for/f 块解析不可靠（实测崩溃），
# 该构建脚本必须始终以 CRLF 行尾存储和检出。
packaging/build-launcher.bat text eol=crlf
# Windows 一键环境配置脚本同样必须 CRLF（否则 cmd 对 UTF-8 REM 行与 && 解析会崩）
setup_all_envs.bat text eol=crlf
