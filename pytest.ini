[pytest]
# 单元测试配置
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    -p no:warnings
# 标记注册
markers =
    slow: 慢速测试（需要数据库连接等）
    requires_db: 需要 PostgreSQL 数据库连接
    requires_gpu: 需要 CUDA GPU
