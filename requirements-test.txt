# Complete test environment: runtime dependencies plus test tooling.
# Install through scripts/setup_test_env.py; never install into system Python.
-r requirements.txt
cryptography>=42.0.0,<47.0.0
jsonschema>=4.23,<5.0
Pillow>=10.0,<13.0
pytest>=8.0,<10
pytest-xdist>=3.6,<4
pytest-timeout>=2.3,<3
textual==8.2.8

# GGUF/llama.cpp 侧的测试需要（`test_llama_*.py`、`test_llama_relay_entry.py`）。
# 此前未声明 ⇒ 这些用例在 `.venv-test` 里**一直 skip**；2026-09-19 补上后真跑通过。
# 注：Windows 上 PyPI 无预编译 wheel 时会走源码构建（需 MSVC Build Tools）。
llama-cpp-python==0.3.35
